{
  "name": "studio-lot-spike",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Isolated visual research spike — isometric movie-studio lot. NOT the Phase 5 build. Presentation layer only; owns no simulation rules.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4317 --strictPort",
    "shots": "node tools/screenshot.mjs"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
