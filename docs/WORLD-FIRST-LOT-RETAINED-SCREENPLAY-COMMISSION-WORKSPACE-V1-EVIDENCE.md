# World-First Lot-Retained Screenplay Commission Workspace V1 Evidence

Status: **IMPLEMENTED, VALIDATED, AND RETAINED ON THE AUTONOMOUS MARATHON BRANCH**

Date: 2026-08-15

Branch: `operation-hollywood-autonomous-marathon`

Frozen contract authority: `57ce4058a122dfdb1443d9607087f30ff472ce48`

Accepted parent / Lot-Retained Package & Greenlight closure:
`910ac51b2c89bc238274db979c75ae379b02f5b9`

Implementation authority: `2ec3b8a3c6451112a423fe3e30f1c9331b831caa`

Closure documentation authority: the documentation checkpoint containing this record; its commit
SHA is intentionally not guessed before that checkpoint exists.

## Keep ruling

World-First Lot-Retained Screenplay Commission Workspace V1 passes its bounded Keep gate.

The accepted ordinary-player loop is:

```text
LIVE LOT → SELECT DEVELOPMENT
→ CANONICAL COMMISSION FORM OPENS OVER THE SAME MOUNTED LOT
→ CHOOSE CONCEPT / WRITER / SHAPE / AUDIENCE PROMISE
→ CANCEL: EXACT SAME LIVE LOT, CANVAS, CAMERA, AND BYTES
→ ACCEPT: EXACT ENGINE SUCCESSOR COMMITS + AUTOSAVES
→ WORKSPACE CLOSES
→ SAME LOT SHOWS SCREENPLAY COMMISSIONED + EXACT WRITER / DUE WEEK / FACILITY / SLOT
→ CONTINUE LIVE LOT PLAY
```

The Writers' Room remains the complete supporting owner for broad Development management, history,
review, and explicit details. The ordinary start-of-filmmaking decision no longer replaces the
player's studio place.

This is a bounded host-continuity and presentation change. It adds no screenplay rule, fee, time
advance, facility, building, writer travel, occupancy, queue, workload, or watched drafting law.

## Canonical form and compatibility

`ScreenplayCommissionForm` is one shared component used by the retained workspace and standalone
Writers' Room. It owns the existing explicit concept, writer, opening, midpoint, ending, audience,
promise, blocker, consequence, and submit UI; it does not fork screenplay legality or defaults into
a second workflow.

The retained path is narrowly gated to the current Operation Hollywood Lot, managed screenplay
mode, the exact active Lot presentation, idle Development attention, canonical `canStart`, and no
conflicting modal or Package owner. Pending screenplay review retains its accepted priority.
Operation-Hollywood-off managed play keeps the standalone Writers' Room route, and legacy mode
keeps standalone Assembly behavior.

The shared standalone form now reports an accepted Engine successor before its owner closes. That
ordering maintenance preserves standalone content and result behavior while making accepted-state
ownership explicit.

## Exact same-mounted world authority

App owns one independent transient commission session with `editing` and `committed` phases. It is
bound to the exact rendered `GameState`, exact Lot `Screen` object, exact opaque Lot presentation
token, monotonic workspace identity, and connected Development opener. It is not serialized and
does not create a second screen, App, Lot, renderer, or canvas.

Across open, cancel, rejection, accepted commit, autosave, close, and witness, browser proof retains:

- one mounted `StudioLotScreen`;
- one `StudioLotView` / Phaser presentation;
- the same canvas DOM node;
- the same Lot `Screen` object and presentation token;
- the same camera and Lot-local presentation memory; and
- one App-owned `GameState` authority tree.

Every Lot-origin navigation callback captures its rendered state, screen, and presentation token.
App independently compares all three with latest authority, so an old Lot callback cannot acquire a
newer Lot merely because the current screen is also `kind: 'lot'`.

The generic `LotRetainedWorkspace` now hosts both Package and Commission containment. One labelled
dialog, one internal scroll owner, inert background, renderer-input suspension, focus trap, Escape
ownership, explicit close, and stable committed-phase focus are shared maintenance law.
Recovery/migration controls join the inert boundary instead of remaining active siblings.

## Exact-once commit, autosave, and receipt

One synchronous App claim makes repeat, double-click, held key, cross-key, virtual-AT, touch,
pointer, compatibility-click, and retained-callback tails stale before a second dispatch.

On Engine rejection, App releases only the current claim. The same form component, complete edited
draft, exact error, state, and save bytes remain present for a legal retry.

On acceptance:

1. App marks the exact session committed;
2. `commissionScriptAction` supplies the sole Engine successor;
3. latest state advances and the established state effect invokes autosave;
4. the committed workspace remains as a bounded noninteractive recording state;
5. a later revalidated effect closes only that exact session; and
6. at most one strict transient receipt reaches the already-mounted Lot.

The pure `acceptedScriptCommissionReceipt` selector accepts only one exact appended Drafting
project with unchanged prior projects and no mutation outside `scriptDevelopment.projects`. It
revalidates same studio/week/tick/RNG/modes, explicit concept/writer/shape/promise, contracted and
available writer, Week+1 due date, deterministic first-free Development & Casting facility/slot,
and unique closed identity. Sparse, duplicate, extra-key, same-title-ambiguous, substituted, or
throwing state returns `null`.

Receipt presentation is optional, not transaction authority. A valid Engine successor still
commits and autosaves when the strict receipt cannot be proven; the workspace closes to neutral
current Lot truth and never guesses a project.

## Exact live-world witness

The already-mounted Lot consumes a valid current receipt once. Development remains the selected
semantic owner and exposes:

- **SCREENPLAY COMMISSIONED**;
- exact screenplay title and writer;
- commissioned and due weeks;
- exact facility; and
- one-based player-facing slot.

The existing stable polite activity channel announces the success once. The committed workspace
is visual-only, avoiding a duplicate live announcement. Invalid, stale, consumed, replaced, or
receipt-free accepted truth produces no substitute witness. No physical Hollywood Writers building,
route, occupancy, travel, or camera target is claimed.

## Replacement, teardown, modal, and focus containment

Whole-studio replacement, accepted load/import, confirmed New Studio, screen drift, Lot unmount,
renderer-boundary replacement, close/reopen, and App teardown invalidate the exact session, claim,
pending close, witness, focus, and activation memory. Rejected imports and declined restarts retain
the current owner.

Package, Commission, and Talent Profile remain mutually exclusive at App authority boundaries.
Covered world, recovery, and migration controls are inert and `aria-hidden`; those properties clear
before exact-opener or stable-heading focus restoration. Scrim gestures do not cancel or scroll the
world. Hidden-tab, blur, pointer-cancel, neighboring-control, and compatibility tails are exact-once
or neutral.

## Real-browser and visual proof

Ordinary Chromium directly played the retained commission at desktop, 960×540, actual browser
page scale 200%, forced colors, reduced motion, and 480×270 CSS pixels at device scale factor 2.
The form remains a bounded right sheet with one scroll owner; header, details, cancel, and submit
remain reachable. Compact presentation may cover most pixels but preserves the exact Lot and canvas
under it.

For the deliberately minimal founded fixture, before/open/cancel/accept remained exactly:

```text
30 display objects
13 dynamic actors
11,096,896 decoded texture bytes
1 persistent renderer draw
```

Lot mount, view constructor/destructor counts, canvas identity, camera, and structural tuple were
unchanged. This tuple is fixture-specific and is not substituted with the richer one-production
42/19 fixture. Structural parity is not GPU/FPS certification.

Retained screenshots:

- `out/world-first-lot-retained-screenplay-commission-v1/01-desktop-form-over-live-world.png`;
- `out/world-first-lot-retained-screenplay-commission-v1/02-cancel-return-same-live-world.png`;
- `out/world-first-lot-retained-screenplay-commission-v1/03-accepted-screenplay-live-world-witness.png`;
- `out/world-first-lot-retained-screenplay-commission-v1/04-commission-960x540-forced-colors.png`;
- `out/world-first-lot-retained-screenplay-commission-v1/05-commission-page-scale-200.png`; and
- `out/world-first-lot-retained-screenplay-commission-v1/06-commission-480x270-dsf2.png`.

All six were independently inspected and accepted. Screenshot 05 is an honest panned reachability
capture at real 200% scale, not a marketing composition.

Product runtime errors, product warnings, and failed requests were clean. Chromium's environment
may emit a WebGL driver-performance diagnostic; the browser harness excludes only that known
non-product diagnostic and does not conceal application warnings.

## Final automated verification

| Gate | Result |
| --- | --- |
| Focused Commission unit authority | **PASS — 33/33** |
| Complete UI suite | **PASS — 113/113 files, 1,421/1,421 tests** |
| Complete repository suite | **PASS — 200/200 files, 2,651/2,651 tests** |
| Governed D-16/D-17 config | **PASS — 10/10 files, 176/176 tests** |
| Focused Commission Chromium | **PASS — 4/4** |
| Combined Commission + Studio Home + formation Chromium | **PASS — 20 passed / 1 explicit GPU-only skip** |
| Independent renderer rejection/delay Chromium | **PASS — 2/2** |
| Root and UI TypeScript projects | **PASS** |
| Production build | **PASS — 151 modules transformed; existing large-chunk advisory retained** |
| Direct Engine action / SaveFileV11 parity | **PASS — exact bytes** |
| Desktop, compact, actual 200%, forced colors, reduced motion, renderer lifecycle, input tails | **PASS** |
| `git diff --check`, protected refs/paths, screenshots, and independent final audit | **PASS** |

The explicit GPU wall-clock evidence case did not run. No GPU certification is claimed and no
threshold was relaxed.

Implementation `2ec3b8a3c6451112a423fe3e30f1c9331b831caa` changes twenty UI/UI-test files
with 4,409 insertions and 283 deletions. Legacy App test doubles were updated to invoke the real
Lot presentation-mount boundary; this is harness parity, not product behavior.

## Simulation, content, renderer, economy, and publication boundary

No file under `src/core/` changed. No GameState, SaveFileV1–V11, schema, migration, import/export,
screenplay legality, capacity, writer availability, reservation, timing, assessment, package,
casting, greenlight, production, release, construction, publicity, employment, tick, RNG, cash,
ledger, awareness/reach, economy, or outcome law changed.

No authored/generated art, manifest, exporter, atlas, texture, Place, building, facility, route,
actor, animation, draw owner, renderer clock, or district geometry changed. The exporter was not
run. V1 adds no writer travel, arrival, personal location, occupancy, queue, workload, task,
relationship, fatigue, stress, autonomy, pathfinding, character control, or second simulation.

Protected refs remain exact:

- `main`: `33eb33ae307904aa3f00db20bc695e40bf46d1e4`;
- accepted D-17B: `35d42687a410a621becf1df35c75986657f8c44e`; and
- Operation Hollywood bridge: `623b8b2a80e9c6b85304eaa2a338b6045e8f6b21`.

The governing result remains exactly:

> **D-17B ACCEPTED — BOUNDED REPAIR, MACROECONOMY RESIDUALS REMAIN OPEN**

Cash runaway, top-studio economic immortality, the week-208 synchronized roster wall, P5
dominance, world-led variance, cheap-film purpose, premium-film purpose, remaining menu breadth,
and formal G12 timing remain open. This milestone certifies no complete macroeconomic balance and
authorizes no financing, loans, bailouts, restructuring, failure ladder, hard bankruptcy, or
arbitrary cash sink.

Nothing was merged to `main`, pushed, tagged, or published.
