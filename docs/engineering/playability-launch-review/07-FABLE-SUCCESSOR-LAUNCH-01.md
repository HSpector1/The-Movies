# Current Ops — Fable successor startup

**2026-09-14 · OPS-FABLE-SUCCESSOR-START-20260914-01**

**OUTGOING ADOPTION / YIELD ACCEPTED. FRESH SUCCESSOR START AUTHORIZED FOR HANDOFF ACCEPTANCE AND BOUNDED READINESS WORK.** This is an amendment to the existing handoff, not another implementation team, a budget reset, whole-overhaul acceptance or automatic native authority. No session is started by publication.

## 1. Transfer and source identities

The [outgoing receipt](https://github.com/HSpector1/project-studio-unity-visual-spike/blob/3e04634d41c243d548317b863950e4e5f4f59b26/docs/evidence/playability-delivery-20260913-01/FABLE-LOCAL-TRANSFER-RECEIPT-01.md) records writer and native-input yield at **2026-09-14T15:27:03.274046+00:00**, with only the administrative publication tail retained. The final transmittal reports that tail complete. Do not request another outgoing yield, restart the prior worker or re-run configuration benchmarking. The successor must record its own acceptance; nobody signs it in advance.

| Identity | Governing value |
| --- | --- |
| Verified configuration source | The-Movies `225845395f85f3e47d398687953931556a18c16b` |
| Actual configuration adoption | `a51ebff89edb0f1830adf090e4673947954f7acb` |
| Narrow instruction preamble | `0fbb9fce30bf663761776711c3a7d836c3e6a6ea` |
| Actual coordination recovery HEAD / updated handoff | `7dfe508bbf6b07d87811be2c48728703e01c0ce0`, `wip/playability-interaction-01-ts` |
| Reviewed gameplay TS / Unity | `6e2c2ca10ce70f43f58e99193e052d4afa59bcfb` / `e8c59d8672b6e32de73ed10628abeff49bc1a95e` |
| Reviewed player / contracts | Build46; Save V20, protocol 4, projection 30; DTO blob `9420d5ef5e5b7db5d9c18d2300f22a3d94074eb2` |

The [updated existing06](https://github.com/HSpector1/The-Movies/blob/7dfe508bbf6b07d87811be2c48728703e01c0ce0/docs/engineering/playability-launch-review/06-FABLE-ADOPTION-AND-FRESH-SESSION-HANDOFF.md) controls over its preserved historical body. Current Ops checked the TS/Unity refs and the 13-path configuration/instruction/handoff diff: no gameplay paths changed in that adoption. This does not independently rehash the withheld player or verify a live Mac process. Preserve newer lawful work if the local state has advanced; never reset to these pins.

P13A remains the accepted product; Build46 remains a qualified engineering checkpoint. UX-STALE-NATIVE-01 remains unpassed and nonblocking for this handoff, not a reason for another refusal race hunt.

## 2. Permission compatibility resolved

Use **`--permission-mode manual`** for the recorded Claude Code 2.1.270 installation. This explicitly replaces the earlier launch spelling, not its permission safeguards. The captured installed help advertises `manual`. Current official documentation explains that Manual is the user-facing name, `default` remains the configuration value, and `manual` is its CLI alias from v2.1.200 onward. Thus no agent-profile edit or installation is needed merely because CLI help uses Manual; retain the six adopted `permissionMode: default` fields.

References checked 2026-09-14: https://code.claude.com/docs/en/permission-modes (Available modes), https://code.claude.com/docs/en/cli-reference, https://code.claude.com/docs/en/sub-agents and https://code.claude.com/docs/en/settings. The semantic mapping is documentation-supported; successful parsing and effective mode are still observations the actual successor must record. Existing allow/ask/deny rules still matter. A skipped dangerous-mode warning preference is not permission to run bypass mode.

Do not omit the mode flag and rely on an account-dependent starting default. No auto, acceptEdits, dontAsk, bypass, bare/safe mode, global settings change or silent client/model substitution is authorized by this correction. Hooks remain inactive.

## 3. Exact fresh-session launch

Open a **new interactive terminal**. Do not resume the outgoing task or use automatic worktree creation.

```bash
cd "/Users/bruce/The Movies - Playability Interaction TS" &&
/Users/bruce/.local/bin/claude --model opus --permission-mode manual \
  --settings '{"disableAllHooks":true}' \
  --add-dir "/Users/bruce/The Movies - Playability Interaction Unity"
```

Fable is the coordinator role, not the model name. Opus is the requested coordinator alias, not independently verified runtime identity. Six specialists keep their adopted model/tool definitions. File access outside the named worktrees in inherited settings does not expand this task. The approved existing source-only design archive root is `/Users/bruce/Desktop/Fable-Verified-Sources-20260914-01`; reading its verified documents/previews is allowed through normal permissions, not permission to traverse other Desktop contents or campaigns.

Read this supplied launch note and `docs/operations/fable-team/FABLE-COORDINATOR.md` first; then SOURCE-INDEX, the updated local06 header, existing plan 00 → 05 → issued04 → needed companion sections and the supplied transfer receipt. Use the already materialized authority/design archives where those planning files are not on the implementation branch. Do not merge the planning branch into gameplay to obtain documents.

First observe the actual Manual mode, hooks state, current worktrees and absence of a competing writer. Accept the outgoing transfer in the existing local continuation record, including the time and scoped role. Fable becomes the sole coordinator/integration owner and custodian of the yielded native-input slot; **custody is not permission to take desktop input now**.

Confirm the six custom roles through the fresh runtime registry before their delegation: contract-auditor, instrumentation, sim-core, test-author, uiux-designer, unity-ui. File presence and the background `claude agents` listing are not that proof. A registry failure blocks affected delegation, not independent coordinator reading. If effective model metadata is absent, record CONFIGURED ALIAS / ACTUAL MODEL NOT EXPOSED. No new smoke fleet or private-log mining.

## 4. Authorized first work and stop

Continue the existing task board/coverage register. Reuse completed transfer/source checks. The existing06 assignments F-A, F-B and F-C are released **in their prepared READ_ONLY modes only**: transfer/authority check, remaining employee-rail/Back integration scout, and connected native proof plan. Do not repeat established answers merely to use a role. Maximum two specialists concurrently; A/B may overlap on frozen inputs, C follows B. No redelegation. Existing designer retains design ownership; missing views/assets are named deliveries, not permission for a competing designer.

The total startup/readiness interval, including coordinator reading, acceptance, all specialist work and persistence, is capped at **150 productive minutes**, inside existing allowance: at most 60 minutes charged to source/preparation capability and 90 to verification/administration. The individual F-A/F-B/F-C maxima in06 still apply; these are ceilings within this total, not additional hours. Use less when prior evidence suffices. Parent writes are limited to the existing handoff/board/coverage/evidence records. No production edits, new tests, builds, benchmarks, gameplay/native launches, asset generation or global/config changes. Existing prototype/previews may be opened read-only with available tools, without installs, a bridge connection or new mockups.

Update actual remaining screen/state work, legal drag-route dependencies, contextual help, attention versus retrievable outcomes, complete scaling, remaining family treatments and art. Preserve R3 HYBRID and 1A/2B/3A/4B, every required domain, corrected advisory withdrawals and current command authority. Record design/native/unknown evidence honestly. Propose one minimal R3-N1 implementation task with exact writable paths, required design/assets, tests and realistic remaining effort. Runtime/capability continuation remains held for Current Ops disposition of this scope/budget reconciliation; do not treat an old conditional order or a tool permission as an automatic release.

## 5. Accounting without a new clock

Latest user-reported final tail: adoption 15.60 minutes. Known R3 remainder is approximately **48.4889 capability hours and 17.462 reserve hours**; original-envelope reserve approximately5.462h. The receipt's earlier snapshot is superseded for the final tail, not rewritten as a measurement it never made. Charge new work once; source/evidence hour units and specialist time must remain explicit.

Unreported setup/smoke/helper time is **unattributed, not zero**. Inspect the existing small receipts/ledger, not private session logs or a new audit campaign. Record actual evidence where available; distinguish any estimate or unresolved interval. Do not subtract work already charged or invent precise timestamps. This unknown does not prevent the bounded readiness interval authorized here, but the displayed balance remains provisional and cannot support a final full-overhaul fit claim until attributed or explicitly dispositioned.

The below18-reserve escalation remains active. Preserve the last six reserve hours for reverify/package/delivery. Do not use reserve to finish missing features. Price the full remainder by stage; return a reasoned revised ceiling/staging proposal if it cannot fit. Do not shrink art, dragging, help or screens to make arithmetic fit.

## 6. Required return and first future native task

Return one compact successor-acceptance/registration/mode record, the updated existing coverage/usage record, any unresolved local gates and one costed next task. A small sanitized continuation may be committed/pushed on the owned branch after checking only those documentation paths; no PR, merge, protected ref or full archive regeneration. Private session identifiers stay local.

First future native task remains **R3-N1**: employees left, hybrid pictures right, useful center; begin with both lists scrolled; inspect an exact employee and Back; inspect an actionable picture then its worksite/company person and Back; inspect a genuine waiting picture. Preserve both offsets, filters/search and invoking focus and keep global tools usable. A strip or main-screen screenshot is not the complete overhaul. Actual input needs a later admitted build/fixture and explicitly available desktop under the unchanged guard.

Program stays full UI/UX → remaining P13B (all eight Ready obligations) → P14 → P15 → P16 through separate readiness/order gates. No P15D purchases, autonomous acquired labels or withdrawn 14-week estate bound are resurrected. Later-phase questions do not block this current readiness task. No replacement session has been started or accepted by Current Ops publishing this note.
