# Current Ops — Build49 native review and R3-N1 correction

**2026-09-15 · OPS-R3-N1-NATIVE-CORRECTION-20260915-01**

**KEEP BUILD49 AS A RECOVERABLE PARTIAL. CORRECT F7–F15 AND REVERIFY WITHIN THE EXISTING N1 ALLOWANCE.** This is continuation of the same N1 assignment, not a new phase, a budget reset, Owner acceptance or P13B authorization. Same coordinator, production/test/design owners and worktrees. No worker or native session starts merely because this is published.

## 1. Reviewed record, identities and evidence boundary

Read the existing C1–C7 header at The-Movies `ccc49ae9e98f6ef255d495546581692d92064e26`, `docs/engineering/playability-launch-review/06-FABLE-ADOPTION-AND-FRESH-SESSION-HANDOFF.md`. This amendment follows issued08 and continuation09 (`3b64da61d373b7e0af5945189ce45a656d6b034f`); preserve their full outcome, shared input/data authority, fixture buffer and protections. The new order is the only new required document in this packet. Do not re-read every historical handoff layer or rerun setup/reconnaissance.

| Identity | Meaning |
|---|---|
| TS coordination/record | `ccc49ae9e98f6ef255d495546581692d92064e26`, existing `wip/playability-interaction-01-ts` |
| Build49 TS source pin | `2f16c22e` in the source record; resolve its full SHA from the actual seal. Production engine remains unchanged; the later record is not a new build |
| Unity source | `18b893a69152b5305e6ecc7f67c077a15bd86508`, existing `wip/playability-interaction-01-client` |
| Reported player | Build49, executable SHA-256 `5374226871edfb14e770d4bdbb92ad26e75ddaeb5b62fb9a405e63a1652351cd`; seal44/admission40 |
| Contracts | Save V20 / protocol4 / projection30, common DTO blob `9420d5ef5e5b7db5d9c18d2300f22a3d94074eb2`; no wire change is authorized |

Current Ops read the C record, prior continuation, working refs, TS changed-path comparison and `tests/r3n1-stale-schedule-take.test.ts`. The rendered 202/202 and native outcomes are reviewed as reported evidence with named local runs, not rerun or independently decoded here. The record's native findings are sufficient to authorize correction; no need to republish thousands of old files first.

The reported 836/716px center and connected native inspections are progress beyond Build47's paper geometry. They do not close F7–F15, the 1280×720/200% fallback, pictures overflow or Owner acceptance. Preserve Build49 source/manifest/evidence, the Build46 sealed package, P13A, previous failures and all Owner campaigns. Normal local build output may advance only with its new manifest; do not overwrite an immutable delivered package. At the next normal handoff update, describe Build46 as **Current Ops-qualified engineering checkpoint**, not Owner-accepted; P13A remains the accepted product.

## 2. Correct shared causes, retaining every defect ID

These are required N1 behaviors, not optional later S2 work. Fable may iterate on the fixes without requesting permission for each button or regression. It coordinates/reviews; registered unity-ui owns production edits and test-author independently owns assigned tests. Keep at most two specialists concurrently on disjoint paths, one production writer, one integration owner and one native-input owner. Sequence rendered Unity tasks around the project lock. Existing registered design continuation owner handles only the necessary responsive-sheet adjustments; no new mockup campaign.

| Finding | Required outcome and regression |
|---|---|
| **F7 + F12 — one Escape answers at multiple layers** | Give the active text/Find, inspector, workspace and Menu routes one shared ordering/consumption rule, including polled keyboard consumers. One press closes/cancels only its applicable top layer; closing the overlay or Find must not also open Menu. A later deliberate press may act on the newly exposed layer. Do not solve this by disabling Menu or camera keys globally. Cover pointer-opened and keyboard-opened routes. |
| **F8 — Profile excursion loses employee scroll** | Overlay → exact full Profile → return restores both rails' saved offsets, filters/search, selection and invoking focus. Keep legitimate session replacement/missing-ID/content-shrink rules; do not reset to zero or select a neighbor as a workaround. Include the existing successful Production excursion as a regression. |
| **F9 + F11 — focus does not follow the active control** | Opening Find visibly and actually focuses its field. Text goes to that field, not the camera. Leaving a field through Tab releases the actual text-input owner so row arrows/Enter operate the focused list. Keep logical focus and GUI text ownership consistent in the same route. An Escape workaround is not a passing Tab transition. Test typed input as well as the focus ring. |
| **F10 — selected lot target makes rail input dead** | After lawful Locate/world selection, one employee/picture row activation opens the exact intended inspection. Preserve explicit camera Locate semantics and world selection as appropriate; the user must not clear the lot selection first to revive the rail. No click-through or duplicate dispatch. |
| **F13 — 200% rail chrome/card overlap** | Reflow header/count/auxiliary controls and image/text as a responsive layout. Stack image above text where necessary; allow sensible wrapping and vertical scrolling. Keep headers, stage, title, state and Locate separately readable/reachable. Reduce wasted chrome without removing controls. Do not shrink requested text, widen both rails until the lot disappears, substitute a mouse-only tooltip for essential text, or declare unmeasured XAG compliance. |
| **F14 — header consumes overlay; controls off-screen** | Constrain header/footer against the actual available rect and give the content a usable scroll region. Keep Open/Locate/Back reachable and mapped to their real rendered positions. No negative/offscreen action bounds or one-pixel body. Cover long/multiline titles and person/picture/script variants at both supported viewports and all text sizes. |
| **F15 — inspection text draws directly on scenery** | Apply the selected opaque/readable body backing throughout the scroll viewport, with correct clipping and header/footer separation. Verify it over differing lot backgrounds and while scrolling, not only an empty panel screenshot. |

Inspect related consumers of the same input/return/layout cause. Add regressions that fail on the broken candidate and pass after correction where practical. Preserve failed test/native evidence. Fix genuinely stale test assumptions by deriving the intended behavior from the current selected contract, not weakening visibility, identity, input or readability expectations to match faulty output.

The allowed production scope includes the relevant existing `StudioInputFocusGate`, `StudioRailReturnContracts`, `StudioRailReturnContext`, `StudioLaneInspectorHud`, employee/picture rails, camera-input gate, Profile/workspace return callers and **the narrow polled Escape handling in `StudioSystemMenuHud`**. That last consumer is explicitly in scope although unchanged in Build49. Record exact files before editing; use current owners and minimal client-only helpers, not a replacement navigation framework. No TS gameplay, wire, save, price, employment or command-law edits.

## 3. The small/enlarged fallback is not silently accepted

The design sheet's declared full-screen fallback at **1280×720/200%** is an honest implementation limitation, not a Current Ops exception to connected compact inspection. It stays open for N1 until corrected or explicitly dispositioned. Do not raise the minimum viewport or disable 200% to erase it.

Apply the shared header/chrome/body reflow fixes first and have the same design owner supply only the necessary responsive revision where the sheet conflicts with the outcome. Keep both lists' context and required global access; do not declare rails “preserved” merely because hidden state survived. Independent F7–F15 source correction need not wait on that one cell. If a working compact solution still cannot fit the authorized pass, return the exact remaining limitation and a concrete alternative at review. Do not substitute a full-screen fallback and report N1 complete.

## 4. DATA-1 and stale-route fixture — one bounded managed-mode attempt

Authorize one additive generator/provenance extension in the existing owned TS test/evidence scope, **up to one capability hour**, to address the seed limitation. Prefer an existing validated managed-mode or quiescent generated source; otherwise use the supported fresh-game/early legal activation route. Do not force `activateScriptDevelopment` while a production is active, flip state fields, inject cash, relax capacity, or alter the sealed r3n1-dense-01 fixture.

Keep continuation09's four-week fixture-only commitments buffer and nonnegative cash checks. Use only existing legal actions; record source/seed, commands, finance/capacity checkpoints, actual script/production counts, mode and rendered filter. Target sufficient lawful card overflow, not 40/20. Scripts may count as script cards where the UI genuinely displays them; they must not be relabelled filming productions. Reach a real currently offered production decision through lawful prerequisites where feasible. Different immutable fixtures may prove density, ordinary waiting and action safety; do not pretend all occurred in one campaign or uninterrupted run.

The current TS stale test explicitly uses the **nearest offered `resolveProductionBlocker` decision** because the literal take-readiness helper throws. It proves command-owner stale-revision/unavailable-intent refusal and fresh-command idempotence, not the exact native Schedule-take route. Preserve that distinction. Do not repair unrelated `_m5Fixtures` or launch a general fixture rebuild merely to obtain the old named scenario.

For UX-STALE-NATIVE-01, attempt the already-disposed **visible invalidation/required fresh review OR genuine lawful native refusal**, linked to the applicable command-owner test. No race hunt, API injection into a native run or weakened input guard. Reading fresh data on reopen alone is not the whole unsafe-action prevention proof. If the bounded generator cannot enable the route, stop it with exact evidence; keep the residual unpassed and continue unrelated UI correction. Synthetic layout snapshots remain component evidence only. A limitation of this legacy seed is not proof the entire game cannot support more pictures.

## 5. Verification and native admission

Use targeted tests while correcting, then the applicable current rendered PlayMode suite and a source-bound native regression of the failed routes/settings. The reported 202/202 baseline is useful but did not prevent these nine user-facing defects; do not substitute another green total for exercising them.

Keep an F7–F15 matrix: original failing run/step/setting, relevant source fix, independently derived regression, new native action/capture and outcome. Verify the shared transitions together: Find/type/Tab/arrow/Enter/Escape; selected worksite → rail; inspector → Profile → Back; long text at 200%; controls and opaque scroll body. Include neighbor routes that already worked. Reuse unaffected evidence with honest source applicability rather than rerunning every historical suite or decode. No blanket claim about a physically untested device or unmeasured text/viewport.

Native use requires the bound updated TS/Unity/build/asset identities, legal disposable fixtures, single input owner and unchanged guard. Use an existing desktop permission only while it is still current for this work; a past availability statement or this publication is not perpetual desktop access. If availability has ended, source and non-native work continue; return only the native hold. Do not take foreground input to obtain permission.

The published driver’s `text` helper only handles its admitted campaign-name input. For rail text regressions, use supported guarded key events where sufficient. If complete Find/search typing cannot be exercised that way, a minimal **test-driver-only extension for currently rendered/focused named rail fields** is allowed after the independent test owner reviews its exact target/focus/admission checks. No raw-coordinate/API bypass, arbitrary OS typing or changes to `hid-guard`, witness or admission policy; record tool hashes separately from player hashes. This does not authorize full driver redevelopment.

Preserve failed runs, live-input/receipt distinctions, no ignored/duplicate effects, and applicable incremental/whole-pass performance obligations. A 30-second frame sample is not a newly measured ACK distribution or matched Save/load pass. GUI automation remains subject to the same desktop and ownership boundary.

## 6. Budget — same increment, no automatic next phase

At the C5 snapshot: N1 capability **160.600 min used / 1,039.400 min remaining** under 20h; N1 verification/correction/delivery **217.550 min used / 142.450 min remaining** under 6h. Deduct the unrecorded publication tail and subsequent work. Whole-program known balances are **45.729 capability / 13.652 reserve hours**, provisional; the last six whole-program reserve hours remain protected.

Use the proposed **about four implementation hours, at most one fixture hour, and about 1.5 verification hours** as planning estimates inside those balances, not additional grants or deadlines. Complete still-missing N1 behavior within its capability allocation; charge actual verification, corrections and delivery consistently with the ledger. Do not move correction/reviewer time into capability merely to evade the six-hour reserve ceiling. Preserve the separate lead/session and specialist-counter qualification; no retrospective accounting research campaign.

At 90 additional verification minutes, check the remaining failing cells and estimated closure against the then-remaining N1 reserve before scheduling another run. Stop affected work before exceeding the smaller of N1 or whole-program remaining allowance. Existing capability 12/16/20h checkpoints still apply cumulatively. No new 72/36 or 155-hour program is activated. Unattributed setup/smoke time remains NOT REPORTED, not zero.

Do not mark still-defective shared input/reflow work complete in the later-stage deductions. Carry the full remaining screens/workflows, portrait/icon/font/stage art, lawful dragging, help/history and text acceptance forward. A cost estimate is not acceptance and lower actual elapsed work is not proof of finished quality.

## 7. Return and stop

Return the corrected connected native candidate and compact defect matrix, or a precise partial at an actual budget/access/design/data stop. Include exact source/build/seal/fixture identities, test discovered/executed/passed/failed/skipped counts, source applicability, native settings and key captures, residuals, costs and ownership. Only the currently scoped work is authorized; no need for another launch prompt for routine F7–F15 fixes or their regressions.

Publish the next small review index plus new essential text evidence and representative failing/fixed captures on the existing private Unity review branch under a new `docs/evidence/r3n1-native-correction-20260915-01/` folder. Link the existing TS handoff/design and prior evidence at pinned commits. Do not copy all historical archives, credentials, live campaigns/save payloads, font files, app bundles or raw movies. Preserve local originals; record sanitization and withheld evidence. Normal non-force publication of owned WIP branches remains allowed; no PR, merge or protected-ref promotion.

N1 completion requires the selected connected experience, not just closing nine issue labels. No new coordinator, setup/model benchmark, competing designer, hooks/goal activation, paid resources, global settings change, P13A reopening or P13B/P14/P15/P16 work. Owner acceptance remains separate. Fable stays the integration/native-input owner; actual new input still requires admission.
