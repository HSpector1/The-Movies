# P14B.4 — cast-class promises (P2), bounded execution expansion

Status: DRAFT prepared during frozen B-F2 full verification on89b5, 2026-09-19.
NOT source-writer release, not T0 completion, not audited acceptance. B-F2's full
run remains the sole heavy runtime. This expansion turns the bounded preparation
in evidence/p14b3-20260919/15-p2-refined-execution-map.md into the next executable
slice; it does not reopen B1/B2/B3 or claim the rest of P14 complete.

## Owner ruling 2026-09-21 (record 600) — amendments in force

The Owner ruled on record 578 (D1 537 §3, D2 538-C §2, 515 §6, the 576 bound); the ruling
text is verbatim in `evidence/p14b4-20260919/600-owner-ruling-578-reopened-program.md`.
Every paragraph below this section stays as written for provenance; where this section and
a later paragraph disagree, this section governs. Reviewed-plan SHA256 before this amendment:
`382252e23b6353acf602d87f38032ff961e9f7f9740bbfdf2b2ae368c30df4e4` (the pin cited in the
B4 test headers); the amended file's hash is recorded in record 600.

**D1 (a) — evaluator 4 is the class-aware scalar; the certificate is evaluator 5.**
Evaluator 4 = class-restricted fixed-seat paths plus shared residual capacity on the existing
count-family scalar, applied to fresh P1 AND tagged P2 (the "Evaluator4 explicitly applies
shared residual capacity" paragraph stays evaluator-4 law: active reservations are subtracted
before classification, `reserved + X` against the spare-event buffer; some new P1 offers may
become nonofferable). Cast masks: legacy count-only of every family → lead, antagonist,
support; tagged lead → lead; tagged leadOrAntagonist → lead + antagonist. A fixed seat outside
the mask is not an event; IMPOSSIBLE at evaluator 4 comes only from an actual bound (no
mask-qualified filming week inside the window; reserved plus requested exceeding the residual
capacity), never from a search miss. Classless legacy P2 stays nonofferable at a new quote or
freeze with an honest missing-class reason. The bounded joint certificate (the "Bounded
capacity implementation appendix"), the UNCERTIFIED → nonofferable FRAGILE mapping with the
exact explanation `bounded capacity analysis could not certify this schedule`, and the "not a
guessed scalar maximum" requirement are evaluator-5 law: they do not gate `PROMISE_RULES_VERSION
4`, Save30, projection 47 or the live cutover, and the detached adapter (record 553) and
enumerator (record 574) are not wired live at evaluator 4. Record 26 §2 row 3's "reviewed
owner-adapter/class-capacity result" reads, at evaluator 4, as the reviewed class-aware scalar
service. Rules 4 is stamped only by that evaluator change, never as a literal-only bump
(record 23 stands). The move lands INSIDE the coordinated core/save/runtime/wire cutover
(record 26 §2–§4), not as a pre-cutover era. The test-author reconciles the B-F2/B3 rules-3
pins and the kernel-vocabulary capacity cases (separate evaluator-5 file or explicit unreached
marking, never loosened), and holds natural-chain reconciliation authority for residual-buffer
movements of fresh P1 classifications.

**D2 (i-c) — budget fixed.** The 200000 shared work cap and the record 162 §2 native Map/Set
metric stay exactly as adopted. Certified offers exist only where the current kernel fits
(record 538: the fixed-cast 2-week cells and the conflicting-claim case; record 576: unforced
started pictures for windows ending at now + 2). No multi-admission or deferred-admission
grammar is opened. UNCERTIFIED → FRAGILE remains evaluator-5 behaviour. The metric and the cap
are revisited only when evaluator 5 is designed.

**515 §6 (c) — the 302 stale route.** `tests/p14b4-ready-replay-stale-target.test.ts:234`
stays RED as a recorded exact boundary (expected `commandRefused`, receives `workLimit`).
C9 and C10 are not authorized; the 302 control (horizon, route, cap) is not rewritten or
weakened. Revisit only if a future D2 ruling changes the metric or cap.

**576 — fact.** The detached certificate track's bound (record 576) is recorded as measured;
no ruling attaches to it.

**C1 stays closed** (record 599). The computational hypotheses paragraph ("at most 32 relevant
claim rows … 200000 charged primitive search operations") is unchanged.

## Authority, order and ownership

Companion Revision3 at8ef5246a §§4.1–4.5/R12–R13 and the existing logic-first
directive govern. Published August design/Builder Annex at2a7ff0d973391f9433d19ec2cb7f6c5582d1e44f
were read for delegated preference/strategy scope; they do not supply P2 policy
pseudocode. Preserve all recovered commits and all genuine historical artifacts.

Before T1: qualified B-F2 acceptance with exact tested/published SHAs, independent
remote verification; genuine FINAL outgoing SaveV29 nine-case corpus AND genuine
outgoing projection46 runtime checkpoint, independently reviewed and published.
No evaluator1 remint; no version restamping. The final-V29 current-rival focus
patch and separate runtime46 minter are inert preparations, not completed gates.

Native Fable roles only. Parent coordinates and owns serialized runtime/evidence/
publication. test-author independently authors RED and required current-law test
reconciliations. sim-core is the ONE production writer. contract-auditor reviews
bounded contracts/source/qualified evidence. Maximum two active specialists;
one heavy process; no Claude, installation campaign or concurrent heavy tests.

## Deliverable and exclusions

Offer/commit/settle/record P2 with the offered class lead-only or lead-or-antagonist,
counting actual first COMPLETED takes at5→4, never shooting entry/technology lock.
Support qualifies for P1, not either P2 class. Apply the same pure service to both
issuers, preserve B-F2 has-discipline law, B3 atomic/current-proposal rules and
B2 public outcomes/private terms. Count distinct productions, not multiple seats.

This slice includes bounded class-aware capacity, real class evidence/outcomes,
class-sensitive material terms, public preference/D3 disposition, rival P2
authoring and final billing preference, governed Save30/projection47 migration,
and the existing bridge proposal/history surfaces. It adds no native composer.

P3 directing, P4 genre, P5 named-project, waiver, relationships/D5, retirement/
transition/VOIDED remain subsequent scoped work. No employment-offer portfolio
priority, arbitrary beneficiary winner, new admission policy, RNG draw, primary-
profession gate, cast read-model pool expansion, facility owner rewrite, UI/native
verification or Owner acceptance. Calendar/Upcoming has its separate named owners.

## Settled shape and compatibility choices for bounded audit

Mechanical proposed live predicate union:

- Legacy count-only `{count}` for every historical catalogue family, including
  old reader-admitted bound/terminal P2. New P1 continues count-only.
- New P2 `{kind:'castRoleCount', count, seatClass:'lead'|'leadOrAntagonist'}`.
  This tagged shape is legal ONLY with P2; both kind and class are material.

Freeze ProfessionalPromiseV29/GameStateV29 and their strict validation semantics
before widening the live types. Introduce governed SaveFileV30/GameStateV30 and
the next writer/migration/export surface; allocate Save30 only after checking it
remains the next unused version. Do not make V29 accept new predicate fields or
feed a fabricated count-only proxy through frozen V29 to validate a new record.
Reuse a genuinely shared frozen lower-state boundary where appropriate.

V29→V30 validates exact old shape first and copies ALL old roots, versions,
feasibility receipts, progress, evidence refs, outcomes, proposal digests and
market history unchanged. Arbitrary positive root/receipt versions stay legal;
numeric2/3/4 never identifies a new shape. Terminal roots never reclassify on load.
Old bound-open count-only families retain the old generic-cast evidence path;
old CURRENT classless P2 remains unofferable on a NEW freeze, with an honest
missing-class/not-offered reason. Never invent a historical seat class, silently
rename P2 to P1 or fabricate a historical freeze. A genuine new freeze may replace
the live receipt under new law without rewriting the root's historical version.

Preserve old MATERIAL-TERM digest formula EXACTLY for old shape. Tagged P2 adds kind/class to
its material formula. Feasibility receipt inputs must cover all actual class/path/
reservation/availability facts read by the service, and exclude irrelevant cash,
RNG and unrelated campaign history. New evaluations/roots use evaluator4 (rules3
is already B-F2); preserve old versions. No separately persisted chooser version
exists today: document the new future-choice law with evaluator4 and tests, do not
backfill old settled choices or introduce an ungoverned save field.

Lossless V30→V29 only when every record fits frozen V29 without stripping any new
predicate. Explicit refusal otherwise. Preserve existing older downgrade refusals
and migration paths. Consumer sweep includes core index/import/makeSave/version
dispatch, bridge/session.ts, bridge/runtime-checkpoint.ts, bridge/runtime/
campaign-library.ts, UI engine adapter (including both legacy import arms), and
other actual references found by the writer. Updating UI adapter serialization is
not rendered UI work. Historical version-specific tests remain historical.

## Feasibility contract — algorithm appendix still required before writer release

OPEN and (bound OR CURRENT attached), self-excluded, half-open overlapping
reservations stay B3 law. Preserve competing-current cross-issuer treatment as
a separate rule; abandoned roots must not reserve. Same-person sequential holds
and same-studio shared lead/antagonist/support capacities BOTH constrain P2.
Other beneficiaries at another studio do not consume this studio's lead.

Deduplicate inProduction script/production identity. Fixed cast cannot recast.
A first-take record means no NEW event from that picture even while the person
remains held through release. Keep facility release intervals separate from
person holds. Actually started events do not slide forward to a later promise
window. Read actual writing/workflow/setup/scenery/post/availability owners,
not the profession label or a historical duration comment.

One flexible P2 plus one lead-only must fit antagonist+lead where lawful; adding
P1 can use support. Greedy array/ID order must not choose which promise fails.
One beneficiary cannot get two events from two seats in the same picture. Preserve
existing claims' access to existing-pipeline paths per ExampleB: do not move all
old claims into future commissions merely to certify a new draft as achievable.

All three count-family tests remain: spare-event buffer, existing earliest paths,
eight-week slack. A constructive schedule is not by itself a maximum/hard-bound
proof. Minimum countdowns are not certified availability. Prove IMPOSSIBLE from
an actual bound/refusal, never from a heuristic search miss. Huge reader-admitted
counts/windows require deterministic bounded work; no unchecked per-count/per-
week expansion or exponential quote stall. Any incomplete-search disposition
must be explicitly audited as conservative/nonofferable, not false impossibility.

Evaluator4 explicitly applies shared residual capacity to BOTH new P1 evaluations
and tagged P2, not a P2-only loophole. Active reservations are subtracted before
classification per companion4.3.1; the old P1 implementation used gross capacity
for its buffer. This is an observable fresh-evaluation correction: some new P1
offers may become nonofferable. Old roots/receipts/history are never rewritten.

Adopt the bounded-computation extension: UNCERTIFIED maps to nonofferable FRAGILE
with the exact explanation `bounded capacity analysis could not certify this
schedule`. This deliberately extends the mathematical FRAGILE definition: it does
NOT establish X<=Nmax, infeasibility, or an immediate BROKEN cause. Historical
large windows/counts remain valid saves. Ordinary launch fixtures hitting the
analysis cap are a performance/design finding, not license to weaken tests.

The refined solver appendix below and actual owner adapter still require bounded
review BEFORE T1 authoring is finalized. No placeholder matcher is authorized.

## Delegated public preference and strategy hypothesis — proposed disposition

Adopt for this bounded slice, subject to expansion review: use the EXISTING shared
public proven/unproven archetype (real credit or age>=30), never priorityOrder[0].
Unproven prefers a significant cast role; proven prefers any cast appearance.
Expose the actual derived preference on the existing profile/case preference
surface. No new hidden/persisted personality or skill/fame threshold.

D3 match table: any-cast accepts achievable P1 or either tagged P2; significant
accepts either tagged P2, not P1. Untagged legacy P2 receives no invented class.
Mismatch means D3=0, NEVER new illegality or freeze-drop by preference alone.
Re-evaluate public live preference at freeze just as existing preferences do;
age/credit changes do not rewrite terms/history or previous settled choices.

Rival unproven authoring tries flexible P2/count1 over its proposed full term,
then existing P1/count1 fallback; proven retains P1/count1. Same pure service,
attach first achievable candidate only, neither means no attachment. Failed P2
must leave no staged root/reservation before fallback. No automatic lead-only
selection, count escalation, role gate or extra RNG. This is a NEW delegated
content/strategy hypothesis, not an existing taste discovered or Owner-selected
rule. Tradeoff: future choices/demand may change; P1 remains enforceable even
when it no longer earns opportunity for an unproven person. No P3–P5 hierarchy
is implied. Universal P1 would leave P2 rival authorship unfinished.

Final seating preference belongs at the actual chooseIndustryPackage billing
comparison (all six permutations), not only an early actor sort. Membership:
bound OPEN, unmet, same issuer, with a prospective qualifying take inside the
window; never CURRENT unaccepted offers merely because they reserve feasibility
capacity. Derive detached class masks; for one beneficiary with multiple applicable
cast promises use their mask intersection, not duplicate-root weighting. Legacy
bound count-only stays generic cast; new P2 classes remain exact. Benefit unit:
DISTINCT beneficiaries whose assigned role satisfies that mask. Maximize that
count among otherwise legal/affordable/viable packages, then ordinary economic
score, then the existing strict-greater comparison's BILLINGS iteration fallback.
That last rule is inherited strategy tie behavior, NOT a claim of beneficiary
fairness or permutation invariance. It introduces no new person-ID priority and
does not decline an otherwise viable picture merely because two promises tie.

Preserve the exact no-preference path, screenplay-planning call and existing
viability gates. Do not let an infeasible/unaffordable film win because it serves
a promise. The initial cast-selection seam must also include eligible promised
people: permutations cannot introduce someone absent from the three selected at
hollywoodTick.ts162. A bounded promised-person candidate generalization may read
has-discipline, actual employment/busy/participant-exclusion law, but must not
silently broaden ordinary no-promise policy, crew assignment or UI read-model
pools. A current screenplay's writer cannot also act in that same picture; one
person cannot double as its director/craft/cast. Missing lawful complementary
staffing is a capacity constraint, never permission to forge a legal package.
Pin an actual non-primary-actor beneficiary where lawfully constructible and
report any remaining strategy gap rather than claiming authorship alone keeps it.
Test conflicting ordinary score and actual production/take, not just input order.

## Outcome and bridge boundary

Class-qualified progress/evidence must use the actual first-take cast, exact
issuer, half-open window and distinct production IDs. Both classes need lead/
antagonist/support positives/negatives, actual unscheduled5→4 refusal and window
edges. Existing terminal/idempotency and one OWN outcome receipt per promise
remain. A shared take may satisfy several people, never share their outcome IDs.
First-take-then-cancel never un-satisfies.

Review only relevant successful greenlight/cancel seams for immediate studio-
caused permanent path loss. A count-family losing one path while another remains
possible is not automatically BROKEN. A merely FRAGILE remainder is not a proven
impossibility. Joint failure of several promises is NOT proof that a particular
beneficiary's predicate is impossible: either lead claimant could still be kept
when one film remains. CURRENT unaccepted offers are not actual causal holds.
Immediate BROKEN requires TARGET-SPECIFIC hard physical/lawful impossibility after
the studio action, independent of conservative reservation optimization, joint
offer conflicts, preference or analysis limits. Otherwise real takes/due-week
evaluation decide outcomes; never break-all or pick a beneficiary loser from a
portfolio conflict. Preserve early termination attribution, due-week evaluation and
legacy generic-cast compatibility. No generalized lifecycle rewrite in this task.

Extend existing promise draft with explicit offered class for P2; no silent
default. Exact wire validation refuses missing/extra/inapplicable class before
mutation. Map to the tagged core predicate; preserve immutable nested drafts,
class-sensitive intent IDs, revision/cap16/replay rules, whole-quote refusal and
actual current-state revalidation at commit/freeze. No partial proposal/root/cash/
ledger/RNG changes on refusal. Profile/workspace/own proposal/history shows real
class or honest legacy unknown; rival terms including class stay UNKNOWN. Public
outcomes and aggregate trust remain visible without leaking class through copy.

Projection46→47 and exact schema generation via existing generator, protocol4
unchanged. Record outgoing literal584bdd… in runtime prior-schema map AND exact
independent literal-set test; retain every prior pin/current exclusion/unknown
rejection. New genuine46 checkpoint tests preserve each old gameplay slot through
its own V29→V30 migration, reset prior runtime authority only by existing law.
Generated C# is generator-owned; handwritten Unity/native/rendering deferred with
concrete backlog for class selector, public preference, own legacy display and
new schema/save loading. No Owner acceptance assertion.

## Tasks and verification boundary

### Bounded capacity implementation appendix (independent proposal, audit pending)

Use ephemeral complete picture-path alternatives and proof-bearing results, not a
guessed scalar maximum. Input: target, relevant remaining bound/current claims,
existing foreign-person reservation debit, deduplicated paths, actual committed
person/facility holds and named computational limits. No real seat is allocated
by analysis; no beneficiary is awarded employment or named the winner.

Path identities: productionId for started pictures (join any inProduction script),
issuer/projectId for unstarted scripts, actual conceptId for the existing bounded
stock door, planner-local identity for a hypothetical commission. Queued work
refers to the same picture, not an extra event. Each path alternative carries
greenlight/take/person-release boundaries, separate phase resource intervals,
existing-path flag and fixed or lawful possible staffing. Alternatives of one
path are mutually exclusive. Missing schedule/staffing provenance is not an open
seat and must not certify feasibility.

Owner adapter work is real scope, not supplied by the current source: it has no
complete forward enumerator. Read productionPhases/operations/productionSetup/
sceneryLoadIn/scriptDevelopment/employment. Stage/scenery release at wrap even
if Post acquisition blocks; person occupancy lasts through release. ReleaseReady
requires its real action, not automatic release. Player load-in derives actual
geometry/heldSince (1–5 weeks); current rival operateStage clears its abstract
scenery blocker and schedules in the same decision. Do not transplant the
companion's historical three-week rival description as current execution law.
Current first-take timing is anchored at now, not retimed to a future window.

Constraint search selects compatible whole alternatives and lawful cast slots:
lead-only->{lead}, flexible->{lead,antagonist}, P1->{lead,antagonist,support}.
Respect fixed cast, one seat/person/picture, temporal person/writing holds,
separate shared phase capacities and authoritative transition order. Include
ALL relevant prior obligations; never maximize a number of winners by dropping
one inconvenient beneficiary. Remaining-count counters avoid count-object
expansion. Nested class inequalities, fixed-seat/window and temporal bounds are
pruning only, not a complete matching theorem. Keep foreign CURRENT alternatives
under existing conservative debit; do not invent simultaneous foreign contracts.

Prior-path protection hypothesis: solve prior obligations first; maximize their
aggregate existing-path use, then lexicographically maximize aggregate fulfilled
reservation units at successive existing event times. Retain equivalent prior
assignments for the target probe, rather than choosing by beneficiary ID. This
protects ExampleB's ready/drafting seats without introducing portfolio admission.
Exact aggregate optimization is a NEW implementation hypothesis, not specified
pseudocode in ExampleB; independently audit permutation invariance/constructibility.

For a new/unbound target X is its promised count. For an already bound target,
remaining X is count minus ACTUAL class-qualified distinct first takes, clipped
at zero; never blindly use the original total or trust a stale progress counter.
Zero remaining means the predicate is already met, not a failed positive-count
probe. Preserve the real exactly-once outcome owner rather than settle in a read.

For positive remaining count X, the minimum residual capacity satisfying the spare-event
inequality is B=X+ceil(X/3), since N-ceil(N/4)=floor(3N/4). Use two probes under
the SAME prior protection: count probe asks for X target events; achievable probe
asks for B target events with the first X on existing paths and the Xth retaining
the eight-week slack. A joint achievable witness proves all three conditions;
never splice incompatible existing-path and spare-capacity schedules together.

Result proof classes:

- CERTIFIED_ACHIEVABLE: complete lawful joint witness and protected-prior profile.
- PROVEN_FRAGILE: feasible X witness and COMPLETE failed achievable probe.
- PROVEN_IMPOSSIBLE for offer classification: COMPLETE failed joint count probe
  over a complete UNOPTIMIZED lawful domain retaining all relevant obligations,
  or a valid hard upper bound/direct legal refusal. Prior-path optimization alone
  cannot prove this. A target can be physically possible by reallocating a prior
  long-window claim to a later path even when the conservative protection refuses
  that reallocation. A joint failure is not a target-specific causal certificate.
- UNCERTIFIED: domain incompleteness, computational cap or incomplete optimization;
  map only to the explicit conservative FRAGILE extension above.

If the protected count probe fails, separately establish unoptimized joint count
feasibility or a valid hard bound. A lawful unoptimized witness which reallocates
protected prior paths is nonofferable FRAGILE with an explicit prior-path-
protection bottleneck, not false physical impossibility. Incomplete proof remains
UNCERTIFIED. Immediate studio-caused outcome checks use the separate target-
specific physical predicate proof described above, not this offer result alone.

Computational hypotheses for audit/measurement: at most32 relevant claim rows,
64 combined required reservation/probe units,1024 path alternatives, and200000
charged primitive search operations. Span begins at the current authoritative
week and ends at the latest relevant target/claim due-exclusive boundary. Its
ordinary-case allowance must include the owner's maximum proposal-start offset
PLUS maximum legal term (currently12+208=220 weeks), not merely208 from now.
Read those catalogue/renewal constants; do not categorically refuse lawful longest
renewals before freeze. Clip pre-existing holds to the analyzed interval without
inventing their release; release beyond the interval remains occupied throughout
it. Larger historical spans produce truthful UNCERTIFIED, never invalid saves.
Check safe arithmetic/size BEFORE
per-count/per-week expansion; saturating comparisons handle admitted large ints.
Charge/cap preprocessing too. Beyond linear inspection of owner collections,
work is budgeted; no claim the unconstrained search is polynomial. Quote/weekly
cost multiplies by invocations, so actual natural-chain measurement is required.
These are NOT gameplay/save-admission limits or permission to raise test timeouts.

Critical author distinctions: lawful claim/path permutations; flexible+lead;
P1+flexible+lead on one picture; ExampleB; fixed-support exclusion; script/picture
dedup; prior real take with continued occupancy; wrap/Post separate holds; active
drafting versus permanent writer credit; large-count/window quick refusal;
search exhaustion never BROKEN; abandoned/nonoverlapping neutrality; legal
current cross-issuer alternatives; longest-term early renewal; partially fulfilled
bound target remaining demand; joint conflict without target impossibility;
actual buffer/slack boundaries. Preserve the
intentional distinction that planning conservatively adds separate reservations,
while ONE real qualifying take may satisfy multiple separate promises to the
same beneficiary: outcomes must not invent disjoint evidence to match planning.

### Ordered execution

- T0: both accepted outgoing-preservation obligations above, actual artifacts,
  hashes/readbacks/independent KEEP/publication. Fill exact producer/publication
  identities here; do not advance solely because draft files exist.
- T1: independent test-author RED against accepted predecessor. Separate files
  for frozen save/migration and reader-admitted synthetic variants; pure capacity
  and actual first-take outcomes; public preference/D3/rival symmetry; atomic bridge
  and privacy; genuine runtime carry-forward. Missing export/module RED is named
  as such, not claimed as reached assertion coverage. Pin old artifact hashes.
- T2: one sim-core writer implements data/migration/core capacity/outcomes, then
  policy/bridge/generator; tests remain test-author-owned. Any legitimate moved
  current-law premise gets independent explicit reconciliation, never weakened
  validators/assertions/timeouts or silent fixture rewriting. Historical tests
  and every genuine old corpus remain strict.
- T3: serialized targeted groups plus actual natural chains, root/UI and bridge
  typechecks, contract and generated fixtures, bounded source/contract audit.
  Require complete bodies for both classes and real API/settlement/take routes;
  synthetic matcher probes alone cannot close behavioral symmetry.
- T4: published exact tested candidate, fixed-source full core then full bridge;
  exact failure IDs AND diagnostics compared with historical baseline and B-F2.
  Investigate/fix genuine new regressions after freeze, retain raw output, matched
  corrective evidence if needed and independent qualified closeout. All-green is
  not claimed if inherited failures remain. Update progress/backlog/next actions,
  commit/push and independently verify exact remote publication.

Then continue remaining P14B families/waiver/relationships and P14C, P15/P16 and
sufficiently specified later work under existing directive; isolate only genuine
new product decisions. No routine permission pause between settled engineering
tasks. This draft is not a substitute for implementation or completed verification.
