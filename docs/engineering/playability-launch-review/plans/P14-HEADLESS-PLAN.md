# P14 — Contested Talent Market, Bonds and Promises, Career Lifecycle — headless plan (logic-first)

Current B4 checkpoint501 qualified (2026-09-20): original first-take + shared
kernel test NOW GREEN495/500; ONE original stale-after-release234 workLimit remains.
494–499 CLOSED/fixedSource:true; full raw/JSON/hash/nonoverlap verified: rootUI PASS;
Ready17PASS1FAIL, Started27PASS, adjacent+callers203PASS, facts+lookup7PASS=254 passes.
498 bridge sole OLD futureP2kind TS2353 test364; no new compiler error.
492 source5267ff51ba638229306c6861fd965ae2f214529808b145ba9c52cd21ccf71bcb;
tested patch64057c6b7058678d9c0cfc8a711bc5317bc6d1897860cec97b279ac1020a763f.
493 actual-source qualified KEEP. Admission comparison width starts0; three
diagnostic floors retain22 and each new expression is prepaid16. Owners/prices/
caps/identity width22 unchanged. 491 additive refusal control c012c1c6…284733;
corrected baseline passed BEFORE492; original test bytes and21 pins preserved.
500 passive diagnostics CLOSED/removed; EXACT492 source + tested patch restored.
First-take producer190818 versus196468: measured saving5650; original combined
kernel assertion now passes. Not an exhaustive choice or certified-offer claim.
Stale week7 Post3 owner now executes; week8 dimensions finish199381, then
work199996/request15 cuts BEFORE setup collector/Post-exit bill or owner.
No stale command/release parity pass is claimed. No active probe or runtime.
Published501: HEAD/exact remote80aec8b348f7322da552ef3cd35ae9ba671ba488;
502 independently records fullSHA verification;53 files preserve source/evidence.
NOW503 bounded commandDimensions direction: ten actual consumer fields;
keep current full ops/workflow/task copies, binding/reservation strings and
static geometry; omit only unused P/Set/setup/technology/genre discovery.
504 additive genuine duplicate-commit baseline PASS on unchanged492 before
writer (1PASS/4 unselected,12:52:02.515–12:52:10.672Z,fixedSource:true).
503 independent design review qualifiedKEEP;505 source FROZEN/yielded:
5d4851713ff53209b569c813b78a1e22e6b53ab91e27522ceb5a9ee7222a941a;
protected patchb6bcfff304a93651577fa662b12e42e6a6e5ecb2007f65a4fa125371a540aa07.
Parent verified21 current pins and unchanged full dimensions/static/fact/frame
plus workflowUpdate/geometry/arrival executable bodies. RUNTIME TRANSFERRED to
Claude Code 2026-09-20 (Codex usage exhausted 12:58:41Z; Owner packet
CLAUDE-RESUME-P14B4, runtime choice only). 506 Codex auditor Qualified KEEP
RECOVERED verbatim from the local thread log; independent Claude contract-
auditor KEEP, no demonstrated defect (506). Recovery commit
34a8db7b2a82eb9e9a495bb41f8089dcdbffc6d1 = exact505: 80aec8b..34a8db7 source
diff hashes b6bcfff…; empty tree patch during runs. 507–512 CLOSED/fixedSource:
true on34a8db7, disjoint intervals (513): Ready17PASS + sole original stale234
workLimit FAIL, Started28PASS(incl504), rootUI PASS, adjacent203PASS, bridge
sole OLD TS2353 test364, facts+lookup7PASS =255 passes; first-take+kernel GREEN.
514 passive probes CLOSED (both fixedSource:true on1f0b968; exact505 restored
5d4851…, no diagnostic remains): first-take189256 vs190818 = net1562 saved,
GREEN incl. kernel; stale now cuts INSIDE week8 sweepBill at199994/request10
(was199996/15 before setup collector): command discovery2825+2290 vs4542+3176
but week5 dimensions+939 deferred; net1472 at week8 entry. Refusal still
≥week8 remainder + week9 commit + week9 release frame + week10 stale away.
Measured targets: preparation+admission66333, Development/Post frames19–26k.
515 CLOSED: two independent READ-ONLY mirrors (sim-core, instrumentation)
reproduce all seven measured bills/calculator costs; remaining gap LOW31.2k/
LIKELY32.5k/HIGH35k (route≈232k, cut at86%). Lawful-as-written candidates
C1–C7 sum≈23–26k gross < gap; tariff clarifications C9/C10 (+≈9k) marginal.
OWNER/Current Ops DECISION ITEM (non-blocking, 515§6): stale302 route not
shown to fit200000 under176 as written; options a) authorize C9/C10, b) rule
on the control, c) keep RED as exact boundary. ADOPTED next slice C2+C3+C4
exact pre-call bills (retained Development, single Post exit, single-early
occupancy/slot; ≈11.2k gross, no discovery change; stale stays RED after it).
516 independent direction review REFINE→adopted; no new test (started-owner259
weeks=2 covers single Post exit; 507/508 are the baselines). 517 ONE sim-core
writer implemented exact pre-call bills; 518 delta review REFINE (one rT6 walk-
width correction); 526 correction; 527 KEEP. Corrected candidate
d5f8cb7171f602df76b2115819e7e63320b84b4f7e602fb5811ad57984b1eb22 (2797 lines),
protected patchadd13897e0d5346b5e3b81bbc10c840d0114462f1fff521cc0335f6e4e8b51b5,
40 pins unchanged. 528–533 CLOSED/fixedSource:true (535): Ready17PASS + sole
original stale234 workLimit FAIL, Started28PASS, rootUI PASS, adjacent203PASS,
bridge sole OLD TS2353, facts7PASS =255 passes. 534 probes: bills EXACT
315/6861/8099/8578/850/7948/576 + Post-exit5668 (predicted); first-take182925
(505:189256) GREEN incl. kernel; stale now cuts at the week8 owner reservation
197631/request5765 (2369 left), refusal ≈22k LIKELY away; RED preserved.
536 CLOSED/fixedSource:true on69d16f8 (published7680d60): eleven non-replay B4
groups 110PASS/83FAIL (kernel41, material17, D3 8, hold5, sort7 GREEN; live-P2
files RED at the Save29/rules3/grammar46 boundaries). 537 (published73c28e2):
three READ-ONLY reports (sim-core map, test-author classification, contract-
auditor review): 26§1 pure steps already landed (fbe2d28); NO installed RED
stops at a pure boundary; A1 (scalar class+residual evaluator stamped rules4)
NOT LAWFUL as proposed: the plan defines evaluator4 as residual + bounded joint
certificate, and any 3→4 move breaks the B-F2/B3 rules3 pins (test change, not
writer-only). OWNER/Current Ops DECISION ITEMS (537§3, non-blocking): D1
evaluator4 definition/sequencing (a: scalar now, kernel as evaluator5, pins
reconciled inside the coordinated cutover — parent recommends; b: hold 4 for
the kernel service; c: revisit cap/tariffs); D2 kernel budget finding (one Ready
plan =182925 of200000; the achievable probe needs two traces). NO writer.
538 CLOSED/fixedSource:true on79b7a738 (published e3c6dd2): evidence-only
kernel budget measurement on the real capacity fixtures (test-author file,
archived in the 538 patch, not installed): fixed-cast 2-week cells at shooting-5
FIT (started producer108265 + kernel4.2–4.5k ≤112722); every Ready plan on the
history-bearing `opened` fixture cuts workLimit at admission (quadratic ID-
occurrence bill in readyProductionId, ≥148 occurrences at width37); a Ready
trace admits ONE fresh picture, so the B=2 achievable probe is structurally
unreachable. 538-C contract review (published2e908da): the bill is the adopted
162§2 native Map/Set metric (176§1, 320 B-addendum, 285) — a TARIFF item, not a
writer fix; one-admission/source-now is the adopted 240/241 design. D2 made
concrete (538-C §2): options a–d for the metric/cap, direction record for
multi-admission; non-blocking alongside 515§6 and D1. 539 (published1b5ba08):
detached owner-adapter FIRST slice designed (539-A) and reviewed KEEP WITH
REFINEMENTS (539-B): new pure module src/core/promiseCapacityOwners.ts (claims
by the B3 law widened to every same-studio beneficiary, assembly over EXISTING
producer results with derived coverage, digest without preparationWork, kernel
class → offer vocabulary with the exact UNCERTIFIED string, no receipt fields),
plus `export` on promiseCastSlots/qualifyingTakes only; lawful under every D1
option. NEXT540 DONE → 553 QUALIFIED (published e753da2): detached owner-adapter FIRST
slice landed. 540-T RED installed (missing-module RED, 781 lines); 541-W ONE
sim-core writer: new src/core/promiseCapacityOwners.ts (198 lines, SHA2f6af5c7…)
+ `export` on promiseCastSlots/qualifyingTakes (zero body change); protected
patch8910c274…; 542–552 serial fixedSource:true: RED33PASS, Ready17PASS+sole
stale234 FAIL, Started28, rootUI typecheck PASS, adjacent203, bridge sole OLD
TS2353, facts7, live-P2 110/83 byte-identical to536, B1–B3+bridge consumers
132PASS/2FAIL/2todo, historical saves137; 552 proves the two B1/B2 failures
(p14b1-trust-chooser test6 tie winner; p14b2-fixture-preconditions poaching
fixture unbound) are PRE-EXISTING on clean HEAD, not caused by the slice.
553-R contract-auditor KEEP, six record-only items (limits key order; rival
gate before the adapter; named-root tolerance; negative edges; rival tariff
P+A; dangling promiseId). NO receipt/rulesVersion/save/projection/kernel/
producer change; D1 (537§3), D2 (538-C §2), 515§6 stand. 554 (published6d7d75a): the two pre-existing B1/B2 control failures were
ATTRIBUTED by bisect (temporary linked worktree; this HEAD untouched) to the
accepted D3 preference law 956a17f: p14b1-trust-chooser test6 is the DESIGNATED
moved premise of record110 (migrate to real tagged P2 at coordinated
activation; stays FAILING by design — now named in the expected failing set);
the p14b2 poaching fixture went unnoticed at the D3 checkpoint (101 ran two
files) and also broke three bridge-p14b2-trust group5 cases. 556 (published
e33022f): test-author reconciliation under plan T2 — one real 52-week actor
contract gives the player a Reliable record, route pinned by receipt reasons;
557 fixedSource:true: controls 133PASS/1FAIL(test6)/2todo, bridge-p14b2-trust
22PASS, capacity/outcomes failing sets identical to 549. 555 (published
267b7dc): enumerator slice designed (555-A) and reviewed REFINE→adopted
(555-B): a derived fresh-take floor (no unstarted picture can record a first
take before now+5; owner fact of actions/operations/tick) plus the forced-take
condition make the Started-only domain COMPLETE for windows ≤ now+5, so the
13 fixed-cast cells and the conflicting-claim case reach PROVEN_FRAGILE /
PROVEN_IMPOSSIBLE through the adapter without running the Ready producer
(D2 side-stepped for short windows); both certificate flags gated by the
floor; option β (no Ready run; long windows truthfully UNCERTIFIED). NEXT558 DONE → 574 QUALIFIED (published 00fd237c): detached enumerator slice landed.
558-T RED installed (missing-module RED, 775 lines); 560-W ONE sim-core writer: new
src/core/promiseCapacityEnumerator.ts (127 lines, SHAc0be83a9…; FRESH_TAKE_OFFSET 5
derived from the phase owner, FRESH_ADMISSION_OMISSION, earliestStartedTakeWeek,
enumerateOwnerTraces, classifyEnumeratedOffer; not index-exported) + one hunk in the
adapter (5th `enumeration` parameter with the unexported fixed-coverage default,
canonical limits re-literal; SHA5514d847…); 561-T test-author amended the RED only
(two TS7022 annotations; addition (iii) queue built by three real commands, pinned
block unchanged; SHAb9da9b76…); protected patch16e522ed…; 562–573 serial
fixedSource:true: enumerator RED45PASS, first-slice33PASS, Ready17PASS+sole stale234
FAIL, Started28, rootUI typecheck PASS, adjacent203, bridge sole OLD TS2353, facts7,
live-P2 110/83 byte-identical to549/536, B1–B3+bridge consumers 133PASS/1FAIL(test6)/
2todo, bridge-p14b2-trust22, historical saves137; 575 the candidate on the UNAMENDED
RED 44/45 (sole self-declared UNCONSTRUCTIBLE). 574-R contract-auditor KEEP, nine
record-only items (owners header :8-9 stale — doc fix deferred to the next writer
release; insertion range :145-169; saturation boundary label; phase-table vs :1680
literal tie; NOT_ENUMERATED by reference; live-wiring rule never hand-supply an
EnumerationCoverage; (b) needs no r===5; workflowById last-wins; controlling plan
text is P14B4-HEADLESS-PLAN). The 13 fixed-cast cells and the conflicting-claim case
now reach PROVEN_FRAGILE/PROVEN_IMPOSSIBLE through the adapter without the Ready
producer; windows beyond now+5 stay UNCERTIFIED with the fresh-admission omission.
NO receipt/rulesVersion/save/projection/kernel/producer change. NEXT574 DONE → 576 MEASURED (published with this header): the Started-alternative
bill for an UNFORCED started picture (test-author probe 576-M, archived in the 577
record-check patch and deleted; 65 deterministic B4_BUDGET_576 lines): the complete
command-timing set fits 200000 ONLY for windows ending at now+2 (k=2 145876; with
the never-schedule trace 172492; kernel total ≤151479, assumed-complete →
PROVEN_FRAGILE); now+3 (≈253k), now+4 (≈444k) and now+5 are CUT at the second plan
under every order; only partial pairs fit at now+3/now+4; `limits.work` above the cap
is refused by replay and kernel. The parent does NOT open a now+2-only unforced slice
(one omission on one fixture state, no live consumer). The detached certificate track
(539→553→555→574→576) is at its lawful bound: every further certificate needs D1
(537§3), D2 (538-C §2) or an Owner budget/tariff ruling (515§6; C9/C10). NEXT579 DONE → 598 QUALIFIED (published c6f4fc79; C6 landed 3f993b1d, C7 landed
21569c7c): replay bill reductions C6+C7 on src/core/promiseCapacityOwnerReplay.ts
(2809 lines, SHA9e97ffed…; five hunks: copyLiteral/HOLD_COPY 61/FIXED_HOLD_COPY 79,
the two hold-close sites pay 18 + literal instead of 14 + copyCost discovery,
uniqueIds through sortedOutput, one doc word). 580-T RED installed (case 1 facility-
order witness RED on unchanged source, 581 baseline; 582 pre rows: 538 probe 108265/
32043/18754, 514 stale cut at used197631, first-take 182925); 583-W ONE writer, two
patches; 584–596 serial fixedSource:true: RED5PASS (witness delta2420), enumerator45,
first-slice33, Ready17+sole stale234 FAIL, Started28, rootUI typecheck PASS,
adjacent203, bridge sole OLD TS2353, facts7, live-P2 110/83 byte-identical to570/549,
B1–B3+bridge consumers 133PASS/1FAIL(test6)/2todo, bridge-p14b2-trust22, historical
saves137; 597 post rows reconcile with ZERO residual (538 scenario1 108265→105351 =
2349+4+3×187; prepare-only 32043→29694; Ready prepare 18754→16475; stale route cut
197631→194726 same frame/request = 2279+14+153+3×153; first-take 182925→180022;
the 576 bound unchanged, complete sets at now+3/+4/+5 still cut). 598-R contract-
auditor KEEP, ten record-only items (:1991 second sort deferred; annex lots ≈+543,
C7 a founding-lot saving; 466 "exactly four" historical; 538/576/514 rows historical
— cite 597; Ready closes 153 vs Started wrap closes 187; Post exit outside the stale
pre-cut prefix; the S6 assumed-coverage class change is an observation, not a
certificate; RED comment cites +12 stale; comparison flag explained; two-pay
preference not taken). NO cap/tariff/refusal/deadline/timeout/metric/test change;
the stale route stays RED (≈28k gap); 578 Owner packet stands (D1 537§3, D2
538-C §2, 515§6). NEXT598 DONE → 599 RECORDED (published 5c5e9e55; source unchanged at 48a43511): C1
(calculator structure + C5(i) static widths as module-init constants) designed READ-ONLY
(599-A sim-core: 481 calc(k) sites on 282 lines, 244 would fall; L1 bucket right-sizing
+ H1–H7 structurally-zero hunks + C5(i) constants ≈13–15k on the stale prefix, ≈11–12k
first-take, ≈3k on 538 scenario1, ≈0.2k prepare-only; bill VALUES byte-identical; closes
no route, the stale gap stays ≈28k, the 576 bound unchanged) and reviewed (599-B
contract-auditor: KEEP as record, REFINE only, three paper slips D1–D3; Ruling 1
convention A′ at six uncovered helper sites; Ruling 2 the six textual under-reservations
record-only): both recommend NOT opening C1 and the parent adopts it — no RED, no writer,
no re-measurement; the PARALLEL TRACK IS CLOSED with 599 (site table, strict table,
counter and route-model scripts archived by hash). Reopen only on an Owner ruling under
578 that makes ≈13k load-bearing. NEXT599 DONE → 600 RECORDED (published 69083734; source unchanged at 48a43511): the OWNER RULED on
578 — D1 (a): evaluator 4 = class-restricted fixed-seat paths + shared residual capacity on
the existing scalar, for fresh P1 AND tagged P2; the kernel joint certificate and
UNCERTIFIED→FRAGILE move to evaluator 5; implemented INSIDE the coordinated
core/save/runtime/wire cutover (26 §2–§4), never as a partial live change; D2 (i-c): the
200000 cap and the 162 §2 metric stay exactly as adopted, certified offers only where the
current kernel fits, no multi/deferred-admission grammar, revisit at the evaluator-5 design;
515 §6 (c): the 302 stale route stays RED as an exact boundary, C9/C10 NOT authorized, the
control untouched; 576 recorded as a fact; C1 stays closed. Plan amended additively
(`plans/P14B4-HEADLESS-PLAN.md` "Owner ruling 2026-09-21 (record 600)" section; SHA
382252e2… → 28b8fdd6…; earlier line cites n>8 are now n+51). ENGINEERING REOPENED. NEXT600 IN PROGRESS → 601/602 RECORDED (39a64ef0 the 600-T RED + 601 baseline; c9ed1fc4 record
602; source still 48a43511 at 602): 600-T test-author reconciliation installed as RED on
unchanged source (B-F2/B3 fresh pins 3→4, historical 1s untouched; conflicting-claim joint case
moved verbatim to `p14b4-cast-class-capacity-evaluator5` as a LIVE designated failure;
trust-chooser test 6 migrated to a real leadOrAntagonist P2; 601: 25 failed / 20 passed / 3 todo);
600-A sim-core READ-ONLY cutover design note (S1 core → S3 save → S4 load/runtime → S2 policy →
S5 wire + generator; 28 citation drifts vs 537-A, 0 source moves; 73/83 GREEN by paper, 1 by law
to the evaluator-5 file, 9 natural); 600-B contract-auditor RELEASE WITH CONDITIONS C1–C12
(adopted verbatim in 602 R1; fifth root-tsc residual `tests/save.test.ts:229-233`; registry line
and disclosure hunks in S5; D17 drivers in S4; the G9 rival flexible-first movements a NAMED
reconciliation class under plan T2). 600-W the ONE sim-core writer is RELEASED on that design
(cumulative patches per step, never commits; parent lands five commits via commit-tree and pushes
them together). NEXT602 DONE → 616 RECORDED (published 1f6015d0): the COORDINATED CUTOVER IS LANDED AND
QUALIFIED WITH RECORD-ONLY ITEMS (600-R). Identity f2192d8b = writer 47b2bbf4 S1 core (evaluator
4: masks, reserved+X buffer, classless-P2/tagged-non-P2 refusals, full-predicate mint, rules 4)
→ 3b8f5b6a S3 (Save V30 live) → 2cbc3de5 S4 (load/runtime → migrateToV30, V30 checkpoints) →
a64ac00b S2 (rival unproven → flexible P2 then P1) → 027155e7 S5 (projection 47, closed P2 draft
union with required seatClass, nullable seatClass rows, preferredOpportunity, outgoing46
registered, generator run; schemaId 6f6b4880…) + test-author e9f4962b (600-T2: 29→30 values-only
sweep of 100 test files, meaning pins re-expressed from evidence, the G9 class re-expressed; no
pinned winner moved; trust-chooser test 6 GREEN) + f2192d8b (600-T3: eight stale UI 28-pins → 30).
Evidence 601–615: live-P2 83 → 12 (all natural/designated); full core 96 files/312 → 13/39 = 22
inherited + 2 designated + 11 natural + 4 load-only (pass alone); root+UI and bridge tsc EXIT 0
(the OLD TS2353 cleared); UI five-file 45/45. Replay module unchanged (9e97ffed…); stale route
:234 stays RED; no cap/tariff/refusal/timeout/fixture moved; nothing native. Owner-visible
behaviour list and Unity backlog in 616. NEXT616 (1) DONE → 618 RECORDED (published bad20a76): 600-T4 seed-scan reconciliation of the three
natural premises under 600-R Q6 (i)–(iv) — rivalWorlds() on 'seed-b' (SLOTS + GENUINE both met
at w215 by the same search; every other case keeps the default seed), outcomes :438 as a CAST
CHOICE of the fixture's own second bound beneficiary (optional `prefer` on playerPayload,
asserted employed non-focus actor), policy P1fallback witnessed on 'seed-b' w196 (FLEX FRAGILE
"needs a picture not yet commissioned" → P1 ACHIEVABLE, root promise-36) with the default
seed's witnesses still required; nothing invented, no timeout/helper/fixture/source change.
617: the live-P2 set is 1 failed (the evaluator-5 case by law) / 192 passed / 1 todo — the
progression 593 → 607 → 617 is 83 → 12 → 1; the nine rival outcome cases RUN on a recorded seed
(partial evidence under 26 §2, not B4 closeout). Findings: O-T4-1 a natural seating-preference
witness (r01 seats two tagged beneficiaries lead + SUPPORT while a P1 holds antagonist; the
permutation would serve 3/3 masks). NEXT618 (a) DONE → 628 RECORDED (published 2c035de1): the FINAL SEATING PREFERENCE (plan :215-236)
is landed and QUALIFIED WITH RECORD-ONLY ITEMS (619-R). Identity 85d23a8b = 4baa4d7b
(promisedCastMasks reader + chooseIndustryPackage lexicographic (benefit, score) with the
inherited BILLINGS fallback; no-member path byte-identical) → fc1c7b07 (initial-cast seam:
promised members first in employment order, writer/director/craft excluded, masks passed only
when non-empty) → 85d23a8b (619-T2: RED re-pinned from receipts, seam law asserted on every
decision row of both seeds, the positive non-primary-actor seam witness live). RED chain 620
5/15/2 → 622 19/1/2 (only the R2-attributed default-seed digest) → 626 21/0/1. 627 FULL CORE
9 files / 24 failed / 3791 passed / 8 todo = exactly the 22 inherited (since 89b5ad2) + 2
designated (stale route :234; evaluator-5) — the natural-premise and load-only failures of 610
are gone; never cited as all-green. 625: 16 natural-chain control files 177/2 todo, no pinned
control moved. G10 ledger (default seed r01 w208 seats its sole writer r02-0 on promise-12,
film:11 before film:6; seed-b r01 f23 permutation at a 309,545 score cost; bridge seed w211 both
rivals seat their sole writer as antagonist, 1.70M / 1.34M costs, pins held; adoption seed
unchanged). OWNER EXPANSION-REVIEW QUESTIONS recorded in 628, nothing adopted: R5 seam ordering
(keep as landed vs actors-first), G-1(A) rival authoring exclusion for sole crew (later, own RED),
G-2 ordinary-pool retry. NEXT628 (b) DELIVERED — records 629–637 (`e273715d06c2`): breakPromisesOnCancel causal-coupling
correction landed as `61833f0d` (targetSpecificImpossibility hard bound, mask-seat relevance),
QUALIFIED WITH RECORD-ONLY ITEMS. NEXT637 (c) DELIVERED — record 645 (`93beaf9f0d23`): text pass;
first full UI-project baseline 643 (643-C classification; 644 single-worker); B4 LOGIC FAMILIES
CLOSED OUT (616/618/628/637 + text pass); evaluator 5 later (D2 (i-c)).
P14B.5 FIRST SHARED-WORK BOND CORE LANDED AND QUALIFIED WITH RECORD-ONLY ITEMS — record 675
(`0f1d56173246`, 672-R). T0 record 652 (`1117717a`): genuine outgoing Save V30 corpus (ten cases)
+ projection-47 runtime checkpoint minted at `d6c11b9b`, 651-B KEEP. Expansion inserted record 653
(`74bd325b`, plan :725-781) with the 647-B dispositions. T1 RED record 657 (`c32961c6`, 657-B
RELEASE WITH CONDITIONS C1–C10; measured R-D5 ledger: 0 of 96 churn-week survivors hold a roster
at 208, cases open at 404 and settle at 416, exposed rows 0). CANDIDATE `040651b4` = five
bisectable 658-W commits (S1 core root + tail seam + cancel seam; S2 D5 descriptor with the
roster-at-W predicate and nemesisOnRoster; S3 Save V31; S4 load/runtime; S5 projection 48), each
tree byte-equal to its patch; test-side `8e03ff97` (662-T2/T2b sweep, 121 files) and `5f116403`
(668-T3 strip class; 668-B + 672-R: production V31 chain COMPLETE, zero writer defects).
IDENTITIES: Save V31, projection 48, schema
`sha256:00c0075bef257634956da7d16d117a145d203047e7169c643156b7971c4c7fec`, outgoing `6f6b4880…`
registered `projection-v47` (36 prior ids); PROMISE_RULES_VERSION 4 unchanged. EVIDENCE: 669 FULL
CORE 9 files / 24 failed / 3932 passed / 8 todo = the 636 baseline set EXACTLY, +119 passing (the
"+141" in df693294's message is corrected in record 675); 671 controls 324 passed / 4 todo, none
moved; 663 RED 118/118; 670 typechecks + both generator checks EXIT 0; 673 FULL UI 26 failed (≤ the
643 baseline); 674 test:bridge exactly the two inherited files.
NEXT675 (1) DELIVERED — record 679, the §8 :645 week-260 measurement, reviewed in 678-C (QUALIFIED
WITH RECORD-ONLY ITEMS; D1–D5 + REFINE 1–5 applied). Harness `676-b5-week260-probe.ts` (test-author
under 676-T): run 677 on sha `4e7438d4…`, then two FALSE COMMENTS corrected (comments only; the
reversal reproduces the old sha byte for byte) and re-run as 682 on `198337d7…` — 682 REPRODUCES
EVERY MEASURED LINE of 677. Both runs on `ddf58e87`, EXIT 0, `fixedSource: true`, EMPTY tested diff,
node v20.20.2 darwin x64, 17.2 s. Four recorded standard seeds ticked one week at a time to 260, no
player action during the ticks (bootstrap named: generateWorld + economyEngagedEver +
activateStudioOperations + initializeHollywood('fresh')); every tier read through the real
`currentTier`; `validateRelationshipsRoot` + `makeSave` pass on all four final states. MEASURED:
edges@260 27 / 27 / 36 / 24; drivers derived == observed == distinct mint keys (675 / 1341 / 1452 /
1080, 4,548 total, ZERO duplicate mints, so idempotency held past the fold); retained 195 / 195 /
246 / 192; STRAINED NEVER READ on any seed at any week, minimum closeness ever 48; Inseparable
11 / 24 / 27 / 24 at 260, first read w36–w45; drift is PARTIAL (a completed return needs 312 dormant
weeks; the most any edge reaches by 260 is span 200 ≈ 77%) and changes the reading on ONE seed of
four; root 23.8–32.2 KB (~900 B/edge) = 1.34–2.06% of the uncompressed save and 0.47–0.78% gzip-9.
FLOOR MEASURED AT 45 by the witness `681-b5-closeness-floor-witness.ts` (`1cc77f71…`, run 681, a
CONSTRUCTED worst case with no seed, staging disclosed) driving the real advanceRelationshipsWeek:
52 mint → 50 after a completed 313-week return → 46 → 49 → 45, `currentTier` = Acquaintances because
45 IS RELATIONSHIP_TIER_FLOOR.Acquaintances, one point above the Strained ceiling 44, and a fixed
point. Record 675 item 1's 47 holds only for a release inside the grace window; 676-P revision 1's
46 was DEMONSTRATED WRONG by 678-C D1. THE DIAL: once the accelerator caps a cycle nets
5 − RELATIONSHIP_FAILURE_DELTA, so the landed 4 gains a point per picture, 5 breaks even and opens
the band (min 42), 6 declines without bound (min 34) — hypothetical arithmetic, no src/ constant
changed. Saturation CAUSE identified and WEAKENED per 678-C D3: while a rival's roster stands its
pictures re-seat the same quartet (one director, three actors), but rosters do change, which is why
edge counts read 27/27/36/24 and not a fixed 24. 260 weeks is not the 6,240-week endurance scenario
and no §8 R25 bound is claimed cleared.
NEXT675 (2) DELIVERED — the Owner packet `680-OWNER-PACKET-b5-decisions.md` (600 words): Finding 1
the unreachable lower half with the FAILURE_DELTA dial, Finding 2 the saturation, OPEN 1–3/11
carried, OPEN 15 closed by events (V31 shipped without the waiver field, so the waiver needs its own
save step), and ONE recommended next slice — P14B.6 READ MODELS over the waiver, with acceptance
checks and an effort forecast. 628 R5 / G-1(A) / G-2 and 637 carried, not blocking.
OWNER RULINGS ACTIVATED 2026-09-22 on record 680 (record 683; the Owner's `01-APPROVE-AND-CONTINUE.txt`,
blob `cbdbca4e`, at source `63688a79`): (1) RELATIONSHIP_FAILURE_DELTA := 5 as PROVISIONAL CANDIDATE
TUNING so repeated failure can reach Strained on the low-proximity path — no conflict driver, no
Enemies/Nemeses, no romance, no seating refusal, no general production penalty; (2) rival casting,
staffing and seat policy NOT touched, saturation retained, 628 R5 / G-1(A) / G-2 keep their own
disposition; (3) OPEN 1 SETTLED as common base growth (proximity weights remain), OPEN 2 SETTLED as no
behavioral backfill (factual shared-credit counts may display as facts), OPEN 3 SETTLED as warnings not
a hard refusal (B.6 must not invent a quality modifier), OPEN 11 retained for the later conflict work,
OPEN 15 OVERTAKEN by V31's publication so the waiver needs its own governed change; (4) P14B.6 read
models next, TypeScript/bridge only. Recorded once in the plan's B.5 OPEN register.
P14B.5-T TUNING CHECKPOINT QUALIFIED WITH RECORD-ONLY ITEMS — record 693 (694-C). Production: ONE file,
ONE hunk, +9/−1 — `RELATIONSHIP_FAILURE_DELTA` 4 → 5 with its doc comment (`relationships.ts:80`, one
call site). NO version step: RULES_VERSION 1, Save V31, projection 48 all unmoved, no migration; the
delta is a driver constant, the validator pins no delta value, no fixture stores a driver, no reader
reads a delta as a magnitude, and `projectRelationshipsPreV31` refuses rather than flattens — 694-C Q2
attacked this and could not break it. RED-first, independently owned: `tests/p14b5-t-failure-tuning.test.ts`
(466 lines, final sha `bc02cfcf…`), group 1 RED at 4 and groups 2–7 GREEN at 4. EVIDENCE: 688 requirement
12/12; 690 the 24 natural-chain controls 324 passed / 4 todo IDENTICAL to the 671 pre row; 691 typechecks
+ both generator checks EXIT 0; 695 FULL CORE on the final bytes `4c138264…` = 9 files / 24 failed /
3944 passed / 8 todo, the 636 baseline set EXACTLY, with the requirement file green at 12 — and the
669→695 failure CAUSES byte-identical but for one ephemeral mkdtemp path. ONE genuinely new failure
occurred and was closed: `hygiene.test.ts` bans the literal `Math.random` anywhere under src/ or tests/
and the new file's line 41 spelled it in a comment; its AUTHOR reworded that one line (restoring it
reproduces the prior hash, `diff` = exactly `41c41`). MEASURED CONSEQUENCE (682 pre → 689 post, same
engine PROVEN: `git diff ddf58e87..63688a79 -- src bridge generated ui scripts` EMPTY): min closeness
ever 48 → 47 on three seeds at the same week and edge; first Inseparable read w40 → w44 and w45 → w54;
one p13a pair now peaks at CloseFriends instead of Inseparable; root +1 byte on p13a stated as an
UNDECOMPOSED net; gzip moves on p13a AND seed-b (seed-b's raw root is byte-identical, its compressed
size is not); edges, drivers and every counter unchanged on all four seeds. STRAINED IS STILL NEVER READ
on the four standard seeds at either value — the capability is proven through the real write path, the
standard campaigns do not exercise it, and per the Owner that is accepted and is NOT a reason to retune.
Player encounter frequency is a PLAYTESTING hypothesis, not a finding. `681-b5-closeness-floor-witness.ts`
is FROZEN as the old-law witness (breaks at :102-104, :113, :144; reproduce at `63688a79`), never a
current-law test. FORWARD CONSTRAINT ON B.6 (694-C Q2): a resumed campaign's `recent` can hold both −4
and −5 failure rows on one edge, lawful and invisible only while nothing renders a delta, so B.6's
readable drivers must keep the no-number pattern or carry an era signal.
P14B.6 RELATIONSHIP READ MODELS QUALIFIED CHECKPOINT — record 718, source `211f1c18`, span
`ab405dbe..211f1c18` (15 commits). `git diff --stat ab405dbe..211f1c18 -- src` is EMPTY: B.6 changed no
engine law, no root, no validator, no migration and no constant. Bridge only: `bridge/relationships.ts`
(+263, new), `bridge/schema/bridge-schema.ts`, the schema JSON, `bridge/casting.ts`, `bridge/people.ts`,
`bridge/runtime-checkpoint.ts`, plus regenerated `generated/unity/*`. IDENTITIES: Save V31 UNMOVED, protocol
4 UNMOVED, projection 48 → 49, schema `sha256:00c0075b…` → `sha256:60af24c5…`, generated C# `370568f3…` →
`c84b5f95…`; outgoing 48 registered as a prior id at `ad49031f`; generator run exactly once;
`genuine-projection48-runtime` minted at T0 `43817117`, FIVE COMMITS BEFORE the writer touched
PROJECTION_VERSION, honouring the hard ordering constraint. All six NEXT696 items delivered: profile block,
readable drivers, casting chemistry rows, factual shared-credit counts, the Q4 warning, the thin wire step.
TESTS: three independently authored files, 33 cases (read-models 24, D2 2, E714 7), plus the 700 values-only
sweep across 30 existing files, which refused to bend two non-version regressions it met. EVIDENCE: 717 full
core on FIXED source `5ba2b8d6` = 9 files failed / 344 passed (353); 24 failed / 3978 passed / 8 todo (4010),
matching record 716's pre-registered prediction in EVERY CELL; the 712 → 717 comparison made on CAUSES (each
FAIL header paired with its own next Error line, one ephemeral mkdtemp suffix normalised) is EMPTY: same 24
failures, same files, same cause strings, the inherited baseline set unchanged. Both sibling B.6 suites green
INSIDE that run (read-models 24/24, D2 2/2), discharging the writer's one named evidence limit. DISCLOSURE
was the slice's real content, and the two carriers disclose on DIFFERENT bases DELIBERATELY: the profile
block publishes a tie only when its counterpart is independently visible on the player roster at W
(`startWeek < W && (endedWeek === null || W < endedWeek)`, subject excluded, strict at both ends), while the
casting rows carry NO roster predicate because the player proposed both people and the seating is
self-disclosing. Record 710 bounds it: `sharedPictureCount` scans `state.firstTakes` unfiltered and rival
receipts land there by design, so a count alone can see work the viewer never commissioned. THREE
CORRECTIONS LANDED INSIDE THE SLICE, each with its own RED: D1 (704), a comment claiming the profile rule
held on every DTO anywhere; D2 (707/712), `WITHHELD_LINE` asserting a POSITIVE employment fact on a state
with no `hollywood` root; E714 (710/714/715/716/717), both absence sentences denying shared work on the
absence of an EDGE. One hazard was closed BEFORE publication: the first E714 casting sentence was a strict
PREFIX of `CHEMISTRY_LINE[0]`, diverging at word 5, so truncation would have rendered "no record at all" as
"a recorded, neutral tie"; amended to diverge at word 1. TWO PREDICTIONS FAILED AND ARE RECORDED AS FAILURES:
709's UI prediction (30 observed / 26 predicted, attributed in 713 to the unstable suite and NOT to the
candidate), and the parent's own 3-failed/4-passed forecast for the disclosure pin, where the actual 4/3 was
correct. NOT CLAIMED: UI NOT RERUN, reliability open (713 re-ran the UNCHANGED baseline source `df693294`
today and got 32 failed against the 26 it recorded as 673, with `livingTurn.scheduler` 1 → 6 → 1; 14 cases
intermittent, so the AGGREGATE carries no signal about a source change, while an INDIVIDUAL failure in it
remains real evidence); UNITY NOT VERIFIED (the manifest's matching Unity-side sha is a RECORDED PAIRING
written by the generator, not a verified one: no Unity checkout read, nothing compiled, nothing rendered;
Build56/32 remain unpaired with 49); the 24 inherited core failures untouched; the natural casting-warning
route UNTESTED (no fixture here releases a player picture, so the covered route is staged). Unity backlog
entry updated for the D2/E714 absence copy and its truncation hazard.
RELIABILITY FOLLOW-UPS ASSIGNED — record 719, on the Owner's 2026-09-23 correction that investigating an
unstable test is legitimate engineering and is NOT "bending it to look green"; the parent's earlier framing
conflated the two and is withdrawn. FU-1 the `ui` project: OWNER test-author; TRIGGER before the next
UI-affecting acceptance claim (not now, engine work continues); five named files carry all the movement and
four are perfectly stable; diagnose to one of synchronisation, leaked state, fixture error, or performance
under load; NOT AUTHORIZED to remove an assertion, skip, quarantine, retry or widen a timeout; RETURNS when
three consecutive full `ui` runs at one unchanged source give the same count AND the same identities.
FU-2 `bridge-runtime-checkpoint-prepared-reuse`: OWNER parent; TRIGGER a recurrence only; threshold RETAINED
and margin RECORDED (≈6.3s of a 20000ms budget at `:367`, ≈68% consumed idle at BOTH `43817117` 14278ms and
`e63d6143` 13328/13710ms, so the B.6 source did not cause it; 21/21 in 176619ms inside run 717); on a
recurrence identify the OPERATION that failed to finish, with comparable timings both sides, never
defaulting to engine defect or environmental noise; NOT AUTHORIZED to widen, quarantine or open an
optimization campaign.
NEXT721: P14B.7 THE PROMISE WAIVER (expansion 720) — the player may WAIVE an open promise by offering a
substitute the person accepts in its place, instead of the single road the engine has today, where an
undeliverable promise simply BREAKS at its due week and the trust label degrades. Accepted only when the
substitute is REASONABLY ACHIEVABLE over the REMAINING contract interval, is AT LEAST AS STRONG as the
promise it replaces, and the person does not already read the studio as `Distrusted`; a refusal returns a
reason and leaves the original open on its original terms. Save V31 → V32 for `supersededByPromiseId: string
| null` (exact-key validation; `convertV32ToV31` REFUSES a non-null value rather than dropping it). NO
PROJECTION STEP, CORRECTED AFTER PUBLICATION BY THE 723-C AUDIT: the conclusion survives and the reasoning
first published here did not. `WAIVED` is already on the wire enum (`bridge-schema.ts:1770`) and already an
enumerated `PromiseOutcome` no path reaches (`types.ts:2173`), so the promise-history row publishes it with
no wire change and B.7 activates an enumerated terminal state rather than widening a union. But that alone
does NOT settle the projection question, and the first version of this line implied it did. TWO BRIDGE
SURFACES CHANGE BEHAVIOUR WITH ZERO CODE EDIT the moment a WAIVED promise can exist: `bridge/trust.ts:68`
gates the attention row on `SATISFIED || BROKEN`, so a player who NEGOTIATES a waiver would get LESS feedback
than one who lets the promise break, which B.7 creates and therefore B.7 fixes; and `bridge/industry.ts:136`
filters the public activity fold the same way, dropping a WAIVED receipt at `:141` and silently falsifying
the module's own invariant at `:133-134` ("one PUBLIC activity per exact outcome receipt"). Announcing
waivers publicly WOULD need a projection bump, because `industry-schema.ts:147` closes `outcomeKind` to
`['promiseKept','promiseBroken']` with no legal third value. RECOMMENDED AND ADOPTED: do NOT announce. THE
BASIS FIRST PUBLISHED HERE WAS WRONG AND IS REPLACED: it said promise terms are private to their issuer,
which is TRUE (`bridge/promises.ts:100-102` filters on `issuerStudioId !== viewerStudioId`) and does NOT
support the conclusion, because the fold publishes no terms. It publishes an OUTCOME, and outcomes are
ALREADY public for every studio: `bridge/industry.ts:135-137` carries NO studio filter and `:146` emits
"Studio X kept/broke its promise to Y" by name, rivals included. So this is the SECOND GENUINE PRODUCT
CHOICE of the slice, not a settled matter, and the objection is stated rather than hidden: silence lets a
studio about to break a promise waive instead and keep the public record clean. What bounds that is the
waiver's own three conditions, since a studio that has already broken promises reads `Distrusted` and
cannot waive at all. Cost of the other reading, so it can be priced: a third `outcomeKind` widens a closed
enum, which is projection 49 → 50 PLUS its own hard ordering constraint to mint
`genuine-projection49-runtime` first, as 45/46/47/48 all were. Under the adopted reading the fold's code
does not change, but its comment is corrected AND the RED pins that a waived promise produces no public
activity, so the decision does not survive as an accident the next person will "fix". Projection stays 49
because of that decision, not because the question did not exist. The original settles through the EXISTING `settle()` (`promises.ts:678`) with ONE
`promiseOutcome` receipt, the B.1 one-kind ruling unamended; the substitute is minted BOUND to the existing
`contractId` and does NOT travel through `attachPromise`, which requires a current proposal and would throw.
STRENGTH RUNS OPPOSITE TO MASK SIZE: "at least as strong" is `substituteMask ⊆ originalMask`, a SUBSET test,
and a writer who tests superset inverts it silently, accepting every downgrade. Three product choices
isolated with recommendations, not blocking: (a) mask inclusion over an explicit table, (b) no
re-classification of a BOUND substitute, (c) NEW, the substitute's count must meet the REMAINING obligation
(`count - progress`), not the original. EXCLUDED with owners: rival waiver policy record-only (R3); the
retirement-moot `VOIDED` branch needs P14C; the bridge intent and read model are the slice AFTER B.7, on the
B.5 → B.6 rhythm; the trust consequence of a waiver is NOT invented here (`trustDrivers` enumerates five
kinds and WAIVED is not among them). EXPANSION AUDITED BEFORE THE SLICE BEGAN, per the plan's standing
rule: record 723-C, read-only, verdict REFINE, five HIGH findings, EVERY ONE re-verified against the
source by the parent before acceptance and none taken on the auditor's word. The sharpest was the
parent's own: the draft pointed the writer at `reclassifyPromise` as a shape to copy, and copying it
would have made BOTH contract-fit refusals (`promises.ts:406-409`) tautological for the one promise
whose window has never been checked against a contract, since that function passes the promise's own
window as the interval BY DESIGN for a live promise (`:449-451`) and has no production caller at all.
The audit also added the `feasibilityReceipt` requirement, the non-`evaluable()` refusals (terminal or
unbound), the identical-substitute refusal, the `progress`/`evidenceRefs` disposition, and showed that
TWO of the three "product choices" were already settled by existing law and by the record's own
architecture, leaving only the substitute's count genuinely open. Record 720 amended in place with its
own §10 log; T1 is authored against the AMENDMENT, not the draft. ORDER: T0 DONE → audit DONE → T1 RED → sim-core →
pre-registered prediction → full core on fixed source → checkpoint. T0 (record 722) minted NINE genuine
outgoing V31 fixtures at `152ee9a4` through REAL ACTION PATHS ONLY, no hand-assembled state and no gap in
the roster, recording BOTH identities as the V30 precedent does (tested source `caa8cdb3`, the last
BEHAVIOURAL V31 writer, since B.5-T moved RELATIONSHIP_FAILURE_DELTA 4 → 5 and that changes edge values
inside a V31 save; `save.ts`'s own last writer `f5310afb`). The parent re-verified every compressed sha256
against the files on disk, the archived minter and support against the manifest's own
`minterSha256`/`supportSha256`, and that the approval gate's VALUE equals the observed head, so the mint
could not have fired by accident; `LIVE_SAVE_VERSION` still reads 31 and `PROJECTION_VERSION` still reads
49 on disk. The operational minter and its support module were archived as executed text and REMOVED from
the tree, the disposition every prior mint used. TWO ENGINE FACTS WERE MEASURED THAT A STATIC READ COULD
NOT SEE, which is why T0 precedes T1: `Distrusted` TOLERATES a positive driver, because `label()`
(`promises.ts:879-884`) tests `negative >= 2 && negative > positive` and not driver purity (the minted
`distrusted-issuer` carries 2 negatives AND 1 naturally-arriving positive and still reads Distrusted), so
a RED asserting purity would pin a stronger premise than the engine holds; and a promise whose count is 2
or more needs a SECOND pipeline opening, where `activateScriptDevelopment` looks like the door and is not,
since it changes `greenlight`'s own admission rule. Then the remaining authorized P14
obligations, P15, P16, P17 and sufficiently specified P18 in dependency order. Carried: positive-saturation,
the 6,240-week endurance obligation, 628 R5 / G-1(A) / G-2, 637, evaluator 5, 702-C REFINE 3-6, the B.6
per-talent projection cost, and `rosterAt`/`SEAT_PAIRS` restated in `bridge/relationships.ts` from private
`src/core/` helpers (they will drift silently if the engine's move).
OWNER DECISIONS 2026-09-23 on the two open choices, both recorded in 720 §5. (c) THE REMAINING-OBLIGATION
RULE IS APPROVED verbatim: "a substitute must cover at least the original's unfulfilled qualifying count,
without erasing completed work or counting it again toward the substitute." So `substitute.count >=
original.count - original.progress`, the original KEEPS its progress and evidenceRefs, and the substitute
starts at `progress: 0` with `evidenceRefs: []` and may NOT absorb the original's takes. A three-picture
promise served once, waived for a two-picture substitute, needs TWO FURTHER appearances: six would
double-charge and one would double-credit. (d) PRIVATE CONFIRMATION, NO PUBLIC ANNOUNCEMENT, as recommended,
with the existing public kept/broken announcements PRESERVED UNTOUCHED and the issuing player getting an
accurate confirmation and accessible history, never mislabelled. THE PROJECTION CLAIM IS NOW SETTLED BY
ENUMERATION, per the Owner's requirement that it be confirmed across consumers and schema rather than rested
on one enum: every promise-outcome consumer in `bridge/` and `ui/` swept (720 §6). `promiseHistoryFor`
already publishes WAIVED, viewer-scoped to the issuer, which IS the companion's "recorded and visible";
`trust.ts:64` promiseDue self-corrects; `contract.ts`'s `outcome` is the command envelope, unrelated;
`supervisor.ts` is an unrelated symbol; and THERE IS NO `ui/` CONSUMER OF A PROMISE OUTCOME AT ALL. Exactly
two surfaces need attention and no third exists. A MISLABEL HAZARD was found by that sweep and is the
Owner's warning made concrete: `trust.ts:69` is a two-way ternary with NO third arm, so widening the gate at
`:68` without touching `:69` publishes a waiver to the player as "BROKEN", reporting a breach that did not
happen against a settlement the person accepted. TWO SETTLED MATTERS WERE MIS-SORTED AS OPEN AND ARE
CORRECTED: the trust consequence of a waiver is settled by the companion (`:379` "none; recorded and
visible", ruling S11 `:568`) and the engine already agrees by construction, and the retirement-moot branch's
OUTCOME is genuinely ambiguous in the companion, which lists the same cause under BOTH the WAIVED and VOIDED
rows, so P14C inherits an ambiguity rather than a rule.
T1 RED DONE — record 725-T, IDENTITY CORRECTED BY RECORD 727. `tests/p14b7-promise-waiver.test.ts` AS
COMMITTED is sha256 `68c76efe…`, 619 lines, **27 FAILED / 2 passed (29)**, exit 1, re-run by the parent on
the exact committed bytes (transcript `727-red-recheck-on-committed-bytes.txt`). The first publication of
this line named `bf5fb83a…`, 573 lines, 26/2 (28), which were the bytes 725-T describes and are NOT the
bytes in the repository: the suite grew by ONE case, covering the Owner's approved remaining-obligation
rule, between the engineer's hand-back and the commit, because the parent's message carrying that decision
arrived after the report was composed and resumed the agent. THE PARENT PUBLISHED A STALE IDENTITY, having
hashed the file, then sent a further message that resumed the agent inside the same tree, then committed
without re-hashing. RULE: re-hash immediately before staging, not merely before the last thing that could
have changed it. Attribution RE-VERIFIED on the committed bytes: 108 "RED premise" guards and ZERO
AssertionError/TypeError/ReferenceError/SyntaxError lines lacking that marker, over two guarded symbols
(`waivePromise`, `convertV31ToV32`), so every failure is a named missing export and none is module
resolution or a masked `toThrow()`. One of the two passes PROVES THE FIRST TRAP IS REAL rather than
asserting desired behaviour: it shows the `reclassifyPromise`-buggy interval substitution ACCEPTING a
substitute the correct reading refuses. 725-T is left as the engineer wrote it, truthful when written; 727
is the reconciliation and the two are read together. The two passes call
only functions that exist today and are premise-proving, not pins. Coverage: all ten brief priorities, the
two foundational items, and the mid-task eleventh pin on the public industry fold, asserted
positive-then-absent so it cannot pass vacuously. Three interface interpretations are NAMED rather than
assumed (720 §2a): `waiverAccepted` returns `string | null`, the substitute draft mirrors
`PromiseAttachment`, and the save functions are `convertV31ToV32`/`convertV32ToV31`. The writer follows them
or a divergence gets a recorded ruling; a silent rename under the suite would make a green run meaningless.
A SECOND TRAP was found by T1 authoring against the real engine, of the same anatomy as the first and
missed by both the parent and the audit: THE ORIGINAL PROMISE DOUBLE-CHARGES ITS OWN REPLACEMENT.
`activePromiseReservations` (`promises.ts:289-297`) filters competing reservations on `promise.promiseId !==
draft.promiseId` and `reservedByActivePromises` (`:300-303`) sums `count - progress`, which is EXACTLY the
remaining obligation the Owner's rule requires the substitute to cover, so the original books the capacity
its own replacement needs. Measured, not reasoned: every happy-path acceptance candidate reads
`FRAGILE — needs a picture not yet commissioned` until the exclusion is applied. `PromiseDraft.promiseId`
exists for precisely this (`:206-207`) and was written for `reclassifyPromise`; the waiver uses the same
field with the ORIGINAL's id on the SUBSTITUTE's draft, which is correct rather than convenient, since the
original settles WAIVED in the same step and its reservation is being released. Both traps share one
anatomy: a documented helper or field written for a neighbouring caller, whose correct use by the waiver
differs in one detail nothing enforces, failing silently and surfacing far away.
THE T0 PROBE IS ARCHIVED, NOT DELETED — record 726, on the Owner's ruling that a stray probe must be
preserved, its effect established and kept outside the suite unless deliberately admitted. The parent's
"delete it" instruction was the wrong remedy and is owned as the parent's error: deleting destroys the
evidence needed to answer what it touched. Recovered verbatim; it asserted NOTHING, so its maximum possible
contribution was one passing file. It ran once deliberately, alone. Four further invocations named a single
file positionally, three confirmed by their own visible "(1)" counts, and the fourth was reported as an
HONEST GAP by the engineer rather than closed with a plausible claim. THE PARENT CLOSED THAT GAP BY
EXPERIMENT: a guaranteed-failing file was planted in `tests/` under a shell trap, a DIFFERENT named file was
run, and the run reported `Test Files 1 passed (1)` — so a positional CLI argument filters COLLECTION, not
merely reporting, at vitest 2.1.9 here. No `record-check.mjs`, full-core or `test:ui` run occurred in the
probe's window, so NO BASELINE WAS TOUCHED and no published count includes it.
P14B.7 THE PROMISE WAIVER QUALIFIED CHECKPOINT — record 742, source `ca5b6c1f`, span
`152ee9a4..ca5b6c1f`. THE PLAYER BEHAVIOUR: a studio with an open promise it can no longer keep may
offer the person a SUBSTITUTE promise in its place; on acceptance the original settles WAIVED and the
substitute binds to the SAME employment contract in the same step, and on refusal the original promise
and the whole game state are left exactly as they were. Nine refusal conditions, each its own
player-facing sentence. IDENTITIES: Save V31 → V32 (`ProfessionalPromiseV32` adds
`supersededByPromiseId: string | null`), protocol 4 UNMOVED, PROJECTION 49 UNMOVED, schema
`sha256:60af24c5…` UNMOVED, `PROMISE_RULES_VERSION` 4 UNMOVED, and `git diff 152ee9a4..HEAD --
generated/` is EMPTY — the FIRST P14B slice to move the save version without moving the wire.
`migrateToV32` is the live load route from nine call sites across seven files; `convertV32ToV31` is the
one allowed downgrade, lossless exactly when no promise names a substitute, and `projectPromisesPreV32`
refuses anything else BEFORE validation so a waived world is refused as a DOWNGRADE, never as a shape
complaint about a field V31 has no schema for. OWNER DECISIONS LANDED AS APPROVED: the
remaining-obligation rule (`substitute.count >= count - progress`; substitute minted at `progress: 0`
with `evidenceRefs: []`, so completed work is neither erased nor counted twice), and private
confirmation with the public kept/broken announcements PRESERVED UNTOUCHED. VERIFICATION — run 739 on
fixed source, `fixedSource: true`, 3151.67s: 10 files failed / 345 passed (355); 25 failed / 4010
passed / 8 todo (4043). Full failure-NAME sets against run 717: 24 SHARED, byte-identical names, ZERO
VANISHED, exactly ONE new. Both B.7 suites green in-suite (28 + 5 = 33). Typecheck root 0, bridge 0, ui
0. THE PRE-REGISTERED PREDICTION EARNED ITS KEEP TWICE. Run 737 against prediction 736 was FALSIFIED
(13 files / 30 failures vs 9 / 24) and caught FOUR SWEEP MISSES the sweep's own "CANNOT-MOVE residue:
none" had denied (record 737). Three were sites asserting that the version ONE ABOVE live is unknown;
the sweep carried that class and applied it to 12 of 15 files, but every site it found states the range
`"1 through 31 only"` on the same line while the three it missed contain ZERO occurrences of `31`
anywhere in the file, so a finder keyed on the OUTGOING version was structurally blind to them. The
fourth was `studio-events.contract.test.ts:129`, which resolves the LIVE boundary reflectively as the
string `'validateSaveV31'`; that one was findable and simply was not in the brief's class list. Fixed in
four values-only lines; the OTHER 28 `validateSaveV31` references all STAY, being frozen-era proofs
including B.7's own validate-then-convert design, and a blanket rename would have destroyed them. A
CORRECTION TO METHOD, recorded because it nearly hid this: the parent's cause-TEXT diff showed no change
at all, because the new failure's text is identical to an existing timeout message and is visible only in
the occurrence count; the failure-NAME set comparison is the robust instrument and is what the records
use. FU-2 IS TRIGGERED AND DIAGNOSED — record 741, and prediction 738 named it in advance. The operation
is prepared-reuse `:367` "…after later changes and restart", explicit budget `}, 20000)`; four
measurements across three commits read 13328 / 13710 / 14161 / 14278 ms, a 7% spread, so THE OPERATION IS
NOT REGRESSING, which is what FU-2 existed to settle. Contention multiplies that file 1.67×–1.74× and the
budget is 1.412× the operation cost, so the crossing point sits below what the full suite imposes; 737
passing is the surprise, not 739 failing. The repository's own restart-class convention is `60_000`
(`bridge-process-restart.test.ts`, all three tests, observed to 21929ms and passing), so 20000 is the
outlier. NEITHER environmental noise NOR an engine defect, the two defaults 719 forbade. THE THRESHOLD
WAS NOT MOVED: the Owner said retain it, and the recurrence instruction was to investigate, not to then
raise the number; the case for alignment is recorded as a recommendation for decision. WHAT THE
CHECKPOINT DOES NOT CLAIM: Unity NOT VERIFIED (no re-vendor is required, the projection did not move);
the `ui` project NOT RUN (FU-1 has not returned); the 24 inherited failures untouched and none of them
B.7's; the natural waiver route UNTESTED, since no fixture in this repository plays a picture forward to
where a studio would genuinely need to waive; and the two coverage gaps briefed at record 740 are now CLOSED
(record 743), together with a THIRD hole the parent found while verifying the first pass and which
mattered more than either: THE OWNER'S OWN APPROVED RULE HAD NO TEST. Rule 7's preservation half was
covered, but the refusal when a substitute covers too few had no case anywhere, so the suite would have
stayed green under an implementation reading the original's raw count instead of the remainder. All
three guards are PROVEN BY INJECTION rather than asserted: the WAIVED trust-driver arm was added to
`trustDrivers` and both group12 tests went red; the naive `remaining = promise.predicate.count` law was
substituted and both group13 tests went red, in the two distinct ways their construction predicts (the
refusal on the NUMBER, "of the 2" against "of the 1", and the boundary on the VERDICT); each injection
was reverted in the same command block under a shell trap with the clean tree confirmed after. Suites
now 38 passed (38), 32 engine + 6 bridge. EIGHT of the nine refusals are covered; rule 3 ("the
employment contract this promise rode in on is no longer on the record") is the one no case reaches,
because every promise with a non-null `contractId` in all nine corpus fixtures resolves to a real
employment record with ZERO orphans anywhere, so reaching it needs a new fixture or a forged state —
recorded untested rather than forced. Rules 4 and 5 could not be logically separated for the same
corpus-shape reason and that limit is disclosed, not hidden. Also recorded: the bridge B.7 suite sha moved from `e6c7ae4d…`/185 (records 731, 733) to
`eedc23fb…`/188 because the T2 sweep legitimately edited it, carrying the genuine V31 fixture up through
`convertV31ToV32` AFTER the frozen validator admits it rather than softening `validateSaveV31`; no case
was added. Unity backlog entry for the V32 save step is written, and it states the native hazard plainly:
nothing fails to parse, but a C# switch handling only kept/broken will now RECEIVE `WAIVED` at runtime.
NEXT743: P14B.8 THE WAIVER'S PLAYER SURFACE — the bridge intent and read model, on the B.5 → B.6 rhythm
this programme already uses (engine slice, then the read-model slice over it). THE PLAYER BEHAVIOUR IT
COMPLETES: today a player can only reach the waiver through the engine API; B.8 gives them the affordance
to offer a substitute, the quote-style refusal shown verbatim when the person declines, and the waived row
in their own promise history. COMPLETION CONDITION: a player can propose a substitute, see the specific
reason it was refused, and see the settled waiver in their history, with no waiver reaching any public
Industry surface. HARD ORDERING CONSTRAINT, inherited from 45/46/47/48 and stated here so it is not
rediscovered: this is a PROJECTION STEP (49 → 50), so `genuine-projection49-runtime` must be minted and
published BEFORE the bump, and the outgoing 49 identity `sha256:60af24c5…` registered as a supported
prior id in the same commit. The nine refusal sentences are the read model's copy and are hypotheses; a
copy change is a test edit plus a projection bump. The record-740 gate is DISCHARGED (record 743), so B.8's
acceptance claims now rest on suites that prove which refusal fires and pin the Owner's approved rule. Then the remaining authorized P14 obligations, P15,
P16, P17 and sufficiently specified P18 in dependency order.
P14B.8 OPENED — THE WAIVER'S PLAYER SURFACE. THE PLAYER BEHAVIOUR BEING IMPLEMENTED: a studio
that can no longer keep an open promise can today reach the waiver only through a TypeScript
function call. B.8 puts it in front of the player as one connected interaction — select an open
promise, propose a substitute, receive an accurate quote or the specific refusal, confirm, and see
the original marked WAIVED with the replacement recorded beside it. COMPLETION CONDITION, eight
checkable clauses at record 744 §2: a refused substitute answers with the engine's own sentence
verbatim and leaves the original promise and the whole game state unchanged; an accepted one
registers exactly one commit intent; confirming settles the original WAIVED and binds the
substitute in one accepted command; the player's own history then shows which promise replaced
which; no waiver reaches any public Industry surface and the existing public kept/broken
announcements stay untouched; a replayed intent, a stale revision and a moved board each fail
closed; and a save taken after the waiver reloads with the link intact. Expansion drafted at record
744 and independently audited (745) before the slice begins, on the 720/723-C precedent. FOUR
CHANGES, and no more: a `quoteWaivePromise` family, a `promiseWaiver` commit arm with the
fail-closed revalidation every other family has, ONE read-model field (`supersededByPromiseId` on
`StudioMarketPromiseHistoryRow`, which three carriers share), and the projection bump. WHAT IS
ALREADY DONE AND MUST NOT BE REBUILT, verified rather than assumed: a WAIVED row ALREADY reaches
the player's history at projection 49 (`bridge/promises.ts:93-118` filters on issuer, beneficiary
and contractId and on nothing else, and `PROMISE_OUTCOMES` has carried WAIVED since before B.7), so
only the LINK is missing; refusal-as-an-accepted-quote is an established pattern across five
families; and all four protections against waiving twice already exist generically in
`bridge/session.ts` (stale revision :1562, moved-board digest :1628, pendingQuotes.clear :1613,
command-id replay :1560) — B.8 PROVES them for this family and builds none of them.
THE T0 GATE IS DISCHARGED, both halves, before anything touches PROJECTION_VERSION.
T0a: `genuine-projection49-runtime` MINTED at `4a268feb` and published (`4cba7090`), while 49 is
still the running identity and its schema id is NOT yet a registered prior. It carries two things
the 48 artifact did not — a REAL publication receipt (`git ls-remote origin <branch>` returned the
minting head; `for-each-ref --contains` is not a publication test in a worktree holding no
refs/remotes/<branch>, which the 48 MANIFEST records as its own corrected error), and a MEASURED
V31→V32 delta: the archived V31 corpus migrates forward with `supersededByPromiseId` null on every
promise, and stripping that field back off reproduces the V31 rows byte for byte, so the whole save
bump is one field, proven. Distinct current/saved slots asserted in the minter, not implied: same
week 45, different digests. Minter archived at record 746.
T0b: the Owner's edge case needed a world no published fixture could express — the largest
remaining obligation in the whole pre-b7 corpus is 1. The Owner's literal 3 promised / 1 delivered
/ 2 owed was MEASURED (record 747) and is blocked by a PRODUCT LAW, not by fixture construction:
count 3 reads FRAGILE "needs a picture not yet commissioned"; `existingPath` has three levers and
all three are measured shut (stock already 1, `commissionScript` throws "screenplay development is
not managed" on every world this corpus builds from, and a second production seating the same
person refuses under M16 exclusivity); and `attachPromise` ADMITS a FRAGILE draft but the freeze
refuses to BIND it, which would fire `waiverAccepted` rule 2 four rules before the count rule the
case exists to test. `genuine-v32-owes-two-p1` was minted instead at count 2 / progress 0 /
remaining 2, by the archived part-served route STOPPED AT THE FREEZE, and it PROVES the case rather
than claiming it: the minter measured both directions on the live engine before writing a byte
(count 1 → "only 1 of the 2 pictures still owed would be covered", count 2 → null) and re-measured
the refusal on the state read BACK from disk. WHAT THE SUBSTITUTION COSTS IS RECORDED, not buried:
at progress 0 the remaining obligation equals the original count, so this world alone cannot
separate `count` from `count - progress`; part-served-p1's group13 already pins that and was proven
by injection at 743, so the two worlds are complementary and neither replaces the other. Minter
archived at record 748; the capacity probe at 747, run alone four times, never inside a pass.
A SHARPER FINDING THAN THE ONE THE SLICE WAS OPENED ON: the wire's `count: integer({minimum: 1})`
(`bridge-schema.ts:1798`) makes B.7's zero-count rule-7 refusal STRUCTURALLY UNREACHABLE from the
player surface, so the Owner's positive-but-insufficient case is not one useful case among several
— it is the ONLY way rule 7's refusal can be exercised through the bridge at all.
P14B.8 VERIFIED AND PUBLISHED — P14B CLOSES. Record 757, source `c368e6b1`, run `755-b8-full-core`
at `3cae7c93`, `fixedSource: true`, empty tree diff both ends. IDENTITIES: projection 49 → 50, schema
`sha256:e2d354dc…`, prior roster 37 → 38 with `60af24c5…` registered as `projection-v49` IN THE SAME
COMMIT; save V32, rules 4 and protocol 4 all UNMOVED and `git status src/` EMPTY. B.7 moved the save
without moving the wire; B.8 IS ITS EXACT MIRROR, moving the wire without moving the save. Suites 44
passed (44). Full core 10 files failed / 347 passed (357); 25 failed / 4060 passed / 8 todo (4093).
THE FAILURE-NAME SET IS IDENTICAL TO RUN 739: 50 distinct lines, ZERO new, ZERO vanished, which was
prediction 754's core claim. The COUNTS were falsified by the parent's arithmetic, not the run, and
are attributed per file: 4043 + 5 (B.7's own gap closure, which landed AFTER run 739) + 1 + 44 = 4093.
The `+1` is `bridge-runtime-checkpoint.test.ts:758`, an `it.each` over the prior-identity roster, so
registering `projection-v49` adds exactly one case from a four-line data edit with no new `it()`.
THE AUDIT EARNED THE SLICE: neither `waiverAccepted` nor `waivePromise` compares a promise's
`issuerStudioId` to anything, and ids are `promise-<index>`, sequential. MEASURED on
`genuine-v31-with-edges`: `waiverAccepted` returns NULL for a rival's promise and `waivePromise`
settles it WAIVED with a substitute bound to the RIVAL's own contract, invisible to trust and to every
read model. B.8 created the first client route, so B.8 owed the gate; it lives in `bridge/promises.ts`
alone, refuses at quote AND commit, and answers a rival's id exactly as an unknown one. That engine
null also settled the refusal's SHAPE, which the expansion left open: no engine sentence exists, so it
is `ENGINE_REJECTED`, not an accepted `ok:false`. FU-2 recurred at 20305ms against 20000ms and record
741 is CONFIRMED not falsified (file total 177669ms against 739's 180289ms, a 1.5% spread); tally is
2 failures in 3 recent full runs; THE THRESHOLD WAS AGAIN NOT MOVED per the Owner. FIVE ERRORS THIS
SLICE CAUGHT, three of them the parent's, listed at 757 §7 because a checkpoint that hides them is
worth less: the rival-waiver hole; the parent's false "excluded twice over" claim about
`outcomeEventId` (`settle` coerces `next.outcomeEventId ?? eventId`, so a waiver carries a REAL
receipt id and the Industry exclusion rests on ONE guard); the parent's A13, which stated rule 6's
subset test BACKWARDS and would have led a reader to drop the P2 family as untestable; the parent's
sweep inventory, which keyed on the SYMBOL `PROJECTION_VERSION` and missed four other spellings while
the writer found three more classes beyond the correction; and the case arithmetic above. THE
GENERALISATION NOW ON THE RECORD: a version sweep is not finished until every test touching the
bumped surface has RUN — static enumeration found 42 of 45 version sites and 3 of 4 row-shape sites.
NEXT757: P14C — CAREER LIFECYCLE, and its first slice C.1 is MATERIALIZED AGING. Scope checked at
record 756 against the companion §2.3 and §6 rather than inferred from the heading, with its
correction: the `p14b4-cast-class-capacity-evaluator5` failure is DESIGNATED by its own file header
("a writer never rewrites it") and is NOT the next obligation. UPSTREAM CHECKED: `Talent.age` already
exists (`src/core/types.ts:123`) and is STATIC, set at founding and clamped 18-70 and incremented
nowhere; `talentMarket.ts:74` already declares the three P14C eligibility rows in the type and states
they are unreachable before P14C. The seam is pre-built. §6.1's law is specific and is NOT to be
redesigned: age is DERIVED FROM PROVENANCE AND MATERIALIZED, never incremented independently; each
person carries birth provenance in the P14 root (`authored_exact_week`, or
`legacy_age_anchor {ageAtMigration, migrationWeek}` for pre-P14C saves); at each person's birthday due
week, an IN-STATE BUCKET and not a scan, the integer age is computed from provenance and `market.tick`
and WRITTEN into the accepted `Talent.age` leaf, with the validator cross-checking against provenance.
Every accepted reader of `talent.age` then ages for free with no P10 reader change. Age NEVER
decrements a skill, genre experience, ceiling, development rate or Star Power. AUTHORITY NOTE, raised
rather than assumed: `CLAUDE.md` lists "aging and career progression" as not-current-scope, and that
same section provides for supersession by the current campaign, which names P14C explicitly in the
companion, the slice order and the Owner's continuation packet.
NEXT761: C.1 T0 IS DONE AND PUBLISHED. Five genuine outgoing V32 worlds minted at `ace2773a` while
that head was already on the remote (`tests/fixtures/p14/genuine-v32-c1-corpus/`, record 761): bare
world (hollywood null, tick 0, 60 people ALL fractional); bare TICKED (null hollywood at tick 1, so
"null industry" and "week zero" separate); fresh tick0 (24 rival rows, 4 INTEGER ages manufactured by
`hollywood.ts:221` with zero authored people, which is record 760's correction to the audit); authored
(2 authored, ageMin exactly 18, and `authored-0001` at 29.75); scientist (tick 260, 1 scientist, ages
to 68.28, so the top-of-draw axis is now covered at two weeks rather than one). The held
`genuine-v32-owes-two-p1` is NOT re-minted. OWNER CORRECTION APPLIED (2026-09-24, expansion 758 §10):
A7 derived the birthday as `frac(age) x 52` and that is the WRONG DIRECTION; the fraction measures
progress SINCE the previous birthday, so 29.75 turns 30 in THIRTEEN weeks, not 39. Pinned as ONE
formula with no stored birth week to disagree with it: `age(w) = floor(a0 + (w - w0) / 52)`. The
predicted crossing week is committed INTO the fixture provenance, before any implementation existed,
so the prediction can falsify the implementation rather than the reverse. Also folded in: the 30
boundary is promoted from an audit finding to a REQUIRED TEST because `talentMarket.ts:690` `isProven`
is a market decision and not a display change; the zero-RNG law is re-scoped to the materialization
entry point, since an ordinary weekly advance may legitimately draw for other activity; and the
permitted downgrade is restated as a BYTE-IDENTICAL V32->V33->V32 round trip, because matching the
older file shape alone is insufficient. Record 762 pins the API contract both specialists work from
(root shape, module surface, the five append sites, tick ordering, the V33 boundary, the downgrade
predicate) and `src/core/aging.ts` ships as a SCAFFOLD whose every export throws, so the RED fails
because behaviour is missing rather than because a missing named export bound to `undefined`. Record
763 is the V32->V33 sweep inventory, keyed on the VALUE: 15 classes, the two large ones being 132
`migrateToV32` call sites and 92 test literals, each decided PER LINE. Two negative results recorded
with the positive ones: the C# DTOs carry NO save version, so unlike B.8 a save bump regenerates
nothing under `generated/`; and `performance-library32` is a false positive whose 32 counts library
records. Order from here: RED (authored independently against 758 as amended) -> implementation ->
verification -> publication.
NEXT772: **P14C.1 IS QUALIFIED CLOSED AND REMOTELY VERIFIED at `1d2359d7`.** Save 32 -> 33, projection
UNMOVED at 50, `git status generated/` EMPTY so no C# regeneration — C.1 is B.8's exact mirror. Run
`772-c1-final-verification` on `3aaf55e0`, `fixedSource: true`, empty tree diff at BOTH ends: 358 files
and 4139 cases EXACTLY as predicted, 55 failures inside the predicted 54 +/-1, all 50 repaired cases
cleared, ZERO new failures outside the two known sets, and `tests/p14c1-materialized-aging.test.ts` at
46/46. 55 = 25 inherited + 30 deliberate. The +/-1 was FU-2 named in advance: failed 739, passed 737,
failed 755, passed 767, failed 772 — THREE OF FIVE, threshold still deliberately unmoved.
THE 30 DELIBERATE FAILURES KEEP THEIR CAUSES RATHER THAN THEIR NUMBERS. 25 are isolated-population
loss (`p14b4-rival-seating-preference` 13, `p14b4-cast-class-outcomes` 9, `p14b1-trust-chooser` 2,
`p14b4-cast-class-policy` 1): each natural search needs a capable-but-unproven subject, and under
materialized aging those subjects cross `isProven` before the events the search finds, so a wider scan
window makes it STRICTLY WORSE. 5 are the `poachingFixture` cascade, where the attribution was correct
but incomplete and the deeper fact was found rather than pinned around: `proposePromise`
(`tests/helpers/p14b2-fixtures.ts:34`) hard-codes `termWeeks: 52` while `preferredTerm`
(`talentMarket.ts:711-713`) returns the SHORTEST term when unproven and the LONGEST when proven, so
the fixture premise held only while the subject was unproven; `priorityOrder` moves `term` from fourth
to second at the same crossing. Both classes are redesigns, not repairs.
OWNER RULINGS RECORDED, both with corrections I needed. D1: the shrinking unproven pool is an
explicitly TEMPORARY C.1 limitation, `isProven` is NOT to be changed to conceal it, and mature-campaign
balance is not accepted until the lifecycle AND young-talent replenishment work completes — and these
are TWO dependencies, not one: retirement (§6.2) manages departures, replenishment (§6.5) creates
entrants. THE ACCEPTANCE CRITERION IS A DEMONSTRATION that mature campaigns retain meaningful access
to younger talent, not that retirement works. D2: the downgrade predicate stands unchanged, and "no V32
ancestor" is the chosen SUPPORT POLICY — not a technical impossibility, since `ageAtEntry` holds the
exact pre-C.1 float (measured 47.445061789257494).
**EXACT NEXT TASK: P14C.2 — RETIREMENT (companion §6.2), and its T0 comes FIRST.** C.1 moved the live
save to 33, so before ANY C.2 source change, mint genuine outgoing **V33** fixtures at the final V33
writer, on the slice rule this programme has followed since B.5 and the archived minter pattern at
`761-mint-v32-c1-corpus-minter.test.ts`. The five C.1 corpus axes are the starting list, not the
completion criterion: C.2's own axes must be MEASURED reachable by a probe before a minter commits
bytes (the 747/760 move, which has twice paid for itself by proving a planned fixture impossible).
C.2's first design question is the INHERITED AMBIGUITY carried from B.7 and B.8 and still open: `WAIVED`
requires propose-and-accept while `VOIDED` would follow automatically from a lifecycle event, and the
companion still lists retirement under BOTH rows. §6.2 confirms or rejects that reading; until it does,
the `VOIDED` branch of the outcome enum stays retirement-moot.
NEXT773 (2026-09-25, Opus coordinator under `OPUS-C2-TO-CODEX-LAUNCH`): **C.2 SPLITS BY DEPENDENCY** —
C.2a lifecycle core (announcement, notice, term cap, market exit, obligations first, finishing, alumni;
Save V34, projection 50 unchanged) → C.2b the single one-year extension → C.2c retirement × promises
(feasibility's retirement input, Owner direction 6, plus the held VOIDED/WAIVED disposition) → C.2-RM read
models. Expansion, decisions D1–D16 with their class, eight traps and the requirement→test map:
`../evidence/p14b4-20260919/773-c2-retirement-expansion.md`. THE AMBIGUITY IS NOT SETTLED BY AUTHORITY:
direction 7 and R14 fit either reading, so ONE question went to the Owner (773 §7, recommendation A:
VOIDED automatically at announcement only when the effective week makes the promise impossible). MEASURED
BOUND ON ITS REACH: every promise due week lies inside its contract (`promises.ts:415`) and E is never
before the end of the contract in force, so only the obligations-first greenlight refusal (`g + 9 > E`)
can truncate a promise window; C.2a leaves `promises.ts` untouched. T0 NEXT: probe, then mint genuine
outgoing V33 worlds before any C.2 source change.
SWEEP GENERALISATION ADDED BY C.1, for the V33 -> V34 step: **a value-keyed inventory does not find an
ENUMERATED ROOT-STRIP LIST**, because such a list must GROW by one line at every save bump and its
failure mode is an OMISSION rather than a stale value. Three sites are known and two are test-owned:
`src/core/save.ts` (chained, correct by construction), `src/harness/roster-wall/historical-control.ts`,
`tests/contracts/_v14Contract.ts`, plus `tests/p13b-r07-save-v25.test.ts` and
`tests/facility-move-demolish.test.ts` which C.1 also found stale.
CARRIED OPEN, unchanged by C.1: the 6,240-week endurance scenario has STILL never been run and NO
runtime cost has been measured by anyone (the scheduler correction closed a shape finding at source
level only; work is NOT proportional only to birthdays, since a birthday call still builds a `rank`
index and traverses `state.talent`). FU-1's `ui` reliability finding is still unreturned and the `ui`
project was not run. FU-2's disposition stays open at three of five. Unity/native verification remains
deferred; no native control exists for aging and none was run. Two repairs carry disclosed limits in
record 764: the opt-in `ageResidue` parameter on `tests/_p08HistoryTwins.ts`, and the
`p13b-s8-finance` provenance recast, whose fixture-mint alternative remains open.
LOGIC VERIFIED · UNITY NOT VERIFIED. Unity/native deferred. Gross2221 omitted fact cost is NOT
net saving: cold discovery can shift into next frame. No route-fit claim.
Draft-discovery, silent-policy, affine and Map alternatives remain unadopted.
T4/B2/B3/B-F2 qualified closed; B4/liveP2/Unity/Owner acceptance NOT closed.
Save29/rules3/projection46 live until coherent cutover; Unity backlog maintained.
Continue beyond checkpoint; max2 specialists/one writer/heavy checks serial;
no routine approval pauses/resets/installs/full-gate redo (older Codex-only/
no-Claude wording superseded for runtime choice only by Owner transfer 2026-09-20).

NEXT789: **P14C.2a IS QUALIFIED CLOSED** (record `../evidence/p14b4-20260919/789-c2a-checkpoint.md`, tested
source `c34b6674`, run 787 `fixedSource: true`). Save 33 -> 34 (root `careerLifecycle`), projection 50
UNMOVED, `generated/` unchanged. 361 files / 4179 cases: 62 failed = run 772's 55 retained with the same
cause + 7 CHANGED-SCENARIO failures traced in 788 (`p14b5-relationships` family 6 lost its natural "listed at
207 and 208" actor when four idle retirements redrew the rotation; the seed-b ledger lost one row to a rival
actor's hard-boundary retirement). They are coverage debt, queued as a labeled test-engineering task. FU-2 now
fails four of six. NEXT: **C.4 deterministic replenishment** before C.2b, by player impact (779: a fresh world
lost 63 of 84 people by week 1600 and the player's market has no entrant). Expansion 782 as amended §6 after
782-A: cohorts mint through `generateIndustryTalent` with an optional entrant age; the acceptance criterion
is the measured 6,240-week demonstration with and without cohorts, pass condition fixed in 782 §4/§6.2.
T0 first: genuine outgoing V34 worlds holding lifecycle records, minted before any C.4 source change. Then
C.2b (780 + §5, 780-A REFINE), C.2c after the Owner's VOIDED/WAIVED ruling (773 §7), C.2-RM, C.3.
LOGIC VERIFIED · UNITY NOT VERIFIED.

NEXT805: **P14C.4 IS QUALIFIED CLOSED** (record `../evidence/p14b4-20260919/805-c4-checkpoint.md`, source `e32c85b4`,
run 805 `fixedSource: true`). Save 34 -> 35 (`careerLifecycle.cohorts`), projection 50 UNMOVED. Once a campaign
year each film profession is refilled to the accepted founding composition (40/14/16/14), plus one entrant when
nobody active will still be under 30 at the next request; entrants 20-29, clip 32. The acceptance demonstration
(772 D1) FAILED on run 1 (799, the parent's at-instant youth predicate) and PASSED on run 2 on all six seeds
(800), including pre-registered 04-06 and one live-economy seed. F-792-1 (rivals insolvent from week 260 on 5 of 6
passive seeds) predates the C track and routes to P15's scope check. First endurance cost: 6,240 passive weeks
take about 17 CPU-minutes with a live industry, under 1 when it collapses. NEXT: C.2b (780 + 806 §8), save step
V36; T0 first.

Earlier additive foundation checkpoint: V30 data support implemented and independently
KEEP; qualified report evidence/p14b4-20260919/29-additive-checkpoint.md.
Corrected candidate bc25e15d…: root+UI and bridge typechecks PASS;137 save tests
PASS across12 full files;14 selected new schema cases PASS,22 unselected future
live-writer cases remain failed at their recorded boundaries. All sessions CLOSED.
Production-only patchbb0858bc… unchanged, source review21 KEEP; test-only unknown31
sentinels/strict outcome typing review31 KEEP. Earlier failures/evidence retained.
Capacity22 remains15 prerequisite FAIL, not reached solver proof (qualification23).
Live29/projection46/rules3 remain until coordinated behavior/wire cutover. B4 is
IN PROGRESS, not full-suite, live P2, Unity or Owner acceptance.
Additive implementation7022ade72a3f22ab555e225e36c528563eb6fba7 PUSHED and
independently exact-remote verified2026-09-19T22:19:59Z (B4/32). T0 a762 unchanged.
Then review/run directly reached material/evidence RED and release ONE bounded
sim-core implementation. Test-author prepares inert direct-core coverage; policy
v1 REFINE preserved before author-time/settlement observer correction. No heavy
runtime or production writer. Continue capacity/policy/bridge and remaining
P14/P15/P16/specified P17/P18 without routine permission pauses.
Active ownership/recovery: evidence/p14b4-20260919/00-start.md. Older notes below
are historical chronology, not instructions to repeat completed preservation.

B4 T0 preservation a76242f2f4bdfda98e38ec706e3110ad6a9bb957 PUSHED and exact
remote SHA independently verified. Both real corpora reviewed KEEP. Three reviewed
save/outcome/runtime tests installed with actual pins; bounded install review then
serialized RED next. No production writer yet. Active recovery: B4/00-start.md.
This supersedes the older publication-next wording below; c06 remains the producer.

**P14B-F2 CLOSED — LOGIC VERIFIED · UNITY NOT VERIFIED**, qualified by
../evidence/p14bf2-20260919-final/17-attribution.md; not all-green or Owner acceptance.
Full tested/published source89b5ad2cfc6947ea07fb043ef5b23eba38d7dbde, after
independent RED957d2de13cef957cf97ee8e3dd746b51d0e9d200. Whole run
2026-09-19T19:19:14.237Z–21:00:23.363Z,101.1521minutes,fixedSource:true.
Session20565 CLOSED. Current B4 T0 runtime/ownership: evidence/p14b4-20260919/00-start.md.

Core3351PASS/22FAIL/6unchanged TODO(309files); bridge761PASS/12FAIL/2unchanged
TODO(72files). Overlapping selections, not unique summed tests. Every failure
matches the full historical identifier AND diagnostic exactly; no new/changed/
absent failures, Failed Suites or unhandled diagnostics. Both typechecks/contracts
PASS; independent source04/core14/final16 KEEP. Core took about12% longer than B3;
matching timeouts do not prove their causes or uncompleted assertions passed.
Historical missing fixtures/PIL/golden mismatches remain qualified, not repaired.

Production ONLYpromises.ts eligibility/digest/fresh evaluator3, tests unchanged
after independent RED. Committed/tested patch9145160c243937f2eae0abd119a670b60f8a2690dc7f0b42e2c7aef6777c14cc.
Candidate36files404PASS/5unchanged TODO plus checks are preserved separately in
../evidence/p14bf2-20260919/11-candidate-disposition.md. No assignment, candidate-pool,
rival-policy, chooser, save/schema or historical-root/receipt rewrite.
Save29/protocol4/projection46/schema584bdd… unchanged.

Qualified B-F2 records c06db6eae2a1350317c018c6f108d115dcba7b19 PUSHED and
EXACT remote SHA verified. Both B4 T0 genuine corpora now completed on that SAME
producer; independent artifact reviews06/08 KEEP. Exact T0 proof09-t0-closeout.md.
Failed runtime02 retained; corrected canonical-writer05 passed. Recorder output-
only fixedSource:false qualifications retained, producing inputs unchanged.
Temporary operational files removed after exact-byte archival; no heavy runtime
or production writer. NEXT: commit/push/exact-remote-verify preservation, then
independent B4 RED before sole sim-core release. Never remint either actual corpus.

Reviewed P2 guidance: P14B4-HEADLESS-PLAN.md SHA382252e23b6353acf602d87f38032ff961e9f7f9740bbfdf2b2ae368c30df4e4.
Candidate BF2/12–14 record expansion/actual-owner reviews, not implemented solver
certification. Independent save/outcome drafts preserve their unexecuted fixture/
coverage limits. Continue P2/rest P14/P15/P16/sufficient P17/P18, no routine
permission pause. Max2 native specialists, ONE production writer, serialized heavy
tests; no Claude/install campaign. Unity/native/rendering/Owner acceptance deferred.
Earlier checkpoint notes below are historical, not current worker instructions.

**Historical checkpoint (2026-09-19): B3 qualified CLOSED.** Tested full source359f636,
implementation425170e, genuine preservation9c605dc and independent REDf4e1230.
Full16:52:49.457Z–18:27:20.807Z, fixedSource:true,94.5225minutes. Core3338PASS/
22FAIL/6TODO; bridge761PASS/12FAIL/2TODO. Exact historical IDs+diagnostics only;
both typechecks/contracts PASS; independent core/final reviewsKEEP. Controlling
closeout `../evidence/p14b3-20260919-final/17-attribution.md`, LOGIC VERIFIED ·
UNITY NOT VERIFIED. Not all-green, whole-P14 or Owner acceptance. Rules2/Save29/
projection46 unchanged. Session26407 CLOSED, no runtime/writer active. Publish
qualified records/verify exact remote, then B-F2 independent RED per evidenceB3/16.
B2 qualified source7f89f75/publication034065b and B1/T4 closure remain preserved.

**Historical B1 closeout (2026-09-19).** P14B.1 T4 CLOSED — LOGIC VERIFIED · UNITY NOT
VERIFIED. Recovered T3 `e78dbc0` preserved; corrected production `ee91913`, final
test corrections `a93bf7776a41df3702c78ae5225a2e7c303ec9da`. Full frozen core:
3253 passed / 29 failed / 7 todo; bridge: 717 passed / 12 failed / 2 todo. Exact
historical diagnostics account for 22 core / all 12 bridge failures. Seven new
core test-compatibility failures plus one typing error corrected without changing
production/validators/fixtures; fresh 13-file run 170 passed / three unchanged
todos, both typechecks and both contract checks GREEN, independent KEEP reviews.
Qualified attribution: `../evidence/p14b1-20260919-t4-final/16-attribution.md`.
Not an all-green full-suite rerun or Owner acceptance. Save V29 / projection 45.
B.2's upstream gate is satisfied; its independent RED/fixture work is next.
The historical task details below remain provenance, not current worker status.

**Status (2026-09-18).** DRAFT v1 written after P13B S8's engine and bridge landed and before its matched pass closed; AUDITED (contract-auditor, read-only, ≈06:40): two blocking items (T0 gated on the S8 close; the Identities row) and six should-fix adopted in place, two coverage tests added (the `staff()` exclusion; the firing disclosure), one P14B flag (first take, not Shooting entry). P14A.1 may begin only after the S8 close commit is named in T0. Engine implementation and verification only; Unity/rendered UI/native/visual acceptance
deferred and never claimed verified; every engine change creating client work is recorded in `../UNITY-INTEGRATION-BACKLOG.md`.

## 1. Authority and inputs

- Owner directive `OWNER-HEADLESS-PROGRAM-20260916-01` (logic-first): headless P13B → **P14** → P15 → P16 engine work proceeds now; later
  corrections override older research. The three-week autonomous directive (2026-09-15) names the P14 preparation index at
  `The-Movies@8ef5246a docs/engineering/P14-PREPARATION-REVIEW-INDEX.md`.
- The P14 preparation package, retained verbatim in `../../p14-preparation-8ef5246a/` (README states provenance): the review index, the
  post-P12 reconciliation (REUSED 57 · SHARED GENERALIZATION 19 · NEW P14 WORK 17 · POST-P13 REFRESH REQUIRED 14), the preparation
  companion (P14A/B/C design, firing note, promise catalogue, relationship model, lifecycle model, decision register §7), the launch-review
  skeleton, and the Owner rulings record §3.4 (fourteen OWNER-SELECTED PRODUCT DIRECTIONS, superseded statements).
- The package's own status ("PREPARATION READY — POST-P13 ACCEPTANCE REFRESH REQUIRED · IMPLEMENTATION NOT AUTHORIZED") predates the
  2026-09-16 directive; this plan performs the post-P13 refresh (§2) against the actual P13B closeout and proceeds under the directive.
- Companion `02-P13B-DECISIONS-AND-ACCEPTANCE.md` / `03-PAPER-ECONOMICS.md` remain off disk (recorded in `CONTINUATION-STATE.md`); P14's
  acceptance wording is drafted from the package and must be re-checked when the Owner side re-supplies it.

## 2. Post-P13 refresh (reconciliation §3), against the P13B closeout

| P13 seam | P13B fact now in force (branch `wip/headless-program-20260916-ts`) | Consequence for P14 |
|---|---|---|
| Era / timeline facts | `campaignDate(week)` (`src/core/calendar.ts`) is the only public era/date law; technology milestones are catalogue data (`researchableWeek`, `commercialWeek`, S7 `publicWindow`); no era root was added | P14 reads `campaignDate` for cohort ages and profile copy; no era symbol to refresh |
| First-filming seam | S5-R07: a production enters Shooting when its setup units are credited (`ProductionWorkflow.setup`, `setupAdmitted`/`setupCompleted` events); the first take is the first `ShootingTask` after entry | P14B's promise qualifying event and the relationship shared-work boundary read the FIRST TAKE COMPLETING (the 5 → 4 advance, companion §4.2/R12), which now follows the S5-R07 setup-gated Shooting entry — not Shooting entry itself and not `phaseEntered` alone (flag for the P14B expansion) |
| Stage durations and capacity | `FACILITY_BLUEPRINTS` (20 entries incl. `restoration-*`, `office-conversion-*`, technology installations); Laboratory capability with 4 seats; installations and cancellations (S6) take bodies offline | the feasibility service (P14B) reads live capability/offline state through `occupancy.ts`; durations from the blueprint table |
| Professions | `CreativeRole = FilmCreativeRole | 'scientist'`, `Discipline = FilmDiscipline | 'research'`; Scientists employ through the shared contract law (P13A), rivals hire them through `staff()`'s scientist slots (S8) | the Scientist joins market eligibility, aging and retirement; its retirement window and market rule are OPEN (companion §7.3-adjacent; no Owner text) |
| Rival movements | `RivalMoneyKind` = 14 kinds (P13A `technologyAdoption`; S8 `researchSpend`, `researchCapacity`, `technologyRestoration`, `technologyRefund`); period exact-key validation | P14's `termination` rival kind lands in its own governed save step, as the package foresaw |
| Persistence | live save **V27** (`LIVE_SAVE_VERSION`), bridge projection **41**, schema `sha256:16b84322…`; frozen chain V12–V26 with positive projections (`projectPlacementPreV26`, `projectHollywoodPreV27`) and policy threading | P14A.1 allocates **V28 / projection 42** at execution; genuine V27 fixtures are minted at the final V27 writer before any P14 source change |
| Identities | the S8 close commit `e0676f9` and its matched-pass attribution (`evidence/p13b-s8-20260918/23-attribution-ef9ff76`; P13B progress table) | cited by the P14A.1 records once it exists; no Owner acceptance of P13B exists (LOGIC VERIFIED · UNITY NOT VERIFIED only) |
| Process-global caches | one pre-existing module-level memo in `src/core/hollywood.ts:50` — `employmentByPerson`, a `WeakMap` keyed by the employment ARRAY instance (object identity, GC-safe), not by seed, `worldId`, `PersonId`, `caseId`, `contractId`, receipt id, `campaignId` or `sessionId`; no other module-level `Map`/cache binding in `src/core` (grep 2026-09-18); every P13B reducer is copy-on-write | admissible under the reconciliation's rule (identity-keyed, not id-keyed); P14 adds no seed/id-keyed module state and does not extend this memo |

## 3. Slice order

P14A.1 **One Contested Expiry Core** (companion §2.1.10) → P14A.2 workspace read models → P14A.3 world route facts → P14B promises/relationships
→ P14C lifecycle. Each slice: expansion written before it begins; genuine fixtures of the outgoing save version minted at its final writer
before any source change; the next governed save/projection versions allocated at execution; RED-before-implementation; specialists
(sim-core, test-author, contract-auditor read-only), max two concurrently; matched pass, attribution against the inherited set, LOGIC
VERIFIED · UNITY NOT VERIFIED label; backlog entry.

## P14A.1 — One Contested Expiry Core — task expansion (draft; audit before it begins)

**Settled law (rulings §3.4.1, by number; not re-asked):** 1–4 (firing as recalibration; the 26-week-capped charge; anti-exploit guard;
market eligibility: free agents and the renewal window only, no in-term poaching, no buyouts), 8 (no agent), 14 (upstream ownership: P10
person/contract truth, P12 employer/exclusivity/intervals, P11 money).

**Engine today (verify at T1).** `signContract {talentId, termWeeks}`, `renewContract {talentId, termWeeks}` (immediate replacement inside the
window), `releaseTalent {talentId}` (D-11.9 charge, `termination` ledger kind); `renewalWindowOpen(terms, week)` with
`TUNING.HIRING_RENEWAL_WINDOW_WEEKS = 12`; rivals renew their own people on the window's first week and never propose (`staff()`); the salary is
the person's ask (`offerForTalent`), no player money lever; contract terms of rival-employed people are private on the wire.

**Scope (companion §2.1.10).** One immutable `PersonId`, two entered studios (player + one rival present at week 0), one market case per
eligible expiry (`discovered → proposals_open → decision_pending → settled | declined | expired | invalidated`), at most one current proposal
per studio, the symmetric premium tier on both proposals — a small TYPED/ENUMERATED tier, not a continuous input (companion §2.1.4 hypothesis {1.00 … 1.25}; the values OPEN), P10 draft references with version invalidation, the rival
proposal trigger inside `nextDecisionWeek`, case-aware admission — `applyRenewContract` on a cased person is REFUSED with a new typed refusal `underMarketCase` that redirects to the proposal path (companion §2.1.3/R6), and `staff()`'s existing renewal loop (`hollywoodTick.ts` ≈95–111, which runs BEFORE `decide()` in the same weekly pass and would otherwise auto-renew a rival's expiring person for 208 weeks in the discovery week) EXCLUDES case subjects — the exclusion lands with the case-open check, not with settlement (the second bounded `staff()` edit, companion §2.5), the terminal atomic settlement at the subject's `endWeekExclusive` (derived on read), appended in `tick.ts` AFTER `finishHollywoodWeek(finishTechnologyWeek(finalized))` (the pipeline's last call, on the already-incremented week; companion §2.1.8) — never inside `advanceHollywoodWeek`/`decide()`, which run pre-increment, the disclosure table (§2.1.5: existence/subject/decision week/competing term public; premium/salary/bonus UNKNOWN; ranking reasons order-only after settlement) — a NARROWING of today's blanket rule that rival contract terms are private (`bridge/industry.ts` ≈239 'Contract terms are kept private in Industry.', `bridge/people.ts` ≈516 `contract: null`): the case block discloses exactly the listed fields and those two call sites keep every other term private (test 6 asserts both halves), receipts for discovery, proposal,
withdrawal, settlement, invalidation; free agents stay instant-sign.
- Persisted facts: a `talentMarket` root (cases, proposals, receipts) — **Save V28**; a `termination` rival money kind if rival firing is
  reached (deferred: companion §4 "Rival early termination — Ready"), else not in this slice.
- Bridge (projection 42, text only): profile case block (case status, decision week label, own proposal, competing proposal's known terms
  and UNKNOWN markers, settlement receipt reasons), intent kinds for propose/withdraw, attention rows for the five interrupt causes.
- SETTLED by the package (not OPEN): the anti-exploit guard is companion §3.5/§3.6 recommendation **R1** — a persistent salary floor toward the releasing studio, no new persisted fact, alternatives (cooldowns) rejected; only its status as a Future-Ops-reviewable recommendation remains, not its form. Direction 8 (no agent) is realised by a `representation` key required and pinned `null` (R10).
- OPEN (recorded, not resolved): the premium tier values and the rival's trigger policy numbers; Q1–Q4 of companion §7.3 (none blocks A.1); the Scientist's retirement window and market rule; acceptance wording.

**Tests (RED-first, against `src/core/talentMarket.ts`).** 1 eligibility table (six states; reserved rivals cannot propose; in-term
approaches refused). 2 case lifecycle and derived decision week; invalidation triggers. 3 proposals: one current per studio, premium tier
bounds, draft reference invalidation, affordability gates (`canAfford` for the player; the rival's reserve). 4 rival trigger inside `nextDecisionWeek` (pure policy, receipt-backed) AND the `staff()` case-subject exclusion: a rival's expiring person under an open case is not auto-renewed in the discovery week, so the rival contest of the Core pass sentence is reachable. 5 atomic settlement: deterministic ranking, exactly once, receipts, incumbent renewal as a
proposal, order-only reasons. 6 disclosure (UNKNOWN markers; nothing private leaks). 7 firing: the 26-week cap, the anti-exploit floor (R1), AND the confirmation's disclosure content per direction 2 (remaining duration, remaining guaranteed compensation, the cap where it applies, the exact charge, the effective end, the other authoritative consequences — the two copy branches of companion §3.4).
8 Save V28 with genuine V27 fixtures; validator refusals incl. the `representation` key required and `null` (R10). 9 bridge projection 42.

### P14A.1 tasks
- [x] **T0 Genuine V27 fixtures (2026-09-18 ≈06:40, minted at `e0676f9`)** — gate satisfied: S8 closed at `e0676f9` (label + attribution), the final V27 writer `ef9ff76` unchanged since; `src/harness/p13b/legacy-v27-fixtures.ts`: `legacy-v27-natural-rival-labs-20.json.gz` (sha `00183e1c…`, week 20: every rival's `laboratoryCommitted`/`laboratoryOperational` receipts, `researchCapacity` movements) and `legacy-v27-renewal-window-456.json.gz` (sha `af659268…`, week 456: the research-ready world with a player Scientist contract inside its 12-week renewal window, rival research and adoption facts present, no market root); provenance in `tests/fixtures/p13b/PROVENANCE.md`; evidence `p14a1-20260918/00`.
- [x] **T1 Tests 1–8 RED-first (2026-09-18 ≈07:20)**: `tests/p14a1-{eligibility,case,proposals,rival-trigger,settlement,disclosure,firing,save-v28}.test.ts` (5 + 6 + 7 + 2 + 1 + 3 + 5 + 7 = 36 cases), each RED at module resolution of `src/core/talentMarket.ts` (evidence `01`–`08`; engine facts probed, not assumed: rows 1–4 entered / 5–9 reserved at week 0, the window at 196 for a rival's 208-week founding roster). Named coverage limits (not conflicts): P14C states and the retirement trigger unreachable pre-P14C; the founding-draft exclusion has no live path; tier values OPEN (1.00 floor, monotonicity, below-1.00 refusal pinned); rival trigger branches (b)/(c) not fixture-tested; tie-order settlement not exercised (numerical hypothesis); R1's downstream pricing effect not exercised; R2 out of test 7's text. **Adjudicated (coordinator):** the V27→V28 lift never fabricates a case (R22: no fabricated pre-P14 facts) — a subject already inside its window at load is DISCOVERED by the next tick's discovery step, one week after load; the save test's empty-root reading stands. Sim-core dispatched (T2). - [~] **T2 Engine increment (sim-core, landed `d49cc27`, 2026-09-18 ≈08:50):** Save **V28** (`talentMarket` root {cases, proposals, receipts, `representation: null`} at top level, stripped before the frozen V27 chain; `projectTalentMarketPreV28` asked before `validateSaveV28`; 13 downgrade guards; sentinel 29; the lift always empty, never a fabricated case), `src/core/talentMarket.ts` (1,015 lines, pure, no RNG: eligibility over the employer facts, cases keyed to the P12 `contractId` with the decision week and status DERIVED on read, enumerated-tier proposals with P10 drafts/digests, the rival trigger a/b/c inside the rival's decided week, atomic settlement appended after `finishHollywoodWeek` — freeze, drop, deterministic ranking (D1/D2/D6/D7; D3–D5 omitted, not faked), commit through the accepted owners, one chooser receipt with order-only reasons — `releaseFloor`/`releaseDisclosure`), `terminationCost = weekly × min(remaining, 26)` (`HIRING_TERMINATION_CAP_WEEKS`; the 0.5 fraction retained HISTORICAL), `underMarketCase` after the unchanged D-11.7 check, `staff()` skips case subjects for the whole open span, the market engages iff `hollywood !== null`; sweep 27→28 over 62 test/ui files (classes listed in evidence `08`; no roster/bound moved). P14A.1 37/37; S1–S8, p13a, employment/contracts, d12, roster-wall, sweep batches green; root typecheck clean; `typecheck:bridge` 21 errors (the live-version move, T3). **Rulings (coordinator):** the two D-11 pins corrected by sim-core in `tests/d11-employment.test.ts` CONFIRMED (directions 1–2 repeal the 50% charge; R6 replaces in-window renewal for a cased person); `p13a-rival-adoption` case 46 is a test premise falsified by the pinned law (the rival roster now settles at 208 instead of auto-renewing at 196, so the 416 period pre-exists the sound purchase) — test-author re-expressing it; **R1 must govern the player's `signContract`/`renewContract` pricing** (direction 3 is settled; sim-core's pricing-entry-only application leaves E1 open) — RED case then wiring; **R4 legacy terminations IN scope** (the global `terminationCost` change makes every frozen reader refuse a genuine old-law termination — a persistence hazard) — RED case then a law-versioned reconciliation with a `legacyTerminations` record on the root; **R2 DEFERRED** (release refused for the busy set / founding; companion recommendation not in A.1's plan text) — recorded for a follow-up. Named limits stand: `--project ui` not run (typecheck-clean only); weekly-advance latency unmeasured (`ponytail:` note names the PersonId-index path). Evidence `09` (tests), `12` (bridge live-version sweep) in flight. **Landed after (2026-09-18 ≈09:20–10:40):** test tasks `7f19228` (`p13a-rival-adoption` re-expressed 2/2; RED cases: R1 on the player's `signContract` path — expected 656,156, got the floor-free 546,797; R4 — an old-law V27 termination refused by the frozen chain: 'employment termination lacks its actual player payment'; evidence `09`); bridge/ui live-version sweep 27→28 `c972f99` (no projection change; typecheck + typecheck:bridge clean; named bridge files 44/44; evidence `12`). **Settlement defect (sim-core diagnosis on seed `p13b-s8-bridge-probe-01`, week 208):** `survivesFreeze` froze the submission-week PRICE into the reservation check and the digest, so ordinary fame drift drops every proposal — all 24 cases DECLINED ('no proposal cleared this person's reservation'); the rival research chain slid ≈5 weeks (`technologyAdopted` 293 not 288; the `bridge-p13b-s8-rivals` regression case). **Ruling (coordinator; companion §2.1.4 line 70, §2.1.7 line 104):** the version digest covers material terms only `(talentId, issuerStudioId, termWeeks, startWeek, premiumTier)`; price is derived, never material; settlement re-derives `annual = round(ask(W) × tier)` and the bonus through `studioOffer` at W and commits those; reservation, ranking, affordability and the commit read the W re-derivation; the stored proposal price is the submission-week QUOTE, never read at settlement. RED cases LANDED `8495733` (test-author: `tests/p14a1-settlement.test.ts` cases A/B — A `expected 'declined' to be 'settled'`, B digest `6cd626e6…` ≠ `031fa0e5…`; evidence `13`; the existing dominant-incumbent case still green). PREMISE DECLARED by the test-author (not a conflict): an idle signed actor's fame never drifts naturally on either seed (fame moves only through the cast/reception path), so drift is induced by a direct copy-on-write fame edit after submission — the law under test is the settlement re-derivation, not the drift source. Sim-core LANDED three commits: **R1 `c25afd6`** (`studioOffer` returns a full `ContractOffer`, floor before any tier, bonus at the published fraction on the floored annual; new `playerOffer`/`playerOfferOptions`; `applySignContract`/`applyRenewContract`, the bridge quote/draft/decision surfaces, the ui adapter option lists and `castingPackageReadModel` all route through it; `contractOffer` stays the pure market ask; no cycle, no DTO change; firing + save-v28 15/15; d11/p13a-employment/p12/S8-save-v27/contracts 150/150). **R4 `ad35a81`** (`legacyTerminationCost` 50% law, `TerminationLaw`, `PRE_V28_TERMINATION_LAW`; the law travels beside the stripped state through the frozen chain as `plans` does; `convertV27ToV28` records every player termination from the ledger's actual charge, fail-loud if absent; `validateTalentMarketRoot` enforces row/week/amount/uniqueness; the V28→V27 downgrade stays lossless). **Settlement `a2d59e0`** (digest = material terms only; one `proposalPriceAt(state, proposal, W)`; freeze, ranking, commit and `caseDisclosure` on the W price; `submitProposal` keeps the submission-week quote for its own affordability check; settlement 3/3, p14a1 41/41). **Rulings CONFIRMED:** the option-list quote surfaces belong to R1 (a quote at 546,797 against a charge of 656,156 would lie); the frozen V≤27 chain accepts EITHER the 50% law or the cap law (a V27 envelope is also the downgrade projection of a live V28 campaign and the V27 shape cannot carry the era; any third number is refused). **OPEN after the fix (measured by sim-core, evidence `14-settlement-baseline-comparison`):** on the probe seed the first cluster now reads 11 settled / 13 DECLINED (was 24 declined) and rival r01's `technologyAdopted` stays at 293 with and without the fix — the 288 guard in `bridge-p13b-s8-rivals` is NOT restored; `chooseProposal` returns null only for an empty survivor set, so each decline is a `survivesFreeze` drop hidden behind the fixed text 'no proposal cleared this person's reservation' (companion §2.1.7 requires typed drop reasons — a disclosure gap, recorded). **Diagnosis (sim-core, evidence `14-settlement-decline-diagnosis`, 356 lines, self-checked against the engine's own 24 outcomes): NOTHING IS DROPPED** — all 96 rival proposals (four rivals × 24 cases, every case decided at 208) pass every freeze predicate with multi-million headroom; the 13 declines are `chooseProposal`'s terminal decline-all reached with a full legal survivor set, because `rivalPremiumTier` discriminates on two bits (incumbent; `reserveWeeks ≥ 16`) so challengers share exact descriptor vectors, D3–D5 are neutral in P14A, every rival submits at 196 for 208 weeks, and the incumbent bids the LOWEST tier (r01/r04 at 1.00 are dominated on compensation and hand all 12 of their people to an unbreakable challenger tie; r02/r03 at 1.05 tie challengers and win on incumbency = the 11 settlements; the 13th is a three-way tie on a `term`-first priority order). **Ruling (coordinator):** decline-all is the companion's own terminal (R8; §2.1.7 line 120 'then by earliest submission week, then by incumbent-if-present, then decline-all'), so the ranking is LAW-CONFORMANT and not a defect; the two-bit rival tier policy and the incumbent-bids-lowest rule are the recorded OPEN rival-trigger hypothesis (isolated in the rival policy constants) — the 54% decline rate on this seed is documented for the Owner as a product consequence, NOT tuned here (no invented numbers). **DEFECT (fix in scope):** `closeCase` stamps every decline with 'no proposal cleared this person's reservation' — false for all 13 (a tie exhaustion and a reservation failure are indistinguishable in the record; §2.1.7 requires typed reasons) — RED case then typed decline reasons (tie exhausted vs dropped, each drop's predicate). **288→293 is the market existing at all** (all 24 rival contracts expire at 208 and only 11 re-sign; `staff()`'s case-subject exclusion suppresses the old renewal path from 196) — `bridge-p13b-s8-rivals`' natural-week guard and `p13a-rival-adoption`'s 416 pin are re-expressed ONCE, test-side, against the landed law `a2d59e0`. **LANDED `1f3fcd6` (test-author):** typed decline reasons RED in `tests/p14a1-decline-reasons.test.ts` (A tie exhaustion — a genuine natural tie between r01/r02 at Standing 47.5/48.1 inside the band tolerance — `expected 'no proposal cleared' to be 'tie exhausted'`; B all dropped — the rival's account drained by a declared edit right before the settling tick — `… to be 'all proposals dropped'`; evidence `16`); `bridge-p13b-s8-rivals` fixture rebuilt at the measured natural week 293 (17/17); `p13a-rival-adoption` re-expressed to walk the chain from the catalogue's `commercialWeek` 416 to the first `route: 'purchase'` adoption (2/2; evidence `17`). **NEW FINDING (test-author, not repaired):** under the market the first commercial sound purchase on the p13a seed lands at week 520 by a LATER-ENTERING fifth rival; none of the four founding rivals can afford the $1,475,000 purchase any more — r03/r04 cash strictly negative and worsening (≈−16M/−8M by week 420). Diagnosis (sim-core, evidence `18`, pre-market baseline in a throwaway worktree at the S8 close `ef9ff76`): **the collapse is INHERITED** — at week 416 on that seed the S8 close already had r03 −14.7M / r04 −9.6M at roster 0 with no market in the build (HEAD: −16.2M / −7.8M); the market's whole effect on r02 is payroll +419,824 (weekly 96,779→101,617 = +5.0%, the premium tiers) and signing +93,490, every other movement kind byte-identical, which costs r02 the §48 purchase gate by 348,487 (it stood 251,911 clear before), so the first purchase falls to a fifth rival at 520. H1 duplicate payroll REFUTED (roster and signing flat 204–207); H4 double charge REFUTED; H2 tiers CONFIRMED and decisive; H3 bonuses CONFIRMED, small; H5 challenger shuffle CONFIRMED and large (r01 wins 12 at 208 — its 6 and all 6 of r04's — roster 6→12→16→28 by 420, payroll 50,756→267,276; r04 refills at 216 for 339,439). **Rulings:** the inherited rival insolvency is a P13 finance balance fact recorded OPEN for the Owner (rival receivership is not authorized; nothing here); H2/H3/H5 are genuine behaviour under the pinned law and the OPEN rival tier hypothesis (a contested re-signing costs more than an uncontested renewal); the re-expression header's premise 'P13B-S8 did not have this trajectory' is FALSE and is corrected test-side. **DEFECT (fix in scope, RED first):** `commitRivalWinner` appends employment rows with NO seat budget and nothing releases a surplus, so a rival that wins challenger cases grows past `RIVAL_TEAM_ROLES` without bound; at a synchronized expiry (a founding cohort on identical 208-week terms) trigger branches (b) and (c) fire for every case in the industry BY THE COMPANION'S OWN TEXT (§2.1.x 'holds a same-role contract ending at or before the effective week' / 'seat deficit at the effective week'), so the trigger is law-conformant and the missing piece is the legality predicate the companion places at freeze: a proposal is DROPPED when the issuer has no seat for that role at the effective week (rows held after W plus wins committed earlier in the same fixed-order weekly pass), typed reason naming the seat — a seventh `FreezeDrop`; the resulting dependence on case order (a challenger win can consume the seat an own-person case needed) is recorded OPEN for the Owner (own-person priority is a product choice, not invented here). **Seat-budget RED LANDED `26a63a6`** (test-author; `tests/p14a1-seat-budget.test.ts`: A natural synchronized expiry on seed `p13-public-commercial-adoption` — at week 209 r01 holds 12 {writer 2, director 2, actor 6, craft 2} against the six fixed seats {1,1,3,1}, `expected 2 to be less than or equal to 1`; every other rival within cap; all 24 cases SETTLED on this seed; B a rival already holding its 3 actor rows through W still wins the player's expiring actor, `expected 'settled' to be 'declined'`; C the player is NOT seat-budgeted — green now, guards over-reach; evidence `19`); the `p13a-rival-adoption` header premise corrected (comment-only, verified) to the inherited-insolvency finding. **Ruling on disclosure of drops:** every case-terminal chooser receipt carries `dropped: string[]` (one CANDIDATE sentence per dropped proposal, naming the studio and its predicate; empty when nothing dropped) on SETTLED receipts too — without it a player whose own proposal is dropped on a settled case never learns why (the receipt is the record); `reasons[]` stays the winner's order-only list the bridge pins. Save V28 receipt shape widened in place (no V28 fixture exists). **Seat budget LANDED `daaf95f`** (sim-core: seventh `FreezeDrop` `noSeatForRole` — `seatsHeldAfter` counts the studio's active rows for the role with `endWeekExclusive > W`, which already includes the wins committed earlier in the same pass because a commit writes a row ending at W + term; the subject's own row is closed at W before settlement so an incumbent always has its own seat back; player exempt; `dropped: readonly string[]` on every case-terminal receipt, defaulted `[]` in `appendReceipt`, validator-checked; one drop list for both outcomes; 117/120 over 24 files; both typechecks exit 0; evidence `20-seat-budget-*`). **Measured after the fix:** all four rivals exactly {1,1,3,1} at week 209 (r01 was {2,2,6,2}); 36 challenges seat-dropped at 208, every incumbent keeps its six (own-person cases are processed first in discovery order, so each rival refills its own seats and is full for every later challenge); r01 at 416 roster 28→10, cash 37.9M→52.0M; r02 byte-unchanged (still −348,487 from the §48 gate — the premium-tier effect of evidence `18`), the first purchase stays at 520 by r05; on the probe seed r01's natural `technologyAdopted` RETURNS TO 288 (the rebuilt S8 fixture at 293 still finds it — its '293' wording is now stale and is restored to 288 test-side). **OPEN product consequence recorded for the Owner (not tuned):** under P12's weekly deficit filling a rival's seat is free only between a lost own case and the next `staff()` pass, so challenger proposals almost always seat-drop at a synchronized expiry and contests arise mainly when a case precedes the challenger's own expiring row in the fixed order (the companion's fixture (ii) depends on that order); a seat reservation for a pending challenge or the §3.6 release path would change this and are not invented here. **Three test-side premise conflicts (adjudicated):** `p14a1-seat-budget` A's secondary assertion reads `reasons[]` for the seat sentence — it must read `dropped[]` (the disclosure ruling post-dates the test); `p14a1-decline-reasons` A/B hand-submit a rival challenge on the player's week-52 actor and every founding rival is full through 52, so both are seat-dropped before the tie/affordability they test — re-expressed with a seat-free issuer (construction and premises declared by the test-author; the claims unchanged). **Re-expressions LANDED `dc58e51`** (test-author): seat-budget A reads `dropped[]` + new case D (a full rival's challenge seat-dropped with the sentence in `dropped[]` while the case settles to the player) — 4/4; S8 rivals `WEEK288` restored with the 293 wording reverted — 17/17; decline-reasons B re-expressed at the synchronized expiry 208 (the first entered rival's cash drained to 0 before the settling tick: it loses its own six-seat defence in the same pass, is genuinely seat-free, and fails affordability on the subject's bonus while the three solvent rivals are seat-dropped — cardinality four proposals / one bonus-typed sentence, declared) — green; **decline-reasons A NOT SATISFIED under the final law:** with `staff()` refilling every freed seat in the same tick before the freeze, no pair of entered rivals is simultaneously seat-free, solvent and Standing-tied (>90 seeded probes; the one Standing-tied pair fails on reserve alone) — RULING: the claim stands unweakened as `it.todo` with the obstacle in its name (the tie-exhausted typed reason was last exercised GREEN at `b3ad486` before the seat budget; a follow-up constructs it under the final law with three computed cash drains at a synchronized expiry — recorded coverage limit, evidence `21`). **Typed decline reasons LANDED `b3ad486`** (`FreezeDrop` union of six predicates, CANDIDATE sentences, `chooseProposal` → `ProposalChoice` with `tiedCount`, `publicPriorityOrder`/`publicPreferredTerm`/`DescriptorKey` exported for T3; 62/62 across 11 files; evidence `14-typed-reasons-*`). **In scope, test-side at T3:** the 5 `bridge-p10a-r1-contract-quote` cases pin the repealed in-window `renewContract` (`underMarketCase`; R6 replaces it with the incumbent's proposal) — the bridge test-author re-expresses them with test 9. **R1 ruling:** both player actions price through `studioOffer(state, playerStudioId, …)`; the player-facing quotes in `bridge/contract.ts` and the ui adapter show the same floored price; `contractOffer` stays the pure market ask. **R4 ruling:** era-versioned termination law — the frozen V≤27 chain validates player terminations under the historical 50% law (`HIRING_TERMINATION_FRACTION`, retained for this); the V27→V28 lift records every player termination as `talentMarket.legacyTerminations [{contractId, endedWeek, amountPaid}]` from the ledger's actual charge (no back-charge, no refund, cash untouched); V28 validates listed contracts against `amountPaid` and all others under the cap law; **Save V28 widened in place** (no V28 fixture exists and the version has not closed; the two positive empty-root pins move with it). Sim-core dispatched on R1 + R4 (evidence `14`); the settlement fix is gated on the RED commit. Records note: this plan file was committed EMPTY at `9bd2ce3` (a truncating records write; 73 lines deleted) and restored from `a4e7e25` in the commit carrying this sentence.
- [~] **T3 Bridge projection 42** — test 9 RED LANDED `f1b7132` (test-author, 2026-09-18 ≈13:05): `tests/bridge-p14a1-market.test.ts` 15/15 RED (3 real assertions — `PROJECTION_VERSION` 41→42, `AVAILABLE_INTENT_KINDS` lacks `marketProposalAction`, `contractActionDecisions` still reads `renewAvailable: true` for a cased person; 12 on the two named not-yet-existing bindings `marketCaseProjection` from `bridge/people.ts` and `marketProposalDraftToEngine` from `bridge/contract.ts`; 0 harness errors; evidence `10`); `tests/bridge-p10a-r1-contract-quote.test.ts` re-expressed under R6 — R0/R2/R3 untouched green, R1/R4/R5(re-price)/R6(solvency→affordability)/R7 moved to the incumbent's proposal path (RED on the missing binding), R5/R6 release-verb sub-claims green, new case: in-window `renewContract` refused with the `underMarketCase` reason verbatim (green; evidence `11`). PREMISES NAMED (coverage limits, not conflicts): two of the five Attention causes not exercised (the stop-horizon threshold; 'terms revised after review' — the review read-state is not persisted in A.1: the implementer derives the row from a competing proposal's revision, and the limit is recorded); the public preference descriptors need a pure engine read accessor (`publicPriorityOrder`/`publicPreferredTerm`, folded into the typed-reasons commit — no law change); the settled case's folded industry row pinned ABSENT (A.2); R4's session-level reasonCode for the new intent not pinned (the accepted stale-revision law applies). NOTED, unrelated and pre-existing: the live in-memory `perceived` genre-experience value can carry IEEE `-0`, which any JSON round trip flattens to `0` (the save round-trip case normalises both sides). `typecheck:bridge` intentionally 2 × TS2305 (+5 cascading) until the implementer lands. **Projection 42 LANDED `562cdf2`** (bridge sim-core, 2026-09-18 ≈15:10; 28 files; schemaId `sha256:a3f8b03c00eee8e7c18b7f7c5d1f56c884f96d6173f5ab59ed7396911f56e981`, `$id urn:project-studio:bridge:protocol-4:projection-42`, manifest 42/protocol 4; `bridge/people.ts` `marketCaseProjection` + `marketCase` on every profile; `bridge/contract.ts` `underMarketCase` renew refusal, `marketProposalDraftToEngine`/`marketProposalQuoteSnapshot`/`playerProposalDraft`; `bridge/session.ts` `quoteMarketProposal` branch, pending family, commit revalidation, an explicit casting branch and a FAIL-LOUD tail on an unknown quote type; both contract checks verified; both typechecks clean; 19 pins 41→42; evidence `15`, 310 lines). Counts: market 14/15, p10a-r1 8/9, named group + schema/generator/lock 173/173, people/profile/envelope 76/76, bumped p13b/p11 group 97 + 1 todo + 1 failed, remaining consumers 21/21. **Adjudicated (all three test-side; the plan's law holds):** (1) `bridge-p10a-r1` R0 pins `renewAvailable: true` for an in-window person — that IS the repealed in-window renewal (a case is open at week 40 on `p10-r1-windows`), so R0 joins the R6 bucket; (2) market group 5's leak check asserts the rival's figure `912361` is absent from the people JSON, but at tier 1.00 the rival's offer equals the public P10 ask and that number is the PLAYER'S OWN employee's published contract (`t-act-05`, projection 19) — the case block leaks nothing (probed); the check must use a figure that is not otherwise public (a rival proposal at a tier ≠ 1.00); (3) `bridge-p11-ready` line 124's renew quote is refused by the engine's `underMarketCase` on its own fixture at week 92 — inherited from `d49cc27`, re-expression owed, in scope. **Stop horizon (§2.1.11 says 'configured', no number):** `MARKET_DECISION_STOP_HORIZON_WEEKS = 1` in `bridge/people.ts` (the session advances one authoritative week at a time); alternative `CONTRACT_HORIZONS_WEEKS.decision = 12` recorded, NOT applied — an Owner call. **Defect caught by the implementer's own hand walk of the route (evidence `15`):** `quotedIntentFor` passed a detached `apply` and lost `this`; fixed; no authorized test exercised `quoteMarketProposal` — a session-level requirement test for the new intent kind (quote → commit → STALE_REVISION → replay INTENT_NOT_AVAILABLE) is owed test-side with the three re-expressions. `termsRevised` and `decisionWeekNear` remain unexercised by any test (recorded). **Bridge re-expressions + session test LANDED `be301c4`** (test-author): p10a-r1 R0 → `renewAvailable:false`, `renewReason` /market case/, `renewalTerms` empty, `caseForTalent` discovered/52 and `profile.marketCase` (9/9); market group 5 → the rival's proposal at tier 1.10, its annual/bonus absent from the case block, the people JSON and the industry JSON, the competing row's three fields the literal `UNKNOWN` (16/16 with the new group 8); p11-ready renew → an accepted `ok:false` `underMarketCase` quote that mutates nothing and refuses `submit()`, the incumbent's proposal quoting `ok:true` (17/17); NEW group 8 through a real `BridgeSession`: quote validates against the quote-response schema, own-row figures = `proposalDraft` at the read week, commit +1 revision and the proposal lands, same intent at the stale revision → `STALE_REVISION`, revise replaces (one per studio), withdraw removes, the original quote at the moved revision → `INTENT_NOT_AVAILABLE` ('Intent was not emitted by the current authoritative TypeScript state.'; `pendingQuotes` is cleared by every accepted command), unknown quote type → `INVALID_COMMAND`. No defect surfaced; both typechecks exit 0 (evidence `21b`). - [x] **T4 Matched pass — CLOSED, P14A.1 LOGIC VERIFIED · UNITY NOT VERIFIED (attribution `26-attribution-d894692`; final source `d894692`, no production source moved after the pass)** — RUN on `e575fc1` (2026-09-18 10:44–11:58Z, matched flags; evidence `22-RUN`, `22-test-core-e575fc1`, `23-test-bridge-e575fc1`; committed `b338ad4`): core 281/291 files · 3141 passed / 24 failed / 2 todo (2775 s); both contract checks verified; bridge 63/66 files · 674 passed / 13 failed / 1 todo (1636 s). **Attribution:** inherited = `p13a-scientist-foundation` 3 (golden digests) + r3n1 6 (three fixture files) + `world-first-scenery-load-in-provenance` 1 + `bridge-p12-campaign-library` 10 core / 11 bridge (5 s / 20 s budgets, bound ≤ 11) + `bridge-p13-campaign-isolation` 1 (60 s budget) = 21 core / 12 bridge. **NOT attributed (3, all rival sound-adoption chains pinned by P13A/S5 before the market):** `bridge-p13-laboratory` 'shows rival sound only from the actual operational receipt' (`expected undefined to be defined`; core and bridge stages), `p13b-s5-conservation` 'a rival's organically-purchased sound adoption is byte-unchanged by the player's own adoption' (`expected +0 to be 300000`), `p13b-s5-equipment` 'an unheld retained asset … NEVER reused' (a frozen-chain VALIDATION refusal: 'Technology save: invalid equipment asset identity'). **Diagnosed (sim-core, read-only, evidence `24`, `1798786`; seven throwaway worktrees; clean baseline `e0676f9`): ALL THREE ARE PREMISE, no engine predicate misbehaves.** (1) `bridge-p13-laboratory`: first broken at `d49cc27` — week 416 is the SECOND synchronized expiry and its settlement writes 50 employment receipts into the 13-week pulse window the case reads at pageSize 50 (54 receipts vs 5 at the baseline), paging the `technologyAdopted` row off; then at `a2d59e0` r02 stops buying at all (the §48 gate fails by exactly evidence 18's 348,487 every week 416–427, stage free) and the first purchase moves to r05 at 520/532 — re-expressed by deriving the purchase from the chain's receipts and filtering the pulse. (2) `p13b-s5-conservation`: first broken at `daaf95f` — the selector `find(a => a.studioId !== own)` is array-positional and the seat budget's cash relief lets r01 adopt sound by RESEARCH at 276/288 (a new first non-player row, equipmentCost 0) while the commercial purchase is byte-unchanged (r05, 520, 300,000, 975,000) — the same `route === 'purchase'` selector correction `bridge-p13-laboratory` made at `e0676f9`. (3) `p13b-s5-equipment`: first broken at `daaf95f` — the case forges `:equipment:0` and HARD-SETS `nextEquipmentId = 1` while the natural chain now holds r01's first-prototype asset at 0 and r05's commercial at 1 (`nextEquipmentId 2`), so the validator refuses the engine's own self-consistent second row; the forge injects at the live counter (the :86 sibling carries the same latent premise). Product note recorded: a settlement week floods the 13-week pulse window with employment receipts — A.2's Pulse fold is the designed compression. **Closure procedure (S3 precedent):** no production source changes, so the pass stands for every other file; the three re-expressed files are reverified on the final commit single-worker (evidence `25`), then the label. Reverification LANDED `3eee428` + `d894692` (laboratory 7/7, equipment 5/5, conservation 3/3 — the isolation claim re-expressed substance-only because the S8 one-inventor law and the global equipment ordinal legitimately move a rival asset's ordinal; identity unchanged; engineering note: per-studio asset ordinals are a P13B-S5 identity refinement, not this slice; evidence `25`). **LABELLED** as above; then LOGIC VERIFIED · UNITY NOT VERIFIED with the named limits (tie-exhaustion todo; `termsRevised`/`decisionWeekNear` unexercised at A.1, exercised by A.2 test 3; stop horizon 1 week = Owner call; `--project ui` typecheck-only).

## P14A.2 — Talent Market workspace read models — task expansion (draft 2026-09-18; audit before it begins)

**Settled law (not re-asked).** The P14A.1 law as landed (`e575fc1`): Save V28 `talentMarket` root; cases with the decision week derived on read; one current proposal per studio at an enumerated tier; prices re-derived at the read and decision weeks; the seven freeze predicates with typed reasons and `dropped[]` on every case-terminal receipt; `'tie exhausted'` / `'all proposals dropped'`; the rival seat budget; R1 floor through `studioOffer`; R4 era law; R6 (in-window renewal repealed — the incumbent proposes; `underMarketCase`); direction 8 (no intermediary; `representation: null`); the five attention causes; the UNKNOWN marker on every competing figure. Companion §2.1.11 (A.2 paragraph, verbatim scope), §2.1.8 and the disclosure row at line 92 (after settlement, who the person joined and on what term length is PUBLIC, folded by A.2's Pulse projection into one `retained` or `moved` activity from the two P12 receipts; salary stays private), R11 (thin surface in A.1; the full workspace in A.2; the world route in A.3).

**Engine today (verify at T1).** `src/core/talentMarket.ts` exports `caseForTalent`, `caseOpenForTalent`, `caseDisclosure`, `currentProposals`, `proposalDraft`, `playerOffer`/`playerOfferOptions`, `submitProposal`, `withdrawProposal`, `marketEligibility`, `publicPriorityOrder`, `publicPreferredTerm`, `UNKNOWN`; receipts `{kind, week, talentId, studioId, reasons[], dropped[]}`; P12 employment rows (`hollywood.employment`, `activeEmploymentOrdinals`) and P10 credits are the history sources; `state.freeAgents` with the accepted instant-signing law. Bridge (projection 42): `marketCaseProjection`, `marketAttentionRows` (`MARKET_DECISION_STOP_HORIZON_WEEKS = 1`), `marketProposalDraftToEngine`, `quoteMarketProposal`, the industry `view` enumeration (`studios · studio · films · film · person · pulse · history · roster · project · employment · laboratory · plans · office`) with page slots `laboratory`, `plans`, `office` (the S4/S8 pattern).

**Scope (companion §2.1.11, A.2).** READ MODELS over the landed law — no new engine law, no new intent kind, **Save V28 unchanged** (no persisted fact: the review token IS the session's quote family, `STALE_REVISION` / `INTENT_NOT_AVAILABLE`; durable "reviewed" marks are NOT persisted — an Owner product choice, recorded OPEN). Bridge **projection 43**, text only: (1) a new industry `view: 'market'` with `StudioIndustryResponse.market` (nullable `StudioMarketPage`): `attention[]` (the five A.1 causes gathered across every open case, deduplicated per (cause, talentId), fixed order: decision week ascending, then the case's discovery order — never PersonId or array order), `cases` in four buckets by DERIVED facts — `renewalWindow` (open cases with status `discovered | proposals_open`), `freeAgents` (people in `state.freeAgents` whose `marketEligibility` is free-agent-signable, each with the ask re-derived at the read week through `playerOffer` and the EXISTING hiring intent — no case, no new action), `settling` (open cases whose decision week is the NEXT authoritative week, `decisionWeek === week + 1` — derived by the workspace, not read from the status: T1 traced that settlement runs inside the same tick that reaches W, so the A.1 status member `decision_pending` is never observable after a normal `tick()` — recorded as a note on A.1's status law, unchanged), `closed` (terminal cases, bounded and paged: `MARKET_CLOSED_PAGE_SIZE` — a constant whose VALUE is a hypothesis, the bound's existence is pinned) — a case appears in exactly one bucket. **Fixed order in every bucket (audit amendment; companion §8 item 3 and §2.1.7: no array, Map/Set, `PersonId` or row order):** `renewalWindow` and `settling` by decision week ascending, then discovery order (the case's position in the receipt-ordered `talentMarket.cases`); `closed` by `closedWeek` descending, then discovery order; `freeAgents` by role in the fixed discipline order, then the ask at the read week descending, then the person's most recent P12 employment ordinal (−1 for the never-employed), then the person's ordinal in the append-only `state.talent` roster as the documented final key (T1 found no separate 'worldgen index' oracle; the roster is append-only, so a person's position is an immutable in-state fact — named as such, not an incidental order). The three live buckets need no page size: they are bounded by the live world (open cases ≤ active employment rows inside a window; free agents ≤ the pool), which the century-scale rule (§8 item 6) treats as hot summaries bounded by construction — stated, not assumed; `selected` (nullable `StudioMarketCaseDetail` for `targetId` = the talentId): the candidate profile rail BY REFERENCE to the projection-42 profile pieces (the case block, the public preference line, the person's role/standing summary — no second copy of any profile field), the offer comparison (one row per proposal: employer, effective week, term, compensation tier for the player's own row and the UNKNOWN marker for every competing row, `submittedWeek`; after settlement the receipt's order-only reasons and, for the player's own dropped proposal, its `dropped[]` sentence), and the paged career and employer history (employer intervals from the P12 rows — employer, from/to week, term length, the transition kind — with rival-employment salaries PRIVATE; credits from the existing history snapshot by reference; `MARKET_HISTORY_PAGE_SIZE`, deterministic paging by row ordinal). (2) `Review & Submit`, `Revise`, `Withdraw` from the workspace through the EXISTING `marketProposalAction` quote family with revalidation — the same resolvers, no new kinds. (3) The Pulse fold: on `view: 'pulse'` the two P12 receipts of one settlement (the subject's `expiry` and the winner's start) fold into ONE activity row — correlated through the exact join (talentId, settlement week, receipt kind): a person holds one contract at a time, so at most one `expiry` and one start receipt exist for that person in that week, which makes the join exact, not a coincidence. T1 FOUND A GAP between the companion's prose and the landed engine: §2.1.8 says the chooser receipt references the two P12 receipts 'by eventId', but the landed `settled` receipt carries no such references and A.1's settlement test never asserted them; persisting them would be a new save fact (a V29 candidate), which no consumer needs because the join above is exact — recorded OPEN/deferred, not added here — `retained` (same employer) or `moved` (new employer), naming the person, the studio and the term length — never the salary; declined/expired/invalidated cases keep the existing rows (the person leaves for the free-agent pool as today); the A.1 pin that no folded row exists is REPLACED by this pin. (4) Disclosure exactly as A.1: nothing private anywhere in the workspace or pulse JSON (leak check both directions); `bridge/industry.ts` "Contract terms are kept private in Industry." and `bridge/people.ts` `contract: null` unchanged outside a case. No world route (A.3), no promises (P14B), no intermediary row, fee, route or "Approached" bucket.
- Persisted facts: none. Save V28 unchanged. Projection 43.
- OPEN (recorded, not resolved): `MARKET_CLOSED_PAGE_SIZE` and `MARKET_HISTORY_PAGE_SIZE` values; durable review marks; the `decisionWeekNear` horizon (A.1's Owner call, inherited: 1 week applied, 12 recorded); the sentence prose around `retained`/`moved` (CANDIDATE — the two enum values themselves are settled verbatim by companion line 92); whether Free agents belong under Cases (the companion lists them there — followed).
- Inherited limits (named, not re-litigated): the tie-exhaustion todo; `termsRevised`/`decisionWeekNear` unexercised at A.1 — A.2's test 3 MUST exercise both (a rival's revision after the player's proposal; a decision week inside the horizon); the OPEN rival tier hypothesis and the weekly-refill consequence; the inherited founding-rival insolvency; `--project ui` typecheck-only.

**Tests (RED-first, bridge; import `marketPage` from the not-yet-existing `bridge/market.ts` and CALL it, per the repo's RED-first rule).** 1 `PROJECTION_VERSION === 43`, schema `$id`/x-project-studio, `view: 'market'` request/response, `converted` law against `LIVE_SAVE_VERSION = 28`. 2 buckets: every case in exactly one bucket by derived status; the FIXED ORDER of all four buckets pinned exactly as the scope states (a fixture with two cases sharing a decision week and two free agents sharing a role and ask, so the secondary keys are exercised); a free agent listed iff eligible with the ask re-derived at the read week and the existing hiring intent; `closed` bounded and paged, deterministic across two reads. 3 attention: the five causes across all open cases, deduplicated, fixed order, nothing else — INCLUDING `termsRevised` (a rival revises after the player's proposal) and `decisionWeekNear` (the decision week one authoritative week away). 4 candidate rail + offer comparison: by reference to the profile (byte-equal pieces), own row re-derived, every competing figure the UNKNOWN marker, after settlement the reasons and the player's own `dropped[]` sentence. 5 paged history: employer intervals from the P12 rows with rival salaries private, credits by reference, page bounds and deterministic paging, no duplication with the profile. 6 intents from the workspace: `Review & Submit` / `Revise` / `Withdraw` through `marketProposalAction` with `STALE_REVISION` and `INTENT_NOT_AVAILABLE` exactly as the A.1 group 8 pins. 7 Pulse fold: one `retained`/`moved` row per settlement from the two P12 receipts correlated through the `settled` receipt's `eventId` references (the fixture carries an unrelated same-week `expiry` of another person of the same studio to prove the pairing is by reference), term length present, salary absent; declined/expired/invalidated unchanged; the A.1 no-fold pin retired. 8 disclosure leak check both directions over the workspace and pulse JSON, with the competing proposal at a tier ≠ 1.00 so its figures are not otherwise public (the A.1 group-5 lesson). 9 save/load: V28 unchanged; the genuine V27 fixtures load converted with an empty workspace; a V28 checkpoint round-trips through the workspace; the genuine V28 fixtures of T0 load byte-stable.

**Audit (contract-auditor, 2026-09-18, read-only): ADOPT WITH THE LISTED AMENDMENTS** — the bucket-order gap (blocking) closed above; the live-bucket bound reasoning added; the Pulse correlation key and the non-1.00 leak fixture pinned in tests 7/8; `retained`/`moved` relabelled settled. Source cross-checks held (exports, receipt shape, statuses, the five causes, the view enumeration, the existing verbs — no new intent kind; no hidden persisted fact; task order keeps fixtures before source).

### P14A.2 tasks
- [x] **T0 Genuine V28 fixtures — DONE `31971bf` (2026-09-18, minted at `dee8fc1`, the P14A.1 final writer):** `tests/fixtures/p14/legacy-v28-open-case-45.json.gz` (sha `c9ff26fe…`, week 45: one case `proposals_open` decided 52, player 1.25/52 + rival 1.0/52), `legacy-v28-settled-208.json.gz` (sha `3a468e06…`, week 209: 24 `settled` receipts at 208, 36 seat-drop sentences across 18 receipts; `advanceTo(p13aGeneratedStudio('p13-public-commercial-adoption'), 209)` and nothing else), `legacy-v28-legacy-terminations-20.json.gz` (sha `6957a857…`: lifted from a GENUINE V27 termination `legacy-v27-player-termination-20.json.gz` sha `30692366…` minted in a throwaway worktree at the V27 writer `ef9ff76` — `releaseTalent` charged 2,498,600 = round(0.5 × 4,997,200) under that commit's own law; `legacyTerminations[0] = {…t-act-10:20:player-24, 20, 2498600}`); every fixture re-read → validate → re-serialise byte-equal; minter deterministic across two runs; provenance in `tests/fixtures/p13b/PROVENANCE.md` (V27 addendum + V28 section; one index kept); save-v28 + s8-save-v27 17/17; evidence `p14a2-20260918/00`. Original text: `src/harness/p14/legacy-v28-fixtures.ts` minting (a) a state with an open case and both proposals at week 45, (b) a state after a settlement week carrying `dropped[]` sentences and a `settled` receipt, (c) a state lifted from the V27 legacy-terminations path; sha256 + commit in `tests/fixtures/p13b/PROVENANCE.md` (V28 section); evidence `p14a2-<date>/00`. Gate: P14A.1 labelled.
- [x] **T1 Tests 1–9 RED-first — DONE `edacd5a`** (`tests/bridge-p14a2-market.test.ts`, 9 groups / 16 cases, RED at module resolution: 'Failed to load url ../bridge/market.ts'; `typecheck:bridge` exactly one TS2307 in that file; evidence `01`). Pinned names: `StudioMarketPage {attention, cases:{renewalWindow, freeAgents, settling, closed:{rows,page,pageSize,total}}, selected}`, `StudioMarketCaseDetail {case, rail:{talentId, role, standingLine, preferences, profileRef}, comparison, history:{employers, credits, page, pageSize, total}}`, request `view:'market'`/`targetId`/`page`/`historyPage`, `MARKET_CLOSED_PAGE_SIZE`/`MARKET_HISTORY_PAGE_SIZE` from `bridge/market.ts`; `PROJECTION_VERSION` 43. PREMISES (coverage limits, adjudicated): free-agent ask ties unexercised (the ask jitter is keyed by talent id, no natural collision on 25 seeds); the final free-agent key is the `state.talent` ordinal (ruled above); the Pulse pairing joins on (talentId, week, kind) (ruled above; the eventId-reference gap recorded); `settling` derived as decisionWeek === week + 1 (ruled above); multi-page history overflow unexercised (no fixture has enough transitions).
- [x] **T2 Bridge projection 43 — DONE `cea19cf` + `e210513` (2026-09-18):** `bridge/market.ts` (`marketPage`; `MARKET_CLOSED_PAGE_SIZE = 20`, `MARKET_HISTORY_PAGE_SIZE = 10` — both OPEN values, marked hypotheses in source; 20 makes settled-208 a genuine 20+4 two-page read), `view: 'market'` on the industry route with `StudioIndustryResponse.market` (required, nullable) and the optional request field `historyPage`, the Pulse retained/moved fold in `bridge/industry.ts` (one row per settlement joined on (talentId, settlement week, receipt kind); optional `settlementKind` ∈ {retained, moved} on `StudioIndustryActivity`, present only on the folded row), `PROJECTION_VERSION = 43`, schema `sha256:95351b9d1af70fd7d890dc6d8dc75ac40430d059f87fec526a42707b1f7bb1c7` (was `a3f8b03c…`), `generated/unity/*`, pins 42→43 in 20 test files, the schema-identity C# sentinel (F10/F11) bumped exactly as at P14A.1-T3, both contract checks and both typechecks clean (re-run independently by the coordinator at e210513), evidence `10` (519 lines). NO engine edit; Save V28 unchanged. Fold effect on settled-208's pulse page 0: people rows 44 → 20, totalRows 56 → 32; no other consumer's activity count moved (p12-industry 4/4, checkpoint-recovery 11/11, save-as 1/1, projection20-migration 10/10). RULINGS at T2: (i) the T1-pinned member `StudioMarketCaseDetail.case` is AMENDED to `marketCase` — the C# generator refuses it (`CF08-IDENTIFIER-COLLISION`: `case` is a C# keyword); the projection-42 Profile already uses `marketCase`; (ii) the free-agent bucket is `state.freeAgents` ∩ free-agent-signable exactly as this expansion words it, NOT all eligible talent (settled-208 has 60 eligible and an empty pool; the existing hiring conversion accepts only the pool); (iii) the fold is Pulse-scoped ("on `view: 'pulse'`"), so the A.1 group-6 pin (two rows at the settlement week on the per-person EMPLOYMENT route) is NOT superseded and stays green — the audit-time note that it would be retired is withdrawn; an Employment-route fold is OPEN, not invented; (iv) the companion's eventId references stay absent from the receipt (V29 candidate, T1 ruling stands). Six of the 16 A.2 cases were falsified TEST-SIDE, none an engine or bridge defect: group 2 case 1 (a case reads `discovered` at its discovery week — `caseStatusAt`; the bucket and order assertions hold), group 2 case 3 (empty week-0 pool; the test's own hiring-conversion line fails for the same reason), group 3 (fewer than 3 actors on the chosen seed, `marketPage` never reached; the 3-actor probe fires all five causes including the two inherited-unexercised `termsRevised`/`decisionWeekNear`, deduplicated per (cause, talentId) in discovery order), group 4 ×2 (`case` → `marketCase`), group 7 (the pre-fold literal 48 — unsatisfiable pre-fold too: the pulse page is paged at 50 releases-first, page 0 carried 44). Coordinator's own run at e210513: 6 failed | 10 passed (16).
- [ ] **T2b Six premises re-expressed test-side + the A.1 group-6 comment** (test-author; `tests/bridge-p14a2-market.test.ts` only, comment-only edit in `tests/bridge-p14a1-market.test.ts`; status read at the read week; a populated `state.freeAgents` pool built through the engine's own paths — arrival after founding, early release; a ≥3-actor seed keeping both causes exercised; `marketCase`; the fold count derived from the settlement receipts, never a literal; evidence `10b`) — DONE `a0611b8`: A.2 16/16 and A.1 16/16 single-worker (32 passed, three green runs), `typecheck:bridge` clean; no `.skip`/`.todo` added; production diff 4cd1a90..a0611b8 over src/bridge/ui/generated EMPTY. Notes from the re-expression: the free-agent pool is built through `signContract` one person at a time re-reading `hiringMarketIds` (a batch precompute throws D-11.14 because each signing reshuffles the market-epoch sample) then `releaseTalent` (D-11.9); the 3-actor seed is `p14a2-bridge-attention-v2`; on settled-208 all 24 settlements sit in week 208 inside the 13-week Pulse window on one 50-row page (totalRows 32), so page-walking is not exercised — open coverage limit for a fixture with older or more numerous settlements.
- [x] **T3 Matched pass — CLOSED on `a0611b8` (2026-09-18 14:36–15:51Z, 75 min; evidence `11-RUN`, `11-test-core-a0611b8`, `12-test-bridge-a0611b8`, attribution `13`):** core 22 failed / 3159 passed / 2 todo (292 files), bridge 12 failed / 691 passed / 1 todo (67 files), both contract checks verified; every failure inherited (campaign-library 11/11 = 9 × 5 s + 2 × 20 s budgets inside the ≤ 11 bound, isolation 1/1 60 s budget, scientist-foundation 3 golden digests, r3n1 6 ENOENT, scenery 1 python export); zero failures in any P14A.2/P14A.1 file; +1 file / +16 tests green in both runs vs the A.1 pass; the three A.1 re-expressed premises now green. **P14A.2 CLOSED — LOGIC VERIFIED · UNITY NOT VERIFIED** (not Owner acceptance). Final source `a0611b8` (production last moved at `cea19cf`). Named limits: page sizes 20/10 OPEN; the Employment-route fold OPEN; the eventId-reference gap a V29 candidate; Pulse page-walking of the fold unexercised (settled-208 fits one page); tie-exhaustion todo inherited from A.1; no Unity verification.

## P14A.3 — World route facts — task expansion (draft 2026-09-18; audit before it begins)

**Companion basis.** §2.1.11 "World route (P14A.3)": selecting a person on the player's lot whose case is open shows a restrained status and text ("renewal window open · decides Week 12") and opens the exact case; a rival's person is reached from the Industry roster and the profile, never from a physical rival lot; arrivals and departures happen at the effective week only; no agency office, no meeting marker. Companion §2.1.10 / R11 and the skeleton: A.3 "adds world navigation (the lot status glyph and world entry), keyboard/controller paths and Unity parity" ("P14A.3 world/client route (P14 + Unity)"; L5 "world and workspace"). Under the logic-first order (`OWNER-HEADLESS-PROGRAM-20260916-01`) this slice delivers the FACTS the world route reads and pins the laws the route relies on. The glyph, the world-entry input, keyboard/controller paths and Unity parity are client work: recorded in the backlog at T3, never claimed verified here.

**Settled law (not re-asked).** A.1 as landed (`d894692`) and A.2 as landed (`a0611b8`): a case decides atomically at the expiry week, so on an expiry case the decision week is the effective week of every proposal (`startWeek === decisionWeek`); the market page opens the exact case by `targetId`; the Profile's `marketCase` is the single source of case facts; every competing figure is `UNKNOWN`. Presence Projection V1 (`src/core/presence.ts`) is pure and save-neutral: a contracted person is on the player's lot iff `contract.startWeek <= week && week < contract.endWeekExclusive` (lines 739–743), engagements `production | script | casting | research | roster`, credits from the `PresenceCredit` union; the projection is player-only (`studioPresence(state)` takes no studio). No rival lot exists anywhere in the bridge and none is added.

**Engine today (verify at T1).** `caseForTalent`, `caseOpenForTalent`, `marketCaseProjection` (status, `decisionWeek`, `subjectStudioId`), `studioPresence`; bridge `BridgePersonPresenceSnapshot {onLot, engagement, credit, facilityId, facilityName, blockedReason, canLocate, locateReason}`, `BridgePersonAttentionSnapshot {tier, reason, cohort}` whose `renewal-open` cohort reads "Renewal window open — contract ends Week N.", `BridgeRosterRowSnapshot` (`attentionReason`, `canLocate`), Industry `StudioIndustryPerson {talentId, name, roleLabel, employerStudioId, employerName, employmentLabel, creditCount, onPlayerLot, notice}`; `FacilityCapability` has no agency value; no presence DTO carries a marker field.

**Scope.** READ MODELS only, **Save V28 unchanged**, projection **44**. (1) New `bridge/world.ts` exporting `personWorldRoute(state, talentId)` → `StudioWorldRouteSnapshot {statusLine: string | null, caseRef: {view: 'market', targetId: string} | null, reach: 'playerLot' | 'industry'}`. `statusLine` exists for an OPEN case only: "Renewal window open · decides Week N" while the case reads `discovered | proposals_open`, "Decides next week · Week N" when `decisionWeek <= week + 1` while the case is still open (exactly the landed A.2 settling rule, `bridge/market.ts` line 260, not a narrower `===`), null for every closed case and for a person without a case (CANDIDATE copy; the Owner's wording is OPEN). `caseRef` opens the exact case: the market page's `selected.marketCase` for that `targetId` is byte-equal to the Profile's `marketCase`. `reach` is `playerLot` iff `presence.onLot`, else `industry` (the roster and the profile). (2) Carriers: the Profile gains `worldRoute`; the player's roster row gains `worldStatusLine`; the Industry person row gains `caseStatusLine` and `caseRef` (public case facts only; rival figures stay absent). (3) The attention snapshot and its cohorts are UNCHANGED: A.2's five causes are the market page's interrupts, and the Profile's `renewal-open` text is not rewritten here (OPEN whether A.3 or P14B retires it). (4) Laws PINNED, not added: presence changes only at the effective week, never at submission, revision or settlement when those differ from it (on an expiry case settlement and effective week coincide, so the pin reads "on the lot at W−1, off at W" for a departure and "off at W−1, on at W" for an arrival); a rival's person is never on the player's lot (`onLot === false`, `canLocate === false`); no facility capability names an agency; the presence engagement and credit unions gain nothing. (5) Nothing else: no intent, no Pulse change, no new page, no engine edit.

**Tests (RED-first, bridge; import `personWorldRoute` from the not-yet-existing `bridge/world.ts` and CALL it, per the repo's RED-first rule).** 1 `PROJECTION_VERSION === 44`, schema `$id`/x-project-studio, `LIVE_SAVE_VERSION === 28`, the converted law. 2 the player's person with an open case (genuine `legacy-v28-open-case-45`): `worldRoute.statusLine` exact, `caseRef.targetId === talentId`, `reach === 'playerLot'`, `presence.onLot === true`; the market page at that `targetId` returns `selected.marketCase` byte-equal to the Profile's; the roster row carries the same line; a person without a case reads `statusLine === null` and `caseRef === null`. 3 the settling line at `decisionWeek <= week + 1` while open, and null after close (walk open-case-45 to 51 and past 52). 4 a rival's person with an open case (derive the talentId from `state.talentMarket.cases` with `subjectStudioId` ≠ the player on the p13a seed at the first week such a case is open; never a magic week): `onLot === false`, `canLocate === false`, `reach === 'industry'`; the Industry roster row of that studio carries `caseStatusLine` and `caseRef`; the rival's salary and tier appear nowhere in the profile, roster or world JSON (non-1.00 tier). 5 the effective-week law from receipts, NON-VACUOUS by construction: `legacy-v28-settled-208` is minted with no player action (`src/harness/p14/legacy-v28-fixtures.ts` lines 80–85), so the player holds no contract there and a "player employee moved away" clause over that fixture would pass on an empty set; the test therefore BUILDS the departure through the engine (sign a person on a short term, let a rival proposal at a higher tier win the expiry case, walk to the decision week) and ASSERTS the moved-away set is non-empty before pinning "on the lot at W−1, off at W"; the arrival clause (the player wins a rival's person: a player proposal above the rival's on a rival-employee case, bonus fundable) is built the same way and, if the construction cannot be made to settle for the player under the landed tie/decline law, becomes an `it.todo` naming the obstacle, never a silent pass; the retained clause uses genuine `legacy-v28-open-case-45` (player 1.25 vs rival 1.00, decided 52): the person is on the lot at 51, 52 and 53 and the set is asserted non-empty. settled-208 is used only for rival-side settlement facts (never on the player's lot). 6 negative pins against the TypeScript unions, because `capability` is plain text on the wire (`bridge/schema/bridge-schema.ts` lines 432, 736, 761, 811, 1817, 2715) and no capability value appears in the JSON schema: `FacilityCapability` (`src/core/types.ts` 592–597) has no agency or intermediary member; `PresenceEngagement` and `PresenceCredit` (`src/core/presence.ts` 68, 75–84) are exactly the P13B sets; the ONE true wire enumeration, `StudioPersonPresenceSnapshot.engagement` (`bridge-schema.ts` 2094), equals `PresenceEngagement`; `credit` stays `nullable(text())`; the key set of every presence DTO in the JSON schema contains no key matching /marker|meeting/i. 7 save/load: the three V28 fixtures read byte-stable and the world route reads identically across the bridge save/load path; a genuine V27 fixture converts with `statusLine === null` for every person.

**Tasks.** T0 NO fixture minting: Save V28 does not move (the writer stays at the P14A.1 law); the three A.2 fixtures serve; the T0 line states this rather than skipping it silently. T1 tests 1–7 RED at module resolution of `bridge/world.ts` (test-author; evidence `01`). T2 bridge projection 44 (sim-core, bridge writer only: `bridge/world.ts`, `bridge/people.ts` profile and roster row, `bridge/industry.ts` person row, schema, generated, both contract checks, pins 43→44; no engine edit unless a pure read helper is proven necessary, then a unit test first; evidence `10`). T3 matched pass, attribution against the inherited set, label LOGIC VERIFIED · UNITY NOT VERIFIED, records, backlog entry INCLUDING the deferred client items (lot status glyph, world entry, keyboard/controller paths, Unity parity), commit, push.

**OPEN (recorded, not decided).** The status-line copy; whether the Profile's `renewal-open` attention text is retired under the market (A.3 leaves it in place); a three-way wording drift for the same fact, flagged for the Owner track and not resolved here: the Profile attention cohort "Renewal window open — contract ends Week N." (`bridge/people.ts` 714), the companion's Core case-block prose "Case open · decides 1934 · Week 12" (§2.1.11), and A.3's candidate "Renewal window open · decides Week N"; the Employment-route fold and the two page sizes (A.2); the glyph and input design (client). IMPLEMENTATION NAMING, decided here as mechanical and reversible at a projection bump: the field names `worldRoute`, `worldStatusLine`, `caseStatusLine`, `reach` and the `caseRef {view: 'market', targetId}` shape (the existing `view`/`targetId` wire convention of `bridge/market.ts`). LABEL CAVEAT: a later LOGIC VERIFIED · UNITY NOT VERIFIED label on this slice closes the world-route FACTS only; the companion's three client deliverables (the lot status glyph and world entry, keyboard/controller paths, Unity parity) stay genuinely open in the backlog.

**Audit (contract-auditor, 2026-09-18, read-only): ADOPT WITH AMENDMENTS, applied above** — test 6 re-targeted from a non-existent schema enumeration to the TypeScript unions plus the one true wire enum; test 5 made non-vacuous (settled-208 carries no player contract, so the departure and arrival are built through the engine and the sets are asserted non-empty); the settling line aligned to the landed `<= week + 1` rule; the field naming named as an implementation decision; the wording drift and the label caveat recorded. No gap against the companion paragraph; no contradiction with the A.1/A.2 rulings; Save V28 confirmed unchanged (`LIVE_SAVE_VERSION = 28`, `save.ts` 6368) and 44 confirmed as the next projection.

### P14A.3 tasks
- [ ] **T0 No fixture minting** (Save V28 unchanged; the A.2 fixtures `legacy-v28-open-case-45` / `legacy-v28-settled-208` / `legacy-v28-legacy-terminations-20` serve).
- [x] **T1 Tests 1–7 RED-first — DONE `016327c`** (`tests/bridge-p14a3-world.test.ts`: 7 groups / 16 cases = 14 `it` + 2 `it.todo`; RED 'Failed to load url ../bridge/world.ts'; `typecheck:bridge` exactly one TS2307; evidence `p14a3-20260918/01`, 67 lines). RULINGS at T1: (i) **FINDING (product consequence, OPEN, not an A.3 defect):** under the landed A.1 seat law a founding rival can never poach a player's employee — every founding roster holds exactly `RIVAL_TEAM_ROLES` with zero slack, so `noSeatForRole` drops any rival bid on the player's person before the first churn (week 196/208), and at the churn itself each bidding rival's own incumbent retention settles earlier in the same weekly pass (`openCasesAt` array order) and refills its seat before the player's case is evaluated (3/3 probed seeds; no persistent seat deficit across weeks 0–416). This sharpens the A.1 OPEN note "contests rare under P12's weekly deficit filling": rival → player poaching is impossible at founding roster sizes. Owner-side items: rival roster slack, the settlement order within a week, or a seat budget above the roster. Recorded here, in the progress row and in the backlog; no engine change in A.3. (ii) Test 5's departure clause is RE-EXPRESSED test-side (owed at T2b, before the matched pass): a market-settled departure at the effective week is constructible through the `declined` outcome — on genuine open-case-45 withdraw the player's proposal, let the rival bid drop under the seat law, the case settles `declined` at 52 and the person leaves the lot at 52 (on at 51, off at 52); the requirement (departures only at the effective week) is unchanged, only the `moved` premise was falsified. The arrival clause stays `it.todo` with its obstacle (the rival's Standing out-Copelands a fresh player studio by ≈30 points against `STANDING_BAND_TOLERANCE = 5`; growing standing needs production activity, out of scope). (iii) `caseRef` is null whenever `statusLine` is null (closed case or no case): the world route is the restrained OPEN-case surface; a settled case stays readable through the Profile's `marketCase` and the market page's `closed` bucket (implementation decision, reversible at a projection bump; unasserted by T1, implemented at T2). (iv) The status-line copy is pinned as the plan's candidate literal (U+00B7 middle dot); a copy change is a test edit plus a projection bump, OPEN as recorded. (v) Group 6's union pins are compile-time exhaustiveness checks (unions do not exist at runtime); accepted. Fixtures reused: open-case-45, settled-208, legacy-terminations-20, `legacy-v27-renewal-window-456`.
- [x] **T2 Bridge projection 44 — DONE `b39e646` + `a3ebe95` (2026-09-18):** `bridge/world.ts` (`personWorldRoute` → `StudioWorldRouteSnapshot {statusLine, caseRef, reach}`; `caseRef` null whenever `statusLine` is null, per T1 ruling iii; copy byte-equal to the candidate literal with U+00B7; settling branch on `decisionWeek <= week + 1`, so `decision_pending` reads the settling line), the three carriers (`StudioPersonProfileSnapshot.worldRoute` REQUIRED non-nullable; `StudioRosterRowSnapshot.worldStatusLine` REQUIRED nullable; Industry `StudioIndustryPerson.caseStatusLine`/`caseRef` REQUIRED nullable, always null together, public case facts only), new DTOs `StudioWorldCaseRef {view: const 'market', targetId}` and `StudioWorldRouteSnapshot` (`reach` enum `playerLot | industry`), `PROJECTION_VERSION = 44`, schema `sha256:05e53f126b28c601a2ed80fb614347da571c90946b65808f111f77bf7bdd5392` (was `95351b9d…`), generated C# sha `6929c399…`, F10/F11 sentinel `48a31e2c…`, pins 43→44 in 21 files (values and one comment only), evidence `10` (513 lines). NO engine edit (src/ui diff empty), NO save change, no union widened, no view/intent/page added, attention cohorts unchanged, the three V28 fixtures re-save byte-identically. Checks (single worker, coordinator re-ran both contract checks and `typecheck:bridge` at a3ebe95): A.3 file 13 passed / 1 failed / 2 todo; A.2 + A.1 32/32; people/profile 63/63; industry consumers 26/26; schema/generator/lock 163/163; P13B pinned 114 + 1 todo; `typecheck` clean. The ONE remaining RED (group 6 test 2 sanity `presence.people.length > 0`) is a TEST-SIDE premise: a week-0 generated world holds no player contract and Presence V1 projects contracted people only; re-expressed at T2b by signing one actor first (bound and compile-time union pins unchanged).
- [x] **T2b Departure clause via `declined` + the group 6 sanity premise — DONE `f32d56c`** (test-side only; 13/1/2 → **15 passed / 0 failed / 1 todo**; evidence `10b`, 183 lines). The departure now pins, on genuine open-case-45 after `withdrawProposal` of the player's bid: weeks 46–51 `proposals_open` and `presence.onLot === true` (no early effect), week 52 `declined` / 'all proposals dropped' with exactly one decline receipt whose first sentence names the rival and the seat predicate, `activeContract` undefined and `onLot === false` — a natural expiry with no successor, never a poaching. The `moved` construction stays unconstructible (seat law; comment kept). The arrival `it.todo` (rival Standing dominance, `STANDING_BAND_TOLERANCE = 5`) is the file's only todo: a follow-up needs a player studio with grown Standing, which needs production activity — recorded, not scheduled. Group 6's sanity: one real actor signed first; bound and compile-time union pins untouched.
- [x] **T3 Matched pass — CLOSED on `f32d56c` (2026-09-18 17:08–18:24Z, 76 min; evidence `11-RUN`, `11-test-core-f32d56c`, `12-test-bridge-f32d56c`, attribution `13`):** core 22 failed / 3174 passed / 3 todo (293 files), bridge 12 failed / 706 passed / 2 todo (68 files), both contract checks verified; every failure inherited and identical in count to the A.2 pass (campaign-library 11/11 budgets, isolation 1/1, scientist-foundation 3, r3n1 6, scenery 1); zero failures in any P14 file; +1 file / +16 tests green in both runs. **P14A.3 CLOSED — LOGIC VERIFIED · UNITY NOT VERIFIED** (not Owner acceptance; the label caveat above holds: the glyph, world entry, keyboard/controller paths and Unity parity stay open in the backlog). Final source `f32d56c` (production last moved at `b39e646`; `src/core` unchanged since `d894692`).

## P14B.1 — First Kept Promise Core — task expansion (draft 2026-09-18; audit before it begins)

**Companion basis.** §2.2 (P14B attaches to P14A through the promise set on proposals as a material term, the promise re-feasibility step at settlement freeze, the trust and relationship descriptors in the person-choice rule and its reservation, and the firing trust driver; none replaces a P14A structure); §4.1 (what a promise is: typed, versioned, attached to a proposal, an objective predicate over authoritative facts, a window, an outcome; never free text, never a salary term; evaluated by events and the due week, never polled; the record named `ProfessionalPromise`); §4.2 (five families; the qualifying event is the FIRST TAKE COMPLETING, the 5 → 4 advance, and no durable first-take fact exists today, so the receipt is NEW P14 WORK appended in both `advanceManagedProductions` callers; rivals promise too); §4.3 (the feasibility service: deterministic, no RNG, committed state by reference, three classes, FRAGILE and IMPOSSIBLE never offerable, the receipt carried for life, re-classification at freeze invalidating the proposal version with a typed reason); §4.4 (outcomes SATISFIED / BROKEN / WAIVED / VOIDED, terminal, once); §4.5 (trust as professional memory: exactly four driver kinds, per (person, studio) with a studio aggregate, public descriptors, consequences in reservation and ranking and beside the firing guard, lives in the P14 root); §2.1.5 (a competing proposal's attached promises are UNKNOWN); §2.1.7 (D3 opportunity and D4 trust bands, the reservation predicate "not `Distrusted`"); §2.1.8 (promise re-feasibility is the one settlement-step addition); §7.1 S9–S11; §7.3 Q1 (no override in the first slice, recommended) and Q3 (count-only at migration, no behavioral backfill); §8 (century rules; one governed save step per wave). R12 (first take = the 5 → 4 advance, distinct from P13's technology lock at shooting entry; copy says "begin filming …"; first-take-then-cancel keeps the promise satisfied and prices the cancellation through trust). The plan's refresh row: the first take now follows the S5-R07 setup-gated Shooting entry, not `phaseEntered` alone.

**Why this slice, and what it excludes.** One family, the receipt it needs, the service that makes it offerable, the outcomes it can reach, and the trust memory that gives it teeth in the chooser: the smallest slice that makes a promise a real lever in a contested case. EXCLUDED, recorded for P14B.2+: families P2–P5 (enumerated in the `family` union, refused as not offerable in B.1 with a typed reason), WAIVED (the waiver acceptance rule) and VOIDED (needs P14C retirement or transition), the relationship model of §5 (ladder, drivers, decay, chemistry, D5), the class-preference filter on D3, calendar/Upcoming rows for due weeks (Ready; P11-REQ-029), the `WRITING_COUNT` family.

**Settled law (not re-asked).** A.1 and A.2 as CLOSED (`d894692`, `a0611b8`) and A.3 as landed at T2/T2b (`a3ebe95`, `f32d56c`; its T3 label is a GATE on this slice's T0, see the tasks): material terms `(talentId, issuerStudioId, termWeeks, startWeek, premiumTier)` under one digest; prices re-derived at the decision week; the seven `FreezeDrop` reasons; `chooseProposal` over `DESCRIPTOR_ORDER = ['compensation','term','standing','incumbency']` with D3/D4/D5 documented NEUTRAL (`talentMarket.ts` 614–623); decline-all; the receipt `{eventId, kind, week, talentId, studioId, reasons[], dropped[]}`; Save V28 root `{cases, proposals, receipts, legacyTerminations, representation: null}` whose version IS the save version. Production lifecycle: `advanceManagedProductions` (`operations.ts` 1553) with the `remainingTicks === 5` branch at 1653 (the first shooting week's completion), called from `tick.ts` 335 (player) and `hollywoodTick.ts` 253 (rivals); the S5-R07 setup gate precedes it; P13's technology lock stays at shooting entry and is not merged. Firing: the recalibrated termination law with its receipt and ledger row (A.1).

**Engine today (verify at T1).** No first-take fact (only the compacted `phaseEntered` event); `TalentMarketProposal` carries no promise; `survivesFreeze` returns `FreezeDrop | null`; `publicPriorityOrder`, `publicPreferredTerm`, `DescriptorKey` exported; `occupancy.ts` for live capability; `busyTalentIds` sources; the P12 employment rows and the `termination` receipts/ledger rows for the trust drivers; `state.talentMarket.receipts`.

**Scope — engine (Save V29).** (1) `FirstTakeReceipt {eventId, week, productionId, studioId, directorId, cast: {lead, antagonist, support[]}}` appended at the 5 → 4 advance inside `advanceManagedProductions` for BOTH callers, once per production (idempotent by productionId), into the root's new `firstTakes` (append-only, in-state ordinal); never at shooting entry. (2) `ProfessionalPromise` exactly as §4.1: `promiseId` (in-state ordinal), `family` (union of the five; B.1 offers `APPEARANCE_COUNT` only), `version`, `issuerStudioId`, `beneficiaryPersonId`, typed `predicate` (`{count: X}` for P1), `windowStartWeek`, `dueWeekExclusive` (inside the proposed contract, else IMPOSSIBLE by construction), `feasibilityReceipt` (at offer and at freeze: classification, inputs digest, rules version, bottleneck), event-derived `progress`, `evidenceRefs` (first-take eventIds), `outcome | null`, `outcomeWeek`, typed `outcomeCause`, `contractId` once committed. Records live in the root's `promises`. (3) Attachment: `promises: readonly string[]` (promiseIds) on `TalentMarketProposal` as a MATERIAL term: the digest becomes `(talentId, issuerStudioId, termWeeks, startWeek, premiumTier, promiseDigest)`, so a promise change is a revision (`materialTermsChanged` at freeze if drifted); at most ONE promise per proposal in B.1 (bounded hypothesis); a promise never rides an out-of-case renewal. (4) The feasibility service (`promiseFeasibility(state, draft, week)`): pure, no RNG, inputs by reference exactly as §4.3.1 (the proposed interval, seats reserved from greenlight through release, writing assignments, already-active promises, scripts, productions, facilities through `occupancy.ts`, the minimum lawful pipeline with the blueprint durations; NO forward schedule exists, so only started productions, queued intents, existing scripts and lawful new commissions count); the count-family rule of §4.3.2 with all three conditions — `X ≤ N_max − buffer`, the X earliest events on existing-pipeline paths, and the X-th event's expected first-take week leaving `slack` weeks before the due week — under the named HYPOTHESES `buffer = max(1, ceil(0.25 × N_max))` and `slack = 8` weeks; the "announced retirement effective week" input of §4.3.1 does not exist before P14C and is read as absent (stated, not assumed); REASONABLY ACHIEVABLE offerable, FRAGILE and IMPOSSIBLE refused at quote with the exact bottleneck (Q1: no override in B.1). (5) Settlement freeze: every attached promise is re-classified against committed state at W; a proposal whose promise is no longer REASONABLY ACHIEVABLE is dropped with the new typed `FreezeDrop` member `promiseNotFeasible` (sentence from the same `DROP_SENTENCE` contract), before the chooser runs; the surviving winner's promise is committed with `contractId` and its freeze receipt. (6) Outcomes (§4.4): SATISFIED once, at the qualifying first-take receipt that reaches X inside the window (evidence refs recorded); BROKEN immediately when the issuing studio terminates the beneficiary early (the A.1 termination path, player or rival), and immediately when a studio-caused event makes the count predicate unsatisfiable: on the studio's `cancel` of a production the beneficiary was seated on before its first take, the feasibility service is re-run against committed state and the promise is BROKEN iff the result is IMPOSSIBLE (§4.4 "any studio-caused event that makes the predicate unsatisfiable"); otherwise the due-week evaluation at the weekly advance when `week === dueWeekExclusive` with the window unsatisfied; WAIVED and VOIDED are enumerated members that no B.1 path reaches (stated, like A.1's `decision_pending`); outcomes terminal and idempotent by receipt id; a P14 receipt kind `promiseOutcome` referencing the causing eventId. (7) Trust (§4.5), DERIVED ON READ from persisted facts, never a persisted meter: drivers per (person, studio) exactly `promiseKept`, `promiseBroken`, `terminatedEarly` (from the termination receipts), `ranToEnd` (P12 rows ended at `endWeekExclusive` without early termination), `cancelledAfterFirstTake` (a studio `cancel` of a production after its first-take receipt with the person seated) — from the P14A recording boundary onward only (Q3: no pre-boundary reconstruction); the versioned descriptor rule `Reliable | Mixed record | Distrusted` with a 260-week recency horizon (HYPOTHESES: thresholds and horizon), the top three drivers as public reasons, the studio-level aggregate as fallback; exported `trustDescriptor(state, personId, studioId, week)`. (8) The chooser: reservation refuses a `Distrusted` issuer (new `FreezeDrop` member `issuerDistrusted`); `DESCRIPTOR_ORDER` gains `opportunity` (band `qualifying promise attached` iff an attached promise is REASONABLY ACHIEVABLE at freeze — the class-preference filter waits for the catalogue) and `trust` (`Reliable` > `Mixed record`) at their companion positions D3/D4, with typed reasons ("they offered an opportunity", "their record with me ranked above yours"); `publicPriorityOrder` widened accordingly to §2.1.7's two archetype orders (capable-but-unproven and under-30: opportunity, compensation, relationships, term, trust, Standing, incumbency; proven veterans: compensation, term, trust, relationships, incumbency, Standing, opportunity), reduced to the live descriptors. INHERITED DEFECT, fixed BEFORE this slice as P14A.1-F1 (below): the landed unproven branch reads `['term','compensation','standing','incumbency']` (`talentMarket.ts` 639–640) where the companion's reduction is `compensation, term, standing, incumbency`; the proven branch is correct; `preferredTerm` derives the term preference from the order's FIRST key (`priorityOrder(...)[0] === 'compensation'`), so the fix must decouple it from the order (archetype → longest/shortest term directly) or every person would prefer the longest term. No test covers either function today. (9) Rival symmetry (bounded P12 generalization, pure policy over the case description): a rival attaches one P1 promise with X = 1 iff its own feasibility is REASONABLY ACHIEVABLE; rival first takes come from the same receipt through the `hollywoodTick` caller; rival outcomes feed the same trust records. (10) Save V29: `convertV28ToV29` adds `firstTakes: []`, `promises: []` and `promises: []` on every stored proposal; the frozen V≤28 chain is untouched; every B.1 row dated ≥ the recording boundary; the V28 downgrade guard as before. Firing: no new write — the trust driver is read from the existing termination receipt.

**Scope — bridge (projection 45), THIN CORE SURFACE on the A.1 precedent (audit: B.1 as first drafted was A.1 + A.2 in size).** The Profile case block's proposal rows gain `promise` (own row: family, X, window, classification; competing rows: the UNKNOWN marker per §2.1.5); the Profile gains the public trust DESCRIPTOR label per viewing studio (`Reliable | Mixed record | Distrusted`, no driver text) and the person's promise rows (open and settled, with outcomes); the market page comparison gains the promise row; `marketProposalAction` quotes accept a promise draft and return the classification, or a typed `INTENT_NOT_AVAILABLE`-class refusal "not offerable: <bottleneck>" for FRAGILE/IMPOSSIBLE; the serialized-envelope leak test extended to every new DTO (a rival's promise terms appear nowhere). MOVED TO P14B.2 (read models over B.1's facts, no engine law): the top-three trust driver reasons text, the Pulse `promiseKept` / `promiseBroken` activities, the attention causes `promiseDue` / `promiseOutcome`, the workspace promise history. Rival promise AUTHORING (item 9) stays in B.1 under S25 symmetry; it is the first deferral candidate if T2 overruns, in which case the chooser still reads a rival promise when present.

**Tests (RED-first).** Engine: 1 first-take receipt appended once at the 5 → 4 advance for a player production and for a rival production, never at shooting entry, after the S5-R07 setup gate, byte-stable across save/load, absent on a V28 fixture. 2 the promise record and the widened digest: attaching, revising or removing a promise changes the digest; a drifted promise drops `materialTermsChanged`; one promise per proposal enforced; a window outside the contract is IMPOSSIBLE by construction. 3 feasibility on constructed pipelines reproducing §4.3.3 Example A's shape (X ≤ 2 REASONABLY ACHIEVABLE, 3–9 FRAGILE with the "needs a picture not yet commissioned" bottleneck, ≥ 10 IMPOSSIBLE) and Example B (an active promise consuming the seat → FRAGILE); the classification is pure (same inputs → byte-equal receipt) and consumes no RNG (`rngState` unchanged). 4 settlement freeze: a promise REASONABLY ACHIEVABLE at submission and FRAGILE at W (the pipeline changed) drops `promiseNotFeasible` with its sentence; the case falls to the next valid proposal or declines. 5 outcomes: SATISFIED at the X-th qualifying first take with evidence refs; BROKEN at `dueWeekExclusive` when unsatisfied; BROKEN immediately on early termination of the beneficiary; each once and idempotent; WAIVED/VOIDED unreachable (pinned as never emitted on the B.1 seeds). 6 trust: drivers derived only from facts at or after the boundary; the descriptor rule at its thresholds; a `Distrusted` issuer dropped `issuerDistrusted` at reservation; on a constructed pair equal on compensation, term, standing and incumbency, the proposal with a feasible promise wins with the opportunity reason; a `Reliable` issuer beats a `Mixed record` issuer on the trust band; `publicPriorityOrder` byte-equal to the companion's REDUCED orders for both archetypes (asserting the companion, never the landed array) and `publicPreferredTerm` unchanged (proven → longest catalogue term, unproven → shortest) — the F1 regression pins carried forward. 7 rival symmetry: a rival proposal carries a P1 promise iff its feasibility is REASONABLY ACHIEVABLE; a rival first take satisfies it; a rival early termination breaks it; the same trust records. 8 save: a genuine V28 fixture converts to V29 with empty new tables and the same digests; a V29 save round-trips byte-stable; the frozen V≤28 chain's tests unchanged; downgrade refused. 9 leak: the rival's promise terms absent from every DTO and from the settlement reasons (ordering only). Bridge (projection 45): 10 the Profile and market read models (promise rows, the trust label), the quote family with the typed refusal, the schema id, `LIVE_SAVE_VERSION === 29`, the leak test.

**Tasks.** T0 fixtures: the three genuine V28 fixtures (`31971bf`) were minted at the final V28 writer and the writer has NOT moved since (A.2/A.3 were bridge-only; `git diff d894692..HEAD -- src` must be empty at T0 — verify and record); mint at HEAD, BEFORE any engine edit, one more genuine V28 fixture holding a player production in Shooting at `remainingTicks === 5` (for test 1) and record its sha in `tests/fixtures/p13b/PROVENANCE.md`. T1 engine tests 1–9 RED (test-author; imports from the not-yet-existing `src/core/promises.ts` and calls them). T2 engine (sim-core, one writer: `src/core/promises.ts` new, `talentMarket.ts` for the digest/freeze/chooser, `operations.ts` for the receipt, `types.ts`, `save.ts` V29, `hollywoodValidation.ts`): make 1–9 green; Save V29 allocated at execution. T3 bridge: test 10 RED, then projection 45, pins 44→45. T4 matched pass, attribution against the inherited set, label LOGIC VERIFIED · UNITY NOT VERIFIED, records, backlog entry (new DTOs, the quote family widening, the attention causes, the Pulse kinds), commit, push.

**OPEN (recorded, not decided).** IMPLEMENTATION NAMING, decided here as mechanical and reversible: `promises: readonly string[]` on the proposal by promiseId (the draft-by-reference precedent of `types.ts` 2046–2049) and one receipt kind `promiseOutcome` carrying the outcome as a typed field. INTERPRETIVE CHOICE, stated: trust is DERIVED ON READ from persisted facts (promise outcome records and receipts, termination receipts, employment rows) — §4.5 "lives in the P14 root" is read as "its inputs live in the root"; if T2 proves a persisted driver table necessary (a unit test first), it becomes a V29 root table `trustDrivers`. The buffer and slack hypotheses; the trust descriptor thresholds and the 260-week horizon; one promise per proposal; the rival promise policy (X = 1 when achievable); the opportunity band without the class-preference filter; Q1 (no override) and Q3 (count-only) as recommendations, not approvals; the calendar/Upcoming rows; the P2–P5 families, WAIVED/VOIDED reachability, and the relationship model (B.2+). Product consequence carried from A.3-T1: rival → player poaching is impossible under the landed seat law at founding roster sizes, so B.1's contested cases on the B.1 seeds will mostly be player-retention or rival-vs-rival; the tests construct contests where the law allows them and name the limit where it does not.

**Audit (contract-auditor, 2026-09-18, read-only): ADOPT WITH AMENDMENTS, applied above** — (1) the A.3 label made an explicit T0 gate and the settled-law citation corrected; (2) the inherited `priorityOrder` defect (unproven branch swaps term and compensation; `preferredTerm` coupled to the order's first key) confirmed by the coordinator against `talentMarket.ts` 636–648 and companion §2.1.7, and scheduled as P14A.1-F1 with a RED regression before any B.1 engine work; (3) BROKEN-on-capacity-collapse for a count family made explicit; (4) the retirement input named absent; (5) the three count-family conditions restated in scope; (6) the promiseId reference, the single receipt kind and the derived-trust reading flagged as decisions; (7) slice cut to a thin Core bridge surface with the trust text, Pulse activities and attention causes moved to P14B.2; rival authoring kept under S25 as the first deferral candidate. Testability confirmed (`rngState`, `busyTalentIds`, `activeContract`, the 5 → 4 branch); V29 / projection 45 confirmed next.

### P14B.1 tasks
- [x] **T0 GATES MET and fixture minted — DONE `206223f` (2026-09-18):** P14A.3 labelled (`658b990`), P14A.1-F1 landed (`a36449f`); `git diff a36449f..HEAD -- src/core` EMPTY (re-checked by the coordinator after the commit); `LIVE_SAVE_VERSION === 28`. `tests/fixtures/p14/legacy-v28-shooting-5.json.gz` (sha `4872e5659c7214b04159d01a97e57261d35b0744bef875c2f37476c5f78f2d82`, 280,466 bytes uncompressed, week 16): production `prod-0008` in `shooting` at `remainingTicks === 5` with the shooting task unassigned (the first Shooting week not yet completed), director `t-dir-00`, cast lead `t-act-07` / antagonist `t-act-08` / support `t-act-10`, craft `t-cra-06`, setup `ballroom-reveal-lighting-01` credited 4/4 (admitted 12, completed 16) — built through the engine's own actions from seed `p14b1-shooting-5` on the operations studio (no rivals; a disclosed 30,000,000 cash bootstrap), stop week derived from state. The three earlier V28 fixtures byte-identical before and after (shas re-checked against PROVENANCE by the coordinator); A.2 group 9 16/16; evidence `p14b1-20260918/00` (86 lines). Pre-check 2026-09-18: `src/core` is unchanged since `d894692`; the only `src` change since is the harness minter `src/harness/p14/legacy-v28-fixtures.ts` (`31971bf`), so the three existing genuine V28 fixtures remain at the final V28 writer; F1 changes chooser law, not the save format, so they stay genuine V28 saves.
- [x] **T1 Engine tests 1–9 RED-first — DONE `c76b3a7`** (five files: `p14b1-first-take` 3, `p14b1-promises` 14, `p14b1-trust-chooser` 10 + 2 todo, `p14b1-save-v29` 6, `p14b1-leak` 2 = 9 groups / 37 cases; every file RED 'Failed to load url ../src/core/promises.js'; `typecheck` exactly five TS2307; evidence `01`, 178 lines). RULINGS at T1: (i) BROKEN is recorded SYNCHRONOUSLY at the causing action, as §4.4 words it ("the causing event's receipt"): T2's file list gains `src/core/actions.ts` (the termination and `cancel` handlers append the `promiseOutcome` receipt through a pure helper in `promises.ts`); the tests search a bounded ≤ 3-tick window, so either timing satisfies them, and T2 implements the synchronous form. (ii) `FirstTakeReceipt.cast` copies the accepted `Production.cast` shape `Record<CastSlot, string>` (one id per slot) — the expansion's `support[]` was wrong; corrected here. (iii) Save naming follows the codebase convention: public `migrateToV29` / `validateSaveV29` / `SaveFileV29` with `convertV28ToV29` as the internal step (the V28 precedent has both). (iv) Test 3 pins the §4.3.2 SHAPE (window outside the contract IMPOSSIBLE by construction, an unreachable X IMPOSSIBLE, purity and `rngState` unchanged, Example B's active-promise-consumes-the-seat FRAGILE), not Example A's numbers; T2 still implements all three conditions and may self-check against Example A. (v) Two `it.todo`: the literal four-way tie of test 6 is unconstructible (incumbency is binary on the one `subjectStudioId`; measured through the second synchronized churn at week 404 on the p13-public-commercial-adoption seed, every surviving pair keeps the incumbent) — a constructible claim replaces it as a real `it` (opportunity or trust added to one side of an otherwise 1–1 contest changes the winner, companion Case 2's shape); test 7's "a rival early termination breaks it" has no landed rival termination action (companion §2.1.9: rival early termination is Ready, not Core) — the expansion's "player or rival" wording for BROKEN-on-termination is corrected to the player's action until that Ready item lands. NEW ENGINE FACT recorded: a second synchronized rival churn occurs at week 404 on that seed (founding 208-week terms renewed once); a rival production reaches `remainingTicks === 5` at week 7 on the default seed and advances to 4 the next tick.
- [x] **T2 Engine — LANDED `8bb5738` (engine + harness, 14 files, +1298/−45) + `6948e31` (live-version sweep 28→29, 77 files, pins only; evidence `10`, 1,699 lines); PARTIAL: 31/37 T1 cases green, 4 RED on named premises, 2 todo; pushed.** V29 SHAPE AS LANDED: two new GameState ROOT tables beside `talentMarket` — `firstTakes: FirstTakeReceipt[]` (`{eventId, week, productionId, studioId, directorId, cast: Record<CastSlot,string>}`) and `promises: ProfessionalPromise[]` (§4.1 fields + `outcomeEventId`; `feasibilityReceipt {classification, bottleneck, inputsDigest, rulesVersion, week}`); the T1 tests pinned the game-state root and the plan's "the root's new tables" is read that way (implementation decision: the P14 roots are now the set `talentMarket`, `firstTakes`, `promises`; the frozen chain strips both new roots and the per-proposal leaf); `TalentMarketProposal.promises: string[]` (≤ 1); receipt kind `promiseOutcome` in `talentMarket.receipts`; the digest gains a 6th element ONLY when a promise is attached, so every V28 proposal re-derives byte-identically; `LIVE_SAVE_VERSION = 29`, `SaveFileV29`, `validateSaveV29`, `convertV28ToV29`/`convertV29ToV28`, `migrateToV29`, downgrade guards on 13 older migrators; `FreezeDrop` + `promiseNotFeasible` / `issuerDistrusted` with sentences; `DESCRIPTOR_ORDER` now six live descriptors; priority orders unproven `[opportunity, compensation, term, trust, standing, incumbency]`, proven `[compensation, term, trust, incumbency, standing, opportunity]` (the F1 fix survives inside both). HYPOTHESES named in code: `WEEKS_TO_FIRST_TAKE = 5`, `SEAT_CYCLE_WEEKS = TUNING.PRODUCTION_TICKS` (8), `PROMISE_SLACK_WEEKS = 8`, `promiseBuffer(N) = max(1, ceil(0.25 N))`, `TRUST_HORIZON_WEEKS = 260`, `TRUST_DISTRUST_MIN_NEGATIVES = 2`, descriptor rule Reliable iff ≥ 1 positive and 0 negative; Distrusted iff ≥ 2 negatives and negatives > positives; else Mixed record (also the no-history default). File-list extensions ACCEPTED: `tick.ts`/`hollywoodTick.ts` (the callers carry the 5 → 4 productions out; plumbing only), `ui/src/engine/adapter.ts` (the V28 precedent pin), `tests/contracts/_v14Contract.ts` (V13-twin strip list). `rngState` untouched (test 3 green); frozen V≤28 validators byte-identical; no bridge/generated edit. RULINGS at T2: (i) **A promise BINDS only when its proposal is committed (`contractId` set)** — companion §4.1 "the `contractId` it rode in on": an unbound promise (a losing or withdrawn proposal) is never evaluated, never BROKEN, never a trust driver; its record keeps `outcome: null`, `contractId: null`. This is the literal reading, not a new outcome; T1's test 5 free-agent construction (a due-week BROKEN on a contract-less promise) contradicts §4.1 (free agents are instant-sign, no proposal, no promise) and is re-expressed through a bound promise. (ii) With (i) in place, **rival promise AUTHORING (item 9) lands at T2b** as planned under S25 (one P1 promise, X = 1, iff REASONABLY ACHIEVABLE; a losing rival's promise is simply unbound). (iii) COMPANION-VS-ENGINE DISCREPANCY recorded OPEN, not resolved here: §4.2 asserts has-discipline seat eligibility, but the landed M16.2 role matching (`actions.ts` ~397–403, `requireRole(actor, 'actor', 'greenlight cast.<slot>')`) gates cast seats by primary role, so `APPEARANCE_COUNT` for a non-actor is IMPOSSIBLE under the engine's own law; P14 added no label gate. (iv) TEST-SIDE premises (T2c): first-take test 1 PLAYER and promises test 5 SATISFIED rely on the shooting-5 fixture, whose shooting task is `unassigned` (the 5 → 4 branch needs `scheduled`, produced only by `assignShootingDirector` + `scheduleShootingTake`) and whose world has `hollywood: null` (minted on the operations studio; no studio identity for the receipt) — re-expressed by building the player production on an industry world through the engine and scheduling the take; the fixture remains a genuine V28 save for the migration proof (test 8, converting with `firstTakes: []`); promises test 5 capacity-collapse used a `signOne()` helper that ignores `hiringMarketIds` (D-11.14) — helper fixed; trust-chooser test 6 opportunity-breaks-the-tie hits the seat law (the lone rival dropped `noSeatForRole`, one survivor, no descriptors compared) — the test-author attempts the week-404 construction (a player proposal with a promise at a tier above the rival incumbent's, where the 2–2 tie falls to the unproven person's opportunity-first order), else `it.todo` with the measurement; D3/D4 are unit-correct and otherwise unexercised end-to-end. (v) MOVED NEIGHBOURS (T2c, values only): F1's two 4-descriptor arrays → the widened six (the F1 requirement survives inside; F1 line annotated); `p14a1-save-v28` one-past sentinel 29 → 30 and "1 through 29 only"; its R4 line `migrateToV28` → `migrateToV29` (the engine now refuses LOUDLY a state missing the V29 roots: `promises: the Save V29 roots are missing — migrate this state to V29 before acting on it`). (vi) BRIDGE at HEAD is red until T3 (`typecheck:bridge` exit 2 at six sites: `session.ts` 41/136, `runtime-checkpoint.ts` 6/843/452, `runtime/campaign-library.ts` 5/48/127, and the `priorityOrder` schema enum at `bridge-schema.ts` 2204, which now receives `opportunity`/`trust` — a REAL wire widening that confirms projection 45 independently of the promise DTOs; `caseDisclosure`'s `DisclosedProposal.promise: Disclosed<DisclosedPromise|null>` is the seam T3 consumes). ENGINE FACT: the first rival `proposalSubmitted` on the natural chain is r01 retaining its own writer at week 196.
- [x] **T2c Test-side — DONE `e125c7f` (pushed under `3cd423e`):** the five files re-expressed, no production source touched (+1014/−126 incl. evidence `10b`, 394 lines): first-take 3/3 (the PLAYER receipt built on an industry world with the take scheduled); promises 15 = 14 green + 1 RED; trust-chooser 13 = 9 green + 1 RED + 3 todo; f1 4 + 1 todo (the two arrays widened to six descriptors); save-v28 10/10 (sentinel 30, "1 through 29 only", R4 via `migrateToV29`); totals 43 → 46 cases, 8 failed → 2 intentional RED, `typecheck` clean. RED awaiting T2b: promises test 5 line 687 `expected 'BROKEN' to be null` (`stillUnbound.outcome` on a withdrawn open-case-45 promise; a `promiseOutcome` receipt and a `promiseBroken` driver also exist today — `advancePromisesWeek` has no `contractId` gate) and trust-chooser test 7 line 443 `expected +0 to be 1` (`actorProposal.promises.length`; no rival authors yet). ITEM 4 FAILED → `it.todo` with the measurement: of the 12 rival-subject actor cases opening at week 404 on `p13-public-commercial-adoption`, the only unproven subject (`person-studio-5a47d054-r04-3`) belongs to an employer insolvent through the whole window (closing −8,933,725 at week 404, −10,127,665 at decision week 416, zero revenue; `submitProposal` refuses its own retention bid under the operating reserve), so D7 incumbency can never be won by the rival side and the case settles `declined` / 'all proposals dropped' (r01/r02 opportunistic bids drop `noSeatForRole`); no second candidate in weeks 0–450. RULING (viii): the D3 opportunity-breaks-a-tie claim is UNEXERCISED END-TO-END in B.1 on both measured constructions (week 52 founding seat cap; week 404 sole-candidate insolvency); it is unit-correct and stays `it.todo` with the measurements, recorded as a B.1 label limit; a synthetic construction (a forged case on a solvent rival with an unproven subject, built at the live counter, the receipts-derived way) is queued as a B.2 test-side candidate, not a B.1 blocker.
- [x] **T2b Engine — LANDED `faf45f3` (pushed under `8a6bcdc`):** `src/core/promises.ts` (+29/−4) and `src/core/talentMarket.ts` (+33/−1) only; `actions.ts` untouched (the termination/cancel filters live in promises.ts). Ruling (i) as a shared private gate `evaluable(promise) = outcome === null && contractId !== null` at the three outcome loops (`advancePromisesWeek` both branches, `breakPromisesOnTermination`, `breakPromisesOnCancel`); NOT gated by design: `reservedByActivePromises` in the quote and the freeze re-classification. Ruling (ii) as pure `authorRivalPromise` at the P12 rival proposal site, outside `submitProposal`'s catch: one `APPEARANCE_COUNT`, count 1, window = the rival's own term, iff its own feasibility reads REASONABLY ACHIEVABLE. Measured on `p13-public-commercial-adoption` at week 260: 24 rival promises, 6 SATISFIED at week 216 on 2 first takes, every losing rival's promise `outcome: null` / `contractId: null` (ruling (i) holds on the chain). RULING (ix) (coordinator; the one judgement call outside (i)/(ii); ACCEPTED): `outcomeEventId` names the promise's OWN `promiseOutcome` receipt in every branch — the SATISFIED branch had pointed it at the causing first take, and one take seats up to three promised people, so three of r01's promises named `first-take-event-76` and the V29 root validator refused every save export ("records a second outcome"); the causing take(s) stay in `evidenceRefs`; the alternative (loosening the validator's one-outcome uniqueness) rejected; no save shape change, and no fixture carries a SATISFIED outcome. Checks: `typecheck` clean; five p14b1 files 32 passed / 4 failed / 3 todo (39) — first-take 3/3, save-v29 6/6, leak 2/2; p14a1 five files 22 + 2 todo; p13a-rival-adoption 2/2; bridge-p13b-s8-rivals 9 passed / 8 failed, IDENTICAL to its baseline at `3cd423e` (the same 8 tests, same three V28→V29/enum causes, red until T3) — NO natural-chain pin moved; `rngState` untouched (pinned by promises test 3 and p13a-rival-adoption on a chain now carrying 24 rival promises); evidence `10c` (691 lines). REMAINING RED = four TEST PREMISES → T2d: trust-chooser test 7 authoring probes `attachPromise` on the live proposal that now already carries the authored promise (refused by design) → probe through `promiseFeasibility`; promises termination-BROKEN, capacity-collapse BROKEN and trust-chooser rival-first-take SATISFIED inject synthetic records with `contractId: null` (T2c's table had predicted them 'UNCHANGED by T2b' — falsified by ruling (i) as landed) → bind them to the real employment row ids. EVIDENCE LIMIT until T2d: the termination and cancel BROKEN paths are unexercised end to end.
- [x] **T2d Test-side — DONE `37dc6c4` (pushed):** tests only (+325/−29 incl. evidence `10d`, 226 lines; `src` diff empty): `p14b1-promises` 13 + 2 failed → 15/15 (a shared `activeEmploymentContractId(state, talentId, studioId)` helper reads the REAL `hollywood.employment` row id, named premise error if absent; termination-BROKEN and capacity-collapse BROKEN now bound and exercised end to end — the T2b evidence limit is closed); `p14b1-trust-chooser` 8 + 2 failed + 3 todo → 10 + 3 todo (authoring probed through the pure `promiseFeasibility` for the writer contrast clause; the actor-subject authored record's `family`/`count`/window/`feasibilityReceipt.classification` REASONABLY ACHIEVABLE/`contractId` null read from `state.promises`; the rival first-take SATISFIED case bound to the lead's real employment row and asserting `outcomeEventId` names a `promiseOutcome` receipt); first-take 3/3, save-v29 6/6, leak 2/2 unchanged; `typecheck` clean. BEFORE state reproduced from `git show HEAD:` copies (deleted before staging). Self-found and recorded: a standalone feasibility probe on a proposal that already carries the rival's promise reads FRAGILE because `reservedByActivePromises` counts that promise against the same pair (companion Example B's shape) — the redundant probe was removed, both runs in the evidence. T2c's "cases 2–4 UNCHANGED by T2b" prediction recorded as falsified by ruling (i). KNOWN TEST-QUALITY GAP (recorded, not fixed here; B.2 T3 test-side): `p14b1-promises` test 5's SATISFIED case still binds with the literal `player:contract:${cast.lead}`, which does not match the real minting pattern (`${owner}:contract:${talentId}:${startWeek}:player-${ordinal}`) and stays green only because `evaluable()` tests `contractId !== null`, not that it names a real row — replace it with the new helper.
- [x] **T3a Bridge test 10 RED — DONE `db13007` (pushed):** `tests/bridge-p14b1-promises.test.ts` (503 lines; 6 groups / 10 cases: version pins 1; case block + market page promise rows 2; trust label + promise history open → BROKEN 1; `marketProposalAction` promise-draft quote family 3; leak 1; save/load 2) RED at module resolution 'Failed to load url ../bridge/promises.ts' (0 collected; the file is absent — the strong precedent, not the missing-named-export hazard). `typecheck:bridge` ACTUAL baseline re-run at `84dab5a` and recorded verbatim: FIVE errors in four files (`bridge/people.ts` 971 TS2322 `priorityOrder`; `bridge/runtime-checkpoint.ts` 847 TS2379; `bridge/runtime/campaign-library.ts` 41, 66 TS2379; `bridge/session.ts` 136 TS2375 — all V28→V29 / enum-widening mismatches), none TS2307; with the test file exactly ONE more (`tests/bridge-p14b1-promises.test.ts` 149 TS2307). The earlier 'six sites' list is the EDIT list for T3 (the V28 type imports at `session.ts` 41, `runtime-checkpoint.ts` 6/452, `campaign-library.ts` 5/127 and the schema enum at `bridge-schema.ts` 2204), not the error list. Evidence `14` (152 lines). ASSUMED SURFACE the RED file pins (T3 implements these names; only the read fields are asserted): `bridge/promises.ts` exports `promiseRowsFor(state, talentId, viewerStudioId, week?)` → `Array<{issuerStudioId, promise: Disclosed<DisclosedPromise|null>}>` (a pass-through of `caseDisclosure(...).proposals`, shared by the Profile case block and the Market comparison row, which already aliases `block.proposals`) and `promiseQuoteSnapshot(state, issuerStudioId, talentId, draft, week?)` → `{ok, classification, message}` with `message` null when ok, else "not offerable: <bottleneck>" verbatim (the engine's own bottleneck text, incl. the five not-offered-in-B.1 family messages); `marketCaseProjection` gains `trustLabel` (the viewer-scoped `trustDescriptor(...).label`, no driver text) and `promiseHistory` (only promises the VIEWING studio itself issued, open and settled with outcome); the existing `quoteMarketProposal` draft widened with optional `promise: {family, count, windowStartWeek, dueWeekExclusive}` and the quote gains `promise: {ok, classification, message}`; `AVAILABLE_INTENT_KINDS` unchanged (the session request/response are built through casts, so the file's only unresolvable import stays `bridge/promises.ts`). RULING (vii) placement: the case-block placement STANDS for B.1 (viewer-scoped, matching the scope's 'per viewing studio'); its LIMIT — `marketCaseProjection` is null without an open case, so the label and history are readable only while a case is open (the test's +40-week window lands the due week on the next window opening) — goes on the B.1 label, and the case-independent Profile trust block with the driver text is P14B.2. Test-side facts (probed against the landed engine, not guessed): every scenario binds its promise on the WINNING proposal — an 8-week window is FRAGILE at offer ('the schedule leaves no spare picture inside the window'), drops the whole proposal at freeze (`promiseNotFeasible`) and leaves the promise permanently unbound (`contractId: null`), i.e. exactly the T2c/T2b unbound premise; a 40-week window reclassifies REASONABLY ACHIEVABLE, settles, binds, then goes BROKEN at `dueWeekExclusive` because nothing seats the actor; the leak test searches exact numeric values recursively (`containsNumber`, never `String(n)` containment), with a 208-week term and window offsets +111/+141 because small week offsets false-positive against other people's `potentialLow` stats; the bare `count` is not number-leak-checked (family string + window weeks + the structural UNKNOWN check carry it); no SATISFIED terminal promise (engine test 5 covers it); rival authoring not exercised (T2b). Both bindings are CALLED in every group so the RED cause is never a regex coincidence.
- [x] **T3 Bridge projection 45, pins 44→45 — RECOVERED DONE `e78dbc0`; evidence `c5c81c1` (`15`).** Preserved at `d19c45b`; no rebuild. Its targeted 46 passed / 3 todo is not T4 completion.
- [x] **T4 CLOSED (2026-09-19), qualified LOGIC VERIFIED · UNITY NOT VERIFIED.** Full frozen pass on `ee91913`, seven test-only corrections at `a93bf77`, separately GREEN 13 files / 170 tests / three unchanged todos, both typechecks/contracts. Independent attribution/diff reviews KEEP. Exact counts, inherited failures, source identities and limits: `../evidence/p14b1-20260919-t4-final/16-attribution.md`. No wholly green full-suite rerun, Unity verification or Owner acceptance claimed.

## P14A.1-F1 — public priority order regression (inherited defect; fix before P14B.1 T0)

**Finding (B.1 audit, confirmed 2026-09-18).** `priorityOrder` (`src/core/talentMarket.ts` 636–641) returns `['term','compensation','standing','incumbency']` for a capable-but-unproven person; companion §2.1.7 orders that archetype opportunity, compensation, relationships, term, trust, Standing, incumbency, which reduces over the live descriptors to `compensation, term, standing, incumbency`. The proven branch `['compensation','term','incumbency','standing']` is correct. Behavior consequence: an unproven person facing a 1–1 Copeland tie between a better-paying, worse-term proposal and a worse-paying, better-term one is retained by the term today and moves for the compensation under the companion. Coupled hazard: `preferredTerm` (643–647) selects the longest term iff `priorityOrder(...)[0] === 'compensation'`, so correcting the order alone would make every person prefer the longest term. No test covers `publicPriorityOrder` or `publicPreferredTerm`.

**Fix (engine, one writer, RED first).** T1 (test-author): `tests/p14a1-f1-priority-order.test.ts` RED — `publicPriorityOrder` byte-equal to the companion's reduced orders for both archetypes (proven by credit or age ≥ 30; unproven otherwise), `publicPreferredTerm` proven → longest catalogue term and unproven → shortest, and one settlement case constructing the 1–1 tie above for an unproven person that must settle for the compensation. T2 (sim-core): correct the unproven order; make `preferredTerm` read the archetype directly; nothing else. Targeted verification single-worker: the F1 file, `p14a1-settlement`, `p14a1-decline-reasons`, `p14a1-seat-budget`, `bridge-p14a1-market`, `bridge-p14a2-market`, `bridge-p14a3-world`, `p13a-rival-adoption`, `bridge-p13b-s8-rivals`, `p13b-s5-conservation`, `p13b-s5-equipment`, `bridge-p13-laboratory`, `bridge-p11-ready`, `bridge-p10a-r1-contract-quote` (the natural-chain files the seat-budget fix moved); premises that shift are re-expressed test-side with the ruling recorded; the full-suite effect is attributed at P14B.1-T4. Save V28 unchanged (chooser law, not format); no projection bump unless a DTO carries the order (check `publicPriorityOrder` consumers in bridge/).

### P14A.1-F1 tasks
- [x] **T1 RED regression — DONE `bc8294f`** (`tests/p14a1-f1-priority-order.test.ts`: 1 failed | 3 passed | 1 todo; RED `expected ['term','compensation',…] to deeply equal ['compensation','term',…]` at line 72; the proven order and both preferred-term pins (208 / 52) pass and pin the behaviour the fix must preserve; evidence `01`). The 1–1 tie settlement case is `it.todo`, NOT constructible under the landed law (measured): a case opens only from an incumbent's own expiring row, so no free-agent case exists to hang two non-incumbent challengers on; every founding rival holds its full `RIVAL_TEAM_ROLES` cap until the synchronized week-208 churn, where every rival auto-submits on all 24 cases at once; a fresh player signing cannot decide before +52 weeks. A post-209 asymmetry (r03 at 2/3 actor seats, r04 at 0/6 at week 209) suggests a controlled construction may exist later in the week range — recorded, not verified.
- [x] **T2 Fix + targeted verification — DONE `a36449f` (2026-09-18):** `src/core/talentMarket.ts` only (+17/−6): `isProven(state, talentId)` extracted (the old predicate verbatim), the unproven branch now `['compensation','term','standing','incumbency']`, `preferredTerm` reads the archetype directly (proven → 208, unproven → 52 preserved by construction). F1 test 4 passed / 1 todo (coordinator re-ran: 4/1; `typecheck` clean); the 14 targeted natural-chain files all green (settlement 3, decline-reasons 1+1 todo, seat-budget 4, A.1 market 16, A.2 market 16, A.3 world 15+1, rival-adoption 2, S8 rivals 17, S5 conservation 3, S5 equipment 5, laboratory 7, p11-ready 17, p10a-r1 9); both typechecks clean; evidence `10` (381 lines). NO moved premise: `priorityOrder` is consumed at one engine site, `chooseProposal`'s tie-break, which no fixture reaches (the tie is unconstructible under the seat law), and `preferredTerm` is value-identical. WIRE NOTE: `marketCaseProjection` (`bridge/people.ts` 961–973) publishes `preferences.priorityOrder`, `preferredTermWeeks` and the derived "Weighs … first, then …" line; for an unproven person the VALUES now read compensation-first; field names, types, enum membership and nullability unchanged → PROJECTION_VERSION stays 44, no Unity coordination (recorded in the A.1 backlog row). Save V28 unchanged. **P14A.1-F1 CLOSED (targeted verification; the full-suite effect is attributed at P14B.1-T4).** The §2.1.7 tie behaviour (an unproven person moving for compensation in a 1–1 tie) stays unexercised end-to-end: only the two accessors are pinned.

## P14B.2 — Promise read models and trust text — task expansion (draft 2026-09-18; audit before it begins)

**Producer reconciliation (2026-09-19, source `d19c45b`, no B.2 code yet).**
The recovered B.1 `MarketPromiseHistoryRow` is already bound-only and viewer-scoped,
but has neither `contractId` nor `outcomeCause` and uses mint order. Therefore B.2
explicitly widens that shared DTO with those two fields and makes both carriers
newest-first at projection 46; the older “keeps its B.1 shape” sentence below is
superseded on these points. Preserve the existing outcome enum (including reserved
WAIVED/VOIDED values); B.2 adds no producer for those outcomes. Reuse
`promiseHistoryFor`, do not duplicate its filtering law. The B.1 carrier also
exists for a terminal latest case; its absence means no case, not no open case.
For Pulse join by `outcomeEventId`, never infer an outcome from prose or a
(person, week) join. A retained person's legitimate `ranToEnd` driver remains:
the kept-promise fixture requires one `promiseKept` driver, not one total driver.
The known literal-contractId correction is now part of T4, not deferred to B.2.

**Gate.** Begins only after P14B.1 carries LOGIC VERIFIED · UNITY NOT VERIFIED on its matched pass (T4) with projection 45 landed. Read models over B.1's facts only: NO engine law, NO save change (`LIVE_SAVE_VERSION` 29 unchanged, the writer does not move, so there is no T0 fixture minting — recorded here so its absence is deliberate), projection 45 → 46 (forced: the `MARKET_ATTENTION_CAUSES` wire enum widens). Engine implementation and verification only; Unity/rendered UI/native/visual acceptance deferred and never claimed verified.

**Companion basis.** §4.5 (trust is a bounded, public, evidence-backed summary per (person, studio) with a studio-level aggregate fallback; its inputs are exactly promise outcomes, early termination, run-to-end and cancellation after the first take — all five driver kinds are ALREADY derived by the landed `trustDrivers` / `trustDescriptor` in `src/core/promises.ts` (`promiseKept`, `promiseBroken`, `terminatedEarly`, `ranToEnd`, `cancelledAfterFirstTake`; top three by recency; `scope: 'person' | 'studio'`; horizon 260 weeks; the Q3 recording boundary); "the top three drivers listed beside the label"); §4.4 (outcomes are terminal and emitted once — the ONE `promiseOutcome` receipt per promise in `talentMarket.receipts` is the Pulse source; its `reasons` field (one engine-owned sentence per outcome — "a promise to this person was kept" / "went unmet to its due week" / "was broken by an early termination" / "was broken by a cancelled picture" — distinct from `ProfessionalPromise.outcomeCause`, a separate field on the promise record) carries no window numbers); §2.1.11 and §2.2 (exception-based attention; the profile shows why; every effect carries a reason); R5 (promise TERMS — family, count, window, classification — are UNKNOWN to every non-issuer; promise OUTCOMES are public trust inputs, "the studio's own and, for rivals, theirs"); the B.1 MOVED TO P14B.2 line (trust driver text, Pulse `promiseKept`/`promiseBroken`, attention causes `promiseDue`/`promiseOutcome`, workspace promise history); ruling (vii) (the case-independent Profile trust block is B.2); ruling (viii) (the synthetic D3 construction is a B.2 test-side candidate).

**Scope — bridge (projection 46), read models only.** (1) PROFILE TRUST BLOCK: `StudioPersonProfileSnapshot.trust` REQUIRED — `{label: 'Reliable' | 'Mixed record' | 'Distrusted', scope: 'person' | 'studio', drivers: StudioTrustDriverRow[] (≤ 3, the engine's recency order, each {kind, week, dateLabel, positive, reason}), line: string}` derived from `trustDescriptor(state, talentId, playerStudioId, week)` (the People projection is player-scoped by construction: `marketCase` already reads the player studio as viewer); `line` is the three driver reasons with the campaign year, joined by U+00B7 ("kept a promise · 1931 · broke a promise · 1933"; COPY HYPOTHESIS — the companion's grouped "Kept 2 promises" form is a compaction the engine does not publish; the individual rows are the facts, the line is a convenience) and reads "No record yet" when `drivers` is empty (the engine's no-history default label `Mixed record` stands; the caption is derived, not a new label). (2) PROFILE PROMISE ROWS: `StudioPersonProfileSnapshot.promises` REQUIRED — the promises the PLAYER issued to this person that BOUND (ruling (i): a promise that never bound is not history), open and settled, as the SAME row DTO B.1 lands for `promiseHistory` on the case block (`{promiseId, family, count, windowStartWeek, dueWeekExclusive, contractId, outcome: null | 'SATISFIED' | 'BROKEN', outcomeWeek, outcomeCause}`), most recent first; the case block's `promiseHistory` keeps its B.1 shape and becomes the same rows filtered to the case's talent (one DTO, two carriers). (3) PULSE: every `promiseOutcome` receipt yields ONE `people`-group activity at its week — SATISFIED "<Studio> kept its promise to <Person>", BROKEN "<Studio> broke its promise to <Person>" (COPY HYPOTHESIS), `detail` = the receipt's engine reason text verbatim (no family, count or window anywhere in it — pinned), `StudioIndustryActivity.outcomeKind` NEW OPTIONAL `'promiseKept' | 'promiseBroken'` on the `settlementKind` precedent; issuer and non-issuer see the same row (public, R5/§4.5); no fold is needed (one receipt per outcome). (4) ATTENTION: `MARKET_ATTENTION_CAUSES` gains `promiseDue` (a BOUND open promise the viewing studio issued whose `dueWeekExclusive − week ≤ PROMISE_ATTENTION_WEEKS` and no qualifying take yet; reason "Promise to <Person> due Week N — filming has not begun"; `PROMISE_ATTENTION_WEEKS = 8` NUMERICAL HYPOTHESIS = `PROMISE_SLACK_WEEKS`, three weeks of scheduling room before the five-week take lead) and `promiseOutcome` (an outcome recorded at the current week on a promise the viewing studio issued; reason "Promise to <Person> kept — <cause>" / "broken — <cause>"), produced beside the five A.1 causes under the same dedup-per-(cause, talentId) rule and visited whether or not a case is open (a promise outlives its case); rivals' promises never raise an attention row for the player. IMPLEMENTATION REQUIREMENT (audit defect 2, confirmed in code): `promiseDue` and `promiseOutcome` are computed by an INDEPENDENT scan over `state.promises` filtered to the promises whose issuing studio is the viewer — never by extending or reusing `marketAttentionRows`'s per-case viewer guard (`currentProposals` is stripped for the talent the moment `closeCase` runs, so after settlement only the case's original `subjectStudioId` passes that guard) or `caseEntries`'s one-entry-per-talentId map; a poaching winner would otherwise never see its own reminder once its case closed. (5) WORKSPACE: `BridgeMarketCaseDetail.history.promises` = the rows of (2) for the case's talent, unpaged (at most one bound promise per contract → a century holds tens, not hundreds; recorded). (6) INDUSTRY STUDIO ROW (AUDIT: include or strike): the studios view row gains `trustLabel` = the studio-level aggregate (`trustDrivers(state, null, studioId, week)` labelled) — the §4.5 "aggregate every person in the market can see", published as a public fact for every entered studio including the player's own; no drivers, no line. (7) LEAK LAW extended and pinned both ways: for the player viewer a rival's promise family, count, window and classification appear nowhere in the serialized profile, market page, attention, Pulse or industry envelopes; the rival's OUTCOME row and the rival studio's aggregate label DO appear (the deliberate public disclosure, pinned positively so a later "privacy" edit cannot silently remove it). (8) Nothing else: no new intent, view or page; `AVAILABLE_INTENT_KINDS` unchanged; no engine edit expected — if the bridge needs a helper the engine does not export (an outcome-receipt selector), the sim-core adds the pure selector to `src/core/promises.ts` without law change and says so.

**Not in B.2 (recorded, not started).** P2–P5 families; WAIVED (the waiver acceptance rule needs an intent and engine law — B.3 candidate); VOIDED (needs P14C); the relationship model of §5 (ladder, drivers, decay, chemistry, D5 — B.3+); the calendar / Upcoming rows for due weeks (Ready; two owners — the Studio Calendar V1 owner for the kind, P11-REQ-029 for any Upcoming row — deliberately not taken here); Q1 override (no override stands); the Profile `renewal-open` attention text drift (Owner track); a persisted rival policy v2; the has-discipline vs M16.2 role-gate reconciliation (OPEN from B.1; a law change, not a read model).

**Tests (RED-first; test-author; independent of the implementer).** Bridge test 11 `tests/bridge-p14b2-trust.test.ts` imports `trustBlockFor` and `promiseRowsForPerson` from `bridge/trust.ts` (ABSENT → module-resolution RED before any body runs; both bindings CALLED in every group). Groups: (1) pins `PROJECTION_VERSION` 46, `LIVE_SAVE_VERSION` 29, schema `$id` projection-46; (2) Profile trust block — a person with a SATISFIED promise (a real qualifying first take: sign, greenlight, cast, schedule, advance through the 5 → 4 branch — reuse `tests/p14b1-first-take` construction) reads `Reliable` with one `promiseKept` driver dated at the take week; a person with a BROKEN one reads its driver; a person with no pair history but a studio driver reads `scope: 'studio'`; a fresh campaign reads `Mixed record` / "No record yet"; (3) Pulse — exactly one row per outcome receipt, in the `people` group at the outcome week, headline names studio and person, `detail` equals the receipt reason verbatim, `outcomeKind` set, no family/count/window in the serialized row; (4) attention — `promiseDue` present at `dueWeekExclusive − 8`, absent at `− 9`, absent for a rival-issued promise, gone the week after the qualifying take; `promiseOutcome` present on the outcome week only; a promise bound by WINNING a case whose `subjectStudioId` is a different (rival) studio — a poaching win — still raises `promiseDue`/`promiseOutcome` for the issuing studio after that case has closed (the defect-2 pin; a retention-only fixture cannot catch it); the A.1 five unchanged; (5) workspace `history.promises` equals the profile rows for the case's talent while a case is open; (6) leak both ways per (7) above; (7) save/load — a V29 round trip of a state holding kept and broken outcomes reproduces every surface byte-identically. Engine-side test 12 (ruling (viii), no engine edit expected): the D3 opportunity-breaks-a-tie synthetic construction — a forged case at the live counter on a SOLVENT rival with an unproven subject, receipts-derived, replacing the `it.todo` "opportunity breaks an otherwise 1-1 tie" in `tests/p14b1-trust-chooser.test.ts` (≈ line 288 at `e125c7f`; the file's other two todos — the both-non-incumbent tie and rival termination — stay untouched); if the construction exposes a chooser defect it is a FINDING for the Owner track, not a fix inside B.2.

**Hypotheses and decisions flagged for the audit.** The `line` copy and its U+00B7 joiner; `PROMISE_ATTENTION_WEEKS` 8; bound-only promise history; the "No record yet" caption over the engine's `Mixed record` default; the industry aggregate label (6) include/strike; the Pulse `detail` = engine reason verbatim (the four strings carry no numbers today — if a later law adds one, the bridge publishes a cause CLASS instead and the test pins it); the profile carrier being player-scoped by construction (no viewer parameter added to `peopleProjection`).

### P14B.2 tasks
- [x] **B2-F1 inherited setup/wrap prerequisite CLOSED:** `af455ef838b8c2227d784ebdc8bafb89e7a6835b`, LOGIC VERIFIED (bounded F1) · UNITY NOT VERIFIED. Lawful first-take→Post fixture at unchanged `e37cd23` could not save because completed setup retains its historical stage while current occupancy correctly releases it. Independent RED 3fail/10pass → narrow permanent-wrap validation → 17 files/179pass/3unchanged todos; root/UI and bridge typechecks PASS, final review KEEP. Exact source/evidence/limits `p14b2-20260919/08-f1-closeout.md`; no full F1 suite claimed. Separate from B2 read-model law, no version change, setup deletion or occupancy restoration.
- [x] **T0 None — no writer move; recorded.**
- **Runtime-schema carry-forward (2026-09-19 producer reconciliation):** `bridge/runtime-checkpoint.ts` requires registering each outgoing schema on a bump. T1 freezes a genuine generated projection-45 checkpoint before T2; T2 registers literal `sha256:5b2a4ca93d930e90a288db55bb5cc3fdc8eea070ef51fa1450a193a325bd755d`, with independent migration/slot-preservation coverage. This does not move Save V29 or require an outgoing SAVE-version mint. Missing historical registry identities 32–44 are an inherited separate gap, not backfilled or called supported here.
- [x] **T1 Bridge test 11 RED — CLOSED 2026-09-19**, native test-author; committed `6d23466`. `11-trust-module-red`: absent `bridge/trust.ts`, zero bodies, fixedSource true; 22 tests/nine groups, both helpers called in each group. Independent draft KEEP (`10-review`), lawful fixtures GREEN. Genuine45 checkpoint-forward RED has two expected failures (`09`). Parent owns serialized runtime; ownership `00-start.md`.
- [x] **T2 Bridge projection 46 — LANDED `3b978d7`** (sim-core bridge writer: `bridge/trust.ts`, the profile block and rows, the Pulse rows and `outcomeKind`, the two attention causes, the workspace rows, the studio row label (audit: INCLUDE; first deferral candidate if T2 overruns), schema/generated through the generator, pins 45→46, the leak pins; first confirm B.1's landed case-block `promiseHistory` is bound-only so the one-DTO-two-carriers factoring holds (audit note 5); evidence `13`–`21`; full pass still owed).
- [x] **T3 Engine test 12 — D3 synthetic construction**, landed `3b978d7`. Original five-survivor premise failed (`14`); corrected controlled real staging preserves exact two survivors, equal bands, causal compensation→opportunity winner reversal and actual binding/freeze receipt. `16`: D3/schema33pass/2unchanged todos; independent actual-test KEEP. No chooser fix. Exact provenance/limits `13-candidate-review.md`.

**Audit (contract-auditor, 2026-09-18): ADOPT WITH AMENDMENTS — applied above.** Coverage against the B.1 MOVED line and rulings (vii)/(viii) met one-to-one; disclosure law met (terms UNKNOWN, outcomes public per §4.5); hypotheses correctly isolated as §7.2-class tuning/copy calls, none an Owner question; RED precedent confirmed (`bridge/trust.ts` absent); test constructibility confirmed (the first-take construction is reusable; the line-288 todo is the one that matches). Defect 1 (citation slip: `reasons` on the receipt vs `outcomeCause` on the promise) fixed in the companion-basis paragraph. Defect 2 (the attention mechanism would exclude a poaching winner after its case closed) fixed by the independent-scan requirement in item (4) and the poaching-win pin in tests group (4). Item (6) ruled INCLUDE with the deferral note. Note 5 became a T2 check.
- [x] **T4 qualified CLOSED — LOGIC VERIFIED · UNITY NOT VERIFIED**, final corrective source `7f89f75bad5a450b50340e3ab074a913c3ef2744`. Fixed-source fullbee7e22:core3303pass/23fail/6todo,bridge741pass/13fail/2todo;22core/12bridge exact historical signatures plus one shared outgoing45 literal omission. Independent assertion-preserving two-line test correction;14files/238pass/two unchanged todos, both typechecks/contracts PASS, bounded KEEP. Closeout `p14b2-20260919-final/18-attribution.md`, backlog projection46 updated. Not all-green full rerun/Owner acceptance. Records publication/exact remote verification required before B3 T0; no production or validator weakening.

## P14B.3 — P1 quote-to-commit integration — qualified CLOSED

Gate: B2 qualified closeout/publication. Bounded execution contract:
`../evidence/p14b2-20260919/12-b3-expansion-draft.md`; native read-only audit KEEP
in `13-candidate-review.md`. This completes the existing preview-only P1 route,
not P2–P5, waiver, relationships or all of P14B. Companion §§4.1–4.4/R5 and B1
T2b's active-attachment ruling govern; no new Owner decision needed for this slice.

- [x] T0 refreshed acceptedB2tested7f89f75/publication034065b (remote verified).
  Actual three-snapshot evaluator1 mint01PASS, independent byte/provenance/producer
  KEEP02 in `p14b3-20260919`; operational entry/helper archived and removed.
  Publish this preservation checkpoint before T1. Expect Save29/projection46 unchanged
  with existing wire shapes; no governed save/runtime schema migration expected.
  Separately preserve genuine old-evaluator current/withdrawn P1 and
  refused non-actor P1 records before revision2, per the amended expansion12.
  Do not fabricate/restamp old fixtures.
- [x] T1 independent behavioral RED:03at9c605dc,22failed/12passed across34cases,
  fixedSource:true; all genuine fixture/hash/load guards pass. No missing-module/
  fixture substitute. Exact failures/patch in03and04 of `p14b3-20260919`.
  Covers abandoned unbound reservations versus
  current/bound-open ones; real session promise attachment, whole-quote refusal,
  draft-value retention, atomicity/revision/replay/session isolation and settlement.
- [x] T2 sim-core candidate complete (2026-09-19; publication/T4 still owed): shared active membership for feasibility and its digest;
  immutable shared quote/apply preparation; attach only through the existing
  reducers; nested feasibility gates outer quote/intent registration. No partial
  proposals/receipts/promise ordinals/money/revisions on failure. Preserve old
  receipts and abandoned evidence; do not rewrite loaded history. New evaluations/
  new roots use rules revision2 (dated traceability ruling in expansion12).
- [x] T3 candidate verification complete:06+09 disjoint27files/335PASS/3unchanged
  todos;all34new tests unchanged from RED. Both typechecks07/08 and contracts10/11
  PASS;independent production/neighbor review KEEP. Source/tests425170e committed,
  exact patch6129b77d… overf4e1230 rehashed. Disposition13;no full-boundary result yet.
- [x] T4 qualified CLOSED: full359f63616:52:49.457Z–18:27:20.807Z, fixedSource:true;
  core3338PASS/22FAIL/6TODO, bridge761PASS/12FAIL/2TODO. All22core/12bridge exact
  historical IDs+diagnostics, no new/changed/absent; independent14/16KEEP; both
  typechecks/contracts PASS. No correction/rerun needed. Closeout17 in
  p14b3-20260919-final; LOGIC VERIFIED · UNITY NOT VERIFIED, not all-green or Owner
  acceptance. Session26407 closed. Qualified records publication/exact remote
  verification remains the operational gate before B-F2 T1.

Independent decision boundary: existing accounting sums competing CURRENT
proposals across issuers although only one may win. Preserve that behavior in
the narrow abandonment correction; its policy remains separately OPEN. Unity,
native/rendering and Owner acceptance remain deferred.

## P14B-F2 — has-discipline promise eligibility (qualified logic closed)

- [x] T1 installed exact13case draft948936ee… plus corrected chooser/default-
  timeout patch0f85aba2… and four live revision-pin patch8a1685ae…. Parent independently
  reconstructed authorized diffs; exact. 01-independent-red on accepted
  publication58b4c2e:11failed/19passed/2unchanged todos, fixedSource:true;
  actual eligibility/version RED, no fixture exception. Attribution02 names
  later renewal/take/missing-profile guards not yet reached. Production unchanged.
  Publish RED/verify remote, then one-writer T2 release; no runtime remains.

- [x] T2 minimal candidate: ONLYpromises.ts3hunks/6insertions7deletions, acting-
  profile presence + matching digest fact + fresh evaluator3. Parent actual-diff
  review and independent04KEEP; no assignment/rival/chooser/history/shape change.
- [x] T3 candidate03+05+06:36disjointfiles404PASS/5unchanged TODO;07root/UI,
  08bridge,09contract,10fixtures all PASS, allfixedSource:true onpatch9145160c…
  over publishedRED957d2de. Alltests unchanged; no fixture correction. Disposition11.
- [x] T4 full boundary on published89b5ad2:101.1521minutes,fixedSource:true;
  core3351PASS/22FAIL/6TODO,bridge761PASS/12FAIL/2TODO, every failure exact
  historical ID+diagnostic. Both typechecks/contracts PASS, independent14/16KEEP.
  Qualified closeout ../evidence/p14bf2-20260919-final/17-attribution.md.
  Core about12% slower than B3; no performance/cause/uncompleted-assertion proof.
  Not all-green or Owner acceptance. Publish records/verify exact remote before T0.

**Authority reconciliation 2026-09-19.** The historical B1 T2 ruling(iii) above
mistakenly read the CALL `requireRole(actor, 'actor', ...)` as a primary-role gate.
The function already checked has-discipline at B1 source8bb5738. D9 OQ-1 is
explicitly RESOLVED in `docs/rev4-open-questions.md` (PM resolutions1400), and the
P14 companion4.2 expressly forbids adding a profession-label gate. The actual
promise code added that gate anyway. Native bounded contract-auditor review and
parent inspection confirm this factual contradiction; no later Owner exception
was found in the relevant authority. This paragraph supersedes the historical
OPEN classification, retaining that mistaken record as provenance. It is a
correction to existing law, not a new product choice or reopening candidate pools.

Gate: B3 qualified closeout; genuine old refused non-actor P1 evidence preserved
before its evaluator changes (scheduled at B2's accepted writer). Scope ONLY P1
feasibility's acting-discipline eligibility and its live input digest. Preserve
all capacity/window/reservation gates, primary professions, casting read-model
pools, candidate generation and rival staffing/authoring policy. Shared assignment
law itself is already correct and need not be broadened.

- Independent RED: a real available non-actor with acting skills can legally be
  cast and has an appropriately feasible P1; real completed first take satisfies
  a genuinely bound promise. Unknown person and missing acting profile refuse;
  a missing-profile probe is deliberately invalid input, not a lawful save.
  Pin eligibility-input digest sensitivity, unchanged old refused receipt/root
  through save/load, no fabricated binding/outcome, and unrelated gate controls.
- Sole production writer: match the existing acting-profile presence predicate;
  include that actual eligibility fact in the bounded feasibility digest. Use
  evaluator revision3 if B3 separately landed revision2. Historical roots/receipts
  remain unchanged on load; a future real quote/freeze can evaluate under3.
- Re-express the old rival-authoring test's false permanent-writer-impossibility
  premise without deleting its meaningful actor-authoring and policy assertions.
  No new writer-specific rival authoring/casting policy is introduced. Source
  clarification: existing `authorRivalPromise` has no primary-role filter; it
  authors P1/count1 iff the shared service says achievable. Correcting eligibility
  may therefore change which natural proposals carry promises under that SAME
  policy. Do not add a role filter to preserve the erroneous refusal. Also inspect
  the preceding first-proposal IFF test: its unconditional second `attachPromise`
  is invalid if that proposal now already carries one. Preserve independent
  authoring evidence and self-exclusion, not a double-attachment premise. Actual
  natural classifications and downstream outcomes remain verification work;
  assignment candidate pools and rival seating policy are not changed here.
- Targeted/B1–B3/neighbour checks, both typechecks/contracts, independent review,
  fixed-source boundary verification, exact failure attribution and publication
  as usual. Expected Save29/projection46 unchanged; verify actual wire impact.

Planning forecast: roughly2hours including independent real-take/continuity
tests, narrow correction/targeted checks and the observed~90minute full boundary;
regressions may add time. One production writer, at most two specialists and one
heavy runtime. Prior usage is carried forward; this is not a new resource grant.

Before P2's writer moves, mint the FINAL outgoingV29 nine-scenario corpus at the
accepted B-F2 source, not the earlier B3 producer. Its separate execution map is
in evidence23; drafts are not fixture evidence. New P2 must preserve all prior
count-only shapes, including arbitrary positive historical version numbers;
version2/3 alone cannot identify a seat-class predicate. No P2 implementation or
broader P14 completion is claimed here. Unity/native/Owner acceptance deferred.

## P14B.4 — logic families landed and qualified through record 645 (evaluator 5 later); the historical T0 note below is retained verbatim

> Status 2026-09-21 (records 616/618/628/637/645): the coordinated cutover (evaluator 4, Save V30,
> projection 47), rival P2 outcomes, the final seating preference, the `breakPromisesOnCancel`
> correction and the text pass are landed and qualified as engineering checkpoints (not Owner
> accepted); the shared status block at the top of this file governs. Original heading of this
> section: "P14B.4 — T0 preservation complete; publication next; production not started".

Prepared during frozen B-F2 full verification: `P14B4-HEADLESS-PLAN.md`, native
bounded guidance review KEEP at SHA382252e23b6353acf602d87f38032ff961e9f7f9740bbfdf2b2ae368c30df4e4.
Exact findings/dispositions: ../evidence/p14bf2-20260919/12-p2-expansion-preparation-review.md.
This does not certify the unwritten capacity adapter/solver, fixture runtime or
production release. Qualified B-F2 publication c06db6e is exact-remote verified;
T0 actual outgoingV29 plus runtime46 preservation completed with independent
KEEP in reviews06/08; proof09 and active next actions in
../evidence/p14b4-20260919/00-start.md. Actual support-seat witness is proven.
Publication/exact remote verification precedes independent test installation.
Preserve original minters/history; drafts are not executed future coverage.

## P14B.5 — First Shared-Work Bond Core — task expansion (draft 2026-09-21, audited 647-B 2026-09-22; audit applied)

**Draft provenance (647-A, sim-core READ-ONLY, paper only; 647-A2 applies the 647-B dispositions, same role, same mode).** Worktree `/Users/zacheryspector/The-Movies-headless-program`. The draft was written at HEAD `d6c11b9b`; the application was made at HEAD `107db93e` (the parent's archive of 647-A/647-B atop the T0 closeout `1117717a`) — `git diff --stat d6c11b9b..HEAD -- src bridge generated ui scripts package.json 'tests/*.ts' tests/helpers` is EMPTY, so every `file:line` below, cited at `d6c11b9b`, holds at HEAD unless the companion is cited (`docs/engineering/p14-preparation-8ef5246a/P14-PREPARATION-COMPANION.md`, Revision 3). The audit is `evidence/p14b4-20260919/647-B-p14b5-expansion-audit.md` (verdict INSERT WITH DISPOSITIONS: DEMONSTRATED D1–D3, REFINE R1–R7, rulings on OPEN 1–16, adds A1/A2, RECORD-ONLY cite slips); every disposition was re-read against the source it cites before it was applied, and the per-disposition change log is `647-A2-change-log.md` beside this file. Nothing here is an execution order, an Owner decision, a measured result or a claim that any check ran. The parent inserts this section into `docs/engineering/playability-launch-review/plans/P14-HEADLESS-PLAN.md`; the scoping it expands is `evidence/p14b4-20260919/645-A-next-family-scoping-note.md` §2–§3 and record 645 §4.

**Gate.** (a) T0 corpus: MET ON RECORD — `tests/fixtures/p14/genuine-v30-pre-b5/` (ten genuine outgoing V30 cases, `MANIFEST.json` written last, minted one-shot at `d6c11b9b`, tested V30 behavioural writer `61833f0d`, text-only `651fea8b` pinned by diff hash) and `tests/fixtures/p14/genuine-projection47-runtime/` (schema `sha256:6f6b48805aadcf14d456614d87bf1571eb1ce0d9aa0bc44f604e7976f4f85538`, projection 47, `savedSaveSha256` = the `current-p1` raw) exist at HEAD, committed `ce04e49e` and closed out in record 652 (`1117717a`) with 651-B's independent KEEP WITH RECORD-ONLY ITEMS — as the parent reports; the drafter read the two MANIFESTs, not records 648–652. (b) This expansion carries the auditor's disposition (647-B, applied here; the insertion is the parent's act). (c) T1 RED is installed against the absent `src/core/relationships.ts` — NOT YET. Source identity at T0: `src/core` has had NO non-comment change since `61833f0d` (`git diff 61833f0d..d6c11b9b -- src/core` = 2 files, 21+/9−, all comment lines — checked 2026-09-21; `651fea8b` is the only commit and it is comment-only; nothing under `src`/`bridge`/`generated` moved between `d6c11b9b` and `107db93e`); the last `bridge`/`generated` writer is `027155e7` (projection 47). `LIVE_SAVE_VERSION = 30` (`src/core/save.ts:6396`), `PROJECTION_VERSION = 47` (`bridge/schema/bridge-schema.ts:233`), schema identity `sha256:6f6b4880…` (`generated/unity/StudioBridgeDtos.Generated.cs:3`, `generated/unity/project-studio-bridge.contract-manifest.json:8`), `PROMISE_RULES_VERSION = 4` (`src/core/promises.ts:45`). Save V31 / projection 48 are ALLOCATED AT EXECUTION, not here (plan §3 :409-410).

**Companion basis.** §5 header :393-395 (deliverable F realizes rulings §3.4.1 items 12–13; "IMPLEMENTATION RECOMMENDATION; every threshold, weight and horizon is a NUMERICAL/CONTENT HYPOTHESIS"); §5.1 :397-407 (the original's one 0–100 pair scalar with named bands, the 45–55 random baseline and decay toward it, work-context growth, relationships as a bounded chemistry read at production, no durable history — carried as a tier ladder, decay-to-baseline, work-driven growth, bounded disclosed consequences, durable history); §5.2 :410-417 (two facts per pair — CURRENT closeness and CAREER history :412; evidence before tier, no all-pairs matrix, no weekly scan, drift computed on read from `lastSharedEventWeek` :413; every change has a readable reason :414; no social commands :415; one ladder :416; evidence joins on employment intervals :417); §5.3 :419-433 (the ladder: Nemeses :423, Enemies :424, Strained :425, Acquaintances :426, Colleagues :427, Friends :428, Close Friends :429, Inseparable :430, Partners "formed on the separate romance track … held alongside the friendship tier" :431; "tiers are derived from the closeness value and the evidence record by a versioned rule; the value is never shown" :433); §5.4 :435-449 (drivers by shape: shared production with proximity weights :439, shared success :440, shared failure :441, repeated collaboration :442, casting competition :443-444, cancelled after first take :445, awards :446, dormancy drift :447, romance :448-449); §5.4a :451-462 (romance as a separate track — EXCLUDED here); compatibility prior :463 (→ Q2); §5.5 :465-470 (drift hypotheses 52 / 260 weeks, negative tiers drift too :467; history append-only in meaning and bounded by a stated compaction rule, "no compaction changes a current tier, a milestone date or a replay result" :468; Partners exempt :469; dormant edges :470); §5.6 :472-482 (consumers: production quality by the result owner :476, casting warning :477, the person-choice rule's reservation and D5 descriptor :478, retention attention :479, no Star Power effect :480; `pairChemistry` as a SHARED GENERALIZATION — "P14 never writes a quality number" :482); §5.7 :484-489 (canonical edge key, hot/cold split, weekly cost proportional to the week's events, the August envelope only under compaction, "no edges are fabricated from old credits by default" :489). §2.1.7 :102-120 (reservation "no `Nemeses`-tier relation of the person is on the issuer's roster" :104; D5 row :116 "`close ties here` (Close Friends / Inseparable / Partners on the roster), `none`, `enemies here` | neutral in P14A"; the priority-order rule :120 — unproven and under-30: opportunity, compensation, relationships, term, trust, Standing, incumbency; proven veterans: compensation, term, trust, relationships, incumbency, Standing, opportunity). §8 :635-646 (items 2 governed save step :640, 3 no RNG and no order dependence :641, 4 identity and validator-enforced recording boundary :642, 5 no fabricated pre-P14 relationships :643, 6 no weekly pair scan :644, 7 measurement before sealing :645). R8 :596, R16 :604, R17 :605, R22 :610.

**Settled law (not re-asked; §7.1 by number).** S19 :576 (the Movies+ model: positive, negative, acquaintances, friendships, close bonds, deep bonds, romance, enemies/nemeses, chemistry consequences, growth through shared work, explainable effects); S20 :577 (no social grind; work is the primary driver; the player never commands "talk"); S21 :578 (current closeness may decay; career history never disappears); S22 :579 (romance regardless of gender; no marriage, children, households, lineage, inheritance, domestic needs in P14); S23 :580 (ownership unchanged; `PersonId` immutable; campaign identity scopes every cache, receipt and pending item); S25 :582 (deterministic replayable choice; symmetric player/rival law). Owner ruling 600 §4 (`evidence/p14b4-20260919/600-owner-ruling-578-reopened-program.md:148-153`): one heavy test process; max two specialists, one production writer; the writer never commits; the parent lands patches through the index; record-check for every run; "no cap, tariff, refusal, deadline, timeout or fixture moves to obtain a pass"; Unity/native deferred; no Owner-acceptance claim. 600 §3 step 5 (:134-135): the writer makes "no test edits". 600 §1 D2 :29-36 (the 200,000 cap and the 162 §2 metric fixed; evaluator 5 later). Landed and NOT reopened: the B.4 seating law (`hollywoodTick.ts:164-170`; record 628's three questions are PENDING OWNER, nothing adopted), the cancel causal proof (`promises.ts:667`, record 637), evaluator 4 and `PROMISE_RULES_VERSION 4`.

**Why this slice, and what it excludes.** The relationship model is the one selected P14B experience with zero engine surface (no `relationships`, `pairChemistry`, `closeness` symbol in `src/`/`bridge/`; 645-A §0), the chooser has carried a documented NEUTRAL D5 slot since A.1 (`src/core/talentMarket.ts:659-662`, whose text still reads "It arrives with P14B.2"), it precedes P14C in the companion's own dependency order (§6.3 :527 consumes collaboration evidence) and in the plan's slice order (:407-409), and its three §7.3 entries each carry a recommended first behaviour — the footing B.1 used for Q1/Q3 (plan :518, the OPEN paragraph; 647-B cite correction from :514). The slice: edges minted from shared first takes, release success/failure and the player cancel; drift-on-read tiers by a versioned rule; D5 live in the chooser; the `nemesisOnRoster` reservation drop enumerated; a read-only `pairChemistry` export with no consumer; Save V31; projection 48 thin. EXCLUDED, each recorded with its owner: the romance track (§5.4a :451-462; own value, own threshold, own drivers, Partners coexistence — a later slice with its own V-step); the casting-competition driver and repeated seat competition (§5.4 :443-444; the only lawful source today is the player-only `CastingSession` (`src/core/types.ts:892-902`) — rivals run no session, `decide()` seats directly (`hollywoodTick.ts:154-175`, the seating law at :164-170) — so a B.5 driver would be asymmetric under S25; deferred with that reason); chemistry CONSUMPTION by the production result owner (§5.6 :476, :482; R17 — the bound constant is the result owner's, `src/core/reception.ts` `ReceptionInputs` :61, a separate shared-generalization slice); the casting warning (§5.6 :477; Q4's recommended first behaviour; a bridge/read-model item); shared awards (:446, P08 by reference); the retention attention item (:479); evidence labels Mentor/Rivals (:433, HIS-014); THE WRITER SEAT (§5.4 :439 counts writer seats "at the lowest weight"; the receipt carries no writer, `types.ts:2135-2142` — scope cut; `Production.writerId` (`types.ts:242`) is joinable at the tail from the delta entry, so a later slice adds the writer pairs without a V-step to the receipt; 647-B R6 on former OPEN 13); TIER-TRANSITION PERMANENCE (§5.5 :468 "tier transitions with their driver … are permanent" — B.5 persists `peakTier`/`peakTierWeek` and the exact counters through folding, not a transition log; the compaction rule's permanent records are a later slice's; 647-B R6); THE HOT/COLD INDEX SPLIT (§5.7 :486, §5.5 :470 — B.5 holds one root array, no hot index and no cold paging, until PERF-010 measures the need; 647-B R6); PER-DRIVER DECAY (§5.4 :441 "decays quickly" for the failure driver — B.5 has one drift horizon per edge, not a per-kind decay; a later constant; 647-B R6); EVERY read model — profile relationship block, drivers text, chemistry rows, attention rows, labelled shared-credit counts (Q3) — → P14B.6 on the B.2 pattern (plan :539-577: read models over the landed facts, no engine law); the waiver → the slice after B.5 (645-A §2 "The waiver as the following slice"; its one V31-candidate field `supersededByPromiseId` is NOT bundled here unless Fable rules otherwise — see OPEN 15); VOIDED (needs P14C; 645-A §1(c)); evaluator 5 (600 §1 D1/D2; the designated case `tests/p14b4-cast-class-capacity-evaluator5.test.ts:235-247` stays RED by law); P2–P5 families beyond the landed tagged P2; everything §7.4 :627 lists later (social events, press/scandal, P15 legacy interpretation of relationship facts, Upcoming rows).

**Engine today (verify at T1).** Facts a relationship edge can be minted from without a new producer: `FirstTakeReceipt {eventId, week, productionId, studioId, directorId, cast: Record<CastSlot,string>}` (`types.ts:2135-2142`; `CastSlot = 'lead'|'antagonist'|'support'` :20 — four people, six pairs; NO writer seat on the receipt), appended ONCE per production at the tick tail for player and rival takes alike (`promises.ts:120-143`, idempotent by `productionId`; the guard order is `entries.length === 0` → return, then `requirePromiseRoots`, then `hollywood === null` → return, :125-127; called at `tick.ts:1102-1109` with the delta entries `productionAdvance.firstTakes` + `industry.firstTakes` (:1104-1107) and `finalized.market.tick`, the week the advance PRODUCES); player releases `state.studio.releasedFilms: FilmResult[]` (`types.ts:253-275`: `productionId`, `releaseTick`, `criticScore` :261 always present, `boxOffice {opening,total}`, optional `forecast` :275; pushed at `tick.ts:642` with `releaseTick: currentTick` = the pre-increment week, :192, :623 — one less than the tail week); rival releases `hollywood.films` `LiveIndustryFilm {filmId (= productionId), result: FilmResult, …}` (`hollywoodTypes.ts:37-45`; `result.releaseTick = week` = the pre-increment week, `hollywoodTick.ts:241, :278`; appended at :286, receipt `filmReleased` :292; the same `filmResult` is handed to the outer tick in `growth` :293-295); the player cancel `applyCancel` (`actions.ts:567-600`) whose tail already branches on `hollywood.playerStudioId` into `breakPromisesOnCancel(withoutProduction, playerStudioId, cancelled)` :592-599 — NOTE `breakPromisesOnCancel` returns early when a first take EXISTS for the picture (`promises.ts:788`); the relationship driver fires on exactly the complementary condition; no rival cancel verb exists (`hollywoodTick.ts:271` only drops released pictures). Rosters: `hollywood.employment` rows for player and rival studios alike (`hollywoodTypes.ts:69-74`; reasons include `player-contract`/`existing-player-contract`), read through `activeEmploymentOrdinals` (`hollywoodTick.ts:54-56` `currentEmployees`; `talentMarket.ts:640-643`, :1044-1052 `seatsHeldAfter`). THE TAIL ORDER AT W (647-B D1's source facts): `finishHollywoodWeek` runs at the tail on `finalized.market.tick` = W BEFORE `appendFirstTakes` and the market step (`tick.ts:1102-1110`) and closes every row with `endWeekExclusive <= W` — `endedWeek: W`, ordinal removed, `expiry` receipt (`hollywoodTick.ts:332-343`); a commit inside the settlement pass writes a row with `startWeek: week` = W (`talentMarket.ts:922` player, :963 rival; `:1069` drops any proposal whose start is not W; `:1111` finds the committed row by `startWeek === week`); the settlement loop iterates `openCasesAt` in ARRAY order (`:1245-1248`, :1252-1254) — the order plan :495 A.3 T1 (i) already flags as an Owner-side item. Chooser: `DESCRIPTOR_ORDER` six members (`talentMarket.ts:668`), `DESCRIPTOR_REASON` :671-678, `isProven` :685-688, `priorityOrder` :698-702, `bandsFor` :779-816 (D4 reads `trustDescriptor` at :811-812), `pairwiseWins` :818, `dominates` :822, `chooseProposal` :833-886 (priority-order tie-break :857-864; a descriptor's sentence enters `reasons` only when the winner's band STRICTLY exceeds EVERY other survivor's, :880-884), `FreezeDrop` :999-1010, `DROP_SENTENCE` :1022-1032, `survivesFreeze` :1057-1099 (`issuerDistrusted` at :1067, ahead of the seat budget by design); the chooser receipt `{eventId, kind, week, talentId, studioId, reasons, dropped}` (:908) carries no rules version. Recording boundary precedent: `trustDrivers` reads nothing before `state.studioHistory.recordingStartedWeek` (`promises.ts:828-834`); the root validator's `recordedWeek` refuses rows outside `[boundary, market.tick]` (`promises.ts:954-960`) and `personId`/`studioId` refuse ids not in `state.talent` / not entered (:969-978). Save device: own validator → strip → frozen prior (`stripV29Roots` `save.ts:8583-8600`; `validateSaveV30` :8679-8695 requires both V29 roots by `Object.hasOwn`; `convertV30ToV29` :8704-8710 refuses a tagged predicate; `migrateToV30` :8714-8717; the dispatch :5282-5285 with the "1 through 30 only" sentinel; `saveVersion === 30` appears at 24 sites: the dispatch, 18 downgrade throws (`migrateToV8`…`V25`, `V20` :8737), 4 route lines (`migrateToV26` :8340, `V27` :8491, `V28` :8572, `V29` :8670) and `migrateToV30` itself). Determinism: no RNG reaches the tail (`appendFirstTakes`/`advancePromisesWeek`/`advanceTalentMarketWeek`); `rngState` is pinned untouched by `tests/p14b1-promises.test.ts` test 3 and `tests/p13a-rival-adoption.test.ts`. Process-global caches: the one identity-keyed `WeakMap` at `src/core/hollywood.ts:50`; B.5 adds none. Wire: `priorityOrder` enum CLOSED at six (`bridge-schema.ts:2320-2324`), published by `marketCaseProjection` (`bridge/people.ts:977-991`; the `line` reads keys `[0]` and `[1]` through `DESCRIPTOR_LABEL` :866-875, a `Record<string,string>` whose totality is convention); `settlementReasons: string[]` carries the engine's `reasons` verbatim (:998; schema `array(nonEmptyText())` :2346) — free text, no enum; the generated C# holds `string[] priorityOrder` (`StudioBridgeDtos.Generated.cs:6492-6493`), not a C# enum. Registry head `projection-v46` (`bridge/runtime-checkpoint.ts:63`); `tests/bridge-p14b4-runtime47-compatibility.test.ts:162` pins "exact 35 prior IDs". Fixtures: `tests/fixtures/p14/` holds `genuine-v29-pre-p2/` (nine cases), `genuine-v29-pre-b3-evaluator1/`, `genuine-projection46-runtime/`, `legacy-v28-*` (`legacy-v28-shooting-5.json.gz`: V28, week 16, `hollywood: null`, plan :523), and — since `ce04e49e` — `genuine-v30-pre-b5/` (ten cases) and `genuine-projection47-runtime/` (one checkpoint), the T0 corpus.

**Scope — engine (Save V31).** (1) ROOT `relationships` at the TOP LEVEL beside `talentMarket`/`firstTakes`/`promises` (R22 :610 "roots at top level, never inside `hollywood`"; the V29 precedent `types.ts:2205-2210`), version = the save version (no second version field; the V28 root comment `types.ts:2120-2122`). `GameStateV31 = GameStateV30 & { relationships: readonly RelationshipEdge[] }`, `GameState = GameStateV31` (`types.ts:2234` moves). PROPOSED SHAPE (field names mechanical and reversible exactly as B.1's `promises: readonly string[]` was, plan :518; the T1 RED pins the fields it reads, T2 may rename with the ruling recorded):
```
RelationshipEdge = {
  edgeId: string                 // in-state ordinal `relationship-edge-<i>` (the `first-take-event-<i>` device, promises.ts:992)
  a: string; b: string           // canonical: a < b by code-unit comparison; both in state.talent (§5.7 :486)
  closeness: number              // integer 0..100 AT lastEventWeek (drift materialized, then the delta applied)
  firstSharedWeek: number
  lastEventWeek: number          // the drift anchor (§5.5 :467 "lastSharedEventWeek"): the week of the last driver of any kind
  sharedProductions: number; sharedSuccesses: number; sharedFailures: number; sharedCancellations: number   // exact, never compacted
  peakTier: RelationshipTier; peakTierWeek: number
  recent: readonly RelationshipDriver[]   // ≤ RELATIONSHIP_RECENT_CAP, oldest first; the overflow folds OUT at write (counters keep the count)
}
RelationshipDriver = { kind: RelationshipDriverKind; week: number; ref: string; delta: number }   // ref = productionId; week = state.market.tick at the write (647-B R1); no prose persisted (kind → copy at read, the B.2 pattern)
RelationshipDriverKind = 'sharedProduction' | 'repeatedCollaboration' | 'sharedSuccess' | 'sharedFailure' | 'cancelledAfterFirstTake'
RelationshipTier = 'Nemeses' | 'Enemies' | 'Strained' | 'Acquaintances' | 'Colleagues' | 'Friends' | 'CloseFriends' | 'Inseparable'   // the eight-member friendship ladder; Partners is the romance track, not a rung (647-B ruling (ii) on former OPEN 8: §5.3 :431 and §5.4a :453 hold Partners "alongside" the tier; R16 :604's "nine tiers" is recorded as met when the romance leaf lands, additively)
```
(2) EDGE MINTING at ONE seam: `advanceRelationshipsWeek(state, delta, week)` in `src/core/relationships.ts`, called at the tick tail between `appendFirstTakes` and `advancePromisesWeek` (`tick.ts:1102-1110` — the takes are already a fact, the market has not yet ranked on D5). It MINTS FROM THE ADVANCE'S DELTA, never from a scan of a root (647-B R3; §5.7 :487 "proportional to the week's shared-work events", §8 :644): `delta = { takes, releases }` where `takes` is the same `entries` array the tail hands `appendFirstTakes` (:1104-1107) and `releases` is this advance's `FilmResult`s — the player's entries pushed at :642 beyond `admitted.studio.releasedFilms.length` (:528) and the rivals' `industry.growth[].filmResult` (:929; `hollywoodTick.ts:293-295`). Guard order copied from `appendFirstTakes` :125-127 (647-B record-only): an empty delta → return `state` unchanged BEFORE the root guard; then the LOUD root guard `requireRelationshipsRoot` on the `requirePromiseRoots` model (`promises.ts:88-92`); then `hollywood === null` → return `state` unchanged, so the headless M0A corpus and the roster-wall observatory stay byte-identical apart from the empty root (`src/harness/roster-wall/historical-control.ts:23-26` strips it from the hash exactly as it strips `firstTakes`/`promises`). Every driver written at the tail is stamped `week = state.market.tick` (the tail week, as takes are; 647-B R1), so `lastEventWeek = week`, the validator's `driver.week ≤ lastEventWeek` and the release step's `releaseTick` (= tail − 1) are one convention: the driver `ref` names the `productionId`, and the `FilmResult` carries `releaseTick` for any read model. (2a) `sharedProduction` + `repeatedCollaboration`: for each take entry in the delta (the seats read from the entry's `production`, the same four ids `appendFirstTakes` copies at :133-140, whose receipt is now in the root — never a scan of `firstTakes` by week; that is how Q3's no-backfill holds by construction on a migrated save, 647-B ruling on former OPEN 12), every pair among `{directorId, cast.lead, cast.antagonist, cast.support}` in the receipt's own seat order (director–lead, director–antagonist, director–support, lead–antagonist, lead–support, antagonist–support — never `PersonId` or Map order for the append order; the canonical KEY may compare ids), with proximity weights (§5.4 :439; HYPOTHESES `RELATIONSHIP_PROXIMITY_HIGH` for director–lead and lead–antagonist, `_MID` for director–antagonist and lead–support, `_LOW` for director–support and antagonist–support) and, when `sharedProductions` after this take is ≥ 2, a `repeatedCollaboration` driver of `min(sharedProductions − 1, RELATIONSHIP_REPEAT_CAP)` (:442 "increasing, capped"). A person seated twice on one take (the engine forbids it: `assertGreenlightStaffingIdle`; rival seats are distinct by construction `hollywoodTick.ts:167-175`) is refused loudly, never skipped silently. Idempotent by `(edgeId, kind, ref)`: replaying the tick from the same state mints nothing twice. (2b) `sharedSuccess` / `sharedFailure` at RELEASE, from the delta's `FilmResult`s (647-B ruling (i) on former OPEN 10 — the tail seam, symmetric, one place; ruling (i) on former OPEN 9 — `result.criticScore`, `types.ts:261`, always present, never the optional `forecast`): each release joined by `productionId` to its `FirstTakeReceipt` (the same join the trust derivation uses for the cancel case, `promises.ts:862-875`); the six pairs of that take receive `+RELATIONSHIP_SUCCESS_DELTA` when `result.criticScore ≥ RELATIONSHIP_SUCCESS_CRITIC_SCORE`, `−RELATIONSHIP_FAILURE_DELTA` when `< RELATIONSHIP_FAILURE_CRITIC_SCORE`, nothing between (:440-441 "above a threshold"). Idempotent by `(edgeId, kind, productionId)`. The owner of the RELEASE FACT stays the tick's release step and `advanceHollywoodWeek`; B.5 adds no receipt — the driver `ref` names the film's `productionId`, and the `FilmResult` is the evidence. (2c) `cancelledAfterFirstTake` at the PLAYER CANCEL, synchronously (the B.1 T1 ruling (i) form): `recordCancelledAfterFirstTake(state, playerStudioId, cancelled)` called in `applyCancel` beside `breakPromisesOnCancel` (`actions.ts:592-599`; `breakPromisesOnCancel` itself untouched), firing iff `state.firstTakes` holds a take for `cancelled.id` — the complement of :788 — for that take's six pairs with `−RELATIONSHIP_CANCEL_DELTA` (:445 "small"), stamped `week = state.market.tick` at the action (as :787 stamps). A cancel of a picture WITHOUT a take is not shared work and mints nothing (:445 "after its first take"; S20). Rival symmetry: the LAW is the same pure helper; no rival cancel verb exists (source fact above), so the reachable set is player-only exactly as the termination trust driver was (B.1 T1 ruling (v), :525). (3) VALUE, DRIFT, TIER — pure, integer, no RNG. Scale by 647-B ruling (i) on former OPEN 4: integer 0–100, `RELATIONSHIP_BASELINE = 50` fixed (the original's random 45–55 replaced by one constant; §8 :641 forbids a new stream without an Owner-authorized `RngPurpose` widening), the tier edges as named constants mirroring the original bands (§5.1 :401, §5.3 :423-430: Nemeses ≤ 10, Enemies 11–30, Strained 31–44, Acquaintances 45–55, Colleagues 56–60, Friends 61–70, Close Friends 71–80, Inseparable ≥ 81 — HYPOTHESES, pinned by name, none settled). A write materializes drift first (`closeness = currentCloseness(edge, week)`), applies the delta, clamps to `[0, 100]`, sets `lastEventWeek = week`, updates `peakTier`/`peakTierWeek` when the new tier is higher than the stored peak, appends the driver and folds the oldest out of `recent` past the cap (counters are the fold target: §5.5 :468 "fold into per-edge exact counts"). `currentCloseness(edge, week)`: `dormant = week − lastEventWeek`; `≤ RELATIONSHIP_DRIFT_GRACE_WEEKS` → stored value; else `closeness + trunc((BASELINE − closeness) × min(dormant − GRACE, RETURN) / RELATIONSHIP_DRIFT_RETURN_WEEKS)` — toward the baseline from either side, never past it (§5.5 :467 hypotheses 52 / 260). `currentTier(edge, week)` under `RELATIONSHIP_RULES_VERSION = 1` (code-only: the chooser receipt carries no rules version, the B.1 precedent; 647-B record-only): value bands with the named edges PLUS the evidence conditions of §5.3 — Nemeses and Enemies require a conflict record (:423-424 "open hostility on the record" / "recorded conflict"); B.5 mints NO conflict-record kind, so a value in those bands reads Strained (:425 "recent negative driver without a conflict record"; in B.5 the only producers of a value below the Acquaintances floor are the negative drivers of (2b)-(2c), so the band reading and :425's evidence condition coincide). Drift never moves a tier AWAY from Acquaintances; counters, `firstSharedWeek`, `peakTier`/`peakTierWeek` never change by drift or compaction (:468 "no compaction changes a current tier, a milestone date or a replay result"). (4) REACHABILITY IN B.5 (paper, from the sign shape of §5.4 and the tier rule of (3); 647-B ruling (ii) on former OPEN 5): the RELEASE-FAILURE driver is the reachable negative — `RELATIONSHIP_FAILURE_DELTA > RELATIONSHIP_PROXIMITY_LOW` is pinned by NAME (a relation between two hypotheses, not a value), so a released flop nets a low-proximity pair (director–support, antagonist–support) below where it stood before the take; from a pair at the Acquaintances floor, or through repeated flops, the value crosses into the Strained band and `pairChemistry.sign` reads −1 (§5.6 :476 has a producer). `RELATIONSHIP_CANCEL_DELTA` carries no reachability pin either way (:445 "small"; a hypothesis). Enemies and Nemeses are UNREACHABLE BY RULE — a conflict record is required and B.5 mints no conflict-record kind — independent of any constant; hence `enemies here` and `nemesisOnRoster` are enumerated and never emitted, and pinned so (the A.1 `decision_pending` / B.1 WAIVED-VOIDED precedent, `tests/p14b1-promises.test.ts:845-850`; the conflict driver is deferred on S25 grounds above). Whether Strained is ever READ on the standard seeds is a T4 measurement, not a pin. (5) D5 IN THE CHOOSER (companion :116, :120; R8): `DESCRIPTOR_ORDER` 6 → 7 with `relationships` between `trust` and `standing` (`talentMarket.ts:668`); `bandsFor` (:779-816) gains `relationships`: for the case's subject at W, over the subject's edges, `close ties here` (2) iff some counterpart is on the ISSUER's roster at W with `currentTier ∈ {CloseFriends, Inseparable}` (Partners does not exist), `enemies here` (0) iff some counterpart is on that roster with `currentTier ∈ {Enemies, Nemeses}` and no close tie (precedence when both: OPEN 11, unreachable), else `none` (1). "ON THE ISSUER'S ROSTER AT W" (647-B D1, ruling on former OPEN 7): one shared helper over the issuer's `hollywood.employment` rows, subject excluded, with the predicate `terms.startWeek < W && (endedWeek === null || W < endedWeek)` — STRICT at W on both ends. The `:640-643` form (`startWeek <= week`) is NOT used: a row committed earlier in the same settlement pass carries `startWeek === W` (:922, :963) and the pass runs in `openCasesAt` array order (:1245-1248), so under `<=` a rival issuer's "roster at W" at a synchronized churn would be exactly the colleagues re-signed earlier in the pass — the first-settled case would see no friends and the last all of them, array-order dependence in a contested choice (§2.1.7 :102; §8 :641). Under the strict form a row `finishHollywoodWeek` closed at W (`endedWeek === W`, `hollywoodTick.ts:332-343`) and a row the pass committed at W are both OFF the roster: the helper reads the roster as it stood when the pass began, which is order-independent (a same-week entrant's founding rows are excluded too — immaterial, since submission requires entry, `talentMarket.ts:409`). `DESCRIPTOR_REASON.relationships` = one ordering-only sentence naming no person, tier or number (CANDIDATE wording `their roster holds this person's close ties`; the contract is the sentence's class, not the prose). `priorityOrder` (:698-702) regains `relationships` at its companion positions: unproven `['opportunity','compensation','relationships','term','trust','standing','incumbency']`, proven `['compensation','term','trust','relationships','incumbency','standing','opportunity']` (:120); `isProven`/`preferredTerm` untouched (the F1 decoupling survives). The comment block :651-662 is corrected at T2 to name B.5 (its "It arrives with P14B.2" is text; 647-B on former OPEN 14(a)). (6) RESERVATION: `FreezeDrop` gains `nemesisOnRoster` (:104), with a `DROP_SENTENCE` entry (CANDIDATE `${studio}'s roster holds someone this person will not work beside.`), checked in `survivesFreeze` immediately after `issuerDistrusted` (:1067) and before `subjectCommittedElsewhere`: true iff a counterpart of a `Nemeses`-tier edge of the subject is on the issuer's roster at W under the SAME helper and predicate as (5). UNREACHABLE in B.5 by (3)-(4): Nemeses needs a conflict record; pinned never emitted on the standard seeds. (7) `pairChemistry(state, a, b, week)` EXPORTED, read-only, NO consumer: `{ tier: RelationshipTier | null, sign: -1 | 0 | 1, reasons: readonly string[] }` — `null`/`0`/`[]` when no edge exists; `sign` +1 for Colleagues and above, −1 for Strained and below (§5.6 :476; −1 reachable in B.5 by (4)), the reasons derived from `recent` kinds and counters with no number in any string (the B.1 leak pattern). The result owner's bound, formula and seam are NOT designed here (R17; :482 "P14 never writes a quality number"). (8) DETERMINISM: no RNG (none needed — `rngState` byte-equal before/after every B.5 write, pinned); no dependence on array, Map/Set, `PersonId` or row order for any CHOICE (the canonical key and the receipt seat order are identities, not choices; the (5) roster predicate is order-independent within the pass by construction); copy-on-write reducers; no module-level cache; no session/campaign id in core (S23 :580; §8 :641-642); Save As copies sharing ids stay independent because the root lives in `GameState`. (9) CENTURY-SCALE BOUND (§8 :639, :644-645; §5.7 :487-488): weekly cost proportional to this week's takes, releases and cancels, never to the population; storage = edges × (fixed fields + ≤ `RELATIONSHIP_RECENT_CAP` drivers), fold-at-write by count (647-B ruling (i) on former OPEN 6: no weekly sweep, no window arithmetic; the ceiling is that a B.6 profile shows at most `RELATIONSHIP_RECENT_CAP` recent drivers, and the §5.5 :468 windowed 260-week compaction is the recorded upgrade path in a `ponytail:` comment once PERF-010 measures the need; :468's tier-transition permanence is an exclusion above); the paper worst case is `firstTakes.length × 6` edges (rosters are stable, so far fewer distinct pairs in practice); the measured edge and driver counts on the standard seeds at week 260 and on the endurance scenario are T4 evidence items (the 645-A read-only research item), not claims here. (10) MIGRATION: `SaveFileV31 {saveVersion: 31, seed, state: GameStateV31, broadcastCache}`; `validateSaveV31` validates the envelope keys, `saveVersion === 31`, `Object.hasOwn(raw,'relationships')`, then `validateRelationshipsRoot(raw)` (in `relationships.ts`: array; each row exact keys; `edgeId` = the ordinal; `a < b`; both in `state.talent`; no duplicate pair; `closeness` integer 0..100; `firstSharedWeek ≤ lastEventWeek`, both inside `[studioHistory.recordingStartedWeek, market.tick]` (the `recordedWeek` device, `promises.ts:954-960`; 647-B ruling (i) on former OPEN 12: the campaign's own boundary is reused, no per-root anchor — B.1 satisfied §8 :642 the same way for a P14 root, and edges are minted only by live events); counters non-negative integers with `sharedSuccesses + sharedFailures ≤ sharedProductions` and `sharedCancellations ≤ sharedProductions`; `peakTier` in the catalogue; `recent.length ≤ RELATIONSHIP_RECENT_CAP`, each driver's kind in the catalogue, `week` recorded, `ref` a non-empty string, `delta` an integer; every driver `week ≤ lastEventWeek`), then strips `relationships` and delegates to `validateSaveV30` (the `stripV29Roots` device). `convertV30ToV31` opens the root EMPTY (`relationships: []`) and recomputes NOTHING — a campaign written before edges existed formed none (Q3; §5.7 :489; `convertV28ToV29`'s comment :8620-8628). `convertV31ToV30` asks `projectRelationshipsPreV31(rawState)` BEFORE validating the envelope (the `projectPromisesPreV29` precedent :8651-8661, `promises.ts:1142-1157`): refused with "cannot downgrade SaveFileV31 or discard the relationship record" when ANY edge exists; lossless when empty. `migrateToV31` is the live route (`31` → validate; else `convertV30ToV31(migrateToV30(save))`); `migrateToV30` gains `if (save.saveVersion === 31) return convertV31ToV30(...)`; `migrateToV26/27/28/29` each gain the 31 route line beside their 30 line (:8340, :8491, :8572, :8670); the 18 downgrade throws each gain a `saveVersion === 31` line; `validateSave` dispatches 31 and its sentinel becomes "1 through 31 only"; `LIVE_SAVE_VERSION = 31`; `makeSave` stamps 31 through `validateSaveV31`; `worldgen.ts:783-785` seeds `relationships: []`; `src/core/index.ts` exports the V31 API beside the V30 block (:1292-1296) and a `relationships` block on the promises pattern (:1394-1427). (11) LIVE-VERSION SWEEP (values only; the `6948e31` / 600-T2 precedent), by class: LITERALS (`saveVersion: 30`, `toBe(30)` — including `tests/bridge-p14b2-checkpoint.test.ts:44` — `PROJECTION_VERSION` 47 pins, the schema-id literal `6f6b4880…` at `tests/bridge-contract-generator.test.ts:562-565`, the F10/F11 generated-C# sentinel, the eight UI pins of 600-T3 in `ui/src/{saves,session}.test.tsx`, `ui/src/lot/snapshot/v14SetHolderBoundary.test.ts`, `ui/src/engine/{d17-save-migration,film-chronicle-adapter}.test.ts`); SENTINELS (`tests/p14b1-save-v29.test.ts:196-201` "sentinel 31 / 1 through 30 only" → 32 / "1 through 31 only"; the sentinel `it` moves to 32); FROZEN BUILDERS / HARNESS (`tests/contracts/_v14Contract.ts:411-417` V13-twin strip gains `relationships` with its "cannot discard" guard; `src/harness/roster-wall/historical-control.ts:23-26` hash strip; `src/harness/p14/legacy-v28-fixtures.ts`, `src/harness/d16/run-d17b-*.ts` V30 arms; the load/runtime arms `bridge/session.ts`, `bridge/runtime-checkpoint.ts`, `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts` → `migrateToV31` on the `2cbc3de5` precedent); ID ROSTERS AND SIZE BOUNDS (`SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` gains one entry → the "exact 35 prior IDs" pin at `tests/bridge-p14b4-runtime47-compatibility.test.ts:162` reads 36, `EXPECTED_PRIOR_IDS` :39-75 gains `6f6b4880…` in sorted position; the seven-member `priorityOrder` arrays at `tests/p14a1-f1-priority-order.test.ts:86,91`, `tests/p14b1-trust-chooser.test.ts:172-173`, `tests/bridge-p14b4-cast-class.test.ts:410-411` and `tests/p14b4-cast-class-policy.test.ts:30-31` (asserted at :52 through `publicPriorityOrder`; 647-B A1) — grep 2026-09-22: these four test files and the schema enum are the only six-member literals). The sweep is a SEPARATE values-only patch from the production patch; ownership by file class is stated in Tasks (647-B R5). (12) NOT TOUCHED: `promises.ts` — the evaluator (`promiseFeasibility` :388, `reclassifyPromise` :459, `targetSpecificImpossibility` :667, `breakPromisesOnCancel` :785-803, `breakPromisesOnTermination` :759), `PROMISE_RULES_VERSION 4` and every hypothesis constant :53-78, the trust derivation :822-901 (relationships are not a trust input: §4.5 lists none), the promise root validator :927-1140; `promiseCapacity*` and the replay module (`promiseCapacityOwnerReplay.ts` SHA `9e97ffed…`, unwired by 600 §2); `hollywoodTick.ts` `decide()`/`staff()` and the seating preference :164-170 (628 pending); `finishHollywoodWeek` :332-343 and the settlement order :1245-1248 (the D1 fact is read, not changed; the order stays plan :495's Owner-side item); `authorRivalPromise` (`talentMarket.ts:1275`); the 200,000 cap and the 162 §2 metric (600 §1 D2); `TUNING`; `STANDING_BAND_TOLERANCE` :746, `RIVAL_TEAM_ROLES` (`hollywoodStartingData.ts:46`), every existing `DROP_SENTENCE` string, timeout, refusal string and historical fixture; every V12–V30 validator and frozen projection; the 302 stale-route RED; the evaluator-5 designated case; the cancel seam's proof — `applyCancel` gains one call beside the existing one, and `breakPromisesOnCancel`'s law, strings and tests (`tests/p14b4-cancel-causal-proof.test.ts`) are unchanged.

**Scope — bridge (projection 48), THIN — forced, not optional.** D5 live means `publicPriorityOrder` returns seven members and `marketCaseProjection` publishes it (`bridge/people.ts:977`) against a CLOSED six-member wire enum (`bridge-schema.ts:2320-2324`), the exact situation that forced B.1's 44 → 45 (backlog :526 "The forced bump adds `opportunity` and `trust` to the priority-order enum"). The WHOLE wire change: (a) the `priorityOrder` enum gains `relationships` (schema :2320-2324; `PROJECTION_VERSION = 48` :233; `$id` `urn:project-studio:bridge:protocol-4:projection-48`); (b) `DESCRIPTOR_LABEL` gains `relationships` (`people.ts:866-875`; CANDIDATE `friends on the roster`) so the record stays total — the `line` VALUE is unchanged for both archetypes because `relationships` sits at index 2 (unproven) and 3 (proven) and the line reads `[0]`/`[1]` (:991); (c) the outgoing 47 identity `sha256:6f6b48805aadcf14d456614d87bf1571eb1ce0d9aa0bc44f604e7976f4f85538` registered as `projection-v47` at the head of `SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` (`runtime-checkpoint.ts:59-63`, the `projection-v46` line's pattern); (d) the generator run ONCE over its three owned artifacts `bridge/schema/project-studio-bridge.schema.json`, `generated/unity/StudioBridgeDtos.Generated.cs`, `generated/unity/project-studio-bridge.contract-manifest.json` (`scripts/generate-bridge-contract.ts:15-16, 192`; `npm run generate:bridge-contract`, then `check:bridge-contract` and `check:bridge-contract:fixtures`, `package.json:12-15`). The D5 settlement reason reaches the wire as a VALUE through the existing free-text `settlementReasons: string[]` (`people.ts:998`) and the receipt `reasons` (`types.ts:2093`) — no schema change. NOTHING ELSE: no relationship DTO, no drivers text, no tier, no chemistry row, no casting warning, no attention cause, no view/intent/page (→ B.6). The leak law extends to the new engine facts: no `closeness`, no edge of a rival-employed pair, no `recent` delta appears on any serialized DTO (the B.1 pattern, `tests/p14b1-leak.test.ts`). If Fable instead keeps projection 47, D5 must stay OUT of `publicPriorityOrder` until B.6 while the engine ranks on it — NOT recommended: §2.1.7 :120 is one rule, and the F1 history (:529-537) shows what a partial public order costs.

**Natural-chain exposure and reconciliation classes (pre-declared for the reviewer; the G9/G10 device — a NAMED class, the receipt facts a lawful movement must show, a per-seed ledger; `00-start.md:214-224, 252`; restated under 647-B D1, R2 and A2).** CANNOT MOVE: `rngState` (no RNG); `firstTakes` and promise outcomes UNTIL the first moved settlement on a seed (no B.5 read enters `decide()`/`staff()`/`advanceManagedProductions`/the evaluator); every pre-settlement rival production digest; the `trustDescriptor` of any pair; every V≤30 fixture byte and every frozen validator. CAN MOVE, by class — R-D5-SETTLE: a `settled`/`declined` receipt on a case with ≥ 2 freeze survivors whose D5 bands DIFFER at W, i.e. the subject holds an edge at `CloseFriends`+ whose counterpart is on some survivor's roster at W under the scope (5) predicate and not on every survivor's. Under that predicate a row the pass committed at W (`startWeek === W`) and a row `finishHollywoodWeek` closed at W (`endedWeek === W`) are both OFF the roster, so at the synchronized churns (cases open at 404 and settle at 416; weeks 208 and 416 on `p13-public-commercial-adoption`; B.1 T1 ruling (v) :524) every counterpart row that churns is closed at W and the CHURN receipts are predicted NOT EXPOSED — the draft's earlier prediction ("those receipts are the exposed set") inverts (647-B D1). The exposed class is the OFF-CYCLE rows: `player-contract`/`existing-player-contract` rows (the player's roster does not churn on the rival cadence), replacement rows `staff()` writes after a departure with staggered end weeks, and any rival row whose term does not end at the churn week. Direction, paper: an issuer holding such a row whose person shares takes with the subject gains a band; at a synchronized churn the incumbent's own churned colleagues are closed at W, so it reads `none` like every other bidder, and the draft's "incumbent gains" direction survives only in the off-cycle class. The test-author MEASURES which receipts move (the per-seed ledger) instead of assuming either prediction; "churn receipts unchanged" is itself a ledger row to confirm. A lawful movement must show: the winner's `settled` receipt `reasons` containing the D5 sentence — REQUIRED iff the winner's D5 band exceeds EVERY other survivor's (`chooseProposal` :880-884 emits a descriptor's sentence only on a strict win over all others; with ≥ 3 survivors a lawful Copeland winner can carry no D5 sentence, and the receipt carries no bands, so the reviewer RECOMPUTES the D5 bands at W from the subject's edges and the survivors' rosters, 647-B R2) — the subject's edge (`edgeId`, counterpart, `currentTier` at W ≥ CloseFriends) and the counterpart's `hollywood.employment` row for the winning issuer satisfying the (5) predicate at W; anything else is a defect. R-D5-CHAIN: after the first moved settlement on a seed, employment → rival cast → takes → films → standing → later cases are chain-exposed downstream; attributed by the first moved receipt, never re-pinned to "expected". R-ORDER-VALUE: every pin of `publicPriorityOrder` / `preferences.priorityOrder` moves to seven members (a wire VALUE move, not a law move; the `line` does not move). R-VERSION: every version, schema-id and registry-size literal (the sweep classes above). Inventory of files reading chooser outcomes or the public order (grep 2026-09-21 from 645-A, extended 2026-09-22): `tests/p14a1-f1-priority-order.test.ts`, `p14a1-seat-budget`, `p14b1-trust-chooser` (:172-173), `p14b1-promises`, `p14b1-t4-regressions`, `p14b3-reservations`, `p14b3-rule-revision`, `p14b4-cast-class-{capacity,outcomes,policy}` (policy :30-31/:52), `p14b4-material-evidence-core`, `p14b4-d3-matching`, `p14bf2-acting-discipline`, `bridge-p14a1-market`, `bridge-p14a2-market`, `bridge-p14a3-world`, `bridge-p14b1-promises`, `bridge-p14b3-promise-command`, `bridge-p14b4-cast-class` (:410-411), `bridge-contract-generator` (:562-565), `bridge-p14b4-runtime47-compatibility` (:162), `bridge-p13b-s8-rivals`, `p13a-rival-adoption`, `tests/helpers/p14b2-fixtures.ts` AND its consumers — thirteen test files import the helper (grep 2026-09-22): the five already listed (`bridge-p14b3-promise-command`, `p14b3-reservations`, `p14b4-cast-class-capacity`, `p14b4-cast-class-outcomes`, `p14bf2-acting-discipline`) and the eight 647-B A2 adds (`bridge-p14b2-trust`, `p14b2-fixture-preconditions`, `p14b2-setup-wrap-regressions`, `p14b4-rival-seating-preference`, `p14b4-owner-adapter-first-slice`, `p14b4-owner-enumerator-slice`, `p14b4-replay-bill-reductions`, `p14b4-cast-class-capacity-evaluator5`). MOST EXPOSED ITEM (647-B D3(b)): the cached shared fixture `poachingFixture` (`tests/helpers/p14b2-fixtures.ts:145-211`; the audit's `poachFixture` is this export) pins a week-208 `settled` receipt's `reasons` byte-for-byte (:200-201: compensation, then term) on a rival-subject case (`person-studio-5a47d054-r04-3`, incumbent `studio-5a47d054-r04`) that the player wins by the unproven tie order; its direct consumers are `bridge-p14b2-trust` and `p14b2-fixture-preconditions`, and the helper's other cached fixtures (`retentionFixture` :62, `historyFixture` :128, `rivalFixture` :215) are pre-declared with their consumers by the same rule (memory: sweep shared-fixture consumers after a law change). Paper prediction under the (5) predicate: it does not move — the subject's own r04 row reads `endedWeek === 208` (:204) and the player's roster at 208 holds the writer signed at 196 (:171), who shares no take with the subject; measured, not assumed. SECOND: the B.2 T3 controlled D3 case (`tests/p14b1-trust-chooser.test.ts:305`, "opportunity changes the winner of an otherwise compensation-versus-incumbency 1-1 tie, with two real surviving proposals"; plan :573) — it settles FIRST at 208 with r04's rows closed at W, so its equal-band premise survives either predicate (647-B Q5); the test-author still checks it at T1 and, if D5 breaks the premise on the measured state, re-expresses it (a friendship-neutral W, or both issuers holding a close tie) with the ruling recorded — never by weakening the assertion. The test-author pre-declares, per the natural-chain rule (memory: pre-declare attribution; derive pins from receipts), which assertions may move and forges any new case at the live counter, receipts-derived.

**Tests (RED-first; test-author; independent of the implementer).** Every engine file imports from the ABSENT `src/core/relationships.ts` so the RED is module resolution (0 collected), the strong precedent (B.1 T1 :524; memory: a missing NAMED export binds `undefined` and passes spuriously — the whole module must be absent, and every binding is CALLED in every group). Files, separated as the B4 plan orders (`P14B4-HEADLESS-PLAN.md:391-393` "separate files for frozen save/migration and reader-admitted synthetic variants"; 647-B R4): `tests/p14b5-save-v31.test.ts` (family 9 only — the frozen chain over the T0 corpus, nothing synthetic), `tests/p14b5-relationships.test.ts` (families 1–8 and 10 — synthetic, staged and reader-admitted worlds), and the bridge file `tests/bridge-p14b5-relationships.test.ts` (family 11), which imports from the absent `bridge/relationships.ts` ONLY if a bridge module is needed — the thin surface needs none, so family 11 pins version literals and the registry and is RED by VALUE, stated as such. T0 corpus cases consumed (names as published in `tests/fixtures/p14/genuine-v30-pre-b5/MANIFEST.json`, all seed `p13a-core-causal-01`): `genuine-v30-empty` (week 0, every root empty); `genuine-v30-current-p1` (45); `genuine-v30-bound-open-p1` (52); the tagged P2 pair `genuine-v30-bound-open-p2-lead` and `genuine-v30-bound-open-p2-lead-or-antagonist` (52); `genuine-v30-kept-and-broken` (61, one SATISFIED and one BROKEN); `genuine-v30-rival-current-p1-and-p2` (196, rival-issued current promises); `genuine-v30-rival-shared-take-terminal` (213); `genuine-v30-first-take-at-five` (60, the first-take world at `remainingTicks === 5`); `genuine-v30-legacy-count-only-p2-current-draft` (45); and `tests/fixtures/p14/genuine-projection47-runtime/genuine-projection47-runtime.checkpoint.json.gz` (week 45, `savedSaveSha256` = the `current-p1` raw). Families, each with the plan sentence it derives from: 1 EDGE MINTING (scope (2a)) — advance `genuine-v30-first-take-at-five` one tick: exactly six edges, canonical keys, `sharedProductions = 1`, one `sharedProduction` driver each with the proximity delta by seat pair, `firstSharedWeek = lastEventWeek = driver.week =` the tail week (`state.market.tick` after the advance); a second advance from the same pre-take state mints byte-identical edges (replay); re-ticking the post-take state mints nothing (idempotent); a RIVAL take (`genuine-v30-rival-current-p1-and-p2` advanced to a rival take, or the `p13aGeneratedStudio` chain — `tests/p14b1-first-take.test.ts` construction) mints under the same law; `rngState` byte-equal before/after; a headless world keeps `relationships` empty — source `tests/fixtures/p14/legacy-v28-shooting-5.json.gz` (V28, week 16, `hollywood: null`, plan :523; lifted through `migrateToV31`; NOT a T0 case, 647-B R7). 2 CANONICAL KEY AND REPETITION (2a) — the same pair on a second production is ONE edge with `sharedProductions = 2` and a `repeatedCollaboration` driver of the accelerator value; `a < b` regardless of seat order. 3 DRIFT ON READ (3) — a staged edge with `lastEventWeek = w` reads the same tier at `w + GRACE` and drifts toward Acquaintances between `w + GRACE + 1` and `w + GRACE + RETURN` (pinned by the exported constants, never literals); drift never moves a tier away from Acquaintances; counters, `firstSharedWeek`, `peakTier`/`peakTierWeek` unchanged by drift; a write after dormancy materializes the drifted value before applying the delta. 4 TIER RULE (3)-(4) (647-B D2/Q3: the baseline pin is REPLACED) — `RELATIONSHIP_RULES_VERSION === 1`; eight members; byte-equal output for byte-equal edge + week; a value in the Enemies/Nemeses bands without a conflict record reads Strained (synthetic edge, reader-admitted); `RELATIONSHIP_FAILURE_DELTA > RELATIONSHIP_PROXIMITY_LOW` pinned by NAME (the relation, not the values); a staged pair at the Acquaintances floor reads Strained after one live flop (its picture advanced through the take, then released below the failure edge) and `pairChemistry` reads `sign: -1` for it; Enemies/Nemeses never read on any B.5-minted edge; Enemies/Nemeses never read on the standard seeds (whether Strained is read there is a T4 measurement, not a pin). 5 RELEASE AND CANCEL DRIVERS (2b)-(2c) — a player picture advanced through its take to release with `criticScore ≥` the success edge mints `sharedSuccess` on all six pairs once (idempotent across a re-tick) with `driver.week === state.market.tick` after the advance `=== film.releaseTick + 1` (647-B R1), `< ` the failure edge mints `sharedFailure`, between mints nothing; a rival release (`hollywood.films`, `industry.growth`) under the same law; a player `cancel` of a picture WITH a first take mints `cancelledAfterFirstTake` on its six pairs once while `breakPromisesOnCancel` is a no-op for it (:788), and a cancel of a picture WITHOUT a take mints nothing while the promise seam may still BROKEN — both seams fire independently (coexistence). 6 D5 IN THE CHOOSER (5) — the B.2 T3 controlled-staging construction (plan :573; receipts-derived, live counter) UNDER THE (5) PREDICATE (647-B R7): a rival incumbent's churned roster is closed at W, so the close tie must sit on an OFF-CYCLE row — the PLAYER as an issuer holding, on a `player-contract` row with `startWeek < W` active at W, the counterpart of the subject's staged (validator-admitted) `CloseFriends` edge, or a staggered rival row of the same shape; two survivors, bands equal on every other descriptor → the issuer with the close tie settles with the D5 sentence in `reasons`; the same case at a W where that edge has drifted below Close Friends settles the other way (or declines by the tie order, stated); a row committed at W or closed at W never counts (the predicate's two strict ends, each pinned); `enemies here` never read. 7 PUBLIC ORDER (5) — `publicPriorityOrder` byte-equal to the SEVEN-member companion orders for both archetypes (asserting the companion :120, never the landed array); `publicPreferredTerm` value-identical (proven → longest, unproven → shortest); the F1 pins (`tests/p14a1-f1-priority-order.test.ts:86,91`) re-expressed to seven by the test-author (the B.1 "widened to six" precedent). 8 RESERVATION AND CHEMISTRY (6)-(7) — `nemesisOnRoster` is a `FreezeDrop` member with a sentence and is never emitted on the standard seeds; `pairChemistry` returns `{tier: null, sign: 0, reasons: []}` for a pair without an edge, `sign: 1` for a staged Colleagues+ pair, `sign: -1` for the family-4 Strained pair, and no digit in any reason; the leak test extends to the new facts (no `closeness`, no rival-pair edge on any serialized DTO). 9 SAVE V31 — FROZEN/MIGRATION FILE (10), T0 corpus and the frozen chain only — `LIVE_SAVE_VERSION === 31`; sentinel 32 refused "1 through 31 only"; every one of the ten T0 V30 cases migrates through `migrateToV31` with `relationships: []` and every other root byte-identical to the V30 bytes; `migrateToV30` of that lifted save reproduces the V30 bytes (lossless when empty); the projection-47 checkpoint's embedded save loads through the live route; the frozen V≤30 chain's own tests unchanged (R4 through `migrateToV31`). 10 ROOT VALIDATOR AND THE ONE-EDGE DOWNGRADE (10), synthetic, reader-admitted (moved out of the frozen file, 647-B R4; the one-edge case moved with it by the same :391-393 rule — a drafter's consistency edit, flagged for the parent) — refuses: a missing root; a non-ordinal `edgeId`; a duplicate pair; a non-canonical key; a person not in `state.talent`; `firstSharedWeek > lastEventWeek`; a week outside the recording interval; a driver week after `lastEventWeek`; `recent` over the cap; `peakTier` outside the catalogue; `closeness` outside 0..100 or non-integer; counters inconsistent; an unknown driver kind; an extra key; and a V31 world holding one edge refuses `migrateToV30` with the downgrade message and refuses every older `migrateToVn`. 11 BRIDGE (projection 48) — `PROJECTION_VERSION 48`, schema `$id` projection-48, `LIVE_SAVE_VERSION 31`; the seven-member `priorityOrder` enum in the generated schema; `preferences.priorityOrder` seven members and `line` unchanged on a staged case; the D5 sentence present in `settlementReasons` on the family-6 world; the genuine projection-47 checkpoint migrates through the registry with `projection-v47` and the registry holds exactly 36 prior ids; the generator `--check` clean. 12 NATURAL CHAINS — the R-D5 ledger on the measured seeds: the pre-declared receipts (the churn receipts predicted unchanged, the off-cycle class, `poachingFixture` and its consumers, the D3 controlled case), each moved one attributed by the three facts above with the D5 bands recomputed at W; every other pinned control unchanged.

**OPEN (kept after 647-B; the original numbers are retained so the audit's count still reads).** 1 Q2 :621 verbatim: "Should relationship growth use the public temperament descriptor as a compatibility prior, or grow at one base rate for every pair? | Prior: variety and chemistry surprises, one more public descriptor to explain. Uniform: any pair can become Inseparable through work alone, less texture | **Compatibility influence remains a later recommendation**; the first slice grows every pair at the base rate (recommended first behavior, not an approval) | P14B relationships". B.5: base rate (647-B: lawful footing; stays OPEN). Reversible mechanism: every delta passes through one pure `driverGain(state, a, b, delta)` that returns `delta` unchanged; a later Owner choice replaces its body with a bounded factor from the public `temperament` descriptor and adds one driver-copy line ("Compatible temperaments") — no shape or version change. 2 Q3 :622 verbatim: "At migration, should anything be reconstructed from existing credits and receipts? Factual shared-credit counts ("worked together on 2 films") are facts, not friendship, romance, chemistry or trust; the question is whether any *behavioral* state is backfilled | Count-only: facts displayed, no tier, no chemistry, no trust driver from pre-boundary receipts. Behavioral backfill: labeled reconstruction of tiers or trust from old data, risk of over-reading | **No behavioral backfill by default**; factual shared-credit counts may be shown as facts, labeled, and are not reconstructed friendship, romance, chemistry or trust (recommended first behavior, not an approval) | P14B migration". B.5: `convertV30ToV31` opens the root EMPTY; the mint reads only this advance's delta; every T0 case holding first takes migrates with NO edge (647-B: lawful; stays OPEN). Reversible mechanism: a later labelled reconstruction is its own migration step with its own receipt (the `existing-player-contract` precedent, :489), never a change to `convertV30ToV31`; the count-only DISPLAY is a B.6 read model over `firstTakes`/film credits, labelled, touching no edge. 3 Q4 :623 verbatim: "Should a Nemeses/Enemies pair be able to refuse a seating outright, or only incur the disclosed penalty? | Refusal: stronger drama, a hard block the player must manage. Penalty: never blocks, always explainable | **Warning plus the disclosed consequence is the recommended first behavior**; hard refusal remains unselected later scope (not an approval) | P14B chemistry". B.5: no greenlight or casting guard; `pairChemistry` is read-only with no consumer; the warning is B.6's (647-B: lawful; stays OPEN). Reversible mechanism: a hard refusal would be one additive guard in the greenlight/casting owner reading `pairChemistry(...).tier`; nothing in B.5 forecloses it. 11 D5 PRECEDENCE when a close tie AND an enemy are both on the issuer's roster (unreachable in B.5): CANDIDATE `enemies here` wins (hostility outranks friendship, the reservation-adjacent reading of :478); decided by the slice that lands a conflict driver (647-B: stays OPEN). 15 THE WAIVER'S V31 FIELD (645-A §2 "the following slice"): whether `supersededByPromiseId: string | null` rides B.5's V31 step to avoid a V32, or waits for its own step. Not recommended here, and the auditor concurs: bundling a field no B.5 path writes makes the V31 validator's exact-key list carry an unexercised member; Fable's call (stays OPEN). RULED BY 647-B AND FOLDED INTO SCOPE ABOVE (each marked "(647-B ruling …)" where it lands): 4 scale → (3); 5 negative reachability → (4) and family 4; 6 storage bound → (9); 7 roster at W → (5)-(6) and the natural-chain paragraph; 8 Partners → (1); 9 success basis → (2b); 10 release seam → (2) and (2b); 12 recording boundary → (10); 13 the writer seat → the EXCLUDED list; 14 (a) the `talentMarket.ts:659-662` comment → a T2 edit in (5), (b) the 645-A "C# enum widening" wording is corrected below (the generated C# holds `string[]`), (c) §5.2 :417's interval join is a B.6 label need, the edge itself is minted from the receipt's own seats. HYPOTHESIS CONSTANTS (647-B: accepted as named hypotheses, none settled; the two candidate sentences and the bridge label are copy hypotheses): `RELATIONSHIP_BASELINE` (50), the seven tier edges, `RELATIONSHIP_PROXIMITY_{HIGH,MID,LOW}`, `RELATIONSHIP_REPEAT_CAP`, `RELATIONSHIP_SUCCESS_DELTA`, `RELATIONSHIP_FAILURE_DELTA` (> `_PROXIMITY_LOW`, the one pinned relation), `RELATIONSHIP_CANCEL_DELTA`, `RELATIONSHIP_SUCCESS_CRITIC_SCORE`, `RELATIONSHIP_FAILURE_CRITIC_SCORE`, `RELATIONSHIP_DRIFT_GRACE_WEEKS` (52), `RELATIONSHIP_DRIFT_RETURN_WEEKS` (260), `RELATIONSHIP_RECENT_CAP` (8), `RELATIONSHIP_RULES_VERSION` (1); the D5 reason sentence, the `nemesisOnRoster` sentence and the bridge label. Product consequence to disclose (paper): incumbency stickiness through friendships — an issuer whose OFF-CYCLE roster holds a subject's collaborators gains a D5 band as soon as those pairs reach Close Friends, by design (§5.3 :428-429 "market preference"); at a synchronized churn no bidder gains it (scope (5)); thresholds hypotheses.

**OWNER RULINGS ACTIVATED 2026-09-22 — these five entries are NO LONGER OPEN (record 683; the Owner's `01-APPROVE-AND-CONTINUE.txt` on record 680, blob `cbdbca4e`, at source `63688a79`).** (i) OPEN 1 / Q2 is SETTLED as a COMMON BASE GROWTH rule: no temperament-compatibility prior now; `driverGain` keeps returning its delta unchanged, and the existing shared-work proximity weights REMAIN — "common base" does not remove them. (ii) OPEN 2 / Q3 is SETTLED as NO BEHAVIORAL BACKFILL at migration: `convertV30ToV31` keeps opening the root empty; real historical shared-credit counts MAY be displayed as facts in B.6, clearly distinguished from recorded friendship or trust history. (iii) OPEN 3 / Q4 is SETTLED as WARNINGS, NOT REFUSAL: B.6 lands relationship-based casting warnings and already-authorized disclosed consequences; it adds no hard seating refusal and MUST NOT invent a quality modifier, and this repeals no separately governed employment or market refusal. (iv) OPEN 11 is NOT newly selected and is retained verbatim for the later conflict work. (v) OPEN 15 is OVERTAKEN by V31's publication: the field did not ride B.5's save step, so the waiver needs its own governed change — never silent omission. SEPARATELY, hypothesis constant `RELATIONSHIP_FAILURE_DELTA` is RULED to 5 as PROVISIONAL CANDIDATE TUNING (not final balance) so that repeated failure can reach Strained on the low-proximity path; every other growth and dormancy constant is unchanged, no conflict driver, Enemies/Nemeses, romance, seating refusal or general production penalty is created, and rival casting/staffing/seat policy is explicitly NOT touched (628 R5 / G-1(A) / G-2 keep their own bounded disposition). That tuning lands as the separate step **P14B.5-T** with its own RED regression, before P14B.6. Measurement qualifications carried: four standard seeds to week 260 are not the 6,240-week endurance proof, not a full-dormancy proof and not a performance-equivalence claim; the staged 45 is a witness, not alone a proof of a global minimum; F=5 supplies none of the conflict evidence Enemies/Nemeses require and fixes nothing about fixed-team saturation.

**P14B.6 — RELATIONSHIP READ MODELS — task expansion (Owner ruling 4 of record 683; full text in `evidence/p14b4-20260919/687-p14b6-expansion-draft.md`, not duplicated here).** TypeScript and bridge only, no native screens. SCOPE: `pairChemistry` gains its first consumers, read-only; a profile relationship block on the landed `BridgeTrustBlock` pattern (`bridge/schema/bridge-schema.ts:2534-2539`, projected like `bridge/trust.ts`, hung beside `trust` at `bridge/people.ts:216`) carrying `{counterpartId, counterpartName, tierLabel, sign, drivers, sharedPictures}` rows and a `line`; casting chemistry rows over the six `seatPairs` of a draft or in-flight production, each with tier label, sign and driver copy, and "no shared work yet" where no edge exists; the Q4 casting WARNING — one sentence when a proposed seating holds a pair whose `pairChemistry(...).sign` is −1, never a refusal, never a quote/cost/forecast change; factual shared-credit counts from `state.firstTakes` and released credits, published for pairs WITHOUT an edge as readily as with one and never presented as friendship (ruling 3 (ii)); `PROJECTION_VERSION` 48 → 49 with `projection-v48` registered as a prior (36 → 37 ids). NO save step: `LIVE_SAVE_VERSION` stays 31, no new root, no validator change, no migration, no new driver, no constant, no policy, no RNG. DISCLOSURE IS THE SLICE'S DECISION: the landed leak law stays GREEN UNAMENDED (`tests/bridge-p14b5-relationships.test.ts:404-408` forbids `"closeness":`, `"edgeId":`, `"recent":`, `"relationships":`, `"lastEventWeek":`, `"peakTier":` and the `relationship-edge-` prefix on every serialized DTO), so B.6 publishes LABELS, COPY and COUNTS only; and because records 679/682 measured that nearly every edge on the standard seeds is RIVAL-INTERNAL, a tie is disclosed only when its counterpart is independently visible to the player (on the player's roster under the landed roster-at-W predicate), with an honest count-free line on the `presence.withheld` precedent (`bridge/people.ts:351-356`) when undisclosed ties exist. HARD ORDERING CONSTRAINT (T0): mint `tests/fixtures/p14/genuine-projection48-runtime/` on source where `PROJECTION_VERSION` is still 48, BEFORE any writer edit, exactly as 45/46/47 were minted through a one-off env-gated minter; minting after the bump would produce a 49 artifact wearing a 48 label. FORWARD CONSTRAINT from 694-C Q2: a resumed campaign's `recent` can now hold both `-4` and `-5` `sharedFailure` rows on one edge, so B.6's readable drivers MUST keep the B.2 no-number pattern or carry an era signal. EXCLUDED, each with its owner: the production-quality modifier (the result owner's, companion §5.6 row 1 — ruling 4 says B.6 must not invent one, and a chemistry READOUT is not evidence any result law consumes chemistry); the reservation refusal (ruling 3 (iii)); the retention attention row (§5.6 row 4); romance/Partners; conflict drivers; the waiver. NOT TOUCHED: `src/core/relationships.ts`, `talentMarket.ts` D5 and the reservation, `promises.ts`, every existing reason/drop/tie sentence, the settlement order, `rngState`, `TUNING`, every historical fixture and frozen validator, the evaluator-5 designated case, 628 R5 / G-1(A) / G-2. ACCEPTANCE: full core equals the then-current baseline set exactly (9 files / 24 failed after P14B.5-T); full UI at or below the 643 baseline with the same failure family; `npm run test:bridge` exactly the two inherited files; `typecheck`, `typecheck:bridge`, `check:bridge-contract` and the fixture check EXIT 0; the leak test green UNAMENDED. Failure identities AND causes compared, never counts alone.

**Tasks.** T0 FIXTURES — DONE ON RECORD (as the parent reports: minted at HEAD `d6c11b9b` BEFORE any source change, final V30 behavioural writer `61833f0d`, bridge `027155e7`; `tests/fixtures/p14/genuine-v30-pre-b5/` ten cases and `tests/fixtures/p14/genuine-projection47-runtime/` one checkpoint, each with uncompressed/compressed sha256, byte lengths, seed, week, `MANIFEST.json` + `.provenance.json` on the `genuine-v29-pre-p2` pattern; committed `ce04e49e`; 651-B independent KEEP WITH RECORD-ONLY ITEMS; record 652 at `1117717a`; the minter sources archived and removed from the tree as in B4 T0). T1 RED (test-author; after this audited expansion is inserted): `tests/p14b5-relationships.test.ts` (families 1–8, 10), `tests/p14b5-save-v31.test.ts` (family 9), `tests/bridge-p14b5-relationships.test.ts` (family 11), RED at module resolution / by value as stated; the R-D5 pre-declaration (family 12) as a record, not a test edit — the churn prediction, the off-cycle class, `poachingFixture` with its consumer set, the D3 controlled case; the most-exposed items checked. Writable: those three files + one evidence record; frozen: `src/`, `bridge/`, `generated/`, every existing test. Checks: `npm run typecheck` (expect exactly the new TS2307s), the three files single-worker (`node_modules/.bin/vitest run <files> --minWorkers=1 --maxWorkers=1`), record-check. T2 ENGINE + WIRE (sim-core, the ONE production writer; BEGINS ONLY AFTER T1's RED IS RECORDED — RED-before-implementation, plan :415; 647-B R5; cumulative patches per step, never commits; the parent lands each through the index and verifies the diff against the patch — 600 §4): S1 core — NEW `src/core/relationships.ts` (mint from the delta, drift, tier rule, the shared roster-at-W helper or its home in `talentMarket.ts`, `pairChemistry`, root validator, `projectRelationshipsPreV31`, constants), `src/core/types.ts`, `src/core/tick.ts:1102-1110` (one call, receiving the take entries and this advance's release results), `src/core/actions.ts:592-599` (one call); S2 chooser — `src/core/talentMarket.ts:651-678, 698-702, 779-816, 999-1032, 1057-1067` (the :659-662 comment corrected to name B.5); S3 save — `src/core/save.ts` (types :499-537, `LIVE_SAVE_VERSION`/`makeSave` :6396-6405, dispatch :5282-5285, the 18 throw sites, the 4 route lines, the V31 block after :8717), `src/core/index.ts`, `src/core/worldgen.ts:783-785`, `src/harness/roster-wall/historical-control.ts:23-26`, `src/harness/p14/legacy-v28-fixtures.ts`, `src/harness/d16/run-d17b-*.ts`; S4 load/runtime — `bridge/session.ts`, `bridge/runtime-checkpoint.ts` (strict V31 + the registry line), `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts` (the `2cbc3de5` arms); S5 wire — `bridge/schema/bridge-schema.ts:233, 2320-2324`, `bridge/people.ts:866-875`, the generator run over its three artifacts. Then the SEPARATE values-only live-version sweep (30 → 31, 47 → 48, the schema-id and F10/F11 literals, the seven-member arrays, the registry count, the V13-twin strip), OWNED BY FILE CLASS (647-B R5): pins in `src/`, `bridge/`, `generated/` and `ui/src/engine/adapter.ts` → the writer (the B.1 `6948e31` and B.4 `2cbc3de5` precedents); pins in test files under `tests/` and `ui/src/**/*.test.*` → the test-author, in a test-side patch after the production patch lands (the B.1 `e125c7f` precedent; 600 §3 step 5 "no test edits" for the writer). The production patch never contains a test file; no two specialists write the same file. Frozen for the writer: every file in scope (12) above, every test, every fixture, `TUNING`. T3 VERIFICATION (parent-run, one heavy process at a time, record-check on every run: `sourceSha`, `testedDiffSha256`, `fixedSource: true`): `npm run typecheck`, `npm run typecheck:bridge`, `npm run check:bridge-contract`, `npm run check:bridge-contract:fixtures`; the three T1 files single-worker; the natural-chain control files (the inventory above, the thirteen `p14b2-fixtures` consumers included) single-worker with the R-D5 ledger filled per seed; the frozen-chain save tests (`tests/*save-v*.test.ts`, `tests/p14b4-save-v30-compatibility.test.ts`, `tests/bridge-p14b4-runtime47-compatibility.test.ts`). Any moved premise gets an explicit test-author reconciliation with the ruling recorded — never a weakened validator, assertion, timeout or fixture. T4 CLOSE: full core `node_modules/.bin/vitest run --project core` compared BY NAME against 636 (`evidence/p14b4-20260919/636-cancel-full-core.txt`: 9 failed files / 24 failed / 3813 passed / 8 todo on `df944978`; the 22 inherited + 2 designated); full bridge `npm run test:bridge`; full UI `npm run test:ui` compared against 643 (9 failed files / 28 failed / 2657 passed / 5 skipped + 1 unhandled error on `0e330da0`; the 643-C classification: deterministic set by name, timeout class by file, exit code not a comparator while the unhandled error exists); attribution against the inherited set; label LOGIC VERIFIED · UNITY NOT VERIFIED (never Owner acceptance, never all-green); the edge/driver counts at week 260 on the standard seeds and whether Strained is ever read there, recorded as the §8 :645 measurement; records; the backlog entry; commit exact paths; push; `git ls-remote`. Specialists: max two concurrently and one production writer throughout (600 §4) — at T1 the test-author (with the contract-auditor read-only if needed); at T2 the writer (with the contract-auditor read-only for the T4 attribution review); the test-author's sweep patch follows the production patch, never overlapping it on a file.

### P14B.5 tasks
- [x] **T0 fixtures** — DONE ON RECORD (parent): genuine outgoing V30 corpus (ten cases) + projection-47 checkpoint minted at `d6c11b9b`, committed `ce04e49e`, 651-B independent KEEP, record 652 (`1117717a`); MANIFEST/PROVENANCE published.
- [x] **Audit** — 647-B contract-auditor read-only (INSERT WITH DISPOSITIONS); dispositions applied in this text (647-A2); the insertion is the parent's act.
- [ ] **T1 RED** — three files, families 1–11 (9 alone in the frozen file; 10 with the synthetic families); R-D5 pre-declaration with the churn/off-cycle prediction, `poachingFixture` and its consumers, the D3 controlled case.
- [ ] **T2 engine + wire** — after T1's RED is recorded: S1 core → S2 chooser → S3 Save V31 → S4 load/runtime → S5 projection 48; then the values-only sweep split by file class (writer: src/bridge/generated/adapter; test-author: test pins).
- [ ] **T3 verification** — typechecks, contract checks, T1 files, natural-chain controls (thirteen `p14b2-fixtures` consumers included) with the ledger, frozen-chain save tests; reconciliations recorded.
- [ ] **T4 close** — full core vs 636, full bridge, full UI vs 643/643-C, attribution, LOGIC VERIFIED · UNITY NOT VERIFIED, the week-260 edge/driver counts and the Strained read, records, backlog, publication.

**Unity backlog line (record-only; nothing native follows automatically).** Projection 47 → 48: the `StudioMarketPreferencesSnapshot.priorityOrder` JSON-schema enum gains `relationships` (the generated C# field stays `string[]`, `StudioBridgeDtos.Generated.cs:6492-6493` — a JSON-schema enum widening, not a C# enum; a native switch over the values without a default would break); one new ordering-only settlement sentence appears in the existing `settlementReasons`/receipt `reasons` strings; new schema identity (minted by the generator at T2, recorded then), new `$id` projection-48, new generated C# sha and F10/F11 sentinel; the outgoing 47 identity `sha256:6f6b4880…` registered as `projection-v47` so durable 47 checkpoints take the governed prior path. Save V31: a new top-level `relationships` root (edges keyed by canonical person pair; no number is ever published); prior saves migrate with an empty root; a save holding any edge cannot be downgraded to V30. NO new DTO, view, intent or page; the profile relationship block, drivers text, chemistry rows, casting warning and attention rows arrive with B.6 as new DTOs and are the native items then. Coordinate the enum widening explicitly across TS and Unity in the entry, as records 616/628/637 did (`UNITY-INTEGRATION-BACKLOG.md:153-157`). Unity NOT VERIFIED.

**Evidence limits.** Paper only: no test, typecheck, probe, generator or measurement ran for the draft or for this application; 647-B's D1 is derived from source order and the fixed-order pass, not measured on a seed, and whether any off-cycle case on the measured seeds reaches a Close Friends counterpart is unmeasured; the `poachingFixture` and D3-case predictions are paper; the edge-count bound is arithmetic, not a run. Line cites are to HEAD `d6c11b9b` and hold at `107db93e` (no source file moved); the companion cites are to the Revision-3 file in this worktree (its own symbol cites are at `592e926` and are historical). The two T0 MANIFESTs were read for the case names, weeks and root counts; records 648–652, 651-B and the provenance files were not read — the T0 closeout is stated as the parent reports it. Whether the Owner has ruled on record 628's three seating questions since 645 was not checked. `pairChemistry` consumption, the result owner's bound and the romance track were not designed; only the export shape and the exclusions are stated. The hypothesis constants carry no values here beyond the companion's own 52 / 260, the baseline 50, the cap 8 and the one pinned relation `FAILURE > PROXIMITY_LOW`; the writer names them and the tests pin them by name. No product decision is decided in this text; nothing here is Owner-approved.

**Drafter's notes on the audit (647-A2; for the parent to rule, none is a refusal).** (1) No 647-B disposition was found wrong on the source; every one was applied. (2) Name slip in 647-B D3(b)/A2: the helper export is `poachingFixture` (`tests/helpers/p14b2-fixtures.ts:145-211`, cache at :144), not `poachFixture`; applied under the correct name. Its DIRECT consumers are two of the eight A2 adds (`bridge-p14b2-trust`, `p14b2-fixture-preconditions`); the other adds consume the helper's other cached fixtures, which this text pre-declares by the same rule. (3) The audit's "19 files / 25 hits" grep counted fixture provenance JSON; thirteen TEST files import the helper (grep 2026-09-22), five of which the draft's inventory already listed. (4) One drafter's consistency edit beyond the letter of R4: family 9's "a V31 world holding one edge refuses `migrateToV30`" is a synthetic variant, so it moved with family 10 into the synthetic file under the same B4 plan :391-393 rule; reverse it if the parent prefers R4 applied to family 10 alone. (5) HEAD at application is `107db93e` (docs-only atop the task's stated `1117717a`); no source cite moved.

> Parent rulings on the drafter's notes (record 653): (2) `poachingFixture` name accepted; (4) the family-9 one-edge migration-refusal case stays in the synthetic file with family 10 (the B4 plan :391-393 rule applies to every reader-admitted synthetic variant); (5) the section is inserted at the T1-release HEAD named in record 653. Nothing here is Owner approval; OPEN 1–3 (Q2/Q3/Q4) and 11/15 stay open.
