# P14 — Contested Talent Market, Bonds and Promises, Career Lifecycle — headless plan (logic-first)

**Status (2026-09-18).** DRAFT v1 written after P13B S8's engine and bridge landed and before its matched pass closed; AUDITED (contract-auditor, read-only, ≈06:40): two blocking items (T0 gated on the S8 close; the Identities row) and six should-fix adopted in place, two coverage tests added (the `staff()` exclusion; the firing disclosure), one P14B flag (first take, not Shooting entry). P14A.1 may begin only after the S8 close commit is named in T0. Engine implementation and verification only; Unity/rendered UI/native/visual acceptance
deferred and never claimed verified; every engine change creating client work is recorded in `../UNITY-INTEGRATION-BACKLOG.md`.

## 1. Authority and inputs

- Owner directive `OWNER-HEADLESS-PROGRAM-20260916-01` (logic-first): headless P13B → **P14** → P15 → P16 engine work proceeds now; later
  corrections override older research. The three-week autonomous directive (2026-09-15) names the P14 preparation index at
  `The-Movies@8ef5246a docs/engineering/P14-PREPARATION-REVIEW-INDEX.md`.
- The P14 preparation package, retained verbatim in `../../p14-preparation-8ef5246a/` (README states provenance): the review index, the
  post-P12 reconciliation (REUSED 57 · SHARED GENERALIZATION 19 · NEW P14 WORK 17 · POST-P13 REFRESH REQUIRED 14), the preparation
  companion (P14A/B/C design, firing note, promise catalogue, relationship model, lifecycle model, decision register §7), the launch-review
  skeleton, and the Owner rulings record §3.4 (fourteen OWNER-SELECTED PRODUCT DIRECTIONS, superseded statements).
- The package's own status ("PREPARATION READY — POST-P13 ACCEPTANCE REFRESH REQUIRED · IMPLEMENTATION NOT AUTHORIZED") predates the
  2026-09-16 directive; this plan performs the post-P13 refresh (§2) against the actual P13B closeout and proceeds under the directive.
- Companion `02-P13B-DECISIONS-AND-ACCEPTANCE.md` / `03-PAPER-ECONOMICS.md` remain off disk (recorded in `CONTINUATION-STATE.md`); P14's
  acceptance wording is drafted from the package and must be re-checked when the Owner side re-supplies it.

## 2. Post-P13 refresh (reconciliation §3), against the P13B closeout

| P13 seam | P13B fact now in force (branch `wip/headless-program-20260916-ts`) | Consequence for P14 |
|---|---|---|
| Era / timeline facts | `campaignDate(week)` (`src/core/calendar.ts`) is the only public era/date law; technology milestones are catalogue data (`researchableWeek`, `commercialWeek`, S7 `publicWindow`); no era root was added | P14 reads `campaignDate` for cohort ages and profile copy; no era symbol to refresh |
| First-filming seam | S5-R07: a production enters Shooting when its setup units are credited (`ProductionWorkflow.setup`, `setupAdmitted`/`setupCompleted` events); the first take is the first `ShootingTask` after entry | P14B's promise qualifying event and the relationship shared-work boundary read the FIRST TAKE COMPLETING (the 5 → 4 advance, companion §4.2/R12), which now follows the S5-R07 setup-gated Shooting entry — not Shooting entry itself and not `phaseEntered` alone (flag for the P14B expansion) |
| Stage durations and capacity | `FACILITY_BLUEPRINTS` (20 entries incl. `restoration-*`, `office-conversion-*`, technology installations); Laboratory capability with 4 seats; installations and cancellations (S6) take bodies offline | the feasibility service (P14B) reads live capability/offline state through `occupancy.ts`; durations from the blueprint table |
| Professions | `CreativeRole = FilmCreativeRole | 'scientist'`, `Discipline = FilmDiscipline | 'research'`; Scientists employ through the shared contract law (P13A), rivals hire them through `staff()`'s scientist slots (S8) | the Scientist joins market eligibility, aging and retirement; its retirement window and market rule are OPEN (companion §7.3-adjacent; no Owner text) |
| Rival movements | `RivalMoneyKind` = 14 kinds (P13A `technologyAdoption`; S8 `researchSpend`, `researchCapacity`, `technologyRestoration`, `technologyRefund`); period exact-key validation | P14's `termination` rival kind lands in its own governed save step, as the package foresaw |
| Persistence | live save **V27** (`LIVE_SAVE_VERSION`), bridge projection **41**, schema `sha256:16b84322…`; frozen chain V12–V26 with positive projections (`projectPlacementPreV26`, `projectHollywoodPreV27`) and policy threading | P14A.1 allocates **V28 / projection 42** at execution; genuine V27 fixtures are minted at the final V27 writer before any P14 source change |
| Identities | the S8 close commit `e0676f9` and its matched-pass attribution (`evidence/p13b-s8-20260918/23-attribution-ef9ff76`; P13B progress table) | cited by the P14A.1 records once it exists; no Owner acceptance of P13B exists (LOGIC VERIFIED · UNITY NOT VERIFIED only) |
| Process-global caches | one pre-existing module-level memo in `src/core/hollywood.ts:50` — `employmentByPerson`, a `WeakMap` keyed by the employment ARRAY instance (object identity, GC-safe), not by seed, `worldId`, `PersonId`, `caseId`, `contractId`, receipt id, `campaignId` or `sessionId`; no other module-level `Map`/cache binding in `src/core` (grep 2026-09-18); every P13B reducer is copy-on-write | admissible under the reconciliation's rule (identity-keyed, not id-keyed); P14 adds no seed/id-keyed module state and does not extend this memo |

## 3. Slice order

P14A.1 **One Contested Expiry Core** (companion §2.1.10) → P14A.2 workspace read models → P14A.3 world route facts → P14B promises/relationships
→ P14C lifecycle. Each slice: expansion written before it begins; genuine fixtures of the outgoing save version minted at its final writer
before any source change; the next governed save/projection versions allocated at execution; RED-before-implementation; specialists
(sim-core, test-author, contract-auditor read-only), max two concurrently; matched pass, attribution against the inherited set, LOGIC
VERIFIED · UNITY NOT VERIFIED label; backlog entry.

## P14A.1 — One Contested Expiry Core — task expansion (draft; audit before it begins)

**Settled law (rulings §3.4.1, by number; not re-asked):** 1–4 (firing as recalibration; the 26-week-capped charge; anti-exploit guard;
market eligibility: free agents and the renewal window only, no in-term poaching, no buyouts), 8 (no agent), 14 (upstream ownership: P10
person/contract truth, P12 employer/exclusivity/intervals, P11 money).

**Engine today (verify at T1).** `signContract {talentId, termWeeks}`, `renewContract {talentId, termWeeks}` (immediate replacement inside the
window), `releaseTalent {talentId}` (D-11.9 charge, `termination` ledger kind); `renewalWindowOpen(terms, week)` with
`TUNING.HIRING_RENEWAL_WINDOW_WEEKS = 12`; rivals renew their own people on the window's first week and never propose (`staff()`); the salary is
the person's ask (`offerForTalent`), no player money lever; contract terms of rival-employed people are private on the wire.

**Scope (companion §2.1.10).** One immutable `PersonId`, two entered studios (player + one rival present at week 0), one market case per
eligible expiry (`discovered → proposals_open → decision_pending → settled | declined | expired | invalidated`), at most one current proposal
per studio, the symmetric premium tier on both proposals — a small TYPED/ENUMERATED tier, not a continuous input (companion §2.1.4 hypothesis {1.00 … 1.25}; the values OPEN), P10 draft references with version invalidation, the rival
proposal trigger inside `nextDecisionWeek`, case-aware admission — `applyRenewContract` on a cased person is REFUSED with a new typed refusal `underMarketCase` that redirects to the proposal path (companion §2.1.3/R6), and `staff()`'s existing renewal loop (`hollywoodTick.ts` ≈95–111, which runs BEFORE `decide()` in the same weekly pass and would otherwise auto-renew a rival's expiring person for 208 weeks in the discovery week) EXCLUDES case subjects — the exclusion lands with the case-open check, not with settlement (the second bounded `staff()` edit, companion §2.5), the terminal atomic settlement at the subject's `endWeekExclusive` (derived on read), appended in `tick.ts` AFTER `finishHollywoodWeek(finishTechnologyWeek(finalized))` (the pipeline's last call, on the already-incremented week; companion §2.1.8) — never inside `advanceHollywoodWeek`/`decide()`, which run pre-increment, the disclosure table (§2.1.5: existence/subject/decision week/competing term public; premium/salary/bonus UNKNOWN; ranking reasons order-only after settlement) — a NARROWING of today's blanket rule that rival contract terms are private (`bridge/industry.ts` ≈239 'Contract terms are kept private in Industry.', `bridge/people.ts` ≈516 `contract: null`): the case block discloses exactly the listed fields and those two call sites keep every other term private (test 6 asserts both halves), receipts for discovery, proposal,
withdrawal, settlement, invalidation; free agents stay instant-sign.
- Persisted facts: a `talentMarket` root (cases, proposals, receipts) — **Save V28**; a `termination` rival money kind if rival firing is
  reached (deferred: companion §4 "Rival early termination — Ready"), else not in this slice.
- Bridge (projection 42, text only): profile case block (case status, decision week label, own proposal, competing proposal's known terms
  and UNKNOWN markers, settlement receipt reasons), intent kinds for propose/withdraw, attention rows for the five interrupt causes.
- SETTLED by the package (not OPEN): the anti-exploit guard is companion §3.5/§3.6 recommendation **R1** — a persistent salary floor toward the releasing studio, no new persisted fact, alternatives (cooldowns) rejected; only its status as a Future-Ops-reviewable recommendation remains, not its form. Direction 8 (no agent) is realised by a `representation` key required and pinned `null` (R10).
- OPEN (recorded, not resolved): the premium tier values and the rival's trigger policy numbers; Q1–Q4 of companion §7.3 (none blocks A.1); the Scientist's retirement window and market rule; acceptance wording.

**Tests (RED-first, against `src/core/talentMarket.ts`).** 1 eligibility table (six states; reserved rivals cannot propose; in-term
approaches refused). 2 case lifecycle and derived decision week; invalidation triggers. 3 proposals: one current per studio, premium tier
bounds, draft reference invalidation, affordability gates (`canAfford` for the player; the rival's reserve). 4 rival trigger inside `nextDecisionWeek` (pure policy, receipt-backed) AND the `staff()` case-subject exclusion: a rival's expiring person under an open case is not auto-renewed in the discovery week, so the rival contest of the Core pass sentence is reachable. 5 atomic settlement: deterministic ranking, exactly once, receipts, incumbent renewal as a
proposal, order-only reasons. 6 disclosure (UNKNOWN markers; nothing private leaks). 7 firing: the 26-week cap, the anti-exploit floor (R1), AND the confirmation's disclosure content per direction 2 (remaining duration, remaining guaranteed compensation, the cap where it applies, the exact charge, the effective end, the other authoritative consequences — the two copy branches of companion §3.4).
8 Save V28 with genuine V27 fixtures; validator refusals incl. the `representation` key required and `null` (R10). 9 bridge projection 42.

### P14A.1 tasks
- [x] **T0 Genuine V27 fixtures (2026-09-18 ≈06:40, minted at `e0676f9`)** — gate satisfied: S8 closed at `e0676f9` (label + attribution), the final V27 writer `ef9ff76` unchanged since; `src/harness/p13b/legacy-v27-fixtures.ts`: `legacy-v27-natural-rival-labs-20.json.gz` (sha `00183e1c…`, week 20: every rival's `laboratoryCommitted`/`laboratoryOperational` receipts, `researchCapacity` movements) and `legacy-v27-renewal-window-456.json.gz` (sha `af659268…`, week 456: the research-ready world with a player Scientist contract inside its 12-week renewal window, rival research and adoption facts present, no market root); provenance in `tests/fixtures/p13b/PROVENANCE.md`; evidence `p14a1-20260918/00`.
- [x] **T1 Tests 1–8 RED-first (2026-09-18 ≈07:20)**: `tests/p14a1-{eligibility,case,proposals,rival-trigger,settlement,disclosure,firing,save-v28}.test.ts` (5 + 6 + 7 + 2 + 1 + 3 + 5 + 7 = 36 cases), each RED at module resolution of `src/core/talentMarket.ts` (evidence `01`–`08`; engine facts probed, not assumed: rows 1–4 entered / 5–9 reserved at week 0, the window at 196 for a rival's 208-week founding roster). Named coverage limits (not conflicts): P14C states and the retirement trigger unreachable pre-P14C; the founding-draft exclusion has no live path; tier values OPEN (1.00 floor, monotonicity, below-1.00 refusal pinned); rival trigger branches (b)/(c) not fixture-tested; tie-order settlement not exercised (numerical hypothesis); R1's downstream pricing effect not exercised; R2 out of test 7's text. **Adjudicated (coordinator):** the V27→V28 lift never fabricates a case (R22: no fabricated pre-P14 facts) — a subject already inside its window at load is DISCOVERED by the next tick's discovery step, one week after load; the save test's empty-root reading stands. Sim-core dispatched (T2). - [~] **T2 Engine increment (sim-core, landed `d49cc27`, 2026-09-18 ≈08:50):** Save **V28** (`talentMarket` root {cases, proposals, receipts, `representation: null`} at top level, stripped before the frozen V27 chain; `projectTalentMarketPreV28` asked before `validateSaveV28`; 13 downgrade guards; sentinel 29; the lift always empty, never a fabricated case), `src/core/talentMarket.ts` (1,015 lines, pure, no RNG: eligibility over the employer facts, cases keyed to the P12 `contractId` with the decision week and status DERIVED on read, enumerated-tier proposals with P10 drafts/digests, the rival trigger a/b/c inside the rival's decided week, atomic settlement appended after `finishHollywoodWeek` — freeze, drop, deterministic ranking (D1/D2/D6/D7; D3–D5 omitted, not faked), commit through the accepted owners, one chooser receipt with order-only reasons — `releaseFloor`/`releaseDisclosure`), `terminationCost = weekly × min(remaining, 26)` (`HIRING_TERMINATION_CAP_WEEKS`; the 0.5 fraction retained HISTORICAL), `underMarketCase` after the unchanged D-11.7 check, `staff()` skips case subjects for the whole open span, the market engages iff `hollywood !== null`; sweep 27→28 over 62 test/ui files (classes listed in evidence `08`; no roster/bound moved). P14A.1 37/37; S1–S8, p13a, employment/contracts, d12, roster-wall, sweep batches green; root typecheck clean; `typecheck:bridge` 21 errors (the live-version move, T3). **Rulings (coordinator):** the two D-11 pins corrected by sim-core in `tests/d11-employment.test.ts` CONFIRMED (directions 1–2 repeal the 50% charge; R6 replaces in-window renewal for a cased person); `p13a-rival-adoption` case 46 is a test premise falsified by the pinned law (the rival roster now settles at 208 instead of auto-renewing at 196, so the 416 period pre-exists the sound purchase) — test-author re-expressing it; **R1 must govern the player's `signContract`/`renewContract` pricing** (direction 3 is settled; sim-core's pricing-entry-only application leaves E1 open) — RED case then wiring; **R4 legacy terminations IN scope** (the global `terminationCost` change makes every frozen reader refuse a genuine old-law termination — a persistence hazard) — RED case then a law-versioned reconciliation with a `legacyTerminations` record on the root; **R2 DEFERRED** (release refused for the busy set / founding; companion recommendation not in A.1's plan text) — recorded for a follow-up. Named limits stand: `--project ui` not run (typecheck-clean only); weekly-advance latency unmeasured (`ponytail:` note names the PersonId-index path). Evidence `09` (tests), `12` (bridge live-version sweep) in flight. **Landed after (2026-09-18 ≈09:20–10:40):** test tasks `7f19228` (`p13a-rival-adoption` re-expressed 2/2; RED cases: R1 on the player's `signContract` path — expected 656,156, got the floor-free 546,797; R4 — an old-law V27 termination refused by the frozen chain: 'employment termination lacks its actual player payment'; evidence `09`); bridge/ui live-version sweep 27→28 `c972f99` (no projection change; typecheck + typecheck:bridge clean; named bridge files 44/44; evidence `12`). **Settlement defect (sim-core diagnosis on seed `p13b-s8-bridge-probe-01`, week 208):** `survivesFreeze` froze the submission-week PRICE into the reservation check and the digest, so ordinary fame drift drops every proposal — all 24 cases DECLINED ('no proposal cleared this person's reservation'); the rival research chain slid ≈5 weeks (`technologyAdopted` 293 not 288; the `bridge-p13b-s8-rivals` regression case). **Ruling (coordinator; companion §2.1.4 line 70, §2.1.7 line 104):** the version digest covers material terms only `(talentId, issuerStudioId, termWeeks, startWeek, premiumTier)`; price is derived, never material; settlement re-derives `annual = round(ask(W) × tier)` and the bonus through `studioOffer` at W and commits those; reservation, ranking, affordability and the commit read the W re-derivation; the stored proposal price is the submission-week QUOTE, never read at settlement. RED cases LANDED `8495733` (test-author: `tests/p14a1-settlement.test.ts` cases A/B — A `expected 'declined' to be 'settled'`, B digest `6cd626e6…` ≠ `031fa0e5…`; evidence `13`; the existing dominant-incumbent case still green). PREMISE DECLARED by the test-author (not a conflict): an idle signed actor's fame never drifts naturally on either seed (fame moves only through the cast/reception path), so drift is induced by a direct copy-on-write fame edit after submission — the law under test is the settlement re-derivation, not the drift source. Sim-core LANDED three commits: **R1 `c25afd6`** (`studioOffer` returns a full `ContractOffer`, floor before any tier, bonus at the published fraction on the floored annual; new `playerOffer`/`playerOfferOptions`; `applySignContract`/`applyRenewContract`, the bridge quote/draft/decision surfaces, the ui adapter option lists and `castingPackageReadModel` all route through it; `contractOffer` stays the pure market ask; no cycle, no DTO change; firing + save-v28 15/15; d11/p13a-employment/p12/S8-save-v27/contracts 150/150). **R4 `ad35a81`** (`legacyTerminationCost` 50% law, `TerminationLaw`, `PRE_V28_TERMINATION_LAW`; the law travels beside the stripped state through the frozen chain as `plans` does; `convertV27ToV28` records every player termination from the ledger's actual charge, fail-loud if absent; `validateTalentMarketRoot` enforces row/week/amount/uniqueness; the V28→V27 downgrade stays lossless). **Settlement `a2d59e0`** (digest = material terms only; one `proposalPriceAt(state, proposal, W)`; freeze, ranking, commit and `caseDisclosure` on the W price; `submitProposal` keeps the submission-week quote for its own affordability check; settlement 3/3, p14a1 41/41). **Rulings CONFIRMED:** the option-list quote surfaces belong to R1 (a quote at 546,797 against a charge of 656,156 would lie); the frozen V≤27 chain accepts EITHER the 50% law or the cap law (a V27 envelope is also the downgrade projection of a live V28 campaign and the V27 shape cannot carry the era; any third number is refused). **OPEN after the fix (measured by sim-core, evidence `14-settlement-baseline-comparison`):** on the probe seed the first cluster now reads 11 settled / 13 DECLINED (was 24 declined) and rival r01's `technologyAdopted` stays at 293 with and without the fix — the 288 guard in `bridge-p13b-s8-rivals` is NOT restored; `chooseProposal` returns null only for an empty survivor set, so each decline is a `survivesFreeze` drop hidden behind the fixed text 'no proposal cleared this person's reservation' (companion §2.1.7 requires typed drop reasons — a disclosure gap, recorded). **Diagnosis (sim-core, evidence `14-settlement-decline-diagnosis`, 356 lines, self-checked against the engine's own 24 outcomes): NOTHING IS DROPPED** — all 96 rival proposals (four rivals × 24 cases, every case decided at 208) pass every freeze predicate with multi-million headroom; the 13 declines are `chooseProposal`'s terminal decline-all reached with a full legal survivor set, because `rivalPremiumTier` discriminates on two bits (incumbent; `reserveWeeks ≥ 16`) so challengers share exact descriptor vectors, D3–D5 are neutral in P14A, every rival submits at 196 for 208 weeks, and the incumbent bids the LOWEST tier (r01/r04 at 1.00 are dominated on compensation and hand all 12 of their people to an unbreakable challenger tie; r02/r03 at 1.05 tie challengers and win on incumbency = the 11 settlements; the 13th is a three-way tie on a `term`-first priority order). **Ruling (coordinator):** decline-all is the companion's own terminal (R8; §2.1.7 line 120 'then by earliest submission week, then by incumbent-if-present, then decline-all'), so the ranking is LAW-CONFORMANT and not a defect; the two-bit rival tier policy and the incumbent-bids-lowest rule are the recorded OPEN rival-trigger hypothesis (isolated in the rival policy constants) — the 54% decline rate on this seed is documented for the Owner as a product consequence, NOT tuned here (no invented numbers). **DEFECT (fix in scope):** `closeCase` stamps every decline with 'no proposal cleared this person's reservation' — false for all 13 (a tie exhaustion and a reservation failure are indistinguishable in the record; §2.1.7 requires typed reasons) — RED case then typed decline reasons (tie exhausted vs dropped, each drop's predicate). **288→293 is the market existing at all** (all 24 rival contracts expire at 208 and only 11 re-sign; `staff()`'s case-subject exclusion suppresses the old renewal path from 196) — `bridge-p13b-s8-rivals`' natural-week guard and `p13a-rival-adoption`'s 416 pin are re-expressed ONCE, test-side, against the landed law `a2d59e0`. **LANDED `1f3fcd6` (test-author):** typed decline reasons RED in `tests/p14a1-decline-reasons.test.ts` (A tie exhaustion — a genuine natural tie between r01/r02 at Standing 47.5/48.1 inside the band tolerance — `expected 'no proposal cleared' to be 'tie exhausted'`; B all dropped — the rival's account drained by a declared edit right before the settling tick — `… to be 'all proposals dropped'`; evidence `16`); `bridge-p13b-s8-rivals` fixture rebuilt at the measured natural week 293 (17/17); `p13a-rival-adoption` re-expressed to walk the chain from the catalogue's `commercialWeek` 416 to the first `route: 'purchase'` adoption (2/2; evidence `17`). **NEW FINDING (test-author, not repaired):** under the market the first commercial sound purchase on the p13a seed lands at week 520 by a LATER-ENTERING fifth rival; none of the four founding rivals can afford the $1,475,000 purchase any more — r03/r04 cash strictly negative and worsening (≈−16M/−8M by week 420). A rival trajectory P13B-S8 did not have: diagnosis dispatched to the sim-core (read-only; hypotheses: duplicate payroll from `staff()` replacing an excluded case subject during the open span, premium 208-week terms, bonuses on every settlement and free-agent re-hire, a double charge at commit, cross-rival shuffles; evidence `18`) BEFORE P14A.1 can be labelled. **In scope, test-side at T3:** the 5 `bridge-p10a-r1-contract-quote` cases pin the repealed in-window `renewContract` (`underMarketCase`; R6 replaces it with the incumbent's proposal) — the bridge test-author re-expresses them with test 9. **R1 ruling:** both player actions price through `studioOffer(state, playerStudioId, …)`; the player-facing quotes in `bridge/contract.ts` and the ui adapter show the same floored price; `contractOffer` stays the pure market ask. **R4 ruling:** era-versioned termination law — the frozen V≤27 chain validates player terminations under the historical 50% law (`HIRING_TERMINATION_FRACTION`, retained for this); the V27→V28 lift records every player termination as `talentMarket.legacyTerminations [{contractId, endedWeek, amountPaid}]` from the ledger's actual charge (no back-charge, no refund, cash untouched); V28 validates listed contracts against `amountPaid` and all others under the cap law; **Save V28 widened in place** (no V28 fixture exists and the version has not closed; the two positive empty-root pins move with it). Sim-core dispatched on R1 + R4 (evidence `14`); the settlement fix is gated on the RED commit. Records note: this plan file was committed EMPTY at `9bd2ce3` (a truncating records write; 73 lines deleted) and restored from `a4e7e25` in the commit carrying this sentence.
- [~] **T3 Bridge projection 42** — test 9 RED LANDED `f1b7132` (test-author, 2026-09-18 ≈13:05): `tests/bridge-p14a1-market.test.ts` 15/15 RED (3 real assertions — `PROJECTION_VERSION` 41→42, `AVAILABLE_INTENT_KINDS` lacks `marketProposalAction`, `contractActionDecisions` still reads `renewAvailable: true` for a cased person; 12 on the two named not-yet-existing bindings `marketCaseProjection` from `bridge/people.ts` and `marketProposalDraftToEngine` from `bridge/contract.ts`; 0 harness errors; evidence `10`); `tests/bridge-p10a-r1-contract-quote.test.ts` re-expressed under R6 — R0/R2/R3 untouched green, R1/R4/R5(re-price)/R6(solvency→affordability)/R7 moved to the incumbent's proposal path (RED on the missing binding), R5/R6 release-verb sub-claims green, new case: in-window `renewContract` refused with the `underMarketCase` reason verbatim (green; evidence `11`). PREMISES NAMED (coverage limits, not conflicts): two of the five Attention causes not exercised (the stop-horizon threshold; 'terms revised after review' — the review read-state is not persisted in A.1: the implementer derives the row from a competing proposal's revision, and the limit is recorded); the public preference descriptors need a pure engine read accessor (`publicPriorityOrder`/`publicPreferredTerm`, folded into the typed-reasons commit — no law change); the settled case's folded industry row pinned ABSENT (A.2); R4's session-level reasonCode for the new intent not pinned (the accepted stale-revision law applies). NOTED, unrelated and pre-existing: the live in-memory `perceived` genre-experience value can carry IEEE `-0`, which any JSON round trip flattens to `0` (the save round-trip case normalises both sides). `typecheck:bridge` intentionally 2 × TS2305 (+5 cascading) until the implementer lands. Implementer (bridge sim-core) dispatched with the amended brief; `bridge/contract.ts` must consult `caseOpenForTalent` (the renew-row disclosure gap). - [ ] **T4 Matched pass, attribution, label, records, commit, push.**
