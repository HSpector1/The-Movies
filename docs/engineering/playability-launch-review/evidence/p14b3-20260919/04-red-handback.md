# B3 independent behavioral RED — T1 complete

Source `9c605dc437e1c04268753c2a7039d092153e11f1`, qualified B2 producer plus
actually preserved evaluator1 corpus. Three independent files installed verbatim
from reviewed drafts; parent owns runtime. `03`16:29:03.363Z–16:29:44.069Z,
exit1,3files/22failed/12passed/34cases, no todos. FixedSource:true;
before/after patchSHA `071b0f76982efd8d19e3644f4a26bb6b2515a95c356c82b9d520d936cbfcd66e`.
Full untracked test bytes captured in03.patch; no production changes or skipped
fixture prerequisite. All three genuine artifact preservation cases PASS.

Parent read the complete log. All failures are behavioral, not missing fixtures/
modules or failed fixture guards:

- Bridge19cases:14fail/5pass. Actual accepted commands omit attachments, canonical
  nested drafts and pending payloads retain caller mutations, all four nonofferable
  promise quotes incorrectly expose outerok:true, direct apply ignores newly
  competing reservations. Later replacement/session-cap/replay/isolation/outcome
  assertions stop at missing real attachment, not at invented contract identity.
- Reservations9cases:5fail/4pass. Actual revisions/withdrawals/replacements retain
  phantom classification AND digest contributions; a genuinely dropped losing
  draft still consumes capacity. Current/self-exclusion, real bound-open,
  nonoverlap and real terminal controls pass their executed assertions. Current
  cross-issuer policy is preserved; its failing assertion is AFTER real withdrawal.
- Revision6cases:3fail/3pass. Current version remains1 rather than2; fresh real
  attachment retains1 (also exposes the abandonment bug); actual later winning
  freeze writes1 rather than2 while correctly retaining the old root.version1.
  All exact-hash genuine old envelopes load byte-identically, including refusal.

Some downstream assertions deliberately remain unexecuted after their first
expected failure; this RED is not end-to-end GREEN or a claim that no further
fixture issue can appear. No assertion, timeout, validator or producer changed.

After this checkpoint is committed/published, release sole sim-core production
writer for the audited three-owner implementation; independent test-author may
re-express ONLY the separately approved old ExampleB unlinked-active premise and
B2 poach comment, preserving meaningful capacity guards/all historical roots.
New34cases remain unchanged. Parent serialtargeted/typechecks/contracts after
both writers hand back frozen files; native contract-auditor bounded review then
committed full boundary/attribution/qualified publication. No B-F2/P2 law changes.
