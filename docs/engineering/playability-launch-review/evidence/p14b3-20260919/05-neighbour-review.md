# B3 neighboring-test reconciliation — independent KEEP

Independent test-author owns only `tests/p14b1-promises.test.ts` and the
historical comment in `tests/helpers/p14b2-fixtures.ts`; handback frozen.
Combined diff29insertions/25deletions. Scoped whitespace checks pass; no runtime.
The three new B3 files remain byte-identical to the recorded independent RED.

ExampleB now uses real sign→current retention proposal→attachPromise. Its window
comes from actual proposal terms, with exact proposal→promise reference, one new
root, correct issuer/person, unbound/open state, original receipt and V29 validation.
It keeps the original baseline ACHIEVABLE→second-overlapping FRAGILE assertion
and uncommissioned-picture bottleneck. No self-exclusion for the second draft;
no forged binding. The old synthetic root had neither a bound contract nor a
current attachment and therefore pinned the very phantom-reservation defect.

The B2poach helper changes comments ONLY, distinguishing the inherited B2
accounting from corrected B3 membership. Every actual commission/action/script/
root/receipt remains unchanged; no fixture premise is silently removed.

Native contract-auditor **KEEP** on actual stable test diff; no assertion weakening,
API/type contradiction or authority conflict found. Reviewer did not inspect
mutable concurrent production and performed no edits/runtime/probes/typechecks.
Parent also inspected the exact diff. Runtime guard verification remains owed
after the sole production writer freezes its separate three-owner candidate.
