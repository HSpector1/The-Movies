# P14B.3 candidate — complete the existing P1 command route

Read-only preparation by native sim-core, source `ee91913`, 2026-09-19; parent
persisted this brief. No B.3 code, tests, runtime probes or product approval claimed.
T4 and B.2 remain the execution gates. This is not yet an audited task expansion.

The current `bridge/contract.ts` explicitly previews promises in
`MarketProposalDraft` but `applyMarketProposal` submits only the base proposal.
The Unity integration backlog records this gap. Completing P1 authoring is a
smaller next slice than adding a new promise family or relationship subsystem.

Reuse the canonical draft/parser, player-owned issuer, case-derived start week,
published terms/tier and existing proposal/attachment reducers. Quote and commit
must use one preparation path; a non-offerable promise must make the entire quote
noncommittable. Quote discards its temporary reducer result. Commit publishes only
the final successfully attached state, so rejection cannot leave a base proposal,
receipt, promise, money movement or revision partially committed.

Existing resubmission clears attachment before attaching a replacement; removal
and withdrawal preserve historical unbound records without inventing outcomes.
Retain draft values rather than a caller-owned mutable object. Reuse session and
revision guards, state-digest-bound opaque intent IDs, quote invalidation and
command-response replay. All promise material terms already enter the engine's
proposal digest. Only the player's issuer is accepted from this route; rival
terms remain UNKNOWN, with B.2's public outcome disclosure intact.

## Dependency to resolve with independent evidence

Source inspection found `reservedByActivePromises` counts every open root record,
including an abandoned unbound record whose proposal was revised or withdrawn.
This can make a replacement compete with its abandoned predecessor. A bounded
independent regression is required before correction; no runtime failure is yet
claimed. The reservation selector and digest must agree. Sim-core's candidate
selection is bound-open or currently linked to a proposal, but the coordinator
has requested a bounded contract review of that interpretation against the
companion and existing test/ruling authority, especially mutually exclusive
competing proposals. Do not silently treat this proposed selector as settled law.

Bounded native contract-auditor follow-through: adopt the narrow abandonment
correction, not bound-only reservations. The later B.1 T2b ruling (plan line 162
at `ee91913`) deliberately leaves reservation accounting ungated by binding;
quote/freeze operates on attached, not-yet-bound promises. No authority makes
abandoned unbound records active. Preserve current attachments and bound-open
promises, self-exclusion and the existing overlap/count laws, while sharing
membership with `feasibilityInputs`. Re-express the older synthetic unlinked
"active promise" test using an actually bound or currently attached promise;
retain its second-overlapping-promise refusal. T4's new digest test already uses
a current attachment and needs no premise amendment.

Separate unresolved boundary: multiple current proposals for one person are
mutually exclusive, but existing reservation accounting accumulates them across
issuers. The companion does not explicitly select that policy. Preserve current
treatment in the narrow orphan correction; changing competing-alternative
accounting requires its own bounded decision and chooser/submission-order tests.
This issue must remain visible without blocking independent P1 route completion.

## Independent acceptance outline

- Pure repeatable quote; all changed promise terms alter intent identity.
- Commit attaches exactly one promise, charges nothing immediately, binds only
  at actual winning settlement to the real employment contract.
- Both contract-window edges and FRAGILE/IMPOSSIBLE refusal; no partial effects.
- Revision, removal and withdrawal without phantom reservations/outcomes.
- Stale revision, wrong session, expired intent and replayed command behavior.
- Real settlement/outcome and V29 reload with B.2's privacy/public trust surfaces.

Existing request/response/save fields appear sufficient: Save V29 and B.2's
projection 46 may remain if no wire field/enum changes. A refused preview may use
existing quote `ok`/nested classification/message and omit a registered commit
intent. A newly introduced refusal enum instead requires a governed projection
bump. This must be checked against actual B.2 producers at execution. Native/UI
acceptance remains deferred; this is a headless command integration slice.

## Separate later inventory (not bundled into B.3)

P2 adds lead-only or lead/antagonist predicates, feasibility/digest/validation and
disclosure with the next governed save/projection wave. P3–P5 add directing,
genre/project references and singular-family feasibility, symmetric rival
authoring/seating, and the opportunity-class preference mapping. The primary-role
versus has-discipline discrepancy remains recorded for reconciliation.

Waiver and external VOIDED outcomes need their specified acceptance/lifecycle
dependencies. Relationships need a sparse pair owner, shared-work/conflict facts,
bounded durable history, read-time decay, a separate romance track, chemistry and
D5 consumers; existing artistic cohesion is not relationship chemistry.
The companion's Q1 override, Q2 temperament prior, Q3 behavioral backfill and Q4
hard seating refusal remain isolated from the first-slice defaults. No new product
decision blocks the existing P1 authoring route itself.
