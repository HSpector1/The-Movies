# P14B.2 T1 main bridge-test draft — authored, not executed

Draft intended for `tests/bridge-p14b2-trust.test.ts`:
`/tmp/studio-b2-main-tests-xLBdsi/bridge-p14b2-trust.test.ts`.
Repository-relative imports are intentional; this draft must be installed at
that exact path before its authorized module-resolution RED. No test/probe,
typecheck, production edit or frozen-helper edit ran during this authoring turn.
Parent retains runtime ownership; the repository test tree was not changed.

Authority: B2 expansion + producer reconciliation/audit in P14-HEADLESS-PLAN;
companion §§4.4/4.5/R5; persisted independent preparation brief. Native
test-author role. Parent explicitly approved using the proven retained fixtures
while sim-core repairs the independent F1 setup/wrap validation prerequisite.

## Coverage map (22 tests, nine groups)

1. Versions/schema: projection46, unchanged Save29, exact unchanged25 intents,
   old-five-plus-two attention vocabulary, real BridgeSession snapshot and all
   relevant Industry responses validated through the closed generated schema.
2. Trust: genuine SATISFIED/ranToEnd pair Reliable, real BROKEN and termination
   drivers, exactly three latest drivers from five material events on fallback,
   public date/year/reason text, immutable/pure reads, fresh Mixed/no-record.
3. History: two genuine contracts, newest first; precise contract/outcomeCause;
   real attached/withdrawn unbound exclusion; rival bound terms excluded from
   player but visible to issuer. Both carriers agree; reserved WAIVED/VOIDED wire
   values are retained without claiming an engine producer.
4. Pulse: exact outcomeEventId/receipt identity, same-week two outcomes,
   verbatim reasons, expected public names and outcomeKind, no term fields;
   harmless promise-array permutation detects positional joins. Actual next
   tick must save-validate across F1 and retain exactly one row per receipt.
5. Attention: actual poaching winner’s closed rival-owned case, empty current
   proposals, exact minus9/minus8 boundary, terminal-week-only outcome, own
   kept/broken cause text, dedup keys, absence after actual first take and during
   the later reminder window, actual withdrawn-unbound exclusion, rival exclusion.
   Explicit no-case read-only carrier perturbation retains the genuine binding
   and proves independent scanning; it is not claimed as a saved campaign.
6. Workspace: next actual open case with a nonempty prior bound SATISFIED row;
   history.promises equals both Profile rows and case.promiseHistory.
7. Privacy both ways: real rival-only open terms UNKNOWN; structural private-term
   exclusion across Profile/Market/attention/exhausted Pulse/entered studios;
   real rival-kept activity and real aggregate trustLabel positively required.
   A declared terminal-term disclosure variant preserves outcome/evidence/event
   identities, must still validate, and leaves public DTO payloads equal.
8. Industry: every entered studio including player; aggregate label uses ALL
   drivers. Actual3-positive/2-negative history is Mixed, while display-only
   truncation would mislabel it Distrusted. No per-person driver/line carrier.
9. Save/load: actual BridgeSession save of validated29 containing real kept AND
   broken outcomes; same session/revision/request identities; all public surfaces
   and session DTO payloads byte-equal after reload, no metrics comparisons.

`readModels` invokes BOTH prescribed imports (`trustBlockFor`,
`promiseRowsForPerson`) in every group. `bridge/trust.ts` remains absent in the
authoring baseline. Initial RED is deliberately module resolution, not a guessed
missing export. No runtime result is claimed until the parent executes it.

## Interpretations, limitations and next actions

- The helper signature `(state, personId, viewerStudioId, week?)` is an explicit
  implementation proposal; DTO facts and disclosure law are the requirements.
- Line/headline copy and the8-week reminder window remain the plan’s adopted
  hypotheses, not newly asserted Owner decisions.
- Pure bridge type extensions are test-side forward DTO adapters, not validators
  or optional checks that hide missing members. Actual session schema validation
  independently ensures the generated wire preserves the required fields.
- Both Pulse and studio pagination are exhausted with count/guard assertions.
  No first50-only absence claim. Raw count1 is not searched globally: unrelated
  rank/count/page values collide. Count privacy follows UNKNOWN/empty bound
  histories/structural term absence. SATISFIED count is NOT changed while retaining
  one real qualifying take; doing that would fabricate a contradictory outcome.
- The hidden-term mutation is explicitly a disclosure probe, not generated
  gameplay or migration provenance; state digests are intentionally excluded from
  semantic public-payload comparison because their source bytes really changed.
- No unknown-person malformed promise is fabricated: UNKNOWN competing terms are
  covered through actual rival proposals. Save validation owns invalid references.
- F1’s real next-week save-validity assertion remains strong. No validator bypass,
  setup deletion or synthetic wrap is used. If a new fixture assertion fails,
  report its premise/production source separately; do not weaken it to get green.
- The main test is not yet typechecked/executed. New absent-module TS2307 is
  intentional only until B2 production; other type errors would need correction.
- Outgoing45 runtime compatibility is covered by the separate frozen checkpoint
  file already handed back. Existing B1 history expectations will need their
  authorized projection46 widening separately. D3 remains untouched.

Next: parent installs this draft when F1 frozen-source verification permits,
records the intended absent-module RED, then releases the B2 production writer.
