# P14B.2 — active execution, 2026-09-19

## Live next action after fixture findings

**Current 13:56Z checkpoint:** F1 CLOSED at
`af455ef838b8c2227d784ebdc8bafb89e7a6835b`, bounded LOGIC VERIFIED · UNITY NOT
VERIFIED. `04`:17files/179pass/3unchanged todos; `05` root/UI and `06` bridge
typechecks PASS; independent final KEEP (`07`), exact-source closeout `08`.
`09`: genuine45 checkpoint-forward RED, two expected failures while45 is current.
No active runtime. Parent publishes evidence then installs independent main B2
draft `/tmp/studio-b2-main-tests-xLBdsi/bridge-p14b2-trust.test.ts`, records
absent-module RED, releases sole production writer for46. Auditor reviews the
draft read-only; author/sim-core idle. Original allocation below is historical.

### Earlier F1 in-flight record (superseded above, preserved)

T1 fixture work exposed **B2-F1**, the inherited setup/wrap save defect. Parent
and independent auditor confirmed the source conflict; contract and complete
review in `03-setup-wrap-review.md`. Dedicated independent RED `02i` completed:
three intended positive failures / ten negative passes, fixedSource and complete
source snapshots. Genuine outgoing45 checkpoint frozen (`02h`, actual source/schema
provenance); its two forward compatibility tests remain intentionally pending46.

Sim-core implemented and handed back the sole production change:
`src/core/productionSetup.ts` now accepts a released stage only with completed
setup, retained Set, exact permanent wrap production/stage/Set and ordered weeks.
All other checks remain; Save29/projection45 unchanged. **Source/tests FROZEN**.
Parent owns runtime: **`04-f1-targeted` running**, 17 files, single worker, then
both typechecks. No HEAD commit or tested-tree edits until checks finish.
Contract-auditor reviews that stable candidate read-only. Test-author writes ONLY
`/tmp/studio-b2-main-tests-xLBdsi/bridge-p14b2-trust.test.ts`, with no runtime or
active-tree changes. Parent inspected the frozen helper/tests/provenance. After
GREEN/review, checkpoint and release installing the B2 test for module RED.

Completed observations on unchanged `e37cd23`:

- `02c`: kept/broken, next-open/second-bound history, and natural rival outcome
  fixtures pass; real next-tick save fails at week61→62 (shooting→Post, stage
  released, completed setup retained). Complete source snapshots are retained.
- `02g-poaching-precondition`: one selected test passes (four intentionally not
  selected by `-t`), exit0, fixedSource true, 13:36:23.711Z–13:36:34.543Z.
  Actual rival-owned week208 case won by the player; reminder239/240 and BROKEN248
  states validate as V29. Two real player screenplay commissions at196/197 plus
  unused stock capacity were needed because two abandoned rival drafts still
  reserve that person's capacity. No promise record was deleted. This corroborates
  the separately recorded B3 orphan-reservation defect, not a changed B2 law.

After dedicated F1 RED: sole sim-core writer fixes historical setup validation
using exact permanent wrap proof, without deleting setup/restoring occupancy or
moving Save29/projection45. Parent runs fresh F1/fixture tests, R07 five files,
bridge R07, C2a stage-release law, all seven B1 files and both typechecks, then
bounded review/checkpoint. Final B2 full suites cover that producer fix too;
do not claim a separate all-green full F1 suite. Then original B2 T1 RED → T2
projection46 → D3 test → full qualified verification. See the plan's explicit
R07 selection-versus-historical-evidence reconciliation, not an invented new rule.

## Entry and original task allocation

Upstream gate: P14B.1 T4 qualified CLOSED at
`e37cd2330be8c9129b0193a2bf8e84258e851307`, pushed to
`wip/headless-program-20260916-ts`; `git ls-remote` returned that exact SHA.
Production `ee91913`, test corrections `a93bf77`; complete qualified evidence is
`../p14b1-20260919-t4-final/16-attribution.md`. No prior runtime remains active.

Scope: existing P14 plan B.2, including its audited attention independence,
projection-46 history widening and exact outcome-receipt join. Save V29 stays;
no T0 outgoing fixture mint. Trust/profile history, public Pulse outcomes,
independent promise attention, workspace history and public studio trust labels.
No chooser law change, B3 authoring/reservation fix, other promise family, Unity
handwritten/rendering/native work or acceptance claim.

Native role ownership:

- `b2_test_author_prep`: T1 independent tests and lawful fixture preconditions;
  writes new `tests/bridge-p14b2-trust.test.ts`, optional narrowly scoped helper/
  fixture probe, evidence `01*`/`02*`. Owns the sole heavy runtime slot for
  single-worker initial module-resolution RED and fixture proofs. Existing D3
  todo stays untouched until its separate task. Additional bounded schema-bump
  check: a genuine generated projection-45 runtime checkpoint (before producer
  moves), `tests/bridge-p14b2-checkpoint.test.ts`, and provenance under
  `tests/fixtures/p14` are authorized. No production writes.
- `t4_sim_core`: bounded READ-ONLY production/schema edit preparation until T1
  RED is recorded; will become the sole production writer on parent release.
  No runtime, mutable test inspection or edits in this preparation task.
- Parent: integration, source inspection, records and publication. No concurrent
  test run while test-author owns the slot. Contract-auditor idle.

Next: receive independently authored T1 RED plus actual kept/broken/poaching
fixture results, inspect them, then release T2 writer. Keep retention's real
`ranToEnd` history, and require actual poaching settlement before testing attention
after a closed case. Scope/fixtures are already described in
`../p14b1-20260919-t4/13-b2-test-preparation.md`; its literal-contract-ID issue
was fixed by T4. B2 forecast and carried-forward usage are in continuation/progress.

Producer reconciliation from read-only sim-core/parent inspection: the runtime
checkpoint registry's established schema-bump law requires outgoing projection
45 (`sha256:5b2a4ca93d930e90a288db55bb5cc3fdc8eea070ef51fa1450a193a325bd755d`)
to be registered when 46 becomes current. Freeze an actual generated 45 checkpoint
before that edit and pin governed migration/slot preservation independently.
This is NOT an outgoing save-version fixture mint; Save V29 does not move.
The registry's missing historical projections 32–44 are an inherited compatibility
gap, not covered or backfilled by B2; do not claim those checkpoints are supported.
