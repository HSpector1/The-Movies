# P14B.2 independent fixture proof and prerequisite RED

Source: `e37cd2330be8c9129b0193a2bf8e84258e851307`; native test-author.
No production changes, legacy fixture overwrites, commits, installs, UI or Owner
campaign access. Runtime handed back to the parent after `02i`.

## Proved engine constructions

- `02c`: real retention winners at week52, actual scheduled first take at week61,
  SATISFIED A and same-week real early termination/BROKEN B. Real employment IDs,
  separate outcome receipts, original ranToEnd history retained, validated V29.
  Aggregate history has three positive and two negative drivers; its newest three
  have one positive/two negative, distinguishing all-driver labelling from a
  label computed after display truncation.
- `02c`: the next actual open case at96 holds real prior history; a second promise
  binds at104. The actual withdrawn unbound draft is a separate branch: replacing
  it on the same branch would retain a real active reservation, so that failed
  premise is not erased to make a later offer feasible.
- `02c`: generated rival-only chain supplies a real open rival promise and real
  SATISFIED outcome; no player-issued record is mixed into the privacy premise.
- `02g`: actual poaching winner, rival-owned original case at208, player-owned new
  contract, no current proposals, reminder boundary239/240, BROKEN248; V29 validates.
  The initial candidate failed feasibility because two real unbound rival drafts
  reserve this person's window. The lawful construction activates script
  development, signs a writer, commissions two actual scripts at196/197, and keeps
  the stock-concept path. No rival promise, employment, outcome or expiry is
  fabricated/deleted. `02d` preserves the rejected commission-before-activation
  premise; `02e`/`02f` preserve the insufficient-one-script diagnosis.

These are engine fixture proofs, not B2 bridge GREEN. Main T1 bridge groups and
the prescribed absent-module RED are still owed at this handoff.

## Inherited setup save finding and dedicated RED

`retentionFixture()` legally signs the crew, records an explicit cash/ledger
bootstrap, commissions/builds a ballroom, retains two promised actors, greenlights
one picture, selects its setup during rehearsal, assigns/schedules a real take,
and ticks5→4. No setup/work/wrap/contract facts are manufactured.

The real next tick61→62 wraps `prod-0052`, clears its stage binding and enters
Post. The completed setup remains stage07/Set2 (admitted56, completed60). At
`e37cd23`, `makeSave` then refuses through frozenV25:
`workflows[0].setup: names a stage or Set this production is not bound to`.
`02c` records the complete before/after workflows and actual next-tick failure.
It is not dismissed as a fixture problem or removed by dropping completed setup.

`02i-setup-wrap-red`: **exit1, three failed / ten passed**, fixedSource true.
The three failures reach the intended setup validator: real wrap→Post; real
27-week advancement with compacted windowed events and retained permanent wrap;
and actual wrapped-but-Post-blocked work through the existing contention fixture.
That last fixture explicitly configures one Post slot and tests the pure setup
validator; it does not claim the configured whole save is accepted. All negative
exact-reference/date/completion/live-binding cases pass before production repair.

Fresh regression: `tests/p14b2-setup-wrap-regressions.test.ts`. Its actual source
bytes and hashes are retained in `02i-setup-wrap-red.sources.json` / `.json`.
The earlier `02a`/`02b` tracked patches were empty because tests were untracked;
their matching hashes and raw diagnostics are valid, but those runs alone do not
preserve full source bytes. From `02c` onward complete test-source snapshots exist.

## Genuine outgoing runtime45 checkpoint

`02h` ran the pre-46 generated-only minter successfully on the pinned outgoing
source/schema. Actual BridgeSession saved its generated initial state at11,
then accepted commissionScreenplay and advanceWeek to12. Three genuine journal
entries and distinct current/saved V29 slots are retained unchanged.

- `tests/fixtures/p14/genuine-projection45-runtime.checkpoint.json.gz`
- Raw JSON SHA256 `433629e8aa3f9e6ebbbaf331a63fa307ffe61a03c9a48257b3a8ca3f61a02fc8`
- Gzip SHA256 `00805fa390cc085a0300984ba094e02e7b1e9e53b0d23e286306d0c82820f9a9`
- Outgoing schema `sha256:5b2a4ca93d930e90a288db55bb5cc3fdc8eea070ef51fa1450a193a325bd755d`

The minter's original raw action caption incorrectly said week0; the provenance
JSON/minter caption were corrected to actual week11, not the checkpoint bytes.
`02h` reports fixedSource false because it created the authorized new fixtures;
existing code/test hashes stayed unchanged. This is a mint, not a frozen test run.
The new `tests/bridge-p14b2-checkpoint.test.ts` pins actual bytes and literal45
registration, new-session/reset-journal migration, and unchanged distinct V29
gameplay slots. It has not run yet; production is still projection45.

## Ownership / exact next action

Fixture helper, precondition test and F1 regression are frozen for the parent's
narrow production correction/verification. Parent owns runtime now. Continue the
main `tests/bridge-p14b2-trust.test.ts` groups and its module-resolution RED once
the parent authorizes test-file writes/runtime without overlapping that run.
The designated D3 todo remains untouched.
