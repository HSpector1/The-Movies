# B4 policy draft — PARTIAL, INERT, UNEXECUTED

Prepared 2026-09-19 UTC / 2026-09-20 Europe/Berlin under the native test-author
role. Only this brief and `p14b4-cast-class-policy.test.ts.draft.txt` were written.
No live tests, implementation, configuration, fixtures, execution, typecheck,
Git/network operation or descendant agent. Parent retains the runtime slot.

Authority: reviewed `P14B4-HEADLESS-PLAN.md` SHA256
382252e23b6353acf602d87f38032ff961e9f7f9740bbfdf2b2ae368c30df4e4,
especially the delegated public preference/strategy hypothesis. This is a new
bounded content hypothesis, not an Owner-selected taste or a discovered fact.
Source identity read from parent's immutable check18 metadata:
d39a9a04e4eae7767d5a15225de36e86f0440c14 plus protected diff
bb0858bc0968dff124f157b47de42095f5aa85e889a180238bdb720e2b03594b.
That identifies the additive data candidate, not a tested policy implementation.

Draft: 376 lines, 7 expanded cases; intended later installation path
`tests/p14b4-cast-class-policy.test.ts`.
SHA256 f422bc0a5f70d6597032b35d9d53f9fc460cbfbb9d8d93140b45ba292d338169.
No reached RED, GREEN, typecheck or constructibility result is claimed.

## Bounded coverage if guards actually execute

- Two pure-read age-edge cases (29/30), one real under-30 credited release
  witness, one irrelevant-cash/read-purity case. Strict expected public literals
  are `significantCastRole` and `anyCastAppearance`. Both existing priority arrays
  and preferred catalogue terms stay independently literal-pinned.
- Two actual settlement cases, one per archetype, each exercise P1, lead-only P2
  and flexible P2 against a no-promise baseline. All six D3 matches are observed
  through actual winners with exactly two real survivors and three identical
  named feasibility drops. The unproven P1 mismatch also attaches to the winning
  player branch and obtains genuine employment binding: mismatch is not illegality.
- One natural rival authoring case must observe all FOUR witnesses: unproven
  achievable flexible P2; genuine non-achievable P2 followed by achievable P1;
  proven P1; and unproven BOTH candidates non-achievable with exactly zero
  attachment. No absent branch passes conditionally. Fullterm/count1/order and
  exact original proposal/root/receipt joins are pinned. Failed candidates leave
  no intermediate root, reservation-bearing state, receipt, cash or RNG change.
  Successful authoring appends exactly one root and preserves all previous roots.

The proposed new public read API is ONLY `publicPreferredOpportunity(state,id)`.
Review its exact export/value spelling before installation. There is no invented
public descriptor, chooser, rival author, solver or final seating seam. Settlement
and authoring use the existing actual owners and transparent pass-through spies;
no feasibility return value or settled receipt is fabricated.

## Archetype and fixture qualifications

The shared source `isProven` reads existing `careerIdentity` OR age>=30.
`careerIdentity` already means a real workHistory credit at usable OVR. Parent
accepted preserving that existing law: this draft does not broaden low-OVR history
into a new identity rule or add another skill threshold. The credit witness must
have a real career event, exact counter increment, a released-film backing fact,
usable identity and age below30. No workHistory/career/release row is invented.
Pure age inputs are explicitly synthetic and change no historical fact or label.

The D3 fixture adapts the accepted controlled B2 shape, NOT a natural scenario.
Synthetic inputs are disclosed cash with matching ledger, age29/30, equal Standing,
and one early case/discovery tied to a real incumbent contract. The genuine
player52 ranToEnd supplies trust; it is never deleted/replaced with a fake driver.
Five offers are real submitProposal output. Three nonpair drafts are really staged
IMPOSSIBLE in [415,416), a lawful subinterval of [208,416). They do not overlap the
compared [208,415) promise. No fake seat drop, missing offer, invented binding,
reordered history or fake terminal receipt simplifies the pair. Actual freeze
must prove Reliable/term/Standing equality and expected compensation/incumbency
differences. A mismatch has the same exact three drops as baseline.

All these premises remain UNEXECUTED under evaluator4. Shared residual capacity
or changed natural rival policies may invalidate a feasibility/winner premise;
record that failure, do not adjust the chooser or reduce the assertion. The subject
must still genuinely have no workHistory before the disclosed age input. Live
makeSave validates constructed pre/post states without a restamped old envelope.

Natural authoring retains the established finite220-week witness guard, with no
timeout override. Observer calls the actual original evaluator/attach/market
functions. PromiseId-bearing freeze reads are excluded from authoring evidence;
only original CURRENT unattached rival proposals with exact real submission joins
qualify. Identical repeated reads may deduplicate; changed state/receipt cannot.
The fallback's input must equal the first candidate's complete input. RNG equality
is measured across the actual market phase, not a whole tick whose reception may
legitimately draw. Primary writer labels remain unchanged and are not refusal
criteria. Actual newly eligible writer authoring may occur under the existing law.

Both mode axes stay independent, per parent evidence17: generated studio fixtures
have managed operations but legacy script development. This policy draft never
forces managed development or commissions/greenlights a player film. No false
assumption that managed operations require managed screenplay development is used.

## Remaining scope / exact next actions

Natural P2 fallback and both-refused witnesses may be absent within220; that is
a fixture finding, not a policy proof or permission to stub the evaluator. The
existing prospective capacity owner might expose no naturally reachable P1-only
path on this seed; a later independent controlled owner-backed route would then
be needed. The real under-30 credit and controlled D3 guards are also unexecuted.

This file does NOT complete B4. Remaining independent coverage includes legacy
classless P2 fresh-freeze refusal (no invented D3 class), live archetype change
between submission/freeze with preserved prior choices, public bridge preference
disclosure, and actual final billing/initial-candidate selection. The private
chooseIndustryPackage route has not been replaced by a fake export here; final
seating needs its separate actual policy-owner fixture with legal complementary
staffing, conflicting ordinary score and real production/take. No claim that
successful rival authoring itself keeps a promise is made.

Next: parent/contract audit exact API and fixture premises, then separately release
installation and recorded serial RED. Preserve this inert artifact and any real
fixture failures. Follow-up outcome typing/current unknown-version reconciliation
is separately leased; it is not part of this policy draft's coverage.
