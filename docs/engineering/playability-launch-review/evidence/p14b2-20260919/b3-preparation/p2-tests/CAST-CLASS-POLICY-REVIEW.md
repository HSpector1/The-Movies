# P14B4 inert policy draft — independent bounded review

2026-09-19 UTC / 2026-09-20 Europe/Berlin. **REFINE one authoring-observation
boundary before installation.** The shared archetype, D3 comparison and four
required natural authoring witnesses are otherwise retained. No runtime, engine
probe, typecheck, live edit, Git write, network or delegation was performed.
This review file is the only edit. No fixture constructibility or test pass is
claimed.

## Exact reviewed artifacts and authority

- Entire 376-line, seven-case `p14b4-cast-class-policy.test.ts.draft.txt`:
  SHA256 `f422bc0a5f70d6597032b35d9d53f9fc460cbfbb9d8d93140b45ba292d338169`.
- Entire `CAST-CLASS-POLICY-BRIEF.md`:
  SHA256 `e6ffc172e9f58d7fa087c3644086d04bb1b5731431ae69e16095eb4ed24acd0b`.
- Reviewed B4 plan:
  `382252e23b6353acf602d87f38032ff961e9f7f9740bbfdf2b2ae368c30df4e4`.
- Relevant source boundary: `d39a9a04e4eae7767d5a15225de36e86f0440c14`
  plus additive-only `bb0858bc0968dff124f157b47de42095f5aa85e889a180238bdb720e2b03594b`.
  Parent has accepted the proposed ordinary derived accessor
  `publicPreferredOpportunity(state,id)` and the two independently pinned
  string values; its absence today is future implementation, not a draft defect.

## Required refinement: author-time facts are not always end-of-tick facts

**Draft lines 336–339 and 365 overconstrain the lifecycle.** For EVERY new rival
submission, the test demands one CURRENT proposal at the end of `tick`, then
compares its newly authored promise to the immediate unbound attachment result.
The accepted owner does not guarantee either persistence:

- `talentMarket.ts:77–92, 383–410` permits an entered studio to submit into an
  open decision-pending case; ordinary expiry is not early invalidation.
- The rival trigger/authoring stage runs before settlement
  (`talentMarket.ts:1180–1221`). A case whose decision week is the current week
  can therefore receive a lawful new proposal and settle in this same pass.
- `closeCase` removes that person's CURRENT proposals (`talentMarket.ts:861–884`).
  `commitWinningPromise` can bind the newly authored root and replace its
  submission feasibility receipt with the actual freeze receipt
  (`talentMarket.ts:1071–1090`). Those are required owner actions, not an
  authoring mutation or a policy failure.

This proves the universal observation assumption is invalid. It does **not**
prove a late proposal occurs on the proposed, unexecuted 220-week seed. No RED
or natural witness was run here.

Narrow correction: retain the transparent original evaluator/attachment spies
and use the captured immediate `write.after` proposal/root for positive
author-time attachment, digest, unbound-state and receipt equality assertions.
For end-of-phase joins, distinguish a still-open case from an ACTUAL same-pass
closed case using its real case/terminal receipt and, for a winner, exact
employment/freeze binding. Preserve old roots and distinguish lawful later
binding/receipt replacement from a duplicate or orphan new root. Do not require
CURRENT persistence after settlement, skip late submissions silently, fabricate
a terminal row, or modify the producer's ordering to fit the observer.

The negative branch must still establish zero attachment and no newly minted
pair root even if the real case subsequently closes. Keep the full input
equality between rejected P2 and fallback P1, exact candidate ordering/full
term/count1, and the final unconditional set of **all four** witnessed paths:
flexible P2, actual P1 fallback, proven P1, and both-refused/no-attachment.
The correction is an observation-boundary repair, not permission to weaken
those assertions or accept absent witnesses.

## Requirements retained / evidence limits

- **MET as source-aligned test contract:** `realProven` uses the exact existing
  shared criterion (`talentMarket.ts:680–683`): age>=30 OR a nonempty real
  `careerIdentity`. That existing helper requires a usable perceived discipline
  OVR and real workHistory credit (`talentSummary.ts:543–560`). The draft does not
  infer taste from priority-array position, add another threshold, invent a
  workHistory row, or change primary-role eligibility. The two age probes are
  expressly synthetic pure inputs; the under-30 credited branch additionally
  requires an actual release event and backing film fact.
- **MET as comparison design, runtime NOT VERIFIED:** the two controlled D3
  cases test all six archetype/class combinations through real settlements,
  retaining five real offers, three identically staged nonoverlapping impossible
  promises, two surviving bids, literal premiums, equal term/standing/trust
  bands and real binding checks. Under the actual Copeland owner
  (`talentMarket.ts:753–855`), baseline compensation versus incumbency is 1–1
  and compensation wins the public tie order for either archetype. A matched
  incumbent promise adds opportunity, making its pairwise score 2–1, even when
  opportunity is last in the proven order. The expected proven winner is
  therefore not an erroneous priority-only inference. Unproven P1 earns no D3;
  its additional winning-player branch protects mismatch-not-illegality.
- **MET as observer intent, subject to the refinement above:** all spies call
  actual originals, restore in `finally`, reject changed duplicate reads, and
  separate promiseId-bearing freeze reads from authoring. State/draft equality
  and RNG-state equality cover the actual market phase rather than falsely
  freezing RNG across a whole tick's reception. There are no stubs, fabricated
  feasibility results, conditional witness passes or larger timeouts.
- **NOT VERIFIED:** future rules4 feasibility, availability of the natural
  fallback and both-refused paths within220, the actual under-30 release witness,
  and controlled-case survival under changed rival behavior. Guard failure must
  be recorded at its reached boundary; it is not authority to forge winners or
  relax the chooser. Final seating, public bridge disclosure, legacy classless
  fresh-freeze refusal and archetype changes between submission/freeze remain
  separately owed, exactly as the brief discloses.

Preserve this original draft and review, then independently refine only the
named lifecycle observation. Re-review changed assertions before installation;
parent alone owns subsequent recorded serial execution.
