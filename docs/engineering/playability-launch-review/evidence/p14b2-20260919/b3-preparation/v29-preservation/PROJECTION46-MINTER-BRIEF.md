# Outgoing runtime46 preservation — INERT draft, UNEXECUTED

Prepared by the parent during B-F2's frozen full verification on89b5. This is the
separate runtime-checkpoint obligation, not a tenth SaveV29 scenario. No operational
installation, typecheck, test, checkpoint, save output or schema bump has occurred.

Draft `bridge-p14b4-mint-projection46.test.ts.draft.txt`, SHA256
`b093809d3be5c54bc9274433a9437c6c7f53cc28019ab17fd52afb8ce24f35f2`.
Intended temporary path `tests/bridge-p14b4-mint-projection46.test.ts`.
It uses the existing Vitest-backed B2 retention builder unchanged; no standalone
vite-node import of a Vitest helper. Independent bounded review is still required.

## Gate and actual recipe

Only after full-qualified B-F2 closeout, its exact publication verification and
parent release of the sole heavy slot: fill the four invalid tested-source,
published-source, closeout-path and closeout-hash sentinels. Set
`STUDIO_MINT_PROJECTION46_APPROVED` to that exact published SHA. Run ONLY the
installed entry with one worker under the fixed-source recorder. Planned
authority is Save29/protocol4/projection46/schema584bdd…/rules3. Reconcile any
legitimate upstream change against exact accepted evidence, never restamp bytes.

The gate requires HEAD equal the accepted published recovery, tested ancestry,
no producing/recipe/configuration diff, no untracked producing files, live version
pins, exact closeout hash, an operational entry under tests, and a completely
absent output directory. Producer paths include src/bridge/ui/generated, package
and lock, tracked tsconfigs/Vitest configuration and reused fixture recipes. It
rehashes the entry, producing files and closeout after ALL construction/readbacks.

Actual recipe: retentionFixture().submitted, real BridgeSession save, real B3
quote and commit withdrawal of one current player P1. The saved slot has the
current attachment; the current slot retains its abandoned unbound root and
receipt without that attachment. They deliberately have the SAME game week and
DIFFERENT bytes. No arbitrary elapsed-week or root ordinal is assumed. The other
person's proposal and all historical promise/take roots survive the actual action.
The generated campaign's disclosed cash bootstrap has an equal ledger delta.

Checks include exact proposal/root premises, no founding draft, unchanged promise
and first-take roots/RNG, stateRevision1, the actual save+command retry journal,
strict V29 validation of BOTH slots, exact same-schema checkpoint readback and
reopened session export. Replaying the original successful command must return
the actual historical response and leave the full encoded checkpoint unchanged.
Real processing-time fields remain actual producer output; no deterministic
remint identity or prospective-schema migration is claimed.

## Outputs and scheduling

Fresh exact directory `tests/fixtures/p14/genuine-projection46-runtime`:
one gzip checkpoint, its provenance, and MANIFEST.json LAST. All individual writes
use exclusive creation. ALL construction/validation/compression readbacks happen
before directory creation. Partial output is evidence to preserve, never a reason
to delete/retry over the same corpus. Provenance pins producer/entry/closeout,
versions, actual action IDs and week/revision/journal facts, raw/compressed hashes
and byte lengths plus both inner-slot hashes. Output is test-only, never an Owner
profile. No old outgoing45 or evaluator1 artifact is altered or reminted.

Run this and the final nine-save minter serially on the same accepted published
HEAD before moving it; if publication legitimately advances between them, reconcile
the second authority to the new exact publication and same tested producer. Archive
executed entry/metadata/raw output, independently verify artifacts, remove ONLY
temporary operational files via apply_patch, then checkpoint and publish both
preservation obligations before releasing the P2 writer.

During the later projection bump, register the literal outgoing46 schema identity
in SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS and independently add it to the exact
historical-set pin (retain all old literals/current exclusion/unknown refusal).
Future migration tests must use these genuine old bytes and compare each V29 slot
to its OWN governed V30 migration, not require version-changing envelope bytes to
stay identical. Preserve gameplay facts, receipt/digest history and distinct slots;
old outer journal/session resets follow the existing prior-schema boundary.
No native restart/Unity/rendering/Owner acceptance is established by this draft.
