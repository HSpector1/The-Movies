# P14B.3 independent draft handback — NOT INSTALLED / NOT EXECUTED

Written outside the checkout while the parent verified frozen P14B.2. Native
test-author role; no test/probe/typecheck/build/runtime was run for these drafts.
Intended installation paths are `tests/p14b3-reservations.test.ts`,
`tests/bridge-p14b3-promise-command.test.ts` and the separately authorized
`tests/p14b3-rule-revision.test.ts`; imports deliberately use those future
repository-relative paths, not the temporary directory. B2 qualified publication
and refreshed source/ownership are execution gates. No B3 production edit exists.

Authority read: P14 plan's audited B3 expansion; B2 evidence
`12-b3-expansion-draft.md` and `13-candidate-review.md`; original T4
`14-b3-preparation.md`; current native test-author role. Contract/session owner
APIs were read from the frozen B2 candidate (base6d23466); they have not changed in
B2. Core promise functions and existing B2 helper exports were read, not executed.

## Draft coverage

Core file: nine executable cases after `it.each` expansion, no todos.

- Current attached OPEN/unbound: reserves the sole existing path, changes digest;
  self recheck excludes only itself and leaves all stored evidence unchanged.
- Real revise and withdraw: abandoned roots and original receipts survive, while
  classification AND digest return exactly to the matched original inputs.
  Valid V29 serialization/reload preserves stored receipt bytes.
- Three real replacements: every live ordinal/root retained, no phantom buildup.
- Real winning settlement52: bound OPEN promise still reserves after current
  proposals disappear; exact employment identity; no forged binding.
- Competing CURRENT cross-issuer reservation remains active. This pins existing
  policy, not a decision to optimize mutually exclusive alternatives.
- Non-overlapping CURRENT attachment ignored; real early-termination BROKEN no
  longer active despite retained binding; actual dropped losing draft persists
  but no longer reserves a later overlapping read.

Bridge file: nineteen executable cases after `it.each` expansion, no todos.

- Quote→commit actually attaches one P1 at a NONZERO actual root ordinal, correct
  person/player/current proposal/material digest; revision once; unchanged money,
  RNG, old roots/receipts/employment and saved slot. Its warm abandoned draft has
  disjoint `[93,104)` window so this first attachment RED is independent of F1.
- Pure repeatable quote, opaque identity varied separately by family, count and
  both window edges. Refused-family variant tests identity, not a new P2 feature.
- Canonical conversion AND session pending draft retain values despite caller
  mutation, including nested promise. Conversion's nested object must differ
  from canonical.promise; canonical count/window are mutated independently of
  the wire object before original-value equality and successful apply are tested.
  Base no-promise route stays usable.
- Four whole-quote refusals: actual one-path FRAGILE count2, IMPOSSIBLE count999,
  lower/upper contract-window edges. Accepted answer has outer `ok:false`, exact
  nested classification/bottleneck, no invented base refusal enum, no registered
  intent. Direct apply returns no `next`; state/slot/revision unchanged.
- Direct apply rechecks an actual new competing CURRENT attachment. Malformed
  count0 and injected issuer are rejected by the actual wire parser; unknown
  person conversion cannot mint anything. No claim about a bypassed HTTP parser.
- Real replacement, removal, withdrawal and new offer preserve all abandoned
  roots without false outcomes/trust/history or immediate charges.
- Wrong session/stale revision, same-command replay, command-ID conflict,
  expired intent; exactly16 retain their oldest quote and a separate17th-quote
  case evicts the oldest; same-seed/ID session isolation; load and
  process restart discard pending authorization. Changed state is exercised by
  normal accepted commands/load. No unsupported in-place GameState mutation or
  forged private pending-map access is used to manufacture a same-revision state.
- Real winning expiry52 and actual due-week BROKEN92 → validated V29 reload,
  real employment/outcome joins, B2 own history and public trust, UNKNOWN rival
  proposal terms, no foreign private history, exhaustive Pulse paging and exact
  public outcome receipt join. Uses the accepted ONE real kept OR broken outcome
  alternative; B2 already independently covers real kept first takes. This is not
  new kept-outcome coverage or a native/client acceptance claim.

Separate rule-revision file: six executable cases after `it.each`, no todos.
This is the parent's source-backed traceability amendment; the original nine
core and nineteen bridge cases above remain byte-identical.

- Literal live PROMISE_RULES_VERSION2 pin, independent of unchanged Save29 and
  projection46. New real submit/attach operation creates root.version2 and
  receipt.rulesVersion2; old genuine roots remain byte-identical.
- All three actual accepted-B2 corpus files survive loadSave, importSave,
  migrateToV29 (same-version validation, NOT a claimed version conversion),
  and live export/readback byte-for-byte. Stored root.version1 and original full
  receipt.rulesVersion1/digests remain unchanged. Current versus withdrawn
  membership and old genuine writer-label refusal are separately guarded.
- Load the actual old retention submitted state, then advance real ticks to its
  current proposal's actual start week. The old root stays version1 but receives
  the fresh rulesVersion2/decision-week receipt and real matching employment row.
  A transparent pass-through spy observes actual pre-commit evaluator calls;
  no invented receipt or incorrect post-commit recomputation is used. Exact
  settled event, removal of current offers, and V29 roundtrip are required.

T0 prerequisite corpus: `tests/fixtures/p14/genuine-v29-pre-b3-evaluator1`, from
the qualified B2/rules1 producer before B3 edits. Exact planned filenames:

- `genuine-v29-evaluator1-current-p1.json.gz`
- `genuine-v29-evaluator1-withdrawn-p1.json.gz`
- `genuine-v29-evaluator1-role-label-refused-p1.json.gz`

Their provenance JSON files and last-written manifest are required. The draft
checks raw/compressed hashes against the actual published corpus metadata,
accepted producer identity, executed entry/helper hashes and focused original
receipt. No fixture byte, source SHA or historical content hash is invented;
these files do not exist until the authorized B2 preservation minter runs.
Independent literal artifact hash pins may be added from actual minted bytes
at T0 handback, never guessed before generation. Missing artifacts are an
explicit prerequisite failure, not B3 behavioral RED. Parent must complete T0
before running T1. This file does not enable writer eligibility (B-F2 remains
separate), remint history with a changed producer, or synthesize old versions.

## Explicit unexecuted fixture candidates / checks owed

Both files start a generated test campaign with the already-owned B2 `fund`
helper (explicit cash delta and matching ledger), sign a real available actor for
52 weeks through `applyActions`, and advance to45. They assert genuine case/
proposal preconditions, no existing promise to this actor and (bridge) validV29.
The stock-path premise is X1 reasonably achievable, X2 fragile because another
picture is not yet commissioned. These are hard guards, not observed results.
If any guard fails, record a fixture failure and reconstruct it lawfully; do not
replace the classification assertion with convenient current output.

Actual bound/outcome candidates use the proven shape of player-max-tier52-week
incumbent retention and no own production: real settlement52, broken92. The
additional rival draft in the final bridge case is explicitly IMPOSSIBLE staging
inside its actual term and disjoint from own promise. It is not natural rival
authoring or a bridge-offerable rival promise. The core losing case must witness
the named real drop before calling that record a loser.

The pending-bound cases separately ask16 distinct due-week quotes80..95 and17
quotes80..96, each required offerable; these remain runtime premises. The
two-session equality assertion
compares deterministic campaign state, not response timing/command journals.
Rejection journals may change as designed; truth assertions include all campaign
bytes, digest, revision and saved slot, not transport-history immutability.

No tests/typechecks have run. Static reading fixed a mistaken initial draft
assumption that TrustDriver exposes promiseId; the actual public DTO has kind,
week, sign and reason. Exact outcome identity is separately asserted on its real
receipt/Pulse event ID. No missing export is invented.

Pre-execution auditor refinement: only the two independent coverage gaps above
were corrected (canonical nested mutation and exactly16 positive boundary).
Neither correction claims a runtime result. A suspected quote-helper union typing
issue was rebutted by source inspection: the literal request selects the existing
market-specific BridgeSession.quote overload. No speculative guard/cast was added
and no TypeScript failure/correction is claimed for that helper.

The rule-revision amendment is likewise entirely UNEXECUTED: actual B2 corpus
minting, retention re-settlement under evaluator2, provenance readback and all
assertions remain runtime obligations. It uses only existing save/market/promise
APIs. The old corpus gate, not an invented missing export, precedes behavioral RED.

Static source identities after this amendment:

- `p14b3-reservations.test.ts`: SHA256 `bb104d96af97c55e9385c332e080c287c953b088721b08134a65b6b5b5dd19ba` (unchanged).
- `bridge-p14b3-promise-command.test.ts`: SHA256 `2074c9616e3d1a306109e8780fb0af87a549a569cbdf308c1a79c6cd8d1934de` (unchanged).
- `p14b3-rule-revision.test.ts`: SHA256 `1fc532f5ef03aee01038061fd3eb354465f8d27b3ddf43b4b23ecc830147ab14` (new, 167 lines / six cases).

## Later narrowly authorized neighboring edits, not made here

- `tests/p14b1-promises.test.ts` ExampleB currently installs an unlinked synthetic
  OPEN root. Re-express with a genuine current/bound attachment, retaining its
  second-overlapping-promise capacity refusal. Parent must allocate this file.
- B2 `tests/helpers/p14b2-fixtures.ts` poach comment says an abandoned earlier
  draft remains a reservation. After F1, qualify this as the B2/inherited premise;
  retain the extra real commissioned scripts and every root/receipt. No helper
  change or new fixture bytes were authored here.
- Root/UI and bridge typechecks, behavioral RED recording with complete source
  bytes, targeted/full checks and publication belong to later assigned T1–T4.
  Save29/projection46 pins assume unchanged wire shape, per audited expansion.

Only repository edit during this drafting turn was the separately authorized B2
generator F10/F11 hash correction, already frozen and handed back. B3 files and
this brief exist only in the fresh task-specific temporary directory.
