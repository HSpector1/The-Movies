# P14B.2 T2 — candidate and independent source review

Base `6d23466bbdefbe0a4061bea31b599e739591a63c` is pushed and independently
remote-SHA verified. Sim-core handed back frozen production after
`npm run generate:bridge-contract` exit0 and scoped diff-check exit0. No tests or
typechecks were run by sim-core. Final source/test candidate committed
`3b978d77c29df2b5de038e3b1b7fc94778257f33`; full verification remains owed.

Candidate schema `sha256:584bdd8565030f049d548b1af4fcbf8c517ca7c9150016736f632f1ef8fcb98c`;
C# SHA256 `1b5c7e889ffe3454858afa8960b4a4c099d88cfe25a9553212ba67f11a4c3268`;
generator-source SHA256 `c80d25c3936c87c202b6ce3de6b0cd7d574cb73e8b3d1d61213e7da4e8ac3cc9`.
Save29/intents unchanged; only outgoing45 registered. Native code untouched.

Native contract-auditor **KEEP**, read-only, no concrete blocking source defect.
Issuer/bound filtering and open+zero-progress due threshold are correct;
workspace attention scans independently of case carriers and deduplicates
cause/person. All history carriers reuse the widened bound-only newest-first
selector. Pulse joins exact outcome IDs and retains only public identity/outcome/
reason, including rivals. Aggregate labels use all drivers before the display
cap. Fallback extraction is behavior-preserving. Required/nullable/optional DTO
members are preserved; no new runtime schema cycle found. Validators/gameplay
law unchanged. Parent also inspected all source diffs.

Limits: source review is not runtime, typecheck, generated-byte, performance or
native evidence. Independent D3/version-pin edits are being finished separately;
parent owns serialized verification after all source/tests freeze. The main
22-test requirement file/checkpoint fixture and F1 tests remain unchanged.

## First frozen runtime result

`14-candidate-target`, 14:08:13.245Z–14:09:27.947Z, fixedSource true, exit1:
five files, **52 passed / one failed / two unchanged todos**. B2 main22/22,
genuine45 checkpoint2/2, F1 13/13, fixture preconditions5/5. D3 file10pass/1fail/
2todo; failure is a premise guard at line377: expected three dropped nonpair
proposals, actual `[]`. Baseline winner and treatment assertions were not reached;
no chooser defect or D3 proof is claimed. Test-author investigates read-only while
parent typechecks the SAME frozen candidate; no correction or assertion weakening.
Complete patch SHA256 `125cd670b8acdc13b34efb5dc7b9193faa21751c68edb666c42277810064df8c`.

Parent static pin review also found `bridge-schema.test.ts:341` still expects
literal45 when rejecting malformed24; this must follow live46 after checks end.
It is not in14's selected files. Genuine45 bytes/hash/registry remain untouched.

`15-candidate-typecheck`: root/UI PASS, 14:10:20.114Z–14:12:04.457Z, exit0,
same patch hash, fixedSource true. Bridge typecheck still owed.

D3 diagnosis: the synthetic case is appended before normal196 discovery, so it
settles first at208 while other rival seats are vacant. Expecting three seat
drops was unsupported, not a chooser defect. Withdrawal at207 is not a fix because
weekly rival proposal triggering runs again at208 before settlement.
Parent authorized a controlled, lawful staging fixture: retain five offers and
all history; three nonpair issuers attach explicit IMPOSSIBLE drafts via real
`attachPromise`, window `[415,416)` inside their actual `[208,416)` contracts.
The pair treatment's feasible window `[208,415)` is disjoint, so these drafts do
not consume its opportunity seats. Require the same three actual feasibility
drops in both branches, exactly two surviving pair proposals, original equal-band
and causal winner-change guards and actual freeze/binding. These are deliberately
staged nonofferable drafts (T4's law), not a bridge-offerability or natural-campaign
claim. Source/chooser unchanged; no result claimed until the corrected run.

`16-d3-schema-target`: **two files /33 passed /two unchanged todos**, exit0,
14:14:15.501Z–14:15:27.502Z, fixedSource true. D3 now passes with all controlled
premises and causal winner/binding guards; malformed projection24 rejects with
the correct live46 diagnostic. Full corrected patch SHA256
`fa9849e9c86885d59e2915a0ebdc6ba995bf0657a1617363b322c02615b0f7f9`.
Independent review of the ACTUAL frozen corrected D3 test KEEP: genuine staged
attachments, exact disjointness/feasibility, same three named actual drops/two
survivors, equal bands, winner reversal and week208 real binding/freeze receipt.
The other two todos remain unchanged. No chooser production change was needed.

`17-candidate-typecheck-bridge`: PASS, exit0, 14:15:54.257Z–14:16:31.689Z,
same corrected patch hash, fixedSource true. Both typechecks have passed;
root/UI15 preceded the two narrow test corrections and is rechecked by the
committed full pass. Neighbour regression18 is now running serially.

`18-neighbour-regressions`: 10 files,149 executed tests: **148 passed /one failed /
one unchanged todo**, exit1, 14:16:49.571Z–14:18:31.089Z, fixedSource true on the
same corrected patch. Nine files pass; sole failure is F10's stale whole-schema
declaration hash in `bridge-contract-generator.test.ts`. Independent sim-core
read-only diagnosis: BOTH F10/F11 are direct current `BRIDGE_SCHEMA` references,
not frozen union subsets; the generator emits all types/vocabulary/converter
bodies. B2 fields/enums/trust classes legitimately change those two hashes.
Generator implementation, DSL, canonical helper, fixture sources and standalone
generated union fixtures are unchanged. Exact observed F10 body SHA256
`9edf6dedf8ac4305a44d3a59ce124e56d5bee4916a9ab63d3d95cf3c9fedb64e`.
Authorize only the two current hash literals plus B2 provenance comment, retaining
all strict assertions and other fixture hashes; F11 needs actual rerun because
the loop stopped at F10. No production correction justified.

`19-contract`: PASS 14:18:46.334Z–14:18:48.328Z; `20-contract-fixtures`: PASS
14:19:11.045Z–14:19:12.667Z. Both exit0/fixedSource true on the same patch; the
standalone generated union fixture remains unchanged. No native/Unity claim.

`21-generator-regression`: **31/31 PASS**, exit0, 14:21:10.486Z–14:21:16.090Z,
fixedSource true. BOTH F10 and F11 exact hashes and deterministic rerendering now
executed, other hashes/negative/mutation checks unchanged. Complete final tested
patch SHA256 `279a682dd47bd41e7d50ae742db59e37531d0e3ecd67b72b94079306eae356d8`.
This differs from16–20 only in the two supported current hash pins/comment.
Root/UI15 preceded D3/pin-only corrections; full committed pass repeats all
typechecks/contracts/tests. No wholly green full suite or B2 closeout is claimed.

Parent preflight for the server-inclusive full run: disposable Node `net` server,
`listen(0, '127.0.0.1')` returned `EPERM` inside sandbox. No server remained open
and no campaign/fixture was modified. Full runtime needs the normal authorized
sandbox escalation; do not weaken/skip HTTP tests or misattribute EPERM to code.

## Separate B3 contract review (not implementation)

Auditor **KEEP** on `12-b3-expansion-draft.md` as the next bounded slice AFTER B2
closeout. Source confirms apply lacks attachment, nested feasibility does not gate
outer quote, and pending drafts retain caller references. Shared immutable
preparation, atomic failure, material-term identity and session-isolation tests
address those omissions without a new promise family/law.

Reservation prerequisite preserves the existing ruling: bound-open/currently
attached reserve; abandoned unbound do not. Competing CURRENT-proposal accounting
remains separately unresolved and unchanged. Independent behavioral RED remains
required; this review is neither B3 execution nor verification.
