=== (coordinator correction: the access-held gate must publish-disabled, not hide; rows now gated on technologyForecast(entry,week).kind === 'exact' alone) ===

=== git diff bridge/laboratory.ts ===
diff --git a/bridge/laboratory.ts b/bridge/laboratory.ts
index f68667b..7304eb2 100644
--- a/bridge/laboratory.ts
+++ b/bridge/laboratory.ts
@@ -359,14 +359,14 @@ export function laboratoryActionSpecs(state: GameState): readonly LaboratoryActi
   // synchronized sound alone). Both rows STATE THE EXACT COMMERCIAL WEEK — the wait row in
   // its own text, the purchase row through the engine's own `commercialAccessRefusal`
   // sentence — so they exist only while that week is PUBLIC, which is exactly while this
-  // technology's forecast is exact (S7-T3 ruling). Sound's window is degenerate: its two
-  // rows are unchanged at every week. Lighting's appear at its announcement week, disabled
-  // with the engine's refusal until its commercial week. Neither row is offered once this
-  // studio holds the access: there is then nothing left to wait for or to buy, and the
-  // engine refuses both verbs.
+  // technology's forecast is exact (S7-T3 ruling). That forecast kind is the WHOLE existence
+  // rule: sound's window is degenerate, so its two rows are published at every week exactly as
+  // P13A published them, and lighting's appear at its announcement week. Whether either row can
+  // be taken is the ENGINE's answer alone, through `add`'s dry run — access already held
+  // disables both with the engine's own sentence, and a purchase before the commercial week
+  // carries `commercialAccessRefusal`. This page hides no decision it can state a reason for.
   for (const entry of TECHNOLOGY_CATALOGUE) {
     if (technologyForecast(entry, state.market.tick).kind !== 'exact') continue
-    if (playerTechnologyAccess(state, entry.id)) continue
     const word = entry.id === SYNCHRONIZED_SOUND.id ? 'sound' : entry.name.toLowerCase()
     add(`wait-${entry.id}`, { kind: 'waitForTechnology', technologyId: entry.id },
       `Wait for commercial ${word}`, `Record deliberate waiting for ${campaignDate(entry.commercialWeek).label}. No access payment or capability is granted. Existing research and employment continue unless you pause or cancel them separately.`)
=== npx vitest run tests/bridge-p13b-s7-disclosure.test.ts --minWorkers=1 --maxWorkers=1 --reporter=dot ===

 RUN  v2.1.9 /Users/zacheryspector/The-Movies-headless-program

 ❯ |core| tests/bridge-p13b-s7-disclosure.test.ts (22 tests | 1 failed) 34417ms
   ✓ P13B-S7-T3 item 2: Laboratory forecast rows — windowed before 884 (no leak), exact at/after 884, sound exact throughout > week 793 (windowed, unannounced): lighting forecast row is a window 884..988; campaignDate(936).label appears NOWHERE in the Laboratory page JSON 426ms
   ✓ P13B-S7-T3 item 2: Laboratory forecast rows — windowed before 884 (no leak), exact at/after 884, sound exact throughout > week 884 and week 900 (exact, announced): lighting forecast row publishes the exact 936 date, window labels null 466ms
   ✓ P13B-S7-T3 item 3: replacementLabel on the purchase row, adopt-* rows and adoptions[] rows, for both technologies > committed adoptions[] rows for both technologies carry replacementLabel verbatim (s6SoundReady/s6LightingReady) 11874ms
   × P13B-S7-T3 item 4: generalised wait-<id>/purchase-<id> rows — sound unchanged, lighting per the MEASURED engine law (see header) > purchase-lighting-control-01 at week 936 (commercial open): enabled, accessCost in its own text; commit charges accessCost once, advances stateRevision, refuses the same intent at the old revision, and the row disappears once access is held 1638ms
     → expected true to be false // Object.is equality
   ✓ P13B-S7-T3 item 5: Industry announcement row — derived, nullable studioId, excluded from per-studio History, absent for sound, Save As identical > identical in a Save As world: two independently-saved-as campaign slots of the same week-884 state publish byte-identical announcement rows 7930ms
   ✓ P13B-S7-T3 item 6: rival-independence on the wire — identical forecast/announcement rows across states differing only in rival facts (PARTIAL until S8, per the engine's own test 2 scope) > Laboratory forecast rows (both technologies) are byte-identical across the two rival-divergent states at week 416 510ms
   ✓ P13B-S7-T3 item 6: rival-independence on the wire — identical forecast/announcement rows across states differing only in rival facts (PARTIAL until S8, per the engine's own test 2 scope) > industry announcement rows are byte-identical across the two rival-divergent states, both at week 416 (empty) and advanced independently to week 900 (one lighting row) 11132ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL |core|  tests/bridge-p13b-s7-disclosure.test.ts > P13B-S7-T3 item 4: generalised wait-<id>/purchase-<id> rows — sound unchanged, lighting per the MEASURED engine law (see header) > purchase-lighting-control-01 at week 936 (commercial open): enabled, accessCost in its own text; commit charges accessCost once, advances stateRevision, refuses the same intent at the old revision, and the row disappears once access is held
AssertionError: expected true to be false // Object.is equality

- Expected
+ Received

- false
+ true

 ❯ tests/bridge-p13b-s7-disclosure.test.ts:441:78
    439|     // exists regardless of state; asserted as a forward regression gu…
    440|     const after = labPage(session, buildingId, nextRequestId('purchase…
    441|     expect(after.actions.some(a => a.id === 'purchase-lighting-control…
       |                                                                              ^
    442|     expect(after.actions.some(a => a.id === 'wait-lighting-control-01'…
    443|   })

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯

 Test Files  1 failed (1)
      Tests  1 failed | 21 passed (22)
   Start at  00:56:10
   Duration  57.95s (transform 3.35s, setup 0ms, collect 22.69s, tests 34.42s, environment 0ms, prepare 130ms)

exit=1

=== npx vitest run tests/bridge-p13-laboratory.test.ts tests/bridge-p13b-s2-labs.test.ts tests/bridge-p13b-s5-adoption.test.ts --minWorkers=1 --maxWorkers=1 --reporter=dot ===

 RUN  v2.1.9 /Users/zacheryspector/The-Movies-headless-program

 ✓ |core| tests/bridge-p13b-s5-adoption.test.ts (11 tests) 30297ms
   ✓ requirement 1: laboratory.adoptions[] structured rows and per-row StudioAdoptionQuote > a committed lighting adoption appears on adoptions[] with technologyId/route/committedWeek/operationalWeek/components/equipmentAssetId/postFacilityId 7822ms
   ✓ requirement 2: lighting rows are access-gated for presence and quote the exact 100,000 first-prototype total > no adopt-lighting-control-01-* row exists anywhere before lighting access is acquired 5679ms
   ✓ requirement 3: sound rows keep P13A stage+Post pairing, existing-Post reuse at $0 > a second stage's sound row reuses the first stage's operational Post: reusedPostFacilityId set, the Post component existing/$0 3782ms
   ✓ requirement 4: committing an adopt row through the bridge (engine-truth components, revision advance, stale refusal, row disappears) > commits exactly one adoption with the wire-quoted components, advances stateRevision, refuses the same intent at the old revision, and the committed stage's own row disappears 1523ms
   ✓ per-technology stage rule: a stage with an adoption of one technology still offers the other, never the same one again > after committing sound on a stage, that stage's own sound row is gone but its lighting row is still offered 6964ms
   ✓ requirement 5: plan-queue-adopt-<technologyId>-<stageFacilityId> companions never mint an adoption and follow the S4 reachable-hide rule > queues the stage-installation blueprint through queuePhysicalPlan, mints no adoption, and then hides itself once queued (duplicate-queue rule) 947ms
   ✓ requirement 6: refusal disclosure and player-safety on a row whose engine quote is refused > a sound row before access is acquired publishes the engine's own refusal/rejections, agrees with disabledReason, and the underlying action throws that exact text 3427ms
 ✓ |core| tests/bridge-p13b-s2-labs.test.ts (10 tests) 80415ms
   ✓ case 1: a fresh Laboratory publishes snapshotVersion 38 and an empty projects array > a Laboratory with no research project at all reports laboratory.projects === [] 847ms
   ✓ case 2: seating is lawful before a brief's research opens; the wire dates the wait instead of hiding the brief > before week 780 (week 263, S1-style state): both the sound and lighting assign rows exist for a free employed person; the lighting row's detail names the wait, the sound row's does not (sound opened 260); dispatching the lighting assign through the bridge is accepted, publishing a paused lighting project whose bottleneckLabel also names the wait; its begin row is disabled with the same engine text 5256ms
   ✓ case 3: two full Laboratories — the shared project reads identically from both pages > dispatches 4+4 sound assign rows through the bridge on both Laboratories, funds the full $80,000 ceiling through the bridge's own budget row (the S2-T6 gap that used to cap this menu at $40,000 was closed 2026-09-16 — the menu now reaches 60,000/80,000) and begins through the bridge; before the first tick both pages already report companion test 4 case (i) (7 funded weeks remain, 0 of 64 verified units); one bridge-advanced week reproduces case (i) exactly (output 9.75, units 1,560,000) on both pages 21526ms
   ✓ case 4: two technologies staffed on one Laboratory > lists two projects rows in stable id order; every project's progressLabel names its own verifiedWork/work; each fully-funded 2-seat project's cooperationLabel reads "One Laboratory: 2 seats" with the real $20,000/60,000 figures; the fifth assign for EITHER technology on this Laboratory is refused with "four seats" 15190ms
   ✓ case 5: a third Laboratory is refused on the wire > a real, completed third Laboratory (placed and its build time elapsed — the same construction tests/p13b-s2-labs.test.ts's own base3Setup uses for the identical core-level "test 3" case) publishes its assign row disabled with the engine's own "third Laboratory" refusal 7574ms
   ✓ case 6: a migrated V21 fixture keeps its honest single-pool receipts on the wire > loads through BridgeSession.fromSaveJson (a genuine V21 fixture, migrated on import): the sound project's three stored receipts read labs:null; the lighting project's three receipts each read one derived row; the next quoted week splits 4+2 seats at $60,000 into $40,000/$20,000, output 7.875 588ms
   ✓ case 7: releasing a seat through the bridge recomputes the funding split > releasing one of Lab 2's two sound seats (4+2 -> 4+1) through the bridge recomputes next week's split to $40,000/$10,000, output 6.9375 6392ms
   ✓ case 8: player-safe — no rival project, seat or talent id ever appears > the Laboratory page names only the player studio's own project and talent ids 6276ms
 ✓ |core| tests/bridge-p13-laboratory.test.ts (7 tests) 8970ms
   ✓ P13A Laboratory bridge > publishes one exact Laboratory with named employment and truthful early-release refusal 794ms
   ✓ P13A Laboratory bridge > rejects wrong building, stale revision and Save As session while keeping output detached 418ms
   ✓ P13A Laboratory bridge > commits the exact named hire once, rejects its stale sibling intent, then assigns the employed Scientist 1886ms
   ✓ P13A Laboratory bridge > acknowledges completed acoustic instruments while the Scientist seat remains unassigned 387ms
   ✓ P13A Laboratory bridge > shows rival sound only from the actual operational receipt, without exposing private finances 5007ms

 Test Files  3 passed (3)
      Tests  28 passed (28)
   Start at  00:57:23
   Duration  124.93s (transform 2.33s, setup 0ms, collect 4.07s, tests 119.68s, environment 1ms, prepare 275ms)

exit=0

=== npm run check:bridge-contract ===

> project-studio-core@0.1.0 check:bridge-contract
> vite-node scripts/generate-bridge-contract.ts --check

verified /Users/zacheryspector/The-Movies-headless-program/bridge/schema/project-studio-bridge.schema.json
verified /Users/zacheryspector/The-Movies-headless-program/generated/unity/StudioBridgeDtos.Generated.cs
verified /Users/zacheryspector/The-Movies-headless-program/generated/unity/project-studio-bridge.contract-manifest.json
exit=0

=== git status --short (schema/generated artifacts unchanged by this correction) ===
 M bridge/laboratory.ts
 M docs/engineering/playability-launch-review/evidence/p13b-s7-20260917/14-bridge-access-gate.txt

=== npm run typecheck:bridge ===

> project-studio-core@0.1.0 typecheck:bridge
> tsc -p tsconfig.bridge.json

exit=0
