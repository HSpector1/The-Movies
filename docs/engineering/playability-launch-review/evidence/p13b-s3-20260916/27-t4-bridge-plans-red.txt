P13B-S3-T4 test-author task — tests/bridge-p13b-s3-plans.test.ts, FIRST run.
Command: npx vitest run tests/bridge-p13b-s3-plans.test.ts --minWorkers=1 --maxWorkers=1 --testTimeout=200000
Run at: 2026-09-16 23:38:35 (local), HEAD 30836b0, worktree wip/headless-program-20260916-ts.

TIMING NOTE (read before this tail): the task briefing expected this first run to be RED
(PROJECTION_VERSION still 34, no `plans` view). That was NOT the observed state. At the
moment this file was first authored and run, the working tree already carried an
UNCOMMITTED, apparently-complete S3-T4 implementation from the concurrent sim-core
specialist: bridge/plans.ts (new, untracked), bridge/laboratory.ts, bridge/industry.ts,
bridge/session.ts, bridge/history.ts and the bridge schema/generated-Unity files all
already modified, `grep -n "PROJECTION_VERSION = 35" bridge/schema/bridge-schema.ts`
already matched, and this same evidence directory already held sim-core's own
26-t4-contract-checks.txt / 26-t4-typechecks.txt / 26-t4-schema-generator-lock-bridge.txt
(timestamped 23:35-23:36, i.e. ~2-3 minutes before this run) confirming the Unity
contract/fixture generator was already verified against the new schema. bridge/plans.ts,
bridge/laboratory.ts and bridge/history.ts's mtimes (23:31-23:32) were unchanged between
that check and this run.

So this is honestly a same-implementation capture, not a true pre-T4 RED baseline — the
per-task instruction to save a RED tail is fulfilled literally (this is genuinely the
FIRST run of this file), but the "projection still 34 and no plans view" premise did not
hold. The task's own escape valve ("if PROJECTION_VERSION = 35 matches, rerun and save
27-t4-bridge-plans-after-t4.txt") applied immediately; both files are produced, and the
second is expected to match this one absent further concurrent edits — see that file for
confirmation.

10 of 11 cases passed even on this "RED" run, because T4 was already substantially
implemented. The ONE failure (case 2's final assertion) is a genuine, reproducible
requirement-vs-implementation gap, not a flaw in the pre-T4 absence this file was meant to
capture — see the inline CONTRACT CHECK comment in the test file and the final report for
the minimal repro and the two readings it separates.

──────────────────────────────────────────────────────────────────────────────

 RUN  v2.1.9 /Users/zacheryspector/The-Movies-headless-program

 ❯ |core| tests/bridge-p13b-s3-plans.test.ts (11 tests | 1 failed) 12607ms
   ✓ case 1: a fresh studio publishes an empty plans page; plans is null off the plans view > snapshotVersion 35; rows and actions are both empty arrays on a fresh studio; laboratory view carries plans: null 1097ms
   × case 2: the Laboratory-page acoustic companion queues, admits and appears on the plans page > plan-queue-acoustic-<lab> exists beside instruments-<lab> naming the cost/build-weeks/no-reservation facts; dispatching it queues a full row on the plans view; one bridge advance starts it, writing planQueued/planStarted history; the acoustic companion then disappears while electrical still offers 1434ms
     → expected false to be true // Object.is equality
   ✓ case 3: the envelope hold (insufficient cash) and the changed-quote hold (forged fingerprint) > a queued electrical plan with cash spent below its cost stays queued after an advance, with next.reason === "insufficient cash" 1223ms
   ✓ case 3: the envelope hold (insufficient cash) and the changed-quote hold (forged fingerprint) > a forged approvedQuote.fingerprint holds at the next boundary; plan-review-<planId> names the approved/pending figures; dispatching it re-queues with the pending fingerprint promoted, then it starts 1531ms
   ✓ case 4: cancel blocks its dependent; a started plan has no cancel row > cancelling the predecessor cancels it and blocks the dependent, naming it, with matching history rows; the cancel row disappears; an independently STARTED plan carries no cancel row at all 1650ms
   ✓ case 5: reorder > three independent queued plans: move-down swaps ordinals 1<->2 on the wire; move-up is disabled at the top and move-down at the bottom 1642ms
   ✓ case 5: reorder > a dependent immediately following its predecessor cannot move up: disabled with the engine's own "before its dependency" text 415ms
   ✓ case 6: admission mode toggle > the mode NOT currently set is the only admission action offered; dispatching it flips the row's admission 1257ms
   ✓ case 7: stale revision > a plans intent submitted with a stale expectedStateRevision is refused with STALE_REVISION, and that specific attempt changes nothing 1966ms
   ✓ case 8: player-safe — no rival plan, planId or facility ever appears > a structurally injected rival plan row never appears on the player plans view, in no action id, and its studio id leaks nowhere in the page 384ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL |core|  tests/bridge-p13b-s3-plans.test.ts > case 2: the Laboratory-page acoustic companion queues, admits and appears on the plans page > plan-queue-acoustic-<lab> exists beside instruments-<lab> naming the cost/build-weeks/no-reservation facts; dispatching it queues a full row on the plans view; one bridge advance starts it, writing planQueued/planStarted history; the acoustic companion then disappears while electrical still offers
AssertionError: expected false to be true // Object.is equality

- Expected
+ Received

- true
+ false

 ❯ tests/bridge-p13b-s3-plans.test.ts:280:84
    278|     const afterStart = labPage(nextSession, buildingId, nextRequestId(…
    279|     const acousticCompanion = afterStart.actions.find(a => a.id === `p…
    280|     expect(acousticCompanion === undefined || acousticCompanion.enable…
       |                                                                                    ^
    281|     const electricalCompanion = required(afterStart.actions.find(a => …
    282|     expect(electricalCompanion.enabled).toBe(true)

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯

 Test Files  1 failed (1)
      Tests  1 failed | 10 passed (11)
   Start at  23:38:35
   Duration  16.88s (transform 2.72s, setup 0ms, collect 3.67s, tests 12.61s, environment 0ms, prepare 123ms)
