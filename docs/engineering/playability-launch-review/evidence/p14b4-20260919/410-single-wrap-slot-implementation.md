# 410 — Certified single-picture wrap slot bound

2026-09-20. Native sim-core. SOURCE FROZEN; production write ownership yielded.
Implemented adopted407 against published/exact-remote base
`e7aa158a04c3dec0b2e04e2a7211545f74fdf485`.

Only `src/core/promiseCapacityOwnerReplay.ts` and this handback were written.
No runtime, tests, typechecks, probes, Git, network or delegation were run.
The independent author's additive Post retained-property control is not this
writer's work and was not inspected or edited here.

Source SHA-256 before:
`6eb19801534c950e91620b4167be07e4285817981d0263e24d660c7d43b4dd9e`.
Source SHA-256 frozen:
`e7cb5ce0e44b7ef22235c391cdd01498c5b22315bb0edc08adb9ef6618e30621`.

## Exact source changes

1. Private allocationBill at1082 adds default-false `singleWrapSlot`.
   Its sole changed reserve expression at1109 is the slot multiplier:
   `wrapOnly ? (singleWrapSlot ? 1 : d.capacity) : 3 * d.capacity`.
   The retainedDevelopment zero arm and ENTIRE per-slot body are unchanged.
2. sweepBill at1319 prepays and initializes that boolean false. Its existing
   wrapOnly proof, paid Post/heldPost walks, successful capacity condition,
   attempts assignment and fallback retry formulas remain.
3. Only INSIDE the successful silent/no-external/sufficient-capacity arm, the
   new prepaid guards certify one production and one workflow; matching ID;
   Shooting phase; nonnull completed task; and exactly one soundstage plus one
   set-scenery reservation, allowing either input order. Any failed guard leaves
   the flag false. No new Error, refusal, unsupported cut or owner call exists.
4. The original `enter` calculator declaration moves from immediately after
   `update` to immediately after the wrap proof, at1362. Its only expression
   change is passing the private flag. The new8-unit caller payment precedes
   its evaluation, including the allocation selector. No other bill is moved,
   removed, replaced or retuned.

Existing singleEarly/restricted early returns remain before these new blocks.
All public types, outputs, commands, owners, owner order, per-slot coefficient,
other allocator terms, literal/copy prices, primitive prices, dimensions,
canonical identities, ledger, caps, timeouts, tests, validators and versions
remain unchanged. No broad alternative wrap tariff or new cache was added.

## Actual added prepayment inventory

The407 finite block payments precede their exact source operations:

| Block | Units | Actual source operations covered |
| --- | ---: | --- |
| Flag initialization |2| Boolean value/local binding before wrap proof. |
| Count guards |16| d.n, productions.length and operations.workflows.length reads; all three equality tests, literals, short-circuit and conditional controls. Exactly-one production is explicit, not merely inferred from d.n. |
| Current record references |12| Both indexed accesses to current productions/workflows and their local bindings. |
| Task/reservation references |12| Both workflow field reads and their local bindings. |
| Presence/count guards |16| Task null comparison, reservations length/equality, short-circuit and conditional controls. |
| Reservation references |8| Two indexed references and their local bindings. |
| Exact identity/shape predicate |64| Argument/property reads, up to seven comparison dispatches, conjunction/disjunction control and local flag assignment. Each reached Work.equal separately prepays1+both actual string spans. |
| Selector/argument controls |8| Flag read/passing/binding and the extra nested slot-selector condition/branch. Paid at the caller BEFORE evaluating enter/allocationBill. |

Thus the full guard path adds138 finite units plus the actually reached
Work.equal spans. There is no uncharged added walk, object literal, copied array,
callback, serialization or owner invocation. Short-circuited guards do not
execute later blocks; each block remains paid before any of its operations.

The exact predicate compares workflow productionId to current production ID,
workflow phase to `shooting`, task status to `completed`, then the two possible
capability orders. Those are the only added string comparisons. The upper of
seven is conservative; only reached calls charge their actual strings.

The existing calculator receivers, saturating sums/products and generic192-unit
blocks remain. The slot multiplier's surrounding calc32 still executes before
its argument arithmetic; the new8 payment is additional rather than a claim
that its added argument/selector work is free. The default-false private
parameter retains the general result when no certificate is supplied. The one
current caller was inspected and always supplies the already-paid flag.

If wrapOnly is false, the added initialization/selector blocks cost10 and the
returned owner reserve is unchanged. If the successful wrap-capacity arm is not
taken, the shape guards do not execute. If any new shape guard fails, the old
capacity multiplier and unchanged reserve remain. A cost cut during proof or
the reordered calculator cannot execute an unprepaid owner: the actual whole
advance still occurs only after the completed frame reserve is accepted.

## Narrow owner-law proof

The existing wrapOnly loop establishes older-than-current-week, remaining4 for
every supplied production. The new counts and ID equality select the exact sole
current Shooting workflow. Its completed task/two-capability guards exclude
already-released Post waiters and other shapes without denying them the existing
general execution path.

Actual productionPhases requirements for Shooting are soundstage/set-scenery;
Post requires only post, so retainedCapabilitiesFor yields none. Actual
releaseCompletedPhase therefore removes both reservations and the task before
allocation. deriveBindings clears stage occupancy. A historical Set ID is not
a claim: resourceClaims emits bound-Set exclusivity only while a stage reservation
exists. With one workflow and zero external slots, allocation's physical occupied
set is empty. Its raw producer/setup passes still run and remain billed.

The previously established allSilent condition means actual policy filtering
admits all facilities. The full array is still copied, filtered, policy-tested
and sorted. Non-Post and zero-capacity rows may all be visited; those terms are
unchanged. Positive Post capacity ensures a first positive Post row. Its slot0
is free, satisfies Post's sole requirement and ends the slot search after one
body. This is proved from pre-call facts, not the actual owner's later output.

The full original per-slot key construction, lookup/insertion allowance,
reservation/result work and all other terms remain. Release, Set wear, copies,
transitions, workflow updates, policy callbacks, ordering/maps, outer scans,
drain/reconcile, public trace construction and later kernel work are not removed.
The existing success proof still supplies attempts1; rounds/visits stay at their
general one-picture3, not a newly narrowed2.

Sound selection, external occupancy, multiple pictures/workflows, unavailable
Post, mismatched shape or already-released reservations cannot set the flag.
No new validator or gameplay eligibility restriction is introduced.

## Paper reserve delta and verification limit

At406's reported dimensions d=28, capacity8 and zero occupied rows, the unchanged
slot body is `5 + 106 + 2*273 = 657`. Its multiplier8→1 removes4599 from the owner
reserve, giving the paper16951→12352 while every other term is unchanged.
Selector/calculator expenditure above is additional. This is source arithmetic,
NOT an observed successful call, end-to-end saving or budget-fit certificate.

Actual406 stopped at used184803/request17048 before the wrap owner; it was1851
short merely of that payment, with drain/output/kernel still ahead. Neither that
measurement nor this paper delta establishes full Ready/stale-route usefulness.

Static review read full407 and the complete changed allocation/sweep blocks,
checked the sole allocationBill caller, matched every prescribed payment, kept
all guard accesses below their counts/null checks, and verified that only enter
construction moved. No known signature/narrowing issue remains. Hashing completed
with the existing host locale warning. Parent411 must review actual source and
412–419 must run the fixed-source matrix and preserve any remaining failure.

SOURCE FROZEN; production ownership yielded. This is not whole-owner accounting
acceptance, Ready/B4 completion, live activation, Unity or Owner acceptance.
