# 486 — Admission key-domain width correction: measured hypothesis

2026-09-20. Published base1baf982520248d15731950d5bda48a6e93bba5ea;
replay466 SHA956a0245872f02e625c93a41cc2cc2033c4279129b85e100f315045d4d4f8ca4.
Original first-take/stale tests remain RED482/483. No implementation adopted
until the exact current width probe and bounded design review are complete.

484 measured admission direct20283 and calculator12702 successful units.
Native sim-core traced at least11342 direct units in the documented six-role,
five-engaged, eleven-contract shape to staffing/busy/employment reserves.
The following is a consumer-domain hypothesis, not a reduction of unit prices.

## Proposed minimal change

In admitReady only, initialize existing idWidth to0 instead of22. Preserve
the current separately paid package-ID discovery, busy-ID widening, contract-ID
widening and market-ID widening, including arbitrary long IDs and duplicates.
Keep progressively widened maxima; no new cache/table or extra validation rule.

Retain the exact original numeric/diagnostic floor in ONLY three error-span
terms by replacing:
- staffing idWidth*4 with Math.max(22,idWidth)*4;
- busy assertion's standalone idWidth with Math.max(22,idWidth);
- employment facts' standalone idWidth with Math.max(22,idWidth).

Add explicit work.pay(16) before each affected outer reserve. Existing calculator
receivers, all expressions/owners and per-operation tariffs remain. Each new
max expression has at most8 scalar operand/access/invocation nodes at2 units.
No default-parameter computation, new local cache, backward refund or cap change.

Actual compared keys are talent IDs: staffing's cast Set and role Map; busy Set
and queries; isContracted's talentId comparison and contracted Set; freelancer
Set and queries. Every stored/query key is included in the appropriate existing
paid maximum. Role labels and craft indices are VALUES/error text, not these
keys. Retained diagnostic floors protect their original allowance.
readyProductionId's independent22 numeric-conversion bound stays unchanged.

For IDs of length8 and the stated shape, unchanged algebra predicts5698 fewer
owner reserve units, minus48 new calculation units =5650 net. This is conditional
paper arithmetic, NOT measured saving or combined-route fit.

## Confirmation / execution ownership

Sole sim-core TEMPORARY writer for487 passive scalar probes in replay only;
handback487-admission-width-probe-handback.md. No production optimization yet.
Capture original width scalars, actual key lengths and relevant counts at
staffing/busy/contracts/market; retain all existing payment expressions/order,
actual owners, source data, limits and tests. No stack per payment or global
observer framework. Freeze/hash, parent complete diff review before runtime.

Parent runs488 first-take then489 stale using record-check, no source/HEAD
change while either runs. Read all raw/JSON, check exact hashes/nonoverlap,
original outcomes and expected scalar premise. Remove every diagnostic line
with apply_patch and verify exact466/protected-clean before candidate release.
Native contract-auditor separately checks the actual consumed domains and
diagnostic floors. Existing required RED tests are preserved, not replaced.

## Separate calculator candidate — NOT adopted

Read-only auditor found smallTransitionBill algebra:
cap(20+58s+110p+E*emitted +(2s+4p)*d.d), with s=before+after,
p=before*after, E=unchanged reservationEvent+stampedEvent+APPEND+5 (currently52).
All private call counts0..2 imply base<=796/slope<=24. A guarded fixed-arity
calculation might reduce execution work while returning the exact same bill;
source tariff, actual frequency and numeric equivalence still need verification.
Estimated five calls/roughly1100 saving is paper-only, not established by480.
Do not mix this into the key-width candidate. No calculator change authorized.

Max two native specialists; one production writer; heavy runtime serialized.
No Claude, installation, whole-suite repeat, weaker validator/test or increased
200000 work limit. B4 and later roadmap continue; no Ready/liveP2 acceptance.

## Independent prospective review — qualified KEEP

Native contract-auditor checked all affected consumers against actual
productionAdmission/employment owners. Every key universe is covered by the
existing paid scans; role/index strings are values/diagnostics, not keys.
activeContract compares talent IDs and numeric dates, with no omitted status
string. Separate full-catalog searchBill remains unchanged. Retaining the
three max(22,current idWidth) diagnostic terms preserves original error spans.
New calculation work must be prepaid. No independent numeric-saving or fit
claim. Native sim-core separately agrees; recommends explicit16 per max.
Test-author is assessing existing long-ID/refusal controls read-only while
487 sole sim-core temporary probes are authored. No candidate runtime yet.
