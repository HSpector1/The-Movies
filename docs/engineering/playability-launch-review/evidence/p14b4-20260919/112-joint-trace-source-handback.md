# 112 — detached joint-trace source handback

2026-09-20. Candidate implemented; SOURCE FROZEN for parent-owned verification.
Published authorization/RED checkpoint: `956a17fe391892d65a8ae10bf9ce1403750bbd34`.
Only production write: `src/core/promiseCapacityKernel.ts`.

## Governing scope

Implemented frozen91 plus controlling94, through the independently reviewed96
factoring design; preserve original49/56 optional semantics. Read the installed
independent95/100 tests after release, including107's sole approved ordering
delta, and the recorded104 missing-entry/tag-guard RED. Native sim-core role
instructions were applied without invoking Claude or delegating.

The kernel file is1047 lines. Frozen SHA-256:

`1faa6fcac443dac046bd97fa3e27c61bada80c7297f8e69ea4ebcff76d899332`

## Implemented source map

- Public trace types and separate `searchPromiseCapacityTraces` entry exactly
  distinguish compulsory joint owner traces from optional49 alternatives. Old
  entry rejects explicit trace mode/path tags; the new entry rejects mixed-mode
  inputs and background picture fields, including fields present with undefined.
- `normalizeBase` and `pictureValue` share genuine common input/calendar checks.
  Nullable actual release is confined to explicit trace pictures; no fake release,
  cast, production or owner record is synthesized. `normalizeTraces` checks exact
  identities, one physical path per trace, variant consistency, occurrence IDs,
  provenance, per-trace additions, fixed-prefix replacement authority and coverage.
- `validateTraces` constructs every complete compulsory ledger before any credit
  search, including null/background work and uncredited pictures. Conflicts are
  input errors, not gameplay impossibility. Replacements retain original fixed
  identity/prefix/other-owner holds. Conditional additional IDs may repeat only
  across exclusive ledgers. Unknown-release pictures require continuously joined,
  exact-path cast-person occupancy from max(now,greenlight) to the effective H;
  adjacent segments compose, gaps/tails do not. No supplied hold is repaired.
- `prepareCommon` creates demands and global existing boundary cuts once.
  `prepareModel` reuses the existing actual-seat matcher preparation for each
  exclusive trace. `prepareDomain` preserves one shared domain and deduplicates
  physical paths across all variants for the optimistic global prior bound.
- The original iterative `search` remains the single credit matcher. Optional
  ledgers retain their immutable-prefix early checks and exact selected-suffix
  validation. Trace models use their independently validated compulsory ledger;
  declining a credit does not remove execution or occupancy.
- `searchDomain` searches all needed exclusive branches with the same budget,
  demands, cut indices and protected profile. `classifyDomain` is the one common
  prior-optimum/protected-X/B/unoptimized-X orchestration for both public entries.
  No public-per-trace calls, fresh budgets, copied solver or combined classifications.
  Every equivalent optimal trace/assignment remains available in subsequent probes.
- `traceWitness` separates full executed paths/effective holds/owner provenance
  from exactly credited picture occurrences and actual-seat credits. B, first X
  existing paths and eight-week slack must belong to that same trace assignment.

## Bounds and proof limits

One shared counter includes preparation, canonical structural normalization,
all trace ledgers, eligibility, global upper/profile comparisons, all searches
and returned proof construction. Caps remain32 claims,64 units including B,
1024 trace headers plus path occurrences,200000 work and220 span. Demands are
counted once, not per trace. Large safe-integer counts/windows encounter bounded
size arithmetic before any derived count expansion; no per-week/count loop exists.
The effective H includes raw horizon, target due and local-prior due, not original
foreign source due. Retained foreign debit normalization is unchanged49/56.

Complete negative results require exhausted globally complete domains. A partial
domain may certify only a lawful B witness plus proved global prior optimum and
complete claims/holds. The physical-path-deduplicated upper profile must saturate
every component for the incomplete-future shortcut with nonzero priors; zero-prior
optimum remains identically zero. Work/size exhaustion never invents a maximum or
IMPOSSIBLE. Trace ledger validation is budgeted and completed before even a trace
ALREADY_MET return; old optional ALREADY_MET fast-path order remains unchanged.

This lower module validates structural certificates, not actual engine execution
behind provenance strings. Owner-domain enumeration/replay, truthful coverage and
before-work preparation accounting still belong to the future owner adapter.
No claim is made that ordinary engine domains have already been constructed or
measured. No live caller, version, save validator, P2 activation, D3 source, schema,
owner adapter, test, fixture or config was changed.

## Verification handoff

Performed bounded source rereads and whitespace-pattern inspection only. No test,
engine probe, typecheck, generator, Git, network or descendant execution occurred.
No runtime pass is claimed for this candidate. Canonical Git diff identity remains
parent-owned under the writer's Git prohibition.

Next: parent runs immutable95/100 plus original46 optional cases, then both strict
typecheck selections and independent bounded review. Preserve actual diagnostics
before any correction. In particular verify whole-result/work permutation equality,
full-ledger negatives after an otherwise usable trace, global equivalent optima,
unknown-release tails/gaps, exact row caps and original selected-release behavior.
No known mechanical blocker remains from the bounded static pass; real execution
and typing verification are still owed, not implied by this handback.
