# 160 — independent bounded-sort tests: frozen inert handback

2026-09-20. Native Fable test-author. INERT / UNEXECUTED: no test, probe,
typecheck, source implementation, Git, network or delegation was performed.
Only these three new160 documentation/draft files were written. Parent owns
installation, actual old-owner baseline, actual helper RED and later verification.

Read complete157 (SHA256
`2e55e5592a2f43f4fd6be1bdeebc80972e83462cfbe717aa7c3495bfba1fd44d`),
complete137/148, frozen158, and the existing relevant owner/fixture bodies.
Parent adopted157 §§1–3 and reported159 KEEP. No new helper implementation was
inspected. The exact API, fresh-copy semantics, native array-length domain and
size-only cost query now replace158's deliberately unresolved interface items.

## Frozen files and installation targets

| Inert file here | Intended executable path | SHA256 | Cases |
|---|---|---|---|
| `160-bounded-stable-sort.test.ts.draft` | `tests/p14b4-bounded-stable-sort.test.ts` | `233e06430e02ef824e15ae0d66c52e8c4c8b23110a20f36aabb6d1dac4625a38` | 7 |
| `160-bounded-sort-owners.test.ts.draft` | `tests/p14b4-bounded-sort-owners.test.ts` | `adf08c43d003fe2559af36035210f1a9c511b30298a771b0a93f6780384ba675` | 7 |

The132-line helper draft imports the genuinely absent new module. Missing-module
RED is expected, NOT claimed executed or claimed to reach its seven bodies.
The258-line owner draft imports ONLY existing exports, never the new helper;
parent can independently execute its old-owner baseline first. Both use default
timeouts, no mocks, skips/todos, unsafe casts or fabricated giant arrays.

Read-only owner-byte identities at drafting: operations.ts
`80eb5d0cfb931938877d181c44a001a8f706dcade992418878fdbf95d532d7f7`;
releaseAuthority.ts
`8739373f159608b9bb9a8869574fb13401254d03ceef52c28ca07448aa31db24`.
Parent's161 baseline is separate existing-file evidence, not execution of160.

## Coverage and independent expectations

- 158 rows1/2: fresh copies including empty/singleton/already-sorted inputs;
  zero comparator calls for0/1; complete inferred generic record types; nested
  markers/original references including duplicates; stable zero/signed-zero/NaN
  ties. NaN appears ONLY as final comparator equality for coherent equal ranks,
  or an all-equal comparator. Infinity has ordinary sign. Each equal-rank input
  permutation keeps ITS tie order; no global tie-break is invented.
- Rows3/4: real public sweep-order function on detached narrow clocks, independently
  preserved old native comparator AND literal orders. Wait4 beats wait2; numeric
  suffix2 beats10; leading zeros remain raw lexical ties; numeric major above
  MAX_SAFE_INTEGER follows a fallback row;310-digit majors both parse Infinity
  and their falsy subtraction must continue to suffix/lexical comparison.
  Detached duplicate-ID/reference probes are not claimed valid active campaigns.
- Row5: real lower-owner append over three insertion permutations, exact literal
  namespace/week/lexical order, input purity and old-row identity. Explicitly
  detached duplicate append pins helper stability, NOT validator admission.
  A separate real greenlight→scheduled take→Release Ready→actual commit route
  proves lawful action reach and unchanged week/cash/RNG/ledger/production facts.
- Row6: real historical-control founded studio, managed operations and legacy
  script development, actual greenlight and two ticks to remaining7. Two isolated
  lower-owner branches retain the SAME real production, Sets and full context;
  only the facility input array is reversed. Transparent policy wrapper calls the
  actual technology owner, observing exactly one pre-sort callback per facility
  in input order. Literal first-fit stage07/slot0 and real bound Set are required.
  Complete returned operations/productions/Sets/take/release outputs, events and
  technology agree after normalizing ONLY the deliberately permuted facility
  array; that exact branch input array/reference is independently preserved.
- Row7: exact existing sticky-retention recipe, real Annex placement/completion,
  real Ready script, two other actual writing reservations occupying base,
  real Annex greenlight, real due release, actual8→7. Exact held facility/slot
  survives while base is free. The fixture explicitly uses historical-control
  founding and80m ledger-balanced bootstrap funding; no natural-economy claim.
  Existing separate Set/stage-order regression suites are unchanged.
- Row8:14 small lengths across odd/power-of-two boundaries, ascending/reverse/
  alternating/deterministically shuffled/all-equal inputs. Independent native
  output oracle plus literal cases; transparent comparator count compared with
  independently calculated `n * ceil(log2(max(1,n)))`. Test-only permutation
  arithmetic never touches campaign RNG. Correct outputs accompany every bound.
- Row9: repeated long-ID semantics are covered. Exactly-once ordinal preparation,
  shared REAL owner call sites, no hidden native sort, slot-write/pass proof and
  identifier/property/callback charges still require source review. Tests neither
  impersonate allocator owners nor claim comparator counting observes all work.
- Row10: adopted API is a length-only bound query, NOT a budget/pre-call gate.
  Exact small/max-domain literals, finite safe products, RangeError invalid inputs.
  Queries at2^31 and2^32−1 allocate no test array. There is no invented ceiling,
  saturation argument, owner-call refusal or complete-replay-cost assertion.

## Reach limits and next action

All new fixtures/guards are UNEXECUTED, including actual branch callback count,
stage07 selection and the default-timeout Annex/Release Ready paths. They reuse
existing lawful owner routes; a failed prerequisite must be recorded and
investigated, never worked around by fabricated history or a relaxed expectation.
The release route uses real lower-owner shooting director/load-in/scheduling
operations, as existing77, and real ticks; it is not a new command/UI test.

The lawful-comparator caveat is essential: no claim about host ordering for
inconsistent malformed-clock comparators, sparse arrays or accessors/proxies.
No validators change, no Set sort is introduced, no whole replay proof closes.
H==now and current-branch stale-command rules remain adopted but outside this
sort prerequisite. Parent next: review these frozen bytes, install exact files,
run owner baseline separately and helper missing-module RED, preserve results,
then release the narrow three-production-file writer only after the gates.
