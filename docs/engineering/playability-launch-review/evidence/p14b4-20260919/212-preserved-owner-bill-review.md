# 212 — PARTIAL / REFINE: preserved191 owner bills

2026-09-20. Native independent contract-auditor report persisted by parent.
No files changed, runtime executed or mutable207 inspected by reviewer. Applies
ONLY to191 in197-started-replay-typecheck.patch SHA256
40b27b819d98693c90b53157db2f6402a90298dee0523b2fca0e68ee0cb3d2ed.
Parent adopts concrete findings and qualifications, not acceptance of pending207.

## Concrete owner-side undercharges

1. Commitment pruning: replay1911054–1055 calls releaseAuthority136–141 after
   5+commitments.length*(4+keyBill(released.length,d.dp)). One actual committed
   release costs K+9, where K=keyBill(1,d.dp). Owner performs membership query
   AND constructs {commitments}, literal-only13, before filter allocation/control.
   Local reservation is short. Genuine200 single-picture release, reached205,
   constructs this without altered roots. Explicitly pay result literal,
   filter allocation/control/output and full membership key charge; zero-release
   early-return may be separately bounded.
2. Technology factory: replay191994–1001/technologyProduction57–95. Old60 pays
   caller facts+nested tick literal45, owner outer{technology,policy}19 and
   inner{allowsFacility,beforePhaseEntered}35. Total99 BEFORE closures/call/local
   work. Correcting only caller literals leaves54 owner units unpaid. Reconcile
   both caller and owner returns in207; no owner rewrite/prerequisite requested.

Both are source-accounting defects, not demonstrated gameplay-output errors.

## Variable work checked — inventory/count bounds MET

Not blanket certification of every fixed coefficient:

- Geometry includes both facility-body queries, structure/provided-ID searches,
  placement filters/cell walks. Arrival counts potential blocked workflows before
  callbacks, replacement maps and full measured task/workflow/operations copies.
- Allocation includes eager raw claims BEFORE filters, shooting-task rows and
  second bound-Set pass. This particular occupancy call supplies no research/
  placement roots, so no general-world traversals of those absent roots required.
- All4 other=n-1 remains justified: transitioning row clears old reservations/
  task/stage before occupancy. Other rows' raw claims still counted.
- Facility policies run before sort. Selection covers technology rows+atmostN
  locks; sound adoption/install scans represented; setup adds access/equipment.
- Set acquisition covers held discovery, candidate filter and associative checks;
  retained/new bindings distinct. Original genre callback's linear picture lookup
  represented.
- General N(2N+1) visits consistent with at most one advance and one release-
  progress contribution per picture. All4 retry bound assumes no Post release.
- Commands include current workflow, refusal strings, real assign/schedule/clear,
  release legality's production/commitment/title/concept scans and append/sort.
- Set wear/novelty covers some then map and measured full Set copy; no unnecessary
  traversal of opaque nested values.

No omitted WHOLE adoption/setup/installation/Set-binding traversal or demonstrated
wrong mixed-phase iteration bound found in this bounded inspection.

## Remaining qualification

Inventory does NOT certify every constant, callback block, repeated string or
construction coefficient. Two definite undercharges require correction; runtime
headroom/passing counter assertions cannot replace before-call composition.
Review207 against preserved findings only afterfreeze, then unchanged usefulH6,
shared-budget/threshold controls must still pass SAME200000. No liveP2, complete
choice domain, wholeB4 or native/Owner acceptance.

Parent relayed both exact findings to207 writer while mutable. Writer reports
explicit separate owner factory schemas19/35 and prune literal13+filter work
saved. That report is NOT independently inspected code or executed proof yet.
