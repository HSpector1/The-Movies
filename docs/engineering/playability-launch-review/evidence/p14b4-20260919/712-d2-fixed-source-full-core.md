# 712 — the fixed-source full core on the D2 candidate: every pre-registered prediction held

Run `712-d2-full-core` discharges the obligation record 706 left open under
"What this run does not establish", item 1. Record 709 pre-registered its outcome
BEFORE it started. This record compares the two.

## Identity and qualification

| field | at start | at end |
| --- | --- | --- |
| `sourceSha` | `fc37bd27efd43fdd171374a1f44aedf525b050db` | same |
| `testedDiffSha256` | `e3b0c442…` (empty diff) | same |
| `untrackedSource` | `[]` | `[]` |

`"fixedSource": true`. The tree did not move in either direction for the full 52
minutes. `end` and `exitCode` are both present, so this is not the truncated shape
run 701 left behind when ENOSPC killed it. Start `2026-09-22T22:17:50.155Z`, end
`23:10:24.347Z`, 3154.19s wall, `exitCode` 1.

This is the first `fixedSource: true` full core the B.6 candidate has had. 703 was
qualified (`fixedSource: false`) because the D1 prose edit landed mid-flight.

## The prediction, and what the run returned

Record 709 registered these five cells before the run. All five match.

| cell | predicted (709) | observed (712) |
| --- | --- | --- |
| test files | 9 failed / 343 passed (352) | 9 failed / 343 passed (352) |
| cases | 24 failed / 3971 passed / 8 todo (4003) | 24 failed / 3971 passed / 8 todo (4003) |
| `exitCode` | 1 | 1 |
| `fixedSource` | true | true |
| new failures | none | none |

A matching prediction is weaker evidence than it looks, so the reconciliation below
is carried to the individual case rather than stopped at the totals.

## Counts reconciled against both prior runs

| | 695 (baseline) | 703 (qualified) | 712 |
| --- | --- | --- | --- |
| test files | 9 failed / 341 passed (350) | 11 failed / 340 passed (351) | 9 failed / 343 passed (352) |
| cases | 24 F / 3944 P / 8 todo (3976) | 26 F / 3967 P / 8 todo (4001) | 24 F / 3971 P / 8 todo (4003) |
| wall | 3126.84s | 3368.92s | 3152.71s |

703 to 712, exactly: one further FILE (`tests/bridge-p14b6-d2-withheld-employment-claim.test.ts`,
the independently authored D2 RED, 351 tracked to 352) and two further CASES (its Case A
and Case B). Passed moves 3967 to 3971, which is those 2 new cases plus the 2 failures
703 carried that are now green. Failed moves 26 to 24 for the same two. Nothing is
unaccounted for.

695 to 712: +2 files and +27 cases. The 27 are the +25 attributed in record 706 (24
authored B.6 cases plus the one GENERATED by `it.each` over the prior-id Map at
`tests/bridge-runtime-checkpoint.test.ts:758`) plus the 2 D2 cases. Passed rises by the
same 27. Failed and todo are unchanged.

## Failure identities: 24 shared, none new, none silently turned green

Comparing the full `FAIL` header text of 695 and 712: 24 shared, 0 present only in 695,
0 present only in 712.

Headers alone would not settle this, because a test can keep its name and fail for a new
reason. So each header was paired with its own next `Error:` line (vitest stacks repeated
failures under one trailing error, so the pairing walks the file in order) and the two
sets were diffed. One cell differs, in
`tests/world-first-scenery-load-in-provenance.test.ts`: the temp directory name inside the
`execFileSync` command string, `studio-scenery-export-hB9RM1` against
`studio-scenery-export-LUVuEP`. That is `mkdtempSync`'s own random suffix. The failure
under it is the same `ModuleNotFoundError: No module named 'PIL'` in both runs. Every
other cause string matches byte for byte.

Failing files, unchanged from 695: `bridge-p12-campaign-library` (11),
`bridge-p13-campaign-isolation` (1), `p13a-scientist-foundation` (3),
`p14b4-cast-class-capacity-evaluator5` (1), `p14b4-ready-replay-stale-target` (1),
`r3n1-stale-schedule-take-02` / `-02p31` / `-02p32` (2 each),
`world-first-scenery-load-in-provenance` (1).

## The three falsifications record 709 named, and what each would have meant

None fired.

1. **CF-09 failing again** would have meant the contract-manifest regeneration at
   `324f2b0c` did not settle the `generatorSourceSha256` move. It passes:
   `tests/bridge-contract-consumer-lock.test.ts` is green across all its CF-09 blocks.
2. **The prepared-reuse timeout returning** would have required the investigation record
   706 now specifies in place of the withdrawn default classification.
   `tests/bridge-runtime-checkpoint-prepared-reuse.test.ts` passed all 21 of its cases,
   in 193,099ms. The case still runs heavy; under this run's conditions it finished.
3. **Any other new failure** would have meant the D2 fix is not as bounded as its
   +12/-1 diff suggests. None appeared.

## The D2 RED is green, and the control held

`tests/bridge-p14b6-d2-withheld-employment-claim.test.ts` collected and passed both
cases in 580ms. Case A (RED) now reads the honest degraded line on a world with no
`hollywood` root. Case B (CONTROL) still reads the original `WITHHELD_LINE`, which is
what stops the fix from having been over-broadened to "any empty roster". The control
passing is the load-bearing half.

## A second reading of 703's slowdown

Not predicted, and worth recording because it bears on an earlier attribution. 703 ran
7.7% longer than 695 (tests 9195.70s against 8521.74s). 712 carries 27 MORE cases than
695 and ran 0.8% longer (tests 8585.55s against 8521.74s). The cost the B.6 read models
add is inside that 0.8%; 703's extra time was not the candidate. This corroborates the
contention account of 703 (the orphaned 535 MB worker left by run 701, and a data volume
at 97%) from a direction independent of the process evidence gathered at the time.

It does not retire the separately recorded per-talent projection cost in record 711.
That cost is real and unoptimized; this only bounds it below what 703's duration
suggested.

## What this run establishes, and what it does not

ESTABLISHES: the B.6 candidate with D1 and D2 landed introduces no core failure, resolves
the two that 703 carried, and leaves the 695 failure set identical in name and in cause.

DOES NOT ESTABLISH:
1. The UI suite. Run `713-d2-full-ui` was in flight against the 673 baseline as this was
   written. It has since returned and FALSIFIED record 709's UI prediction. Record 713 holds
   the result, the investigation and the attribution; the short form is that the unchanged
   baseline source, re-measured today, moves further than the candidate does and lands worse
   than it, so the movement is not attributable to this slice. The UI suite is not currently
   a usable regression baseline.
2. That the 24 remaining failures are acceptable. They are the inherited baseline, carried
   forward unexamined by this slice, not a clean suite.
3. Anything about the neighboring false-empty defect. That correction is the next item and
   has not been written.
