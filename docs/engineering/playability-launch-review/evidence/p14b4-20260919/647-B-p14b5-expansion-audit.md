# 647-B — Contract-auditor read-only audit of the P14B.5 expansion draft (647-A)

Scope: `/private/tmp/claude-501/-Users-zacheryspector-The-Movies-headless-program/6ed44184-f9ac-40ba-bf1b-321fc7506549/scratchpad/647-A-p14b5-expansion-draft.md` against HEAD `d6c11b9b` (worktree `/Users/zacheryspector/The-Movies-headless-program`). Read/Glob/Grep only; nothing ran. 646-T's untracked minter files were not read (fixture NAMES under `tests/fixtures/p14/genuine-v30-pre-b5/` were listed for Q6 only). Result: PARTIAL-none / DONE for the audit itself; no changes made.

## Verdict (Q8)

**INSERT WITH DISPOSITIONS.** The draft is companion-faithful in its rules, lawful in its Owner footing, and its source citations check (24 `saveVersion === 30` sites, the closed enum, `line` reading `[0]/[1]`, the 35-id pin, the cancel seam). Three DEMONSTRATED defects (one changes the natural-chain prediction), seven REFINE items, and a set of RECORD-ONLY notes. Not Owner acceptance; the insertion is the parent's act, the RED is T1's.

### DEMONSTRATED (paper, cited)

**D1 — OPEN 7's "order-independent within the fixed-order settlement pass" is false under the predicate it cites, exactly at the churn weeks the draft names as the exposed set.** `finishHollywoodWeek` runs at the tail with `week = finalized.market.tick = W` and closes every row with `endWeekExclusive <= W` (`src/core/hollywoodTick.ts:332-343`: `endedWeek: week`, ordinal removed). A commit inside the same settlement pass writes a row with `terms.startWeek === week` (`src/core/talentMarket.ts:1069` drops any other start; `:1111` finds the committed row by `startWeek === week`). The settlement loop iterates `openCasesAt` in array order (`:1245-1248`). So at the synchronized churn (208/404) a rival issuer's "roster at W" under the `:640-643` form (`startWeek <= week`) is exactly the set of colleagues re-signed EARLIER in this pass: the first-settled case of a rival sees no friends, the last sees all of them. That is array-order dependence in a contested choice (companion §2.1.7 :102) and it widens the already-flagged Owner-side item (plan :495 A.3 T1 (i), `openCasesAt` order). Ruling: the D5 and `nemesisOnRoster` roster predicate is `terms.startWeek < W && (endedWeek === null || W < endedWeek)`, subject excluded. Consequence for the natural-chain paragraph: under the corrected predicate every counterpart row at 208/404 is closed at W, so the CHURN receipts are NOT exposed; the exposed class becomes off-cycle rows (player contracts, replacements, staggered rival rows). The draft's paper prediction ("those receipts are the exposed set") inverts; the test-author measures instead of assuming.

**D2 — OPEN 5 (i) rests on a wrong justification and pins a hypothesis as law.** The draft says (i) "is the pin that makes the four unreachable surfaces checkable". Enemies/Nemeses (hence `enemies here`, `nemesisOnRoster`) are unreachable in B.5 by the tier RULE the draft itself states (a conflict record is required; no conflict-record kind is minted), independent of any constant. (i) therefore buys no checkability; it only forecloses the landed failure/cancel drivers' companion-described effect (§5.4 :441 "strain, not enmity"; §5.3 :425 Strained = "recent negative driver without a conflict record") and leaves `pairChemistry.sign = −1` (§5.6 :476) with no producer. Family 4's "closeness never falls below `RELATIONSHIP_BASELINE` on any B.5-minted edge" converts a NUMERICAL HYPOTHESIS (§5 :395) into a RED-pinned invariant. Ruling under Q3 below.

**D3 — sweep and control inventories are incomplete.** (a) The seven-member array sites omit `tests/p14b4-cast-class-policy.test.ts:30-31` (asserted at `:52` via `publicPriorityOrder`); grep shows the six-member literals only at F1 `:86,91`, trust-chooser `:172-173`, bridge cast-class `:410-411` and policy `:30-31`. (b) The natural-chain inventory names `tests/helpers/p14b2-fixtures.ts` but not its consumers: `bridge-p14b2-trust` (6 uses), `p14b2-fixture-preconditions`, `p14b2-setup-wrap-regressions`, `p14b4-rival-seating-preference`, `p14b4-owner-adapter-first-slice`, `p14b4-owner-enumerator-slice`, `p14b4-replay-bill-reductions`, `p14b4-cast-class-capacity-evaluator5` (grep, 19 files / 25 hits). `poachFixture` (`p14b2-fixtures.ts:150-211`) pins a week-208 settled receipt's `reasons` byte-for-byte (`:200-201`) on a rival-subject case settled by the unproven tie order; it is a cached shared fixture, so it, not the D3 controlled case, is the most exposed item (memory rule: sweep shared-fixture consumers). Under D1's corrected predicate it likely does not move (roster empty at 208); it must still be pre-declared with its consumer set.

### REFINE

R1. Release-driver week stamp: (2a) stamps takes with the tail week (`tick.ts:1108` `finalized.market.tick`), (2b)/OPEN 10 say the film's `releaseTick` (= tail − 1; `tick.ts:192,623`; `hollywoodTick.ts:241,278`) is the driver week, while scope (3) sets `lastEventWeek = week` and scope (10) requires every driver `week ≤ lastEventWeek`. A same-pair take+release in one advance is blocked by the busy set (`decide()` `busy.has`, `assertGreenlightStaffingIdle`), so the regression is not reachable today, but the mixed convention is a validator fragility. Stamp every tail-minted driver with `state.market.tick`; `ref` = `productionId`; the `FilmResult` carries `releaseTick` for any read model.
R2. R-D5-SETTLE receipt facts assume two survivors ("exactly one survivor's roster"). The D5 sentence enters `reasons` only when the winner strictly beats EVERY other survivor on D5 (`talentMarket.ts:880-884`); with ≥3 survivors a lawful Copeland movement can carry no D5 sentence, and the receipt carries no bands. Restate: the reviewer recomputes bands at W; the sentence is required iff the winner's D5 band exceeds every other survivor's.
R3. Mint from the advance's delta (the take entries the tail already holds at `tick.ts:1104-1107`; this week's `releasedFilms`/`growth`) rather than scanning the roots for `week === tick` / `releaseTick === tick − 1` (§5.7 :487 "proportional to the week's shared-work events"; §8 :644). Idempotency keys stay.
R4. Family 10 (synthetic validator refusals) sits in the frozen-save file; B4 plan `:391-393` separates frozen save/migration from reader-admitted synthetic variants. Split it out.
R5. Tasks: T2 begins only after T1's RED is recorded (RED-before-implementation, plan :415); "T1/T2 overlap on disjoint files" reads as concurrent authoring. Sweep ownership: test-file pins → test-author (600 §3 step 5 "no test edits", §4); `src/`/`bridge/`/`ui/src/engine/adapter.ts` pins → the writer (B.1 `6948e31`, B.4 `2cbc3de5` precedents).
R6. Exclusion list additions (companion rules dropped without an entry): §5.5 :468 tier-transition permanence (only `peakTier` survives folding); §5.7 :486/:470 hot/cold index split; :441 "decays quickly" (per-driver decay; the draft has one drift horizon); the writer seat (:439; OPEN 13 — say "scope cut; `Production.writerId` is joinable at the tail, no V-step needed").
R7. Family 1's headless world: name the source (`tests/fixtures/p14/legacy-v28-shooting-5.json.gz`, `hollywood: null`, plan :523); it is not a T0 case. Family 6's construction note: under D1 a rival incumbent's roster at 208 is empty, so the case must use off-cycle rows or a staged (validator-admitted) edge on the player's roster.

## Q1 — Companion fidelity: MET WITH EVIDENCE, with R6 exclusions owed

Verified quote-by-quote: §5.1 :397-407, §5.2 :410-417, §5.3 :419-433, §5.4 :435-449, §5.4a :451-462, :463, §5.5 :465-470, §5.6 :472-482, §5.7 :484-489; §2.1.7 :102-120 (reservation :104; D5 row :116; both archetype orders :120 match the draft's seven-member arrays exactly); §8 :639-646; §4.5 :385-389 (no relationship input to trust, as the draft claims); §6.3 :527. Ladder tiers and drift-on-read (52/260, toward baseline from either side, never past) follow :467; driver set and signs follow :439-447 with casting competition (:443-444), awards (:446), romance (:448-449) excluded with reasons; D5 position and reservation follow :104/:116/:120; persistence bound follows :487-488. Nothing invented beyond the companion: `peakTier`/dates (:412), counters as fold target (:468), the accelerator cap (:442), `pairChemistry` with reasons (:482) all derive. Silently narrowed (list them): the four R6 items. The tier rule's reading of :425 (Strained as the fallback for a negative band without a conflict record) is a versioned interpretation; D2 covers its constants.

## Q2 — Owner authority: MET WITH EVIDENCE

S19–S23, S25 quoted correctly (`:576-582`); R8/R16/R17/R22 at `:596/:604/:605/:610`; Q2/Q3/Q4 verbatim at `:621-623` including "(recommended first behavior, not an approval)". The B.1 footing is real, but at plan `:518` (the OPEN paragraph "Q1 (no override) and Q3 (count-only) as recommendations, not approvals"), not `:514` (cite slip, inherited from 645-A). Each is carried with a named reversible mechanism (pass-through `driverGain`; a later labelled migration step with its own receipt; an additive greenlight/casting guard). No §7.3 question beyond Q2–Q4 and no §7.4 item is decided; Q1 untouched. 600 §1 D2 respected (evaluator, cap, metric, admission grammar untouched; the evaluator-5 designated case stays RED); §4 constraints restated in Tasks. R16's "nine tiers" wording versus OPEN 8 (ii): record as satisfied when the romance leaf lands; :431 "held alongside" supports (ii).

## Q3 — OPEN 5 ruling

Enemies/Nemeses (and `enemies here`, `nemesisOnRoster`) unreachable in a first slice is LAWFUL: S19 directs the whole model, not each slice; the B.1 pattern accepts enumerated members whose PRODUCER is deferred with a stated reason (WAIVED/VOIDED, plan :510 (6), :514; `tests/p14b1-promises.test.ts:845-850`), and the conflict driver is deferred on S25 (rivals run no `CastingSession`; `decide()` seats directly, `hollywoodTick.ts:160-175`). Strained is different: its producers LAND in B.5 (:441, :445) and (i) neuters them by constant choice (D2). Ruling: **(ii)** — `RELATIONSHIP_FAILURE_DELTA > RELATIONSHIP_PROXIMITY_LOW` so a release failure on a low-proximity pair (director–support, antagonist–support) can net negative and, from a staged Acquaintances pair or repeated flops, reach Strained and `sign = −1`; Enemies/Nemeses stay unreachable BY RULE and are pinned that way. The smallest lawful reachable negative from the draft's own list is the release-failure driver; the parent's "cancel driver on a shared pre-take picture" is not lawful (no take = no shared work, :445 "after its first take", S20). Replace family 4's baseline pin with "Enemies/Nemeses never read on a B.5-minted edge" plus "a staged Acquaintances pair reads Strained after one live flop".

## Q4 — Save/projection: MET WITH EVIDENCE (with D3(a))

V31 device consistent with the chain: `stripV29Roots` `save.ts:8583-8598`, `validateSaveV30` `:8679-8693`, `convertV30ToV29` `:8704-8710`, `migrateToV30` `:8714-8717`, the `projectPromisesPreV29`-before-envelope precedent `convertV29ToV28` `:8651-8662`, `convertV28ToV29`'s "nothing recomputed / Q3" comment `:8620-8628`, route lines `:8340/:8491/:8572/:8670`, 18 downgrade throws, dispatch `:5282-5284`, `LIVE_SAVE_VERSION` `:6396`. Empty root on lift and refusal on downgrade with any edge match the plan's preservation law (genuine outgoing fixtures at T0 before any source change, plan :414-415). Projection 48 is forced: enum closed at six (`bridge-schema.ts:2320-2324`) while `marketCaseProjection` publishes the engine order (`bridge/people.ts:977,987`); `line` reads `[0]/[1]` (`:991`) so it is value-stable; `DESCRIPTOR_LABEL` is `Record<string,string>` (`:866-875`), totality is convention, add the label anyway. Reason string: `settlementReasons: array(nonEmptyText())` (`bridge-schema.ts:2346`), engine `reasons` verbatim (`people.ts:998`), no enum — claim correct. Registry: `runtime-checkpoint.ts:59-63` head `projection-v46`; `tests/bridge-p14b4-runtime47-compatibility.test.ts:162-170` pins `toBe(47)`, `toBe(30)`, and `EXPECTED_PRIOR_IDS` (35 literals, `:39-75`) → 36 with `6f6b4880…` inserted in sorted position, title moved. Generated C# `string[] priorityOrder` (`StudioBridgeDtos.Generated.cs:6492-6493`), identity `:3`, manifest `:8`, generator-test literal `:562-565` confirmed. Sweep classes cover the memory rule (literals, sentinels `p14b1-save-v29.test.ts:196-201`, frozen builders/harness `_v14Contract.ts:411-417` and `historical-control.ts:23-46`, id rosters/size bounds) except D3(a); `bridge-p14b2-checkpoint.test.ts:44` `toBe(30)` is in the literal class.

## Q5 — Natural-chain exposure: PARTIAL (D1, D3(b), R2)

D5 touches the chain at ONE seam: `settleCase` → `chooseProposal` → `bandsFor` (`talentMarket.ts:1119-1153, 833-885, 779-816`) plus `survivesFreeze` for the unreachable drop (`:1057-1099`). Nothing in `decide()`/`staff()` (`hollywoodTick.ts:154-232`), `advanceManagedProductions`, the evaluator, `rivalProposalTrigger` (`:636-649`) or `authorRivalPromise` (`:1275`) reads an edge; `pairChemistry` has no consumer. Pre-first-movement, only settlement receipts and public-order VALUES can move; `rngState` cannot (no RNG). R-D5-CHAIN, R-ORDER-VALUE, R-VERSION are sound. R-D5-SETTLE needs D1 (which set is exposed) and R2 (how the receipt evidences it). Trust-chooser (`:305-364`: r04 subject, case settled first at 208) survives either predicate; `bridge-p13b-s8-rivals` and `p13a-rival-adoption` are in the inventory; the p14b2 fixture consumers are not (D3(b)).

## Q6 — RED families / T0 consumption: MET WITH EVIDENCE (R4, R7)

Twelve families each cite a scope item that cites a companion line; split frozen (9–10) vs synthetic (1–8) per B4 plan `:391-393`, with R4's correction. T0 consumption is within the parent's list: empty, bound-open-p1, tagged P2 (bound-open-p2-lead / -lead-or-antagonist), kept-and-broken, rival-current-p1, first-take-at-five, projection-47 checkpoint; family 5's player release/cancel and rival release advance first-take-at-five and the rival cases; family 6 is a staged construction; family 12 uses the standard seeds. No family needs an absent case. In-flight names differ slightly from the parent's list (`rival-current-p1-and-p2`, `legacy-count-only-p2-current-draft`); the published MANIFEST is authority; not read.

## Q7 — Tasks/ownership: MET WITH EVIDENCE (R5)

Writable/frozen per task stated; one production writer; test files never in the writer's patch; max two specialists; verification order and the 636/643 comparators check (`636-cancel-full-core.txt:2758-2759` 9 files / 24 failed / 3813 passed / 8 todo; `643-b4-ui-project.txt:5997-5999` 9 files / 28 failed / 2657 passed / 5 skipped / 1 error). No cap/tariff/timeout/refusal/fixture move; Unity deferred; no acceptance claim. `ui/` load-arm and pin edits have B.1/B.4 precedent (plan :525; 600-T3); 645 §3's "ui/ writes outside authorization" concerned UI fixes.

## Dispositions for the parent (OPEN 1–16)

1. Q2 base rate — stays OPEN; lawful footing; `driverGain` pass-through accepted.
2. Q3 no backfill — stays OPEN; lawful; empty root, live-only mint, validator boundary accepted.
3. Q4 warning not refusal — stays OPEN; lawful; no guard in B.5.
4. Scale — RULING (i): integer 0–100, baseline 50, named tier edges; hypotheses pinned by name.
5. Negative reachability — RULING (ii): failure delta above the lowest proximity weight; Strained and `sign −1` reachable via the release-failure driver; Enemies/Nemeses/`enemies here`/`nemesisOnRoster` unreachable by the conflict-record rule and pinned so; family 4's baseline pin replaced (D2, Q3).
6. Storage bound — RULING (i) fold-by-count; list :468 tier-transition permanence as a B.5 exclusion (R6).
7. Roster at W — RULING (i) with the predicate `startWeek < W && (endedWeek === null || W < endedWeek)`, subject excluded, shared by D5 and `nemesisOnRoster`; natural-chain paragraph re-stated per D1.
8. Partners — RULING (ii): eight-member friendship ladder; record R16's "nine tiers" as met when the romance leaf lands.
9. Success basis — RULING (i): `criticScore` (`types.ts:261`, always present).
10. Release seam — RULING (i) tail seam; all tail drivers stamped with `state.market.tick`; mint from the advance's delta (R1, R3).
11. D5 precedence — stays OPEN (unreachable); decided with the conflict driver.
12. Boundary — RULING (i): reuse `studioHistory.recordingStartedWeek` (`promises.ts:828, 954-960`); no per-root anchor (§8 :642's anchors are P14C's).
13. Writer seat — stays excluded; move to the EXCLUDED list with the corrected reason (R6).
14. (a) T2 comment edit at `talentMarket.ts:659-662`; (b) correct (`string[]`, not a C# enum); (c) correct.
15. Waiver field — stays OPEN, not bundled (auditor concurs: an unexercised exact-key member); Fable's call.
16. Constants — accepted as named hypotheses; none settled; the two candidate sentences and the label are copy hypotheses.
Auditor-added: A1 sweep sites gain `tests/p14b4-cast-class-policy.test.ts:30-31,52`; A2 the natural-chain inventory gains the eight `p14b2-fixtures` consumers with `poachFixture` `:150-211` pre-declared.

## RECORD-ONLY

Cite slips: plan `:514` → `:518`; `types.ts` FirstTakeReceipt `:2135-2142`, V29 roots `:2205-2210`, `GameState` `:2234`; `promises.ts` id predicates `:969-978`; `hollywoodTick.ts` seating `:164-170`. The chooser receipt carries no rules version (B.1 precedent); `RELATIONSHIP_RULES_VERSION` is code-only. `appendFirstTakes` returns before the root guard when `entries` is empty (`promises.ts:125-127`); copy that order. Records 648–650 and the in-flight `genuine-projection47-runtime/` exist at HEAD; the draft's "no record ≥ 646 read" stands for the draft, not for this audit's grep.

## Sources read

Draft (54 lines); companion `:95-124, :385-389, :390-494, :520-649`; P14 plan `:400-429, :494-579`; B4 plan `:384-415`; 600 (full); 645-A (full); 645 `:60-106`; `src/core/save.ts` (grep + `:8578-8722`), `types.ts` (`:253-276, :2115-2239`), `promises.ts` (`:84-149, :755-902, :927-1001`), `talentMarket.ts` (`:636-735, :740-885, :995-1284`), `actions.ts:560-600`, `tick.ts` (`:600-660, :1085-1111`, grep), `hollywoodTick.ts` (`:50-59, :150-299, :322-343`), `hollywoodTypes.ts:37-75`, `worldgen.ts`/`index.ts`/`reception.ts`/`hollywoodStartingData.ts` (grep), `historical-control.ts:15-54`, `bridge/schema/bridge-schema.ts` (`:2300-2346`, grep), `bridge/people.ts` (`:860-879, :970-1004`), `bridge/runtime-checkpoint.ts:20-69`, generated C# (grep), `package.json:11-15`, `scripts/generate-bridge-contract-fixtures.ts` (grep), tests: runtime47 `:36-184`, p14b2-fixtures `:150-219`, trust-chooser (grep, `:305-364`), policy `:26-55`, `_v14Contract.ts:405-422`, save-v29 `:190-202`, promises `:840-851`, plus greps over `tests/` for order literals, `priorityOrder`, registry pins, fixture consumers; 636/643 tallies.

## Evidence limits

Paper only; no test, typecheck, generator or probe ran. D1 is derived from source order and the fixed-order pass, not measured on a seed; whether any off-cycle case on the measured seeds actually reaches a Close Friends counterpart is unmeasured. The "same-pair take+release in one advance is unreachable" claim rests on the busy-set law, not a run. Rival picture cadence (how fast rival pairs reach Close Friends) is inferred from `RIVAL_TEAM_ROLES` (6 seats) and `decide()`'s one-picture-at-a-time rule, not measured. In-flight T0 artifacts were not opened. Whether the Owner has ruled on record 628 since 645 was not checked.