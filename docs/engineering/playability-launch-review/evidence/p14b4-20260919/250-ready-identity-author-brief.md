# 250 — genuine cancelled-production identity supplement

2026-09-20. FROZEN INERT draft, one test / 196 lines. No runtime, typecheck,
installation, Git/network or protected source/test edits; original244 untouched.
No promiseCapacityOwnerReplay implementation or pending production changes read.

Draft: `250-ready-identity.test-draft.ts`

SHA256: `41329acc809449b1122ac10940b73ccd241627d04314dc92d760c70bc8f7e9e3`.

Intended installation: `tests/p14b4-ready-replay-identity.test.ts`.
Authority: frozen240/241 and the existing same-week cancellation/identity law in
`tests/script-projects-actions.test.ts:289`. Current work continues the parent’s
qualifiedc643 predecessor and independently reviewed244; no new runtime/source
identity is claimed by this inert preparation.

## Concrete discriminant

Reuse the genuine242/244 base Ready route: named historical-control founding,
actual extra hires, fresh industry at actualweek0, managed operations/development,
real commission→tick→Review→accept. This is not ordinary natural founding.

At week1, actual greenlight mints the first ID, checked against the existing
public predictor. An exact production-cost ledger row is independently computed
from the actual payload budget. Actual cancel removes the live picture/workflow,
returns the real assessed project to Ready with null production link, discards
unfilmed technology, and changes neither week, sunk ledger nor cash. Strict
makeSave is retained throughout. No production, ID, receipt, clock, cash, history
or root context is stamped or deleted by the fixture.

The old identity is absent from active/released/workflow/take/commitment roots,
but remains in the real sunk ledger and the authoritative persisted-ID union.
Real same-week re-greenlight and predictProductionId must both produce
`${old.id}-1`, not old.id, preserving separate exact accounting groups. The
retained formation rows are explicitly Tier-W observations, NOT falsely labelled
wrapped/premiere Tier-D authority: the permanent collision witness here is the
ledger. This does not claim a history-only wrapped-cancellation variant.

The Ready replay must allocate that same distinct would-be identity, call one
whole-slate actual owner sweep, and match real action+tick local projection with
the new8 skip. It retains the immediate admission linker snapshot, current
screenplay physical path, six compulsory new company/resource holds, and no old
cancelled calendar. Source/old-action states are compared afterward; the owner
spy is transparent and restored in finally. Work remains<=200000 with the
default test timeout, no private tariff oracle or affordability certificate.

## Execution limits and handoff

Entire combined fixture/replay remains UNEXECUTED, including two actual paid
greenlights, strict current validators, foreign relevance joins, predicted
suffix and the measured cost. Guard failures must be retained as findings, not
repaired with histories or budget changes. No RED/GREEN is claimed.

This is one identity case only: no released/wrapped-only history collision,
collision-chain exhaustion, changed source week, queue policy, longer take or
release command, or full capacity certificate. It is additional coverage, NOT a
prerequisite to begin the already-authorized249 implementation after its RED.
Parent owns review, installation and the only serialized execution slot.
