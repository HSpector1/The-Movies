# 287 — single-picture early-transition bounds

2026-09-20. Native sim-core, sole production writer, bounded implementation.
Base: published/exact-remote `b1ad9f6bdacb34fb6ae04508e5633ae0de4c444b`.

SOURCE FROZEN; production write ownership yielded. Changed production file:
`src/core/promiseCapacityOwnerReplay.ts`, SHA-256
`290dc598921abdcef7a04093496d05274bd03ae8e7fa110fdbc362565392d955`.
The only other edited file is this handback. No runtime, probes, tests,
typechecks, Git, network or subagents. Static source/whitespace/conflict-marker
inspection only (no whitespace/conflict matches).

## Observations and scope

284 proves the old single-picture7 sweep requested159363, combined frame
payment159460 at used131259, BEFORE that actual sweep. Lowering that bill alone
does not prove the full remaining route fits200000. Later commands, frames,
wrap, ledger and output still cost work. Candidate usefulness is UNVERIFIED.

The known freelancer failure is unchanged and explicitly outside287. Neither
`hollywood.ts` nor any other actual owner changed. Original26 and Ready14,
including the genuine long-first-take test, remain immutable. No metric/cap/
timeout/default/assertion/version/validator changes or refunds were made.

## Selection and real execution

New private `singleEarlySweepBill` runs after the existing restricted-branch
test, before the generic retained-Development/general bill. It returns a bill
or null; null means the unchanged general calculation, NOT an unsupported cut.
The caller prepays20 before the helper arguments/return test.

All of these actual pre-call facts are required:

- Exactly one supplied picture and exactly one workflow; all actual selected
  technology records are silent (the existing measured `d.allSilent` fact).
- The picture is older than the current sweep, has remaining7 or6, and its exact
  workflow ID/phase agrees. No shooting task, exactly one reservation, and the
  existing requiresSetBinding flag is true.
- At7: actual Development reservation, no stage or Set binding.
- At6: actual soundstage reservation and nonnull Set binding; setup is absent
  or completed. Live setup work returns to the general bound.

Checks and string comparisons are prepaid before access. Capability capacity
discovery pays14 per facility visit, plus paid saturating additions. It gathers
stage count/capacity and Scenery capacity without simulating or allocating.
The full reservation-copy footprint is inspected with paid `copyCost` at6.

No allocation success is predicted. The same single existing
`advanceManagedProductions` call executes the entire supplied slate using fresh
real policy/setup collectors, original binding callback and full event sink.
All downstream observations, joins, ledger updates and projections are unchanged.

## Visit/attempt proof

7→6 releases its one Development reservation before asking for stage/Set.
Success settles the picture, restarts because of the release, then skips it.
Failure can cause one retry because of that release. The retry sees identical
stage/Set/external/technology facts: the Development reservation was already
absent for the first allocation; only the ignored blocker changed. Thus that
failure cannot become a success on retry. The retry releases nothing and stops.
Bound: two allocation attempts and two outer visits, at most one success.

6→5 retains its stage/Set and never releases a resource. Failure stops in that
round; success settles and causes one final skipped visit. Bound: one allocation
attempt and two outer visits, at most one success. Setup advancement is not
reached under the checked absent/completed setup predicate.

These arguments use the actual source ordering in operations1252–1442 and
1649–1835, not assumptions that the implementation skips its mandatory retry.

## Exact owner-block inventory

Notation: E(x)=1+2x; K(n,L)=1+L+nE(L); C=50+2d.d; L=d.d+26.
All arithmetic uses the existing prepaid saturating helpers. F=d.f, T=d.t,
S=d.sets, X=d.external. Copy footprints come from existing paid dimensions.

| Block | Prepaid bound / actual source reason |
| --- | --- |
| Raw occupancy | At7 raw claim count0 AFTER release; at6 count2 (retained stage reservation and occupied bound-Set row). Both workflow passes, empty research collection and stage-presence check remain. Per raw row: full claim literal, append,36 controls,2C key construction and2E(d.d) filtering. Own rows are emitted but BOTH excluded before the occupancy Map. No task exists. |
| External occupied Set | X times `4+K(X,L)` for complete copying/insertion; future allocation lookups use `K(X+2,L)`. No external row is discarded because the current owner is alone. |
| Facility preparation | All F facilities copied, filtered and sorted. Each actual silent policy callback reserves `12+E(14)+T*(8+2E(d.d))`; adoption/installation calls are unreachable under allSilent. Full existing `sortBill(F,6+2E(d.d))`. |
| Capacity search | At7, twice total stage capacity: the composite scan AND the general selected-stage scan. At6, only Scenery capacity; stage uses sticky retention. Each visited slot pays `5+C+2K(X+2,L)`. Nonmatching facility outer visits remain separately paid. |
| Requirement/retention |30 controls, three capability-Set operations `K(1,19)`, two full facility walks, two reservation capability comparisons, full retained reservation copy at6, phase override, key construction and retained occupied insertion. Allocation separately pays two more capability comparisons for needsSet/retainingSet. |
| Composite at7 | heldSetIds visits/excludes the sole workflow by exact ID. Every potentially free stage still runs a FULL state-order `bindableSetsOn` filter over S Sets: mounted-on/status/condition/key tests, result capacity/writes and held-key lookup. No Set sorting, first-candidate invention or free owner call. At6 actual retained binding bypasses this block. |
| Allocation results |80 control/array/requirements/result-branch steps; full reservation literal/append capacity for one or two rows, complete allocation and possible blocker literals. |
| releaseCompletedPhase | Per possible attempt:40 controls, four capability comparisons, two claimed-capability Set operations, full release-result literal and array overhead6. Source tables/filter/kept/released arrays and actual one-reservation walk are included. |
| Initial release copy at7 | Full workflow copy plus reservation/task/binding overrides; full derived-binding copy/overrides;16 empty-kept guards; real replaceWorkflow bill and real1→0 reservation transition. Exactly once. No wrap or Set wear occurs on this phase edge. |
| Attempt result | Per attempt: real allocation and release blocks,60 branch/local controls, full phase-entry result literal, workflow copy/override reserve and replaceWorkflow bill. This covers either successful full overrides or the smaller blocker-only copy. |
| Successful transition | At most once:0→1 at7 or1→2 at6, with the actual full map/includes and sink append bill below. |
| Binding at7 | Real deriveBindings copy/overrides, full second copy for new Set lock, exact bound-field literal,60 uplift/genre scalar steps and full real picture-ID genre lookup. Quality/fit/novelty are not invented. |
| Binding at6 | Real deriveBindings copy/overrides. No second Set lock or uplift call; the actual retained bound Set is not reselected. |
| Technology callback at7 | Actual callback invoked; `phase !== shooting` returns immediately. Reserve6+E(14). No technology lock/root copy. |
| Technology callback at6 | Full silent selection over T rows, new locked-row OR full prior-row copy, lockedWeek write, full technology root copy and productions override. Array append/map allocation plus8T callback/reference-write reserve. No adoption route is reached for silent selection. This lock occurs AFTER facility callbacks. |
| Shooting task at6 | Exact five-field task literal,30 scalar/key-construction controls,2*d.dp shooting-ID span and two stage-capability comparisons. At7 no task is created. |
| Phase event | Exact phase-event literal, stamped sink record, append and8 controls. Paid once even if the attempt ultimately has no success. |
| Common sweep | Existing priority/ordinal order bill; at most eight production Map/settled Set operations and two workflow searches, two full phase/countdown/control visits, one full generic production copy plus remainingTicks write, complete advance-result literal and return-array reserves. The additional80 controls cover initial roots/locals/empty collections and final map/return control. |

The two successful-entry copies/events are not multiplied by failed attempts:
at most one transition can succeed and settle this sole picture. Conversely,
allocation/blocked-workflow copies ARE multiplied by both possible7 attempts.

### Reservation transition formula

`smallTransitionBill(d,b,a,e)` directly prices real
`recordReservationTransition`:

`20 + (b+a)*(8+C) + (2*b*a)*(2+E(L))`
`+ e*(reservationEventLiteral + stampedEventLiteral + APPEND + 5)`.

This covers both key-map result arrays, all callback/key/capacity/writes, both
outer loops and both opposite `includes` walks. Counts are `(1,0,1)` for the
Development release, `(0,1,1)` for rehearsal acquisition, `(1,2,1)` for shooting
acquisition. The retained stage appears in both maps at6 and is compared; it
does NOT invent a release/grant pair. Existing reservation-event literal is a
conservative superset of the actual emitted fields.

### Literal/copy and calculator accounting

New static `literalCost` schemas spell out actual source keys for release,
entry and advance results; workflow release/phase overrides; derived binding
and bound-Set overrides; shooting task; capacity blocker. Each construction
reserve uses those key-derived constants, not an opaque allowance replacing
record/copy work. Generic P and actual workflow/binding/technology/reservation
own-field copies retain measured footprints and opaque reference values.

Helper guard groups have explicit before-access reserves (20/40/12/12/22/8/12,
then phase-specific28 or20), independent charged exact ID/phase/capability
comparisons, and a charged facility-count loop. Scalar receiver bounds remain
8/16/32/64. Conditional block selection pays before its expression; bare
workflow-update and transition helper argument trees have their own precharges.
Nested sum/product/key/comparison helpers retain their own charges and the
shared saturation rule. No caller method is executed to discover a cheaper bill.

## Limits and verification handoff

This is a source-bounded candidate, not a numerical certificate. It retains
general fallback for mixed/multiple pictures, other phases, synchronized sound,
legacy unbound pictures and live setup. Those cases do not gain a new rejection.
The existing arrived-scenery geometry, factories, commands, exact hold/event
chronology, ledger, wrap, release and kernel costs are untouched.

Parent290 review and292–296 serialized checks should determine actual full-route
usefulness, original26 stability and type correctness. Freelancer remains the
separately identified failure; no bill aggregation or employment changes were
bundled into287. Any later work-limit must be attributed from fresh fixed-source
evidence rather than presumed solved by the smaller7/6 owner bills.
