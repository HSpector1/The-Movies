# 220 — independent two-picture retained-Development control

2026-09-20. INERT / UNEXECUTED. Native Fable test-author read. Parent-supplied
baseline16119d9/207 has the retained-writing case budget finding; sole218 writer
is repairing the bounded implementation. No replay implementation/218 handback
was inspected. No protected edit, runtime, probe, typecheck, Git/network or
delegation. Existing twenty-four cases remain immutable.

Draft `220-started-replay-retained-development.test-draft.ts`:261 lines, ONE
default-timeout case. Intended installation
`tests/p14b4-started-replay-retained-development.test.ts`.
SHA256 `2f80623ff3e00d93b023c157621bf4761de823737ff1ef01aec3531f9427c896`.

## Genuine candidate / independent expectations

200's actual week-zero initialization recipe uses
`foundedStudio(SEED,{actor:3,director:1,craft:1})`: actual hires, no contract edits.
The same one contracted writer sequentially commissions A/ticks/accepts, then
commissions B/ticks/accepts. Both scripts must really be Ready before either
greenlight. `remainingPackage` uses actor offsets0/3 and director/craft offsets0/1.
Both actual greenlights occur in the SAME visible week, admitting two new8
pictures, ten distinct company people and two distinct slots in the same real
Development facility. The writer is credited by both pictures but unengaged.
Strict saves/root/foreign-work guards are retained; no clock, cash, Ready root,
slot, employment, history, cast or capacity is fabricated.

The actual reference takes two ticks: both first-sweep new-start skips remain8,
then both become7/preProduction with the exact original facility/slot and updated
reservation phase. Two actual phaseEntered events, and no grant/release events,
are the independent event oracle. Real Sets remain unchanged.

Replay must complete within the UNCHANGED200000 allowance, invoke the original
whole-slate owner exactly once each week, preserve all complete local projections
and foreign technology, and reproduce the real event sequence/owner weeks.
The compulsory ledger is exact: ten company-person holds +two existing slot
holds +every standing Set mount; no writer hold, duplicate screenplay path,
invented Set exclusivity, acquisition, suffix release, first take or release.
All holds span now→H and all paths remain present despite no claim credit. Exact
subject identity is compared by fields, not incidental object-key order.

The pass-through owner spy invokes the original function and restores in
`finally`; input/source purity is asserted. There is no private tariff oracle,
new observer interface, mock result, optional/skipped assertion, increased
timeout or budget relief. This is a requirement regression test, not a new rule.

## Limits / next action

The expanded-hire seed, two sequential commissions, both same-week paid
greenlights, strict current-save conjunction and two-picture replay are all
UNEXECUTED in220. Existing helper recipes/source laws support the construction,
not a claim that it has passed. If available applicants, funds, root relevance,
or the200000 budget fail, retain the real finding; do not alter campaign facts
or weaken assertions. It is historical-control founding, not ordinary natural
founding or an Owner campaign. No kernel feasibility/choice-completeness result
is claimed from two distinct people/pictures merely retaining Development.

Parent next reads/reviews/freezes/installs after218 source freeze, executes with
fixed-source evidence and attributes any genuine fixture or budget failure.
This author took no runtime ownership.
