# B4 public opportunity reader — bounded source handback

2026-09-20. Assignment source c979f1bc27f1cd4e5525f2b758d62a0bbcfd1089;
parent subsequently published the independent bridge RED/doc checkpoint c7ef8c3
while this task ran. No writer Git/network operation or publication claim.

DONE; production SOURCE FROZEN and handed back before parent checks.
Only `src/core/talentMarket.ts` changed: six added lines after
`publicPreferredTerm`, exporting:

```ts
publicPreferredOpportunity(state, talentId): 'significantCastRole' | 'anyCastAppearance'
```

It returns `isProven(state,talentId) ? 'anyCastAppearance' : 'significantCastRole'`.
The EXACT existing private isProven remains the one owner (careerIdentity's real
public identity or age>=30). No copied threshold, priority-position inference,
new missing-person gate, persisted field, RNG, receipt or mutation. No index
export, D3/chooser, rival authoring/seating, type/rules/save/wire/bridge change.

Read full independent review44 and raw43, plus the focused public-read test
requirements. Original43 reaches missing API at age29, age30 and cash-invariance
cases. Its real under30 credit case instead fails a witness guard; D3 cases fail
their no-credit prerequisite, and rival policy remains separate unimplemented
behavior. Adding the reader does not repair or claim those other paths.

Writer ran NO tests/typechecks/probes or Git commands. Parent reports check52
three accessor cases PASS, four deliberately unselected, fixedSource:true;
parent owns that raw verification and the active neighbor regression check53.
This handback is not full policy or B4 acceptance.

Other authorized output: new49 definitively refines42 after44 (no-event release
continuations, exact hold suffix replacement, canonical credit/profile namespace,
permutation-stable budget accounting, reviewed conservative foreign debit and a
fourth minimal finite test). Original42 and all tests/fixtures remain untouched.
