# 492 — Admission talent-key width correction

2026-09-20. **SOURCE FROZEN; sole production-write ownership yielded.** Read
complete486 (including prospective review) and490; implemented only the adopted
minimal delta. Native sim-core role followed. No runtime or fit claim.

## Scope and exact identities

- Supplied published base: `1baf982520248d15731950d5bda48a6e93bba5ea`.
- Sole changed source: `src/core/promiseCapacityOwnerReplay.ts`.
- Before SHA256, checked before editing:
  `956a0245872f02e625c93a41cc2cc2033c4279129b85e100f315045d4d4f8ca4`.
- Frozen after SHA256:
  `5267ff51ba638229306c6861fd965ae2f214529808b145ba9c52cd21ccf71bcb`.

Complete executable delta: seed admitReady's existing idWidth with0 instead of22;
wrap its three original diagnostic-span uses in Math.max(22,idWidth); add one
work.pay(16) immediately before each affected outer reserve. One explanatory
comment describes key widths; the three payments state their source-node bound.
No other formula, helper, owner, field, call ordering or file was changed.

The independently added491 test was not edited. Parent reports its corrected
baseline passed on unchanged466, with test SHA
`c012c1c65ec55df13e941df874ac14ed60e1b7d5510364d3094d13acde284733`.
That is predecessor evidence, not verification of this candidate.

## Consumed-domain and preservation proof

All existing paid discovery loops are unchanged. The package loop includes
writer, director and all cast IDs; the craft loop includes every craft ID. Its
maximum therefore bounds all keys and queries in the actual staffing cast Set
and role Map. Role labels and numeric craft indexes are values/diagnostic text,
not keys. Full-catalog person searchBill reserves remain unchanged.

Busy widening includes every prepared company/active-writing ID before the
actual busy Set and every engaged query. Contract widening then includes every
contract talent ID before the unchanged full current-contract predicate scans
and contracted Set writes. Market widening includes every actually returned
freelancer ID before that Set and final membership queries. Duplicates, long
IDs, cold-market execution and the all-contracted short circuit are unchanged.
No source row, query, error guard or owner call was removed.

For each visited prefix, the former width is exactly max(22,new width).
The three retained diagnostic allowances consequently remain identical:

| Original location | Candidate error-span term | Extra executed work |
| --- | --- | --- |
| Staffing reserve | 4*Math.max(22,idWidth) | One prepaid16 before outer reserve |
| Busy refusal reserve | 160+Math.max(22,idWidth), with all other terms unchanged | One prepaid16 before outer reserve |
| Employment refusal reserve | 180+Math.max(22,idWidth), with all other terms unchanged | One prepaid16 inside the existing economy branch before outer reserve |

Each new Math.max expression has at most eight scalar nodes: receiver/property
access, the two operands, call and result use. At the adopted two units per node,
16 is paid before JavaScript evaluates that expression. There is no new loop,
allocation, string traversal, callback, local cache or default-argument work.
Existing calculator receivers, the staffing multiplication and surrounding
arithmetic retain their original payments. Width initialization is the same
existing scalar assignment, with only its numeric initial value changed.

This separates consumed string bounds from error-formatting allowances; it does
not reduce Work.equality/keyBill prices or their comparison counts. The existing
saturating calculators and200000 cap remain unchanged. readyProductionId's
independent width22 at its numeric identity/padding boundary is unchanged.

## Magnitude and verification limits

490's CURRENT-source probes measured six package IDs/five engaged IDs of length8,
eleven contract IDs of length8, empty busy and market collections, all contracted.
For that measured shape, unchanged reserve algebra predicts reductions of
2478 staffing +70 busy +2310 contracts +840 employment =5698. Three extra16-unit
payments give a paper net5650. This is NOT an executed saving, whole-route fit,
kernel classification or late-stale-refusal acceptance claim.

Static review read the complete changed admission block and every occurrence of
idWidth/contractWidth, confirming unchanged widening loops and independent
production-identity width22. Source hashes were collected. No runtime, tests,
compiler, probes, Git, network or delegation was performed. No diagnostic logs,
hints, caches, separate calculator candidate, test/validator/cap/timeout changes
or other optimizations were added.

Parent next: full actual diff/payment review493, then serialized494–499 checks
including original first-take/kernel and stale RED cases, existing regressions
and491 long-refusal preservation. Any remaining failure must remain visible;
this handback does not qualify B4 or activate live P2/save/projection work.
