# Record 683 — OWNER RULINGS ACTIVATED on record 680, and the P14B.5-T tuning scope

Status: the Owner's decisions on record 680, activated 2026-09-22, plus the bounded scope of the one
tuning step they authorize. Not a checkpoint, not evidence, not Owner acceptance of any result.
Source at activation: `63688a79` (= the verified remote). The activating instruction is the Owner's
`01-APPROVE-AND-CONTINUE.txt`, sent to this session; the unchanged record 680 it rules on is the git
blob `cbdbca4e01292d39cb15e2b0029d9bee86885dae`.

## The four activated rulings, as issued

1. **Finding 1 option (b): `RELATIONSHIP_FAILURE_DELTA = 5`**, as PROVISIONAL CANDIDATE TUNING, not
   final balance. Repeated failure must become capable of producing Strained on the applicable
   low-proximity path. It creates NO conflict driver, NO Enemies/Nemeses, no romance, no new seating
   refusal and no general production penalty. No other growth or dormancy constant moves.
2. **Finding 2 option (a): rival casting, staffing and seat policy are NOT touched.** Rapid positive
   saturation is retained. 628 R5 / G-1(A) / G-2 keep their own bounded disposition. Changing the
   failure penalty alone does not establish that saturation is fixed.
3. **The initial product choices are now explicit, replacing "keep open as recorded":**
   - Q2 / B.5 OPEN 1 — a COMMON BASE GROWTH rule; no temperament-compatibility prior now. The
     existing shared-work proximity weights REMAIN: "common base" does not remove them.
   - Q3 / B.5 OPEN 2 — NO behavioral backfill at migration. Real historical shared-credit counts may
     be displayed as facts, clearly distinguished from recorded friendship or trust history.
   - Q4 / B.5 OPEN 3 — relationship-based casting WARNINGS and already-authorized disclosed
     consequences, NOT a new hard seating refusal. B.6 must not invent a quality modifier. This is
     not a repeal of any separately governed employment or market refusal.
   - OPEN 11 friend/enemy precedence is NOT newly selected; it is retained for the later conflict
     work. OPEN 15's "bundle into V31" question is OVERTAKEN by V31 publication; waiver behaviour
     remains unfinished and needs its own governed change, never silent omission.
4. **P14B.6 read models follow the tuning step** as a separate slice: the profile relationship block,
   readable drivers, casting chemistry information, factual shared-credit counts and warnings.
   TypeScript and bridge work, not native Unity screens. Its own scope carries no new relationship
   driver, policy or tuning.

Carried unchanged by the same instruction: the four-standard-seed week-260 runs are NOT the
6,240-week endurance proof, NOT a full-dormancy proof, NOT a performance-equivalence claim and NOT a
completed relationship ladder; the staged 45 is a WITNESS, not alone a proof of a global minimum.
F=5 opening Strained supplies none of the conflict evidence Enemies/Nemeses need and fixes nothing
about successful or neutral fixed-team saturation. The negative friendship tiers at issue are
Strained, Enemies and Nemeses; romance/Partners is a separately scoped track, so "four of eight
rungs" is not carried forward as an established count.

## P14B.5-T: the tuning step's exact scope

**Production change (one writer, smallest correct):** `RELATIONSHIP_FAILURE_DELTA` 4 → 5 in
`src/core/relationships.ts:72`, with its doc comment updated to state the adopted reachability and
cite this record. NOTHING else in `src/`, `bridge/`, `generated/` or `ui/` changes.

**Why no version step, and the migration implication (to be confirmed by the auditor).**
`RELATIONSHIP_RULES_VERSION` is documented at `relationships.ts:33-36` as the TIER RULE revision —
the value bands plus the §5.3 evidence condition. `RELATIONSHIP_FAILURE_DELTA` is a DRIVER constant,
not part of that rule, and the chooser receipt carries no rules version (the B.1 precedent). The save
format is untouched: `validateRelationshipsRoot` requires `driver.delta` to be an integer and pins no
value, so every historical edge carrying `delta: -4` stays valid and is NOT restamped. Applying the
new value prospectively is automatic, because the delta is read at write time. Therefore: no
`RELATIONSHIP_RULES_VERSION` bump, no `LIVE_SAVE_VERSION` bump, no `PROJECTION_VERSION` bump, no
migration.

The one implication to document rather than hide: a campaign RESUMED FROM A SAVE continues
consistently, because closeness is stored; a campaign REPLAYED FROM ITS SEED from week 0 now diverges
in relationship values from any archived pre-change digest, at its first shared failure. No archived
digest in the suite is affected, because the natural-chain digests exclude the `relationships` key
(`src/harness/roster-wall/historical-control.ts:23-26`) and `rngState` is untouched (B.5 uses no RNG).

**Pre-declared natural-chain attribution (before any writer moves).** The pre row is already
published: run 682 on `ddf58e87`, whose measured values are byte-identical to 677. Predictions:

- NO settlement receipt on the four standard seeds may move. The D5 band is gated on EXPOSURE, which
  654-T measured as ZERO receipts through week 416 on all four seeds: no survivor of any settlement
  holds a roster member at W who shares a first take with the subject. Exposure is a structural
  predicate over employment intervals and take seats; it does not read `closeness`, so a delta change
  cannot alter it. Any moved receipt is a defect to attribute, never a re-pin.
- NO existing B.5 assertion may move for the constant's sake. Every reference to the constant in the
  suite is BY NAME (`tests/p14b5-relationships.test.ts:403,:407,:420,:663,:665,:680,:705,:735`), and
  no test in the repository pins a literal closeness value. The family-4 staged pair at the
  Acquaintances floor still reads Strained (45 − 5 = 40, as it did at 45 − 4 = 41). The one pinned
  relation `RELATIONSHIP_FAILURE_DELTA > RELATIONSHIP_PROXIMITY_LOW` still holds (5 > 2).
- The natural-world CONSEQUENCE is measured, not predicted: after the candidate lands, the parent
  re-runs `676-b5-week260-probe.ts` as the POST row and diffs it against 682. Closeness values on the
  standard seeds must fall by exactly one point per `sharedFailure` driver applied after the change,
  and any tier that moves must be attributable to a named edge crossing a named band floor.

**Test ownership (independent).** test-author writes the requirement regression: RED at 4, GREEN at
5, reaching the REAL shared-take and release write path, never a manually forced negative edge.
Constructed worlds and natural-seed worlds are labelled as such and never conflated. Historical event
values, counters and fixtures are preserved; no historical test is frozen to the new law, no
validator is loosened, no timeout is enlarged.

**Stop condition.** If applying the tuning exposes a material new policy question, that step is
isolated and reported, and independent B.6 work continues on the existing engine law.
