## P14B.5 — First Shared-Work Bond Core — task expansion (draft 2026-09-21; audit before it begins)

**Draft provenance (647-A, sim-core READ-ONLY, paper only).** Worktree `/Users/zacheryspector/The-Movies-headless-program`, HEAD `d6c11b9b` (clean apart from 646-T's three untracked minter files, not read). Every `file:line` below is at HEAD unless the companion is cited (`docs/engineering/p14-preparation-8ef5246a/P14-PREPARATION-COMPANION.md`, Revision 3). Nothing here is an execution order, an Owner decision, a measured result or a claim that any check ran. The parent inserts this section into `docs/engineering/playability-launch-review/plans/P14-HEADLESS-PLAN.md` after the contract-auditor's read-only audit; the scoping it expands is `evidence/p14b4-20260919/645-A-next-family-scoping-note.md` §2–§3 and record 645 §4.

**Gate.** Begins only after (a) 646-T's T0 corpus exists, is independently KEPT and published (the genuine outgoing V30 saves and the genuine projection-47 runtime checkpoint at HEAD, shas in `tests/fixtures/p14/*/MANIFEST.json` and PROVENANCE), (b) this expansion carries the auditor's disposition, and (c) T1 RED is installed against the absent `src/core/relationships.ts`. Source identity at T0: `src/core` has had NO non-comment change since `61833f0d` (`git diff 61833f0d..HEAD -- src/core` = 2 files, 21+/9−, all comment lines — checked 2026-09-21; `651fea8b` is the only commit and it is comment-only); the last `bridge`/`generated` writer is `027155e7` (projection 47). `LIVE_SAVE_VERSION = 30` (`src/core/save.ts:6396`), `PROJECTION_VERSION = 47` (`bridge/schema/bridge-schema.ts:233`), schema identity `sha256:6f6b48805aadcf14d456614d87bf1571eb1ce0d9aa0bc44f604e7976f4f85538` (`generated/unity/StudioBridgeDtos.Generated.cs:3`, `generated/unity/project-studio-bridge.contract-manifest.json:8`), `PROMISE_RULES_VERSION = 4` (`src/core/promises.ts:45`). Save V31 / projection 48 are ALLOCATED AT EXECUTION, not here (plan §3 :409-410).

**Companion basis.** §5 header :393-395 (deliverable F realizes rulings §3.4.1 items 12–13; "IMPLEMENTATION RECOMMENDATION; every threshold, weight and horizon is a NUMERICAL/CONTENT HYPOTHESIS"); §5.1 :397-407 (the original's one 0–100 pair scalar with named bands, the 45–55 random baseline and decay toward it, work-context growth, relationships as a bounded chemistry read at production, no durable history — carried as a tier ladder, decay-to-baseline, work-driven growth, bounded disclosed consequences, durable history); §5.2 :410-417 (two facts per pair — CURRENT closeness and CAREER history :412; evidence before tier, no all-pairs matrix, no weekly scan, drift computed on read from `lastSharedEventWeek` :413; every change has a readable reason :414; no social commands :415; one ladder :416; evidence joins on employment intervals :417); §5.3 :419-433 (the ladder: Nemeses :423, Enemies :424, Strained :425, Acquaintances :426, Colleagues :427, Friends :428, Close Friends :429, Inseparable :430, Partners "formed on the separate romance track … held alongside the friendship tier" :431; "tiers are derived from the closeness value and the evidence record by a versioned rule; the value is never shown" :433); §5.4 :435-449 (drivers by shape: shared production with proximity weights :439, shared success :440, shared failure :441, repeated collaboration :442, casting competition :443-444, cancelled after first take :445, awards :446, dormancy drift :447, romance :448-449); §5.4a :451-462 (romance as a separate track — EXCLUDED here); compatibility prior :463 (→ Q2); §5.5 :465-470 (drift hypotheses 52 / 260 weeks, negative tiers drift too :467; history append-only in meaning and bounded by a stated compaction rule, "no compaction changes a current tier, a milestone date or a replay result" :468; Partners exempt :469; dormant edges :470); §5.6 :472-482 (consumers: production quality by the result owner :476, casting warning :477, the person-choice rule's reservation and D5 descriptor :478, retention attention :479, no Star Power effect :480; `pairChemistry` as a SHARED GENERALIZATION — "P14 never writes a quality number" :482); §5.7 :484-489 (canonical edge key, hot/cold split, weekly cost proportional to the week's events, the August envelope only under compaction, "no edges are fabricated from old credits by default" :489). §2.1.7 :102-120 (reservation "no `Nemeses`-tier relation of the person is on the issuer's roster" :104; D5 row :116 "`close ties here` (Close Friends / Inseparable / Partners on the roster), `none`, `enemies here` | neutral in P14A"; the priority-order rule :120 — unproven and under-30: opportunity, compensation, relationships, term, trust, Standing, incumbency; proven veterans: compensation, term, trust, relationships, incumbency, Standing, opportunity). §8 :635-646 (items 2 governed save step :640, 3 no RNG and no order dependence :641, 4 identity and validator-enforced recording boundary :642, 5 no fabricated pre-P14 relationships :643, 6 no weekly pair scan :644, 7 measurement before sealing :645). R8 :596, R16 :604, R17 :605, R22 :610.

**Settled law (not re-asked; §7.1 by number).** S19 :576 (the Movies+ model: positive, negative, acquaintances, friendships, close bonds, deep bonds, romance, enemies/nemeses, chemistry consequences, growth through shared work, explainable effects); S20 :577 (no social grind; work is the primary driver; the player never commands "talk"); S21 :578 (current closeness may decay; career history never disappears); S22 :579 (romance regardless of gender; no marriage, children, households, lineage, inheritance, domestic needs in P14); S23 :580 (ownership unchanged; `PersonId` immutable; campaign identity scopes every cache, receipt and pending item); S25 :582 (deterministic replayable choice; symmetric player/rival law). Owner ruling 600 §4 (`evidence/p14b4-20260919/600-owner-ruling-578-reopened-program.md:148-153`): one heavy test process; max two specialists, one production writer; the writer never commits; the parent lands patches through the index; record-check for every run; "no cap, tariff, refusal, deadline, timeout or fixture moves to obtain a pass"; Unity/native deferred; no Owner-acceptance claim. 600 §1 D2 :29-36 (the 200,000 cap and the 162 §2 metric fixed; evaluator 5 later). Landed and NOT reopened: the B.4 seating law (`hollywoodTick.ts:163-169`; record 628's three questions are PENDING OWNER, nothing adopted), the cancel causal proof (`promises.ts:667`, record 637), evaluator 4 and `PROMISE_RULES_VERSION 4`.

**Why this slice, and what it excludes.** The relationship model is the one selected P14B experience with zero engine surface (no `relationships`, `pairChemistry`, `closeness` symbol in `src/`/`bridge/`; 645-A §0), the chooser has carried a documented NEUTRAL D5 slot since A.1 (`src/core/talentMarket.ts:659-662`, whose text still reads "It arrives with P14B.2"), it precedes P14C in the companion's own dependency order (§6.3 :527 consumes collaboration evidence) and in the plan's slice order (:407-409), and its three §7.3 entries each carry a recommended first behaviour — the footing B.1 used for Q1/Q3 (:514). The slice: edges minted from shared first takes, release success/failure and the player cancel; drift-on-read tiers by a versioned rule; D5 live in the chooser; the `nemesisOnRoster` reservation drop enumerated; a read-only `pairChemistry` export with no consumer; Save V31; projection 48 thin. EXCLUDED, each recorded with its owner: the romance track (§5.4a :451-462; own value, own threshold, own drivers, Partners coexistence — a later slice with its own V-step); the casting-competition driver and repeated seat competition (§5.4 :443-444; the only lawful source today is the player-only `CastingSession` (`src/core/types.ts:892-902`) — rivals run no session, `decide()` seats directly (`hollywoodTick.ts:160-171`) — so a B.5 driver would be asymmetric under S25; deferred with that reason); chemistry CONSUMPTION by the production result owner (§5.6 :476, :482; R17 — the bound constant is the result owner's, `src/core/reception.ts` `ReceptionInputs` :61, a separate shared-generalization slice); the casting warning (§5.6 :477; Q4's recommended first behaviour; a bridge/read-model item); shared awards (:446, P08 by reference); the retention attention item (:479); evidence labels Mentor/Rivals (:433, HIS-014); EVERY read model — profile relationship block, drivers text, chemistry rows, attention rows, labelled shared-credit counts (Q3) — → P14B.6 on the B.2 pattern (plan :539-577: read models over the landed facts, no engine law); the waiver → the slice after B.5 (645-A §2 "The waiver as the following slice"; its one V31-candidate field `supersededByPromiseId` is NOT bundled here unless Fable rules otherwise — see OPEN 15); VOIDED (needs P14C; 645-A §1(c)); evaluator 5 (600 §1 D1/D2; the designated case `tests/p14b4-cast-class-capacity-evaluator5.test.ts:235-247` stays RED by law); P2–P5 families beyond the landed tagged P2; everything §7.4 :627 lists later (social events, press/scandal, P15 legacy interpretation of relationship facts, Upcoming rows).

**Engine today (verify at T1).** Facts a relationship edge can be minted from without a new producer: `FirstTakeReceipt {eventId, week, productionId, studioId, directorId, cast: Record<CastSlot,string>}` (`types.ts:2131-2138`; `CastSlot = 'lead'|'antagonist'|'support'` :20 — four people, six pairs; NO writer seat on the receipt), appended ONCE per production at the tick tail for player and rival takes alike (`promises.ts:120-143`, idempotent by `productionId`; called at `tick.ts:1102-1109` with `finalized.market.tick`, the week the advance PRODUCES); player releases `state.studio.releasedFilms: FilmResult[]` (`types.ts:253-275`: `productionId`, `releaseTick`, `criticScore`, `boxOffice {opening,total}`, optional `forecast`; pushed at `tick.ts:642` with `releaseTick: currentTick`, the week being advanced); rival releases `hollywood.films` `LiveIndustryFilm {filmId (= productionId), result: FilmResult, …}` (`hollywoodTypes.ts:37-45`; appended at `hollywoodTick.ts:286`, receipt `filmReleased` :292); the player cancel `applyCancel` (`actions.ts:567-600`) whose tail already branches on `hollywood.playerStudioId` into `breakPromisesOnCancel(withoutProduction, playerStudioId, cancelled)` :592-599 — NOTE `breakPromisesOnCancel` returns early when a first take EXISTS for the picture (`promises.ts:788`); the relationship driver fires on exactly the complementary condition; no rival cancel verb exists (`hollywoodTick.ts:271` only drops released pictures). Rosters: `hollywood.employment` rows for player and rival studios alike (`hollywoodTypes.ts:69-74`; reasons include `player-contract`/`existing-player-contract`), read through `activeEmploymentOrdinals` (`hollywoodTick.ts:54-56` `currentEmployees`; `talentMarket.ts:640-643`, :1044-1052 `seatsHeldAfter`). Chooser: `DESCRIPTOR_ORDER` six members (`talentMarket.ts:668`), `DESCRIPTOR_REASON` :671-678, `isProven` :685-688, `priorityOrder` :698-702, `bandsFor` :779-816 (D4 reads `trustDescriptor` at :811-812), `pairwiseWins` :818, `dominates` :822, `chooseProposal` :833-886 (priority-order tie-break :857-864), `FreezeDrop` :999-1010, `DROP_SENTENCE` :1022-1032, `survivesFreeze` :1057-1099 (`issuerDistrusted` at :1067, ahead of the seat budget by design). Recording boundary precedent: `trustDrivers` reads nothing before `state.studioHistory.recordingStartedWeek` (`promises.ts:828-834`); the root validator's `recordedWeek` refuses rows outside `[boundary, market.tick]` (`promises.ts:954-960`) and `personId`/`studioId` refuse ids not in `state.talent` / not entered (:966-975). Save device: own validator → strip → frozen prior (`stripV29Roots` `save.ts:8583-8600`; `validateSaveV30` :8679-8695 requires both V29 roots by `Object.hasOwn`; `convertV30ToV29` :8704-8710 refuses a tagged predicate; `migrateToV30` :8714-8717; the dispatch :5282-5285 with the "1 through 30 only" sentinel; `saveVersion === 30` appears at 24 sites: the dispatch, 18 downgrade throws (`migrateToV8`…`V25`, `V20`), 4 route lines (`migrateToV26` :8340, `V27` :8491, `V28` :8572, `V29` :8670) and `migrateToV30` itself). Determinism: no RNG reaches the tail (`appendFirstTakes`/`advancePromisesWeek`/`advanceTalentMarketWeek`); `rngState` is pinned untouched by `tests/p14b1-promises.test.ts` test 3 and `tests/p13a-rival-adoption.test.ts`. Process-global caches: the one identity-keyed `WeakMap` at `src/core/hollywood.ts:50`; B.5 adds none. Wire: `priorityOrder` enum CLOSED at six (`bridge-schema.ts:2320-2324`), published by `marketCaseProjection` (`bridge/people.ts:977-991`; the `line` reads keys `[0]` and `[1]` through `DESCRIPTOR_LABEL` :866-875); `settlementReasons: string[]` carries the engine's `reasons` verbatim (:998) — free text, no enum; the generated C# holds `string[] priorityOrder` (`StudioBridgeDtos.Generated.cs:6492-6493`), not a C# enum. Registry head `projection-v46` (`bridge/runtime-checkpoint.ts:63`); `tests/bridge-p14b4-runtime47-compatibility.test.ts:162` pins "exact 35 prior IDs". Fixtures: `tests/fixtures/p14/` holds `genuine-v29-pre-p2/` (nine cases), `genuine-v29-pre-b3-evaluator1/`, `genuine-projection46-runtime/`, `legacy-v28-*`; NO genuine V30 save and NO projection-47 checkpoint at HEAD (646-T mints them).

**Scope — engine (Save V31).** (1) ROOT `relationships` at the TOP LEVEL beside `talentMarket`/`firstTakes`/`promises` (R22 :610 "roots at top level, never inside `hollywood`"; the V29 precedent `types.ts:2207-2212`), version = the save version (no second version field; the V28 root comment `types.ts:2121-2123`). `GameStateV31 = GameStateV30 & { relationships: readonly RelationshipEdge[] }`, `GameState = GameStateV31` (`types.ts:2236` moves). PROPOSED SHAPE (field names mechanical and reversible exactly as B.1's `promises: readonly string[]` was, :516; the T1 RED pins the fields it reads, T2 may rename with the ruling recorded):
```
RelationshipEdge = {
  edgeId: string                 // in-state ordinal `relationship-edge-<i>` (the `first-take-event-<i>` device, promises.ts:992)
  a: string; b: string           // canonical: a < b by code-unit comparison; both in state.talent (§5.7 :486)
  closeness: number              // integer 0..100 AT lastEventWeek (drift materialized, then the delta applied)
  firstSharedWeek: number
  lastEventWeek: number          // the drift anchor (§5.5 :467 "lastSharedEventWeek"): the week of the last driver of any kind
  sharedProductions: number; sharedSuccesses: number; sharedFailures: number; sharedCancellations: number   // exact, never compacted
  peakTier: RelationshipTier; peakTierWeek: number
  recent: readonly RelationshipDriver[]   // ≤ RELATIONSHIP_RECENT_CAP, oldest first; the overflow folds OUT at write (counters keep the count)
}
RelationshipDriver = { kind: RelationshipDriverKind; week: number; ref: string; delta: number }   // ref = productionId; no prose persisted (kind → copy at read, the B.2 pattern)
RelationshipDriverKind = 'sharedProduction' | 'repeatedCollaboration' | 'sharedSuccess' | 'sharedFailure' | 'cancelledAfterFirstTake'
RelationshipTier = 'Nemeses' | 'Enemies' | 'Strained' | 'Acquaintances' | 'Colleagues' | 'Friends' | 'CloseFriends' | 'Inseparable'   // the friendship ladder; Partners is the romance track, not a rung (OPEN 8)
```
(2) EDGE MINTING at ONE seam: `advanceRelationshipsWeek(state)` in `src/core/relationships.ts`, called at the tick tail between `appendFirstTakes` and `advancePromisesWeek` (`tick.ts:1102-1110` — the takes are already a fact, the market has not yet ranked on D5). Engagement rule copied verbatim from `appendFirstTakes` (`promises.ts:127-131`): `hollywood === null` → return `state` unchanged, so the headless M0A corpus and the roster-wall observatory stay byte-identical apart from the empty root (`src/harness/roster-wall/historical-control.ts:23-26` strips it from the hash exactly as it strips `firstTakes`/`promises`). LOUD root guard `requireRelationshipsRoot` on the `requirePromiseRoots` model (`promises.ts:88-92`). (2a) `sharedProduction` + `repeatedCollaboration`: for each take with `take.week === state.market.tick` (this advance's takes; never a scan of older takes — that is how Q3's no-backfill holds by construction on a migrated save, OPEN 12), every pair among `{directorId, cast.lead, cast.antagonist, cast.support}` in the receipt's own seat order (director–lead, director–antagonist, director–support, lead–antagonist, lead–support, antagonist–support — never `PersonId` or Map order for the append order; the canonical KEY may compare ids), with proximity weights (§5.4 :439; HYPOTHESES `RELATIONSHIP_PROXIMITY_HIGH` for director–lead and lead–antagonist, `_MID` for director–antagonist and lead–support, `_LOW` for director–support and antagonist–support) and, when `sharedProductions` after this take is ≥ 2, a `repeatedCollaboration` driver of `min(sharedProductions − 1, RELATIONSHIP_REPEAT_CAP)` (:442 "increasing, capped"). A person seated twice on one take (the engine forbids it: `assertGreenlightStaffingIdle`; rival seats are distinct by construction `hollywoodTick.ts:170-175`) is refused loudly, never skipped silently. Idempotent by `(edgeId, kind, ref)`: replaying the tick from the same state mints nothing twice. (2b) `sharedSuccess` / `sharedFailure` at RELEASE, from the release records the tail can already see: player `state.studio.releasedFilms` rows with `releaseTick === state.market.tick − 1` (the release step stamps the advanced week, `tick.ts:642`) and rival `hollywood.films` `LiveIndustryFilm` rows with `result.releaseTick === state.market.tick − 1` (`hollywoodTick.ts:280-286`), each joined by `productionId` to its `FirstTakeReceipt` (the same join the trust derivation uses for the cancel case, `promises.ts:862-875`); the six pairs of that take receive `+RELATIONSHIP_SUCCESS_DELTA` when `result.criticScore ≥ RELATIONSHIP_SUCCESS_CRITIC_SCORE`, `−RELATIONSHIP_FAILURE_DELTA` when `< RELATIONSHIP_FAILURE_CRITIC_SCORE`, nothing between (:440-441 "above a threshold"; OPEN 9 for the basis; OPEN 10 for the seam). Idempotent by `(edgeId, kind, productionId)`. The owner of the RELEASE FACT stays the tick's release step and `advanceHollywoodWeek`; B.5 adds no receipt — the driver `ref` names the film's `productionId`, and the `FilmResult` is the evidence. (2c) `cancelledAfterFirstTake` at the PLAYER CANCEL, synchronously (the B.1 T1 ruling (i) form): `recordCancelledAfterFirstTake(state, playerStudioId, cancelled)` called in `applyCancel` beside `breakPromisesOnCancel` (`actions.ts:592-599`; `breakPromisesOnCancel` itself untouched), firing iff `state.firstTakes` holds a take for `cancelled.id` — the complement of :788 — for that take's six pairs with `−RELATIONSHIP_CANCEL_DELTA` (:445 "small"). Rival symmetry: the LAW is the same pure helper; no rival cancel verb exists (source fact above), so the reachable set is player-only exactly as the termination trust driver was (B.1 T1 ruling (v), :525). (3) VALUE, DRIFT, TIER — pure, integer, no RNG. `RELATIONSHIP_BASELINE` fixed (the original's random 45–55 replaced by one constant; §8 :641 forbids a new stream without an Owner-authorized `RngPurpose` widening; OPEN 4). A write materializes drift first (`closeness = currentCloseness(edge, week)`), applies the delta, clamps to `[0, 100]`, sets `lastEventWeek = week`, updates `peakTier`/`peakTierWeek` when the new tier is higher than the stored peak, appends the driver and folds the oldest out of `recent` past the cap (counters are the fold target: §5.5 :468 "fold into per-edge exact counts"). `currentCloseness(edge, week)`: `dormant = week − lastEventWeek`; `≤ RELATIONSHIP_DRIFT_GRACE_WEEKS` → stored value; else `closeness + trunc((BASELINE − closeness) × min(dormant − GRACE, RETURN) / RELATIONSHIP_DRIFT_RETURN_WEEKS)` — toward the baseline from either side, never past it (§5.5 :467 hypotheses 52 / 260). `currentTier(edge, week)` under `RELATIONSHIP_RULES_VERSION = 1`: value bands with named edges (OPEN 4) PLUS the evidence conditions of §5.3 — Nemeses and Enemies require a conflict record (:423-424 "open hostility on the record" / "recorded conflict"); B.5 mints NO conflict-record kind, so a value in those bands reads Strained (:425 "recent negative driver without a conflict record"). Drift never moves a tier AWAY from Acquaintances; counters, `firstSharedWeek`, `peakTier`/`peakTierWeek` never change by drift or compaction (:468 "no compaction changes a current tier, a milestone date or a replay result"). (4) REACHABILITY IN B.5 (paper, from the sign shape of §5.4): every negative driver presupposes a prior `sharedProduction` on the same take, so with `RELATIONSHIP_FAILURE_DELTA` and `RELATIONSHIP_CANCEL_DELTA` both smaller than `RELATIONSHIP_PROXIMITY_LOW` the stored value never falls below the baseline — Strained, Enemies and Nemeses are UNREACHABLE in B.5 and pinned so; `enemies here`, `nemesisOnRoster` and a negative `pairChemistry` sign are enumerated and unreachable (the A.1 `decision_pending` / B.1 WAIVED-VOIDED precedent, `tests/p14b1-promises.test.ts:845-850`). OPEN 5 records the alternative. (5) D5 IN THE CHOOSER (companion :116, :120; R8): `DESCRIPTOR_ORDER` 6 → 7 with `relationships` between `trust` and `standing` (`talentMarket.ts:668`); `bandsFor` (:779-816) gains `relationships`: for the case's subject at W, over the subject's edges, `close ties here` (2) iff some counterpart is on the ISSUER's roster at W with `currentTier ∈ {CloseFriends, Inseparable}` (Partners does not exist), `enemies here` (0) iff some counterpart is on that roster with `currentTier ∈ {Enemies, Nemeses}` and no close tie (precedence when both: OPEN 11, unreachable), else `none` (1); "on the issuer's roster at W" = the issuer's `hollywood.employment` rows active at W excluding the subject — the predicate `seatsHeldAfter` and the rival-seat read already use (:640-643, :1044-1052; OPEN 7). `DESCRIPTOR_REASON.relationships` = one ordering-only sentence naming no person, tier or number (CANDIDATE wording `their roster holds this person's close ties`; the contract is the sentence's class, not the prose). `priorityOrder` (:698-702) regains `relationships` at its companion positions: unproven `['opportunity','compensation','relationships','term','trust','standing','incumbency']`, proven `['compensation','term','trust','relationships','incumbency','standing','opportunity']` (:120); `isProven`/`preferredTerm` untouched (the F1 decoupling survives). The comment block :651-662 is corrected to name B.5. (6) RESERVATION: `FreezeDrop` gains `nemesisOnRoster` (:104), with a `DROP_SENTENCE` entry (CANDIDATE `${studio}'s roster holds someone this person will not work beside.`), checked in `survivesFreeze` immediately after `issuerDistrusted` (:1067) and before `subjectCommittedElsewhere`: true iff a counterpart of a `Nemeses`-tier edge of the subject is on the issuer's roster at W. UNREACHABLE in B.5 by (3)-(4): Nemeses needs a conflict record; pinned never emitted on the standard seeds. (7) `pairChemistry(state, a, b, week)` EXPORTED, read-only, NO consumer: `{ tier: RelationshipTier | null, sign: -1 | 0 | 1, reasons: readonly string[] }` — `null`/`0`/`[]` when no edge exists; `sign` +1 for Colleagues and above, −1 for Strained and below (§5.6 :476), the reasons derived from `recent` kinds and counters with no number in any string (the B.1 leak pattern). The result owner's bound, formula and seam are NOT designed here (R17; :482 "P14 never writes a quality number"). (8) DETERMINISM: no RNG (none needed — `rngState` byte-equal before/after every B.5 write, pinned); no dependence on array, Map/Set, `PersonId` or row order for any CHOICE (the canonical key and the receipt seat order are identities, not choices); copy-on-write reducers; no module-level cache; no session/campaign id in core (S23 :580; §8 :641-642); Save As copies sharing ids stay independent because the root lives in `GameState`. (9) CENTURY-SCALE BOUND (§8 :639, :644-645; §5.7 :487-488): weekly cost proportional to this week's takes, releases and cancels, never to the population; storage = edges × (fixed fields + ≤ `RELATIONSHIP_RECENT_CAP` drivers); the paper worst case is `firstTakes.length × 6` edges (rosters are stable, so far fewer distinct pairs in practice); the measured edge and driver counts on the standard seeds at week 260 and on the endurance scenario are T4 evidence items (the 645-A read-only research item), not claims here; windowed compaction (§5.5 :468's 260-week fold) is the recorded upgrade path when PERF-010 measures the need (OPEN 6). (10) MIGRATION: `SaveFileV31 {saveVersion: 31, seed, state: GameStateV31, broadcastCache}`; `validateSaveV31` validates the envelope keys, `saveVersion === 31`, `Object.hasOwn(raw,'relationships')`, then `validateRelationshipsRoot(raw)` (in `relationships.ts`: array; each row exact keys; `edgeId` = the ordinal; `a < b`; both in `state.talent`; no duplicate pair; `closeness` integer 0..100; `firstSharedWeek ≤ lastEventWeek`, both inside `[studioHistory.recordingStartedWeek, market.tick]` (the `recordedWeek` device, `promises.ts:954-960`); counters non-negative integers with `sharedSuccesses + sharedFailures ≤ sharedProductions` and `sharedCancellations ≤ sharedProductions`; `peakTier` in the catalogue; `recent.length ≤ RELATIONSHIP_RECENT_CAP`, each driver's kind in the catalogue, `week` recorded, `ref` a non-empty string, `delta` an integer; every driver `week ≤ lastEventWeek`), then strips `relationships` and delegates to `validateSaveV30` (the `stripV29Roots` device). `convertV30ToV31` opens the root EMPTY (`relationships: []`) and recomputes NOTHING — a campaign written before edges existed formed none (Q3; §5.7 :489; `convertV28ToV29`'s comment :8620-8628). `convertV31ToV30` asks `projectRelationshipsPreV31(rawState)` BEFORE validating the envelope (the `projectPromisesPreV29` precedent :8651-8661, `promises.ts:1142-1157`): refused with "cannot downgrade SaveFileV31 or discard the relationship record" when ANY edge exists; lossless when empty. `migrateToV31` is the live route (`31` → validate; else `convertV30ToV31(migrateToV30(save))`); `migrateToV30` gains `if (save.saveVersion === 31) return convertV31ToV30(...)`; `migrateToV26/27/28/29` each gain the 31 route line beside their 30 line (:8340, :8491, :8572, :8670); the 18 downgrade throws each gain a `saveVersion === 31` line; `validateSave` dispatches 31 and its sentinel becomes "1 through 31 only"; `LIVE_SAVE_VERSION = 31`; `makeSave` stamps 31 through `validateSaveV31`; `worldgen.ts:783-785` seeds `relationships: []`; `src/core/index.ts` exports the V31 API beside the V30 block (:1292-1296) and a `relationships` block on the promises pattern (:1394-1427). (11) LIVE-VERSION SWEEP (values only; the `6948e31` / 600-T2 precedent), by class: LITERALS (`saveVersion: 30`, `toBe(30)`, `PROJECTION_VERSION` 47 pins, the schema-id literal `6f6b4880…` at `tests/bridge-contract-generator.test.ts:562-565`, the F10/F11 generated-C# sentinel, the eight UI pins of 600-T3 in `ui/src/{saves,session}.test.tsx`, `ui/src/lot/snapshot/v14SetHolderBoundary.test.ts`, `ui/src/engine/{d17-save-migration,film-chronicle-adapter}.test.ts`); SENTINELS (`tests/p14b1-save-v29.test.ts:196-201` "sentinel 31 / 1 through 30 only" → 32 / "1 through 31 only"; the sentinel `it` moves to 32); FROZEN BUILDERS / HARNESS (`tests/contracts/_v14Contract.ts:411-417` V13-twin strip gains `relationships` with its "cannot discard" guard; `src/harness/roster-wall/historical-control.ts:23-26` hash strip; `src/harness/p14/legacy-v28-fixtures.ts`, `src/harness/d16/run-d17b-*.ts` V30 arms; the load/runtime arms `bridge/session.ts`, `bridge/runtime-checkpoint.ts`, `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts` → `migrateToV31` on the `2cbc3de5` precedent); ID ROSTERS AND SIZE BOUNDS (`SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` gains one entry → the "exact 35 prior IDs" pin at `tests/bridge-p14b4-runtime47-compatibility.test.ts:162` reads 36; the seven-member `priorityOrder` arrays at `tests/p14a1-f1-priority-order.test.ts:86,91`, `tests/p14b1-trust-chooser.test.ts:172-173`, `tests/bridge-p14b4-cast-class.test.ts:410-411`). The sweep is a SEPARATE values-only patch from the production patch; test files are never in the writer's patch. (12) NOT TOUCHED: `promises.ts` — the evaluator (`promiseFeasibility` :388, `reclassifyPromise` :459, `targetSpecificImpossibility` :667, `breakPromisesOnCancel` :785-803, `breakPromisesOnTermination` :759), `PROMISE_RULES_VERSION 4` and every hypothesis constant :53-78, the trust derivation :822-901 (relationships are not a trust input: §4.5 lists none), the promise root validator :927-1140; `promiseCapacity*` and the replay module (`promiseCapacityOwnerReplay.ts` SHA `9e97ffed…`, unwired by 600 §2); `hollywoodTick.ts` `decide()`/`staff()` and the seating preference :163-169 (628 pending); `authorRivalPromise` (`talentMarket.ts:1275`); the 200,000 cap and the 162 §2 metric (600 §1 D2); `TUNING`; `STANDING_BAND_TOLERANCE` :746, `RIVAL_TEAM_ROLES` (`hollywoodStartingData.ts:46`), every existing `DROP_SENTENCE` string, timeout, refusal string and historical fixture; every V12–V30 validator and frozen projection; the 302 stale-route RED; the evaluator-5 designated case; the cancel seam's proof — `applyCancel` gains one call beside the existing one, and `breakPromisesOnCancel`'s law, strings and tests (`tests/p14b4-cancel-causal-proof.test.ts`) are unchanged.

**Scope — bridge (projection 48), THIN — forced, not optional.** D5 live means `publicPriorityOrder` returns seven members and `marketCaseProjection` publishes it (`bridge/people.ts:977`) against a CLOSED six-member wire enum (`bridge-schema.ts:2320-2324`), the exact situation that forced B.1's 44 → 45 (backlog :526 "The forced bump adds `opportunity` and `trust` to the priority-order enum"). The WHOLE wire change: (a) the `priorityOrder` enum gains `relationships` (schema :2320-2324; `PROJECTION_VERSION = 48` :233; `$id` `urn:project-studio:bridge:protocol-4:projection-48`); (b) `DESCRIPTOR_LABEL` gains `relationships` (`people.ts:866-875`; CANDIDATE `friends on the roster`) so the record stays total — the `line` VALUE is unchanged for both archetypes because `relationships` sits at index 2 (unproven) and 3 (proven) and the line reads `[0]`/`[1]` (:991); (c) the outgoing 47 identity `sha256:6f6b48805aadcf14d456614d87bf1571eb1ce0d9aa0bc44f604e7976f4f85538` registered as `projection-v47` at the head of `SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` (`runtime-checkpoint.ts:59-63`, the `projection-v46` line's pattern); (d) the generator run ONCE over its three owned artifacts `bridge/schema/project-studio-bridge.schema.json`, `generated/unity/StudioBridgeDtos.Generated.cs`, `generated/unity/project-studio-bridge.contract-manifest.json` (`scripts/generate-bridge-contract.ts:15-16, 192`; `npm run generate:bridge-contract`, then `check:bridge-contract` and `check:bridge-contract:fixtures`, `package.json:12-15`). The D5 settlement reason reaches the wire as a VALUE through the existing free-text `settlementReasons: string[]` (`people.ts:998`) and the receipt `reasons` (`types.ts:2093`) — no schema change. NOTHING ELSE: no relationship DTO, no drivers text, no tier, no chemistry row, no casting warning, no attention cause, no view/intent/page (→ B.6). The leak law extends to the new engine facts: no `closeness`, no edge of a rival-employed pair, no `recent` delta appears on any serialized DTO (the B.1 pattern, `tests/p14b1-leak.test.ts`). If Fable instead keeps projection 47, D5 must stay OUT of `publicPriorityOrder` until B.6 while the engine ranks on it — NOT recommended: §2.1.7 :120 is one rule, and the F1 history (:529-537) shows what a partial public order costs.

**Natural-chain exposure and reconciliation classes (pre-declared for the reviewer; the G9/G10 device — a NAMED class, the receipt facts a lawful movement must show, a per-seed ledger; `00-start.md:214-224, 252`).** CANNOT MOVE: `rngState` (no RNG); `firstTakes` and promise outcomes UNTIL the first moved settlement on a seed (no B.5 read enters `decide()`/`staff()`/`advanceManagedProductions`/the evaluator); every pre-settlement rival production digest; the `trustDescriptor` of any pair; every V≤30 fixture byte and every frozen validator. CAN MOVE, by class — R-D5-SETTLE: a `settled`/`declined` receipt on a case with ≥ 2 freeze survivors where the D5 bands differ, i.e. the subject has an edge at `CloseFriends`+ whose counterpart is on exactly one survivor's roster at W. Direction: the INCUMBENT gains (a rival roster of six makes one picture at a time with the same director and three actors, `hollywoodTick.ts:160-171`, so its pairs accrue `sharedProductions` every cycle and reach Close Friends after a few pictures — paper, from the hypothesis weights; not measured). On the measured seeds the synchronized churns at weeks 208 and 404 (`p13-public-commercial-adoption`; B.1 T1 ruling (v) :524) are where every rival-subject case opens at once; those receipts are the exposed set. A lawful movement must show: the winner's `settled` receipt `reasons` containing the D5 sentence, the subject's edge (`edgeId`, counterpart, `currentTier` at W ≥ CloseFriends) and the counterpart's active `hollywood.employment` row for the winning issuer at W; anything else is a defect. R-D5-CHAIN: after the first moved settlement on a seed, employment → rival cast → takes → films → standing → later cases are chain-exposed downstream; attributed by the first moved receipt, never re-pinned to "expected". R-ORDER-VALUE: every pin of `publicPriorityOrder` / `preferences.priorityOrder` moves to seven members (a wire VALUE move, not a law move; the `line` does not move). R-VERSION: every version, schema-id and registry-size literal (the sweep classes above). Inventory of files reading chooser outcomes or the public order (grep 2026-09-21, from 645-A): `tests/p14a1-f1-priority-order.test.ts`, `p14a1-seat-budget`, `p14b1-trust-chooser` (:172-173), `p14b1-promises`, `p14b1-t4-regressions`, `p14b3-reservations`, `p14b3-rule-revision`, `p14b4-cast-class-{capacity,outcomes,policy}`, `p14b4-material-evidence-core`, `p14b4-d3-matching`, `p14bf2-acting-discipline`, `bridge-p14a1-market`, `bridge-p14a2-market`, `bridge-p14a3-world`, `bridge-p14b1-promises`, `bridge-p14b3-promise-command`, `bridge-p14b4-cast-class` (:410-411), `bridge-contract-generator` (:562-565), `bridge-p14b4-runtime47-compatibility` (:162), `bridge-p13b-s8-rivals`, `p13a-rival-adoption`, `tests/helpers/p14b2-fixtures.ts`. MOST EXPOSED SINGLE TEST: the B.2 T3 controlled D3 case (`tests/p14b1-trust-chooser.test.ts` ~:294-353, "equal bands, causal compensation → opportunity winner reversal", plan :573): if its two survivors' issuers differ in roster friendships at W, D5 breaks the equal-band premise; the test-author checks it at T1 and re-expresses it (a friendship-neutral W, or both issuers holding a close tie) with the ruling recorded — never by weakening the assertion. The test-author pre-declares, per the natural-chain rule (memory: pre-declare attribution; derive pins from receipts), which assertions may move and forges any new case at the live counter, receipts-derived.

**Tests (RED-first; test-author; independent of the implementer).** Every engine file imports from the ABSENT `src/core/relationships.ts` so the RED is module resolution (0 collected), the strong precedent (B.1 T1 :524; memory: a missing NAMED export binds `undefined` and passes spuriously — the whole module must be absent, and every binding is CALLED in every group). Two engine files, separated as the B4 plan orders (`P14B4-HEADLESS-PLAN.md:391-393` "separate files for frozen save/migration and reader-admitted synthetic variants"): `tests/p14b5-save-v31.test.ts` (families 9–10 over the T0 corpus and the frozen chain) and `tests/p14b5-relationships.test.ts` (families 1–8, synthetic and staged worlds); the bridge file `tests/bridge-p14b5-relationships.test.ts` (family 11) imports from the absent `bridge/relationships.ts` ONLY if a bridge module is needed — the thin surface needs none, so family 11 pins version literals and the registry and is RED by VALUE, stated as such. T0 corpus cases consumed (names as recorded in 646-T's MANIFEST; the intent list is record 645 NEXT645 (1)): the EMPTY root; the bound open P1; the tagged P2; kept-and-broken; the rival current P1; the first-take world at `remainingTicks === 5`; the genuine projection-47 runtime checkpoint. Families, each with the plan sentence it derives from: 1 EDGE MINTING (scope (2a)) — advance the `remainingTicks === 5` case one tick: exactly six edges, canonical keys, `sharedProductions = 1`, one `sharedProduction` driver each with the proximity delta by seat pair, `firstSharedWeek = lastEventWeek =` the take week; a second advance from the same pre-take state mints byte-identical edges (replay); re-ticking the post-take state mints nothing (idempotent); a RIVAL take (the rival current-P1 case advanced to its take, or the `p13aGeneratedStudio` chain — `tests/p14b1-first-take.test.ts` construction) mints under the same law; `rngState` byte-equal before/after; a headless world (`hollywood === null`) keeps `relationships` empty. 2 CANONICAL KEY AND REPETITION (2a) — the same pair on a second production is ONE edge with `sharedProductions = 2` and a `repeatedCollaboration` driver of the accelerator value; `a < b` regardless of seat order. 3 DRIFT ON READ (3) — a staged edge with `lastEventWeek = w` reads the same tier at `w + GRACE` and drifts toward Acquaintances between `w + GRACE + 1` and `w + GRACE + RETURN` (pinned by the exported constants, never literals); drift never moves a tier away from Acquaintances; counters, `firstSharedWeek`, `peakTier`/`peakTierWeek` unchanged by drift; a write after dormancy materializes the drifted value before applying the delta. 4 TIER RULE (3)-(4) — `RELATIONSHIP_RULES_VERSION === 1`; eight members; byte-equal output for byte-equal edge + week; a value in the Enemies/Nemeses bands without a conflict record reads Strained (synthetic edge, reader-admitted); `closeness` never falls below `RELATIONSHIP_BASELINE` on any B.5-minted edge (the reachability pin); Strained/Enemies/Nemeses never read on the standard seeds. 5 RELEASE AND CANCEL DRIVERS (2b)-(2c) — a player picture advanced through its take to release with `criticScore ≥` the success edge mints `sharedSuccess` on all six pairs once (idempotent across a re-tick), `< ` the failure edge mints `sharedFailure`, between mints nothing; a rival release (`hollywood.films`) under the same law; a player `cancel` of a picture WITH a first take mints `cancelledAfterFirstTake` on its six pairs once while `breakPromisesOnCancel` is a no-op for it (:788), and a cancel of a picture WITHOUT a take mints nothing while the promise seam may still BROKEN — both seams fire independently (coexistence). 6 D5 IN THE CHOOSER (5) — the B.2 T3 controlled-staging construction (plan :573; receipts-derived, live counter): two survivors, bands equal on every other descriptor, the incumbent's roster holding the subject's `CloseFriends` collaborator at W → the incumbent settles with the D5 sentence in `reasons`; the same case at a W where that edge has drifted below Close Friends settles the other way (or declines by the tie order, stated); `enemies here` never read. 7 PUBLIC ORDER (5) — `publicPriorityOrder` byte-equal to the SEVEN-member companion orders for both archetypes (asserting the companion :120, never the landed array); `publicPreferredTerm` value-identical (proven → longest, unproven → shortest); the F1 pins (`tests/p14a1-f1-priority-order.test.ts:86,91`) re-expressed to seven by the test-author (the B.1 "widened to six" precedent). 8 RESERVATION AND CHEMISTRY (6)-(7) — `nemesisOnRoster` is a `FreezeDrop` member with a sentence and is never emitted on the standard seeds; `pairChemistry` returns `{tier: null, sign: 0, reasons: []}` for a pair without an edge, `sign: 1` for a staged Colleagues+ pair, and no digit in any reason; the leak test extends to the new facts (no `closeness`, no rival-pair edge on any serialized DTO). 9 SAVE V31 — FROZEN/MIGRATION FILE (10) — `LIVE_SAVE_VERSION === 31`; sentinel 32 refused "1 through 31 only"; every T0 V30 case migrates through `migrateToV31` with `relationships: []` and every other root byte-identical to the V30 bytes; `migrateToV30` of that lifted save reproduces the V30 bytes (lossless when empty); a V31 world holding one edge refuses `migrateToV30` with the downgrade message and refuses every older `migrateToVn`; the projection-47 checkpoint's embedded save loads through the live route; the frozen V≤30 chain's own tests unchanged (R4 through `migrateToV31`). 10 ROOT VALIDATOR (10) — refuses: a missing root; a non-ordinal `edgeId`; a duplicate pair; a non-canonical key; a person not in `state.talent`; `firstSharedWeek > lastEventWeek`; a week outside the recording interval; a driver week after `lastEventWeek`; `recent` over the cap; `peakTier` outside the catalogue; `closeness` outside 0..100 or non-integer; counters inconsistent; an unknown driver kind; an extra key. 11 BRIDGE (projection 48) — `PROJECTION_VERSION 48`, schema `$id` projection-48, `LIVE_SAVE_VERSION 31`; the seven-member `priorityOrder` enum in the generated schema; `preferences.priorityOrder` seven members and `line` unchanged on a staged case; the D5 sentence present in `settlementReasons` on the family-6 world; the genuine projection-47 checkpoint migrates through the registry with `projection-v47` and the registry holds exactly 36 prior ids; the generator `--check` clean. 12 NATURAL CHAINS — the R-D5 ledger on the measured seeds: the pre-declared receipts, each moved one attributed by the three facts above; every other pinned control unchanged.

**OPEN (recorded, not decided; numbered so the audit can count them).** 1 Q2 :621 verbatim: "Should relationship growth use the public temperament descriptor as a compatibility prior, or grow at one base rate for every pair? | Prior: variety and chemistry surprises, one more public descriptor to explain. Uniform: any pair can become Inseparable through work alone, less texture | **Compatibility influence remains a later recommendation**; the first slice grows every pair at the base rate (recommended first behavior, not an approval) | P14B relationships". B.5: base rate. Reversible mechanism: every delta passes through one pure `driverGain(state, a, b, delta)` that returns `delta` unchanged; a later Owner choice replaces its body with a bounded factor from the public `temperament` descriptor and adds one driver-copy line ("Compatible temperaments") — no shape or version change. 2 Q3 :622 verbatim: "At migration, should anything be reconstructed from existing credits and receipts? Factual shared-credit counts ("worked together on 2 films") are facts, not friendship, romance, chemistry or trust; the question is whether any *behavioral* state is backfilled | Count-only: facts displayed, no tier, no chemistry, no trust driver from pre-boundary receipts. Behavioral backfill: labeled reconstruction of tiers or trust from old data, risk of over-reading | **No behavioral backfill by default**; factual shared-credit counts may be shown as facts, labeled, and are not reconstructed friendship, romance, chemistry or trust (recommended first behavior, not an approval) | P14B migration". B.5: `convertV30ToV31` opens the root EMPTY; the mint reads only this advance's takes; the T0 case holding first takes migrates with NO edge. Reversible mechanism: a later labelled reconstruction is its own migration step with its own receipt (the `existing-player-contract` precedent, :489), never a change to `convertV30ToV31`; the count-only DISPLAY is a B.6 read model over `firstTakes`/film credits, labelled, touching no edge. 3 Q4 :623 verbatim: "Should a Nemeses/Enemies pair be able to refuse a seating outright, or only incur the disclosed penalty? | Refusal: stronger drama, a hard block the player must manage. Penalty: never blocks, always explainable | **Warning plus the disclosed consequence is the recommended first behavior**; hard refusal remains unselected later scope (not an approval) | P14B chemistry". B.5: no greenlight or casting guard; `pairChemistry` is read-only with no consumer; the warning is B.6's. Reversible mechanism: a hard refusal would be one additive guard in the greenlight/casting owner reading `pairChemistry(...).tier`; nothing in B.5 forecloses it. 4 VALUE SCALE, BASELINE, TIER EDGES (645-A gap 2): (i) integer 0–100, `RELATIONSHIP_BASELINE = 50`, edges as named constants mirroring the original bands (§5.1 :401, §5.3 :423-430: Nemeses ≤ 10, Enemies 11–30, Strained 31–44, Acquaintances 45–55, Colleagues 56–60, Friends 61–70, Close Friends 71–80, Inseparable ≥ 81 — HYPOTHESES); (ii) a small signed integer around 0. RECOMMEND (i): the parity anchor is legible and every test pins the constants by name. 5 NEGATIVE REACHABILITY IN B.5 (645-A gap 3 restated): (i) `RELATIONSHIP_FAILURE_DELTA` and `_CANCEL_DELTA` below `RELATIONSHIP_PROXIMITY_LOW` → no B.5 edge ever falls below the baseline; Strained, Enemies, Nemeses, `enemies here`, `nemesisOnRoster` and a negative chemistry sign are all enumerated-unreachable and pinned; (ii) a failure delta above the lowest proximity weight → Strained reachable through repeated flops on low-proximity pairs, giving a later result-owner slice a reachable negative sign. RECOMMEND (i): §5.4 :441 words the failure driver "− (small) … strain, not enmity", the negative half of the ladder is the conflict driver's (:443 "the principal source of negative relationships"), which B.5 defers on S25 grounds, and (i) is the pin that makes the four unreachable surfaces checkable; (ii) is a two-constant change. 6 EDGE STORAGE BOUND (645-A gap 1): (i) fold-at-write by count (`recent` ≤ `RELATIONSHIP_RECENT_CAP`, counters exact) — no weekly sweep, no window arithmetic, ceiling = a B.6 profile shows at most N recent drivers; (ii) the §5.5 :468 windowed compaction (260-week fold under PERF-010's trigger). RECOMMEND (i) with a `ponytail:` comment naming (ii) as the upgrade once the endurance envelope is measured. 7 WHERE D5 READS THE ROSTER AT W (645-A gap 4): (i) the issuer's `hollywood.employment` rows active at W (the `seatsHeldAfter`/rival-seat predicate); (ii) the roster after this week's commits. RECOMMEND (i): it is the fact the subject can see when deciding, and it is order-independent within the fixed-order settlement pass. 8 PARTNERS REPRESENTATION: (i) a nine-member `RelationshipTier` with `Partners` enumerated and never returned by the tier rule; (ii) an eight-member friendship ladder with NO romance field in V31 — the romance track lands with its own field/root and V-step. RECOMMEND (ii): §5.3 :431 and §5.4a :453 say Partners is "held alongside the friendship tier", not a rung, so enumerating it in the ladder would misstate the companion; nothing is foreclosed (a later `romance` leaf is additive). 9 SUCCESS/FAILURE BASIS (645-A gap "P07 threshold"): (i) `result.criticScore` against two named constants `RELATIONSHIP_SUCCESS_CRITIC_SCORE` / `RELATIONSHIP_FAILURE_CRITIC_SCORE` (always present on `FilmResult` :261); (ii) `boxOffice.total` against `forecast.expectedTotal` (met its own greenlight forecast; `forecast` is OPTIONAL on `FilmResult` :275, absent on legacy films). RECOMMEND (i): no optional field in a driver's predicate; the constants are hypotheses the audit can move. 10 RELEASE-DRIVER SEAM: (i) the tick-tail step reads this advance's release records (player `releasedFilms`, rival `hollywood.films`) — one seam, symmetric, idempotent by `productionId`, the `appendFirstTakes` "one root, one place" device (`tick.ts:1097-1101`); (ii) a helper called at both release sites (`tick.ts:642`, `hollywoodTick.ts:286`). RECOMMEND (i): two fewer edits in the release owners and no reordering risk; the film's own `releaseTick` is the driver week either way. 11 D5 PRECEDENCE when a close tie AND an enemy are both on the issuer's roster (unreachable in B.5): CANDIDATE `enemies here` wins (hostility outranks friendship, the reservation-adjacent reading of :478); decided by the slice that lands a conflict driver. 12 RECORDING-BOUNDARY SOURCE: (i) reuse `studioHistory.recordingStartedWeek` (the B.1 precedent, `promises.ts:828`, :954-960) and rely on live-only minting for no-backfill; (ii) the root carries its own `recordingStartedWeek` set to the migration week so the validator can refuse an edge older than the root. RECOMMEND (i): B.1 satisfied §8 :642 the same way for a P14 root; edges are minted only by live events; (ii) is one integer field if the auditor reads :642 as requiring a per-root anchor. 13 THE WRITER SEAT: §5.4 :439 counts writer seats "at the lowest weight"; the receipt carries no writer (`types.ts:2131-2138`), so B.5 excludes it — a later slice either adds `writerId` to a V32 receipt or reads `Production.writerId` at mint; recorded, not decided. 14 COMPANION VS CODE, three notes: (a) `talentMarket.ts:662` says D5 "arrives with P14B.2" — text, corrected at T2; (b) 645-A §"Unity backlog implications" called the wire change "a C# enum widening" — the generated C# holds `string[] priorityOrder` (`StudioBridgeDtos.Generated.cs:6492-6493`); the widening is a JSON-schema enum, and a native switch over the strings without a default is what would break; (c) §5.2 :417 "evidence joins on employment intervals, not Industry credit rows" is what a B.6 "worked together at Studio X" LABEL needs; the edge itself is minted from `FirstTakeReceipt`, which already carries `studioId` and the seats, so B.5 needs no interval join. 15 THE WAIVER'S V31 FIELD (645-A §2 "the following slice"): whether `supersededByPromiseId: string | null` rides B.5's V31 step to avoid a V32, or waits for its own step. Not recommended here: bundling a field no B.5 path writes makes the V31 validator's exact-key list carry an unexercised member; Fable's call. 16 HYPOTHESIS CONSTANTS, all named in code and pinned by name, none settled: `RELATIONSHIP_BASELINE`, the seven tier edges, `RELATIONSHIP_PROXIMITY_{HIGH,MID,LOW}`, `RELATIONSHIP_REPEAT_CAP`, `RELATIONSHIP_SUCCESS_DELTA`, `RELATIONSHIP_FAILURE_DELTA`, `RELATIONSHIP_CANCEL_DELTA`, `RELATIONSHIP_SUCCESS_CRITIC_SCORE`, `RELATIONSHIP_FAILURE_CRITIC_SCORE`, `RELATIONSHIP_DRIFT_GRACE_WEEKS` (52), `RELATIONSHIP_DRIFT_RETURN_WEEKS` (260), `RELATIONSHIP_RECENT_CAP` (8), `RELATIONSHIP_RULES_VERSION` (1); the two CANDIDATE sentences (D5 reason, `nemesisOnRoster`) and the bridge label. Product consequence to disclose (paper): incumbency stickiness through friendships — a subject's incumbent gains a D5 band as soon as its collaborators reach Close Friends, by design (§5.3 :428-429 "market preference"), thresholds hypotheses.

**Tasks.** T0 FIXTURES (646-T, in progress; the parent owns serialized runtime/evidence/publication): at HEAD `d6c11b9b` (final V30 behavioural writer `61833f0d`; bridge `027155e7`), BEFORE any source change, mint the genuine outgoing V30 corpus (empty root; bound open P1; tagged P2; kept-and-broken; rival current P1; a first-take world at `remainingTicks === 5`) and the genuine projection-47 runtime checkpoint (`sha256:6f6b4880…`), each with uncompressed/compressed sha256, byte lengths, seed, week, `MANIFEST.json` + `.provenance.json` on the `genuine-v29-pre-p2` pattern; independent KEEP; commit exact paths; push; `git ls-remote`. Record `git diff 61833f0d..HEAD -- src/core` (comment-only) in the record. Writable: `tests/fixtures/p14/genuine-v30-*/`, `tests/fixtures/p14/genuine-projection47-runtime/`, the minter test files, `scripts/`; frozen: everything else. T1 RED (test-author; after the audit): `tests/p14b5-relationships.test.ts`, `tests/p14b5-save-v31.test.ts`, `tests/bridge-p14b5-relationships.test.ts` (families 1–11), RED at module resolution / by value as stated; the R-D5 pre-declaration (family 12) as a record, not a test edit; the most-exposed D3 case checked. Writable: those three files + one evidence record; frozen: `src/`, `bridge/`, `generated/`, every existing test. Checks: `npm run typecheck` (expect exactly the new TS2307s), the three files single-worker (`node_modules/.bin/vitest run <files> --minWorkers=1 --maxWorkers=1`), record-check. T2 ENGINE + WIRE (sim-core, the ONE production writer; cumulative patches per step, never commits; the parent lands each through the index and verifies the diff against the patch — 600 §4): S1 core — NEW `src/core/relationships.ts` (mint, drift, tier rule, `pairChemistry`, root validator, `projectRelationshipsPreV31`, constants), `src/core/types.ts`, `src/core/tick.ts:1102-1110` (one call), `src/core/actions.ts:592-599` (one call); S2 chooser — `src/core/talentMarket.ts:651-678, 698-702, 779-816, 999-1032, 1057-1067`; S3 save — `src/core/save.ts` (types :499-537, `LIVE_SAVE_VERSION`/`makeSave` :6396-6405, dispatch :5282-5285, the 18 throw sites, the 4 route lines, the V31 block after :8717), `src/core/index.ts`, `src/core/worldgen.ts:783-785`, `src/harness/roster-wall/historical-control.ts:23-26`, `src/harness/p14/legacy-v28-fixtures.ts`, `src/harness/d16/run-d17b-*.ts`; S4 load/runtime — `bridge/session.ts`, `bridge/runtime-checkpoint.ts` (strict V31 + the registry line), `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts` (the `2cbc3de5` arms); S5 wire — `bridge/schema/bridge-schema.ts:233, 2320-2324`, `bridge/people.ts:866-875`, the generator run over its three artifacts. Then the SEPARATE values-only live-version sweep patch (30 → 31, 47 → 48, the schema-id and F10/F11 literals, the seven-member arrays, the registry count, the V13-twin strip) — ownership assigned by the parent (B.4 precedent: test-author, 600-T2; B.1 precedent: writer, `6948e31`); the production patch never contains a test file. Frozen for the writer: every file in scope (12) above, every test, every fixture, `TUNING`. T3 VERIFICATION (parent-run, one heavy process at a time, record-check on every run: `sourceSha`, `testedDiffSha256`, `fixedSource: true`): `npm run typecheck`, `npm run typecheck:bridge`, `npm run check:bridge-contract`, `npm run check:bridge-contract:fixtures`; the three T1 files single-worker; the natural-chain control files (the inventory above) single-worker with the R-D5 ledger filled per seed; the frozen-chain save tests (`tests/*save-v*.test.ts`, `tests/p14b4-save-v30-compatibility.test.ts`, `tests/bridge-p14b4-runtime47-compatibility.test.ts`). Any moved premise gets an explicit test-author reconciliation with the ruling recorded — never a weakened validator, assertion, timeout or fixture. T4 CLOSE: full core `node_modules/.bin/vitest run --project core` compared BY NAME against 636 (`evidence/p14b4-20260919/636-cancel-full-core.txt`: 9 failed files / 24 failed / 3813 passed / 8 todo on `df944978`; the 22 inherited + 2 designated); full bridge `npm run test:bridge`; full UI `npm run test:ui` compared against 643 (9 failed files / 28 failed / 2657 passed / 5 skipped + 1 unhandled error on `0e330da0`; the 643-C classification: deterministic set by name, timeout class by file, exit code not a comparator while the unhandled error exists); attribution against the inherited set; label LOGIC VERIFIED · UNITY NOT VERIFIED (never Owner acceptance, never all-green); the edge/driver counts at week 260 on the standard seeds recorded as the §8 :645 measurement; records; the backlog entry; commit exact paths; push; `git ls-remote`. Specialists: max two concurrently (test-author + sim-core at T1/T2 overlap only on disjoint files; contract-auditor read-only for the expansion audit and the T4 attribution review); one production writer throughout.

### P14B.5 tasks
- [ ] **T0 fixtures** — 646-T minting the genuine outgoing V30 corpus + projection-47 checkpoint at `d6c11b9b`; MANIFEST/PROVENANCE, independent KEEP, publication, `git ls-remote`.
- [ ] **Audit** — contract-auditor read-only over this expansion; disposition applied above before T1.
- [ ] **T1 RED** — three files, families 1–11; R-D5 pre-declaration; the D3 controlled case checked.
- [ ] **T2 engine + wire** — S1 core → S2 chooser → S3 Save V31 → S4 load/runtime → S5 projection 48; then the values-only sweep patch.
- [ ] **T3 verification** — typechecks, contract checks, T1 files, natural-chain controls with the ledger, frozen-chain save tests; reconciliations recorded.
- [ ] **T4 close** — full core vs 636, full bridge, full UI vs 643/643-C, attribution, LOGIC VERIFIED · UNITY NOT VERIFIED, records, backlog, publication.

**Unity backlog line (record-only; nothing native follows automatically).** Projection 47 → 48: the `StudioMarketPreferencesSnapshot.priorityOrder` JSON-schema enum gains `relationships` (the generated C# field stays `string[]`, `StudioBridgeDtos.Generated.cs:6492-6493`; a native switch over the values without a default would break); one new ordering-only settlement sentence appears in the existing `settlementReasons`/receipt `reasons` strings; new schema identity (minted by the generator at T2, recorded then), new `$id` projection-48, new generated C# sha and F10/F11 sentinel; the outgoing 47 identity `sha256:6f6b4880…` registered as `projection-v47` so durable 47 checkpoints take the governed prior path. Save V31: a new top-level `relationships` root (edges keyed by canonical person pair; no number is ever published); prior saves migrate with an empty root; a save holding any edge cannot be downgraded to V30. NO new DTO, view, intent or page; the profile relationship block, drivers text, chemistry rows, casting warning and attention rows arrive with B.6 as new DTOs and are the native items then. Coordinate the enum widening explicitly across TS and Unity in the entry, as records 616/628/637 did (`UNITY-INTEGRATION-BACKLOG.md:153-157`). Unity NOT VERIFIED.

**Evidence limits.** Paper only: no test, typecheck, probe, generator or measurement ran; the natural-chain direction (incumbent gains) is reasoned from `bandsFor`/`chooseProposal` and `decide()`'s roster reuse, not measured; the edge-count bound is arithmetic, not a run. Line cites are to HEAD `d6c11b9b`; the companion cites are to the Revision-3 file in this worktree (its own symbol cites are at `592e926` and are historical). 646-T's untracked minter files were not read; the T0 case NAMES are the record-645 intent list until the MANIFEST exists. No record ≥ 646 was read. Whether the Owner has ruled on record 628's three seating questions since 645 was not checked. `pairChemistry` consumption, the result owner's bound and the romance track were not designed; only the export shape and the exclusions are stated. The hypothesis constants carry no values here beyond the companion's own 52 / 260 and the cap 8; the writer names them and the tests pin them by name. No product decision is decided in this draft.
