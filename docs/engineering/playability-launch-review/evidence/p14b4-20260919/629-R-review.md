**629-R contract-auditor report — breakPromisesOnCancel causal-coupling checkpoint (READ-ONLY; nothing run, nothing edited)**

Status: DONE. Reviewed at HEAD b0486e63 (clean) in /Users/zacheryspector/The-Movies-headless-program. No changes made.

## Q1 — Landed source vs record 630 R1–R7 and 629-B C1–C6: MET WITH EVIDENCE, no deviation found

/Users/zacheryspector/The-Movies-headless-program/src/core/promises.ts
- R1/C1 formula, :661-667: `remaining = max(0, count − qualifyingTakes(state, promise).length)` (:662); `remaining === 0 → null` (:663); `t0 = max(windowStartWeek, expectedFirstTakeWeek(state, promise, week, 0))` (:664); `nMax = t0 < due ? ceil((due − t0) / WEEKS_TO_FIRST_TAKE) : 0` (:665); `remaining > nMax ? sentence : null` (:666). No loop. `SEAT_CYCLE_WEEKS` appears only at :58 and :369 (the k-term of `expectedFirstTakeWeek`, which vanishes at k = 0). No reservation, family, class, buffer, slack or receipt term.
- R2/C2 relevance, :785-787: `evaluable` (:593-595: outcome null ∧ contractId non-null) ∧ same issuer ∧ `promiseCastSlots(promise).some(slot => cancelled.cast[slot] === beneficiaryPersonId)`. No take-inside-window precondition.
- R3/C3: immunity :782 (`state.firstTakes.some(t => t.productionId === cancelled.id) → return state`) precedes the loop; proof called on `state` (:788), not `next`. Cause :791 and reason :793 are byte-identical to the RED constants authored on cb13a457 (:78, :546) — proven by record 631 (unamended RED 16/16 on the landed source). `settle` :672-694, `advancePromisesWeek` :706-746 (due cause :736, reason :738), `breakPromisesOnTermination` :753-767 (cause :762), `promiseFeasibility` :388-447, `reclassifyPromise` :453-465 read and consistent with 629-B's pre-writer descriptions and every RED string pin.
- R4: :352-357 parameter is `Pick<PromiseDraft, 'issuerStudioId' | 'beneficiaryPersonId' | 'predicate'>`; callers :422/:432 pass a full `PromiseDraft`, :664 passes a `ProfessionalPromise`; typecheck 632 EXIT 0.
- R5: `targetSpecificImpossibility` exported from promises.ts only; grep of src/bridge/ui/scripts/tests finds it in promises.ts :661/:788 and the RED; src/core/index.ts :1405 re-exports only the pre-existing `breakPromisesOnCancel`.
- R6: /Users/zacheryspector/The-Movies-headless-program/src/core/actions.ts :591-595 five comment lines describing the landed rule; call :599 `breakPromisesOnCancel(withoutProduction, playerStudioId, cancelled)` unchanged; `cancelled` is the pre-removal production (:574), `withoutProduction` the post-cancel state (:579-590).
- R7: PROMISE_RULES_VERSION 4 (promises.ts :45), LIVE_SAVE_VERSION 30 (save.ts :6396), PROJECTION_VERSION 47 (bridge/schema/bridge-schema.ts :233).
- C4 doc :641-660 states M16 one-seat exclusivity (`assertGreenlightStaffingIdle`), fixed cast (no recast verb), floor on take weeks hence ceiling on count, "deliberately looser than the quote's SEAT_CYCLE_WEEKS hypothesis", window-start floor rationale, no reservation/family/class/FRAGILE/receipt. Body doc :769-778 matches.
- C5: NOT VERIFIED at byte level by me (no git); content-verified by reading plus 631/635 (p14b1-promises 15/15 incl. the :780 capacity-collapse cancel pin; outcomes 23/23). 629-W lists hunks @@-350,5 / @@-634,4 / @@-736,8 / @@-746,10 only.
- Independent soundness of the bound: `expectedFirstTakeWeek` k = 0 with no running in-mask picture returns week + 5 (:369); post-cancel the cancelled picture is gone (actions.ts :583-585) and M16 forbids a second running seat, so t0 = max(windowStart, week + 5) is the earliest qualifying take regardless of whether a hold at ticks 5 is possible (an unheld picture greenlit before the window would take outside it and not qualify: `qualifyingTakes` :634). Later takes need a fresh greenlight ≥ the previous take week, so t0 + 5k is a floor. ceil arithmetic checked at due − t0 ∈ {1, 5, 6}: 1/1/2.

## Q2 — Amended RED soundness: MET WITH EVIDENCE

/Users/zacheryspector/The-Movies-headless-program/tests/p14b4-cancel-causal-proof.test.ts (765 lines)
- Case 5N :631-661 (arithmetic): `took('support')` real take at 30 with P in support; tagged lead count 2, window [21,38); `qualifyingTakes` = [] asserted :642; remaining 2; t0 = max(21, 35) = 35; nMax = ceil(3/5) = 1; 2 > 1 → BROKEN at 30. Case 5 (:498-511) differs only in A's seat (lead → remaining 1 ≤ 1 → untouched). A class-blind count would give remaining 1 → untouched → 5N fails. Discriminating.
- Separator :665-690: window [21,33), count 2, P lead on the greenlit-this-week picture. Quote: k=0 → 26 < 33, k=1 → 21+8+5 = 34 ≥ 33 → 1 (asserted :674); hard: t0 = 26, nMax = ceil(7/5) = 2 ≥ 2 → untouched (:684-685), due-week BROKEN at 33 (:688). Pre-cancel premise `reclassifyPromise` IMPOSSIBLE/PHYSICAL_BOUND (:679) confirms the quote refuses; the pre-writer coupling (reclassify on the post-cancel state, same nMax 1 < 2) would BROKEN here. Discriminates A from B.
- D1 :695-717: window [23,28), cancel at 21. Hard: t0 = max(23, 26) = 26 < 28, nMax 1 ≥ 1 → untouched; quote post-cancel: from = 23 → 28 ≥ 28 → IMPOSSIBLE (:711, the pre-writer BROKEN path; also the unclamped note's t0 = 28). Discriminates the clamp. Mirror :719-733 ([23,26): t0 26 ≥ 26 → nMax 0 → BROKEN) pins the boundary in the windowStart > week shape; it does not by itself separate clamp variants (all give BROKEN there) — the [23,28) case does.
- Direct pins :738-753 at t0 = 26: count 1 due 26 → sentence, due 27 → null; count 2 due 31 → sentence, due 32 → null; `hardBound` 0/1/1/2 (:752). Remaining 0 :755-764: real lead take, count 1, due = week + 1 → null. All correct boundary values.
- Relabel: the 633 patch's removed lines (patch :9-51 header comments, :97-98 helper doc/name, :118-146 `took` builder, ten `nMaxAfterCancel(` → `quoteNMax(` with values 1,1,2,0,2,0,0,1,0,0 unchanged; :140 `cast: { lead }` → `cast: { [seat] }`, byte-equivalent at the default). No expectation line changed; nothing weakened. 16 pre-T2 cases pass unchanged (631 → 633).
- Nothing synthesized: roots via `bind` :240-267 with the real employment row and `lawful()` :269-271 (validateSaveV30); takes only from the real 5→4 (:209-231); outcomes only via `advancePromisesWeek`/`tick`/actions. `hardBound` :105 is test-local arithmetic on exported constants.
- ":755" trivia fixed: header :59 now names the landed :791 literal (byte-identical to pre-writer :756). Landed :791 is the cause line.

## Q3 — Evidence chain: MET WITH EVIDENCE, one parent-side byte check outstanding

- 629 baseline: cb13a457, testedDiff 104739c5…, untracked RED, exit 1, 7 failed / 9 passed, all seven at `untouched` (:267), fixedSource true.
- 631: sourceSha 61833f0d, testedDiffSha256 e3b0c442… (sha256 of empty), untracked [], exit 0, 16/16, start 19:43:51Z — precedes the T2 run (19:52:00Z per the T2 report) and 633 (19:55:09Z); 16 = the 592-line file's count.
- 632: same identity, `tsc --noEmit && tsc -p ui/tsconfig.json --noEmit`, exit 0.
- 633/634/635/636: sourceSha df944978 with testedDiffSha256 03488791… = the T2 tests-only patch (index fb62065..e0e5f1d, same in all four .patch files), fixedSource true. This is NOT an empty diff: the RED was amended-but-uncommitted at run time and committed afterwards as 885ef07. Correct sequencing (candidate captured against the unamended RED first), but the committed bytes must equal the tested diff — parent check: `git rev-parse 885ef07:tests/p14b4-cancel-causal-proof.test.ts` should begin e0e5f1d, and `git diff --stat 61833f0d HEAD -- src bridge ui` should be empty.
- 634 vs 624: 624 was 12 files, 1 failed / 192 passed / 1 todo; 634 is 14 files (adds rival-seating-preference 21 + 1 todo, p14bf2-acting-discipline 13), 1 failed / 226 passed / 2 todo; 192 + 21 + 13 = 226. The single failure is p14b4-cast-class-capacity-evaluator5 :239 `expected 'FRAGILE' to be 'IMPOSSIBLE'`, identical in 624. Chain digests 2aec0184… / d3218295… identical across 622/626/627/634/636.
- 635 vs 625: both 16 files, 177 passed / 2 todo (179); same file list.
- 636 vs 627: 9 failed files / 24 failed tests in both, same nine files with the same per-file counts (bridge-p12-campaign-library 11, p14b4-ready-replay-stale-target 1, evaluator5 1, r3n1 ×3 files 2 each, bridge-p13-campaign-isolation 1, p13a-scientist-foundation 3, world-first-scenery 1) and the same assertion values; 3791 → 3813 passed (+22 = the RED, 636 :34), 345 → 346 files, 8 todo both.

## Q4 — Invariants: MET WITH EVIDENCE

Only caller actions.ts :599 (grep src/bridge/ui/scripts/tests). `kind: 'cancel'` only in src/core/actions.ts :567/:2860 and types.ts :2289; no rival code path; `breakPromisesOnCancel` is called with `hollywood.playerStudioId` only. Due-week/termination owners and immunity: read (Q1) and pinned green (RED cases 1 tails, 4 tails, 7a, 7b; p14b1-promises :780; outcomes :488 re-pin). No test outside the RED pins either cancel string or calls the helper. Bridge tests ran under `--project core` in 634/635/636 (vitest.workspace.ts: core = tests/**, ui = ui/**). The ui project was NOT run; the only ui use of the cancel verb is ui/src/screens/a3-queue-admission-ui.test.tsx :288 on a fixture with no promise roots (grep: no `state.promises`/`ProfessionalPromise` in ui), so the correction is unreachable there by construction; ui typecheck is covered by 632. No bridge/ui run is required for this checkpoint.

## Q5 — Owner-visible behaviour (source facts, paper)

Changes: when the studio cancels a picture that has not filmed, each open promise to a person seated on it in a seat the promise counts (lead for a lead promise; lead or antagonist for lead-or-antagonist; any cast seat for a count-only root of any family) is judged at once and marked BROKEN with the cancel cause only if no schedule can still deliver the remaining count before the due week (first take no earlier than max(window start, now + 5), later takes 5 weeks apart). Otherwise the cancel records nothing and real takes or the due week decide. No longer broken at the cancel: a joint reservation conflict, an unsupported family (directing/genre/named-project), a legacy count-only P2 (previously refused for lacking a class), and a count ≥ 2 root the quote's 8+5 cadence would refuse but the 5-week chain admits. A support-seat cancel under a lead-class promise is not judged at the cancel. Unchanged: cause strings (:736/:762/:791), receipt reasons, one own receipt per outcome, trust driver kinds (:837-838 read outcome only; `promiseBroken` dated `outcomeWeek`, so its week moves in the cases that changed), save/wire/versions. No user-facing copy or wire field changes. Source fact: no bridge or ui route builds a production `cancel` action (bridge cancels are cancelInstallation/cancelAdoption/cancelResearch/cancelPhysicalPlan; session.ts :965/:1122-1165 apply pre-built row actions), so today the seam is reachable only through engine `applyActions` (harness/tests). Unity player not inspected.

## Q6 — RECORD-ONLY / REFINE carry

Carried from 629-B/630/T2: RED comment line drift (:414 validator refs now ~:1027-1039; :422 `promiseCastSlots` now :597-602); 26 §2 drift (`breakPromisesOnCancel:656` → :779; `:739-746` → actions.ts :567-600); DIRECTING_COUNT judged over generic cast (a director sits in `directorId`, never in `cast`, so a director's root is never relevant here and never qualifies — due-week only); FRAGILE placeholder receipts on labeled bound roots; cancel attribution for the Owner (5N and D1m were already impossible before the cancel; the seam credits the cancel); the 5-week two-take witness is unbuilt (fixture take at +9; the bound is optimistic relative to this fixture's pipeline, the safe direction).
New: (1) promises.ts :449-452 doc on `reclassifyPromise` still says the §4.4 studio-caused step shares it; it now has no production caller (grep: definition :453 and the RED only) — frozen region, next comment sweep; keep the export (RED premises depend on it). (2) tests/p14b1-promises.test.ts :578 and :780 describe the pre-writer "re-feasibility IMPOSSIBLE" coupling; assertions still lawful (premise on `promiseFeasibility`, BROKEN within one tick). (3) 629-B Q6 said "17 files" for the 625 set; both .txt files show 16. (4) The 633-636 identity carries the T2 diff, not an empty diff (Q3 check). (5) The direct-pin block transcribes the same arithmetic as the source; the separator/D1 behavioural cases are the law-derived discriminators.

## Q7 — Verdict: QUALIFIED WITH RECORD-ONLY ITEMS

No DEMONSTRATED defect; no REFINE item blocks. Independent technical assessment only; not Owner acceptance, not a merge instruction.

## Rulings for the parent
- Land nothing further for this checkpoint; write the qualified-checkpoint record citing 631/632 (committed candidate, unamended RED) and 633-636 (candidate + T2 diff 03488791…).
- Before sealing, run: `git rev-parse 885ef07:tests/p14b4-cancel-causal-proof.test.ts` (expect e0e5f1d…) and `git diff --stat 61833f0d HEAD -- src bridge ui` (expect empty).
- Record the six carry items in Q6; schedule the :449-452 and p14b1 :578/:780 wording for the next comment sweep, not now.
- Cancel-attribution and the 5-week floor vs the fixture's 9-week route stay Owner/record items; no code change implied.

## Sources read
src/core/promises.ts :37-58, :88-92, :205-217, :272-294, :330-465, :593-796, :816-896; src/core/actions.ts :567-600; vitest.config.ts; vitest.workspace.ts; tests/p14b4-cancel-causal-proof.test.ts (all); tests/p14b1-promises.test.ts :572-583, :780-838; ui/src/screens/a3-queue-admission-ui.test.tsx :265-309; bridge/session.ts :955-979; bridge/contract.ts :240-264; plan P14B4-HEADLESS-PLAN.md :236-259, :322-357; 26-live-cutover-implementation-map.md :60-99; records 629-B, 630, 629-W, 629-T2; evidence 629/631/632/633/634/635/636 .json and .txt, 624/625/627 .txt, 629/633 .patch; greps listed inline.

## Evidence limits
Paper only: no vitest/tsc/git run. Byte-identity of the frozen promises.ts regions and of the committed RED to the tested T2 diff is inferred from records and content reading, not from a diff I produced. Bridge/ui non-reachability of the `cancel` verb is grep-based. The Unity player identity was not inspected. No native playtest; a green suite is not usability.