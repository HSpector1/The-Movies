## 672-R — Independent contract audit of the P14B.5 First Shared-Work Bond Core checkpoint
READ-ONLY. Worktree `/Users/zacheryspector/The-Movies-headless-program`, HEAD `df693294`, candidate source `5f116403`. I ran nothing: every runtime claim below is a source read or a re-read of the archived record-check `.json`/`.txt`. Not Owner acceptance.

### Q1 — Does the landed source implement exactly Scope engine (1)–(12), Scope bridge, and C1–C10?

**MET WITH EVIDENCE (each re-derived from source, not from the reports):**

- **(1) root shape / placement** — `src/core/types.ts:2260-2284`: `RelationshipEdge` field-for-field the plan `:739-748`; `GameStateV31 = GameStateV30 & { relationships }` at `:2276-2280`; `GameState = GameStateV31` `:2284`. Top level, no second version field. `src/core/worldgen.ts:787` seeds `[]`.
- **(2) one tail seam, delta-fed, `week = state.market.tick`** — `src/core/tick.ts:1117-1121`, between `appendFirstTakes` (`:1107`) and `advancePromisesWeek` (`:1122`), fed `takeEntries` plus `records.map(r=>r.filmResult)` + `industry.growth.map(r=>r.filmResult)`. `records` is this advance's release list only (`tick.ts:524` init, `:762` push inside the per-release loop) — a true delta, never a root scan. Guard order copied from `appendFirstTakes`: empty delta → **same reference** before the root guard (`relationships.ts:264`), then loud guard `:265`, then `hollywood === null` → unchanged `:266`. C6 met.
- **(2c) cancel seam** — `src/core/actions.ts:597-603` one call beside an untouched `breakPromisesOnCancel`; `relationships.ts:312-322` returns the same reference when no take exists (`:313-314`), throws loudly on a foreign take (`:317`), stamps `state.market.tick` (`:320`).
- **I2 precision + the counter invariant** — `relationships.ts:233-243`: release/cancel drivers never create an edge (`if (i === undefined) continue`) and apply only when `take.week >= edge.firstSharedWeek` (`:238`). I re-derived the invariant: every take with `week ≥ firstSharedWeek` was counted by the mint step, so `sharedSuccesses + sharedFailures ≤ sharedProductions` holds by construction; validator enforces it at `:453-454`.
- **(3) drift/tier** — `currentCloseness` `:114-119` is the plan formula verbatim (`trunc`, `min(dormant−GRACE, RETURN)/RETURN`, never past baseline); named floors `:51-53`; evidence condition (Nemeses/Enemies → Strained) `:131-134`; write order drift→delta→clamp→anchor→peak-only-if-higher→fold `:169-181`.
- **(9) fold-by-count** — `RELATIONSHIP_RECENT_CAP = 8` with the required `ponytail:` upgrade-path comment `:85-88`; counters are the fold target `:155-163`.
- **(5) D5 + D1 roster predicate** — `talentMarket.ts:794-801` `rosterAt` is the strict predicate verbatim (`startWeek < week && (endedWeek === null || week < endedWeek)`), subject excluded `:797`; used by both the band `:842-844` and the reservation `:1107`. No `relationships.ts → talentMarket.ts` import (`relationships.ts:26-29` imports `./types.js` only). C7 met.
- **strict-every-survivor sentence rule** — unchanged at `talentMarket.ts:911-916`; the eight frozen sentences are byte-identical where the record quotes them (`:913`, `:916`, tie `:1186`).
- **exactly one new reason / one new drop sentence** — `talentMarket.ts:680` `"their roster holds this person's close ties"`; `:1068` `${studio}'s roster holds someone this person will not work beside.` Neither carries a digit, person id or tier name. C5 met (the "no other string changed" half is paper-checked against the record's own quotes; I have no shell to diff).
- **(6) `nemesisOnRoster`** — `FreezeDrop` member `:1046`, total `DROP_SENTENCE` `:1068`, checked immediately after `issuerDistrusted` and before `subjectCommittedElsewhere` (`:1104 → :1107 → :1108`). Unreachable by rule.
- **(7) `pairChemistry` exported, no consumer** — grep: only `src/core/relationships.ts:357`, the `index.ts:1446` re-export and the two RED files. Confirmed.
- **(8) determinism** — no RNG symbol anywhere in `relationships.ts` (whole file read); iteration is seat order/array order for identities only; copy-on-write ledger `:219-226`.
- **(10) validator + projection + V31 chain** — `relationships.ts:389-471` (exact keys, ordinal `edgeId` `:437`, `a<b` `:440`, people-of-this-world `:426-430`, `recordedWeek` against `studioHistory.recordingStartedWeek`/`market.tick` `:417-424`, counter laws `:453-454`, catalogues, cap `:459`, `driver.week ≤ lastEventWeek` `:466`); `projectRelationshipsPreV31` refuses any held edge `:479-485`. Chain re-derived independently: `save.ts:8759` strip, `:8770-8783` validate-own-root-then-strip-and-delegate, `:8792-8798` convert **up** with an empty root and insertion-order clone (C3), `:8808-8819` convert **down** asking the projection **before** validating, `:8823-8825` `migrateToV31`, `:5295/:5297` dispatch + "1 through 31 only", `:6409` `LIVE_SAVE_VERSION = 31`, `:6413-6417` `makeSave` validates before the detach, 18 downgrade throws (`:7330…:8205`, `:8846`), 4 route lines (`:8370/:8522/:8604/:8703`) plus the direct `:8749`; `migrateToV4-7` refuse 31 through their existing `saveVersion > N` guards (`:7263`, `:7284`, …).
- **Scope bridge, thin** — `bridge-schema.ts:240` projection 48, `:2331` the closed enum gains `relationships` (order-free); `bridge/people.ts:877` label added, `:994` `line` still reads `[0]/[1]` so the published line value cannot move; `bridge/runtime-checkpoint.ts:63` outgoing `sha256:6f6b4880…` registered `projection-v47`; generated artifacts carry `sha256:00c0075b…` / `projectionVersion 48` (`generated/unity/StudioBridgeDtos.Generated.cs:3,20`; `…contract-manifest.json:6,8`); schema `$id` `…projection-48` (`project-studio-bridge.schema.json:16365`). No relationship DTO anywhere.
- **Frozen list (12) intact where checkable** — `PROMISE_RULES_VERSION = 4` (`promises.ts:45`); the settlement order, seating law, `authorRivalPromise`, TUNING and every pre-V31 validator show no relationship touch (grep: no `relationships` symbol in `promises.ts`); `historical-control.ts:23-24,:27,:31-33,:47` strips the empty root behind a loud never-discard-authority guard.

**No OPEN item silently decided.** OPEN 1 stays open through the pass-through `driverGain` (`:147-150`); OPEN 2 through the empty lift + delta-only mint; OPEN 3 — no consumer exists; OPEN 15 — `supersededByPromiseId` absent from `src/` (grep: 0). **OPEN 11 note (RECORD-ONLY, not a decision):** the landed precedence gives close ties priority (`talentMarket.ts:843-844`), which is scope (5)'s literal sentence, while OPEN 11's *candidate* is the opposite; the branch is unreachable and the code comment names it.

**Deviations found: none.** One classification refinement: plan (9)'s "weekly cost proportional to this week's events" is met only up to a history factor — `openLedger` copies the whole edge array and rebuilds its Map on any non-empty delta (`:220-223`), `state.firstTakes.find` runs per release (`:297`) and per cancel (`:313`), and `bandsFor` scans all edges per proposal (`tiersOnRoster` `:332`). No population scan and no weekly pair scan (§8 :644 holds), storage bound holds. PARTIAL / PERF-010, not a defect.

### Q2 — The 662-T2/T2b sweep (121 files)

Spot-checked against the files, not the table: `tests/contracts/_v14Contract.ts:418-423` (V13 twin refuses to discard non-empty relationship authority, mirroring `firstTakes`/`promises`); `tests/helpers/p14b2-fixtures.ts:7,:122,:210,:225` moved to `validateSaveV31` while the `poachingFixture` week-208 `reasons` pin at `:200-201` is byte-unchanged (two frozen sentences, no D5) and `endedWeek === 208` at `:204` holds; `tests/p14b1-save-v29.test.ts:109-110,:197-201` sentinel moved 31→32 / "1 through 31 only" with every V28→V29 boundary call untouched; frozen-corpus readers kept on their own frozen validators (662-T2b §"Frozen calls deliberately left", corroborated by the surviving `validateSaveV30(`/`migrateToV30(` sites in `p14b5-save-v31`, `p14b5-relationships`, `p14b4-save-v30-compatibility`). Every move I sampled is R-VERSION or R-ORDER-VALUE; **no assertion weakened, no meaning lost, no frozen validator re-pointed.**

- **B4 (epoch-boundary actor, 208)** — LAWFUL. `TUNING.HIRING_MARKET_ROTATION_WEEKS = 13`, 208 = 16×13, so the 207 listing is resampled at 208; selecting the actor listed in **both** epochs fixes an unmeasured premise without touching the law assertions, and the `:851` guard on the real pre-market input is retained. It changes which actor the probe signs, not what the probe proves.
- **B5 (DECLINED control reads `caseDisclosure`'s own `settlementReasons`)** — LAWFUL and strictly more honest: `caseDisclosure` publishes only the latest *settled* receipt, so `[]` on the wire for a declined case is the wire's law; the tie sentence stays asserted on the receipt (`:412-413`), and the treated branch's verbatim D5 sentence pin is untouched.
- **Agent death (API 529)** — handled soundly on the evidence available: the continuation verified the inherited tree hash `e2c8f631…` against the parent's partial patch before editing, re-audited all 65 non-precedent blanket lines plus 19 boundary files, and produced the final patch `8ed90a18…` (258032 bytes, 121 files, `tests/`+`ui/` only). I cannot re-hash either artifact (no shell) — taken from the report plus the fact that `664`/`670` typechecks and `663`/`669` runs are green on the landed bytes.

### Q3 — The strip class (668-T3, five files) and 668-B's verdict

All eight re-expressed premises read **preserved or strengthened**, and every device is lawful:

- `tests/facility-move-demolish.test.ts:838-839` — assert `[]` **then** delete, byte-identical to the file's own V29 chain at `:829-832`; the guard under test (`:846-848`) is still the first violation reached.
- `tests/p13b-r07-save-v25.test.ts:175-176` — same device inside `asV25Envelope`, beside its `talentMarket`/`firstTakes`/`promises` lines.
- `tests/p14b1-first-take.test.ts:288-290` — the move **up** to `validateSaveV31` is forced, not convenient: the asserted take mints edges and `convertV31ToV30` lawfully refuses any world holding one (`save.ts:8808-8819`); the added runtime premise `expect(afterFirstWeek.relationships.length).toBeGreaterThan(0)` records that fact instead of asserting it in prose, and the byte-stability claim at `:297` is unchanged. The removed `:102-107` shim carried no assertion.
- `tests/bridge-owner-ux-projection20-migration.test.ts:120,:133` — strictly stronger: destructure **and** pin `[]`, with the 30-root deep equality at `:139` unchanged.
- `tests/bridge-p13b-r07-setup.test.ts:587` — plain R-VERSION 30→31, kept as a literal so the next bump is visible to the sweep.

**668-B's PRODUCTION PATH COMPLETE verdict: I concur, re-derived from source** (chain, strip symmetry, order of projection-before-validate, dispatch/sentinel/`LIVE_SAVE_VERSION`/`makeSave`, 18 throws, 4+1 route lines, the `>N` guards on V4–V7, every live load/write route on `migrateToV31`: `bridge/session.ts:136`, `bridge/runtime-checkpoint.ts:854`, `bridge/runtime/campaign-library.ts:48,:127`, `ui/src/engine/adapter.ts:3796,:3808,:3822`, `src/harness/d16/run-d17b-*.ts`). **No load/save/checkpoint route can hand a live state to a pre-V31 validator.** Its one PARTIAL (frozen builders project positively and have no `relationships` retention assert) is correctly classified as inherited from V23 and closed today by `assertFrozenBuilderRetainsHollywood` plus the `hollywood === null` precondition on both mint seams.

### Q4 — Evidence chain and reconciliation

Every record-check I opened carries the claimed identity and `fixedSource: true`, with `testedDiffSha256` = the empty-string sha256 (clean tree): 655/656 on `8708d6a9` (3 files, **37 failed / 35 passed**; typecheck EXIT 2 with exactly **8** errors — 6×TS2724, 1×TS2307, 1×TS2322 `FreezeDrop`); 659/660/661 on `040651b4`; 663/665/666/667 on `8e03ff97`; 664 on `8e03ff97`; 669/670/671 on `5f116403`.

**669 = the 636 baseline set exactly.** Nine files, identical per-file counts in both runs: `bridge-p12-campaign-library` 11 (669:1418 / 636:1331), `p14b4-cast-class-capacity-evaluator5` 1 (669:1686 / 636:1580), `r3n1-stale-schedule-take-02` / `-02p31` / `-02p32` 2 each, `p13a-scientist-foundation` 3 (669:2313 / 636:2198), `world-first-scenery-load-in-provenance` 1, `p14b4-ready-replay-stale-target` 1 (669:1555 / 636:1457), `bridge-p13-campaign-isolation` 1 (669:2167 / 636:2054) = 24. Both designated failures still designated.

**The "+141 passing" in commit `df693294` is wrong — DEMONSTRATED.** 636 = 3813 passed / 3845 total (`636-cancel-full-core.txt:2758-2759`); 669 = 3932 passed / 3964 total (`669-b5-full-core-final.txt:2873-2874`). The delta is **+119**, fully accounted: **+118** from the three new RED files (`p14b5-relationships` 46, `p14b5-save-v31` 57, `bridge-p14b5-relationships` 15 — 669:11, :1356, :679) and **+1** from `it.each(Array.from(SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS.entries()))` at `tests/bridge-runtime-checkpoint.test.ts:758`, whose registry grew 35 → 36 (62 → 63 tests). Nothing is unexplained; only the record line is.

666 → 669 reconciles exactly: total 3964 in both, 32 → 24 failures, 14 → 9 files — the 8 strip-class failures closed and **no test added or removed** by 668-T3. 671: 24 files, 324 passed / 4 todo, EXIT 0, same 328 total as 665 (1 failed / 323 passed) — the single 665 failure was the `p14b1-first-take` strip item; the CANNOT-MOVE inventory of `654-T-ledger.md` (four seed digests + `rngState(416)` at :113/:165/:225/:285, `poachingFixture` :75, the D3 case :86) is carried by the family-12 ledger inside `bridge-p14b5-relationships` (15/15 in 669) and the control set, none re-pinned. 667 UI: same nine files as 643, same 2690 total, 26 vs 28 failures (`WorldInspectorDefault` 8 vs 10 — within 643-C), **and zero "Unhandled" occurrences** vs 643's unhandled-error block — an improvement, not a regression.

### Q5 — Owner-visible behaviour

**Changes for a player:** offers from a studio whose roster already holds this person's close collaborators now rank higher (D5 is the 5th descriptor and sits at companion positions 3/4 in the two tie orders, `talentMarket.ts:670,:702-705`), and when that band strictly beats every other survivor the settlement receipt gains exactly one sentence, "their roster holds this person's close ties". The save is V31 and the projection 48; the `priorityOrder` wire array has seven members. Bonds are minted only from this week's shared first takes, release success/failure and the player's cancel-after-take; they drift toward baseline on read; **no read model shows them in B.5** — no profile block, no drivers text, no chemistry row, no warning.

**Does not change:** any existing reason, drop or tie sentence; the published `preferences.line` (the label sits at index 2/3, `people.ts:994`); trust, the promise law, the cancel proof, seating, the settlement order; `rngState` (no RNG on any relationship path).

**Wire/native impact (the backlog line):** exactly two things — the closed `StudioMarketPreferencesSnapshot.priorityOrder` enum gains the value `relationships` (generated C# stays `string[]`, so a native `switch` without a default would break), and one new free-text value can appear inside `settlementReasons`. Plus the new identities: `sha256:00c0075b…`, projection 48, Save V31, outgoing `6f6b4880…` registered `projection-v47`, registry 36 ids. **The landed backlog entry has not been written yet** — `UNITY-INTEGRATION-BACKLOG.md:159,:161` are the pre-landing forward-looking lines (record 645/653).

### Q6 — Record-only / REFINE to carry, plus new findings

1. **NEW — the negative tier has no natural producer under the landed constants (paper calculation, DEMONSTRATED on arithmetic).** A freshly minted LOW-proximity edge starts at `50+2 = 52`; one flop → 48; each further shared production adds `+2 + min(n−1,3)` against a `−4` flop, so the trajectory is 52→48→51→47→51→47→52→48→… **Minimum reachable = 47**, and the Strained band needs `< 45`. MID/HIGH start higher; cancels are net-positive (`+2` take, `−2` cancel, plus the accelerator); drift only pulls toward 50. So plan (4)'s "through repeated flops, the value crosses into the Strained band" does **not** hold with `BASELINE 50 / PROXIMITY_LOW 2 / FAILURE_DELTA 4 / REPEAT_CAP 3`; the only witness is the staged Acquaintances-floor edge at `tests/p14b5-relationships.test.ts:674`. No writer defect (the code implements the named hypotheses, and the one *pinned* relation `FAILURE_DELTA > PROXIMITY_LOW` is satisfied) — this is a hypothesis-value item for Fable/the Owner, and it is the concrete form of the "unbuilt negative-tier witness".
2. **§8 :645 measurement still outstanding** — the week-260 edge/driver counts and "is Strained ever read on the standard seeds" are T4 deliverables and are **not** in the 655–671 evidence. 658-W's observation (rival fixed teams saturating at closeness 100 / Inseparable, ~24 edges by week 207 on `p13-public-commercial-adoption`) is an observation, not that measurement; combined with finding 1 it says the landed constants are asymmetric (positive saturates, negative cannot fire).
3. **Idempotency is bounded by the `recent` cap** — "idempotent by (edgeId, kind, ref)" is enforced by scanning `recent` only (`relationships.ts:198-199, :239, :281`), which holds ≤ 8 drivers. Unreachable today (one take, one release, one cancel per production), but the guarantee degrades if a driver folds out before a duplicate delta arrives. One-line record.
4. **Plan-text correction still unapplied** — `P14-HEADLESS-PLAN.md:757` still reads "the synchronized churns (weeks 208 and 404…)"; the ledger measured cases **open at 404, settle at 416**. Carried since 657; no prediction changes.
5. **657-B REFINE (a) and (b) are CLOSED by a later correction** — the substring probe `String(closeness) + '"'` was replaced by the `"closeness":` key form and the literal `8` by `RELATIONSHIP_RECENT_CAP` (662-T2b §B item 3). Record them as done; they no longer stand as open refinements.
6. **Cost/PERF-010** — per-week work is O(existing edges) whenever the delta is non-empty, plus O(`firstTakes`) per release/cancel, plus O(edges) per proposal in `bandsFor`. The `ponytail:` upgrade note exists for the compaction; a second note for the hot/cold split or an id-indexed take lookup would be the honest place to record this.
7. **668-B's frozen-builder PARTIAL** — carry as recorded: closed today by `assertFrozenBuilderRetainsHollywood` plus both seams' `hollywood === null` precondition, and it would reopen for `physicalPlans`/`talentMarket`/`firstTakes`/`promises` at the same moment.

### Q7 — Verdict

**QUALIFIED WITH RECORD-ONLY ITEMS.** No production defect is demonstrated in the candidate: the engine, chooser, Save V31 chain and projection-48 wire implement the expansion as written, decide no OPEN item, touch nothing on the frozen (12) list, and the full-core result is the 636 baseline set exactly with the three new RED files green. The items to record before closing are (i) the wrong "+141 passing" figure in the checkpoint commit (true delta +119, accounted above), (ii) the §8 :645 measurement and the negative-tier reachability finding, (iii) the outstanding backlog entry for the landed identities, (iv) the evidence-limit items below. Nothing here is Owner acceptance, and this is LOGIC-only: nothing native, nothing visual, no playtest.

### Rulings for the parent

1. Correct the record line: 669 vs 636 is **+119 passing** (3813 → 3932; totals 3845 → 3964) = 118 new RED tests + 1 `it.each` case from the 35 → 36 prior-id registry (`tests/bridge-runtime-checkpoint.test.ts:758`). Do not publish "+141".
2. Record 667 (full UI) as run on `8e03ff97`, not on the final `5f116403`; lawful because 668-T3 wrote only `tests/*.test.ts` in the core project, but the UI comparison **at the final source is NOT VERIFIED**. Either state that or re-run `npm run test:ui` on `5f116403`.
3. Record that `npm run test:bridge` was not run as one command; its three components were satisfied on the final source (670 `check:bridge-contract` + `:fixtures` EXIT 0; `tests/bridge*.test.ts` inside 669's `--project core` run). If the T4 line requires the literal command, run it.
4. Accept the 662-T2/T2b sweep and the 668-T3 strip class as designated test-side classes with zero writer defects, and record B4/B5 as the two measured RED-side premise amendments (neither decides an OPEN item).
5. Carry to the Owner/Fable, separately from acceptance: the negative-tier unreachability under the landed constants, the rival fixed-team saturation to Inseparable, and the still-open OPEN 1–3/11/15. These are hypothesis-value questions, not defects.
6. Apply the "404 → open at 404, settle at 416" plan-text correction at `P14-HEADLESS-PLAN.md:757`, and mark 657-B REFINE (a)/(b) closed by 662-T2b.
7. Write the landed backlog entry (Save V31, projection 48, `sha256:00c0075b…`, outgoing `6f6b4880…` as `projection-v47`, 36 prior ids, enum + one reason value; nothing else native).

### Sources read
`src/core/relationships.ts` (whole), `src/core/save.ts:508-549, 5295-5297, 6409-6417, 7256-7290, 7330-8205, 8370/8522/8604/8703, 8740-8826`, `src/core/tick.ts:618-650, 752-767, 1095-1123`, `src/core/actions.ts:575-604`, `src/core/talentMarket.ts:655-706, 780-848, 876-917, 1020-1135`, `src/core/types.ts:2228-2290`, `src/core/index.ts:1435-1471`, `src/core/worldgen.ts:787`, `src/harness/roster-wall/historical-control.ts:20-47`, `bridge/schema/bridge-schema.ts:233-240, 2322-2335`, `bridge/people.ts:863-878, 991-997`, `bridge/runtime-checkpoint.ts:45-75`, `generated/unity/*` identities, `tests/facility-move-demolish.test.ts:820-849`, `tests/p13b-r07-save-v25.test.ts:160-178`, `tests/p14b1-first-take.test.ts:275-298`, `tests/bridge-owner-ux-projection20-migration.test.ts:112-141`, `tests/bridge-p13b-r07-setup.test.ts:580-591`, `tests/contracts/_v14Contract.ts:414-428`, `tests/helpers/p14b2-fixtures.ts:193-212`, `tests/p14b1-save-v29.test.ts:109-201`, `tests/p14b5-relationships.test.ts:655-704`, `tests/bridge-runtime-checkpoint.test.ts:758-761`; law: plan `:723-763` (scope, OPEN, tasks — read in fragments; `:725-737`, `:753`, `:755`, `:757`, `:759`, `:761`, `:763`), `657-p14b5-red-review-writer-release.md`, `657-B-b5-red-review.md`, `654-T-ledger.md`, `658-W-report.md`, `662-T2b-report.md` + audit table, `668-T3-report.md`, `668-B-v31-strip-chain-audit.md`; evidence `655`, `656`, `659`, `660`, `661`, `663`, `664`, `665`, `666`, `667`, `669`, `670`, `671` (`.json` headers + `.txt` summaries/per-file counts) and `636-cancel-full-core.txt`, `643-b4-ui-project.txt`; `UNITY-INTEGRATION-BACKLOG.md:159-161`; `package.json:10-17`.

### Evidence limits
I ran nothing and have no shell: I could not diff `df693294` against `5f116403` (taken as evidence-only on the parent's word), could not re-hash `662-T2b.patch` (`8ed90a18…`), the inherited tree `e2c8f631…` or `668-T3.patch`, and could not verify "no other reason/drop/tie string changed" by diff — only by matching the three sentences the records quote. The `--project core` run covers `tests/bridge*.test.ts`, but `npm run test:bridge` as a literal command has no record-check. Plan lines `:725-763` are single very long lines; I read them in bounded chunks and Grep suppressed several long context lines, so a clause inside an unread long line could exist (I read every scope/OPEN/task line cited above in full). Finding 1 (negative-tier unreachability) is my own paper arithmetic over the landed constants, not a measured run. No native, no UI-visual and no usability surface exists in this slice, so no screen-family or craft assessment is claimed. Nothing here is Owner acceptance or a command to merge.