# 651-B — Independent review of the P14B.5 T0 preservation artifacts

Read-only (Read/Glob/Grep). No hashing, decompression, git, test or engine execution by me. Where the 06/08 pattern hashed independently, I could not; those points are marked NOT VERIFIED (by me) and rest on the passing gate, the record-check harness and cross-document agreement.

## Q1. Producer identity — MET WITH EVIDENCE (byte-level); git-log level NOT VERIFIED by me

- Authority block identical in corpus MANIFEST (`tests/fixtures/p14/genuine-v30-pre-b5/MANIFEST.json:2-17`), runtime provenance (`tests/fixtures/p14/genuine-projection47-runtime/genuine-projection47-runtime.provenance.json:2-17`), runtime MANIFEST (`:2-17`) and both executed minters (`648-mint-final-v30.executed.ts.txt:33-48`, `649-mint-runtime47.executed.ts.txt:28-40`).
- 61833f0d as the behavioural writer: record 637 `:11-14` ("Candidate source: 61833f0d = 629-W S1 c5a2ecdc + S2 61833f0d … `git diff --stat 61833f0d HEAD -- src bridge ui` empty at sealing"); 645 `:9-13, :91-93` and 645-A `:26, :451-452` say 651fea8b is comment-only and the writer identity is 61833f0d. The 646-T report `:29` records `git log 61833f0d..d6c11b9b -- src bridge ui generated` = only 651fea8b. I cannot run git; the byte-level equivalent is established by the gate that passed in both mints: `git diff --exit-code 651fea8b -- PRODUCER_PATHS` empty (`648-mint-support.executed.ts.txt:85`) plus the hash pin of `git diff 61833f0d 651fea8b -- PRODUCER_PATHS` (`:86-87`).
- Diff-hash device: sound. 638-W `:7` records the text pass patch (`git diff HEAD -- src/`, 52 lines) with sha256 prefix `456a099e448f5c4f`; MANIFEST `:6` pins `456a099e448f5c4f7ff2a3506dd1cf3c82611c5e7b16ddfc29cb26d70cc0b31a`. A 16-hex-char prefix match across two independently produced records is strong. The hash pins the diff bytes; "comment-only" rests on 638-W's line-class grep (`:9`, 0 non-comment changed lines), 639 typecheck and 640 tests (645 `:21`). Caveat (durability, not correctness): the pin depends on git's diff text (index-line abbreviation); a different git would refuse loudly (646-T `:124`), never accept drift.
- 0e330da0 (test titles, seven files) touched none of the PRODUCER_PATHS test files (645 `:14-20` list vs support `:42-45`); the gate would have refused otherwise.
- sourceFiles: 41 entries = B4's 23 (`01-mint-support.executed.ts.txt:31-37`) + the 18 additions named in 646-T `:46` (index, tuning, worldgen, hollywood, operations, technologyProduction, hollywoodPolicy, hollywoodTick, promiseCapacity×3, bridge/protocol, runtime-checkpoint, promises, people, p14b4-cast-class-outcomes.test.ts, V29 MANIFEST, V29 refused gz). Counted in MANIFEST `:20-60`. Corpus MANIFEST and runtime provenance carry identical hashes for all 41 (two processes, 22:12:04Z and 22:12:36Z). Two cross-document anchors: V29 MANIFEST hash `77fa32da…` (`:59`) equals records 06/08/09; V29 refused gz `dedd68ed…` (`:60`) equals `genuine-v29-pre-p2/MANIFEST.json:1177`. I cannot recompute any of the 41 hashes, and none was re-verified at ce04e49e by 650.
- Source facts re-read at HEAD: `src/core/save.ts:6396` LIVE_SAVE_VERSION 30; `src/core/promises.ts:45` rules 4; `bridge/schema/bridge-schema.ts:233` projection 47; `generated/unity/StudioBridgeDtos.Generated.cs:3,20` schema `6f6b4880…`; `bridge/runtime-checkpoint.ts:63` last registered prior = projection-v46.
- Immaterial deviation: 645-A `:451` said "at HEAD 40451858"; the mint ran at d6c11b9b. Commits between are docs-only per their messages and the gate proved producer bytes unchanged since 651fea8b. The P14 plan precondition ("minted at its final writer before any source change", `plans/P14-HEADLESS-PLAN.md:414-415`, not :410-411 as the task cited) is satisfied in substance.

## Q2. Genuineness — MET WITH EVIDENCE

Read the full executed minter (`648-mint-final-v30.executed.ts.txt`) and all ten provenance `role/recipe` blocks.
- Routes: `p13aGeneratedStudio()` (`src/harness/p13a/fixtures.ts:9-12`) and `advanceTo` (pure ticks, `:13-17`); `retentionFixture` (`tests/helpers/p14b2-fixtures.ts:62-125`: real signContract, strikeSet/commissionSet, submitProposal+attachPromise, greenlight, setProductionSetupRecipe, assignShootingDirector, scheduleShootingTake, tick → take; BROKEN by the real `releaseTalent` action `:118`); `rivalFixture` (`:215-231`, ticks only). Tagged-P2 route copied, not imported, from `tests/p14b4-cast-class-outcomes.test.ts:144-197, :309-346` (minter `:95-200`); `variant()` (`:253`) appears nowhere in the minter. All state changes are `applyActions`/`tick`/`submitProposal`/`attachPromise`/`migrateToV30`; the only direct field writes are the disclosed `fund()` cash+ledger bootstrap (`p14b2-fixtures.ts:15-19`, recorded as `economicInput`) and the P13A harness `economyEngagedEver` bootstrap, both as in B4.
- No restamp: bytes are `exportSave(makeSave(state))` (support `:110`); for the legacy case `{...JSON.parse(originRaw), saveVersion: 30}` at minter `:368` is an assertion comparand, not a write. No receipt edit: focus copies `p.feasibilityReceipt` (support `:132`); rival focus is `structuredClone`d and re-asserted equal after selection (minter `:314, :385`); `rival.open` asserted byte-unchanged (`:272, :384`).
- Legacy substitution honestly labeled: provenance `genuine-v30-legacy-count-only-p2-current-draft.provenance.json:71-85` (role, origin gz `dedd68ed…`/raw `7f7529cb…`/V29 MANIFEST/89b5ad2c/c06db6ea, migration, droppedVariant). Origin pins match `genuine-v29-pre-p2/…refused-p2….provenance.json:4-5, :69-70`. Corroboration: byteLength 676337 identical V29→V30 (`:71` vs V30 `:91`), root counts identical, focus promise-2 v3/rules-3/IMPOSSIBLE identical with the V29 receipt key order preserved. Minter asserts `validateSaveV29` first (`:363`), real `migrateToV30(importSave(raw))` (`:365`; `src/core/save.ts:8714-8717`), state deep-equal (`:367`).
- Dropped bound case correctly explained: `src/core/promises.ts:398-399` refuses a count-only P2 at `promiseFeasibility` (used at attach and at freeze via `attachedFeasibility`, 638-W `:66`); the V29 corpora hold no bound P2 (`genuine-v29-pre-p2/MANIFEST.json:1189-1191` the only P2 focus, contractId null; evaluator1 corpus three APPEARANCE_COUNT unbound `:70-142`). I could not decompress the 12 gz to confirm the full-root scan 646-T `:68` claims; the source law makes a bound count-only P2 unproducible in V29 regardless.
- Rival rename is a true source fact: `src/core/talentMarket.ts:1275-1296` `authorRivalPromise` offers `[flexible, p1]` for an unproven person, flexible = `castRoleCount/1/leadOrAntagonist` (`:1280-1285`). MANIFEST focus: 48 entries, 6 tagged (promise-10/11/28/29/32/33), 42 P1. "Since 600-W S2" at commit granularity NOT VERIFIED (doc comment `:1263` attributes it to record 600).
- Shared-take facts (film:11, first-take-event-45 @213, promise-12/16/18, talent-market-event-120/121/122, cast r02-3/r02-2/r02-0; provenance `:169-184`) match the landed 619-R `:33` and 619-T2 `:59-60, :92-93`.

## Q3. Readbacks and runtime premise — MET WITH EVIDENCE

- Order: `mintV30Corpus` = gate → `prepareV30Corpus` (export→`validateSaveV30`→re-export equal→`importSave` re-export equal→`BridgeSession.fromSaveJson` re-export equal + promises/firstTakes/talentMarket deep-equal→gzip identity, support `:110-121`) → gate again with sourceFiles/closeout/minter/support hash equality (`:150-154`) → `mkdirSync` (`:162`) → `wx` writes, MANIFEST last (`:173`). Runtime: gate → `prepare()` → gate → deepEqual(final, initial) → mkdir (`649-…executed.ts.txt:132-154`).
- −0→0 premise preserved: each complete inner slot compared to `exportSave(makeSave(<its own expected state>))` (`:92-94`), both slots strict `validateSaveV30` + canonical re-export (`:88-91`), decode without migration (`:99-100`), reopen byte-identical, same-identity retry replays the journal response, bytes unchanged after retry (`:102-105`).
- Byte identities: savedSaveSha256 `b7a32680…` (runtime MANIFEST `:32`, provenance `:93`) = corpus current-p1 raw (`genuine-v30-pre-b5/MANIFEST.json:95`), produced in separate processes; currentSaveSha256 `67882251…` = 646-T dry `:72`; journal `['save','command']` (`:85`), entries 2, revision 1, schema/protocol 4/projection 47, weeks 45/45, saved 2 proposals vs current 1 (`:97`).
- Outer non-determinism disclosed in 646-T `:72, :115` (processingMs; confirmed `bridge/session.ts:1599, :1744, :1992, :2040, :2114`); real outer raw/gz/journalDigest (`c7e3cd2a…`/`38275e91…`/`5cad7265…`) differ from 646-T's dry values as predicted. Record-only: the disclosure lives in 646-T, not in the runtime provenance or a closeout yet (B4's 09 `:86` carried it).
- Observation: worlds are memoized by the DRY `it` before the ONE-SHOT's first gate (646-T adaptation 2). The gate's git/hash checks still bound producer identity at write time and the record-check harness bounds it at process start/end; readbacks re-run inside the ONE-SHOT. Not a defect.

## Q4. Evidence integrity — MET WITH EVIDENCE, one record-only gap

- 648: exit 0, 2 passed (`648-mint-final-v30.txt:1809-1814`), sourceSha d6c11b9b start/end (`648-mint-final-v30.json:2, :22`), untracked 3 → 24 = exactly the 21 corpus files (`:4-8` vs `:24-49`), fixedSource false (`:50`). MANIFEST startedAt/endedAt 22:12:04–22:12:10Z inside the command window 22:11:42–22:12:11Z. ONE-SHOT stdout hashes (`:1751-1807`) equal MANIFEST, provenance and the 646-T dry table for all ten (deterministic across processes, as predicted at `:81`).
- 649: exit 0, 2 passed (`649-mint-runtime47.txt:146-151`), sourceSha d6c11b9b both ends (`649-mint-runtime47.json:2, :43`), untracked 24 → 27 = exactly the 3 runtime files (`:45-51`). Chain: 648 end patch `0f18ba5d…` = 649 start patch (`648….json:23` vs `649….json:3`), so the corpus bytes did not move between the two mints. 649 provenance startedAt/endedAt 22:12:36.858–37.177Z inside 22:12:23–22:12:38Z; ONE-SHOT 601 ms consistent with memoized prepare.
- Patches: `648-mint-final-v30.patch` contains exactly the three sources (`:1, :183, :345`); `649-mint-runtime47.patch` = the same three plus the 21 corpus files (its START state), as the recorder's untrackedSource says. The archived `.executed.ts.txt` files match the 648 patch in line counts (176/156/432 vs `@@ -0,0 +1,176/156/432 @@`) and at every spot-checked line (patch `:45, :220, :250, :387, :409, :752, :768`). MANIFEST minterSha256/supportSha256 (`46b8306e…`/`1133a180…`) and runtime provenance minterSha256 (`a91c3168…`) equal the 646-T authored-file table `:11-13` (sizes 30509/12816/10698). I cannot hash the archives.
- 650 (`650-parent-artifact-check.txt`): every gz/raw pinned in provenance, all saveVersion 30, every raw in the dry table, runtime savedSaveSha256 = corpus current-p1, PROBLEMS []. Record-only gap: `:3` prints "cases in manifest: 0", so the parent's script keyed on a name the MANIFEST does not use (`fixtures`) and did not cross-check MANIFEST entries against provenance; my manual cross-check above covers all ten. 650 also does not print the executed-archive hash comparison the task attributes to the parent.
- Operational files removed: no `scripts/v30-mint-support.ts` or `tests/bridge-p14b5-mint-*.test.ts` in the tree; no test references the new fixtures yet (expected before T1).

## Q5. Coverage — MET WITH EVIDENCE against 645-A; forward needs named

645-A `:449-456` list: empty ✓; bound open P1 ✓; tagged P2 ✓ (two, lead and leadOrAntagonist, bound OPEN); kept-and-broken ✓; rival current P1 ✓ (plus 6 genuine tagged P2); first-take world at remainingTicks 5 ✓ (shooting task unassigned, technology locked, discarded-clone premise proof recorded); projection-47 checkpoint ✓. Extras: current-p1 (the runtime saved slot), rival-shared-take-terminal, legacy count-only P2 draft. No B.5 expansion draft exists in `plans/` (only the parent holds it).

Absent but plausibly wanted by the B.5 RED families (645-A `:326-358`): (a) a V30 world holding a RELEASED player film joined to its first take (Q3 no-backfill witness for `sharedSuccess/sharedFailure`; rival released films with takes already exist in the w196/w213 worlds); (b) a V30 world after a player `cancel` following a first take (the `cancelledAfterFirstTake` driver's migration witness). Both are mintable later from `retentionFixture().outcomes` / the cancel-causal route with the same producer bytes, while no source change lands. Two mechanics to record: the gate pins `HEAD === publishedRecoverySha` (support `:81`), so a later mint needs a re-pinned authority block at the then-HEAD with the same 61833f0d/651fea8b/diff-hash pins; and it refuses an existing output directory (`:100`), so it must write a new corpus directory rather than append.

## Q6. Verdict — KEEP WITH RECORD-ONLY ITEMS

No demonstrated defect in any artifact byte, provenance field, route, gate or evidence record. Record-only items for the parent's closeout:
1. 650 `:3` "cases in manifest: 0" — script key miss; state that MANIFEST↔provenance agreement was established by 651-B's manual cross-check (and optionally re-run with the `fixtures` key).
2. Runtime provenance/MANIFEST carry no `supportSha256` although the runtime minter imports `gate/git/sha` from the support script; the support identity for 649 rests on the 649 START patch (blob `f666d693`, identical to 648's) and 646-T `:11`.
3. Carry the processingMs outer-hash non-determinism sentence into the closeout (B4 09 `:86` precedent); the inner slot hashes, journal routes, intent id and command id are the comparable facts.
4. The 41 sourceFiles hashes, closeoutSha256 and the executed-archive hashes were verified by the passing gate at d6c11b9b and by 646-T's shasum, not re-hashed at ce04e49e by 650 or by me.
5. 645-A's "at HEAD 40451858" vs the actual d6c11b9b: note the docs-only interval and the gate's producer-diff proof.

Not Owner acceptance; not forward-migration, T1, native or product verification. T0 closeout remains the parent's record.

## Sources read
`evidence/p14b4-20260919/{06,08,09}` pattern records; `646-T-report.md`; `648-mint-final-v30.{txt,json,patch,executed.ts.txt}`; `648-mint-support.executed.ts.txt`; `649-mint-runtime47.{txt,json,patch,executed.ts.txt}`; `650-parent-artifact-check.txt`; `637-cancel-causal-qualified-checkpoint.md`; `638-W-report.md`; `645-A-next-family-scoping-note.md`; `645-text-pass-b4-closeout-and-next-family.md`; 619-R/619-T2 (grep); `01-mint-support.executed.ts.txt` (grep); both new MANIFESTs, runtime provenance, legacy/rival-shared/at-five provenance (others by grep); V29 corpus MANIFEST/provenance (grep); `src/core/{talentMarket,promises,save}.ts`, `bridge/{runtime-checkpoint,session}.ts`, `bridge/schema/bridge-schema.ts`, `generated/unity/StudioBridgeDtos.Generated.cs`, `tests/helpers/p14b2-fixtures.ts`, `tests/p14b4-cast-class-outcomes.test.ts`, `src/harness/p13a/fixtures.ts`, `plans/P14-HEADLESS-PLAN.md:266-285, :404-415`.

## Evidence limits
No sha256, gunzip, JSON-parse of the gz payloads, git ancestry/log, typecheck or test execution by me. Genuineness of the saved bytes is inferred from the executed minter's assertions plus the passing run, not from parsing the artifacts. The "all 12 V29 gz scanned" claim and the exact commit that introduced the rival tagged-P2 authoring are not independently verified. Line citations are to the files at HEAD ce04e49e.