# 894 — independent880-B call-path audit

2026-09-26 on publishedff7c93999644f8f9289e70f8e93d22f8497fcaef. Native
c2rm_contract_review read-only audit; no edits/tests/nested agents. **KEEP** the
bounded approach. Independent tests are being authored; production remains unchanged.

Exact live delegation:
37→36→35→34→33→32→31→30→28→27→26→25→24→19→18→17→16→15→14→13→12→11→10→9.
V29 andV20–23 are absent. Stop atV9;V8 has no screenplay assertion.

Derive invocation-local immutable grants from guarded original input atV37. Match
studio/project/writer and the exact existing assignment dates; never serialize the
grants. Thread them beside existing policy/window/termination arguments through all
private delegates. Extract private V28/30/31/32/33 implementations; their exported
validators call strictly without grants. Historical migration source/downgrade
destination checks stay strict. Do not skip any structural or relational validator.

Critical context loss: V34 removes lifecycle beforeV33; V19 removes Hollywood
beforeV18. V9's player script assertion and V13's placement assertion consequently
cannot derive authority from the fragments they receive. A GameState cast restores
neither root. V19 must also pass the original grants to validateHollywood, which
selects by actual business studioId before its rival screenplay assertion.

Placement forwards grants through BOTH construction branches (legacy and managed).
Construction currently rebuilds a narrow script context, so it must carry them into
the one contract assertion too. Current action screenplay assertions derive from
their full live state. Current placement action callers, tick and
studioConstructionView/Calendar need explicit live wiring. A small shared
implementation behind strict/live wrappers is acceptable. Never infer permission
from a cast, facility policy, root presence or function identity.

ScriptDevelopment's shared invariant remains strict by default. Consult grants only
where it currently checks active employment, with collision-safe tuple matching.
Preserve due dates, reservation/capacity, membership, profiles, duplicate assignments,
production occupancy and every other law. No global context, synthesized contracts,
extended terms or state rewriting. Current commission/rewrite/admission stays owned
by its existing authority.880-B defines the full positive/negative acceptance set.
