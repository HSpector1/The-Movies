# 396 — Independent sibling public-subject retained-property control

2026-09-20. Native test-author, narrowly authorized additive exception to394's
unchanged-tests boundary. No production inspection/edit, runtime, probe,
typecheck, Git command, network or delegation. Parent owns all execution.

## Baseline and frozen handback

Test: `tests/p14b4-ready-owner-replay.test.ts`.

- Baseline source: `51cfcc6ea3fb8908623f999b5644510c08d6deda`.
- Baseline441-line SHA256:
  `431a380965938c614364a382a17bfd33607c85bf0bce630f206629ae6dafe692`.
  Test-author measured the pre-edit working file; parent independently read the
  complete file using git-show at51cfcc6 and confirmed that same hash by message.
- New477-line SHA256:
  `c1804450b4eecd7fc87be118fd4d6e7804e5948e42956253659060ecc6bf8ed6`.

Exact scope:36 added lines in the existing SAME Ready sibling case only, in three
additive hunks. No original line removed or replaced; imports, helpers, fixtures,
every original assertion, caps, timeouts and all other cases are retained.

## Independent oracle

Each sibling already executes its own real public greenlight and tick reference.
The addition derives five distinct company-person subjects from that actual
admitted production and the sixth resource subject from its actual single
Development & Casting reservation. Exact six-count, one-to-one subject matching,
closed subject values and the genuine screenplay owner path are required.

The lawful sibling choices swap lead/antagonist while preserving the same company
and independently acquiring the same development facility/slot. Matching each
subject across branches must yield exactly one counterpart, equal subject/path
values, but distinct public Hold and Hold.subject object references. Shared
private canonical identities must not introduce public occurrence aliasing.

No output is mutated. No private identity helper or implementation output supplies
expected subjects. Intentionally shared fixed Hold reference semantics are not
changed or constrained. No extra replay invocation or new campaign is added.

## Limits and next action

UNEXECUTED retained-property control, not a proven RED regression. This pins the
fresh-public-value obligation explicitly adopted394; it does not prove private
table lifetime/payment, every namespace collision, impossible source diagnostic
precedence, whole cost correctness or numerical fit. Existing source and input
purity, sibling order/work, full projection and strict-save assertions remain.

Parent should mechanically verify the additive diff, then execute only after both
writers yield. This test and brief are frozen at handback; test-write ownership
is returned. Preserve any actual failure rather than changing the expected
freshness or cap.
