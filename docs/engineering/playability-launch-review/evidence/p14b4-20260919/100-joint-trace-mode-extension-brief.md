# 100 — nonvacuous optional49 mode-separation supplement

INERT / UNEXECUTED. Two cases, default timeouts, no mocks/skips/TODOs.
No runtime, probe, typecheck, Git/network, mutable-kernel inspection or live
source/test edits. Parent owns installation and actual execution.

- Draft: `100-joint-trace-mode-extension.test.ts.draft.txt`, 82 lines.
- SHA-256: `135ce653c336258b412cf1e69c8c358fe9fc6549a8a3d08329a9bb049e950ad2`.
- Intended path: `tests/p14b4-joint-trace-mode-extension.test.ts`.
- Governing contract: frozen91 explicit trace tags at the original49 entry
  must be input errors; original49 optional semantics remain intact.

Before EACH refusal, the same complete optional49 baseline is actually called
and must return the literal FRAGILE/achievableProbeFailed result: one existing
P take10 pays target T/lead X1, no second event can pay B2, prior profile zero at10.
Its exact count witness and bounded integer work are checked. The three distinct
cast people, nonnull release14, empty hold/replacement arrays, nonempty staffing
and fact references, all original fields and complete coverage are supplied.
These are detached mathematical inputs, not engine history/admission evidence.

The first probe adds ONLY top-level `mode:'jointOwnerTraces'`. The second adds
ONLY alternative `kind:'jointTracePicture'` and `jointTraceKey`, retaining the
nonnull release and empty arrays. Neither can pass because an alternatives
array or another original49 field was absent. Both baseline and refusal inputs
are deep-frozen and compared to independent pre-call copies. No cast is needed
to hide missing fields or force the additional tagged properties through typing.

Original95 remains byte-identical at
`b094de96394d4478d000c79ec7600643fcff0b63872c460cf9fa190fe94ada99`;
all existing49 tests remain unchanged. This supplement corrects only the evidence
gap in95's old-entry misuse probe; it does not revise95 or claim actual RED.
