# 963 — independent C.3 RED plan and complete acceptance map

2026-09-26. **Docs only; no C.3 test source or execution.** This plan is prepared
while the parent records962/964/965 for the prerequisite digest correction. Those
runs and the prerequisite checkpoint must close before the parent releases test
authoring. The parent owns all production, fixture publication and execution; the
test specialist owns only explicitly released test paths. Source/HEAD stays frozen
during every recorded run. There is one heavy lane and no nested delegation.

Controlling law is reviewed/frozen942 and exact946, including946-A's current-only
validation/anchor map and946-B's live two-gate admission and writing clarification.
The public exports now explicitly live in `src/core/index.ts`:
`professionAtWeek`, `transitionInputsFor`, `chooseProfessionTransition`,
`advanceProfessionTransitions`, `TRANSITION_RULES_VERSION`. Internal implementation
file names are not test contracts. The new boundaries are **Save38 and projection53**,
with protocol4, lifecycle intent1, promise4 and transition rules1.

938's24 rows remain the scope checklist. Its previously unresolved choices are now
settled by942/946. In particular, current P1/P2 still concern acting; there is no
new directing/writing promise, no blanket transition-triggered VOIDED rule, and no
requirement that a requested unretired discipline equal primary profession.

## Evidence and test construction policy

The primary genuine outgoing input is the immutable953 corpus published at
0fc34e9ec8b6ade9ff18485a215193cdbcbd4591, generated at1f44aa50 with951. Keep every
gzip/raw byte, manifest and original failed945/948/950 record. Its original
default-development trajectory remains labelled; new compared continuations use
the explicitly selected same tick option on both sides. Its preserved continuous208
and actual runtime208 differ only in the documented historical digest receipts;
neither is overwritten by the prerequisite correction or C.3 migration.

| Input | Concrete useful facts | C.3 use and limit |
| --- | --- | --- |
| Genuine953 saved207 | Both focus actors remain announced, contracts end208; three actual acting takes/released pictures each; separate directing/writing capability and repeated real counterpart evidence | Migration at207 followed by the real208 retirement should make both first choices at208. This is the main positive witness, not a manually injected retirement/evaluation. |
| Genuine953 continuous208 and runtime208 | Both focus actors already retired at208; distinct preserved historical receipts | Loading either opens the boundary208 and schedules prospective209 reconciliation. Loading alone changes no role and creates no choice/finality fact. Both branches are tested independently. |
| Genuine953 created0 / released35 / preannouncement103 / announced104 / post209 | Actual pre-work, work-complete and retirement chronology | Migration scaffolding, no early transition, immutable supporting references and boundary controls. Counts come from actual source facts rather than guessed whole-world totals. |
| Genuine953 runtime52 saved207/current208 | Actual one-command journal, distinct slots and real public command | Prior52→53 reset, two independent Save38 migrations, restart/load/branch tests. Use actual runtime current208, never substitute continuous208. |
| Existing Scientist37 at617/618/669/670 and outgoing51 runtime | Genuine non-catalogue retirement670 and old independent slots | Prospective non-actor finality671, finishing/no-early-finality controls, older migration chain and input preservation. It is not a substitute for outgoing52 reset. |
| Genuine V35 rival/cohort2600, previously inspected941 | Real retained cohorts and authored credits; one retired target-capable actor below75 but no qualifying acting career among the seven inventoried capable candidates | Strict historical control and natural deferred candidate. No positive transition may be inferred from capability alone. Cohort832 actor0 is a separately bounded public-work candidate; its positive continuation is still unproved. |
| Existing real880-B writer fixtures | Genuine natural expiry, finishing screenplay and current entry-point validation | Non-catalogue preservation controls; not proof of two-profession writing. That later positive must follow an actual Actor→Writer/Director history. |

Future fields are accessed through typed boundary views of existing save/tick/public
index modules until live types exist. Namespace existence checks may diagnose absent
API, but cannot be the only RED: existing `migrateToLive`, `makeSave`, `importSave`,
`tick` and public actions must fail actual specified state/output assertions. No
import of an uncreated internal module. A valid positive control must pass before
any negative family is credited; an old reader's blanket refusal of all38 inputs
does not prove a current38 malformed-state invariant.

Pure chooser tables may use explicitly synthetic `TransitionInputs`; those prove
the deterministic comparison law, never historical gameplay. State-level positives
use real public authoring/work/ticks and validated genuine inputs. Detached malformed
copies are labelled negative cases, not new history. No expected schema/save/declaration
hash is guessed or copied from a failing output. Projection53 identities and generated
body pins are independently measured after the writer exists, with producer/source
identity and deterministic repeat, before any attributed pin maintenance.

## Proposed file ownership and sequencing

These are proposed paths, not authorization to create them during the current run.
Core files use core `.js` imports. Any bridge dependency belongs in `bridge*.test.ts`
under its existing TypeScript configuration;963 incorporates the960 layout lesson.
Helpers shared by core suites must remain bridge-free.

| Stage | Proposed paths | Release/verification boundary |
| --- | --- | --- |
| A — first bounded slice | `tests/p14c3-save-v38.test.ts`, `tests/p14c3-transitions.test.ts`, `tests/helpers/p14c3-fixtures.ts` | Save38 foundations, actual two-target positives, pure choice and initial due behavior. Freeze exact bytes; parent records and attributes RED before releasing matching production. |
| B — evidence and current episodes | `tests/p14c3-transition-evidence.test.ts`, `tests/p14c3-profession-episodes.test.ts`, additions to the owned core/helper files | Full retained evidence, all eligibility/timing branches, actual new work, old-role refusal, per-profession extensions and dual-episode writing. New behavior receives its own RED before writer release. |
| C — historical authority and entrant paths | `tests/p14c3-profession-history.test.ts`, remaining Save38 negative families | Cohort/authored-role history, actual append sites, private validation route, dormant activation and older builder refusals. Positive witness limits remain explicit. |
| D — bridge/runtime/public surfaces | `tests/bridge-p14c3-runtime.test.ts`, `tests/bridge-p14c3-career-read-models.test.ts`, `tests/bridge-p14c3-campaign-isolation.test.ts`, `ui/src/screens/StudioCalendar.careers.test.tsx` | Exact projection53 DTOs, actual52 reset and current53 replay, genuine campaign Save As, UI route and news retention. Separate source freeze around each execution. |
| E — complete verification | Parent-owned selected neighbors, generated checks/types, matched full gate and C-track endurance evidence | No whole-law or launch qualification from Stage A alone. Attribute every failure and maintain only proven superseded expectations. Unity/native remains deferred. |

The split is execution sequencing, not a scope cut. Any helper consumed by a running
batch remains frozen; later authoring cannot modify it until that run closes. If a
later fixture needs changes to a consumed helper, request a freeze boundary from the
parent rather than writing through an active recorder. Routine implementation and
test work needs no new user approval.

## Stage A: first concrete RED slice

The first batch is intentionally bounded around short actual continuations and pure
tables. Exact observed counts/timings are recorded after execution, not promised here.

| ID | Required assertion through existing/public boundary | Genuine or synthetic premise |
| --- | --- | --- |
| A01 | Live writer/save dispatch moves coherently to38; import/export/current migration produce a38 envelope with exact six new lifecycle fields and transition rule1 while preserving intent1/promise4 | Genuine37 inputs; current fresh generated world. Missing version/root is a behavioral RED. |
| A02 | Migration opens `transitionBoundaryWeek` at the actual input week, anchors exactly once in talent order with existing kind, empty evaluation/change/finality arrays, and the exact sorted prospective queue | Derive queue from all actual completed retirements and engagement, not a guessed count. An announced focus actor207 is absent from the migration queue. Every old lifecycle field and historical receipt remains byte-equivalent. |
| A03 | Genuine207→208 retires the two focus actors, chooses director/writer respectively, records one linked chosen evaluation/change per person at208, and adds each free agent once | Independently count actual takes/context, inspect P10 target capability and public tiers first. Do not pin focus ordinal0 when other real people may also reconcile; assert each actual array ordinal/id and deterministic order. |
| A04 | Genuine retired208 loads without a choice, then chooses prospectively at209, retaining the actual profession-retirement208 | Run continuous208 and actual-runtime208 separately. No backdated208 transition at load; no replacement of their old promise digests. |
| A05 | `professionAtWeek` is actor before the real change and destination inclusively at/after it; default retirement lookup is current profession, explicit actor lookup retains old retirement | Actual accepted history from A03/A04. Save/load and another ordinary tick cannot duplicate the change. |
| A06 | `transitionInputsFor` is pure, current-week-only, returns exact counts/witness ordering and current public P10 facts; refuses a request to reconstruct past mutable public values | Genuine focus actors and preserved before/after inputs. Three real takes/context3 already discriminate exact totals from bounded3/2 witness lengths. |
| A07 | Choice tables cover capability59/60, acting2/3, context1/2, proven alternative, no eligible target, exactly one eligible, unequal tuples, equal eligible tuples, and age74/75 precedence | Explicit synthetic public snapshots with valid tier/capability/proven relationships. No fake state/receipt. Use fixed director/writer slot order; malformed reordered target arrays are not mistaken for a lawful input permutation. |
| A08 | Tuple comparison prioritizes proven→roleTier→contextBand→public potential, uses bands rather than raw OVR/counts, permits Limited upside, and returns exact reason/outcome/selected values | Paired synthetic snapshots; swap which named target owns a stronger tuple and require selection to follow evidence. Exact ties decline all, so target id/order cannot break a tie. |
| A09 | Already-retired non-catalogue Scientist670 migrates without industry finality; the first engaged tick671 creates one noCatalogue finality with null evaluationId, actual source key, no change, and no remaining due entry | Genuine Scientist37. Keep actual retirement670 distinct from prospective finality671; later tick/read/load remains idempotent. |
| A10 | A real under75 retired actor with insufficient career is deferred at the first prospective due week; next due is exactly min(W+52, actual75 crossing), never weekly | Select the measured genuine retired actor from941 only after asserting age/retirement/no-eligibility on actual input. If that candidate fails its premise, record it and supply a new public-history control; do not patch ages/counts or claim this case passed. |
| A11 | Empty38 scaffolding/anchors and untouched prospective due can downgrade losslessly; any actual evaluation/change/finality must refuse37 downgrade before stripping; genuine public37 remains strict | Valid boundary-only and actual chosen/finalized branches. Extra C.3 fields on a37 envelope cannot authorize a current-role mismatch. Original37 bytes remain unchanged. |
| A12 | Initial38 shape/authority negatives reject missing/extra fields, invalid rule/enum/week, duplicate/missing anchors, role rewrite without change, missing predecessor/link and malformed due queue | First require an actual accepted38 control. Each detached one-fault mutation names its invariant; no “unknown38” blanket refusal credit. Full evidence/dated validation matrix follows Stage B/C. |

A03/A04 are actual semantic transition RED, not merely absent export tests. The
root/type/persistence and choice work may be implemented incrementally only after
the relevant recorded RED. Stage A does not claim natural two-profession employment,
rival transition, cohort continuation, second retirement, runtime53 or UI coverage.

## Remaining law, retaining all938 obligations

| 938 ID | Concrete remaining or first-slice coverage | File/API owner |
| --- | --- | --- |
| A — Director | A03 chooses; then ordinary player sign and a real director assignment/shooting path succeed for that same person | transitions; profession-episodes; `applyActions`, production admission |
| B — Writer | A03 chooses; then ordinary sign and actual commission/rewrite complete, with no transition-created writing credit | transitions; profession-episodes; script actions |
| C — Full eligibility | A07 plus real no-depth/irrelevant-context/insufficient-capability worlds. Capability alone and birthday alone never select | transition-evidence; `transitionInputsFor`, chooser, tick |
| D — Choice/decline | A07/A08 plus an actual public-history two-eligible tie reaching declinedAll and linked finality; never inject the outcome | transitions/evidence; pure chooser and due processing |
| E — Final industry retirement | Real noCatalogue, declinedAll and ageBoundary; no hiring, new work, queue or repeated evaluations afterward, including past another due interval | transitions/episodes; tick, assignment/signing |
| F — Obligations first | Actual actor held across E in an existing production; no evaluation while announced/finishing, actual release/cancel clears the final obligation before one choice. Non-catalogue writer finishing remains intact | profession-episodes; tick and existing880-B controls |
| G — Old role stays closed | Transitioned actor cannot enter a new acting seat or P1/P2; lawful new profession and any other unretired discipline remain assignable. Current global C.2 restrictions still apply too | profession-episodes; public greenlight, direct core, rival seatable, promises |
| H — Two episodes | Later destination retirement preserves old actor row; current/default and explicit episode lookups differ correctly; legal new-role contracts after old E validate, old-episode overrun does not | profession-episodes; Save38; retirement/contract APIs |
| I — Extensions | Actual old acting extension retained; one later destination extension is allowed, never a second in the same episode. Discovery/acceptance modifies the correct row and exact D→E+52 term; closed old cases remain readable without offer/reason leakage | profession-episodes; talentMarket and bridge proposal readers |
| J — Cohort history | A genuinely cohort-born actor transition leaves original entrant block/role and previous receipts unchanged. Current counts use the new active role once; historical requests resolve their own dated prefix | profession-history; cohortRequest/Save38 |
| K — Starting credits | A real starting-rival actor's later profession history validates without rewriting authored credits. Wrong canonical original role/name/person, entry interval and forged missing-change history remain rejected | profession-history; current38→Hollywood authority plus strict37 controls |
| L — Player/rival symmetry | Actual natural rival retirement/choice and ordinary new-role staffing when lawfully reachable; same public eligibility/due/obligation law, no incumbent claim or auto-employment | profession-history/episodes; rival tick and ordinary hiring |
| M — Money/employment | Isolate `advanceProfessionTransitions` on an actually due completed-retirement state; no cash, ledger, employment, price, debt or trust write. Later real signing uses existing prices/bonus/exclusivity | transitions/episodes; transition owner, P11/P12 observations |
| N — Identity/write set | At isolated commit only specified lifecycle, role, free-agent and derived primary skill/salary proxy fields change. All underlying skills/ceilings/rates/experience/history/fame, identities/order, provenance and old facts survive | transitions/Save38; roleOVR/salaryCurve authority |
| O — Relationships/promises | No invented/deleted driver, mentorship, trust penalty, terminal rewrite or duplicate outcome. Later actual new-role shared work changes real ties; displays follow current versus completed-career as-of rules | episodes and bridge read models |
| P — Promise correction | Supersedes938's conditional future-profession-promise premise: no new directing/writing family exists. Retired acting refuses P1/P2 forever; original non-actor with acting skills still respects current profession's global cap. Preserve committed seats and terminal/half-open law | episodes; promise reads/admission/caps/digests, existing823 controls |
| Q — Determinism | Repeat pure reads/due processing/reload/command replay; exact canonical state and event multiplicity across choice, annual defer, completion and finality. No RNG or campaign-global state in the transition owner | transitions/Save38/bridge-runtime |
| R — Migration | A02/A04/A09 plus genuine older chain, unfinished people, null-Hollywood and later actual industry activation. No historical event at load; immutable boundaries/anchors/source bytes | Save38/profession-history |
| S — Frozen boundaries | Public V1–37 strictness, all actual private delegates and common makeSaveV1–18 guard; semantic downgrade before stripping, including null-Hollywood malformed authority | Save38;946-A route inventory |
| T — Evidence validation | Exact field/range/date/id/count/witness/proven/tier/digest/choice/link/source/due negatives from real accepted controls; named limits on historical snapshot proof | Save38/transition-evidence |
| U — Runtime | Genuine52 reset to53 with distinct migrated207/208 slots, prior command refusal, new53 command replay/restart, corrupt identities/digests and current53 no-second-migration | bridge-runtime |
| V — Isolation/Save As | Same person/seed/counters in detached branches plus actual runtime-coordinator `saveAs`, save, switch and restart using a test store; inactive complete state never advances | bridge-campaign-isolation |
| W — Current writing allowance | Actual Actor→Writer and Actor→Director with writing discipline reach second retirement and retain real pre-E screenplay work through current save/tick/Calendar/construction/unrelated action | profession-episodes;880-B negatives and current-primary authority |
| X — Truthful surfaces | Exact53 career/current-lifecycle/alumni/Industry fields, bounded news/attention, Calendar navigation, current hiring and no Finance event; same one person identity throughout | bridge-read-models and UI Calendar careers |

## Evidence, due scheduling and strictness details not to lose

Evidence tests re-count all retained facts at or before the evaluation. Deduplicate
player participants and Hollywood representations by `(studioId,pictureId)`;
cancelled actual takes count as takes, unreleased writer association does not count
as a released co-credit, uncaptured old participants stay unavailable, and authored
starting credits require the containing studio's actual entry by the evaluation
week. They can support writing context, never invented first takes.

Different one-off counterparts do not make repeated context. Compare actual2-with-one
against two1-with-different; total3 context still keeps only2 witness pictures.
Equal maxima pick the code-point-smallest counterpart for evidence only. Acting
witnesses sort week/studio/production/event and keep3. Mutate a stronger omitted
counterpart, total count, duplicate representation or witness order to ensure the
validator rederives exact totals, not just the bounded witness list. A stale or
altered digest must refuse, but digest agreement alone does not prove historical
capability/potential/workHistory truth. Their stored range/tier/proven consistency
and choice are tested honestly; current mutable values cannot reconstruct the past.

Due tests include exact waiting75 crossing via `nextBirthdayWeek(row,74)` and its
fractional-anchor correction, never naive ceil arithmetic. Use public fractional
age/provenance inputs or genuine old anchors, not a shifted clock. No target choice
at/after75. A young normally retired61 actor can evaluate through75; a real long
deferred chain must exceed five evaluations to reject an invented five-event cap.
No change/finality has a remaining queue. Non-actors are queued only for initial
prospective reconciliation. Repeated initialize/load cannot reset it.

Null-Hollywood imports have empty due/event arrays despite retained old retirement.
Actual later `initializeHollywood`/beginFounding schedules from
max(boundary, persisted originWeek)+1, after real entrant creation; non-null repeat
initialization is idempotent. Dormant actors read pendingReconciliation, never
awaiting a running transition. OriginWeek is actual and bounded; fresh origin is0.

First-slice field negatives expand to all exact objects and joins: safe finite
weeks/counts/ordinals; contiguous `transition-evaluation-N`/`profession-change-N`;
catalogue/source actor key; completed retirement; age from immutable provenance;
no live obligations at commit; exact one-to-one choice/change/finality; unique
person+profession retirement keys; no second change, post-finality event, missing
or stale due. Later-save validation must accept genuinely later new-role contracts
and work while refusing a contradictory dated interval. It must not claim a full
history of cancelled tasks if no retained evidence exists.

## Historical authority, append paths and writing controls

946-A's actual38 private delegate route is37→36→35→34→33→32→31→30→28→27→26→25→24→19,
then Hollywood validation. It skips29 and20–23; tests must not require calling their
public readers to manufacture a consecutive route. Public V1–37 and public lifecycle
validation omit current authority. Current proof is immutable and invocation-local;
raw extra fields, forged role or a second campaign cannot enable it. The separate
880-B authority keeps its18→9/construction/placement path and temporal controls.

Anchor tests cover fresh final population, all three public creators, actual new
Scientist recruitment, initial/scheduled rival entry, reused free agents, actual
rival supply including Scientist, and annual cohorts. Reused people retain one
anchor; each real appended person has an entrant anchor at creation. A real tick
must carry an appended lifecycle root through finalized state, catching946-A's
materialized-talent/provenance propagation trap. Older converter intermediates
must not open38 anchors; final37→38 opens one complete existing roster.

Cohort validation first takes exactly talentCountBefore and refuses a prefix
containing an entrant anchor later than its receipt week. It then resolves dated
profession/retirement, retaining actual canonical entrant roles. Starting-rival
authored credit roles use original observed profession and retain all old names,
ids, credit and entry checks. Financial interval caps use endedWeek when present;
they do not erase a longer guarantee. An interval beginning at the actual change
week belongs to the new episode; an older live interval cannot cross a completed
retirement/change.

946-B's two admission gates are tested separately: requested role retirement and
current primary profession's global restriction. Thus a transitioned director can
direct or perform unretired writing but cannot act; an original director/writer
with an acting profile still cannot evade its announced current boundary through
an acting assignment/promise. Preserve direct-core has-discipline authority,
director→cast→craft admission order, M16 collisions, permanent screenplay credit,
and existing rival casting policy. No primary-role-only shortcut is acceptable.

For later writing across expiry, select the **current primary episode**, which may
be director, not just writer. Real completed actor plus current director/writer rows
must not trigger the old duplicate-person denial. Duplicate same-key records and
malformed/future/missing authority still confer no allowance. Keep all880-B guards:
exact person/studio/project, pre-E commission, actual due date, natural expiry,
valid temporal status, no early termination, slot/resource integrity and retained
employment evidence. Exercise actual save, tick, Calendar, construction inspection
and unrelated script action callers; bare strict historical assertions stay strict.

## Projection, runtime and full-program completion

Profiles must expose exactly946's `career` fields and literal change professions;
current lifecycle remains current primary, completed retirements stay date/role
ordered, at most two, and recordingNotice distinguishes prospective migration.
Industry lists retain one identity, current work/employer and former-profession
alumni facts, with the existing page bounds/sort. Do not leak raw transition inputs,
hidden ceilings, rival private terms, edge magnitudes or undisclosed counterparts.

Working new-role people show live relationships. Waiting/pending use the latest
actual completed-profession boundary; industry-final rows use that same actual
retirement boundary even when prospective finality is later. Construct a real
post-transition shared-work tier change so a stale freeze fails; retain unavailable
history and current counterpart-disclosure controls.

Calendar `careerEvents` is separate from commitments/counts, has exact event shape,
date/order and existing profile navigation. Industry `careerKind` rows have null
studio/film without accidentally receiving permanent retention. Calendar, Industry
and Market retain actual changes/finality for ages0..12 weeks, remove at13, preserve
profile history, and invent no future choice, stop, unread event or Finance ledger.
Market ordering keeps existing lifecycle priority, then changes/finality and their
specified week/person order. Technology's pre-existing retention exception stays.

Actual runtime52→53 uses the measured outgoing52 identity, never a guessed new53
hash. Each saved/current slot independently migrates to38; reset incompatible old
session/revision/journal authority, reject the old command, then prove new53 save,
load, restart and duplicate replay. The interim955 current52 replay assertion is
explicitly superseded under an attributed cutover; its old evidence remains.
955's core history controls also distinguish frozen37 export/import identity from
live migration: at38, preserve the former exact-byte assertion and every old receipt,
but attribute and amend only the obsolete expectation that the new live envelope
equals the old37 envelope. Do not update either case silently during implementation.
Actual campaign-library Save As uses the runtime coordinator and in-memory test store,
following the public operation path in the existing Save As tests. Copied imports
alone are only branch-isolation evidence, not proof of that verb.

Open witness work is visible: natural cohort-born positive, canonical starting-rival
positive, rival continuation, actual tie/finality history and two-episode expired
writing still require bounded lawful construction and execution.941 did not supply
those positives. Cohort832's proposed roughly600-tick continuation is a separately
bounded candidate, not permission to lower thresholds, rewind or guarantee success.
An unavailable witness is recorded and pursued; it does not silently remove a row
from scope or become synthetic historical proof.

After all stages: parent records source-relevant neighbors, frozen historical
readers and genuine migration chains, current generated contracts/fixtures and root,
UI and bridge types, independent stable-diff review, matched full regression and
C-track endurance cost. Preserve old expected values until a specific failure is
attributed to the new contract; no bulk37/52 replacement, hidden skip or timeout
extension. Full-run and isolated results stay distinct. Every acceptance row above
must have actual passing evidence or a named unresolved limitation before a final
qualification claim; the first foundation batch is never that claim.

This plan changed only963-c3-red-plan.md. No C.3 test source, fixture, production,
probe, typecheck, generator, commit or push was authored/executed in this planning
turn. The next action is the parent's prerequisite checkpoint and explicit Stage A
test-file release, followed by independent RED on the recorded exact source.
