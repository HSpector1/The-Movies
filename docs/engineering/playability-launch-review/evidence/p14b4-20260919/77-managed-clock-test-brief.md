# 77 — narrow managed-clock owner tests

Status: INERT, UNEXECUTED, frozen for parent review. No installation, runtime,
probe, typecheck, Git operation, source edit, or fixture artifact generation was
performed by this task. This does not claim RED or GREEN.

Authority: full frozen `72-managed-clock-extraction-plan.md`, parent
`next-owner-extraction-notes.md`, native `.claude/agents/test-author.md`; parent
reports published kernel checkpoint `a8d277d91ccc323fe976d7cbb74940046ab0f22c`
and unchanged owner baseline 76. The tested source identity is for the parent to
record at actual execution, not inferred from this draft.

## Draft identity

- `77-managed-clock-owner.test.ts.draft.txt`: 248 lines, five cases.
- SHA-256: `9e45309c9942ab0f45e6135a8e956480c39834186d8cf5db02b10468608dea92`.
- Intended installation: `tests/p14b4-managed-clock-owner.test.ts`.

Read-owner bytes captured while drafting:

- `src/core/operations.ts`: `13b16db62b353a5572ba74b9b566b83880e2624c6d578f1ae549cf7de28a795c`.
- `src/core/technologyProduction.ts`: `4e2a66d8d187f69051598de96301234318e8ccbc6faf71d21d5eaf8fb101b0da`.
- `tests/p13a-production-technology.test.ts`: `6ec6ed1b776d4e92cba93a707a9901a33c2ebec627d07a98fa190fdcf7022723`.
- `tests/contracts/_contractFixtures.ts`: `7554a6b11f0756c7cd286a7732ab888757a129fe0d9f10657c93c4d214f10de2`.

## Fixture provenance and comparisons

The existing `operationsStudio('p13a-production-consumer')` helper uses explicitly
named `beginFoundingHistoricalControl`, then real sign/found/activate actions.
It is a controlled founded fixture, NOT an ordinary natural-founding claim.
Real greenlight emits every production; actual ticks drive all later clocks.
The fixture preserves managed operations plus legacy script development (hence
stock-script greenlight is lawful). Shooting assignment, scenery clearance, and
scheduling call the existing actual operations owners, as the technology suite
does. No Production, admission, history, take, or employment row is fabricated.
Sixteen actual tick attempts are a failing construction guard, not a timeout
override. All prerequisites, including the second concurrent greenlight and
nonnull wrapped Set, remain unexecuted.

Each paired call derives the four-field view from those actual productions and
adds an unrelated marker. Full and narrow branches get separate structured-cloned
contexts, event sinks, and real technology collectors; genre lookup reads each
context's complete real production/concept records. Both retain real Set binding
and setup-route resolver arguments. The stock policy is never stubbed. A custom
full-record callback reads `conceptId` and marker and invokes the original stock
callback to preserve its lock effect.

The five cases cover greenlight-week skip/two-record order; actual shooting entry
and lock without a take; unassigned hold then scheduled 5→4; completed wrap releasing
stage/Set even under an explicit detached occupied-Post input; and real Release
Ready hold followed by actual `commitPictureToRelease` and exact 1→0 admission.
Paired assertions compare complete operations, sets, collector events, committed
event log, technology, clock records, take/release IDs, original array order, all
extra/full caller fields, and input purity. The unchanged strict operations
validator receives full surviving records, after exact proof that ephemeral
zero-tick rows equal admitted release IDs. No validator accepts narrow records.

## Expected evidence and limits

The future `ProductionClockView` export and generic signatures are TYPE contracts.
`expectTypeOf` pins both marker-preserving output/firstTake types and order return
types, including a stock policy that must not widen inferred P. They require the
root TypeScript check: ordinary Vitest transpilation does not prove them. Existing
JavaScript may already satisfy all five runtime bodies; runtime GREEN together
with type-level RED before extraction is an honest possible outcome, not a missing
behavioral defect. No missing runtime export is invented to force RED.

This bounded tranche does not establish a forecast domain, alternative completeness,
multi-production fixed-point fairness, legacy mode, synchronized-dialogue adoption,
nonempty setup-plan progression, or every occupancy producer. Existing owner suites
remain required. The occupied-Post branch is a pure owner-input test, not a forged
saved campaign or an installation history claim. No timeout, validator, or existing
test was changed. Next action: parent review/install and separately record targeted
runtime plus root type-level results before releasing the mechanical extraction.
