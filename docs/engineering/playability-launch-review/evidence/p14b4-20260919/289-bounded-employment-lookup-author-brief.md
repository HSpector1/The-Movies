# 289 — Independent bounded employment lookup tests

2026-09-20. DONE as an inert draft; all five cases are **UNEXECUTED**.
Native test-author, adopted288 direction, parent-supplied published base
`b1ad9f6`. No mutable287 replay inspection, protected-file edits, installation,
runtime, typecheck, Git, network or delegation.

## Frozen artifact

- Draft: `289-bounded-employment-lookup.test-draft.ts` (174 lines, five cases).
- SHA256: `5da775f878103df3bc76d5e01abaef3d0695a00c63d9aca984fde01c4f404635`.
- Intended installation: `tests/p14b4-bounded-employment-lookup.test.ts`.
- Fully read authority288 SHA256:
  `2382bb8424aa1b117afafac179744b5ea8f6e5554cd992574400badc464b82e0`.

## Coverage and independent expectations

1. Real hire/found actions under the explicitly disclosed founding historical
   control, then fresh Hollywood at week zero and strict `makeSave`: original
   rival row references, ordinary employer/status callers, and exclusion from
   the actual freelancer market. Nonempty rival/player rows are hard guards.
2. Null/empty roots and absent IDs before, within and after the lexical domain.
3. Interleaved equal-ID rows, multiple eligible rows, unchanged original-order
   selection, same employment array with changed week/player ID, and a reversed
   replacement array. Each expected winner is literal and separately checked
   against original-array `.find`, not derived from the new index.
4. Inclusive starts, exclusive contractual ends and endedWeek, and null-ended
   contracts. Original row identity and complete lower-owner input purity are
   asserted throughout.
5. A transparent generic spy calls the actual original `boundedStableSort` and
   preserves its result. A cold fresh array requires one sort of all original
   references; warm queries, including changed player and week, require no
   rebuild. A replacement array requires its own sort. Frozen rows detect
   in-place mutation. Native stable lexical order is an independent expected
   order; comparator calls are bounded by the published sort contract
   `n * ceil(log2(n))`, not a private replay tariff.

The synthetic row probes use the actual public
`Pick<GameState, 'hollywood'>` owner input. They are explicitly NOT valid campaign
or save history: other Hollywood indexes need not match the detached rows.
They are never cast to GameState, saved, ticked or passed to gameplay actions.
The separate genuine fixture remains subject to actual execution and validators.

## Evidence and limits

Static reading and artifact hashing only. No RED or PASS is claimed. The four
semantic cases may already pass the original Map implementation; the fifth
should expose the adopted cold shared-sort obligation without inventing a policy
failure. Actual Vitest spy interception and generic typing remain unexecuted.

This is not full cold-bill proof: bounded lower-bound iteration counting, both
lexical operations, equal-run scans, query/calculator/cache charges and whole
Ready replay affordability still require source review and parent-owned checks.
No wall-clock threshold, private cost constant, timeout increase, schema change
or 200000-cap relief is introduced.

Next: parent reads frozen bytes, obtains bounded review, installs and records a
serialized baseline after287 source handback; only then releases any employment
owner implementation. Existing tests and all other artifacts remain unchanged.
