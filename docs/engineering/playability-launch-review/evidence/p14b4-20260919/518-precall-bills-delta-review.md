# 518 — Independent actual-delta review of the frozen 517 candidate: REFINE (one bounded correction)

2026-09-20. Project `contract-auditor` profile, READ-ONLY, model override fable, observed as
Fable 5.1. It read only the frozen snapshot copies of the 517 candidate
(`65cf1bf1…c697e`), the 505 predecessor and the exact production patch (`44e54ca7…4745`),
the writer handback, the owners, 176/516/514/515 and the pinned tests, and reproduced the
505-measured bills 11193 / 9587 / 8905 and the paper 9234 from the source before recomputing
the 517 values. Verdict: **REFINE**.

## Parent addendum

- KEEP, verified against the owners: every C2 term (n = 1, n = 2 all-retained, and the mixed
  new + retained slate), every C3 term, the rT7 occupied-key and walk-width terms, the
  admission one-slot arm, all selected-arm prepayments, and the byte-identical values AND
  payment sequences of every non-selected arm (both 112-node lump bounds recounted at 72 and
  70 nodes on the general arm, so the disclosed unpaid selections are absorbed with 40+ nodes
  of headroom; no unconditional `work.pay` insertion is needed or wanted).
- Predicted values CONFIRMED by hand and, independently, by the 525 measurement: 315 / 6861 /
  8099 / 8398 / 850 / 7948 / 576 and 5668 on the now-reached Post exit.
- Defect 1 (the one correction): at 517 line 1359 the single-early requirement-walk width
  `2 + capabilityText` is a lawful static bound at rT7 (405 ≤ 410) but NOT at rT6, where the
  `soundstage` requirement's retained arm compares facility IDS of width up to `d` and the
  517 term has no other `f × (2 + text)` component: guarded-domain worst case 521 against 410.
  On the 276/302 fixture the executed compares (≈ 322) still fit, so no measured control
  moves, but 176 requires a static bound for every input the guards admit. Minimal lawful
  repair, exactly as the writer had offered and the reviewer specifies:
  `work.calc(16).times(2 * d.f, 2 + (shooting ? text : capabilityText))` — with the calc
  allowance noted (the calc(16) tree would hold 9 nodes, so either `calc(32)` there or hoist
  `const walkWidth = shooting ? text : capabilityText` under the existing `pay(12)` at 1342,
  which has spare nodes). Label 4 then re-measures at 8578 (516's expectation); labels 2/3/8
  and every unchanged arm keep their values.
- Defect 2 (record only, no source change this slice): the `calc(16)` tree at 1512 is exactly
  full and the per-row assignment write is a ninth node (2 units/row), absorbed by lump
  headroom; changing it would move the retained-arm values, so it is recorded, not fixed.
- Defect 3 (record only): the handback's wording that the `calc(16)` allowance at 1568–1569
  covers selection nodes outside its tree is not the lawful reason; the payment closes because
  pay(40) covers the binding, three selections and the first call, while the second call's
  caller-side nodes sit inside `add`'s argument list (28 ≥ 21). Corrected here.
- Fixture-exact executed compare counts recorded for later audit: rT7 ≈ 255 ≤ 410; rT6 ≈ 322.
- Routing: ONE bounded correction to the sole `sim-core` writer (526), limited to line 1359
  and its calc allowance; re-freeze/hash/pins; a bounded independent re-review of that delta
  (527); the six serial fixed-source groups (528–533) and a 514-pattern passive
  re-measurement (534) requiring 315 / 6861 / 8099 / **8578** / 850 / 7948 / 576, 5668 and
  708; then the qualified checkpoint (535). The 519–525 evidence is retained as the record of
  the pre-correction frozen candidate `65cf1bf1…`; nothing is discarded.

## Verbatim 518 handback (SHA256 `c9f37bdbb3fc2b72e3234244ec7cc3d63024947330d491aa3c47834c2a78f1fd`)

> **Verdict: REFINE** — KEEP every C2/C3/admission term and the rT7 occupied-key term as written; one bounded correction required at 517 line 1359 (`shooting ? text : capabilityText`), which restores 516's label-4 value 8578 and makes the single-early bill a lawful domain bound at rT6 again. No other change.
>
> **Status: DONE** (read-only; no file created/modified, no command run, no delegation). Model observed: harness identifies me as Fable 5.1 (`claude-fable-5-1`). The lean-ctx MCP tools named in the user CLAUDE.md were not exposed; Read/Grep only.
>
> ## Sources read (absolute paths, ranges)
> - Frozen snapshot `/Users/zacheryspector/The-Movies-recovery-snapshots/20260920T155452Z-517-frozen-candidate/`: `517-protected-patch.patch` whole; `517-exact-precall-bills-implementation.md` whole; `517-promiseCapacityOwnerReplay.ts` 118–200 (LITERAL), 200–345 (Work), 340–440 (sortBill/sorted/find), 1080–1170 (workflowUpdate/orderBill/restrictedSweepBill head), 1160–1600 (allocationBill, smallTransitionBill, hasUsableRehearsalSet, singleEarlySweepBill, singlePostExitProof, sweepBill), 1840–1990 (frame; external at 1898–1916; reserve site 1946–1950), 2380–2450 (initialAdmissionBill); `505-promiseCapacityOwnerReplay.ts` 1170–1220, 1415–1530, 2340–2390 (spot-read against the patch hunks).
> - Worktree `/Users/zacheryspector/The-Movies-headless-program`: `src/core/operations.ts` 96–131, 212–427, 540–654, 1240–1436, 1576–1831; `src/core/occupancy.ts` 244–250, 280–659; `src/core/productionPhases.ts` whole; `src/core/technologyProduction.ts` 40–104; `docs/engineering/playability-launch-review/evidence/p14b4-20260919/516-exact-precall-bills-direction-review.md` whole; `176-started-replay-work-contract.md` whole; `514-command-result-probe.md` whole; `514-command-result-stale.txt` sweepBillDone/frameEntry lines (grep); `515-stale-route-attribution-direction.md` by grep (§4 heading, 9234 decomposition line 298); tests `p14b4-started-owner-replay.test.ts` 150–360, `p14b4-started-replay-retained-development.test.ts` whole, `p14b4-ready-owner-replay.test.ts` 150–270, `p14b4-ready-replay-background.test.ts` 100–200.
>
> Calibration: from the 517 expressions and the 514 `Dimensions` record I reproduced the 505-measured 11193 (label 2), 9587 (label 3), 8905 (label 4) and the analysts' 9234 (label 8) exactly, before recomputing the 517 values below.
>
> ## Item-by-item
>
> **1. Scope — MET WITH EVIDENCE.** The patch has exactly four hunks: `allocationBill` (517 1172–1225), `singleEarlySweepBill` (1316, 1354–1360), `sweepBill` (1474–1516, 1560–1574), `initialAdmissionBill` (2422–2426, 2436). Spot-reads of the 505 copy at 1170–1213, 1415–1530, 2340–2389 match the `-` sides. `dimensions`/`commandDimensions`/`LITERAL`/`Work`/`smallTransitionBill`/`singlePostExitProof`/`restrictedSweepBill`/`frame`/admission owners are outside every hunk. `restrictedSweepBill` returns at 1441 before any changed line, so labels 1/5/7/rT1 cannot move.
>
> **2. C2 terms — MET WITH EVIDENCE (n=1, n=2 all-retained, mixed new+retained).**
> - `phaseTransitions = small(1,1,0)` (1567) = 470: `releaseCompletedPhase` keeps the slot (`retainedCapabilitiesFor('development','preProduction')` = `['development-casting']`, productionPhases.ts 55–56/160–161), `released.length === 0` returns the same workflow (operations.ts 1264) so `enterPhase` 1321 skips the release transition; one `recordReservationTransition` at 1380 with before=[1 key], after=[1 key], both `includes` hit, no event (554–573). Key width `facility-development-casting:0` ≤ d+26.
> - `releasePhase` 339 (1479–1480): two capability compares (productionPhases.ts 161 filter `includes`; operations.ts 1257 `retained.includes`) ≤ 4×equality(19); `claimed.has/add` on one 19-char key ≤ 2×keyBill(1,19); `LITERAL.phaseRelease` 19; no spread. Same shape as the accepted 1379.
> - Walk 1504–1516: runs only after the retained loop completed without `break` and after `attempts`; reads every `operations.workflows` row. `retainedClaims` = Σ(reservations + task + setId≠null) ≥ actual `resourceClaims({operations})` rows (occupancy.ts 409–437 one per reservation, one per non-null task; 570–587 a set claim only when setId≠null AND a soundstage reservation exists) — upper bound. `retainedOccupied = external + Σkeys − 1`: `occupiedSlots` 242–259 indexes owner `production` only (task claims are owner `shootingTask`, never indexed; set claims are owner `production` and key `set:<id>`, counted by the walk), excludes own id (`claimPasses` 637–646); each retained picture holds exactly one key by the loop proof (1459–1462), so others' keys ≤ Σkeys − 1 exactly. (a) n=1 fixture: claims 1, occupied 0. (b) n=2 all-retained (`p14b4-started-replay-retained-development.test.ts` 120–135: each workflow one dev-casting reservation, setId null, no task): claims 2, occupied 1 — exact for both attempts (retainedMoves 2). (c) mixed (`p14b4-ready-owner-replay.test.ts` 231–236): the NEW picture is skipped by the retained loop at 1454 but its admitted workflow row (`addManagedProductionWorkflow` 645–653 appends it with one reservation) is counted by the walk: claims 2, occupied 1; the retained picture's `occupiedSlots` sees exactly the new picture's key. Attempts stay `retainedMoves` = 1 (the new picture settles at operations.ts 1658–1660 without `enterPhase`).
> - Nonnegative operand: the walk executes only when `restricted` is false, which requires some production with `startTick < week` (1438); for `retainedDevelopment` to survive that production is at 8 with exactly one reservation, so `slateKeys ≥ 1` and `slateKeys − 1 ≥ 0`; `calc(8).add(d.external, slateKeys − 1)` never receives a negative operand.
> - Retention ×1 with `keyBill(occupied, keyLength)` (1214–1216): `requirementsForPhase('preProduction')` is one static entry; the retained arm (375–391) runs once; `facilities.some` id compares ≤ `times(d.f, 2+text)` = 295 (this term DOES sit inside the retention expression, unlike the single-early bill — see defect 1); `occupied.add` at 389 is the first insertion, Set holds exactly the supplied union. 20/140 constants kept.
> - Walk ×1 (1224): the retained arm `continue`s at 390 before 394–415; one walk is conservative (exact 0). `update` ×1 (1572): `replaceWorkflow` only at 1430 (1356 skipped; 1370 unreachable because `allowsFacility` never excludes `development-casting`, technologyProduction.ts 69–77, and the own slot is by construction free).
>
> **3. C3 terms — MET WITH EVIDENCE.** `retainedCapabilitiesFor('postProduction','releaseReady')` = `[]` (productionPhases.ts 60): `[].includes` in the filter and `retained.includes` in the 1256 loop perform zero compares; `claimed` untouched; released=[post] → spread (1266) and `deriveBindings` spread (227) → copy terms stay, `8*text` leaves: 343 ✓. Transition 1354: before [1 key], after [] → one `reservationReleased` → small(1,0,1)=186; `allocateForPhase('releaseReady')`: `needsSet` false, loop 375 has zero iterations → always `ok`; transition 1380 empty→empty → small(0,0,0)=20 (covers invocation/empty arrays per 176 §6 "zero retained keys do not remove the invocation cost"). `update ×2`: 1356 and 1430; 1370 unreachable. Two sweep visits (1654–1817): round 1 visit → advanced, `released` true → `break` (1806); round 2 → `settled.has` → `continue`; loop exits. `rounds = 2` → `visits = 2` → `commonVisits = 2` (1498, 1577); `attempts` stays 1. The pinned control `p14b4-started-owner-replay.test.ts:259–352` (weeks=2) exercises exactly this path.
>
> **4. C4 terms.**
> - `occupiedKey = keyBill(d.external + (shooting ? 1 : 0), keyLength)` (1316) — MET WITH EVIDENCE. rT7: Development slot released before allocation (`retainedCapabilitiesFor('preProduction','rehearsal')` = `[]`), sole workflow, own claims excluded, `setId === null` (guard 1288) → every `occupied.has` at 326/403 sees exactly the external union; the one `add` at 411 is last. rT6: `retainingSet` true (313–317), no composite; the retained stage key is added at 389 before the set-scenery walk, so its `has/add` see ≤ external+1; the 389 add itself sees `external` (≤ 164 used, conservative).
> - Requirement-walk width `2 + capabilityText` at BOTH frames (1359) — **DEVIATES at rT6** (defect 1), MET at rT7. rT7: only executed walk is `soundstage` over f facilities: `facility.capability !== capability` ≤ 1+19+10 per facility, plus on the two stages `capability === 'soundstage'` (21), `boundSet !== null` (2), `facility.id !== boundSet.mountedOn` (≤ 2+equality(28)=59): domain worst 5×41 + 2×(39+2+59) = 405 ≤ 410 (writer's figure confirmed; fixture-exact ≈ 255 with the real ids `facility-development-casting`/`-post`/`-scenery-shop`/`-soundstage-07`/`-soundstage-12`). rT6: the `soundstage` requirement takes the retained arm, whose `facilities.some((f) => f.id === retained.facilityId)` (382) compares facility IDS of width up to d, not capabilities; in 505 that was absorbed by the `2 + text` width (590). The 517 single-early `requirements` term has NO other `f × (2+text)` component (30, 3×capabilityKey, 2f×(2+capabilityText), 2×capabilityText, reservationCopy, 6+keyConstruction+occupiedKey), so the guarded-domain worst case at rT6 is some 5×59 = 295 + scenery walk 5×41 = 205 + one `capability === 'soundstage'` 21 = 521 against 410 (+78 if `2×capabilityText` is credited): shortfall 33–111 units. On THIS fixture the executed cost is ≈ 176 (four id compares before `facility-soundstage-07`) + 146 = 322 ≤ 410, so no measured under-reservation, but 176 §8 item 4 / §10 require the reserved bill to be a static source bound for the inputs the guards admit, and the writer's comment at 1355 ("their operands are phase-table capabilities") is false for the retained arm. The writer disclosed the deviation and named the repair.
> - `initialAdmissionBill` `examined = occupied === 0 ? 1 : developmentCapacity`, `slotKey = keyBill(0, keyLength)` (2425–2426, 2436) — MET WITH EVIDENCE. `addManagedProductionWorkflow` 604–626 allocates the draft (no reservations/task/set) with the external union; with `occupied.size === 0`, the first Development slot in id order is free: one `has` (403) and one `add` (411) on an empty Set, then `break`; zero capacity throws before examining any slot (pricing one is conservative). `occupied > 0` keeps `developmentCapacity × (14+106+key+key)`; `key` still feeds both occupancy terms (×0 at occupied 0). The `calc(8).keyBill(0, …)` executes only on the `occupied === 0` arm, so the background control (`p14b4-ready-replay-background.test.ts` 176–178, casting row on the same facility) keeps 505's values and payment sequence.
>
> **5. Prepayment on selected arms — MET, one nit.** Recount: 1506 pay(16) = guard read/branch 2 + `slateKeys` binding/literal 2 + `operations.workflows` 2 + iterator setup/terminal 2 = 8 ✓. 1509 pay(24) = visit 1 + `row.reservations.length` 3 + `row.bindings.setId` 3 + `=== null` 1 + branch 1 + literal 1 + `+` 1 + `keys` binding 1 = 12 ✓. 1511 calc(8): 2 operand reads + the assignment write = 3 ≤ 4 ✓. 1512 calc(16): retainedClaims 1, keys 1, `+` 1, `row.shootingTask` 2, `=== null` 1, branch 1, literal 1 = 8 = 8, so the assignment write to `retainedClaims` is a 9th node per row (2 units/row uncovered; absorbed by lump headroom, nit only). 1514 pay(4): covers the `retainedOccupied` write; the `slateKeys − 1` operands are inside 1515's calc(8) tree (d.external 2 + slateKeys 1 + `−` 1 + literal 1 = 5 > 4 by one node if the literal is counted; the pay(4) spare covers it) ✓. 1480 calc(32) tree: 40, `4*`, `2*`, `LITERAL.phaseRelease`, 6 = 8 ≤ 16 ✓; nested equality/keyBill pay themselves. 1567: binding 1 + selections 4 + call 7 = 12 ≤ 20 in pay(40) ✓. 1568–1569: the ternary tests are evaluated BEFORE the `work.calc(16)` receiver, so they are not that calculator's argument tree and the writer's sentence "the calc's 8-node allowance carries the excess [selection]" is not a lawful assignment as worded. The arithmetic still closes lawfully: pay(40) covers binding + three selections (7) + the first call's 7 nodes; the second `smallTransitionBill(...)` call expression IS inside `add`'s argument list, so its caller-side nodes (callee, five reads, invocation = 7 ≤ 8) are lawfully in the calc(16) tree; total allowance 28 ≥ 21 executed. No repair needed; the handback wording should be corrected in the record. 1572 ternary inside `calc(32).times`: 7 + `update` 1 = 8 ≤ 16 ✓. 1573: the `allocationBill(...)` call now has 10 caller-side nodes vs pay(16)'s 8; it sits inside the `enter` `calc(64).plus` argument list whose tree holds ≈ 12 of 32 nodes, so covered ✓. 2425–2426: 13 new nodes (two bindings, two `occupied === 0` compares/branches, `key` read, literals) against pay(64) at 2418 with ≈ 8 previously used → 21 ≤ 32 ✓.
>
> **6. Non-selected arms byte-identical — MET WITH EVIDENCE (values and payment sequence).** General arm: 1479 false → `calc(64)` receiver → `singlePostExit ? 0 : calc(8).times(8,text)` → plus: pays 64, 8, 10, 52 in 505's order; `wear`/`rounds`/`attempts`/`policyLock`/`setup`/`binding` unchanged; 1564–1570 falls through three false tests to `calc(32).times(2, transition)` (same pays 32, 10); 1572 `times(3, update)`; `allocationBill` executes `calc(8).times(4, rawOwners)` then `calc(8).add(d.external, calc(8).times(3, other))` in 505's order, general `retention`, walk ×2. Label-6 arm (wrapOnly/singleWrapSlot): retained loop breaks at remaining 4, proof false at remaining 2 check, `singleWrapSlot` arm of `phaseTransitions` executes first, so 1567/1568 tests never run; `retention` takes the `singleWrapSlot` arm before the new `retainedDevelopment` test. Admission occupied > 0: no new calculator. Restricted: returns at 1441.
> Recount of the two 112-node lumps (nodes outside calc argument trees and outside locally paid sub-blocks; I count parameter bindings, `Math.max` call nodes and helper call nodes on the caller, so my figures are more inclusive than the writer's 54/58): `sweepBill` 1469–1582 general arm = 72 nodes (1469 2, 1470 2, 1473 1, 1479 3, 1482 6, 1483 5, 1486 8, 1489 8, 1492 8, 1498 13, 1499 8, 1504 4, 1505 2, 1572 1, 1577 1); `allocationBill` 1172–1224 general arm = 70 nodes (params 8, 1182 14, 1183 5, 1184 3, 1185 3, 1186 3, 1189–1198 five bindings 5, 1199 10, 1206 6, 1212–1218 net 4 after pay(8), 1219 8, 1223 1). Both ≤ 112 with ≥ 40 nodes headroom on every arm; the new unpaid selections (my count 10 in sweepBill + 8 in allocationBill = 18 nodes; writer 20, the difference being the 1568 test which lands inside pay(40)'s region where the general arm used 3 of 20) are absorbed. No shortfall; no unconditional `work.pay` is needed, and adding one would shift every arm's `used`.
>
> **7. Predicted values — CONFIRMED** (hand arithmetic from the 517 text, 514 record, t=0 at weeks 1–4, t=1 at weeks 5–8, commitments 0):
> - Label 2: allocationBill 4194 (occupancy 640 = raw 459 + 4 + 177; callback 235; sort 2158; retention 708 = 20 + [20+114+295+98+106+55]; walk 295; 18 + 140); enter 5580 (339+65+470+68+4194+98+95+101+150); common 1218; policyLock 63 → **6861** ✓. The 85 below 516's ≈6946 is exactly 424 − 339 (516 estimated the release at −375 from 799; the adopted 1379 shape is −460).
> - Label 3: occupancy 213, facilities 2381, requirements 695, slotSearch 663 (3×(5+106+110)), composite 1147, +80+78+50+26+29 → allocation 5362; entries 6023; releaseCopy 530; success 740; common 806 → **8099** ✓ (= 9587 − 1308 − 180).
> - Label 4: occupancy 1131 (LITERAL.claim 94), requirements 1028, slotSearch 439, allocation 5292, entries 5953, success 1639 (858+260+291+182+25+12+3+8; LITERAL.shootingTask 56), common 806 → **8398** ✓ as written; **8578** after the defect-1 repair (the −180 at rT6 reverses), matching 516's expectation.
> - Label 8: allocationBill 3193 (t=1 → callback 810), enter 4387, common 1218 (2 visits), policyLock 63 → **5668** ✓.
> - Admission: slot term 896 → 230 (−666); **3913** conditional on 515-A's measured 4579, which I did not recompute.
> - 315 / 850 / 576 / 708 (restricted) and 7948 (label 6): unchanged by construction (item 6).
>
> **8. Coverage and risk — MET, with limits.** Four pinned controls cover: n=1 retained (276 label 2), n=2 all-retained, mixed new+retained (Ready 231–236), admission occupied>0 (background 161–178), single Post exit (started 259). `external > 0`: retained arm uses `d.external` in `retainedOccupied`, raw and occupancy; single-early keeps `+ d.external` and drops the `min` at 1346 → conservative; no GREEN control exercises external>0 at a single-early or retained frame (NOT VERIFIED by test, source-verified only). A slate mixing a retained-8 picture with a started picture at any other countdown breaks the loop at 1455 → general arm; only NEW pictures can coexist, and NEW pictures are admitted at 8 with no task/set (the walk would count them anyway). Discovery: the walk reads workflow fields already discovered by the 176 §4 join and pays them at 1509; `dimensions`/`LITERAL.dimensions` untouched; no deferred-cold or table-history effect. Pre-existing observation, out of 517 scope: `allocationBill`'s per-claim constant 89 (1190) is below `LITERAL.claim` 94 + APPEND used in the admission bill; unchanged by 517, not a 517 defect.
>
> ## Defects, ranked
> 1. **rT6 requirement-walk width (517 line 1359; 176 §8 item 4 / §10 static-bound requirement; 516 item 5 authorized the width only with the rT7 coverage statement).** Concrete failure: for a guarded-domain input whose retained stage sorts late and whose facility ids approach d, the reserved single-early bill at 6→5 is below the source count by up to ≈ 111 units (33 if `2×capabilityText` is credited to the walk compares). On the 276/302 fixture the executed compare work (≈ 322) still fits 410, so no measured control moves; the violation is of the static-bound law, not of a fixture. Minimal lawful repair, as the writer offered: `work.calc(16).times(2 * d.f, 2 + (shooting ? text : capabilityText))` at 1359 (one-node growth inside a calc(16) tree that currently holds ≈ 6 of 8 nodes: `2*`, d.f, `+`, capabilityText → adding shooting read + branch + text read = 9, so use `calc(32)` there or hoist `const walkWidth = shooting ? text : capabilityText` under the existing pay(12) at 1342 which has spare nodes). Label 4 then re-measures at 8578; labels 2/3/8 unaffected; the rT7 frame keeps 8099 (the `text` arm is not taken).
> 2. **1512 `calc(16)` tree is exactly full; the assignment write is a ninth node per row** (2 units/row). Absorbed by lump headroom; cosmetic. Repair if desired: `calc(32)` at 1512 (shifts only the retained arm by 16 units per row; would move the predicted 6861 and both retained controls, so I recommend NOT changing it in this slice and recording it).
> 3. **Handback wording at 1568–1569** claims the calc(16) allowance covers selection nodes outside its tree; the payment is lawful for the reason given in item 5, not the reason stated. Record correction only; no source change.
>
> ## Proposals (separate from defects)
> - Record in 518 the exact-fixture executed compare counts for rT7 (≈ 255 ≤ 410) and rT6 (≈ 322) so the static-bound margin is auditable later.
> - Keep the walk placement after the loop (stricter than 516 item 3's in-loop offer); do not "fix" it to the in-loop form.
> - Out of scope, for a later slice: the `89` per-claim constant at 1190 versus `LITERAL.claim` 94 + APPEND in the admission bill deserves a source recount.
>
> ## Evidence limits
> No execution; every figure is hand arithmetic from the frozen 517 text and the 514 record. Label-3/4/8 totals recomputed in full; admission 4579 taken from 515-A. Facility ids and the set-0/soundstage-07 mount are from `operations.ts` 96–116 and 516's fixture note, not from a fresh fixture read. Tests other than the four pinned files were not opened; Codex logs not opened; the live worktree replay file was not read.
>
> ## Next action for the parent
> Route a bounded correction to the sole writer for line 1359 only (`shooting ? text : capabilityText`, with the calc allowance or hoisting noted in defect 1), re-freeze/hash, then run the serial groups and the 514-pattern passive re-measurement with equality required on 315 / 6861 / 8099 / **8578** / 850 / 7948 / 576, 5668 on the unreached Post exit, 708 restricted, and 3913 admission. If the parent instead chooses to qualify 517 as frozen, record defect 1 as an accepted domain-bound deviation with the fixture-safe figures above; do not pair that with any unconditional `work.pay` insertion.
