# 209 — explicit director / non-grandfather-clear command supplement

2026-09-20. INERT / UNEXECUTED. Native Fable test-author body read. Parent reports
205's corrected199/200 plus182 run22PASS; that does not execute209.191 is frozen,
and no replay implementation or191 handback was inspected. No protected edit,
runtime, probe, typecheck, Git/network or delegation. Only209 draft/brief written.

Draft `209-started-replay-director.test-draft.ts`:229 lines, TWO default-timeout
cases; intended `tests/p14b4-started-replay-director.test.ts`.
SHA256 `6b2be605e96d517f3567a443bd04baed5107cd2bbf732957c25bb186d1dd9cf0`.

## Genuine route / exact scope

Uses178's historical-control-founder fixture, real week-zero fresh Hollywood
initialization, actual greenlight and ticks to the first genuine countdown5.
It stops BEFORE the Director action. The source must have exactly one current
Shooting workflow, its real locked director, task unassigned, no blocker, actual
stage/Set bindings and `requiresSetBinding === true`. Strict current-save and
all supported-root/foreign-relevance guards remain in force. No clock, binding,
trip, cash, history, employment or Set provenance is authored by the test.

1. The actual reference Director action must settle the real due-at-call trip
   to Ready at this current boundary. This is a hard UNEXECUTED fixture guard,
   not an assumed time or a fabricated trip. Actual schedule then tick must
   produce5→4 and one arrived-week firstTake. Replay receives the commands in
   reversed array order with correct ordinals0/1, proving settled ordinal order
   rather than incidental input order. Original-time pass-through spies pin the
   actual locked ID, assignment → restricted arrival → schedule → next-week
   arrival → whole-slate sweep. They call the original owners without stubbing
   results or replaying callbacks for measurement. Exact local projections,
   actual sceneryArrived draft/owner week, relative provenance order, no lower
   manual clear, unchanged holds and input purity are required.

2. On the SAME genuine unassigned current workflow, the scenery classifier is
   `none`, not `manual-clear`, and the actual public Clear action refuses because
   no load-in blocker exists. A hypothetical `clearGrandfatheredScenery` command
   must be `commandRefused` BEFORE lower clear or sweep, leaving all source roots
   unchanged.137 permits lower clear only for actual manual-clear classification.
   This deliberately pins the reached source branch truthfully: it is NOT a
   claim to have constructed in-transit or arrived-pending current scenery.

The requirement sources are137/176's command order and actual established
action/scenery owner laws (actions1778–1810, operations684–713/747–785 and
sceneryLoadIn classifier). No new replay observer/API, implementation-private
tariff, guessed step number, skip, tolerance or timeout increase.200000 cap remains
strict. The first take lies exactly at H, so no count/ACHIEVABLE assertion follows.

## Explicit limits / handback

Both cases and the director-due-at-call fixture guard are UNEXECUTED.178/205's
related reached routes are not substitutes for running this new unassigned-source
variant. If actual geometry is not due at this boundary, retain the fixture
failure; do not manufacture Ready or patch a load-in trip.

Grandfathered manual-clear SUCCESS is not constructed. Existing scenery-owner
documentation names historical V14-migrated workflows with exact false provenance,
but no current replay-ready genuine migration fixture/root conjunction was
established in this bounded task. No flag flip or fabricated grandfather history
is used. In-transit/arrived-pending refusals and a successful migrated manual clear
remain explicit later coverage, not silently counted as209 proof.

Next: parent independently reads/reviews/freezes/installs, then serially runs the
two cases and typecheck against its frozen source.209 takes no runtime ownership;
all prior tests/drafts remain unchanged.
