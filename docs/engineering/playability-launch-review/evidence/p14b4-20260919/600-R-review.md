# 600-R — contract-auditor review of the landed P14B.4 coordinated cutover candidate (HEAD f2192d8b)

Status: DONE (read-only). Nothing executed; no file edited; no git, hash, gunzip or vitest available to me. Every PASS/FAIL number below is read from the archived record-check captures (603–615) or from the writer/test-author reports; every source claim is from the committed files at HEAD as the Read tool shows them. Model observed: harness banner only; the lean-ctx MCP tools named in the user CLAUDE.md were not exposed (Read/Glob/Grep only).

Verdict up front: **QUALIFIED WITH RECORD-ONLY ITEMS.** S1–S5 as committed implement the record-600 evaluator-4 law and the C1–C12 conditions; the 600-T2/600-T3 reconciliations are value swaps and evidence-derived re-expressions with no loosened assertion, no moved historical read and no missed live pin that I could find; every one of the 39 failures in 610 is classified (22 inherited, 2 designated, 11 natural premise, 4 load-only cleared alone in 611); root, UI and bridge typechecks are clean (612/613 exit 0). No DEMONSTRATED defect in source or tests. The record-only and REFINE items are bookkeeping (stale titles/comments, one C2 deviation lawfully reconciled, fixture findings for a later test-author pass, the Unity backlog).

---

## Q1 — Law conformance of the landed source: MET WITH EVIDENCE (S1–S5), C1–C12 as stated below

Verified by reading the committed files at HEAD (line numbers are HEAD).

**S1 core (47b2bbf4).**
- Live aliases: `src/core/types.ts:2231-2234` (`ProfessionalPromise = ProfessionalPromiseV30`, `GameState = GameStateV30`); V29/V30 definitions :2205-2229 verbatim.
- Union: `src/core/promises.ts:194` `PromisePredicate = { count: number } | CastRoleCountPredicate`; `PromiseDraft.predicate` :200; `PromiseAttachment.predicate` :466.
- Rules 4 with the law named and evaluator 5 named as later: :39-45. Record 23 satisfied (the literal moves inside the same commit as the evaluator change; parent-verified diff).
- Mask law: `seatedPreFirstTake(state, studioId, personId, slots)` :272-282 (recorded-take exclusion :278 kept); threaded at `expectedFirstTakeWeek` :354 and `existingPath` :424 through `promiseCastSlots(draft)` (:592-597: count-only → three seats, lead → lead, leadOrAntagonist → lead + antagonist). A fixed seat outside the mask is not an event (falls to :364 `from + k*8 + 5`).
- Refusals (C11): the P2 line is gone from `NOT_OFFERED_IN_B1` :213-217 (P3–P5 sentences byte-identical to 600-A :206-208); classless-P2 read refusal :393-395 `a seat-class promise needs its seat class selected (lead, or lead-or-antagonist); without one it is not offered` (plain-English per 600-B Q11 REFINE, same meaning, same IMPOSSIBLE classification); tagged non-P2 read refusal :396-398 `a selected seat class applies only to a seat-class promise`; attach-time throw :510-514. Placement after the P3–P5 check and before the numeric/window checks (:399-404), so P1 and P3–P5 orders are unchanged.
- Residual buffer (C12): IMPOSSIBLE at `reserved + X > nMax` :422; FRAGILE "the schedule leaves no spare picture inside the window" at `reserved + X > nMax - promiseBuffer(nMax)` :432-434; existing-path :435-437 and slack :438-440 unchanged. Exactly R2.
- Inputs digest: the class row appended at the END only when tagged :340-341; every count-only tuple :320-339 unchanged.
- Attach: full detached copy in two typed branches :544-550 (600-B REFINE adopted); `version: PROMISE_RULES_VERSION` :527; at-most-one :503-507; redigest :553-559.
- `settle` narrowed to the five outcome fields :615-621.
- Untouched: `breakPromisesOnCancel` :723-740 (causal coupling :732 still `!== 'IMPOSSIBLE'`, record-only as 602 R6); both validator branches (V30 :962-971, V29 `exact(predicate, ['count'])` :975); `promiseBuffer` :66-68, `PROMISE_SLACK_WEEKS` :62, `WEEKS_TO_FIRST_TAKE` :53, the 1000 loop bound :419.

**S3 save (3b8f5b6a).** `LIVE_SAVE_VERSION = 30` `src/core/save.ts:6396`; `makeSave` → `validateSaveV30`/`SaveFileV30` :6400-6405, validation before detachment :6401-6404; `SaveFileV30` doc :497-498; `migrateToV30` doc :8712-8713; `validateSaveV29` :8604-8618, `convertV30ToV29` lossless-only refusal :8704-8710, `migrateToV29` :8669-8673 unchanged; `index.ts:1292` comment only. **C7 MET:** `src/harness/p14/legacy-v28-fixtures.ts:25` (import adds `SaveFileV30`) and `:40` (parameter union) are the only changed lines; FROZEN header :13-17 and the `!== 28` guard :47 read exactly as 600-A quoted them.

**S4 load/runtime (2cbc3de5).** `bridge/session.ts:41,136` (`converted` :135 still compares to `LIVE_SAVE_VERSION`); `bridge/runtime/campaign-library.ts:5,48,127`; `ui/src/engine/adapter.ts:112,3796,3808,3822`; **C6 MET:** `src/harness/d16/run-d17b-continuation.ts:37,183`, `run-d17b-week86.ts:35,124`; `bridge/runtime-checkpoint.ts:6-7` import, `:443` `CurrentEnvelopeSave = SaveFileV30`, `:458-462` strict "must be a current V30 save" / "canonical V30 save bytes", `:847-850` prior chain via `migrateToV30`, `:996` R05 annotation type-only (guard :997-998 not widened). No remaining live consumer of `migrateToV29`/`SaveFileV29` in src/bridge/ui (grep: only the frozen save.ts converters and the fixture minter's type union).

**S2 policy (a64ac00b).** `src/core/talentMarket.ts:1275-1296`: `window` = full proposed term :1278; `p1` :1279; `flexible` `{kind:'castRoleCount', count:1, seatClass:'leadOrAntagonist'}` :1280-1284; order `isProven ? [p1] : [flexible, p1]` :1285 (`isProven` :685-688 unchanged archetype); each read via `promiseFeasibility` on the same unchanged `state` :1286-1292; attach on the first REASONABLY_ACHIEVABLE :1293; else `return state` :1295. No RNG, no count escalation, no lead-only selection, no staged failed root. Matches plan :199-207 exactly. Doc block :1256-1274 rewritten. Type-only import :37.

**S5 wire (027155e7).** `bridge/schema/bridge-schema.ts:222-233` projection-47 header + `PROJECTION_VERSION = 47`; closed family-discriminated union :1769-1792 (member A `literal('LEAD_OR_SIGNIFICANT_ROLE_COUNT')` + REQUIRED `seatClass` enum :1779-1783; member B four count-only families :1785-1788; union name kept so :1802 and the exported type are unchanged); definitions :3301-3302; `seatClass: nullable(enum)` on own snapshot :2256 and history row :2268; `preferredOpportunity` :2328. ONE wire→core conversion `corePredicateOf` `bridge/promises.ts:48-52`, used by `promiseQuoteSnapshot` :136 and `prepareMarketProposal` attach `bridge/contract.ts:467`; `MarketPromiseDraft = WirePromiseDraft` :339; `promiseHistoryFor` shape-derived `seatClass` :108. `bridge/people.ts:42,990` `preferredOpportunity` on the one emitter (`line` :991 unchanged). **C4 MET:** `DisclosedPromise.seatClass` `talentMarket.ts:462-464`, `disclosedPromise` :547 (shape-derived, never version-derived). **C3 MET:** registry entry `bridge/runtime-checkpoint.ts:60-63` (`584bdd…` → `projection-v46`), 35 entries; `SCHEMA_ID` now `sha256:6f6b4880…` (manifest :8, Generated.cs :3), so the current-ID exclusion holds (runtime47 6/6 in 610 :1269). Generator artifacts: manifest `projectionVersion 47`, `protocolVersion 4`, `generatorSourceSha256 17da544b…`, `typescriptGeneratedContractSha256 5df92d48…`; C# abstract base :6667, sealed members :6570/:6607, converter :9887 registered :283.

**Nothing evaluator-5 smuggled; nothing evaluator-4 missing.** grep of src/bridge/ui: `promiseCapacityKernel`/`OwnerReplay`/`Owners`/`Enumerator` are imported only by each other; the only `UNCERTIFIED` in `promises.ts` is the :43 doc line naming it as later; `MAX_LIMITS.work` still 200000 (`promiseCapacityKernel.ts:117`, replay invariant :2691). The stale route still stops at `tests/p14b4-ready-replay-stale-target.test.ts:234` expecting `commandRefused` and receiving `workLimit` (610 :2420-2429). No cap, tariff, timeout, refusal-in-force or fixture moved in the files I read. Refusal strings in force unchanged apart from the authorized P2 NOT_OFFERED replacement.

**Per-commit placement (C3/C4/C5/C6 in the RIGHT commit; commit messages; one push):** NOT VERIFIED by me (no git). The writer report §S4/§S5 states the registry line and disclosure hunks are S5-only, and the brief records the parent's byte-for-byte verification of each cumulative patch; I take that as the parent's evidence, not mine.

**One deviation from C2 (RECORD-ONLY, lawfully reconciled):** the bridge typecheck on the frozen S5 candidate (604 :7-13) reported `tests/bridge-p06-checkpoint-recovery.test.ts:167,174` (C2) PLUS `tests/bridge-p14b3-promise-command.test.ts:155` (TS2322: a classless P2 literal is not a wire shape under the closed union). Not a src/bridge/ui diagnostic, hence not a defect under C2's own terms; inherent to S5.4 (`MarketPromiseDraft = WirePromiseDraft`), reconciled by 600-T2 §B(iii); 613 exit 0. Root typecheck 603 :7-19 = exactly the five C2 lines (D1 of 600-B included); 612 exit 0.

## Q2 — The 600-T2 sweep and 600-T3: MET WITH EVIDENCE; no historical read moved, no live pin missed

Sampled at HEAD (34 changed lines across 18 files, all value swaps of the precedent classes):

| File:line | Class | Read at HEAD |
|---|---|---|
| `tests/_historicalCurrent.ts:1,8,9,11` | A3/A4/A2 | `migrateToV30`, `SaveFileV30`, `saveVersion:30` (load-to-live helper) |
| `tests/helpers/p14b2-fixtures.ts:7,122,210,225` | A1 | `validateSaveV30(JSON.parse(JSON.stringify(makeSave(...))))` (live envelope) |
| `tests/bridge-runtime-checkpoint.test.ts:232,233,431,987` | A2 | hydrated live saves `toBe(30)`; `/canonical V30 save bytes exactly/`; `584bdd…` appended to `EXPECTED_PRIOR_IDS` (:984-986 comment) |
| `tests/save.test.ts:226-233` | A4 | `wellFormedSave(): SaveFileV30 { … makeSave(state) }` (the 600-B D1 fifth residual) |
| `tests/d17a-adv-migration.test.ts:274`, `tests/contracts/v14-boundary-guards.contract.test.ts:324` | A5 | one-past sentinel `saveVersion: 31` / `unknown saveVersion 31` (dispatch handles 1..30: `save.ts:5282-5284`) |
| `tests/contracts/cross-owner-refusal.contract.test.ts:70,72,83,84,88` | A4/A1 | `legalSave(): SaveFileV30`, `validateSaveV30(save)`, typed params |
| `tests/contracts/determinism-floor.contract.test.ts:84`; `phase-table-agreement.contract.test.ts:102,104` | A4/A1 | typed helper + `validateSaveV30` |
| `tests/p14b1-t4-regressions.test.ts:12,23,74,84,86,166,306` | A3/A2/A1 | V28 fixture lifted to live via `migrateToV30`; live envelope `saveVersion: 30` validated by `validateSaveV30` |
| `tests/bridge-p14b1-promises.test.ts:207-210,295,302,316,320,495,511` | A2/A6 | `LIVE_SAVE_VERSION 30`, projection 47, `seatClass: null` on hand-built P1 history rows, bridge re-save `toBe(30)` |
| `tests/p14b1-save-v29.test.ts:109` | A2 | `LIVE_SAVE_VERSION … toBe(30)`; the V28→V29 boundary reads :116,:123-124,:154,:162,:187,:197 STAY at 29 (historical) |
| `ui/src/saves.test.tsx:125`, `session.test.tsx:332,360,387`, `engine/d17-save-migration.test.ts:131,155`, `engine/film-chronicle-adapter.test.ts:289`, `lot/snapshot/v14SetHolderBoundary.test.ts:46` (600-T3) | A2 | the eight live-writer pins `toBe(30)`; grep of ui/ finds no other 28/29 save-version pin (the three remaining `toBe(28|29)` hits are `lotWidth`/table values in e2e specs) |

Historical reads confirmed UNMOVED (each still 29 / `validateSaveV29` / `migrateToV29`, all reading a genuine V29 corpus or a hand-built V29 envelope AS V29): `tests/p14bf2-acting-discipline.test.ts:271,281,302`; `tests/p14b3-rule-revision.test.ts:61,74-75,107`; `tests/bridge-p14b2-checkpoint.test.ts:19-20`; `tests/bridge-p14b4-runtime47-compatibility.test.ts:109,141-144,186`; `tests/p14b4-save-v30-compatibility.test.ts:83,94-95,158,215,238,260,277,302,312,335`; `tests/p14b4-cast-class-outcomes.test.ts:127,129`; `tests/bridge-p14b4-cast-class.test.ts:55-57,395`; `tests/p14b4-material-evidence-core.test.ts:55,59`; `tests/p14b1-first-take.test.ts:103-107,285-286`; `tests/p14a1-save-v28.test.ts:280`; `tests/p14b1-save-v29.test.ts` (all but :109).

Missed live pins: none found. Greps of `tests/` (excluding fixtures) for `validateSaveV29(`, `migrateToV29(`, `saveVersion).toBe(29)`, `saveVersion !== 29`, `saveVersion: 29`, `LIVE_SAVE_VERSION).toBe(29)`, `PROJECTION_VERSION).toBe(46)`, `projection-46`, `snapshotVersion).toBe(46)`, `ProjectionVersion = 46` return only the historical sites listed above plus two title/comment strings (below). 612/613 exit 0 and 610 (no A1–A6 class failure left) corroborate.

Two corrections to earlier recommendations, both resolved by 600-T2 in my favour of the sweep: (a) 600-A §4.3 said the one-past sentinel class was empty; three sentinels were at 30 and RED in 608 (`d17a-adv-migration:274` 608 :44787-44796; `d17b-save-v7:147`; `v14-boundary-guards:324`) and are correctly at 31 now (A5). (b) 600-A §4.3 listed `p14b1-t4-regressions` among "version-boundary files reading V29 fixtures"; it lifts a V28 fixture to play (:21-23) and validates live envelopes, so moving it to V30 is the load-to-live class, not a historical read.

RECORD-ONLY (text only, assertions correct): stale titles/comments left by the values-only sweep, as 6948e31 also left them: `tests/p14b1-save-v29.test.ts:108` "LIVE_SAVE_VERSION is 29", :193-194 "before the live writer cuts over"; `tests/bridge-p14b1-promises.test.ts:206` "LIVE_SAVE_VERSION stays 29 … projection 46", :498 "a live V29 state"; `tests/p14b3-rule-revision.test.ts:97` "unchanged Save29/projection46"; `tests/bridge-operations-events.test.ts:323` "projection-46 envelope" (assertion :333 is 47); `tests/d11-cycle2.test.ts:227`-style `'expected V28'` throw strings (pre-existing). 600-T2 §A's table reports 99 files while its summary and the brief say 100; the parent's commit stat governs.

## Q3 — Meaning pins: MET WITH EVIDENCE; purposes preserved, at least as strong, none self-referential

- `tests/p14bf2-acting-discipline.test.ts:302-312` and `tests/p14b3-rule-revision.test.ts:107-117`: the V29 half stays byte-exact (`exportSave(loaded/reloaded) === raw`, :303/:108); then `governed = migrateToV30(importSave(raw))` (:309/:114), `governed.saveVersion === 30`, `JSON.parse(exportSave(governed))` deep-equals `{ ...JSON.parse(raw), saveVersion: 30 }` (:311/:116: the migration moves only the tag; `convertV29ToV30` `save.ts:8697-8700` is validate + clone + tag), and `exportSave(makeSave(loaded.state)) === exportSave(governed)` (:312/:117). Together these imply the original claim one tag apart (the writer's bytes equal raw with only the version moved) and add the migration pin the original did not have. The comparison target is the fixture literal `raw`, not the test's own output. The downstream assertions (:313-317 promises/talentMarket equality; p14b3 :118-121 promise/market bytes and version 1 pins) are untouched. Purpose sentences kept in the titles.
- `tests/bridge-p14b2-checkpoint.test.ts:35-49`: per slot `after[slot] === exportSave(migrateToV30(importSave(before[slot])))` with the governed bytes pinned to `{...before[slot], saveVersion: 30}` (:45-46); digests now pinned as sha256 of the hydrated bytes (:48-49), which is the runtime's own recomputation law (`runtime-checkpoint.ts:847-850`); a test-local sha against the runtime's stored digest is an independent check, not self-equality. Source-slot pins :19-20 stay 29; session/revision/journal/tick pins :31-34,:50-53 kept. Pattern = runtime47 :185-205 as 600-B Q7(b) required.
- Generator pins `tests/bridge-contract-generator.test.ts:554-566`: fixed literal `sha256:6f6b4880…` asserted against both the generated header and `schemaIdentity(schema)`; it equals the checked-in `generated/unity/project-studio-bridge.contract-manifest.json:8` and `StudioBridgeDtos.Generated.cs:3` (read independently). `:670-671` F10/F11 = fixed literal `53058c23…`, computed once (comment :668-669 names the source); the frozen aggregate `tests/fixtures/bridge-contract-union-fixtures.ts:260-267` does not reference `BRIDGE_SCHEMA` (F10/F11 :226-233 do) and 606 (`check:bridge-contract:fixtures`) is clean. NOT VERIFIED by me: that `53058c23…` is the generator's actual output (I cannot run it); the 608 failure text prefix cited by 600-T2 and the 610 pass (bridge-contract-generator 31/31, :61) are the evidence. REFINE (comment only): `:663` says "APPEARANCE_COUNT count-only"; the count-only member enumerates four families (`bridge-schema.ts:1770-1772`).
- `tests/bridge-p14b3-promise-command.test.ts:150-181`: purpose ("each material field participates in the opaque intent identity, including refused variants; no partial effect") kept; the ENGINE-refused variant is now the still-enumerated, still-engine-refused P3 `DIRECTING_COUNT` (:165, refusal asserted :177-179 against `promises.ts:214`), two lawful tagged variants added (:168-169), 7 distinct identities (:176), session truth unchanged (:180). Stronger than before (5 variants, no explicit refusal assertion). No `@ts-expect-error`.

## Q4 — The G9 class: MET WITH EVIDENCE; faithful, stronger, no loosening; F-G9-1 recorded honestly and not patched

`tests/p14b1-trust-chooser.test.ts:540-788`: the spy records ORDERED reads per submission with the public archetype at the read (:603); the sequence law is asserted per read (:615-622: proven → exactly [P1]; unproven → [FLEX, P1] and the P1 read only after a non-achievable FLEX :622); same state object for a second read (:611 `Object.is`); exact draft tuple per read (:562-572, :621); `assertOriginalAuthoring` :650-697 requires the FIRST achievable read to be attached with no read after it (:675), pins family/predicate/receipt to that read (:683-694), and for the negative branch requires the full sequence read with zero attachments (:665-672). Test 7a title/purpose re-expressed (:699); test 7b (:721-788) keeps the first-writer IFF and the actor's UNCONDITIONAL positive (:743), splits it by archetype (:746-752), and ADDS the unproven FLEX witness with the D3 reason (:758-770) and the unproven "neither" witness (:775-779) beside the proven negative. This is plan :199-207 and S2 as committed, read from receipts. Nothing dropped, nothing conditional; the missing P1-fallback branch is named as a fixture finding in the comment (:714-720), not synthesized.

Test 6 GREEN and no pinned winner moved, confirmed from 610: `p14b1-trust-chooser` 13 tests / 2 skipped, no failure (:35); `p14b2-fixture-preconditions` 5/5 (:2195) with the poaching stdout showing rival FLEX roots promise-42/43 for unproven r04-3 and the PLAYER settlement at 208 as `talent-market-event-145`, contract `…:208:player-47` (:1988-2088) = record 556; `bridge-p14b2-trust` 22/22 (:434); `rivalFixture` consumers GREEN. F-G9-1 (600-T2 §C): the default seed has no unproven read pair with FLEX non-achievable and P1 achievable within 220 weeks; seeds 'seed-b'/'seed-c' carry one (probe log, NOT VERIFIED by me). The test-author did not require the branch and did not fabricate a witness: honest.

## Q5 — Full-core accounting: every 610 failure classified; no UNEXPLAINED

610 (`fixedSource: true`, sourceSha e9f4962b, empty tested diff `e3b0c442…`, 610 .json :2-20): 13 files / 39 failed / 3755 passed / 7 todo (:2531-2532).

| Class | Failures (610 line) | Match to 89b5ad2 (`p14bf2-20260919-final/11-test-core-89b5ad2.txt`) |
|---|---|---|
| Inherited 22 | `bridge-p12-campaign-library` 11 timeouts 5000/20000 ms (:2212-2229); `bridge-p13-campaign-isolation` 60000 ms timeout (:2232-2234); `p13a-scientist-foundation` 3 digest pins (:2276-2304); `r3n1-stale-schedule-take-02*` 6 ENOENT (:2436-2510); `world-first-scenery-load-in-provenance` PIL (:2514-2519) | identical files and messages at 89b5ad2 :1996-2016, :2020-2048 (the scientist hashes are byte-identical: expected `16857a26…`/received `4a4b2e78…` etc.), :2053-2131. Campaign-isolation's stop line moved in 608 to the class-A3 `cannot downgrade SaveFileV30` refusal at `:26` (608 :32793-32803) and is back at the inherited 60 s timeout in 610, as 600-T2 §F(b) predicted |
| Designated 2 | stale route :234 `workLimit` vs `commandRefused` (:2420-2429); evaluator-5 case :239 `FRAGILE` vs `IMPOSSIBLE` (:2309-2318) | 515 §6 (c); 600 §3 2(b) / D2 (i-c) |
| Natural premise 11 | outcomes `rivalWorlds` ×9 at :233 "prerequisites absent by350" (:2325-2381); outcomes :438 second beneficiary not in antagonist (:2385-2390); policy :529 missing `P1fallback` (:2397-2414, all in-loop assertions passed) | 600-A §4.1 G9/G10, 26 §2 :86-87 |
| Load-only 4 | `bridge-supervisor` ×3 "Fake Unity did not report health/helper" (:2237-2272); `bridge-runtime-checkpoint-prepared-reuse` 20000 ms (:2227-2229) | both files 35/35 alone in 611 (:38-39, same sourceSha). Not in the 89b5ad2 list, so "load-sensitive under a 47-minute full run", not inherited |

22 + 2 + 11 + 4 = 39. The seventh todo is the evaluator-5 file's `it.todo` (600-T). 608 (pre-sweep, 45d62b30) 96 files / 312 failed = the sweep surface plus these; the fourteen `cannot downgrade SaveFileV30 or discard a tagged promise predicate` arrows in its summary (:975-:2253) show natural chains carrying tagged roots by the time load-to-live sites run.

## Q6 — The three natural premises: investigations correct against the test bodies; the seed step is lawful as a recorded test-author reconciliation

- `rivalWorlds` (`tests/p14b4-cast-class-outcomes.test.ts:204-234`): requires a bound OPEN root in each of lead/antagonist/support on one rival film at `remainingTicks 5` (:210-220) AND a `genuine` tagged root seated in lead or (antagonist with `leadOrAntagonist`) (:221-224). 600-T2 D.1's finding (slots satisfied at 212 on r01 film 6 with P1 roots; the only bound tagged root, promise-11, is a craft worker never cast) is consistent with the body; NOT VERIFIED beyond the probe log.
- `:438` (`:144-152, :433-438`): `playerPayload` fills the non-promised seats from `state.contracts` in order after excluding the beneficiary (:146-152), so antagonist = the first available actor; the assertion at :436-438 wants that seat's actor to be a second bound OPEN beneficiary. With the fixture order t-act-12, t-act-13, t-act-09, t-act-08 (writer §S3, 600-T2 D.2; fixture bytes NOT readable here) the premise cannot hold; the cause is fixture construction, not the engine.
- `policy :529` (`:413-415, :521, :529`): `P1fallback` needs an unproven read pair with FLEX non-achievable and P1 achievable; under evaluator 4 the two reads differ only through the mask on `seatedPreFirstTake` (`promises.ts:354,:424`), so the issuer must run a pre-first-take picture seating the person in SUPPORT with no unproduced script and no stock door (rivals have none, :258). 600-T2 D.3's derivation is correct; the default seed's only wave (196) has no rival in that shape.

Ruling on the seed step: choosing `p13aGeneratedStudio('seed-b')` (or scanning several seeds) is a fixture choice inside the tests' existing parameterisation, not a magic week; under plan T2 :396-400 and 600-B C10 it is a test-author reconciliation, provided (i) it is its own recorded pass with the seed-scan evidence archived (the probe logs currently live only in the scratchpad), (ii) no default-seed assertion is dropped and the witness set stays required, (iii) the rivalWorlds SLOTS prerequisite on 'seed-b'/'seed-d' is verified by the same search first (600-T2 §G says it was not), and (iv) the `:438` fix stays a cast choice (prefer the issuer's other bound OPEN beneficiary, or cast t-act-08 explicitly), never an invented binding. No Owner ruling is needed; it is not "closure" of the rival outcomes family (26 §2 :86-87 partial-evidence boundary) until those nine cases run GREEN on a recorded seed.

## Q7 — Owner-visible launch-behaviour changes at rules 4 / Save30 / projection 47

Plan law already accepted by the Owner (record 600 §2 / plan :18-40, :98-139, :237-276):
1. Fresh P1 and tagged P2 are classified with the residual buffer: a person who already holds overlapping reservations can now read FRAGILE "the schedule leaves no spare picture inside the window" where rules 3 read ACHIEVABLE (`promises.ts:432-434`; plan :169-173 "some new P1 offers may become nonofferable"). Paper thresholds: ≥ 2 competing reservations at a 28-week window, ≥ 3 at 40, ≥ 4 at 52, ≥ 19 at 208. No installed control moved (p14bf2 13/13, p14b3 6/6, p14b1-promises, p14b3-reservations GREEN in 610).
2. Tagged P2 counts only mask-qualified fixed seats as existing paths/events (`:272-282, :354, :424`); a support seat on a running picture no longer helps a lead/flex P2.
3. A classless P2 is refused at a new quote/freeze with the missing-class sentence (`:393-395`); on the wire it cannot be sent at all (projection-47 grammar → INVALID_COMMAND). A reader-admitted legacy classless P2 still reclassifies IMPOSSIBLE at freeze and on cancel (same classification, new sentence).
4. Rules 4 is stamped on every fresh receipt/root; historical roots/receipts untouched (:45, :527).
5. Save V30 is the only writer; every prior save migrates on load; a save holding any tagged root can no longer be downgraded to V29 or older ("cannot downgrade SaveFileV30 or discard a tagged promise predicate", `save.ts:8704-8710`). Because rivals author tagged P2 in every natural chain by about week 196 (610 :1988; 608 arrows), an older build will refuse most mid-game saves from this one.
6. Wire: projection 47; the proposal draft's `promise` is a closed union (P2 REQUIRES `seatClass`, no default; count-only families take no class); own promise snapshot and history rows carry `seatClass` (real class or `null`); preferences carry `preferredOpportunity`; competing rows stay whole `UNKNOWN`; Pulse untouched. Runtime: outgoing 46 identity registered as `projection-v46`; prior checkpoints migrate each slot to V30 bytes (digests recomputed).

The plan's own hypothesis "subject to expansion review" (plan :185-207, delegated content/strategy, NOT an Owner-selected rule):
7. Rival authoring order: for a publicly UNPROVEN person (no identity discipline and age < 30, `talentMarket.ts:685-688`) a rival now authors a flexible `leadOrAntagonist` P2 (count 1, full term) when achievable, P1 only as fallback; proven persons keep P1 (`:1275-1296`). Observed consequences (600-T2 §C, 610 stdout): tagged rival roots to unproven persons in every natural chain (default chain promise-10/11/28/29/32/33, one BOUND at 208; poaching seed promise-42/43); rivals earn D3 opportunity against a player P1 for unproven persons; rival material digests change. Pinned winners did not move (poaching event 145 PLAYER; rivalFixture promise-16 @213; trust-chooser test 6). The pre-cutover values of the UNPINNED settlement reasons for unproven persons at 208 are not recoverable from any record (600-T2 §G evidence limit). The P1-fallback branch has no natural witness on the default seed.

## Q8 — Unity/native backlog created by this candidate (nothing native touched)

Confirmed from `generated/unity/StudioBridgeDtos.Generated.cs` and the manifest; the generator ran without `--unity-project` (writer §S5; 605/606 clean):
1. `StudioMarketProposalPromiseDraftPayload` is now `abstract partial class` (:6667) with sealed members `StudioMarketProposalCastClassPromiseDraftPayload` (:6570, `family` discriminator, `seatClass` `Required.Always` :6587-6588) and `StudioMarketProposalCountPromiseDraftPayload` (:6607), plus `StudioMarketProposalPromiseDraftPayloadJsonConverter` (:9887) registered at :283. Any native composer that instantiated the previously sealed DTO must instantiate a member.
2. Native class selector for P2 (lead / lead-or-antagonist; no default) in the proposal composer.
3. Nullable `seatClass` on own promise snapshot and history rows: render the real class or an honest legacy/unknown, never a fabricated lead.
4. `preferredOpportunity` on the preferences snapshot (public archetype display).
5. Projection-47 schema identity `sha256:6f6b4880…` and Save V30 loading; prior 46 checkpoints migrate through the registry.
6. Consumer-repo sync of `StudioBridgeDtos.Generated.cs` (manifest `unityGeneratedContractSha256` = `5df92d48…` is the expectation, not evidence the Unity repo was updated).
No Unity source/player was built or run; no native input; consistent with the ruling's "Unity/native work remains deferred".

## Q9 — Record-only items to carry; REFINE for a later writer

Carried (still record-only, none reopened): 599 (Ruling 2's six textual under-reservations; C5(ii) closed as 579 left it; four-commit shape for any future C1 slice; `599-A-calc-skeleton.tsv` naming); 598-R items 1–10 (`:1991` second `sorted`; annex-lot class; five `sortedOutput` callers; measured rows historical; Ready/Started close costs; Post exit; S6 class observation); 574-R items 1–9 (owners header :8-9 stale, still deferred; saturation label; insertion range); 553-R items 1–3 (digest key order; rival ALREADY_MET mapping gated before the adapter; adapter tolerance of mismatched issuer). New from this candidate: 602 R6 items (evaluator-4 parity artifacts: a non-mask running seat is neither event nor hold; `unproducedScripts` counts `inProduction`; `breakPromisesOnCancel` coupling → 600 §3 step 8; seating preference → own RED); C12 reading (verified above); the C2 deviation (Q1); the A5 sentinel correction and the `p14b1-t4-regressions` classification correction to 600-A §4.3 (Q2); F-G9-1, D.1–D.3 with the seed-scan logs to be archived (Q6); `promises.ts:90` refusal text still says "migrate this state to V29" (a frozen string; the V29 roots are still the roots, so it is imprecise, not wrong); 600-T2 99-vs-100 file count.

REFINE (no hunk before the checkpoint): the stale titles/comments listed in Q2; the generator-test comment `:663`; the `p14bf2`/`p14b3` describe titles that still say Save29/projection46; a doc-only refresh of `promiseCapacityOwners.ts:8-9` (574-R item 1) at the next writer release.

## Q10 — Verdict for the parent

**QUALIFIED WITH RECORD-ONLY ITEMS.** Not Owner acceptance; not all-green (39 classified failures remain); not closure of the rival outcomes family (nine natural cases still unexecuted) nor of evaluator 5, seating preference or `breakPromisesOnCancel`.

Identity to cite: HEAD `f2192d8b` on `wip/headless-program-20260916-ts`, clean tree = base `0af0dabe` + writer commits `47b2bbf4` (S1) → `3b8f5b6a` (S3) → `2cbc3de5` (S4) → `a64ac00b` (S2) → `027155e7` (S5; = cumulative patch `600-W-cum-S5.patch` sha256 `fee048b5…`, the `testedDiffSha256` of 603–607 on `0af0dabe`) + evidence commits + `e9f4962b` (600-T2, tests only) + `6f9c8006` (evidence) + `f2192d8b` (600-T3; = diff `f928f68a…` on `6f9c8006`, the identity 615 carries). Seven commits in all (five writer + two test-author); the brief's "six" omits 600-T3. Record-check identities: 608/609 on `45d62b30`; 610–614 on `e9f4962b` (empty diff `e3b0c442…`, `fixedSource: true`); 615 on `6f9c8006` + `f928f68a…`. Contract identity: `PROMISE_RULES_VERSION 4`, `LIVE_SAVE_VERSION 30`, `PROJECTION_VERSION 47`, schemaId `sha256:6f6b48805aadcf14d456614d87bf1571eb1ce0d9aa0bc44f604e7976f4f85538`, generator source `17da544b…`, generated C# `5df92d48…`, F10/F11 `53058c23…`, prior registry 35 entries incl. `584bdd…` → `projection-v46`. Replay module `promiseCapacityOwnerReplay.ts` unchanged (`9e97ffed…` per record 600): NOT VERIFIED by me.

---

## Rulings for the parent (each one checkable)

- R-1. Record the checkpoint as QUALIFIED WITH RECORD-ONLY ITEMS with the identity above; no source or test change is required before recording.
- R-2. State the C12 reading in the record: `reserved + X > nMax` → IMPOSSIBLE (`promises.ts:422`); `reserved + X > nMax − promiseBuffer(nMax)` → FRAGILE "the schedule leaves no spare picture inside the window" (`:432-434`).
- R-3. Record the C2 deviation: the S5 bridge typecheck (604) carried a third test-side diagnostic `tests/bridge-p14b3-promise-command.test.ts:155`, inherent to the closed wire union, reconciled by 600-T2 §B(iii); 613 exit 0. Not a defect.
- R-4. Record the G9 class by name with: no pinned winner moved; F-G9-1; D.1–D.3; and the unpinned unproven-person settlements as an Owner-visible change whose pre-cutover values are unrecoverable.
- R-5. Put the Q7 list before the Owner with items 1–6 marked "plan law (record 600)" and item 7 marked "delegated hypothesis, subject to expansion review (plan :187)".
- R-6. Authorize, as a separate recorded test-author pass (plan T2), the seed-scan reconciliation of `p14b4-cast-class-policy:529`, `p14b4-cast-class-outcomes` rivalWorlds and `:438` under the four conditions in Q6; archive the 600-T2 probe logs as evidence first; the six live-P2 files change only in that pass.
- R-7. Record the Q8 Unity backlog and that nothing native was touched or built.
- R-8. Carry the Q9 record-only list; schedule the stale-title text pass and the 574-R doc fix at the next test-author/writer release, not before this checkpoint.
- R-9. Cite 610 as 39 failed = 22 inherited + 2 designated + 11 natural + 4 load-only (611), never as all-green.

## Sources read (line ranges)

Brief: `scratchpad/600-R-brief.md` whole. Records: `600-owner-ruling-578-reopened-program.md` whole; `602-…-writer-release.md` whole; `600-A-design-note.md` 1-765; `600-B-review.md` whole; `600-W-report.md` whole; `600-T-report.md` whole; `600-T2-report.md` whole; `600-T3-report.md` whole; `26-…` whole; `23-…` whole; `17-…` whole; plan `P14B4-HEADLESS-PLAN.md` 1-280, 380-416; `599-…` 93-106, greps; `598-…` 47-62; `574-…`/`553-…` greps of record-only sections. Evidence: `603`/`604` .txt (diagnostic lines + identity); `607` .json; `608` .txt greps + 44785-44798; `610` .txt 2205-2537 + greps of all `✓`/`×`/FAIL lines, .json; `611` .txt greps; `612`/`613` .txt whole; `614` .txt 44-65 + summary; `615` .json + summary; `p14bf2-20260919-final/11-test-core-89b5ad2.txt` 2018-2054 + FAIL/summary greps. Source: `src/core/promises.ts` 28-97, 185-660, 715-744, 940-989 + greps; `src/core/types.ts` 2200-2244; `src/core/talentMarket.ts` 455-466, 536-553, 682-746, 1250-1304 + greps; `src/core/save.ts` 494-505, 6390-6407, 8600-8724 + greps; `src/core/index.ts` 1288-1297; `src/harness/p14/legacy-v28-fixtures.ts` 10-51; `bridge/session.ts` 130-141 + grep; `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts`, `src/harness/d16/*` greps; `bridge/runtime-checkpoint.ts` 56-69, 452-465, 990-1009 + grep; `bridge/schema/bridge-schema.ts` 205-236, 1760-1807, 2246-2275, 2316-2331 + grep; `bridge/promises.ts` 18-62, 92-149; `bridge/contract.ts` 440-484 + grep; `bridge/people.ts` grep; `generated/unity/project-studio-bridge.contract-manifest.json` whole; `generated/unity/StudioBridgeDtos.Generated.cs` 6565-6594 + greps; kernel/replay/enumerator greps. Tests: `p14b1-trust-chooser.test.ts` 540-789 + it-list; `p14b4-cast-class-policy.test.ts` 396-531; `p14b4-cast-class-outcomes.test.ts` 130-244, 424-453; `p14bf2-acting-discipline.test.ts` 262-317; `p14b3-rule-revision.test.ts` 56-121; `bridge-p14b2-checkpoint.test.ts` whole; `bridge-p14b3-promise-command.test.ts` 140-189; `bridge-contract-generator.test.ts` 540-579, 640-699; `tests/fixtures/bridge-contract-union-fixtures.ts` 222-269; `_historicalCurrent.ts` whole; `helpers/p14b2-fixtures.ts` 1-12 + grep; `bridge-runtime-checkpoint.test.ts` 228-235, 424-433, 976-995; `save.test.ts` 224-235; `d17a-adv-migration.test.ts` 270-276; `contracts/cross-owner-refusal` 66-91; `contracts/determinism-floor` 82-85; `contracts/phase-table-agreement` 100-106; `contracts/v14-boundary-guards` 318-325; `p14b1-t4-regressions.test.ts` 8-27, 70-89, 160-169, 298-309; `bridge-p14b1-promises.test.ts` 204-211, 292-323, 492-513; `p14b1-save-v29.test.ts` 100-202; `p14b1-first-take.test.ts` 98-109, 280-291; `bridge-operations-events.test.ts` 321-336; greps across `tests/` and `ui/` for every 29/46 pin class.

## Evidence limits

- Nothing executed and no git: per-commit placement (C3/C4/C5/C6 in their commits, commit messages, one push, cumulative-patch byte identity, tree equality of 615's tested diff with f2192d8b) and the unchanged replay-module SHA rest on the parent's and writer's statements.
- Hashes are compared as literals I read (manifest, Generated.cs header, test pins); I cannot recompute the F10/F11 declaration hash, the schema identity or any file SHA.
- Fixture facts (contract order in `genuine-v29-bound-open-p1`, promise-11 and seed-b/seed-c/seed-d witnesses, r03's shape at week 196) come from the 600-T2 probe logs and the 610 stdout, not from the gzipped fixtures.
- Paper `nMax`/buffer thresholds use `WEEKS_TO_FIRST_TAKE 5`, `SEAT_CYCLE_WEEKS = TUNING.PRODUCTION_TICKS` (8 per 600-A; `tuning.ts` not re-read), `promiseBuffer` :66-68.
- No native/Unity, usability or Owner-acceptance claim; a green suite is not usability and this record is not acceptance.