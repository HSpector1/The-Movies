# 503 — Command-only discovery projection

2026-09-20. After501/502 publication80aec8b348f7322da552ef3cd35ae9ba671ba488.
Sim-core READ-ONLY proposal and parent actual-owner inspection agree on this
bounded next implementation. Not a route-fit proof or whole billing audit.

## Scope

executeCommand currently calls full sweep dimensions, including P/Set/setup and
technology copy/string discovery and genre/allSilent facts. None of the actual
command owners copies these records or uses those omitted string domains.
Add one private commandDimensions returning the ten consumed fields:
n,d,dp,operationsCopy,workflowCopy,taskCopy,structures,provides,placements,cells.
Use Pick<Dimensions,...>, not casts or zero-filled full Dimensions.

Retain existing staticDimensionFacts and current exact-reference fact table.
Read all current production IDs/director IDs, current operations copy footprint,
all current workflow/task copy/string facts, binding facts and reservation
strings. Keep both binding modes initially: later full-frame discovery still
needs them and splitting could merely force another scan. New projection is
fresh, not cached; only complete intrinsic facts enter the existing table.
No owner-return record is replaced, stripped, synthesized or mutated.

Change only executeCommand to use this projection. Narrow parameter types of
workflowUpdate, geometryBill and arrivalBill to their consumed structural fields;
keep executable bill bodies and all tariffs unchanged. Full frame/admission
Dimensions remains unchanged. Same counter,200000cap, spans and validators.

## Consumed owner proof

- Common commandBill consumes n,d,dp and operations/workflow/task copy maxima.
  Actual operations owners preserve arbitrary own string fields when spreading
  these real records; discovery still covers their complete copy footprints.
- assignShootingDirector reads production.id/directorId and current task/phase;
  it copies task/workflow/root. arriveDueScenery retains full current workflow
  guards, actual restricted callback, geometry and possible replacements.
- clearSceneryLoadIn has actual manual-clear classification before owner, then
  copies task/workflow/root. Geometry uses bindings.stageFacilityId, reservation
  facilityId/capability and source static property/placement facts, not Sets or
  workflow.setup. Parent inspected sceneryLoadInFor/facilityBodyCentre.
- scheduleShootingTake reads current workflow/task and copies task/workflow/root.
- commitRelease retains original concept title/ID scan, release-owner refusal,
  commitment count/sort/mint/error bounds and common commandBill calculation.
  No new shortcut or refund. Current-branch commitments remain authoritative.
- Preserve missing-production refusal BEFORE discovery, provenance placement,
  exact owners/order, error strings, catch and sink reconciliation.

Every new literal, call/setup, loop visit, reference/field access, maximum and
fact-table request needs named source prepayment BEFORE execution. No discount
for work that still runs. Existing native-map/key tariffs do not change.

## Evidence and verification

500 measured command discovery4542/3176.482 attribution gives2221 gross units
for omitted technology-root/P/Set/technology-row fact calls across those two
commands. This is NOT net saving: cold discovery can move to the next frame and
table membership alters later lookup costs. Do not claim either full7718 or2221
whole-route saving without measurement. All source facts/refusals remain real.

Test-author independently found substantial current command controls:209 actual
assign/arrival/schedule,222 genuine migrated manual clear,200 commit/release and
stale refusal,276 Ready first-take/kernel GREEN,302 original Ready stale RED.
Arbitrary new workflow/task/setup extension roots are outside176's generic-P
extension seam; no invented fixture campaign. Add only a genuinely new same-week
duplicate-commit branch to the existing actual Release-Ready fixture, if its
baseline passes before production edits.504 records independent test handback.

Then one sim-core production writer in replay module ONLY, exact handback;
independent contract-auditor reads actual source delta and complete payments.
Parent freezes source, runs targeted first-take/stale/command controls serially,
then proportionate full focused matrix/types with fixed-source evidence.
No weakened tests, deadline, work cap or expected failures. If stale remains
RED, report exact reached boundary and preserve it, continuing evidence-led work.
Unity/code-generation/save/protocol changes remain outside this source slice.

## Bounded review and independent baseline

Contract-auditor independently reviewed the complete original direction
bf01822ffaf16d470a50c0116b29fd7eb2f9e9b2043ff66d2e7c558afa851ec9
against immutable80aec8b, actual owners and137/176/365: qualified KEEP.
No omitted command copy/string domain found. Fresh ten-field literal, closures,
loops/maxima/calls/table access still require actual-delta payment review;
later full frames must pay all deferred cold discovery. No fit claim accepted.

504 independent additive control baseline CLOSED/PASS before production writer:
12:52:02.515–12:52:10.672Z,1PASS/4 unselected, fixedSource:true.
Full raw/JSON read; exact base/start/end/patch/current-source/no-untracked checked.
Test-only patch865daf6da13fea7ef8ee6600ceb39820f571f5a4cc2df9d9bfba5eaaff4889e1.
Testdcefec0fa621def52af013cbe11bdda5e94b1671252d6e1b059b8676167b76ee;
briefae60646c9824ecb1f0a0cf7d337cb1dd71529258b3e1919d22026c65d89d51d0.
Parent also reconstructed all original373 test lines exactly by removing only
the two marked insertion blocks. No original test/validator/cap changed.
ONE505 sim-core writer now authorized for the scoped replay-module candidate.
