# 124 — independent production-admission test handback

2026-09-20. INERT / UNEXECUTED. Native test-author; parent owns installation, actual
RED, all runtime and subsequent source release. Only this brief and the sibling
`124-production-admission.test.ts.draft` were written. No live tests, production,
fixtures or config were changed; no runtime/probe/typecheck/Git/network/delegation.

Authority: parent-adopted118 plus the COMPLETE121 precisions;120's minimal first
tranche. Parent-reported source is `5cae2da841d6d50d2c460dc87aba3b603435d00e`.
Parent123's113 existing baseline passes are not execution of this new test file.
No new module implementation was inspected or used to derive expectations.

## Frozen draft

- Intended installation: `tests/p14b4-production-admission.test.ts`.
- Draft:572 lines;32 intended cases (21 ordinary, six missing-profile cases,
  five writer/other-role collision cases). Default Vitest timeouts unchanged.
- SHA256: `a3dc1655c8923be8eebd71c41e622dee3365aa4735e9dbe324c255a058a7a883`.
- Imports the actual future118 entry/types from `productionAdmission.js` and
  calls all six staged exports. Missing-module collection failure is possible;
  it must not be reported as reached behavioral assertions. Typing and every
  new fixture conjunction remain unexecuted until parent checks them.

## Coverage and independent oracles

1. Genuine Ready/second-draft credit remains outside engaged seats and fees;
   actual greenlight links the screenplay. A separate actual `releaseTalent`
   from idle `readyOnly` preserves the screenplay and real termination history:
   subsequent greenlight passes but new commission refuses. No removed-contract
   synthetic campaign replaces this route. Actual engaged director still refuses.
2. Full Talent and narrow id/skills/marker generic results retain their exact
   fields, readonly craft typing and explicit director/cast/craft engaged order.
   Secondary directing/acting/writing/craft profiles pass without changing primary
   labels. Unknown IDs, separately missing profiles, cast duplicates and writer/
   director/craft/cast collisions pin the THREE distinct121 orders. Literal
   expected role diagnostics are not computed by the new helper.
3. Real current contracted/non-market/freelance facts feed labour checks. An
   actual current freelance actor produces one exact fee row and unchanged
   canonical fee/cash effects; outsider refusal commits nothing. An ordered
   narrow list of real market people proves generic return/order separately
   without claiming it is a staffed picture. Engaged one-craft law remains
   distinct from real legacy unengaged no-craft admission.
4. Actual assessed Ready/no audition passes; actual Draft/Review, unknown project
   and writer/concept/shape/promise mismatches refuse. Real audition actions/tick
   reach review, refusal, acknowledgement, then actual admission. Legacy script
   development plus managed operations remains legal. Header-before-people,
   founding-before-concept, unknown concept and mismatched genre remain explicit.
5. Commission callbacks transparently call existing contract/busy owners and
   record contract-before-busy, no early read on refusal, real drafting refusal,
   full/narrow return types and caller-specific supplied busy facts. Deep-frozen
   helper input checks include equality after success/refusal; actual action
   controls retain caller-state, cash/ledger, screenplay, production and RNG
   assertions. Existing `freelancerFee` is the canonical money owner, not the new
   admission helper or a test-invented fee formula.

## Provenance and limits

`buildScenario(SEED)` and `foundedStudio` are the existing disclosed historical-
control founding route, with real hires/found/commission/tick/accept actions.
The new release, role-swap action, actual-market selection and casting conjunctions
have hard guards but HAVE NOT RUN. `generateWorld('gl-xdisc-cast')` supplies the
separate real legacy/no-craft route. `freeSlate` selects real idle contracted
primary actors; no audition observations are fabricated.

Missing skill profiles, unassessed Ready header and synthetic founding guard flag
are explicitly INVALID/detached pure probes, never saved or labelled historical
engine states. Distinct narrow/broad supplied sets are a fact-level discriminator,
not fabricated research or rival employment evidence. No fake contract, take,
production, credit, receipt, cash or past workflow was minted.

Half-open future employment/expiry PLUS actual market membership, research/industry
conjunctions, pooled-writer cross-package admission, queue revalidation after
eligibility changes, exhaustive uniqueness pairs, forecast-call instrumentation,
natural performance and full trace completeness remain separate coverage. This
draft does not promise malformed deep/getter/fee read-timing equivalence:121 adopts
precollection on well-formed state only. Existing123 and planned regressions cover
the unchanged queue/pooling/action neighbors; they do not replace new owner proof.

Static review only: current source types/exports and fixtures checked, no `any`,
partial-as-Talent/Production, skip/todo, mock eligibility or timeout override added.
Next: parent installs frozen bytes, records actual reach/failures and identities,
then releases the sole implementation writer under118/121/125. Draft is frozen.
