# 222 — genuine grandfather candidate; modern-negative fixture gaps

2026-09-20. Independent INERT preparation, not an218 prerequisite. One candidate
case in `222-started-replay-scenery-commands.test-draft.ts`,211 lines/default
timeout; intended `tests/p14b4-started-replay-scenery-commands.test.ts`.
SHA256 `fb28c1464f617ae730153d5c4ce9c9104eede03f7975b3b01d7fd31aa2d1949f`.

No engine execution, runtime probe, typecheck, protected edit, replay/218
implementation inspection, Git/network or delegation. Only222 draft/brief are
written. Static file reading, SHA/byte-count verification and existing-source
inspection do not constitute validator or test execution.

## Preserved incoming evidence actually found

`ui/e2e/world-first-scenery-load-in-v1/week-30-nights-of-watchtower-stage-7-blocked.save.json`
exists,230339 bytes, independently rehashed SHA256
`34f062fdd44e3a7116731d214de5aaa2eb4d02af85b874764d5de1cf66229b22`.
Its existing manifest is independently rehashed
`e1bfbf86d329f75e96452185e79e36554a512cdb74b9e987eef93afcb807de5d`.
The manifest records the public-action/adapter recipe, native Save13 producer,
no hand-editing, actual week30/production prod-0026/locked director t-dir-01 and
blocked scenery task. These literal file/hash/size/identity pins are copied into
the independent test; expected hashes are not derived from metadata at runtime.

The candidate first uses STRICT `validateSaveV13` on the unchanged bytes, then
the actual `migrateToV14` → `migrateToV29` chain. It explicitly verifies original
V13 has no bindings and that ONLY the real V14 migrator introduces
`requiresSetBinding:false`/null Set for this already-in-flight picture. Original
task/reservations/clock/identity are preserved and actual later migration origin
is30, not a newly invented fresh origin. No `_historicalCurrent` synthetic lift,
version restamp, manual provenance toggle or geometry rewrite is used.

Strict CURRENT saves and all137 root/foreign-relevance guards are mandatory.
**Neither the frozen incoming reader nor current migration/root conjunction has
been executed in222.** The artifact's existence/provenance/hash are established;
its complete current replay eligibility remains an explicit hard runtime guard.
If any reader or guard refuses, retain that genuine failure without bypass.

## Positive command test

Actual public schedule first refuses on the blocked task, then actual Clear
makes Ready, actual schedule and tick perform5→4. The replay must call the real
clear/schedule/sweep owners once in order, within unchanged200000, preserve input
and fixture bytes, reproduce exact local projection/sceneryArrived owner event
and next-week firstTake, retain original resource holds, and never invent an
exclusive Set hold for a migrated null binding. Default timeouts and all strict
validation remain. No private bill oracle or mocked owner result appears.
The first take equals H, so the test claims command parity, not capacity credit
or ACHIEVABLE/completeness. No actor/employment/history is reminted by the test.

## Modern negative controls NOT honestly constructed here

- `tests/p05a-w1-scenery-truth.test.ts::inTransit/arrivedPending` changes the
  authored property origin using `moveStage`; its grandfather controls directly
  replace bindings. These are disclosed controlled owner tests, not untouched
  current action-state fixtures that222 can relabel as genuinely produced.
- The inspected preserved modern
  `ui/e2e/p05-visual-oracle-v1/s3-scenery-load-in.save.json` manifest explicitly
  documents normalization to unmintable charter identities and a transit route
  involving concurrent Set work. That artifact is not adopted as a genuine
  producer snapshot for this scope, and its Set-work context cannot simply be
  deleted to evade137 cuts.
- The genuine current fixture already used in209 is unassigned/`none`, with
  automatic due-at-call settlement, not an inTransit/arrivedPending witness.
  Public call/tick settlement is atomic; pausing/rebuilding its intermediate
  operations root would require a separately qualified owner-cut fixture, not
  an unannounced claim of a saved current campaign.

Accordingly this draft contains ONE positive grandfather candidate and NO
pretend modern-negative coverage, skipped test, todo or forced fixture. A future
modern transit case needs a real completed distant-facility route with no active
cut roots, or a suitable genuinely emitted incoming snapshot. A genuine
arrived-pending incoming snapshot/authorized real-owner-cut seam is still needed.
No campaign-scale construction or new authority is pursued here.

Next: parent reviews/freezes/installs the one candidate after218 handback if useful,
then records actual reader/fixture/replay reach. The named gaps remain visible
and do not delay independent218 verification.
