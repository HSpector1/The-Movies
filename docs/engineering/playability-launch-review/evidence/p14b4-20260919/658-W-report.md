# 658-W — P14B.5 T2 writer report (sim-core, IMPLEMENTATION; HEAD c32961c6; no commits, no test edits)

STATUS: PARTIAL — S1–S5 production patches are complete, self-checked and verified to apply byte-for-byte; the 657-B expected control counts are NOT met because of (a) three RED-side assertions that cannot be satisfied lawfully (test-author amendments needed, none decides an OPEN item) and (b) the values-only test-side sweep the plan already assigns to the test-author. No law question is open on the writer side. One writer defect was found and fixed inside S3 (key-order clone in the V31 converts). Git READ-ONLY throughout (`git diff`/`git status`/`git show`/`git log` only); worktree is dirty with the S5 state.

## Cumulative patches (each = `git diff HEAD --binary` + `git diff --no-index --binary /dev/null src/core/relationships.ts`, since `git diff HEAD` cannot carry an untracked file and `git add -N` is forbidden; each verified with `git apply --check` + `git apply` against HEAD copies in a scratch dir — S1 and S5 byte-compared against the worktree, no mismatch)

| step | patch | sha256 | status file |
|---|---|---|---|
| S1 | scratchpad/658-W-cum-S1.patch | 94daf9021aee90709f94564fb5563b279a2a2a1e79324a703534c05fafe8d03c | 658-W-status-S1.txt |
| S2 | scratchpad/658-W-cum-S2.patch | 2cf2f3ce7f1b3044174f385ddb5edd5a25a72ff62a8b54064e066a3441ef90e3 | 658-W-status-S2.txt |
| S3 | scratchpad/658-W-cum-S3.patch | 493ea55bf77590107000f864dd3daf21f5e34615d23044c74f5f2981fafd5ede | 658-W-status-S3.txt |
| S4 | scratchpad/658-W-cum-S4.patch | 5cf4bcf9cc870cc998eb27373c52c7e13b95c5c98da36b4af69535c94231c080 | 658-W-status-S4.txt |
| S5 | scratchpad/658-W-cum-S5.patch | e0b9ae24e8940418c605d413a231c1f5f5a4612146259123ae5a21a7f8df7bcc | 658-W-status-S5.txt |

Scratchpad = /private/tmp/claude-501/-Users-zacheryspector-The-Movies-headless-program/6ed44184-f9ac-40ba-bf1b-321fc7506549/scratchpad. Final tree: 20 modified files + 1 new (422+/144−).

## New schema identity (S5, generator run ONCE)
- `sha256:00c0075bef257634956da7d16d117a145d203047e7169c643156b7971c4c7fec`, `$id urn:project-studio:bridge:protocol-4:projection-48`, projectionVersion 48; outgoing `sha256:6f6b4880…` registered as `projection-v47` (registry now 36 prior ids).
- Artifacts: bridge/schema/project-studio-bridge.schema.json fd22878c…, generated/unity/StudioBridgeDtos.Generated.cs 370568f3…, generated/unity/project-studio-bridge.contract-manifest.json 56f6c997… (full sha256 in 658-W-S5-artifacts-before.txt vs the run output). Generated C# keeps `string[] priorityOrder`.

## Per step — files / hunks (line numbers = post-patch)

S1 core
- NEW src/core/relationships.ts (485 lines): constants :37-88 (RULES_VERSION 1, BASELINE 50, TIERS, TIER_FLOOR {Nemeses 0, Enemies 11, Strained 31, Acquaintances 45, Colleagues 56, Friends 61, CloseFriends 71, Inseparable 81}, DRIVER_KINDS, PROXIMITY HIGH/MID/LOW 6/4/2, REPEAT_CAP 3, SUCCESS_DELTA 5, FAILURE_DELTA 4 (> LOW), CANCEL_DELTA 2, SUCCESS/FAILURE_CRITIC_SCORE 60/40 (mirror the shipped critic bands, not bound to them), GRACE 52, RETURN 260, RECENT_CAP 8); requireRelationshipsRoot :98; currentCloseness :114 (plan formula verbatim); currentTier :137 (Nemeses/Enemies band → Strained); driverGain :147 pass-through; writeEdge/newEdge/seatPairs/ledger :157-247; RelationshipDelta + advanceRelationshipsWeek :249-303 (guard order copied from appendFirstTakes; idempotent by (edgeId, kind, ref) over `recent`; I2 precision `take.week >= edge.firstSharedWeek` in driveTake :233-244); recordCancelledAfterFirstTake :312 (no-take → same reference; loud on a foreign take); tiersOnRoster :329 (the D5/reservation read; roster passed in — no talentMarket import); pairChemistry :357; validateRelationshipsRoot :389 (promises.ts :927-1004 message model, `validateSaveV31:` prefix); projectRelationshipsPreV31 :479.
- src/core/types.ts :2231-2284 (RelationshipTier/DriverKind/Driver/Edge, GameStateV31, GameState = GameStateV31).
- src/core/tick.ts :3 import; :1103-1122 `takeEntries` hoisted, ONE `advanceRelationshipsWeek(withTakes, { takes: takeEntries, releases: [...records.map(r => r.filmResult), ...industry.growth.map(r => r.filmResult)] }, finalized.market.tick)` between appendFirstTakes and advancePromisesWeek.
- src/core/actions.ts :62 import; :597-603 ONE call `recordCancelledAfterFirstTake(breakPromisesOnCancel(...), playerStudioId, cancelled)` (breakPromisesOnCancel untouched).
- src/core/worldgen.ts :786-787 `relationships: []`.
- src/harness/roster-wall/historical-control.ts :23-24, :27, :30-31, :47 (four touch points).

S2 chooser — src/core/talentMarket.ts: :37 import tiersOnRoster; :660-664 comment names B.5; :670 DESCRIPTOR_ORDER 7 (relationships between trust and standing); :678-680 the ONE new reason `their roster holds this person's close ties`; :695-706 both seven-member companion orders; :783-800 `rosterAt` (the D1 predicate verbatim, subject excluded, over hollywood.employment); :809, :838-845 bandsFor D5 band; :1043-1046 FreezeDrop `nemesisOnRoster`; :1068 DROP_SENTENCE `${studio}'s roster holds someone this person will not work beside.`; :1105-1107 check after issuerDistrusted, before subjectCommittedElsewhere.

S3 save — src/core/save.ts: :56 GameStateV31 import, :90 relationships import; :499-517 SaveFileV31 type; :548-549 union; :5295-5297 dispatch +31 and sentinel "1 through 31 only"; :6409-6417 LIVE_SAVE_VERSION 31 / makeSave → validateSaveV31; 18 downgrade throws each +1 `saveVersion === 31` line (:7330 … :8205, :8846); route lines :8370/:8522/:8604/:8703; migrateToV30 :8746-8749 gains the 31 arm; V31 block :8754-8827 (stripV31Root, validateSaveV31 strip-and-delegate, convertV30ToV31 EMPTY root nothing recomputed, convertV31ToV30 asks projectRelationshipsPreV31 BEFORE validating with `migrateToV30: cannot downgrade SaveFileV31 or discard the relationship record — …`, migrateToV31). 25 `saveVersion === 31` sites (= 24 V30 sites + migrateToV31 itself). src/core/index.ts :102-106 type exports, :1297-1306 V31 save exports, :1346 SaveFileV31, :1440-1471 relationships block. src/harness/d16/run-d17b-continuation.ts :37/:183 and run-d17b-week86.ts :35/:124 → migrateToV31. src/harness/p14/legacy-v28-fixtures.ts :25/:40 type-only (typecheck demanded it).

S4 load/runtime — bridge/session.ts :41/:136; bridge/runtime-checkpoint.ts :6-7 import, :447 CurrentEnvelopeSave V31, :462-466 strict V31, :851-854 importPriorSaveViaCanonicalChain → migrateToV31, :1000 annotation; bridge/runtime/campaign-library.ts :5/:48/:127; ui/src/engine/adapter.ts :112/:3796/:3808/:3822.

S5 wire — bridge/schema/bridge-schema.ts :233-240 PROJECTION_VERSION 48 (+comment), :2330-2331 enum + 'relationships'; bridge/people.ts :875-877 DESCRIPTOR_LABEL.relationships `friends on the roster` (:977/:991 untouched); bridge/runtime-checkpoint.ts :60-63 projection-v47 line; three generated artifacts.

## Self-checks actually run (one command at a time; logs in the scratchpad)

- S1 `npm run typecheck` (658-W-S1-typecheck.log): EXIT 2. TS2307 GONE. Remaining: 6 TS2724 (V31 save exports, until S3), 2 src/harness/d16 TS2375 (S3 moves them), and 148 TS2379 + 19 TS2375 + 1 TS2322 in TEST files (`migrateToV30(...).state` / hand-built V30 states typed against the moved GameState — sweep class, test-author) + the RED's TS2322 `FreezeDrop` (S2).
- S2 `npm run typecheck` (658-W-S2-typecheck.log): EXIT 2. FreezeDrop TS2322 GONE; six TS2724 remain; test-side sweep unchanged.
- S3 `npm run typecheck` (run 1 658-W-S3-typecheck-run1.log; run 2 658-W-S3-typecheck.log): EXIT 2 — src/ + ui/ CLEAN (0 errors outside tests/); 161 test-file errors, all sweep class (143 TS2379 / 12 TS2375 / 6 TS2322: `migrateToV30(...).state` as GameState, `SaveFileV31` vs `SaveFileV30` annotations in tests/contracts/*, tests/legacy-parcel-ground :356, tests/save.test.ts :137 hand-built state missing `relationships`, :232). Not EXIT 0 because the root tsconfig includes tests/**.
- S3 RED files run 1 (658-W-S3-red-run1.log): engine 38/46, save-v31 56/57, bridge 6/15. WRITER DEFECT found: family 10 one-edge :984 — `convertV31ToV30`/`convertV30ToV31` used `clonePlainJson` (sorted keys), so the appended first-take receipt's key order differed from the in-memory state. Fixed (insertion-order `JSON.parse(JSON.stringify())`, the convertV28ToV29 device; C3).
- S3 RED files run 2 (658-W-S3-red.log): engine 39/46, save-v31 56/57 (RED only :175 `LIVE_SAVE_VERSION toBe(30)` — sweep), bridge 6/15 (GREEN: checkpoint-loads-current, generator artifacts, four seed ledgers; RED: :286 literal 47/30/35 sweep, :319 six-member enum sweep, :326/:330/:342 family-11 RED-by-value until S5, :365 enum sweep until S5, :396 RED DEFECT below, :441 poachingFixture helper `validateSaveV30(makeSave())` sweep).
- S3 controls (658-W-S3-controls.log): tests/p14b1-promises.test.ts 12/15 and tests/p14b4-cast-class-outcomes.test.ts 0/23 — NOT the 15/15 and 23/23 657-B predicted; every failure is sweep class by stop line: promises :503 `save.validateSaveV30(save.makeSave(...))` ("expected version 30"), :684/:727 via :665 `migrateToV30(legacy V28).state` then live verbs → "relationships: the Save V31 root is missing" (loud by design); outcomes :79 `toBe(30)`, :355 `validateSaveV30(...makeSave...)`, and `validateSaveV31: relationships root missing` where states are built from `migrateToV30(...)` then `makeSave`. 657-B's expectation was mistaken: these files call the outgoing frozen builder/validator, which the plan's own sweep list (tests/helpers/p14b2-fixtures.ts :122/:210/:225 and the toBe(30) class) already assigns to the test-author.
- S4 `npm run typecheck` (658-W-S4-typecheck.log): EXIT 2, identical 161 test-side set, src/ui clean. `npm run typecheck:bridge` (658-W-S4-typecheck-bridge.log): EXIT 2 — ONE production error bridge/people.ts:987 (seven-member order vs the still-closed six enum, the S5 hunk) + 57 bridge-test errors (sweep: bridge-p14b4-cast-class 33, bridge-p11-finance 9, p07a 3, p08a 2, p06 2, p14b2-trust/p13b-s3/p10a/p09a 1 each; bridge-p14b5-relationships :353-356 TS2339 is the RED's own `importSave(...).state.promises` on the SaveFile union — test-author refine). RED files (658-W-S4-red.log): engine 39/46, save-v31 56/57, bridge 5/15 (:295 "must be a current V31 save, received V30" and :341 — strict V31 under the still-current 47 identity, transitional until S5's registry line).
- S5 `npm run typecheck` EXIT 2 (same 161 test-side; src/ui clean; 658-W-S5-typecheck.log); `npm run typecheck:bridge` EXIT 2 (0 production errors; 57 bridge-test sweep; 658-W-S5-typecheck-bridge.log); `npm run check:bridge-contract` EXIT 0 (658-W-S5-check-contract.log); `npm run check:bridge-contract:fixtures` EXIT 0 (658-W-S5-check-fixtures.log); generator 658-W-S5-generate.log EXIT 0.
- S5 RED files (658-W-S5-red.log): engine 39/46, save-v31 56/57, bridge 11/15 (657-B expected 12/15). Bridge RED: :284 `PROJECTION_VERSION toBe(47)` sweep; :294-295 "current schema must not migrate" sweep (47 now takes the prior path); :441 poachingFixture helper sweep; :396 RED DEFECT below. Bridge GREEN now includes the seven-member enum, identity moved off 47, projection-v47 + 36 ids, both checkpoint slots through migrateToV31 with `relationships: []`, seven-member `priorityOrder` with the `line` unchanged, all four seed ledgers with every survivor band === 1 and every digest/rngState CANNOT-MOVE pin unchanged.
- S5 tests/bridge-p14b4-runtime47-compatibility.test.ts (658-W-S5-runtime47.log): 2/6; RED :164 (literal 47 / 35 ids), :194 ×2 (`migrateToV30` expected bytes vs live V31 bytes), :216 (`migrateToV30` current/saved) — all sweep class as 657-B predicted.

## Failing RED titles that are NOT sweep class and NOT writer defects (test-author amendment needed; each traced to the plan)

1. tests/p14b5-relationships.test.ts:351 (f6Base premise, fails all six family-6 cases at :778/:792/:810/:823/:830/:856): `expect(edges(state)).toEqual([])` "the player filmed nothing on this campaign" — on `p13-public-commercial-adoption` advanced to 207, RIVAL takes have minted 24 edges (S25 symmetry; family 2 of the same file asserts a rival take mints six edges; the ledger's own `edges(state)` bound assumes edges on plain campaigns). The premise is wrong by the plan's law, not by the implementation. The D5 behaviour itself is exercised by the bridge file's f6Base (no such premise): the treated world SETTLED for the player with exactly one new sentence, no digit, and `settlementReasons` verbatim — every assertion up to bridge :394 passed (measured; the case then stopped at the :396 leak string). Paper trace of the other five cases against the landed predicate: drifted edge at 208 reads 71 + trunc((50−71)×130/260) = 61 → Friends → `none` → decline by tie; closed-at-W row (endedWeek 208) and committed-at-W row (startWeek 208) are both off the strict roster → decline; the same actor signed at 207 is on → settled; an Enemies-band value reads Strained (family 4 measured GREEN) → `none` → decline. Not measured by a passing test in this run.
2. tests/p14b5-relationships.test.ts:700: `for (const edge of edges(after)) expect(findEdge(again, edge.a, edge.b)).toEqual(edge)` loops over ALL edges after one more tick; rival r03 `film:6` (take at 62, edge-6) releases at 66 with criticScore < 40 and lawfully receives a `sharedFailure` driver (plan (2b), symmetric). The comment's intent ("no release for THIS picture") holds for the six player pairs; the loop scope is over-broad. Every other assertion in that case passed (the six pairs' driver, week stamp, rngState control, idempotent replay).
3. tests/bridge-p14b5-relationships.test.ts:396: the leak loop contains the literal `"relationships"`, which the serialized DTO now contains as the ENUM VALUE inside `preferences.priorityOrder` (`["opportunity","compensation","relationships",…]`) — required by family 7/11 of the same file. The leak law's intent (no `relationships` ROOT on a DTO) needs a key-form check (`"relationships":`) or the value excluded; cannot be satisfied lawfully as written.

## Observations (source facts / measurements, not claims)
- No `pairChemistry` consumer outside src/core/index.ts (grep). `src/core/relationships.ts` imports only `./types.js` (no talentMarket import, no RNG symbol). Exactly one new DESCRIPTOR_REASON; no existing reason/drop/tie string removed or changed (diff of removed lines checked). D5 sentence and `nemesisOnRoster` sentence carry no digit, person id or tier name.
- On `p13-public-commercial-adoption` at week 207 the rival fixed teams have saturated: several rival pairs read closeness 100 / Inseparable after ~20 shared productions (24 edges by 207). Not a defect under the named hypotheses (HIGH 6, repeat cap 3); a T4 §8 :645 measurement item and a hypothesis-value note for Fable.
- Four seed ledgers: settlement/receipts/employment/takes digests and rngState byte-equal to the 654-T controls with D5 ranking live (bands all 1) — R-D5 CANNOT-MOVE class held.

## Evidence limits
- Root/bridge typecheck EXIT 0 was not reachable: the tsconfigs include the test trees the sweep owns; src/, bridge/ and ui/ production code are error-free in both.
- Family-6 predicate ends, drift case and `enemies here` are paper-traced only (their RED cases stop at the :351 premise); no scratch probe was run (unauthorized check).
- `npm run typecheck` also runs the ui tsconfig; it reported no ui errors in any run. UI test pins (600-T3's eight) were not run (not authorized). Nothing native; nothing Owner-accepted.

## Next concrete action
Parent lands S1–S5 via the index (patches apply cleanly; the new file arrives through the appended `--no-index` hunk); test-author sweep patch (30→31, 47→48, schema id 00c0075b…, 35→36, six→seven-member arrays, helper validator calls, `_historicalCurrent` → migrateToV31) PLUS the three amendments above (:351 premise, :700 loop scope, :396 leak form) with the ruling recorded; then the T3 control run with the ledger.
