# Kernel hold-order extension70

Status: INERT / UNEXECUTED; five standalone Vitest cases, 196 lines. Draft `70-kernel-hold-order-extension.test.ts.draft.txt` is intended for `tests/p14b4-kernel-hold-order-extension.test.ts`.

Draft SHA256: `b2a48307aeb3c8856a22def4076a662757ffc121111a15ec97b97fbb2cd00ead`.

Read the full independent review69, definitive interface49 and clarification56. This extension addresses only69's non-vacuous fixed-hold/replacement order gap. No mutable kernel implementation was inspected, and no test, typecheck, engine probe, Git, network, production/live test/fixture change or delegation occurred. Existing57 installed test remains byte-identical at SHA256 `076f5050aed93c33c269e00ef9d4f6477c3df9da775bc4c2b44022309eab291f`.

The detached finite fixture has six fixed holds: two explicitly replaceable F holds (target person and exact stage0), two immutable holds for F's other cast, and another owner's immutable person and stage1 holds. F's selected `firstTake:null` continuation replaces BOTH named suffixes, retaining original identities/prefixes, and has a nonempty Post hold. Existing G has all three person holds plus stage/Post holds; later Q has its own five additional holds. G seats target T, flexible prior A and generic prior B; Q supplies T's spare. All interactions are literal mathematical fixture facts, not fabricated engine staffing, saves or admission evidence.

Paper-derived full-budget result is strictly `CERTIFIED_ACHIEVABLE`, remaining1/B2, prior existing profile2 at18, selected F+G+Q, exactly four credits and target take boundaries18/28. No other selected set can meet both priors and the target buffer: G is their only event, T/stage0 cannot reach G without F's releases, and Q is the only spare. A local non-search checker verifies the returned selected replacement legality, exact identity/prefix retention, every immutable other-owner hold, all17 effective/additional holds, pairwise person/exact-resource-slot compatibility, actual cast/window/demand joins, one credit per person/path, zero credit from F and same-witness existing-first-X/slack.

Full-budget and capped comparisons reverse every relevant nonempty collection used in this fixture: fixed holds, selected continuation's replacements, all additional holds, alternatives, prior claims, masks and omission annotations. Foreign debits are empty here; original57 separately covers their nonempty order. Owner-fact reference packets retain their internal order: the requested gap concerns canonical scheduling collections, not a new assertion about owner-proof packet semantics. Every comparison requires equality of the ENTIRE result, including witness serialization and workUsed; deep-frozen input and exact before/after equality guard purity.

Budgets1/64/256 are provably binding before normalization completes: the test independently sums only a subset of literal input string leaves and requires that character lower bound exceed256. Interface49 charges every such character. Those cases therefore require exact `UNCERTIFIED/workLimit`, workUsed equal to the cap and sole omission `normalization work limit`. Budget2048 is an additional intermediate comparison without an invented normalization/search threshold; any returned positive certificate still passes the independent exact checker. Normal work remains200000; no cap or timeout is increased.

All bodies and fixture guards remain UNEXECUTED. No behavior, runtime performance, owner-adapter completeness or full-B4 acceptance is claimed. Parent may review exact bytes, authorize installation and run serially after the production writer freezes. Original57 and its historical missing-module RED remain unchanged.
