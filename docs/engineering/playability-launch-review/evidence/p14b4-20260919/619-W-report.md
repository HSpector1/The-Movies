# 619-W report — final seating preference (record 621; plan :215-236). Writer 619-W, sim-core, IMPLEMENTATION.

STATUS: DONE (both steps; the RED lands in R2's second permitted outcome, attributed to G10-b with receipts). No commits.
Worktree /Users/zacheryspector/The-Movies-headless-program, HEAD 68b10834; working tree carries only the three writable files.

## Step 1 — reader + chooser (cumulative patch 619-W-cum-S1.patch, sha256 6646be64…, 5700 bytes; status 619-W-status-S1.txt)
- src/core/promises.ts: +21 lines after promiseCastSlots (HEAD :597), before qualifyingTakes — `export function promisedCastMasks(state: Pick<GameStateV30,'promises'>, issuerStudioId, takeWeek): ReadonlyMap<string, readonly CastSlot[]>` exactly per 619-A §2(a) (evaluable, progress<count, same issuer, half-open window, intersection via CAST_SLOTS filter). No import change, no index export.
- src/core/hollywoodPolicy.ts: :7 `CastSlot` type import; :19 `const CAST_SLOTS=[...] as const`; :35-36 optional option `promisedMasks?:ReadonlyMap<string,readonly CastSlot[]>`; :40 `let bestBenefit=0`; :41-43 `masks`/`benefitOf` (0 when undefined); :46 `const benefit=benefitOf(inp.cast)`; :69 `if(benefit>bestBenefit||(benefit===bestBenefit&&score>bestScore)){bestBenefit=benefit;bestScore=score;best={…unchanged…}}`. :47-67 byte-identical; return shape unchanged.
- Self-checks: `npm run typecheck` EXIT 0 (619-W-S1-typecheck.log). RED file 5 failed / 15 passed / 2 todo = the 620 baseline exactly (619-W-S1-red.log) — expected, since no caller passes `promisedMasks` before the seam; both chain digests and all 15 controls green (no-member path byte-identical). Failing titles: witness RED (:441 expected 2 to be 3), seed-b member table (:458, 2 misses w211/w220 r01), CONFLICT (:473), TIE FALLBACK (:514), met-promise r01 chain (:619).

## Step 2 — seam (cumulative patch 619-W-cum-S2.patch, sha256 eeeb75a2…, 8840 bytes; status 619-W-status-S2.txt)
- src/core/hollywoodTick.ts: :6 `import { promisedCastMasks, WEEKS_TO_FIRST_TAKE } from './promises.js'`; :161-170 director/craft by the existing first-non-busy rule, `masks=promisedCastMasks(state,b.studioId,week+WEEKS_TO_FIRST_TAKE)`, `taken=new Set([ready.writerId,director?.id,craft?.id])`, `promised=employees.filter(masks.has && !busy && !taken && skills.acting!==undefined)`, `actors=[...promised, ...first-non-busy actors not already included].slice(0,3)`; guard :171 unchanged; :177-178 chooser call gains D1 `...(masks.size>0?{promisedMasks:masks}:{})`. Planning call (:219-221) and everything after :178 untouched.
- Self-checks: `npm run typecheck` EXIT 0 (619-W-S2-typecheck.log). RED file 19 passed / 1 failed / 2 todo (619-W-S2-red.log): the ONLY failure is the default-seed digest at :406 (expected dba473b9…, received 2aec0184…); seed-b digest unchanged; SEED-B MEMBER DECISIONS 414, MISSES 0; witness chosen a4/a2/a3 benefit 3 @ -1735081 (ordinary a4/a3/a2 #2 @ -1425536; [021] NOT VIABLE). `tests/p14b4-cast-class-outcomes.test.ts` 23/23 (619-W-S2-outcomes.log).

## R2 attribution of the default-seed digest (scratch probes, current source vs `git archive HEAD src` copy; logs 619-W-S2-probe-{current,head,receipts-current,rivalworlds}.log; rows probe/rows-{current,head}-default.json)
- Default seed: HEAD 1014 rows / current 1015; pictures 51 both; every current row `ok` under the RED's law (notOk 0); first moved week 208 (< w208 byte-identical, as 619-B corrected).
- Moved rows (pool ≠ HEAD): 285 pool movements + 1 chosen movement + 2 rows only at current / 1 only at HEAD, all from ONE cause, D2/G10-b:
  - studio-aca408ec-r01 (w208): employees in order r02-0(writer), r02-1(director), r02-2/3/4(actors), r02-5(craft) after the w208 team swap; ready script-0006 and script-0011 both carry writer person-studio-aca408ec-r01-0 (departed) ⇒ taken={r01-0, r02-1, r02-5}; admitted member = person-studio-aca408ec-r02-0, role writer, promise-12 (APPEARANCE_COUNT count 1, generic mask, window [208,416), bound contract studio-aca408ec-r01:contract:person-studio-aca408ec-r02-0:208). Pool [r02-0, r02-2, r02-3] (HEAD [r02-2, r02-3, r02-4]). script-0006: null (no viable package with that triple; HEAD produced film:6 here) → loop proceeds → script-0011 produces studio-aca408ec-r01:film:11, cast lead r02-3 / antagonist r02-2 / support r02-0, writer r01-0, director r02-1, craft r02-5, budget 3033668+594746; first take first-take-event-45 at w213 (= 208 + WEEKS_TO_FIRST_TAKE) with that cast; promise-12 (r02-0), promise-16 (r02-2), promise-18 (r02-3) SATISFIED on first-take-event-45; promise-20 (r02-4) stays OPEN (at HEAD film:6 served r02-4/3/2 instead). Later r01 pictures w229/242/255/264 keep the same cast r02-4/r02-3/r02-2 with marketing drift only (cash downstream).
  - studio-aca408ec-r02 (w208-349, 284 rows): symmetric — admitted member person-studio-aca408ec-r01-0, role writer, promise-1 (P1 generic, [208,416)); pool [r01-0, r01-2, r01-3] (HEAD [r01-2, r01-3, r01-4]); chosen null on BOTH sides for every row (r02 already had no viable package at HEAD), promise-1 stays OPEN.
- rivalWorlds (seed-b, current source): search still meets at w215 on studio-bc14baf6-r01:film:23 — lead promise-8 (r01-4, tagged leadOrAntagonist) = genuine; antagonist promise-4 (r01-2, tagged leadOrAntagonist); support promise-6 (r01-3, P1). HEAD had antagonist promise-6 / support promise-4; outcomes comment :211-216 is now stale (record-only).

## Evidence limits
- Probes are scratch-only replicas of the RED's lawRow/rivalWorlds logic, run under a scratch vitest config against the project source and a HEAD archive; not project tests. No native, bridge, policy-file, capacity or full-core run (parent's list). The RED's default-seed digest re-pin and the positive seam witness (todo :697) belong to the test-author per R2.
