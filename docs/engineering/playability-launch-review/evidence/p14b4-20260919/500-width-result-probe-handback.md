# 500 — Temporary width-result and remaining-cut probes

2026-09-20. **SOURCE FROZEN; temporary write ownership yielded.** Authorized
diagnostic only, on the parent-verified492 candidate. No additional optimization.

## Exact scope and pins

Only `src/core/promiseCapacityOwnerReplay.ts` and this handback were edited.

- Original492/restoration SHA256, checked before editing:
  `5267ff51ba638229306c6861fd965ae2f214529808b145ba9c52cd21ccf71bcb`.
- Frozen temporary source SHA256:
  `56a0cc5d204b768294160f3f5e8cdf42e381f41e2c1f67366dba894c13217a62`.
- Original492 handback pin supplied by parent:
  `e8620d747acd8ed2bd3477400a3cd6c77673844007202e8d7dbe4710b74b4f8b`.

No tests, owners, kernel, limits, timeouts, validators, configuration or other
production source changed. No runtime, compiler, tests, probes, Git, network or
delegation was performed. Native sim-core scoped ownership was followed.

## Complete temporary delta

Exactly six passive `console.log('B4_RESULT_500', JSON.stringify(...))` sites and
their comments were added. All observer work is **unmetered diagnostic overhead**:

| Site | Position and payload |
| --- | --- |
| payFailure | Only inside the original failed-payment branch, BEFORE saturation. Old used, requested units, limit and a newly captured Error stack. The original saturation and WorkLimit throw immediately follow. |
| dimensionsEntry | Before the original first dimensions payment. Current used and supplied production remainingTicks array. |
| dimensionsDone | After the unchanged discovery loops, immediately before original return. Current used. |
| frameEntry | Before the original first frame payment. Current branch week, used and whole-slate remainingTicks array. |
| sweepBillDone | Immediately after original sweepBill returns, before the original combined owner/observation prepayment. Week, used, returned bill, remainingTicks and the existing complete numeric/boolean Dimensions record. |
| result | Immediately before the original normal final replayPlans return. Final used and attempt kind/reason/through only. No proof, projection, provenance or source records are serialized. |

The failed-payment one-line branch was expanded into braces solely to insert
the observation before its original `this.used = this.limit; throw new
WorkLimit()`. Its comparison, assigned value and thrown class are unchanged.
There are no per-success-payment logs or stacks.

The generic replay result projection uses observer-only typeof/null/property
guards rather than an unsafe cast or a change to the driver type. It reads only
the three requested discriminants/boundary. The two authorized fixtures reach
the normal final return; early preparation-return paths were not instrumented.

All original numeric payment expressions, units, calculations, scans, owner
calls, branch conditions, catches, public returns and execution ordering remain
unchanged. Extra diagnostic maps/serialization/stack capture are not proposed
free production operations. This is not an adoption of silent-callback narrowing
or any other pending candidate.

## Static review and parent execution

Static reads covered the original six insertion sites and every added block.
Source was hashed before and after editing. Runtime neutrality and resulting
meter values remain for the parent to verify, not claimed here.

Parent reported494–499 already closed: types passed; Ready17 passed with the sole
original stale workLimit failure; first-take plus unchanged kernel passed;
started27, adjacent203 and supplemental7 passed. Bridge retains its earlier
future-P2 kind diagnostic. These are predecessor results, not runs of this patch.

Next: parent full neutral-diff review, then the two original first-take and stale
files serially under the fixed-source harness. Read complete raw/metadata and
compare original outcomes before interpreting the new stage/cut observations.
No additional kernel/test instrumentation or cap/timeout relief is needed.

After both sessions close, archive the exact patch and logs, remove all six
statements/comments and restore the original one-line failed-payment branch.
Verify the exact492 restoration hash above and the complete parent-protected
tree/patch (`64057…`) before501 qualification/publication or another source task.
Write ownership is released; this diagnostic source remains frozen until parent
restoration or explicit correction authority.
