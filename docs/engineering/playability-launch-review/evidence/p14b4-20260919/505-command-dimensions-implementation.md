# 505 — Paid command-only dimensions

2026-09-20. **SOURCE FROZEN; sole production-write ownership yielded.** Native
sim-core implemented only full adopted503, including its independent review.
No executed saving, stale-route fit or B4 acceptance claim.

## Scope and pins

Only `src/core/promiseCapacityOwnerReplay.ts` and this handback were edited.
Supplied published source base: `80aec8b348f7322da552ef3cd35ae9ba671ba488`.

- Original492 SHA256, checked before editing:
  `5267ff51ba638229306c6861fd965ae2f214529808b145ba9c52cd21ccf71bcb`.
- Frozen505 SHA256:
  `5d4851713ff53209b569c813b78a1e22e6b53ab91e27522ceb5a9ee7222a941a`.

No tests, owners, kernel, schema/save/projection versions, configuration,
validators, limits or timeouts changed. No runtime, compiler, tests, probes,
Git, network or delegation performed. Independent504 duplicate-commit baseline
was reported PASS by parent before this source release; no candidate verification
is claimed from that predecessor result.

## Complete implementation delta

Added private ten-field `CommandDimensions` Pick and consumed structural Picks
for workflowUpdate/geometryBill. Added the exact fixed commandDimensions literal
cost and one private commandDimensions function. Changed only executeCommand's
discovery call to that function, with its own prepaid call-site work.
workflowUpdate, geometryBill and arrivalBill parameter types were narrowed;
their executable bodies are unchanged. Complete Dimensions remains assignable.

The fresh projection fields are n,d,dp,operationsCopy,workflowCopy,taskCopy,
structures,provides,placements,cells. There is no zero-filled full Dimensions,
unsafe cast, new callback/closure, public API or copied owner pipeline.

The existing full dimensions body, staticDimensionFacts body,
dimensionRecordFacts body, Work helpers and every actual owner remain unchanged.
Frame and Ready-admission call sites still use full dimensions. No existing
tariff, bill formula or other optimization was altered.

## Discovery and ownership inventory

| Current record/fact | Command discovery |
| --- | --- |
| Invocation static facts | Existing fully paid staticDimensionFacts; reuse only its established immutable source facts and current completed cell value. Static geometry counts/width remain conservative. |
| Current operations root | Copy-only exact-reference fact request; full own-string key footprint, no root-value traversal. |
| Every current production | Direct ID/director string payments and fresh ID/width maxima; no generic-P key/value enumeration because no command copies P. |
| Every current workflow | Both modes, including complete current shallow copy footprint and intrinsic direct-string maximum. |
| Every current binding | Both original modes retained; copy fact is not projected, but preserving the existing fused mode avoids splitting a later full-frame discovery. Width contributes to geometry. |
| Every current reservation | String-only mode, current membership/order retained. |
| Every current nonnull shooting task | Both modes; full shallow footprint and direct strings. |
| P copies, genre, setup, Sets, technology root/rows/allSilent | Not enumerated by command discovery. They remain the responsibility of unchanged subsequent full discovery when actually required. |

Projection maxima use CURRENT records only. Existing intrinsic facts remain
keyed by exact immutable record reference; changed command results miss normally,
even if an ID or shape is unchanged. No mutable branch/ledger/calendar is cached.
Only the original helper publishes completely prepaid cold/upgrade facts.
No new cache/table, guessed availability or historical maximum is introduced.

All command owners and their order are unchanged. Missing/released production
still refuses before discovery. Command provenance, director due-at-call arrival,
manual-grandfather classifier, scheduling, concept/title scan, actual commitment
owner/sort, catch conversion and event drain remain in their original positions.
Even the common commandBill calculation on commitRelease is retained. Current
binding/reservation strings plus existing static geometry continue to bound the
actual scenery owner; setup/Set/technology records are not scenery dependencies.

## Source-counted prepayments

All new work is prepaid before its source block; helper bodies charge themselves
separately. Fixed call expressions use two units per scalar access/operand/call/
binding node. Traversal/max-update blocks use the existing176 finite-operation
and keyed-write model. No string span, table scan or discovered property is
hidden in the fixed blocks.

| Block | Payment and counted source work |
| --- | --- |
| executeCommand call site | New24: callee1, input.source2, branch.productions2, branch.operations2, work1, prepared.dimensionFacts2, invocation1, result binding1 =12 nodes. Original shared-cell payment2 remains. |
| Static helper call |12: callee1 + three reference arguments3 + call1 + binding1 =6 nodes. The existing helper then pays all cold/warm body work. |
| Operations fact call |16: callee1 + record/two modes/cell/work5 + call1 + binding1 =8 nodes. Existing helper owns its actual reference search/discovery/publication. |
| Fresh projection |40 scalar allowance covers at most20 value/binding nodes (17 value nodes, binding and construction-result controls), plus exact literal82. No nested array or closure is created. |
| Production traversal | Setup/terminal4, then44 per row: visit/control8, two text-call sites10, d/dp maxima including keyed writes26. Work.text independently pays each actual ID/director string span. |
| Workflow traversal | Setup/terminal6 includes operations.workflows access. Each row starts24: visit/control8 plus the eight-node fact call16. |
| Workflow maxima |32 covers copy maximum21 and width maximum10, with one conservative control unit. Copy maximum includes write(workflowCopy)=13; width write(d)=2. Receiver/property operands and both Math.max invocations are included. |
| Binding fact and width |18 for nine-node call (nested workflow.bindings adds one node); then10 for width maximum, receiver/operand/call and d keyed write. Both fact modes remain requested. |
| Reservation traversal | Setup/terminal6 includes workflow.reservations access. Each row24 for visit/control8 and fact call16, then10 for width maximum/write. Actual string-only helper work remains separate. |
| Task guard |6 covers current task reference/property, null operand, comparison and branch controls. |
| Present task fact/maxima |18 for the nested-record nine-node call; then28 covers taskCopy maximum17 plus width maximum10 and one control unit. write(taskCopy)=9; write(d)=2. |
| Return |2 for fresh projection reference and return. |

For each `d.d = Math.max(d.d, facts.width!)`, the eight scalar operations are:
left-hand d reference1, Math reference1, max member read1, right-hand d reference1,
d member read1, facts reference1, width member read1 and invocation1. The keyed
write adds1+length('d')=2, giving10. The copy maximum has the same eight scalar
operations and instead write(workflowCopy)=13 or write(taskCopy)=9, giving21/17.
Thus the paired maxima are31/27, conservatively prepaid32/28. No separate string
scan occurs in these maxima; the invoked fact helper already pays its discovery.

The literal is exactly:

`1 + 2(n) + 2(d) + 3(dp) + 15(operationsCopy) + 13(workflowCopy)`
`  + 9(taskCopy) + 11(structures) + 9(provides) + 11(placements) + 6(cells) = 82`.

For P current production rows, W workflows, R reservations and T present tasks,
the new projection/call wrapper contributes
`186 + 44P + 96W + 34R + 46T`, **in addition to** all actual Work.text,
staticDimensionFacts and dimensionRecordFacts payments. This is an inventory of
the implemented fixed blocks, not a new unmetered computed bill or a whole-owner
tariff. No variable-cost arithmetic is executed without the original helper
payments. Every iterator has an empty-list/terminal allowance; every visited
record's accesses/calls/updates have their own paid body block.

## Static verification and remaining limits

Static review covered complete new function, type signatures, literal keys,
executeCommand call and its adjacent refusal/provenance/owner code. Before/after
hashes collected. All owner-copy reservations and executable shared bill bodies
remain; consumed maxima may now exclude unrelated record domains, as authorized.

500 measured original command discovery4542/3176;482 attributed2221 gross fact
units to omitted P/Set/technology requests. Neither number is a net-savings
prediction: some cold discovery shifts into the next full frame, and the shared
linear-table history changes. Those later observations must still be charged.
New finite wrapper overhead is also real. No fit or performance result is asserted.

Parent next: independent actual-source/payment audit and serialized type/command/
first-take/stale/regression checks. Preserve276 first-take/kernel GREEN and the
original302 stale RED honestly if it remains.504 duplicate-commit and existing
director/manual-clear/schedule/release controls remain immutable. B4/liveP2,
save/wire cutover and Unity/native/Owner acceptance remain outside this tranche.
