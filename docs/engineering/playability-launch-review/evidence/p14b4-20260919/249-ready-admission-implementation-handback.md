# 249 — Ready admission candidate handback

2026-09-20. Native sim-core. SOURCE FROZEN for parent verification.
Base supplied by parent: published/exact-remote
`78796c185c8fa7b3a8e716796760e4f11a4c905f`.
Read full240/241/247 and all installed244 before implementation. No runtime,
tests, typecheck, generator, Git, network, delegation or test edit performed.
This is an implemented candidate, NOT a green or budget-acceptance claim.

## Scope and identities

Only three production files changed:

| File | Frozen SHA256 |
| --- | --- |
| src/core/promiseCapacityOwnerReplay.ts | 21d5827cb5bca77b063b4cd768c2643283322e897e89ff631f600d95f62b0b24 |
| src/core/productionIdentity.ts | 34a9ca45c58bf87f3af24c4d0717d6efd64a8cc3eae1b85b19761ae6ed023eb5 |
| src/core/actions.ts | 6a4b3c19265367c5d59b76e87806e1e0cc672cf8d1a89b5eea1f66b8134bb6a5 |

The exact pure productionId body moved to exported allocateProductionId;
predictProductionId still uses the same current tick and persistedProductionIds.
No action validation, finance, forecast, participants, queue or commitment body
changed. Source inspections and an empty trailing-whitespace search completed.
The hash command produced its usual host-locale warning, then all hashes.

Installed244 remains
`431a380965938c614364a382a17bfd33607c85bf0bce630f206629ae6dafe692`.
Protected source pins still match parent baselines:

- kernel `1faa6fcac443dac046bd97fa3e27c61bada80c7297f8e69ea4ebcff76d899332`
- operations `9935b09495df1d11f39bf3b1a37b244c3d7bf7d0994cd4c8ce286571b3be3411`
- sorter `87861f3ceefe2b2a9163a34bd9762a991d5c8a7f1640b84fbe6d1133f33ff964`
- release `9ab259623d3b5f5f75f1d750168e68ccb5c14d27d96f06f4656985db8064b218`
- scriptDevelopment `a82629e70d467c4716a280c7cac6fcaa67ce26137accc0d5d9e7835fefd63a49`
- castingSessions `497a8a32152e556378db96d8363be6de9a0719f2eed1fbec4a4fcfe222cf3a3b`
- productionPeople `2c6ea9affd6811d25cd36d85808d9470441f579153a7f0a2fb5b6a2c8a118d58`

## Implementation

Both public entries call one private replayPlans driver, with one Work and one
source preparation. Generic finishing/admission functions are module-level;
there is no recursive public replay, state clone, fresh branch allowance, copied
sweep or independent calendar. The existing public started result stays unchanged.
Its preparation now shares command-mode validation and the foreign guard; small
additional dispatch/type-shape bookkeeping is charged rather than hidden.

Ready input takes the original GameState reference. Each canonical sibling plan
independently resolves the genuine Ready project, shared header/staffing owners,
busy facts, actual current contracts and (when needed) actual freelancer owner.
No caller eligibility/affordability certificate is accepted. The exact identity
owner runs over its full persisted union, including retained cancellation and
history identities; history is used only to bill/derive this execution key.

The new narrow clock has no forecast, shape, promise, budget or participants.
The unchanged initial allocator gets the exact script/casting/Set slot union and
nextSetId marker, without technology/setup callbacks. Its refusal is a
commandRefused cut; no linker, queue, later commands or sweep follows. On success
the actual linker produces admissionScriptDevelopment, an IMMEDIATE snapshot,
never a horizon GameState/script root. The branch appends picture/calendar facts,
the exact readyAdmitted boundary(now,1), company holds there, and facility holds
at their actual grant-event boundaries. Original fixed holds are unchanged.

New physical identity is ['screenplay', issuer, projectId], with existingPath
true. All sibling choices can use the same Ready project and would-be production
identity; neither the linked root nor new clock is shared mutably. Expanded
foreign-company/writer/research checks use the same extracted guard per branch.
New clocks are included in all actual sweeps, dynamic workload dimensions,
genre lookup bounds, event joins, releases and trace output. Commands resolve
the admission's ID, then the CURRENT branch lookup refuses any released clock.
No source-record resurrection occurs. Ready-target commands normalize to the
existing production-ID command owner and provenance shape.

The final projection splits originals and planned clocks by the actual admitted
identity, not by a caller's optional extension property. Original full records
and their opaque references survive the generic owner. Horizon/source-now
ordinal checks occur in preparation before admission owners. Empty plan input
does not pretend to request admission; nonempty Ready plans require H>now.

## Before-call cost inventory

Same source-level model and caps as176/218/227; not a JS-instruction, native hash
implementation or wall-clock theorem. Consumed containers are genuine validated
plain engine records, with opaque own-string production extension values kept by
reference. New cost arithmetic uses the existing paid/saturating scalar methods.
No calculation is used to run an owner first and charge it afterward, and no
success-path bill is refunded on refusal. Fixed literal key/capacity/write costs
are declared in LITERAL, including every new result/context/clock/provenance row.

Let E(L)=1+2L and K(n,L)=1+L+n*E(L), as in the existing Work methods. Each
calculator invocation and inspection loop also consumes Work before its own
reads/arithmetic; the following are OWNER reserves, not free preparation.

| Source block | Preparation and owner reserve |
| --- | --- |
| persistedProductionIds | readyProductionId visits exactly the owner's roots and nested lists. Each inspected row pays its preparation; V counts visits, A counts nonnull candidate adds, W is maximum identity length. Owner reserve64+40V+A*(8+K(A,W)) covers root/list/callback/switch/null guards, event-ID temporary output, inserts and return. Nested subjects/reservations/business roots count separately. No recursive whole-record traversal. |
| allocateProductionId | Actual taken.size bounds the collision loop by size+1. Reserve100+(size+1)*(100+K(size,max(W,50))) covers base/suffix conversion, templates, loop/return and comparisons against even long retained IDs. Source week is a safe integer from shared validation. No new ID rule. |
| Header | searchBill charges the actual find-prefix inspection and separately reserves each owner find. Header reserve includes120 scalar/control/range-call steps, exact result literal, six fact-string comparisons, every intended-segment comparison, plus longest refusal text with project/concept/session spans. Caller fact/payload literals and closure are prepaid. No assessment or screenplay copy is invented. |
| Staffing | Every actual person lookup prefix is prepaid independently; fixed/profile control and dynamic craft-label construction are additional. Full returned cast/arrays, role-assignment rows, distinct-cast Set, all-role Map, engaged arrays and ordered refusal strings are reserved. Assignment count is5+craftCount. No free unused full-Talent scan. |
| Busy | Original prepared picture companies were produced by productionCompanyTalentIds; active source drafting/rewriting pools by scriptProjectWriterIds. Their union is exactly greenlight's company+activeWritingAssignmentIds on strict source data, without its UNUSED titleMap/label records. Temporary rows, actual Set and assertGreenlightStaffingIdle reads/errors are paid. Credit itself contributes no row. |
| Current labour | Each isContracted call reserves the complete contract scan/half-open predicate, then pays contracted Set insertion. If every engaged ID is genuinely contracted, greenlightFreelancers continues before consulting freelancerIds for EVERY row, so the empty unused market Set is equivalent. Otherwise the actual freelancer owner is called, with the following full bill. No fee/solvency call is made or asserted. |
| Freelancer busy subtree | freelancerBill counts actual company/pool/research seats, project/index guards and concepts. It includes eager concept-map tuple/Map work, per-writer assignment literals and title label strings, flatMap arrays, up to THREE company/industry/busy Set layers, and research contract scans. This is independent of the lean direct-admission busy path above. |
| Freelancer membership | Full talent filter/current contracts, busy checks, scientist filter, pool copy and final ID mapping are paid. rivalEmployment's WeakMap is conservatively COLD: all employment rows, group arrays and index writes are reserved before the call. Unique genuine talent IDs bound indexed row predicates globally by employment rows, plus one lookup per candidate; no warm-cache premise. |
| Freelancer RNG | Real derived hiring stream only. The seed/key/hash span, four splitmix state steps, state/stream construction, and at most configured sample-count sfc32 draws/swaps are reserved; no Gaussian/rejection loop and no sim RNG advance. Formula includes240 fixed setup,8*(seedLength+50),100*min(talentCount,marketSize),6*talentCount. |
| Occupied slots | readyOccupancyBill pays ALL eagerly produced rows for the narrow screenplay view, narrow casting view and broad Set view, including production/task/bound-Set/mount claims discarded by the final filter. It also pays three empty research collections, root scans/Set filters, full claim literals, constructed qualified/bare keys, Map/Set collision work and result arrays. prepare already refuses under-construction Sets, so no unpaid scenery-slot assignment branch is claimed supported here. |
| Initial allocator | initialAdmissionBill uses new whole-slate N, full facility/capacity bounds, eager raw production claims, exclusion/occupancy Maps/Sets, actual bounded facility sort and slot-key scans. Initial Development has no policy, Set composite or retained slot. Workflow/binding/operations literals and copies, duplicate lookup, append/map arrays, allocation result, refusal strings, transition key and BOTH stamped owner events are prepaid. No sweep coupon or alternate allocator is used. |
| Linker | Paid root/project own-string key discovery and actual shallow copies; managed/Ready guards, requireProject find, conflicting-link scan, replacement map, new status/production fields and error spans. The new linked root is never a synthetic full state. |
| Branch/proof/output | Separate Ready clock/cast/craft copies, branch admission record, appended picture/calendar, picture greenlight field, prepared branch literal, new holds, event reconciliation, original/planned split arrays and full Ready projection/result literals. Existing shared completeTrace/ledger sorts and finish hooks retain the same cumulative meter. |

The initial allocation bound is specifically its actual Development entry body,
not the general transition's technology/setup upper bound. Later sweeps use the
unchanged restricted/retained/general owner bills; dimensions now include all
branch picture facts in the real genre callback bound. Preparation's global
trace/path occurrence bound includes the extra Ready path per plan. No cap,
timeout, phase-support cut or validator changed.

## Verification handoff / qualifications

No compiler or test was run by this writer. Numerical sufficiency of the genuine
7/26 cases, cancelled-ID and command supplements, and whole replay+kernel within
200000 is deliberately UNVERIFIED until parent checks this exact source. The
new typed driver and cost sums need real diagnostics, not inferred green.
Independent audit should check this full owner/caller inventory, particularly
cold freelancer/identity walks and eager occupancy, rather than treating previous
started-only reviews as approval of new coefficients.

Operational trace only: no affordability, future financial entitlement, natural
offer, maximum/count classification, exhaustive calendars/future-choice proof,
save/promise-rule/projection activation or native/Owner acceptance. Existing
complete claimsAndHolds remains contingent on the expanded actual context guards;
the enumerated single Ready staffing is not a complete choice domain.

Next concrete action: parent installs only independently frozen supplements,
runs root/UI and Ready/original replay suites serially, reviews exact delta/costs,
then directs bounded corrections from actual failures. Production ownership is
released now; all three source files remain FROZEN until explicit authorization.
