# 120 — independent staffing/admission case matrix

2026-09-20. INERT test preparation only; no executable draft or settled-API claim.
Parent reports published kernel source `5cae2da841d6d50d2c460dc87aba3b603435d00e`.
Read B-F2 owner13/14, `next-owner-extraction-notes.md`,113 rows7/8, the existing
action/employment/person-occupancy owners and the bounded fixtures below. Read
frozen118 (`f51debf358229440fbb80b0d31b49afb89e231ba2df4020315efc22c850fccdc`)
to align the staged boundary;121 review is pending. No new implementation was
inspected. No runtime, probe, typecheck, Git/network or delegation was used.

## Fixture categories and independent assertions

**Real-action state** below means existing actions/ticks produce the facts, with
the fixture's bootstrap disclosed. It does not mean ordinary natural play or a
newly observed strict-save pass. **Invalid pure probe** means a detached malformed
read input, never an admitted save or lawful historical campaign. Invalid commands
against a legal state are distinct from an invalid state. Every new conjunction
and all proposed assertions remain UNEXECUTED; finite premise guards must fail,
not skip, when a required person, profile, current market entry or status is absent.

| # | Requirement-derived executable design | Exact available route / construction limit |
|---|---|---|
| 1 | **Credited writer is free, not an engaged seat.** A Ready A credited to W may start while W actually drafts B. Assert W is busy for writing but absent from engaged director/cast/craft IDs and all freelancer charges; same credited W may appear on two otherwise disjoint pictures. Keep W's actual identity/profile and same-picture uniqueness. | `_p04a2WriterCreditFixtures.ts::buildScenario('p04a2-writer-credit-deadlock')` supplies `readyOnly` and `deadlock` using historical-control founding, actual commission/ticks/accept and second commission; no edited contract/cash in this builder. `p04a2-writer-credit-law.test.ts::twoPictureWorld` uses extra real hires and two accepted scripts, with ten distinct engaged people. Do not replace writer occupancy with commissioning's broader predicate. |
| 2 | **No writer employment gate or writer fee, but commission still requires employment.** From idle `readyOnly`, actually release W using `releaseTalent`; preserve resulting termination ledger/history and Ready screenplay. Then greenlight A with employed disjoint crew: W remains credited, no W fee. A new commission by that same now-uncontracted W refuses. | Existing `p04a3-greenlight-law.test.ts::withLapsedWriterContract` HAND-REMOVES a contract and is not genuine expiry/release evidence. The proposed replacement uses real release: actions2757 checks active contract and refuses active script assignments; `readyOnly` has no such assignment. Exact release-plus-greenlight conjunction is unexecuted and must guard affordability/status. Do not release W from `deadlock`, where W truly has an active writing assignment. |
| 3 | **All engaged roles remain exclusive; every pooled active writer is busy.** Reuse one incumbent director/lead/antagonist/support/craft from active A in otherwise disjoint B: exact subject refuses. Disjoint B passes. Separately use an actual pooled writer with the appropriate secondary discipline in an engaged B seat: drafting/rewrite refuses; actual completion to Review/Ready removes writing occupancy, without erasing credit. | The five incumbent-seat probes and disjoint control exist in `p04a2-writer-credit-law.test.ts`. `c2a-m3-rename-and-pooling.test.ts`, seed `m3-pool-busy`, really commissions and adds a second writer. Its established negative is commission/pool reuse; the new cross-discipline engaged-seat conjunction must be built with an independent Ready screenplay and six distinct IDs. `script-projects-actions.test.ts` supplies actual Draft/Rewrite/Review/Ready progression and persisted due, not a rewritten due date. |
| 4 | **Eligibility is the requested discipline, not primary-role label.** Genuine non-primary writer/director/actor/craft assignments pass when their actual profile exists. Assert primary labels/history unchanged. For unknown ID use an invalid command on the original legal state; for missing writing/directing/acting/craft profile use explicitly invalid detached pure probes, one relevant profile at a time. | `actions.test.ts` seeds `gl-xdisc-w`, `gl-xdisc-d`, `gl-xdisc-cast`, `gl-xdisc-craft` provide real generated/action positives under unengaged legacy economics. `p14bf2-acting-discipline.test.ts` additionally supplies an engaged real primary-writer-as-lead route with separate credited writer and actual contract/actions. Its missing-acting-profile probe is expressly INVALID, not a lawful save. Do not import audition's primary-actor candidate restriction or change candidate pools. |
| 5 | **Cast uniqueness and whole-picture uniqueness are separate.** Parameterize duplicate cast pairs, writer/director collisions, writer/cast, writer/craft, director/cast, director/craft, craft/cast and repeated craft IDs. Begin with an all-distinct legal package and enough real people. Assert cast duplication first where applicable, then the existing writer/director/craft/cast whole-role diagnostic order. | Real roster/Ready packages from rows1/3 supply identity facts; only command selections change. Writer's labour exemption never exempts uniqueness. A repeated craft command can fail identity uniqueness before the economy's exactly-one-craft gate; do not flatten all failures into a generic staffing result or expect the later diagnostic. |
| 6 | **Actual current contract OR actual current freelancer market; exactly one craft only when economy engaged.** Fully contracted package has no freelancer rows; a real market actor substituted into one seat has exactly its existing fee and no writer fee. A real noncontracted non-market actor refuses. Engaged zero/two distinct craft choices refuse; legacy unengaged zero craft keeps its existing legal behavior. | `d11-employment.test.ts` uses public `beginFounding`, real sign/found actions, and `assign-contracted`, `assign-freelancer`, `assign-illegal`, `assign-craft`. Read actual `freelancerMarketIds` at that week, never all idle/free agents or a predicted later rotation. Current contracts are half-open `start <= week < end`; a boundary test must actually advance/observe the market and condition its expected admission on actual membership, or separately test detached current-contract fact construction. Historical rows alone do not authorize the seat. `economyEngagedEver`, not nonempty contracts, selects this gate. |
| 7 | **Preserve caller-specific busy sets AND indirect market filtering.** On the same actual source facts, greenlight's direct busy set is player production-company + all active writing, whereas commission adds rival industry and active contracted research seats. Commission must refuse a contracted research-busy writer-profile subject; greenlight must not gain that direct refusal. For an uncontracted subject, passing the narrow check is insufficient: actual current freelance membership is still required. | `p13a-research-employment.test.ts` provides actual laboratory placement/ticks260/recruit/assign/beginResearch and an active contracted scientist, with disclosed engaged-world bootstrap. It proves broad busy and real release clears it; a creative-profile greenlight/commission conjunction remains unconstructed and must guard the actual profiles, staffing distinctness, budget and Ready package. A scientist is independently excluded from the freelancer market by primary role: that negative CANNOT isolate the market's research-busy filter. A non-scientist research-seat conjunction is not established here. For rival industry, use actual initialized industry assignments and current market facts; no invented employment/production. Until such conjunctions are built, detached caller-supplied fact-set tests may establish predicate separation only, not real market entitlement. |
| 8 | **Managed screenplay/casting admission precedes people.** Actual assessed Ready plus exact concept/writer/shape/promise passes; Draft/Review, missing project, or a command with mismatched screenplay facts refuses. An existing audition in review is incomplete until real acknowledgement; absent session remains lawful. Legacy DEVELOPMENT plus managed OPERATIONS remains lawful stock greenlight. | `script-projects-actions.test.ts` seed `script-actions-lifecycle` supplies real status/assessment/project link. `_m4Fixtures.ts::contendedGreenlightStudio` supplies actual Ready/audition/free-crew evidence. Use actual action progression for positive sessions; do not hand-stamp Ready, assessment or acknowledgement. Managed development and managed operations are independent switches. Missing assessment on otherwise Ready data is only an explicitly malformed pure header probe, not a valid state. |
| 9 | **Staged reads and refusal precedence.** Invalid header beats unknown person; identity/discipline/uniqueness precedes the active-writing busy read; commission founding/person/profile precede current-contract read, and uncontracted refusal precedes broad-busy read. Craft/employment remain after existing forecast. Transparent call-through observation may count real owner reads without replacing their values. | Existing `activeScriptWriterAssignments` can throw for a missing concept. Such a malformed detached input can discriminate eager reads, but cannot be presented as a valid campaign. For narrow helper tests, recording lazy fact callbacks can prove which read was reached without inventing engine history. Exact freelancer-fee versus later-seat-refusal order needs121's pending decision: eagerly collecting all freelancers before the old interleaved fee loop may change malformed-fee diagnostic precedence. Do not lock an oracle or call this behavior-preserving before that issue is resolved. |
| 10 | **Capacity-only waiting is not staffing admission.** Real Ready package queues on capacity only, with unchanged production count/ID, cash, ledger and crew commitments. The same eligible package admits with capacity; actual dequeue rechecks later staffing rather than trusting the stored command. An illegal engaged seat must refuse rather than become queued. Preserve one project-to-production link. | `p04a3-greenlight-law.test.ts` builds the real Ready/next-draft state then EXPLICITLY controls development capacity to one; label that capacity override. The genuine changed-eligibility-at-dequeue conjunction remains to be built using real release/expiry/assignment and the actual queue owner; never hand-edit a queued history or assume release necessarily removes current freelance eligibility. Queue priority and capacity classifiers are unchanged owners, not part of staffing extraction. |
| 11 | **Generic result preserves the actual caller record and pure inputs.** Derive narrow id/skills views from real people, add unrelated literal markers, and compare corresponding resolved IDs/order with full genuine Talent inputs. Both full and narrow return values retain their compile-time extra fields; engaged order is director, lead, antagonist, support, then craft, never writer. Deep-frozen input/sets remain byte-identical after success and refusal. | This is a detached read/type test, not a narrow view cast back into Talent or GameState. No `any`, partial-as-Production/Talent escape or discarded extra fields. The exact test signature waits for118/121 settlement. Real action controls separately preserve old financial/production effects; pure guards must never allocate ID, debit cash, consume RNG or mint history. |

## Minimal first tranche / explicit remaining proof

Start with rows1/2,4/5/6 and11: existing real packages plus bounded pure defensive
and type checks. Add managed header/commission callback staging once118/121 settles
the exact interface and fee-order concern. Use existing script/casting/queue and
discipline regressions for unchanged call-through behavior; no timeout increase,
validator relaxation, fixture rewriting or current-output snapshot oracle.

Research/industry-plus-greenlight conjunctions, contract-boundary market membership,
pooled writer in a different Ready package, changed eligibility at dequeue and all
new exact diagnostic observations are candidate constructions, not executed proof.
They must retain their hard guards and provenance. This small extraction does not
certify future employment, rival authoring, joint-owner trace completeness, natural
performance or the full P14B4 capacity adapter. Parent owns interface adoption,
future executable-test release, runtime and publication. This matrix is frozen.
