# 738 — the confirming full core run, predicted before it starts

Registered after the four-line correction in record 737, on a clean tree, before the run begins.
This is 736's prediction re-registered: 736 was falsified only by the four sites now corrected,
so if the attribution in 737 is complete, the re-run lands on 736's original numbers.

## Predicted

| quantity | predicted |
| --- | --- |
| test files | **9 failed / 346 passed (355)** |
| cases | **24 failed / 4011 passed / 8 todo (4043)** |
| exit code | 1 |
| `fixedSource` | true, `testedDiffSha256` the empty-tree hash at both ends |

The failing set is predicted to be run 717's nine files at their exact per-file counts:
`bridge-p12-campaign-library` 11, `p13a-scientist-foundation` 3, `r3n1-stale-schedule-take-02` 2,
`-02p31` 2, `-02p32` 2, `world-first-scenery-load-in-provenance` 1,
`p14b4-ready-replay-stale-target` 1, `p14b4-cast-class-capacity-evaluator5` 1,
`bridge-p13-campaign-isolation` 1. Causes byte-identical to 717 after normalising `mkdtempSync`.

## What falsifies it

- **IF ANY OF THE FOUR CORRECTED CASES STILL FAILS**, the correction is wrong rather than
  incomplete, and the site is to be re-read rather than re-edited.
- **IF A FIFTH NEW FAILURE APPEARS**, record 737's enumeration of the two miss classes is
  incomplete. The finder rule it states would then be necessary but not sufficient, and the next
  step is enumeration, not another edit.
- **IF A 717 FAILURE VANISHES**, the four-line correction reached further than its diff suggests.
  Four values-only lines cannot legitimately turn a red case green anywhere else, so this would
  be a defect to attribute, not an improvement.
- **IF THE CASE TOTAL IS NOT 4043**, the correction added or removed a case, which it did not.
- **IF `bridge-runtime-checkpoint` IS NOT 64/64, OR PREPARED-REUSE TIMES OUT**, both held in 737
  and nothing since then touches either, so either result would indicate run-to-run instability
  in the core project rather than a source change. That is a finding about the suite, and record
  719's FU-2 language applies: name the operation, compare timings both sides, do not default to
  engine defect or environmental noise, and do not widen the threshold.

## Not predicted, and deliberately not run

The `ui` project, for the reason record 736 gave and record 719's FU-1 holds open. B.7 makes no
UI-affecting claim.
