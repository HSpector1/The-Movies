# 351 — One-pass two-way reconciliation counts

2026-09-20. Native contract-auditor qualified KEEP; parent adopts smallest bounded
next change. Sim-core cross-check requested. No source edits/runtime/fit claim.
Restored339 source28e7e010c9887709ade1f3010018487931b710fdad1e14282fa5604e0681bc37.
350's actual cut occurs in repeated expected/ledger comparison AFTER the take
owner returns but BEFORE first-take output bookkeeping and wrap.352 full evidence.

## Adopted minimal implementation

In reconcile retain complete expected-fact construction and the existing first
ledger pass. Each temporary expected fact gains a prepaid numeric matches field,
initialized0. On an existing successful exact path/subject comparison, increment
BOTH current ledger-row count and that expected fact's counter.

Keep immediate matches===1 guard for each live production-resource ledger row.
Replace ONLY the second expected×ledger scan with original-order expected.matches
===1 checks. Every relevant pair is evaluated once rather than twice.
Pay the extra literal field, zero initialization, increments, reads and final
traversal BEFORE execution. Counters are per reconciliation invocation, never
cached across calls/frames/branches, never reused as boolean membership.

## Equivalence and diagnostics

Every expected fact is constructed as a RESOURCE on a known prepared-picture
path. First-pass skipped closed rows, people, null paths and non-picture paths
therefore cannot match any expected fact in the old second pass.

Retain BOTH diagnostics and their original order:
1. Live row count must equal1: duplicate expected facts or stale live resources
   still fail 'live production resource ledger differs from owner output'.
2. After all live-row checks, each expected counter must equal1: duplicate live
   holds or missing reservations still fail 'owner output has an uncertified reservation'.

Never fail an expected counter>1 early, silently deduplicate, overwrite duplicates,
replace exact multiplicity with membership, or omit a direction. Empty sides and
mixed closed/person/background/mount rows retain the same behavior.
This removes executed comparisons; it does not reduce a price for work still run.

## Explicit scope exclusion

Leave drainEvents, closeHold, addHold, closePath and actual gameplay owners
unchanged in this first slice. No new index/sort, static-picture index rebuilding,
global cache, metric adjustment, cap/timeout/test/validator weakening.
354 sourcewriter may implement ONLY the adopted reconciliation change and its
full payment inventory. Further changes require actual measured evidence.

## Retained constraints for any later index work (NOT this release)

A fully paid per-call index over CHANGING owner snapshots can satisfy176; that
is distinct from repeatedly rebuilding unchanged preparation indexes. Preserve
original references/structured fields/multiplicity. No parsing source IDs,
decoding tokens, ambiguous concatenated keys or assumed hash constant-time law.
Pay projection/literals/arrays/sort/comparisons/searches/updates before execution.

Event drain stays chronological: release joins exact BEFORE reservation, grant
joins exact CURRENT reservation, exactly one resource-key match with facility/
capability/capacity validation. No batching/reordering. Wrapped Set closes at its
matching stage-release boundary, not earlier wrapped event. closeHold retains
exactly-one live path/subject; addHold retains global live-subject exclusion;
closed historical holds persist. Any future index must reflect every prior
closure/grant, not just the pre-drain snapshot.

## Verification and ownership

Existing original26/Ready15/lookup5/ordinary66/adjacent137 and types remain fixed.
341sourceKEEP and freelancerGREEN retained; firsttake/stale RED are independent
next regressions. After352 publication/exactremote353,354 one sim writer ONLY
replay+handback, then freeze/fullparentread/pins/355 independent source review and
serialized unchanged checks. No owner-policy or numeric-fit claim here.

