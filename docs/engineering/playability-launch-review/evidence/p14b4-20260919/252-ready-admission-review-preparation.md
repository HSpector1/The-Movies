# 252 — Frozen-review preparation

READY. Native contract-auditor immutable owner/call-tree checklist; candidate249
NOT inspected. No files changed, runtime executed or source mutation by reviewer.
Parent preserved full checklist2026-09-20. Published78796c1 used for actions and
production identity. These are checks for eventual frozen candidate, not new
prerequisites; no implementation acceptance or cost sufficiency is claimed.

1. Admission ordering and refusal costs.
   Preserve header→staffing→production/writing-idle→engaged employment/craft
   checks. Header includes project/concept/session searches and screenplay shape,
   segment and range comparisons. Staffing resolves writer, director, three cast
   slots and every supplied craft ID before later craft-count refusal. Do not
   bill only final lawful one-craft case. Include diagnostic interpolation,
   role-label arrays, Sets/Maps, returned arrays/records and actual string spans.
2. Production/writing occupancy.
   productionCompanyTalentIds includes director/cast/craft, never credited writer.
   activeWritingAssignmentIds is not merely ID scan: activeScriptWriterAssignments
   eagerly constructs all-concept title Map, visits every project, calls pooled
   writer helper and creates per-writer records/label strings. Preserve this gate's
   narrower scope; do not substitute broader industry/research busy policy.
3. Employment and freelancer derivation.
   Charge repeated current-contract searches and engaged-ID Set construction.
   If called, freelancerMarketIds performs full busy union, talent filtering,
   contract/rival-employment checks, scientist filtering, pool copies and partial
   Fisher–Yates sampling. rivalEmployment has existing WeakMap index: cover cold
   employment-index construction and subsequent lookup/search work; warmed cache
   is not free-budget authority. Pay seed/derived-key construction/hashing, stream
   initialization, draws, swaps and result mapping. No campaign RNG mutation.
   Actual all-contracted proof may omit unnecessary market derivation without
   inventing availability.
4. Complete persisted identity union.
   Cover every root traversed by persistedProductionIds: player production/
   loadout/film/run/ledger/career/broadcast/context/workflow/project/commitment;
   raw studio-event scan and returned Tier-D ID array; queue visits; history
   subjects and kind-specific IDs; Hollywood films, careers, business subroots
   and receipts. Nonmatching rows still cost traversal. Filtering to active
   productions or emitted IDs alone is insufficient. Moved allocator retains
   exact base/suffix behavior. Prepay strings, Set lookups and collision loop
   without relying on collision-free fixture.
5. Initial allocation's real eager work.
   Script, casting and Set slot helpers are separate calls with own producers
   and containers. setOccupiedFacilitySlots eagerly produces production/task,
   screenplay, audition, mount and bound-Set claims before filtering. No active
   Set work does NOT make that call empty/free. addManagedProductionWorkflow
   creates draft/bindings/root, appends draft, then calls real allocator. Include
   extra draft workflow in raw visits. Charge occupied-slot construction,
   complete facility filter/copy and bounded sort, Development capacity scans,
   keys, reservation/result literals, event transitions, derived bindings and
   replacement map. Initial admission supplies no technology/setup policy; do
   not add those callbacks or charge fictional call path.
6. Linking and dynamic replay.
   Linker searches project, checks other production links, copies project/root
   and maps complete project array. Preserve result as admissionScriptDevelopment,
   not later assessment/Produced state. New picture facts expand branch-local
   company, calendar, genre lookup, copy footprints and workload dimensions before
   shared whole-slate frame. Original fixed holds remain global; new holds are
   branch-local. Pay joins, boundary/event records, final splits and output.

Throughout: reserve BEFORE helper arguments/actual work including calculator
traversal. Preserve single cumulative counter, normal-cut versus administrative
envelope distinction, unchanged limits and original controls. Finite bounds and
useful runtime results will be assessed against frozen implementation; none is
certified by preparation. Auditor IDLE pending frozen-source review.

Parent relayed nested writer-display/cold-index costs to249. Writer reports using
already-paid prepared production-company plus drafting/rewriting pooled writer
facts (same statuses and scriptProjectWriterIds owner), avoiding unused display
helper; actual freelancer call still covers cold index. This is a writer report,
NOT yet a reviewed equivalence or observed implementation.
