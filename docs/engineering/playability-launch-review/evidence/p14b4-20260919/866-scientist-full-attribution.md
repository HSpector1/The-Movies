# 866 — Scientist full run closed; attribution before final test maintenance

861 is CLOSED, fixedSource:true. Source7ca52f877878266c9659a7a491693ae2128f681e,
empty tested patch, no untracked source at either endpoint. Started
2026-09-26T14:36:06.088Z, ended15:29:51.981Z, exit1.372 files,4339 cases:
4242PASS,86FAIL,11inherited todo. All27 independent Scientist cases pass in this
full run:15 core,10 runtime,2 bridge. Production remains697a6039 throughout.

## Complete failure attribution

`861-vs-837-failures.json`:32 NEW,0 VANISHED,54 SAME CAUSE,0 CHANGED CAUSE.
The supplementary865 comparison preserves complete primary diagnostics before
stack/source excerpts. Its raw derived results are861-vs-837-diagnostics.json and
861-vs-822-diagnostics.json; long Vitest whole-envelope context is retained.
`861-diagnostic-attribution.json` is the compact per-identity assessment.

- All54 failures from837 remain.53 primary diagnostics are literally identical;
  the scenery-export failure differs only in its temporary output directory.
  Normalizing that path gives equality; the actual cause remains missing PIL.
- One additional failure against837 is the exact known FU-2 checkpoint-recovery
  timeout from822/843. Its complete primary diagnostic matches822. Its20000ms
  limit is unchanged.837 passed it with a reported23746ms;861 timed out with a
  reported22160ms. This is an inherited intermittent result, not a new fix claim.
- The other31 are current-boundary test-maintenance omissions.14 schema-URN
  assertions still say50; three numeric projection assertions use50 (two B.6,
  one SNAPSHOT_VERSION); B.6's exact prior roster omits the actual outgoing50;
  R07's parsed live save expects36; twelve B.5 live-write equality cases stop their
  explicit conversion chain at36. The twelve whole-envelope diffs change ONLY
  the envelope's saveVersion36→37. Historical roots and state bytes do not differ.

Against822:31 NEW,0 VANISHED,55 inherited causes.54 primary diagnostics match
literally and the same one exporter temporary path differs. The result is not
all-green. No new production defect has been identified by this matched pass.

## Authorized repair and next verification

Apply the already reviewed864 patch, SHA256
5461d3ca811bef095735afa97e1e932c8f08704116f06efbc2d7913d360e132a,
only after checking861's closure/source guards.17 existing test files change.
Every genuine fixture, frozen conversion/reader assertion, independent Scientist
test and production/generated byte stays. The B.5 live equality chain gains the
actual convertV36ToV37; it does not relabel or mutate historical fixture bytes.
No timeout, budget, skip or todo changes. The parent remains test-maintenance
owner, separate from the prior production writer.

Run867 on the31 precisely selected repaired cases (selection manifest864), then
root/UI and bridge typechecks and a separate unchanged FU-2 case. Do not report
these as passed before their recorder JSON closes. The full run is the exact
7ca52f87 source; subsequent test-only checks must name their own recorded patch.
An aggregate qualification must preserve this distinction rather than relabel861
as a full run on a later commit. A new production change would require its own
appropriate regression qualification. Publish the closed evidence, then continue
the C.2-RM contract/review/T0 sequence in863 under the Owner's fresh-specialist
authorization. No new specialists have yet been created.
