# 178 — generated scheduled-take constructibility probe

2026-09-20. AUTHORING ONLY / UNEXECUTED. Native Fable test-author; parent owns179
execution and any typecheck. No live test/source/config/fixture edits, runtime,
Git/network or delegation. No replay API, capacity certificate or complete-cost
claim. Did not inspect mutable176; sim supplied the requested dimension list.

Frozen operational probe: `178-first-take-fixture-probe.ts`,293 lines,
SHA256 `7265a9689747497b144641af73765a368371f20342f606ea876edcac6a0a6d5f`.
Declared published source base:
`d9ebb81090dafc0d18d7c0f3b86e5f92d66ae123`.
The probe prints its own executed file hash and this DECLARED base; the parent's
fixed-source recorder independently verifies actual source identity. No shell/Git
command is invoked from the probe. Run from repository root, after parent review:

```text
node_modules/.bin/vite-node docs/engineering/playability-launch-review/evidence/p14b4-20260919/178-first-take-fixture-probe.ts
```

## Exact route and hard guards

Two separately generated scenarios (one picture, two pictures), seed
`p13a-production-consumer`. Existing `operationsStudio` means disclosed
historical-control founding, not ordinary recruitment; hires and studio formation
are real. Initialize actual Hollywood, real legacy-development/managed-operations
stock-script greenlight(s), real ticks to5, actual locked-director actions, real
tick-based scenery arrival, then schedule every Ready task in the SAME week.
No manual lower-owner clear, clock/role/cash/RNG/history mutation or new resource.

Twenty entry ticks and eight arrival ticks are finite prerequisite guards, not
timeout changes. Require real IDs, disjoint cast/company, exact workflow/task/
binding joins, no blocker/pending scenery, no prior take, and successful unchanged
`makeSave` validation. Print the generated source/local facts before checking
root-cut array emptiness. Original whole source remains untouched.

Foreign scope uses the existing `productionCompanyTalentIds` and
`scriptProjectWriterIds`, original activeScriptOrdinals/indexed development/cost/
concept joins, actual research status/seats/released flags. Missing source joins
throw. No fabricated foreign reservation or guessed future employment. These are
read-only prerequisites, not a replacement replay validator/cut implementation.

## Bounded observations

- Actual week/issuer/production IDs, clocks, locked company, full workflows,
  bindings/reservations/scenery, player technology and standing Sets. Each full
  Production and selected full local projection has a SHA256; bulky opaque
  forecast/participant objects are not dumped. No whole GameState/private saves.
- Original queue/physical/construction/placement/Set-work/own research/adoption
  facts plus relevant foreign company/pooled writer/research joins. Disjoint rival
  data is retained and counted, never removed to get past the cut prerequisites.
- Actual dimensions requested by176; enumerable own-key counts/string lengths
  and162 shallow-copy arithmetic for full productions/workflows/tasks/bindings/
  operations/Sets/technology/placement/structure records. ID occurrence/unique
  string metrics are explicitly the enumerated list, not an asserted full charge;
  additional shallow string-value summaries preserve the distinction. No opaque
  nested value is recursively traversed merely because spread retains it.
- Actual next tick: exactly5→4, one raw firstTake receipt per picture stampedw+1,
  retained production facts, no first-take Set wear. Actual second tick: wraps,
  ordered reservation/event rows, actual3/Post or4/Post-blocked state, stage
  released, task removed, one literal9 condition wear per actual wrap, novelty
  unchanged and no duplicate take. Raw local observations print before their
  effect assertions, allowing a failed premise to remain inspectable.

The two-picture Ready/cut-free conjunction and every type/import/assertion remain
UNEXECUTED. Probe failures are findings; do not erase roots, reschedule timestamps,
force capacity or relax guards. The emitted second-tick Post counts are measured,
not predetermined. This work only establishes real fixture dimensions and local
effects for the later work-bill review. The169 half-open horizon correction and
count-versus-B/slack distinction remain unchanged. Parent next: read/freeze exact
bytes, record179 execution with script hash, inspect both actual outcomes, and
send factual dimensions to176 without calling them a completed replay budget.
