# 423 — Certified late-phase reserves (adopted)

2026-09-20. Published base812d50d8b6e83677c752a4188d1180b23e394c3e.
Production source remains frozen410e7cb5ce0...; no writer/runtime.
420 actual attribution: firsttake completes actual wrap but cuts in drain;
newPost completes3→2, then2→1 cuts before owner at42630/request161747.
Do not weaken tests/caps/validators, infer fit, or discount actual prices.

## A. Single wrap common visits — independently source-reviewed

Reuse EXACT existing410 singleWrapSlot certificate, no new or broader witness.
Auditor source-proof KEEP: first sole older4 transition succeeds, releases,
settles originalID and restarts; second scan sees settledID and exits. No third
visit. Phase callback immediately returns without mutating settlement.
Retain earlier visits/rounds/attempts calculations and fallback expressions.
Immediately before common, separately prepay8 then bind
commonVisits = singleWrapSlot ? 2 : visits.
Use commonVisits ONLY in current common visits-multiplied term:
25 + 4*keyBill(n,dp) + 1 + n*(1+pid) + 2*text.
Keep order, initialization, output/copy/update/commitment terms and attempts1,
entire enter/allocation/callback/release/wear/event reserves unchanged.
No claim that this small saving alone closes the Ready route.

## B. Sole Post2→1 — read-only sim inventory, review required

Add private predicate singlePostExitProof using separately prepaid current facts;
false means the unchanged general bound, not refusal/unsupported.
Call ONLY after existing restricted and singleEarly return paths.
At call site prepay8 for argument/call/local binding.
Within proof:
16 before n/production-count/workflow-count guards;
12 before indexed production/workflow references;
24 before older-than-week, remaining2, nulltask, reservations-count1 guards;
8 before sole reservation reference;
32 before three reached string comparisons/return controls.
Compare workflow.productionId/currentP.id, workflow.phase/postProduction,
reservation.capability/post via Work.equal (actual spans additional).
Counts must precede indexing. No iteration, availability, silent-policy or
external-empty proof required. TOTAL92finite internal plus reached strings.
No new data object, global cache, input validation or gameplay restriction.

Production4/3/5/6/1/8 and multi-picture domains keep old bound (aside from paid
proof overhead on applicable existing general branches). 3→2 stays restricted.

Extend PRIVATE allocationBill with default-false singlePostExit flag. Solecaller
passes paid proof; prepay16 at callsite (replacing4108 with8 additional) for both
flag argument/selector dispatch groups; additionally prepay32 INSIDEallocation
before any newly added flag/selector controls. Preserve existing192block.
Only when singlePostExit:
- other/rawOwners=0 (same max(n-1) for certifiedn1) afteractualsolePostrelease;
  keep complete raw-producer empty walks/constants, d.external and occupied
  calculations using actual external slots.
- policy selection uses d.t (no possible new shooting lock); retain adoption,
  installations, callbacks, fullfacilitycopy/filter/sort, originalprices.
- slots0; composite0; retention20 (keep existing setupconstant, zero ONLYloopbody).
- requirement-driven facility scans0 (empty ReleaseReady requirements).
- allother expressions/constants unchanged. No change to actualallocator.

In sweepBill use prepay32 BEFORE the newly added selection controls below (keep
existing192 payment too). Only when singlePostExit:
- wear0 (not wrapping); policyLock=existing6+text nonshooting callback;
- setup0 and shootingbinding0; attempts1 (guaranteedemptyrequirements success).
- retain releasePhase,65, TWO fulltransition bounds, THREE fullworkflowupdates,
  allocation result, fullworkflow/binding/Pcopies,150 and existingcommon3visits.
- do NOT combine Post with wrap commonVisits2; its extra visit allowance stays.
No retuning coefficients, saturating helper prices or dimension discovery.
Extra32allocation and32sweep finite blocks conservatively cover added boolean
reads/branches/control; actual helper calls must retain separate prepayments.
Parent425 actualsource review must inventory new controls, not assume those
numbers cover added loops/copies. No loops/copies are proposed.

Symbolic candidate: raw0=40+1+2; occupied=E. occupancy keepsraw0+4+E+
E*(5+3*KB(E,K)). selection17+t*(1+2T); adoption30+adoptions*(8+3T)+
2placements*(8+4T). allocation keeps occupancy+3+3F+F*(selection+adoption)+
sortBill(F,6+2T)+20+140. enter keeps releasePhase+65+2transition+3update+
allocation+workflowCopy+bindingsCopy+pCopy+150. Totalcommon(visits3)+enter+(6+T).
These are unchanged qualified component prices with unreachable cardinalities
zeroed, not independent certification of every inherited coefficient.

Actual owner proof: Post→ReleaseReady requires no capabilities; release removes
the sole Post before allocation. No stage means historicalSet is notoccupied.
Rawproducer/externalSet stillrun; facilitiescopy/filter/policy/sort STILLRUN even
forSound. Emptycapability list prevents slots/retention/composite/capacityrefusal.
Actualnonshootingcallback stillinvoked; release/event/copy/update/finalprojection
costs remain. No wrap/setup/take/newtechlock/bindinguplift/movie-release.
Successfulproduction settles; oneattempt. ReleaseReady1 remains onslate;1→0
still requires actual commitment and later release laws, unchanged.

## Ownership / tests / verification

Parent ADOPTED after independent design review below; sole424 writer authorized.
Native sim-core may read/review but NOTwrite production yet. Parent metadataonly.
Author idle; existing410 newPost is actual RED and alloriginaltests remain pinned.
Author may independently review/add coverage only through separate bounded task;
no test edits authorized here. Max2specialists; oneproductionwriter.
Afteradoption, sole424writer source+handback; actual425source/testreview;
426+ serial root/UI,lookup5,ordinary66,Ready15,started27,adjacent137,bridge,facts2.
Readfullraw, closeeveryruntime, preserveactualfailures and diagnosticattribution.
Publishqualifiedcheckpoint, verifyexactremote, continueB4/P14/P15/P16/specifiedP17/P18.
Unity/native/Owneracceptance deferred; integrationbacklogretained.

## Independent design review and adoption

Contract-auditor FULLread candidate df4942004ec749c6d1b12660cf2904f2efb150d351c36ded455747fb92afa04e.
Qualified KEEP for both A+B. No concrete domain/formula contradiction: certified
wrap two-visit law; Post release then emptyrequirements, fullSoundpolicy and
externaloccupancy preserved; nonshootingcallback and broadcopies/transitions
retained.92internal predicate finite units plausible from prescribed no-loop
shape, plus reachedstrings/caller payments. Added32selector blocks conservative;
ACTUAL425 must still inventory actual code. No fit/inheritedwhole-accounting claim.
Parent independently read actualallocator/phaseentry/sweep/technologycallbacks
and423 in full. Adopted exactly; sole424sim writer only source+handback.
Independent author coverage: singlePost actualRED410 is sufficient current
behavioral witness. Existingmultipicturecase coverswrap only, notPostexit.
Potentialfuture genuine scheduled(2)+threerealticks→twoPost2 thenoneexitstep
would exercise generalfallback; NOTauthored/authorized in424 and no current
coverageclaim. No genuineSound/externalPostexit replay control found. These
limits remain explicit, not fabricated fixtures or new blockers to settledcode.
