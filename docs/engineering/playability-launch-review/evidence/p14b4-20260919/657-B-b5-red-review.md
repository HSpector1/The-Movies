# 657-B — Contract-auditor review of the P14B.5 T1 RED (read-only; nothing ran; no edits)

Scope: HEAD `9e58ec9d` in `/Users/zacheryspector/The-Movies-headless-program`. Law read in full: plan `:725-779`, records 653 and 647-B, companion §5 `:393-489`, §2.1.7 `:102-120`, §8 `:639-646`, 600 §1 D2 / §4. RED read in full (three files), 654-T report and ledger, 655/656 record-checks (headers consistent: `sourceSha 8708d6a9`, `fixedSource: true`, exit 1 / exit 2, 37 failed / 35 passed, exactly the 8 TS errors). Source anchors re-read at HEAD (talentMarket, tick, promises, actions, save, types, worldgen, historical-control, bridge schema/people/runtime-checkpoint, load arms, p14b2-fixtures).

**Verdict: RELEASE WITH CONDITIONS.** No REFUSE item; no assertion decides OPEN 1–3/11/15; no frozen pin is weakened; every new-behaviour expectation traces to a plan/companion sentence, and every current-output value in the files is a labelled CANNOT-MOVE or R-VERSION control, not a law expectation. The conditions below are writer/sequencing disciplines, not RED changes.

## Q1 — Derivation walk (12 families)

Distinction applied: law-derived expectations vs measured controls. Measured values (rng strings, `postTakeDigestStripped`, weeks 61/64/65/213/217/222, the F6 decline, the four seed digests) are labelled CANNOT-MOVE / premise and are lawful pre-declared controls, not expectations of new behaviour.

- **Edge shape / top-level root** — `tests/p14b5-relationships.test.ts:88-96` mirrors plan (1) `:739-751` field for field; `:434-436` pins ordinal `relationship-edge-<i>`, canonical `a < b` (`:162`, §5.7 :486), seat order (`:169-175` = plan (2a)'s six pairs and proximity classes). New-edge value `BASELINE + weight` (`:194`) is the only reading of "drift materialized, then the delta applied" from the fixed baseline (plan (3), §5.1 :401-402). MET.
- **Single tail seam, delta-fed** — `:417-423` guard order copied from `promises.ts:125-127` (empty delta → same reference before the root guard; loud guard on the `requirePromiseRoots` model `:88-92`; hollywood null `:484-495`). `:454-458` "anything new came from THIS week's takes" = plan (2)/R3. MET.
- **Drivers minted only from shared takes / release / cancel, week = `state.market.tick`** — `:440-441`, `:683`, `:692`, `:745-748`; `driver.week === film.releaseTick + 1` matches `tick.ts:623/:642` (pre-increment) and R1. MET.
- **D1 roster predicate** — `:301-305` and `tests/bridge-p14b5-relationships.test.ts:145-149` are the plan (5) predicate verbatim, subject excluded, strict at both ends; family 6 pins each end separately (`:822-827` closed-at-W via `finishHollywoodWeek` `hollywoodTick.ts:335-341`; `:829-853` committed-at-W via a `signContract` on the captured pre-market week-208 input, `startWeek === W` as `talentMarket.ts:922` writes). MET.
- **Scale and drift-on-read** — `:383-402` (baseline 50 inside Acquaintances, ascending named floors, Nemeses 0, the ONE pinned relation `FAILURE > PROXIMITY_LOW`, §5.4 sign shape); independent oracle `:178-182` is plan (3)'s formula as written; `:560-593` monotone, never past baseline, tier never away from Acquaintances, read is pure. MET.
- **Fold-by-count** — `:602-619`: cap-full `recent`, oldest folds out, counters keep the count, peak untouched (equal tier is not "higher"). MET.
- **D5 band / order / strict-every-survivor rule** — `:799-800` (the D5 sentence is the ONLY reason when all other bands tie — `talentMarket.ts:880-884`), `:874-875` companion `:120` seven-member orders, bridge `:196-202` the R2 rule recomputed at W on every settlement. MET.
- **Release-failure driver reaching Strained / sign −1** — `:641-669` (low-proximity pairs net below baseline; a staged Acquaintances-floor pair reads Strained; `pairChemistry.sign −1`), plan (4)/647-B D2. MET.
- **Enemies/Nemeses/`enemies here`/`nemesisOnRoster` unreachable BY RULE** — `:635`, `:670/:673`, `:855-863` (Enemies-band value reads Strained → band `none`, decline by tie), `:906-912`, bridge `:184` (nine frozen drop templates only across 4 seeds × 416). The TS2322 pin (`:907`) is the right device: the union member is demanded at typecheck, and the sentence is demanded transitively because `DROP_SENTENCE: Record<FreezeDrop, …>` (`talentMarket.ts:1022`) is total — so `npm run typecheck` is the check that closes family 8's enumeration (vitest alone would pass it once the module exists). MET, with the caveat that the sentence's CLASS is unobservable by test (DROP_SENTENCE is private) — reviewer greps at T3.
- **`pairChemistry`, no consumer** — `:884-904` (null/0/[] without an edge; +1 Colleagues+, 0 Acquaintances (§5.3 :426), −1 Strained and below (§5.6 :476); symmetric; no digit). "No consumer" is a negative the RED cannot pin — T3 grep. MET (export), NOT VERIFIED (no consumer) by design.
- **Determinism** — `:448-450`, `:468-474`, `:750`, `:806`, `:899`; `talentMarket.ts` has no RNG symbol (grep), so the treated-vs-base rng equality at `:806` is satisfiable. MET.
- **Recording boundary** — `:943-946`, `:812`; `promises.ts:953-960` device. MET. **Century bound** — cap 8, fold-at-write, counters (`:386`, `:616-619`, bridge `:436`); edge counts left to T4 as the plan says. MET.
- **Validator refusals (family 10)** — `:916-965`: every plan (10) clause has a case; regexes are loose and match the `promises.ts:941-960` message model (`${at}.<key> is not a field of this record`, `must be a non-negative integer`, `outside this campaign's recording interval`). One-edge downgrade `:967-985` pins the plan's exact string and every older route (`save.ts:8176` class + the 4 route lines). MET.
- **Family 9** — `tests/p14b5-save-v31.test.ts:143-170` per-key byte identity, empty root, live routes agree, `makeSave` restamps, lossless downgrade to the raw bytes (`exportSave` is `stableStringify`, `save.ts:6417-6419`, so envelope key order is immaterial); V29/V28 corpora `:256-279`; checkpoint slots `:281-294`; V31 envelope boundary `:296-308`. V31 asserted RELATIVE to `LIVE_SAVE_VERSION`, never a literal. MET.
- **Families 11–12** — enum widened (order-free, `:326`), identity moves + `projection-v47` + 36 ids (`:329-336`), prior path through `migrateToV31` (`:338-358`, matches `runtime-checkpoint.ts:844-855` + `:1272-1282`), seven members with the `line` unchanged (`people.ts:991` reads [0]/[1]), D5 sentence verbatim (`people.ts:998`), leak law, generator artifacts pinned (the `--check` spawn is T3's). Ledger installed as family 12. MET.

**OPEN items** — none decided. OPEN 1: `driverGain` pass-through pinned (`:405-406`) is the plan's own reversible mechanism. OPEN 2: empty root + delta-only mint + I2 (below). OPEN 3: no guard asserted. OPEN 11: the bridge helper `d5Band` (`:153-160`) encodes close-ties-first — plan (5)'s literal wording — and is dead code in B.5 (Enemies/Nemeses never read); not a decision. OPEN 15: family 9's `promises` byte identity follows the plan's own non-bundling family-9 sentence; if Fable bundles, the test-author re-expresses with the ruling. **Frozen pins**: none weakened; the frozen-side describes in the RED files (`save-v31:175`, bridge `:282-320`) mirror existing pins and are labelled R-VERSION for the test-author's post-T2 sweep. **Synthetic state**: staged edges go through `live()` (validator-admitted, `:157-161`, `:207`); family 3's unvalidated `staged()` edges are inputs to pure readers; the clock-moved dormant world (`:606`) and the I3 FilmResult are reducer INPUTS, not claimed campaign state; the mid-tick captured state (`:833-844`) is reader-admitted by `advanceTalentMarketWeek` and sits in the synthetic file per B4 plan :391-393. All lawful.

## Q2 — Interpretations I1–I4

- **I1 — ACCEPT (binding on the writer).** Every name except three is the plan's own: `advanceRelationshipsWeek(state, { takes: [{studioId, production}], releases: FilmResult[] }, week)` (plan (2) — the same `entries` array `tick.ts:1104-1107` hands `appendFirstTakes`, plus this advance's `FilmResult`s from `records[].filmResult` / `industry.growth[].filmResult` `tick.ts:932`), `recordCancelledAfterFirstTake(state, playerStudioId, production)` (2c), `currentCloseness/currentTier(edge, week)` (3), `pairChemistry(state, a, b, week)` (7), `validateRelationshipsRoot(rawState)` and `projectRelationshipsPreV31(rawState)` (10), `driverGain(state, a, b, delta)` (OPEN 1), `requireRelationshipsRoot(state)` (2), the 13 constants at plan `:761`. The RED's three additions — `RELATIONSHIP_TIER_FLOOR` (record keyed by tier, Nemeses 0), `RELATIONSHIP_TIERS`, `RELATIONSHIP_DRIVER_KINDS` — are the smallest total shape the validator's "in the catalogue" clauses need. Ruling: no rename at T2 (a rename buys nothing and forces a test-author re-expression).
- **I2 — ACCEPT WITH PRECISION (wording amended; no RED change).** A release (and a cancel) never CREATES an edge; its driver applies to a pair's edge iff the edge exists AND the release's take was counted by that edge, i.e. `take.week >= edge.firstSharedWeek`. This is forced by the RED's own invariant `sharedSuccesses + sharedFailures <= sharedProductions` (`:729`, validator (10)): a per-pair "edge exists" rule alone would break it when a pre-boundary take's film releases after a later live take mints the same pair. It is the live-seam consequence of §5.7 :489 / §8 :643, not a decision of OPEN 2 (which is migration-time reconstruction); a later labelled reconstruction step that mints edges for old takes is admitted by the same rule unchanged. The RED's chain never reaches the corner (all pre-migration films release before any edge exists), so its assertions are consistent with both readings; the writer takes the invariant-preserving one.
- **I3 — ACCEPT.** The flop is a labelled in-memory `FilmResult` variant fed to a seam that, by the plan's own R3, MUST take releases as an argument; nothing is claimed as a campaign release, no state is hand-written, and the natural branch is also asserted (`:679-704`, `:672-673`). Lawful synthetic-file construction under B4 plan :391-393. Success basis stays `criticScore` (OPEN 9 ruling).
- **I4 — ACCEPT.** The eight frozen sentences are exactly `talentMarket.ts:672-677` (six), `:881` ("theirs was the only proposal on the table"), `:884` ("their proposal ranked above the others overall"); the tie sentence `:1146` is handled separately; drop sentences never enter a settled receipt's `reasons`. The D5 sentence is one new `DESCRIPTOR_REASON.relationships` entry. Binding: exactly one new entry, no existing reason/drop/tie string changes, no digit/person/tier in it.

## Q3 — The ledger

Sufficient as the R-D5 pre-declaration: named class (§A), receipt facts a lawful movement must show (§A R2, installed at bridge `:196-202`), per-seed rows with survivors/rosters/shared-take counterparts (§F), settlement/receipts/employment/takes digests + `rngState(416)` per seed, `poachingFixture` (§D; `p14b2-fixtures.ts:200-201/:204` re-verified) and the D3 case, plus the F6 base (§E). "404": record-only plan-text correction — cases OPEN at 404 (renewal window 12 weeks before 416) and SETTLE at 416; the D1 predicate is evaluated at the settlement week, so no prediction changes. "Exposed rows 0" is consistent with the off-cycle CLASS claim: the class exists (r01's `*-supply-265-6..9` replacement rows are active under the predicate at 416) but those people never sit in a take, so nothing is exposed on plain campaigns. Consequence: on the four seeds NOTHING may move at T2 except R-ORDER-VALUE and R-VERSION pins (see the T3 rule below). Gaps: player-acted fixtures are pre-declared by rule, not measured (stated); the F6 probe output is not among the archived logs (`654-T-probe-logs/` holds controls/red/typecheck only).

## Q4 — Writer plan (ONE sim-core writer; cumulative patch per step; no commits; no test files)

Line numbers verified at HEAD.

- **S1 core** — NEW `src/core/relationships.ts` (constants, catalogues, `advanceRelationshipsWeek` with the copied guard order, `recordCancelledAfterFirstTake`, `currentCloseness/currentTier`, `pairChemistry`, `driverGain`, `requireRelationshipsRoot`, `validateRelationshipsRoot` on the `promises.ts:927-1004` model, `projectRelationshipsPreV31` on `promises.ts:1142-1157`); `src/core/types.ts` (`RelationshipEdge/Driver/Tier`, `GameStateV31 = GameStateV30 & {relationships}`, `GameState = GameStateV31` at `:2234`); `src/core/tick.ts:1102-1110` (one call between `appendFirstTakes` and `advancePromisesWeek`, fed `[...productionAdvance.firstTakes…, ...industry.firstTakes]` and this advance's release results); `src/core/actions.ts:596-599` (one call beside `breakPromisesOnCancel`). Typecheck-forced into S1 by the `GameState` widening: `src/core/worldgen.ts:783-785` (`relationships: []`) and `src/harness/roster-wall/historical-control.ts` at `:23`, `:25-26`, `:30`, `:45-46` (four touch points, not just `:23-26`). No cycle: `relationships.ts` never imports `talentMarket.ts`.
- **S2 chooser** — `src/core/talentMarket.ts:668` (`DESCRIPTOR_ORDER` 6 → 7, `relationships` between `trust` and `standing`), `:671-678` (+1 reason, typecheck-forced), `:698-702` (both seven-member orders; NOT typecheck-forced — family 7 pins it), `:779-816` (`bandsFor` gains D5 through the shared roster-at-W helper housed here), `:999-1010` (`FreezeDrop` + `nemesisOnRoster`), `:1022-1032` (sentence, typecheck-forced), `:1067` (check after `issuerDistrusted`, before `:1068`), comment `:659-662` names B.5.
- **S3 save** — `src/core/save.ts`: types after `:499-504` + union `:537`; dispatch `:5282-5285` (+31, sentinel "1 through 31 only"); `LIVE_SAVE_VERSION`/`makeSave` `:6396-6405`; the 18 throws (`:7317…:8176`, `:8737`) each gain a 31 line; route lines `:8340/:8491/:8572/:8670`; `migrateToV30` `:8715` gains the 31 arm; V31 block after `:8717` (`validateSaveV31` strip-and-delegate on `:8583-8598`/`:8679-8693`; `convertV30ToV31` empty root, nothing recomputed; `convertV31ToV30` asks `projectRelationshipsPreV31` BEFORE validating, message "cannot downgrade SaveFileV31 or discard the relationship record"; `migrateToV31`); `src/core/index.ts` after `:1296` and a relationships block on `:1394-1427`; `src/harness/d16/run-d17b-continuation.ts:37/:183`, `run-d17b-week86.ts:35/:124`; `src/harness/p14/legacy-v28-fixtures.ts` (V28 minter — typecheck decides whether an arm is needed).
- **S4 load/runtime** — `bridge/session.ts:41/:135-136`, `bridge/runtime-checkpoint.ts:6/:848-854` (strict V31 in `importPriorSaveViaCanonicalChain`), `bridge/runtime/campaign-library.ts:5/:48/:127`, `ui/src/engine/adapter.ts:111-112/:3795-3796/:3808/:3822` → `migrateToV31` (typecheck-forced by the `GameState` move).
- **S5 wire** — `bridge/schema/bridge-schema.ts:233` (48) and `:2320-2324` (+`relationships`), `bridge/people.ts:866-875` (+label; `:977/:991` untouched), `bridge/runtime-checkpoint.ts:59-63` (+`projection-v47` line for `sha256:6f6b4880…`), generator once over its three artifacts.
- **Frozen**: everything in plan (12): the promises law/evaluator/`PROMISE_RULES_VERSION 4`/trust derivation/root validator, `promiseCapacity*` + replay module, `decide()/staff()`/seating `:164-170`, `finishHollywoodWeek` and the settlement order `:1245-1248`, `authorRivalPromise`, cap/metric, `TUNING`, every `DROP_SENTENCE` string, every V≤30 validator, every test and fixture, the cancel proof (`applyCancel` gains one call only).
- **Self-check per step (honest)**: after S1/S2 the controls CANNOT be green — `makeSave` refuses a state carrying `relationships` until the V31 validator strips it (`validateSaveV28 → … v12ExactKeys(state, V14_STATE_KEYS)` `save.ts:4803`), and the engine RED binds `migrateToV31` to `undefined` — so S1/S2 are typecheck-only (expect: TS2307 and TS2322 gone after S1/S2, the six TS2724 remain until S3). First control checkpoint after S3: engine file 46/46; save-v31 56/57 (RED only `:175` `LIVE_SAVE_VERSION toBe(30)`, sweep class); bridge 6/15 (GREEN: `:292`, `:300`, `:371`, four seed ledgers; RED: `:282`, `:313` (7 members vs 6-enum), the six family-11 cases minus `:371`, and the `poachingFixture` control — RED for the SWEEP reason, `tests/helpers/p14b2-fixtures.ts:210` `validateSaveV30(makeSave(…))`); `p14b1-promises` 15/15 and outcomes 23/23 from S3 on. After S5: bridge 12/15 (RED: `:282`, `:292`, poaching-helper) plus the existing `runtime47:162-170` pins RED — all sweep class. After the test-author's sweep: 15/15, and the frozen suites re-expressed.
- **Sweep split**: writer — `src/`, `bridge/`, `generated/`, `ui/src/engine/adapter.ts` values; test-author (AFTER the production patch, before the T3 control run) — the three RED files' frozen describes, `tests/helpers/p14b2-fixtures.ts:122/:210/:225`, `tests/contracts/_v14Contract.ts:411-417`, `p14a1-f1-priority-order:86,91`, `p14b1-trust-chooser:172-173`, `bridge-p14b4-cast-class:410-411`, `p14b4-cast-class-policy:30-31`, `bridge-p14b4-runtime47-compatibility:39-75/:162-170`, `bridge-contract-generator:562-565`, `p14b1-save-v29:196-201`, `bridge-p14b2-checkpoint:44`, the eight UI pins.
- **Anything the writer cannot satisfy lawfully?** None. Two disciplines: family 1's stripped digest (`:470`, plain `JSON.stringify` order-sensitive) holds only if V31 states are built by spread (never re-listed keys) in `convertV30ToV31`, the tail write and `convertV31ToV30`; and the `poachingFixture` control is unreachable for the writer until the helper sweep (sequencing, not law).

## Q5 — Verdict and lists

**RELEASE WITH CONDITIONS** (all checkable at T3):
1. I1 names binding; no rename.
2. Release/cancel drivers apply only to an existing edge with `take.week >= edge.firstSharedWeek`; never create an edge (I2 precision); `:729` and the validator counter law are the checks.
3. Spread-preserving state construction (key order) so `:470` and family 9 per-key bytes hold; a digest moved by reordering is a writer defect.
4. Validator messages carry the field path on the `promises.ts:941-960` model; `validateSaveV31` wraps without dropping the inner text.
5. Exactly one new `DESCRIPTOR_REASON`; no other reason/drop/tie string changes; D5 and `nemesisOnRoster` sentences carry no digit, person id or tier name.
6. Same-reference returns for the empty-delta early return and the no-take cancel (`:417`, `:756`).
7. Roster helper in `talentMarket.ts`, set-equal to the RED's `rosterAt` (`:301-305`); no `relationships.ts → talentMarket.ts` import.
8. Worldgen seed + historical-control strip land with S1 (typecheck-forced); verification checkpoints at S3/S4/S5 with the expected counts above.
9. Test-author's sweep patch lands before the T3 control run; T3 separates sweep-class failures (`expected version 30`, `toBe(30/47)`, six-member arrays, 35-id count, `current schema must not migrate`) from chain movements by error text before attribution.
10. Typecheck (root + bridge + ui) is part of T3; a residual TS2322 on `FreezeDrop` means family 8 is not satisfied.

**DEMONSTRATED defects in the RED**: none.

**REFINE (test-author, later, ruling recorded; not blocking)**: (a) bridge `:397` `String(closeness) + '"'` is a substring check that any DTO string ending in the floor value (e.g. `…:film:71"`) can trip for a non-law reason — replace with the key-based check already at `:396` or drop; (b) bridge `:436` literal `8` could read the cap through the dynamic import (cosmetic); (c) plan text "404" → "open at 404, settle at 416".

**RECORD-ONLY**: OPEN 11 dead branch in `d5Band`; OPEN 15 stays Fable's call (family 9 re-expressed only if bundled); the `nemesisOnRoster` sentence class and "`pairChemistry` has no consumer" are T3 greps; the pre-boundary-take cancel corner is unexercised (condition 2 covers it); `historical-control.ts` has four touch points; `legacy-v28-fixtures.ts` is a V28 minter; the F6 probe output should be archived beside the other logs if the parent wants it on record.

## Rulings for the parent
- I1 ACCEPT (no rename). I2 ACCEPT with the `take.week >= firstSharedWeek` precision (does not touch OPEN 2). I3 ACCEPT. I4 ACCEPT (eight sentences listed above).
- Writer steps and edit points: as in Q4, with the S1 amendment (worldgen + historical-control) and the S3-first verification checkpoint.
- Conditions 1–10 above.
- **T3 attribution rule**: on `p13a-core-causal-01`, `seed-b`, `p13b-s8-bridge-probe-01`, `p13-public-commercial-adoption` (416 ticks), `poachingFixture` and the D3 case (`p14b1-trust-chooser:305`), the settlement/receipts/employment/takes digests, `rngState`, every receipt kind/winner/reasons/dropped, and every FirstTake are CANNOT-MOVE; the only lawful movements anywhere in the control inventory are R-ORDER-VALUE (seven-member public order) and R-VERSION (30→31, 47→48, schema id, 35→36, sentinel, frozen-builder/helper validator calls); any other movement is a writer defect attributed to the first moved receipt, never re-pinned.

## Sources read
Plan `:725-779`; records 653, 647-B; 654-T report + ledger; 655/656 record-checks (headers, tallies, failure lines); companion `:102-120, :393-489, :639-646`; 600 `:25-36, :128-154`; RED: `tests/p14b5-relationships.test.ts` (986), `tests/p14b5-save-v31.test.ts` (308), `tests/bridge-p14b5-relationships.test.ts` (458); source: `talentMarket.ts:636-735, 740-885, 995-1289`, `tick.ts:520-531, 618-647, 920-934, 1085-1111`, `promises.ts:84-149, 780-839, 941-1004, 1138-1157`, `actions.ts:560-600`, `save.ts:495-537, 539-568, 4015-4023, 5248-5286, 6390-6433, 8506-8520, 8578-8727` + greps (24 `saveVersion === 30` sites), `types.ts:2115-2142, 2200-2234`, `worldgen.ts:778-787`, `historical-control.ts:15-54`, `hollywoodTick.ts:326-345`, `index.ts:1288-1299, 1392-1427`, `bridge-schema.ts:233, 2316-2327`, `people.ts:150-161, 668-681, 864-877, 974-1001`, `runtime-checkpoint.ts:20-74, 820-855, 1246-1288`, load arms (grep), `p14b2-fixtures.ts:10-39, 140-219`, `src/harness/p13a/fixtures.ts:1-25`, `legacy-v28-fixtures.ts` (grep), six-member-literal grep, `validateSaveV30(` count grep.

## Evidence limits
Paper only; nothing ran. I could not hash the committed RED files against the 654-T sha256s or the 655/656 `testedDiffSha256`; the parent's commit is taken as staging the tested bytes. The F6 base premise (decline by tie; rosters at 208), the family-1/2/5 chain weeks and the "no rival take at 61" premise behind the six-edge pin are the test-author's measurements (ledger §C/§E), not re-measured here; the F6 probe log is not archived. Whether any `film:<floor>`-style id makes REFINE (a) trip is unmeasured. The `vi.spyOn` capture on the ESM namespace is accepted as measured to work. Nothing here is Owner acceptance; the writer is released only by the parent.