# 918 — attributed legacy test maintenance, frozen for review

2026-09-26. Test-author handback under917, after916 closed with
fixedSource:true on published `17e65c36ddcb2d67f174bb413ec927245db1021a`.
916 observed exactly47 selected cases:45FAIL /2PASS,405unselected.
This patch changes only the32 test files enumerated by914.

## Original frozen diff identity (before920)

SHA-256 of `git diff --binary 17e65c36ddcb2d67f174bb413ec927245db1021a -- <914.files>`:

`8b4036ebeb4687777c4b7addcaa7fb812981c773648b80ee2c4efde2507ee7d7`

The32-file patch has113 insertions and98 deletions. This evidence document is
outside that source-patch digest. No production source is part of this patch.

## Changes and authority

- Current projection, schema metadata, response snapshotVersion and the expected
  current-literal decoder diagnostic now pin52. Save37/protocol4 stay exact.
- The generator's current projection argument and the current schema-id assertions
  use52 / `sha256:f036ccdd62c4ac2a700a27796631e1c4f8c85f9cccfb14ac6850083fb8dba5f2`,
  independently inspected in914.
- Current aggregate prior-roster expectations append exactly the genuine outgoing51
  `sha256:a690e6f9e6f93f3a78f8eed8eaa20a1532a9ebd82812b0bc9414a04fdcb5968f`;
  current count assertions become40. Added mapping checks require projection-v51.
  Existing literal predecessor arrays, including EXPECTED_35_PRIOR_IDS,
  EXPECTED_36_PRIOR_IDS and the earlier EXPECTED_PRIOR_IDS list, are unchanged.
- B2's masked exact Market attention enum gains only875's three named causes:
  retirementAnnounced, finishingCommitments and retirementExtensionOpen.
  Its intent vocabulary and B6's distinct seven-member preference priorityOrder
  remain unchanged.
- Four W10 exclusions become affirmative875 assertions using the same genuine
  axis and natural advance to92: exactly one list row for authored-0000, exact
  sole issuer studio-d7df6c8e-player, the fixed58-week opportunity98→156, one own
  extension-decision attention row with no competing-proposal alert, identical
  selected Market/profile case, and the exact open World route plus final-extension
  wording. The existing no-promise and one-final-extension-refusal controls are
  unchanged. No fabricated work, case or receipt is introduced.

The selected declarations and scenario setup remain in place. Numeric title
maintenance is accepted by914's existing selector. W10 retains its suite prefix
and six declarations; the four superseded exclusion titles now describe their
affirmative requirements. Other historical scenario titles remain unchanged.

## Checks actually performed and remaining work

Read the complete source diff and ran `git diff --check`: PASS.
A static source-title scan using the existing914 global RegExp still selects
47 declarations across32 files. This is a static check, not a Vitest run or
collection. No tests, typechecks, generators, gameplay probes or native work were
executed by the specialist.

No historical fixture/hash/source identity, saved/current slot, journal,
Save37 migration chain, timeout, setup budget, skip/todo/only marker or additional
test declaration was changed. All owned source is now frozen for independent
review, the parent's exact selected GREEN, appropriate types and the required
matched full qualification. Any new failure requires fresh attribution.

## 920–922 attribution and final frozen diff

Parent reports the entire series CLOSED/fixedSource:true:920 selected GREEN
observed46PASS/1FAIL/405unselected;921 root/UI types and922 bridge types PASS.
The sole920 failure is declaration order among the three added attention causes.
All ten expected members are present, with no extra or missing member. Schema
enum ordering is not the separately tested gameplay attention-row ordering.

Under explicit release, only the B2 schema-enum comparison now sorts a copied
actual enum and the literal exact ten-member expected array. Exact array equality
continues to reject duplicates, omissions and additions. The original schema,
gameplay row-order assertions, preference priorityOrder, intent vocabulary and
selected declarations are unchanged. No production change is warranted.

Final SHA-256 of the same32-file binary diff against
`17e65c36ddcb2d67f174bb413ec927245db1021a`:

`2de90bb385dcf1ad82e8e08784e1523ae84845eca6d63fbafc2efc2813b782c4`

The original918 patch/log identities remain historical evidence; this digest
identifies the revised handback. `git diff --check` passed. No test, typecheck or
other execution was performed by the specialist. All owned files are frozen
again for the parent's selected rerun and independent review.
