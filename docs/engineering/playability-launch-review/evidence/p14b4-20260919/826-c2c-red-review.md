# 826 — C.2c independent RED verified; production released

Run `825-c2c-red` on `8f893e0e9fd8623e79bfc30d673f0e0aaf502fab`:
2026-09-26T11:34:06.813Z–11:34:22.258Z, test exit1, `fixedSource:true`.
Tested diff `b6241e3380a63fa15d28a3f5ecff4d2e7911ba54294889987cea6365169a4dbf`,
no untracked source. New files were indexed with intent-to-add before recording,
so the recorded patch contains their bytes, not merely an untracked filename list.
No production source differs from the recovery SHA in this run.

Measured **19 cases: 9 failed, 10 passed** (17 core cases and two bridge cases).
The test author's preliminary hand-back said 20; this executed count controls.
Every failure is an expected missing outcome or missing retirement feasibility:
C2 cutoff; C5 nonqualifying seat; C7 waiver substitute; C11 cap/digest;
C4 partial work; C9 save/replay; C8 extension terminal preservation; two C10
private-disclosure tests. None failed on imports, fixture validation or command
construction. Their post-outcome assertions remain unverified until GREEN.

The passing controls executed the real natural renewal/birthday route and actual
production commands. In particular, removing only this person's retirement
restriction permits greenlight148 → first take153 before due156. The existing
committed lead seat greenlit at147 films at152 and satisfies. Announcement alone,
admission equality, existing generic-P1 support, ordinary impossibility, prior
termination, unbound withdrawal and existing-seat feasibility controls also pass.
These are observed runs, not reconstructed call-order arithmetic.

Parent released the sole sim-core writer for corrected823's admission-cutoff rule:
`src/core/promises.ts`, the narrow order seam in `src/core/tick.ts`, and the private
VOIDED word in `bridge/trust.ts`. No tests/fixtures may be changed by that writer.
No test process is active after825. Additional rival symmetry tests are separately
owned by test-author; a natural open promise148 exists in the genuine V35 rival
corpus, unlike its older already-terminal promise. No terminal row may be reopened
to fabricate the fixture. Stable-source review and GREEN/full verification follow.

Published recovery `8f893e0e9fd8623e79bfc30d673f0e0aaf502fab` was verified by actual
`git ls-remote` and local `git rev-parse HEAD`. It preserves the map and genuine
outgoing runtime50 fixture, not a completed C.2c implementation. The prior full
baseline remains822's 55 qualified failures. Unity/native unverified.
