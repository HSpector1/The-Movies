# 320 — Next bounded replay-cost correction

2026-09-20. Native contract-auditor: qualified KEEP for these directions, with
local accounting corrections. Parent adopts the smallest sequence C and B first;
A remains a subsequent option if actual evidence requires it. Design guidance,
not a production-write release or numerical-fit claim.

Reviewed ONLY immutable e34af4777a12607d90435048e2703297403eecaf and176/240.
No mutable301 source inspection, edits or runtime.307 established the actual
long-route cut before director assignment: used199267, requested1659. Its two
early phase sweeps now execute, but take/wrap/output/kernel remain unreached.

## C — Omit unused calculator work

arrivalBill eagerly computes geometryBill and workflowUpdate even when the
actual possible-workflow count is zero. A paid guard can skip those calculations
while returning the SAME owner reservation. Keep the real arrival invocation,
all workflow guards and all nonzero-case geometry/copy charges. The reduction is
in calculator operations actually executed, not a discount to owner work.

## B — Prove the unsuffixed identity is free

Only AFTER the fully prepaid ACTUAL persistedProductionIds(source), separately
prepay base-string construction and taken.has(base) under the existing metric.
If absent, reserve the unchanged allocator's actual base construction and lookup
AGAIN before calling allocateProductionId. The proof lookup does not pay for the
owner lookup. If occupied, retain existing conservative collision reservation
and actual allocator. Never return a guessed candidate or omit identity roots.
Historical/cancelled identity controls and actual allocator remain unchanged.

### B addendum — paid pre-insert prefixes for the actual union owner

Independent immutable-source review KEEP. The actual owner starts an empty Set
and performs one add per non-null identity occurrence. Before zero-based
occurrence i, at most i keys exist; duplicates only reduce the count. For N
insertion OCCURRENCES and maximum ID width W, replace the repeated full-final-
count reservation with this same-metric upper bound:

`N * (9 + W) + [N * (N - 1) / 2] * E(W)`, where `E(W) = 1 + 2*W`.

Retain full identity-root walk,40*visits+64, occurrence/width discovery and all
calculator payments. Never substitute final distinct Set size for N. Handle
N<=1 with zero pairs; halve the even factor BEFORE paid saturating multiplication.
Saturating N*(N-1) then dividing can UNDERestimate and is prohibited. Prepay all
argument evaluation, branch arithmetic and subsequent saturated products/sums.
No counterexample found in genuine-source domain. No owner/history/cache change,
runtime or edits by reviewer; parent adopts for the later B+C implementation.

## Required local payment corrections

- arrivalBill pay5 per row is insufficient for separate phase/status/blocker
  string comparisons; preserve short circuits with explicitly paid comparisons.
- Wrap-only Post counters' pay4/pay3 do not cover capability comparisons. The
  nested workflow loop also requires an outer-visit payment for empty lists.
- If A later refactors dimensions, independently pay method-versus-silent;
  earlier discovery of the string does not pay a later comparison.

Later owner reservations never retroactively pay calculator work. Existing
290's new early-transition comparison fix remains in301, not waived here.

## A — Later option: explicit invocation-local immutable facts

Prefer a lazily initialized static-fact record at the FIRST required dimensions
calculation, not a general/global/reference cache. Eligible facts: unchanged
facility capacity/strings, placement/property geometry, adoption/access/equipment.

- Fully pay discovery/construction once and every subsequent access/composition.
- No global/module/cross-source reuse, counter reset or refund.
- Keep changing productions/workflows/reservations/tasks/bindings/setup/Sets and
  technology-production rows current. Refresh root-copy footprints as needed.
- Recompute current n, external occupancy, technology count and allSilent.
- Genre dimensions follow actual prepared-picture collection, including Ready
  additions, not merely current active productions.
- Conservatively combine static/dynamic maxima; preserve arbitrary admitted
  own-string-key production extensions and opaque referenced values.
- Do not move discovery before existing admission refusals or zero-work return;
  do not rebuild prepared indexes each frame.
- Actual owner traversal/callback/copy reservations are unchanged.

After301 freezes and actual reviewed verification is preserved/published, next
sole sim-core release may implement C+B and local payments. Existing26+Ready14,
new employment5 and reviewed stale-Ready regression stay independent and fixed.
No test/cap/timeout/validator/owner-policy changes. Actual checks decide whether
the complete route plus shared kernel fits200000; this document cannot do so.
