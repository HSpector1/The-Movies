# 944 — bounded genuine outgoing Save37 C.3 T0 producer

2026-09-26. **Prepared only; not executed.** Exact allowed HEAD is
`1f44aa505c0d677430451ab5fcacaf5e0ce205d6`; production/tests/fixtures must remain
byte-identical to qualified C.2-RM `9afae8874486fbb20dc5d373526698aacbec2114`.
Only this report and `944-c3-t0-producer.ts` were authored. Parent owns review,
execution and any new-fixture publication. No fixture directory exists as a result
of this authoring task.

Frozen producer SHA256 (file inspection only, not execution):
`133507f0628cc8e4662b35dcfa1a890236f520346bde3f1acf692c85898b8156`.

941 found no existing actor with both target capability and the required recorded
work. This producer keeps937's prior hypotheses intact. It uses the existing public
**Full Custom** creator to author two age68 actors with acting skills75 and their
respective target skills80; the other target stays20. These are six underlying
skills per discipline, not a direct OVR write. All creator input is recorded.
Ceilings/experience/work history are not supplied: the public creator supplies its
ordinary defaults, including zero work history. Later skills, age, history and
receipts are never edited. This tests a lawful publicly authored career; it does
not establish natural worldgen frequency or represent a previously existing star.

Four distinct support people use the same public creator/signing route: director,
writer, craft lead and third actor. Everyone receives an actual208-week contract.
The two focus actors share three real films: the directing candidate is the lead,
the writing candidate the antagonist. One real director repeatedly works with the
lead; one real writer appears in each released picture's credits with both actors.
M16 still has six distinct people per film. No actor is simultaneously the writer
or director of their own picture to manufacture target work-history proof.

## Bounded phases and failure behavior

1. Build one `p13aGeneratedStudio` world and disclose its established economic
   bootstrap: cash30,000,000 with an equal signed ledger entry. The builder's
   existing economy/operations initialization is named in the manifest. No earned
   revenue, employment, credit or retirement fact is fabricated by this bootstrap.
2. Author/sign the six people at0, assert no created work history and each actual
   end208. Validate/capture the created world. Confirm the target capability facts
   and the opposite target's lack of capability through `careerIdentity`.
3. Use the existing natural C.2c stage/set route: Soundstage07, grand-ballroom set,
   actual construction ticks. Greenlight three unused real concepts sequentially;
   assign the shooting director, schedule ready takes, commit ready releases and
   tick. Each film has a40-tick limit; all must release before104. Missing concept,
   stage, legal action, receipt or timing premise throws. No bypass, extra time,
   forged take or threshold reduction occurs after a failed premise.
4. Independently check three actual acting first takes per actor, three released
   acting credits, repeated director–lead work and three real writer/actor credit
   joins. Capture the completed-work world. Advance normally to103/104 and assert
   actual hard70 announcements at104 with E208 from the real contracts.
5. Advance to207/208/209. Assert actual no-offer final-extension opportunity,
   unchanged E, natural expiry, retirement208, preserved provenance and capable
   target facts. No proposal or extension acceptance is forced. Save37 remains
   current; these bytes contain acting alumni, not C.3 transitions.
6. Open the genuine207 save in `BridgeSession`, submit the published advance intent
   and prove current208 equals the continuous branch, saved207 remains distinct,
   and one actual journal entry survives current52 restart and exact duplicate
   replay. No journal, revision, session response or digest is fabricated.

Continuous advance is bounded to209 ticks; the runtime branch separately performs
one real public advance command. No cohort continuation, search over seeds or long
campaign stress run is hidden in this first producer. Progress lines identify each
captured phase/release without dumping raw saves.

## Artifact and source discipline

All eight payloads are prepared and fully validated in memory first: created0,
three-real-releases, preannouncement103, announced104, preretirement207, retired208,
postretirement209, and runtime52 current208/saved207. Each save must pass the actual
Save37 writer/reader and canonical round trip. The runtime must pass its codec,
current-schema loader and replay control. Compression is independently decompressed
before any write; raw/gzip hashes and byte counts come only from actual produced
bytes, never a predicted expected value.

The producer pins exact HEAD and current Save37/projection52/protocol4 identity,
requires initially clean consumed source, records its own hash and the consumed
diff hash, and refuses drift before writes. Default mode prints the prepared
manifest without writing files. The parent may select `--write` to create only
the new directory `tests/fixtures/p14/genuine-v37-c3-corpus` after every preceding
check succeeds. Existing output causes refusal; each file uses `wx`. No old fixture
is overwritten. The final source check permits exactly the declared new artifact
files and manifest as untracked output, while all existing consumed bytes and HEAD
must stay unchanged. An interrupted/partial write remains recoverable evidence;
the producer contains no cleanup or overwrite path.

The manifest records source/producer, complete public authoring/action inputs,
funding disclosure, actual dates/ids/public target facts, phase bounds, real runtime
command and current/saved/journal digests. It does not claim that a future chooser
accepted either target; that requires the independently tested C.3 rule.

Parent execution, from the existing repository root:

```sh
node_modules/.bin/vite-node docs/engineering/playability-launch-review/evidence/p14b4-20260919/944-c3-t0-producer.ts --write
```

Omit `--write` for a preparation-only run. Either mode performs real simulation
and therefore belongs solely to the parent's one heavy-process lane. No mode was
run by this specialist.

## Separately bounded next phase

941's genuine `person-cohort-832-actor-0` in the preserved V35 world at2600 has
directing69 and age57 but no acting work. A later producer can lift that exact
world, hire through ordinary free agency, create real acting work and preserve
every old cohort/credit/retirement byte before advancing toward natural retirement
within a separately reviewed approximately600-week limit. Employment/age/retirement
premises must be measured first; no current role or old cohort receipt may be
edited. That phase is deliberately absent here. Starting-rival authored credits
remain a strict historical validation control until a real positive witness exists.
