# 846 — Scientist runtime RED; independent acceptance review

845 closed2026-09-26T13:59:39.548Z, started13:59:32.225Z. Isolated source
8f893e0e9fd8623e79bfc30d673f0e0aaf502fab; fixedSource:true; tested patch
7be88573a0c5d2e790779efb8a032647429152641d8c2e8ba4987e44708087a4;
untrackedSource:[] at both ends. Ten cases:6 expected failures,4 controls pass.
Raw JSON/PATCH/TXT copied exactly to this evidence directory, not reconstructed.

Both genuine824 and842 artifact/manifests and independently preserved current/saved
slots pass. Each corrupted inner-slot age/provenance control refuses. Six failures
are attributed to the missing approved boundary: literal projection50 instead of51;
the two old50 checkpoints are still current, so no governed migration; strict
current decoding still accepts old50; real LOAD case first observes no migration;
and the reused-session factory is not invoked because old50 is still current.
The last case's preceding unknown-schema and damaged-digest assertions pass.
No missing import/API, malformed fixture or timeout causes a failure.

Four new Scientist test/helper files were copied exactly from the independent
baseline into main and made recorder-visible. Production writer released only
after844 and845 were read and attributed. Scope remains840: Scientist62/72,
Save37 semantic boundary/frozen34–36 readers, projection51 registering exact50,
bounded default recruitment and private Laboratory exclusions. No C.2-RM fields.

Independent contract-auditor re-read the acceptance tests: KEEP for genuine RED
and production release. No fabricated history found. Explicit V33 lift, labelled
age/cache variants and actual film/research/employment actions are valid. Both
runtime50 journals are genuinely EMPTY; no claim of historical nonempty-response
replay is made. S11 separately tests a NEW-schema LOAD receipt after migration.

Two acceptance refinements are assigned to the same independent test-author:

1. S6 must assert the existing post-settlement research pause, verify an uncommanded
   tick adds no work/charge, then explicitly resume. Its conditional resume could
   otherwise accept an invented automatic-resume policy. Existing next-week
   employment check runs before extension settlement (technology535/555).
2. S6 must compare full canonical bytes of tick(saveRoundTrip(before)) with the
   continuous extension decision result. The previous replay matrix omitted an
   already-offered extension at its decision boundary.

Apply these only to S6 in baseline/main; preserve all earlier evidence. Parent847
will record the refined S6 against old production. One production writer, separate
tests/review, max two active specialists, no nested workers; no heavy run currently
active after845. No commits while a recorded run is active.

Exact restart: main HEAD/last verified actual remote0058a42b0bc096c30882f1d588656271f12e04b1.
Sim-core is implementing840, test-author owns the two narrow S6 refinements; auditor
has yielded. After source freeze: recorded focused Scientist GREEN, strict schema/
type/generated checks, unchanged older tests and attribution before any expectation
repair, supply measurements, matched full run, publication and actual remote check.
C.2c remains qualified837 with54 inherited failures, not all-green. Then C.2-RM821.
