# 795 — P14C.4 T1: independent RED (test-author)

Task: P14C.4 T1, deterministic replenishment (annual cohorts of young entrants replacing
retirements). Role: test-author, running as a general-purpose agent carrying that contract
(`.claude/agents/test-author.md`) — the project's roles are not registered in this session.
Authority: `782-c4-replenishment-expansion.md` (§3 R1-R9 as amended by §6-§8, §7.1 REPLACING
R3) and `793-c4-api-contract.md` (names, shapes, step order, Save V35, §8's T0 facts).

Worked EXCLUSIVELY in the isolated detached worktree
`/Users/zacheryspector/The-Movies-c4-red-scaffold`, HEAD `bd27de939f8856761367f895e3b30f640b0b8abf`
(confirmed clean before and after). Never opened, read, ran or diffed anything under
`/Users/zacheryspector/The-Movies-headless-program` (the concurrent writer's worktree). Git:
read-only (`git status --short`, `git rev-parse HEAD`) only — no add/commit/stash/checkout/reset.
Times below are from `date` (host clock, CEST), run 2026-09-25 22:44–23:13.

**Status: DONE.** Every requirement ID (A1-A3, B1-B6, C1-C6, D1-D5, E1) is mapped to at least
one case below. Two cases legitimately PASS against the scaffold (justified, disclosed in
§4, matching the C.2a RED's own precedent for a P1-style non-discriminating premise); every
other case FAILS for missing behaviour, probed and attributed in §3.

## 1. Files, sha256, line counts

| file | sha256 | lines |
| --- | --- | --- |
| `tests/p14c4-cohorts.test.ts` | `c10c1fd04f503934d31aadd1aa18727d3fb6bf929fd8fbf7852d065d313dafea` | 291 |
| `tests/p14c4-save-v35.test.ts` | `97f4ec778544eebdca690d6c1595667bb2a85bc7e506fcae10aa38598c096320` | 203 |
| `tests/helpers/p14c4-fixtures.ts` | `edb3195b47f014c871c616655bc23b85d2c8c04688b5ce91c9f95d196748a6ff` | 193 |

Plus this record. `git status --short` at close shows exactly these three new paths (filenames
only checked, no diff read) — no existing test, helper, fixture or production file was touched.

## 2. Requirement map → case

| id | file | case(s) |
| --- | --- | --- |
| A1 | cohorts | `A1` |
| A2 | cohorts | `A2` (JUSTIFIED PASS — see §4) |
| A3 | cohorts | `A3` |
| B1 | cohorts | `B1` |
| B2 | cohorts | `B2` |
| B3 | cohorts | `B3` |
| B4 | cohorts | `B4` |
| B5 | cohorts | `B5` |
| B6 | cohorts | `B6` |
| C1 | cohorts | `C1` |
| C2 | cohorts | `C2` |
| C3 | cohorts | `C3` |
| C4 | cohorts | `C4` |
| C5 | cohorts | `C5` (JUSTIFIED PASS — see §4) |
| C6 | cohorts | `C6` |
| E1 | cohorts | `it.each` over 5 corpus worlds with hollywood (mid-year, cohort-week, all-statuses, migrated-chain, deep-deficit) |
| (bonus) `isCohortWeek` | cohorts | `it.each` over 7 weeks (0,1,51,52,53,104,156) — not its own lettered id, but the primitive A1/B*/E1 all depend on |
| D1 | save-v35 | `it.each` over all 6 corpus worlds |
| D2 | save-v35 | `LIVE_SAVE_VERSION === 35`, `makeSave stamps 35` |
| D3 | save-v35 | `lossless iff cohorts is empty`, `refused as a downgrade...` |
| D4 | save-v35 | baseline + 9 tamper cases: `requested`, `talentCountBefore`, `personIds`, missing provenance row, wrong provenance row, age out of bounds, non-increasing weeks, non-52k week, wrong clipped |
| D5 | save-v35 | `two independent V34->V35 migrations...byte-identical`, `save/load at mid-year...`, `migrateToV35 lifts...` |

Case counts: `p14c4-cohorts.test.ts` = 27 (15 `it` + 7-way `isCohortWeek.each` + 5-way `E1.each`,
counting each expansion as vitest does). `p14c4-save-v35.test.ts` = 23 (17 `it` + 6-way
`D1.each`). 50 cases total.

## 3. RED runs — this scaffold worktree, HEAD `bd27de93`

Each file run alone: `node_modules/.bin/vitest run tests/<file> --minWorkers=1 --maxWorkers=1`.
Never run inside a suite pass. `tsc --noEmit -p tsconfig.json` accepted both files and the
helper with zero errors (run repeatedly through the edits below; final run exit 0, 0 errors
project-wide).

| file | result | duration | exit |
| --- | --- | --- | --- |
| `tests/p14c4-cohorts.test.ts` | **25 failed / 27** (2 passed, both JUSTIFIED — §4) | 13.72s | 1 |
| `tests/p14c4-save-v35.test.ts` | **23 failed / 23** | 9.40s | 1 |

**Failure attribution — every failure probed; zero caused by a typo or an unlawful
construction.**

| cause | count | files |
| --- | --- | --- |
| direct throw from a scaffolded export (`isCohortWeek`, `cohortRequest`, `cohortEntrantAge`, `validateSaveV35`, `convertV34ToV35`, `convertV35ToV34`, `migrateToV35`, all `not implemented (P14C.4)`) | 30 | cohorts (B1,B2,B5,C6, E1×5, isCohortWeek×7 = 15); save-v35 (D1×6, D3-lossless, D5×3 = 10); plus B3/B4/C3 reach the throw one line after their own oracle-only assertions pass (3, counted here since the throw is the proximate cause) |
| assertion mismatch: a receipt/entrant was expected from the natural `tick()`/`advanceCareerLifecycleWeek` route and none appeared (the cohort step is not yet wired into `advanceCareerLifecycleWeek` — confirmed by reading it: it runs settlement+intent only, no reference to `isCohortWeek`/`cohortRequest`/`cohortEntrantAge` anywhere) | 8 | cohorts: A1, A3, B6, C1, C2, C3 (precondition), C4 (precondition) |
| direct constant/stamp mismatch (`LIVE_SAVE_VERSION`/`makeSave` still 34) | 2 | save-v35: D2 |
| regex-narrowed throw mismatch (message is the generic scaffold string, not the cause-specific wording the real validator will use) — see design note below | 10 | save-v35: D3-refusal, D4×9 |

**Design note on D4 (and D3's refusal case):** `validateSaveV35` throws unconditionally for
ANY input on this scaffold. An initial draft asserted a bare `.toThrow()` for each D4
tamper — probing the run showed all 9 passed trivially (a counterfeit: they would have
passed against a genuinely LAWFUL, untampered save too). Fixed the same way 778's G4 handled
an analogous total-throw situation: each case now asserts `.toThrow(/<field-name>/i)`
(`requested`, `talentCountBefore`, `personIds`, `provenance`, `age`, `increasing`, `52`,
`clipped`), which the generic `not implemented (P14C.4)` message never matches, so every
case now fails today for the right reason and becomes a real discriminating check once the
validator names its own cause per field (matching `validateSaveV34`'s own established
convention of embedding the field name in every refusal).

**A second bug found and fixed during probing (B3/B4).** An early draft hand-recomputed the
youth-floor branch inline (`Math.max(accepted - active, 1)`, assuming the floor always
fires) to build its own expected value — a test bug, not a scaffold gap: for
`genuine-v34-c4-cohort-week` at week 104 this assumption is simply wrong for `writer`/`actor`
in a way unrelated to any missing behaviour. Fixed by factoring `expectedActiveAndYoung`
out of the oracle (`tests/helpers/p14c4-fixtures.ts`) so B3/B4 read the SAME `young`/`activeCount`
facts the full request-oracle uses, rather than re-deriving the floor by hand a second time.

## 4. Two justified passes — not gaps (778's P1 precedent)

- **A2** (null-hollywood world never gets a cohort): passes because `advanceCareerLifecycleWeek`'s
  existing `if (state.hollywood === null) return state` (773 D6/782 R8, already shipped in
  C.2a) short-circuits before any cohort logic could ever run, today or after the writer
  lands step 3 (793 §4 itself says the cohort step engages "iff `isCohortWeek(w)` (and
  still nothing at all when `hollywood === null`)" — the SAME gate). This case calls no
  unimplemented export and cannot fail by construction; kept as a regression guard (the
  same role T0's own Y4/791 world-4 findings play), not removed.
- **C5** (byte-identity golden pin for `generateIndustryTalent(seed, id, role)` with NO age):
  passes because the no-age path already works today and is untouched by the C.4 amendment
  (782 §6 amendment 1's own promise). The golden is captured NOW, at this exact scaffold
  (2026-09-25, HEAD `bd27de93`), so a future run that diverges is a real regression in the
  writer's implementation, not a moving target.

## 5. Requirements not fully covered, and why

- **E1** covers the 5 corpus worlds that carry `hollywood` (mid-year, cohort-week,
  all-statuses, migrated-chain, deep-deficit); no `it.todo` was needed — every one of the 5
  had complete `activeByProfession`/`activeUnder30` continuation facts in its own
  `.provenance.json` (790/791), so the task's "if a fact is missing, write it.todo" branch
  was not triggered.
- **D4** covers 9 of the task's 8 listed tamper causes by splitting "a missing or wrong
  provenance row" into two independent cases (missing entirely vs. anchored at the wrong
  week) for extra rigor, matching 778's own G4 precedent of exceeding the minimum.
- **G4-style Scientist/duplicate-record edge cases from C.2a are NOT re-tested here** — out
  of scope: C.4 never mints a Scientist (782 R4) and this suite's own D4 baseline never
  produces a duplicate-personId record; only V35-specific tampering is exercised.

## 6. Ambiguities where 782/793 admit two readings — both resolved by MEASURING, not guessing

1. **"Tick to its first cohort week W1" for a world whose OWN save week is already a
   multiple of 52 (deep-deficit, saved at week 2600 = 52·50).** Reading A: W1 = 2600 itself
   (the save's own week). Reading B: W1 = 2652 (the next week a post-C.4 `tick()` actually
   PRODUCES). Resolved via B5's own task text ("78 requested... at week 2652", not 2600) and
   790/791's own continuation-facts convention (computed at "the next TWO 52k weeks AFTER
   the save week"): a save is a snapshot, not a tick that fired the (not-yet-existing)
   cohort step, so the first week a cohort can actually appear is the next one a real
   `tick()` produces — 2652. Used consistently in B5 and E1's deep-deficit case. Both
   readings were checked against the fixture's own numbers (§ above) before picking B: the
   population is unchanged between 2600 and 2652 (790/791: "zero further retirements"), so
   this choice does not silently paper over a discrepancy — it is unambiguous once measured.
2. **Whether D3/D4's "genuine V35 state" must be reached ONLY through real ticking, or may
   be built synthetically-but-lawfully.** 793 §4's own algorithm is fully specified in text
   (ids via `uniqueIdentity`, ages via `cohortEntrantAge`'s documented formula, the existing
   no-age `generateIndustryTalent` path, `provenanceRowFor`) — none of which requires reading
   any unimplemented code. Resolved by treating this as the SAME class of case 778's
   A2c/A3a/B4a/B4b redesigns already established as lawful (a SYNTHETIC-but-lawful
   construction from real primitives, never a hand-edited save), restricted to D4 only
   (`mintLawfulCohortReceipt`, `tests/helpers/p14c4-fixtures.ts`); D3's own refusal case was
   simplified instead to reuse B6's naturally-reachable all-zero receipt, needing no
   synthetic entrant at all, once it became clear the downgrade refusal fires on ANY
   non-empty `cohorts`, not specifically one with real entrants.

## 7. Next action for the parent

- Treat §3's two scaffold-worktree runs as the authoritative RED evidence.
- Once the writer's implementation lands and is committed, re-run both files expecting
  GREEN except where a genuinely-corrected regex/expectation is needed (D4's cause-name
  regexes are reasoned guesses grounded in `validateSaveV34`'s own established
  field-naming convention, not read from any real V35 validator source — flag here in case
  the writer's actual wording differs enough that a regex needs a follow-up adjustment,
  which would be a test-side fix, not a defect).
- This task did not commit; the parent copies these files into the main tree.

## 8. Repair (2026-09-25 23:21–23:52 CEST), against implementation HEAD `476046da`

Moved to the MAIN worktree `/Users/zacheryspector/The-Movies-headless-program`, HEAD
`476046daa87a8653e4a53720d117fe9ed9db756a`, per the coordinator's repair task. Touched ONLY the
three files below plus this record; never opened `src/` diffs or record 796; a concurrent sweep
author's own edits (visible as ~48 `M` test files in `git status --short` at close) were never
read beyond their filenames and never touched. Git: read-only throughout.

**Status: DONE.** All 21+2 originally-failing cases traced to THIS SUITE's own bugs (F1-F5,
verified independently below, never retuned toward the implementation's output). Four cases
now legitimately target 782/793 §9 (an addendum received mid-repair, after record 799's
demonstration failed 2/3 seeds) and are EXPECTED to disagree with `476046da`, which predates
it; each is labeled in-code and listed in §8.4. Two further test-construction bugs were found
and fixed while PROBING (not retuning) the repair itself — §8.3.

### 8.1 Root causes, verified independently, and the fix applied

| finding | verified how | fix |
| --- | --- | --- |
| F1 (21 cases) | Read `careerLifecycle.ts:303-309`: `advanceCohort` throws "the Save V35 cohort receipts are missing" when `root.cohorts === undefined`. Confirmed by running B1 pre-fix: exactly that throw. | Added `c4LiveFixture(name)` (migrates a corpus V34 fixture live via `convertV34ToV35` before any tick/save). Every case that ticks or saves a corpus world now calls it instead of raw `c4Fixture`. |
| F1b (design, B1/B2/B5/B6/E1) | Confirmed the real `cohortRequest` re-derives from `state.talent` AS GIVEN — calling it again after `advanceTo(state, W)` counts the just-appended entrants as "active", inflating `activeCount`. | Dropped the post-tick `cohortRequest(state, W)` re-calls entirely (per the brief's own suggested alternative); every B/E case now reads the REAL receipt (`state.careerLifecycle.cohorts.find(r => r.week === W)`) and compares it to an oracle built from T0 facts / live re-derivation. B3/B4 keep calling `cohortRequest` directly, but only on the RAW, never-ticked fixture state (a different, still-valid use), cast through `GameState` since the export's param type is now the live V35 shape. |
| F2 (D2) | `makeSave` takes `GameState` (= V35); handing it a raw V34 fixture is itself a type error (confirmed via `tsc`). | `makeSave(c4LiveFixture(name))`. |
| F3 (C4 premise) | Ran the ORIGINAL C4 (`p14c4-red-c4`, week 52) against `476046da`: `receipt.personIds.length` was 0, confirming the brief's own measurement. | Rebuilt C1-C4 on a shared `deepDeficitReceipt()` helper: `deep-deficit` migrated live and ticked to 2652, guaranteed 32 actor entrants by 782 §7.1 (the SAME fact B5 pins), never a fresh seed's hoped-for youth gap. |
| F4 (vacuity) | Confirmed directly: C1/C2 against `476046da` passed even before any other fix, because their `personIds` loops ran zero times over the empty receipts F3 describes — a silent vacuous pass. | Every C1-C4 case now asserts `personIds.length > 0` up front (via the shared helper), on the guaranteed-non-empty world. |
| F5 (D4) | The scaffold-era `mintLawfulCohortReceipt` was a documented SUBSTITUTE for a state the (then-unwired) engine could not produce. With the engine real, confirmed the substitute is no longer needed. | `lawfulBaseline()` now ticks `deep-deficit` live to 2652 and reads its genuine receipt directly — no synthetic entrant construction anywhere in D4. `mintLawfulCohortReceipt`/`withSyntheticCohorts` deleted from the fixtures helper as dead code. |

### 8.2 782/793 §9 addendum (received mid-repair; record 799's demonstration failed 2/3 seeds)

`§9` changes two things, NOT YET landed at `476046da` (verified by reading `careerLifecycle.ts:268`,
still `ageAt(row, week) < TUNING.COHORT_YOUTH_BELOW_AGE`, and `tuning.ts:426`, still
`COHORT_ENTRANT_AGE.hi = 32`):
1. `young_p` becomes a LOOK-AHEAD: some active prefix person of `p` has `ageAt(row, w+52) < 30`.
2. The entrant-age validator bound narrows to `[20, 29]` (was `[20, 32]`).

Added `expectedYoungLookahead` (re-derives the look-ahead flag from the live state's own
provenance, over the REAL receipt's own `talentCountBefore` — never an independent T0
measurement, since none captured a look-ahead fact) and `expectedCohortRequestS9` (combines the
T0-MEASURED deficit with that re-derived flag) to `tests/helpers/p14c4-fixtures.ts`.

**Discriminating fact, found by direct measurement (a throwaway diagnostic, run and deleted —
never committed), not guessed:** in the `mid-year`/`cohort-week` worlds (identical population,
both reach week 156), every profession's deficit is exactly 0, and the youngest active person
per profession is: actor 23, director 24, writer **29**, craft 24. Writer is the ONLY one where
`ageAt(w)=29 < 30` (old rule: young, floor does not fire) but `ageAt(w+52)=30`, not `< 30` (new
rule: not young, floor fires). This is the required §9-vs-§7.1 discriminator, built from a
genuine corpus world, no synthetic construction.

| case | what it asserts | against `476046da` | against scaffold `bd27de93` |
| --- | --- | --- | --- |
| B6 | `cohort-week` world, week 156: writer must get exactly 1 floor entrant under §9 | **FAILS** (real receipt: writer 0) | FAILS (not implemented) |
| C3 | every `deep-deficit` entrant's age must be in `[20, 29]` | **FAILS** (one entrant measured at age 29.64) | FAILS (not implemented) |
| E1 `mid-year` | receipt must equal the §9 oracle | **FAILS** (writer 0 vs 1) | FAILS (not implemented) |
| E1 `cohort-week` | same, same world | **FAILS** (writer 0 vs 1) | FAILS (not implemented) |
| D4 "age out of bounds" | age 30 (inside old bound, outside new) must be refused | **FAILS** (476046da's `[20,32]` bound accepts 30 — confirmed via a throwaway probe: no throw at all) | FAILS (not implemented) |

B2 was checked against §9 too (`expectedYoungLookahead`) and found UNCHANGED for its own
world/professions (director 28→29, craft 35→36 — neither crosses 30), so it still passes
`476046da` legitimately; B1/B5's worlds have deficits far exceeding 1 in every profession, so
the floor (and therefore §9) is moot there regardless.

### 8.3 Two further test-construction bugs found while probing the repair (not scaffold gaps, not implementation defects)

1. A D4 "requested" tamper originally redistributed counts between two roles
   (`actor - 1, director + 1`). Probed directly: `deep-deficit`'s cohort is 100% actor, so this
   tripped the per-entrant role check ("names X as a director entrant, but the person's role is
   actor") before ever reaching the aggregate re-derivation — a legitimate refusal, but not the
   one the case claimed to isolate. Fixed: tamper only the total (`actor + 1`, no redistribution),
   matching the message the real validator actually gives (`/requests \d+ people|but names/i`).
2. A `withReceipt` helper matched an existing receipt BY THE TAMPERED WEEK, so the "non-52k
   week" case (which changes `.week`) silently found no match and inserted nothing — the original
   untampered receipt passed straight through. Probed via the exact same technique (run, read the
   real thrown message, compare against the regex): message was `undefined` (no throw at all),
   which flagged the bug immediately. Fixed: `withReceipts` now replaces the WHOLE `cohorts`
   array (baseline always holds exactly one receipt), not a week-matched single entry.
3. (Caught the same way, folded into F5/§9 above rather than listed as a third bug) the "age out
   of bounds" tamper initially edited only `talentProvenance.ageAtEntry`, leaving `Talent.age`
   and `talentProvenance.due` stale — this tripped an UNRELATED frozen-V33 "stored age disagrees
   with provenance" / "due is stale" refusal, and the broad regex `/age/i` even coincidentally
   matched the word "ages" inside that unrelated message (a false-positive pass, caught only by
   reading the actual thrown text, never by trusting a green run). Fixed: the tamper now also
   updates the stored `age` and recomputes `due` via the real `recomputeDue`, isolating the
   age-bound check cleanly.

### 8.4 Runs (each file alone, `--minWorkers=1 --maxWorkers=1`, never in a suite)

**Against the implementation** (main worktree, HEAD `476046da`, 2026-09-25 23:47–23:49 CEST):

| file | result |
| --- | --- |
| `tests/p14c4-cohorts.test.ts` | **4 failed / 27** (all four are §9 cases, §8.2 — everything else passes) |
| `tests/p14c4-save-v35.test.ts` | **1 failed / 23** (the §9 age-bound case, §8.2 — everything else passes) |

**Against the scaffold** (disposable detached worktree `/Users/zacheryspector/The-Movies-c4-red-recheck`,
created via `git worktree add --detach ... bd27de93`, `node_modules` symlinked from the main
tree, repaired files copied in, removed with `git worktree remove --force` immediately after,
2026-09-25 23:49–23:51 CEST):

| file | result | dominant cause |
| --- | --- | --- |
| `tests/p14c4-cohorts.test.ts` | **26 failed / 27** (1 pass: C5, the SAME justified byte-identity golden pin as §4 — untouched by this repair) | `not implemented (P14C.4)` throws, or a receipt/entrant expected and none present (the cohort step still unwired on this scaffold) |
| `tests/p14c4-save-v35.test.ts` | **23 failed / 23** | `not implemented (P14C.4)` throws from the V35 save stubs |

A2, previously a second justified pass (§4), now legitimately FAILS on the scaffold too: the
repaired version routes through `c4LiveFixture`/`convertV34ToV35` (needed for type-correctness
against the live `GameState`), which the scaffold has not implemented — a strictly BETTER
outcome (one fewer case that could only ever pass by construction).

### 8.5 Files after repair

| file | sha256 | lines |
| --- | --- | --- |
| `tests/p14c4-cohorts.test.ts` | `d0ae94fb5876f6d985a948092b345c89f17095794a59aed5f9cea605bb374f54` | 306 |
| `tests/p14c4-save-v35.test.ts` | `b8a8dcda6abeee53d74ad2867fffd54278112fd5d40a52072de2c5b7f2958383` | 202 |
| `tests/helpers/p14c4-fixtures.ts` | `34f89d125e95be4e4840e293d1aa0f93cc283326f61918c0a6618045fcf0d48b` | 226 |

### 8.6 Genuine implementation defects found: NONE

Every disagreement between this suite and `476046da` traces to either a bug in this suite's own
construction (§8.1, §8.3 — fixed) or the disclosed, expected §9 gap (§8.2 — `476046da` predates
that amendment by design; the writer implements it next). No case surfaced behavior that
disagreed with 782/793 AS THEY STOOD at `476046da`'s own authority.
