# Owner clock tests77 and joint-trace recommendation89 — independent review

Native contract-auditor, 2026-09-20. Two separate verdicts: KEEP the frozen clock
tests; KEEP89 as a sound engineering direction, with the explicit interface
refinements below required before its implementation/test contract is finalized.
Neither verdict certifies an unimplemented owner adapter or full B4 behavior.

Read complete77 draft/brief, exact installed test, complete79 raw/metadata,
complete89, its immediate A/W/B owner/regression source and the relevant49/56
contract. No runtime, typecheck, engine probe/import, Git, network, delegation,
source/test/plan edit occurred. This report is the sole authorized write.

## 1. Clock owner tests — KEEP

Independently verified the249-line installation is one provenance comment plus
the unchanged248-line draft:

- Draft77: `9e45309c9942ab0f45e6135a8e956480c39834186d8cf5db02b10468608dea92`.
- Installed: `04c457f6798509bfa6498f4749dd052150d8bd5af83decd1159d080fe1888eff`.
- Brief: `3136c5fff1562648a35d3ee42584f583839ca850b261df53f4ecf706526871a2`.

Fixture provenance is appropriately bounded. `operationsStudio` uses the existing
historical-control founding helper, real sign/found/activate actions and managed
operations with legacy development. It is NOT ordinary natural founding or a
minimal-roster scarcity proof. Greenlight and subsequent owner/tick calls create
the actual productions; the test does not restamp a partial record as Production,
forge admission/history, or bypass the screenplay mode's lawful stock door.

The four-field narrow records are projections of those real productions. Both
branches have distinct cloned contexts, actual stock technology collectors and
event sinks; real genre lookup, Set binding and setup resolver arguments remain.
The custom full-P callback observes conceptId/marker and passes through to the
stock callback. Full results must preserve every field except actual countdown;
narrow results preserve all of their supplied fields. Full input/context equality
checks prevent branch leakage.

All five bodies discriminate existing behavior: greenlight-week skip and order,
shooting-entry lock without a take, unassigned hold then scheduled5→4, actual wrap
with detached occupied-Post input, and committed1→0. The occupied slots are
explicit owner inputs, not invented persisted reservations. Strict operations
validation receives full surviving records only after exact proof that zero-tick
rows equal admitted release IDs. No validator is widened to accept the narrow
view. Repeated `StudioEventSink.drain()` is non-destructive in the actual owner,
so event and committed-log comparisons are not vacuous.

Compile assertions pin marker-preserving production/firstTake/order types for
both complete and narrow records. The broad stock policy must not widen P and
custom callbacks must retain full-P access. Ordinary Vitest does not execute
these TypeScript obligations; the separate type-level gate remains necessary.

### Actual pre-extraction execution

Read complete79: five PASS, exit0, fixedSource:true, one worker,
23:39:42.456–23:39:48.326Z. Base
`a8d277d91ccc323fe976d7cbb74940046ab0f22c` and protected test-only patch
`fbefa1dcc7b96189a950e9a8ca2f997f183a160efbb1d62d97a6f0cdc9f3ef32` match at both
ends. Vitest5.01s, bodies2.29s; no failed suite, timeout or unhandled diagnostic.
Raw SHA256: `f03aab1ba5251b45697294021e95ebeb46e7517f8de0018630fc782ddc2451d0`;
metadata: `c7453a72ee915cc004f8a4f13145ad6b3e8bd8588f56eeda8ef1c59d473785f1`.

This is honest pre-extraction runtime GREEN, not evidence that future type exports
or generic signatures exist. Actual type-level results and post-extraction
regressions remain separate. The brief correctly excludes synchronized-dialogue
adoption, nonempty setup progression, all occupancy producers, legacy mode and
multi-production fairness from these five cases; existing suites still cover
those extraction risks.

## 2. Joint-trace recommendation89 — KEEP direction; refine definitive interface

Reviewed89 SHA256: `9b4843850b254ff3b24c90089cf0c526963ba92d2b8c22a3f3adac00b714bbc6`.
Its central finding is source-supported, not a kernel regression. Actual
operations computes queue order once and restarts when capacity is returned.
The controlled `c2a-m4-release-law.test.ts:245` fixture has A/W/B waits3/2/1,
countdowns7/4/7; lines330–343 assert order A/W/B and final A rehearsal, B still
preProduction, W Post. Thus a physically nonoverlapping W→B→A calendar assembled
from incompatible contexts is not that owner's execution. The cited fixture is
controlled owner evidence, not a claim that these exact records arose naturally.

89 correctly requires one certified joint execution with compulsory background
holds/releases, not just a trace label attached to freely mixable alternatives.
Credit subset selection cannot remove mandatory activity. Null-event continuation
releases remain real trace facts but earn no credit. Exclusive traces prevent
false composition; separate branch-local technology/event collectors preserve
the real stage/Post, Set-wear, setup and week dependencies.

The global common-boundary prior profile and search over every equivalent trace/
credit assignment are essential and correctly stated. Per-trace classifications
cannot be combined into a global protected result. B and first-X/slack must be
one trace/one witness; failed protected X still needs an unoptimized global joint
search. Incomplete enumeration, unproved optimum and exhausted shared work remain
UNCERTIFIED, never maximum/impossibility/BROKEN evidence.

Before independent tests and definitive type implementation, make these currently
illustrative details exact. They implement89's own constraints, not new policy:

1. Use an explicit tagged domain/entry distinction for old optional alternatives
   versus compulsory traces. Define trace-key joins, unique physical paths within
   a trace and consistent path identity across variants. Reject mixed-mode rows,
   duplicate identities or nonempty per-event hold fields in trace mode. Do not
   silently change49's existing mode or assume a new `jointTraceKey` alone enforces
   these semantics.
2. Define trace-level replacement ownership explicitly. Moving a replacement out
   of an optionally selected picture must NOT remove49's same-path authority:
   its fixed hold's ownerPathKey must join the certified trajectory in the chosen
   trace, including uncredited/null-event background paths. Validate immutable
   prefix, exact hold identity and one replacement per trace; preserve every
   other-owner hold. A credit subset has no authority to apply or suppress it.
3. Distinguish trace execution identity from credit selection in returned proofs.
   A witness must identify the complete chosen trace/ledger and separately list
   credited events. Do not reuse `selectedAlternativeKeys` as if unselected
   mandatory pictures had not executed. Include the same trace join in count
   witnesses as in positive B witnesses.
4. Define global existing-domain completeness across ALL legal choices that can
   change an existing picture's calendar, not merely an inventory of existing
   picture IDs or completed individual traces. The shared boundary index and any
   saturated prior upper bound must cover that domain. An unexplored future-work
   choice can affect existing schedules through contention; labeling only its
   new picture as future must not hide that uncertainty. Complete-domain and
   early-positive proof flags therefore need explicit global meanings.
5. Specify the global trace-row/occurrence counting rule, including empty or
   context-only traces, and charged replay/normalization before implementation.
   Claims/units are shared demands, not reset per trace; alternative occurrences
   and trace overhead must not escape1024/200000 by empty rows or nested calls.
   A single canonical counter/remaining allowance must cover owner enumeration
   plus every prior/X/B probe, not one fresh budget for each trace/kernel call.

These are bounded engineering interface obligations. No new beneficiary priority,
portfolio winner, resource law or Owner taste is needed. The proposed explicit
trace domain is preferable to a first-witness retry wrapper for the stated proof
requirements;89 correctly notes that any oracle alternative would need equivalent
global completeness, continuation and budget guarantees.

Next: preserve89 and settle the above precise domain/proof/budget contracts before
trace tests or writer release. Continue the independently reviewed two-file clock
extraction; this architectural refinement does not block it. Actual lawful trace
construction, ordinary-case coverage/performance and full B4 verification remain
unimplemented/unverified by this report.
