# 130 — shared production-admission source handback

2026-09-20. Candidate IMPLEMENTED / SOURCE FROZEN for parent verification.
Native sim-core role; adopted118 plus complete121 precisions. Parent-reported
published/exact-remote base:
`5485241da3326440d68b156bc207fff3c6de3128`.

Only two production files changed:

- New `src/core/productionAdmission.ts`, SHA-256
  `fba9441b361920491e430ca87306912e7e54b40ee9bb0ce25a05c3da3d9ecd6e`.
- `src/core/actions.ts`, SHA-256
  `9f85aad1b304ff58bc602bb6c98b3f9c05715ce097b3bd68cb9f247d0f322ea9`.

No tests, fixtures, other source, configuration, index exports, versions or live
P2/trace-producer activation changed. This handback is the only document written.

## Concrete changes

The lower module owns the six adopted stages: authoritative screenplay/header
validation, generic staffing resolution, caller-supplied engaged-idle check,
engaged craft-count gate, ordered freelancer selection and lazy commissioning
validation. Common person lookup and has-discipline validation moved once; the
lookup is also exported for the action's remaining raw person lookups. The sole
runtime import is the actual `screenplayFactsMatch`; other imports are types.
No GameState, employment, action, queue-admission, forecast or RNG owner was copied.

Generic results retain full/narrow caller person types. Cast construction uses
explicit ordered properties without a partial-record assertion. Role/profile
resolution remains writer → director → lead/antagonist/support → craft. Cast
duplicates precede whole-role collisions; that latter order remains writer →
director → craft → cast. Engaged occupancy and labour remain director → cast →
craft, excluding permanent writer credit but not exempting it from same-picture
identity, profile or uniqueness checks.

`applyGreenlight` calls the header at the original stage, then resolved staffing,
then builds the same narrow production-plus-active-writing busy union. It makes
a fresh craft array for the existing mutable ReceptionInputs boundary. Forecast,
production-ID calculation and subsequent commit bodies stay in actions. After
forecast, the engaged branch checks craft count, always reads the actual current
freelancer market, obtains current contracted IDs for engaged people, and uses
the ordered freelancer result in the existing fee/ledger/solvency loop. Legacy
open-pool salary treatment remains unchanged.

The private commissioning wrapper still occupies its existing call sites; it
supplies lazy `isContracted` and broad `busyTalentIds` facts to the shared guard.
Founding/person/profile/current-contract/broad-busy order and exact refusal labels
are retained. Rewrite, screenplay credit/linking, queue priority/revalidation,
participants, cash arithmetic, action IDs and workflow commitment were not moved.
Legacy development plus managed operations remains a legal direct-stock header;
operations mode is not a new screenplay gate.

## Verification boundary and next action

Read complete adopted118/121, frozen installed124, brief124, independent128 KEEP,
129's qualification and relevant source. Per parent prohibition, ran NO tests,
typechecks, executable engine probes, Git, network or descendants. Performed only
source reading, a trailing-whitespace search (no matches), and SHA-256 reads.
Hashing emitted the environment's Perl locale fallback warning but produced the
identities above. No executed behavioral or typing success is claimed here.

121's accepted limitation remains: freelancer precollection changes pure read
timing on well-formed state; this does not claim identical getter/deep-malformed
TypeError ordering. No additional known implementation blocker was found in the
bounded static pass. New fixture conjunctions and generic typing still require
actual execution:127 reached ZERO bodies due to the absent module, while123's
113 passes establish only the pre-extraction baseline.

Next: parent runs frozen32 new cases plus the113 baseline, strict typechecks and
independent source review. Preserve and attribute any real diagnostics before
correction. The125 replay/owner-producer design remains inert and out of scope.
