# 193 — independent background / explicit-command supplement

2026-09-20. INERT, UNEXECUTED test draft; no runtime, probe, typecheck, Git,
network, delegation or protected-source/test edit. Parent-supplied published
base: `0bc641a10f995a418a180b1d4fe4e6018aa4a14a`. Native Fable test-author body
read; no Claude runtime invoked. Sole production writer191 was active. Its four
mutable files and handback were not inspected. Fixed182 remains unchanged.

Draft: `193-started-replay-background-command.test-draft.ts`, 326 lines, four
default-timeout Vitest cases. Intended installation:
`tests/p14b4-started-replay-background-command.test.ts`.

SHA256: `d5ff2adb3ad95ca75336c8563401f2fe6d006037d3d833312a89574bbbaeb414`.
Hashing/read-only static inspection is not test execution or type validation.

## Requirements and independent controls

1. **132's lawful chronology, not its disproven same-slot sweep demand.**
   `buildScenario(SEED).deadlock` + actual `initializeHollywood(...,'fresh')` +
   `greenlightA` produce the candidate. Strict guards require B drafting/due next
   week, its exact original reservation, A's different slot in the same real
   Development facility, new-start8, A's exact inProduction project link and its
   five-person company excluding credited writer B. Actual first tick makes B
   Review and leaves A8; actual second tick makes A7 retaining A's slot. Replay
   must match complete local projections, close BOTH B slot/writer holds at the
   original pre-sweep completion observation, retain A company/resource holds,
   and preserve the full path set including B/mounts. A's inProduction screenplay
   is provenance of ONE picture, not another background path. No guessed step
   number; only the specified relative order and original due week are pinned.

2. **Explicit scheduling from actual Ready.** Reuses178's genuine one-picture
   historical-control founding/greenlight/tick/locked-director/scenery route,
   stopping before scheduling. The replay command is compared with real
   `applyActions(scheduleShootingTake)` + `tick`: exact5→4 and arrived-week take.
   The event equals H and earns no claimed capacity/count certification here.

3. **Illegal same-phase command.** A real scheduled Shooting picture cannot be
   committed to release. The reference action really refuses. A replay plan with
   lawful schedule then illegal commit must be `commandRefused` before any sweep,
   with source, commitment roots and actual Ready input unchanged. No partial
   trace is accepted as complete; no error-message spelling is invented for the
   replay diagnostic.

4. **A valid original ID is not a perpetual command target.** Real ticks take
   the same genuinely filmed picture to uncommitted Release Ready. Real commit
   + tick releases it; a second real commit refuses the now-absent production.
   The one-command replay first proves exact local projection, actual admitted
   release, company suffix endpoints and null future firstTake. A later command
   for that original ID must refuse against current branch state, after exactly
   the same single owner call/release observation, without resurrection.

Each replay uses the137 public API only, unchanged200000 allowance/span220/1024
alternatives and a transparent original-owner `advanceManagedProductions` spy,
restored in `finally`. No result stubs, private tariffs, new observer interface,
RNG/cash/clock/history edits, fixture-root deletion, skipped cases or increased
timeouts. Input purity is checked on every replay, including refused plans.
Real current-save validation guards each constructed input and relevant actual
reference transitions. All root-cut prerequisites are checked from actual facts;
disjoint foreign work is preserved, not removed. Company membership is independently
director/cast/craft, not a call to the mutable productionPeople implementation.

## Unexecuted premises / limits / next action

The combined managed-screenplay scenario plus Hollywood initialization/root-cut
conjunction has NOT been executed.132 supplied a source-supported recipe, not a
reached replay witness. The one-picture Ready route follows previously reached178,
but this new stopping point, later Release-Ready conjunction and all193 assertions
remain UNEXECUTED. If a guard fails, retain the result and investigate; do not
repair persisted roots or relabel the fixture as natural gameplay.

These are generated historical-control-founder worlds, not Owner campaigns or
ordinary natural founding. Both commands and engine ticks are real. All replay
branches still require complete results where asserted; a broader fixture that
exceeds200000 is a real runtime finding, not permission to raise the budget or
relax the core useful1/2-picture gate in182.

No new casting-background, queued after-sweep greenlight, grandfathered-manual
clear, rival policy or whole-capacity completeness claim is supplied. No kernel
count/ACHIEVABLE/slack claim follows from193.186's stage/Set endpoint law remains
covered by fixed182; this supplement does not alter it.

Next: parent independently reads/freezes/installs after191 writer handback,
records actual serialized execution and typecheck reach, then attributes any
fixture, budget or implementation finding. No execution slot was taken here.
