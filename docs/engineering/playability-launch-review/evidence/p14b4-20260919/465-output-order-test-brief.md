# 465 — additive public output-order and array-isolation controls

Independent test-author handback. **UNEXECUTED** additions; parent owns the full three-file / 15-test baseline on unchanged 452 source before any 466 writer. No production inspection, runtime, Git, network or delegation was performed.

## Authority and byte preservation

Published base supplied by parent: `0bbf2843e8fc4e658a22e1d140fe8bb1c609983f`. Fully read the native test-author role and adopted `465-output-order-direction.md`; the latter's SHA-256 matched `d6ac8f9a0bfa42ea27ffaa2fd831595ca53d9d289db9727de390aa0241d58f26`.

All three baseline pins matched before editing. Only marked `// 465 BEGIN:` through `// 465 END` blocks were inserted inside the authorized existing cases. Removing those blocks in a read-only stream reconstructs each exact baseline hash; every original line remains byte-identical. No new import, helper, fixture, test case, replay invocation, timeout or cap was introduced.

| Test file | Baseline SHA-256 | Frozen SHA-256 | Lines / additions |
| --- | --- | --- | --- |
| `tests/p14b4-ready-replay-background.test.ts` | `a4b409f9072ba1ed96c2a53bab47cb109024e91c55d1f56fc76d000bd277e960` | `d0a7ef550d592c5b0f4a060c041dbb7e448e92437650cae6d7d9a03c9b71cfab` | 248 → 258 / +10 |
| `tests/p14b4-started-replay-background-command.test.ts` | `de3e976ef97b5b37196327f03ac9905175c881740b1c6bbbed87bba1c40d2b47` | `b26abdcf492cdf66cbb2c8fd4d514c382f25fe5b12cd0d5c209f6caece03794a` | 363 → 373 / +10 |
| `tests/p14b4-ready-owner-replay.test.ts` | `0e1a25ed6e9b5c1c88d8e4459845636ca7caef6e131f78ff823c5d3cc542f864` | `af5d87eeb80da9da0dc7ee5d20c195f89ce1162e82238a40180879afd93db15b` | 719 → 745 / +26 |

## Independent oracles and precise scope

- **Ready active audition:** exact emitted path sequence equals independently constructed castingSession/screenplay/standing-Set-mount keys sorted on the expected side. The six additional hold IDs must already be in ordinary string order. Original real-company membership, owner-path, timing, strict-save, owner-call and projection assertions remain intact.
- **Started due writing:** exact emitted production/screenplay/Set-mount path sequence; the two actual writer/Development fixed holds yield an exact replacement array sorted by holdId, both ending at the real background-completion observation. Completed background keys are also asserted directly as `[pathB]`. Existing one-to-one physical/person identity and release checks remain intact.
- **Same Ready sibling plans:** each branch's four emitted key sequences are canonical; their four output arrays must be distinct across siblings. Reuse the existing `zeroOffset` result to assert array isolation across invocations, after its existing full value-equality checks and an explicit complete-attempt guard. No additional replay occurs. Existing newly emitted Hold/subject isolation and intentional global fixed-Hold sharing semantics remain untouched.

Only fresh key arrays or expected arrays are sorted; returned arrays are never sorted in place. Ordinary string sorting / explicit `<` and `>` comparison is used, never `localeCompare`. Existing independent membership assertions remain essential: checking hold-ID ordering alone is not a membership oracle.

## Limits and next action

These are retained-property controls, not a claimed behavioral RED. The unchanged-source baseline may pass. Existing completed-background outputs contain only zero or one key: **multi-key `completedBackgroundPathKeys` ordering remains untested**. Cross-sibling/repeat public-array isolation does not prove copying relative to a private temporary, nor internal element-reference retention, callback timing, payment sufficiency or the private helper's tariff. Those remain source-review obligations. Original long-route acceptance failures are separate and unchanged.

All assigned tests and this brief are frozen; test-write ownership is yielded. Parent next: independently verify add-only reconstruction, review, then run the three complete files serially on unchanged 452 source before releasing 466.
