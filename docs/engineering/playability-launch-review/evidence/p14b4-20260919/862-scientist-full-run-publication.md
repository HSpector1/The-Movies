# 862 — focused checkpoint published; matched full verification active

Actual remote refs/heads/wip/headless-program-20260916-ts was read after the push
and equals local7ca52f877878266c9659a7a491693ae2128f681e. Working tree was clean.
This checkpoint preserves860 and the full848–859 evidence plus attributable test
maintenance. The raw logs/patches deliberately retain tool-emitted whitespace;
the authored test/document changes pass their separate git diff --check.

861-scientist-full-core started2026-09-26T14:36:06.088Z on that exact published
source with an empty tested patch and no untracked source. One parent-owned heavy
process. Do not commit or change consumed input until the run closes. Consult its
JSON and current process state; an unfinished log is never a final result.
Compare the completed failure identities/causes with837. New Scientist checks,
generated checks and targeted regressions are already covered by860; this is the
remaining full-boundary gate, not a repeated C.2c implementation or a new broad audit.

During the frozen run only documentation/design-ahead may be written. C.2-RM821
reconnaissance has been refreshed against current source; no C.2-RM production,
test, save/projection bump or runtime fixture generation is released by this note.
No new specialists or nested workers were created in this restart.

Design-ahead now exists as863-c2rm-expansion-draft.md, explicitly unreviewed and
not released to a writer. The old c2c_contract_auditor target could not be resumed;
the Owner has been asked to clarify whether fresh specialists for the next new
task are allowed under the no-recreation instruction. The Owner answered “Allow
specialists for the next new task”: fresh C.2-RM specialists are authorized after
Scientist verification, at most two concurrent with separate production/test
ownership. No specialist has yet been created and the Scientist source lock holds.

Partial861 output exposed additional test-maintenance omissions:14 live schema-URN
assertions still name projection50, the B.6 file's local incoming-version and
prior-registry expectations still name50/38, bridge.test pins SNAPSHOT_VERSION50,
and R07's parsedSaveVersion local still expects36. The independent outgoing50
identity is already registered correctly in production. Later partial output also
exposes12 B.5 live-write equality cases: their explicit forward chain stops at36
before comparing against live makeSave37. The proposed repair appends the genuine
convertV36ToV37 step; every V30/V31 frozen-byte assertion remains. A proposed17-test-file repair is
preserved as864-pending-live-projection-literals.patch, NOT APPLIED while861 runs.
It touches only current-wire/live-writer expectations and keeps every historical fixture and
outgoing identity. Attribute against the completed run before applying; further
failures, including any production defect, remain possible. This is not a final
result or a claim that the full test boundary passes.

Another partial861 result requires separate attribution: the unchanged
`bridge-runtime-checkpoint-prepared-reuse` test “preserves live authority on failed
Save and replays the exact successful receipt after later changes and restart”
timed out at its existing20000ms limit (reported22160ms). In837 the same test passed
with a reported23746ms duration. Record843 identifies this exact name as the known
intermittent FU-2 timeout seen in822; it vanished in837 without a fix. Rerun this
exact case unchanged in the sole lane after861;
retain the failure and original timeout regardless of a subsequent result. No
functional fix or causal regression conclusion is justified by the partial log.

## Closed resolution

861 closed15:29:51.981Z.866 records complete attribution before864 applied;
867 repairs and868/869 types pass.870 unchanged FU-2 passes in isolation.
Read871 for composite qualification and precise limits. The active-run statements
above preserve their original chronology and are superseded by this resolution.
