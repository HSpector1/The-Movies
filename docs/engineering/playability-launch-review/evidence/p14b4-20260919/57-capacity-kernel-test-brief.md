# Detached capacity kernel — independent first RED tranche

Status: INERT / UNEXECUTED, ready for bounded review and parent installation. Draft `57-promise-capacity-kernel.test.ts.draft.txt` is intended for `tests/p14b4-capacity-kernel.test.ts`; SHA256 `ba84bbb42d3f3f7b8f0e6ed0868868b27b9ea8a4af1fe315e6a232e022d79597` (485 lines, 41 expanded cases). No source/live test/config/fixture changes, tests, engine probes, typechecks, Git, network or delegation were performed. The earlier policy46 and bridge drafts remain frozen.

Authority: complete definitive `49-capacity-kernel-interface-refinement.md`, original examples42, review44 and the reviewed B4 plan's full capacity appendix. Assigned authoring reference was published `c7ef8c3` plus the parent-frozen six-line public accessor; parent subsequently reported publication `65c7502`. No independent Git/runtime verification of those publications is claimed. Future actual imports are `searchPromiseCapacity` and the definitive49 types from `../src/core/promiseCapacityKernel.js`. That module does not yet exist. Initial missing-module RED would therefore leave **all 41 bodies unreached**, not demonstrate their assertions.

## Fixture and oracle discipline

Every input is an explicitly detached finite mathematical scheduling domain. Names such as `detached-fixture-staffing:P` describe those assumed finite facts; they are not fabricated engine staffing/admission evidence and do not certify any owner adapter. Casts are whole, distinct-person assignments. Event paths are mutually exclusive by path key. Person intervals and relevant exact resource slots are explicit. There is no GameState, save restamping, actual production record, chooser winner, new game outcome or engine-history rewrite in these fixtures.

The local checker validates one returned witness using only fixture facts: unique selected alternatives/paths; exact fixed-hold replacement identity, immutable prefix and legal suffix endpoints; all other holds retained; exact person/resource-slot half-open compatibility; canonical tuple demand namespace; actual selected nonnull event/cast seat/window; one credit per person/path; every prior/debit and target counter paid exactly; exact sorted target boundaries; protected prior profile; and same-witness existing first-X/eight-week slack for a positive certificate. It does not search, optimize, import solver helpers, calculate expected output from the implementation, or supply a best-so-far optimum as an oracle. Expected prior profiles are independent explicit literals. Deep-frozen inputs and exact before/after comparison apply on every ordinary result; work is always a safe integer in the requested bound.

## Covered finite distinctions

- Shared lead-only + flexible + P1 seats on one picture, with and without a spare. Two prior people contribute two existing units, not one picture. Fixed support rejects both P2 masks but serves P1. Half-open start/due edges never slide a take. Two script/queue alternatives sharing a path remain one event. A same-person event cannot pay prior and target planning counters.
- Equivalent prior assignments remain available when the lexically first alternative occupies the target's lead. The earliest cumulative profile remains protected even when a later assignment preserves the same existing-unit total. Separate future reallocation yields `PROVEN_FRAGILE/priorPathProtection`, not joint impossibility. Every finite ordinary negative is strict `PROVEN_IMPOSSIBLE/jointOfferOnly` where appropriate; no such result authorizes a specific BROKEN outcome.
- X/B cannot splice incompatible witnesses. Four target units require six residual units. Exactly8 versus7 weeks slack is pinned. Bound count4/actual3 uses X1; an unbound target still uses count4. Already-met reads do not invent outcomes or witnesses.
- Same-week ordered boundaries distinguish release at step2 from acquisition at step1. Disjoint people still conflict on the same exact stage slot. A no-event continuation releases only its certified fixed-hold suffix, earns no event/profile/target credit, and is necessary before the later picture. Removing it retains unresolved occupancy. Other owners' holds survive even similar/equal owner labels. Empty suffix is legal; invalid prefix/owner/path/duplicate identity inputs throw, rather than become gameplay IMPOSSIBLE.
- Foreign debit uses its full remaining count even for one-week source overlap. It may share a local picture with another person's distinct seat, but cannot share its person's event with the target. The earlier existing debit event, next existing target event and later spare coexist in one witness; removing the next existing event cannot be rescued by future-only target counts. Original source window/identity remain input facts, never foreign fulfillment evidence. Tuple namespaces resist concatenation collisions.
- Complete saturated prior profile plus a legal B witness can certify despite omitted FUTURE alternatives. Missing relevant holds/existing alternatives or a best-so-far prior profile cannot certify. A partial failed count or observed X alone proves neither IMPOSSIBLE nor FRAGILE. Large admitted integers and large absolute safe weeks exercise bounded arithmetic without count/week expansion. Isolated lowered row/unit/alternative/span caps are strict sizeLimit; preparation/string/normalization budget is shared and exact at exhaustion. Reversed claim, mask, path, hold, replacement and omission collections must produce equal entire results including workUsed at five budgets. Above-governed limits are internal input errors, not raised gameplay limits.

## Mechanical clarifications adopted from parent

Malformed detached identity/hold-suffix input must throw an Error; tests use only `toThrow()` and no brittle message/type subclass. This is not save invalidity or a legal gameplay refusal.

Kernel defensively filters foreign rows using only available49 fields: issuer differs; same target person; source promise not self; positive remainder; source due > max(now.week,target.start); source start < target due. Other-person/same-issuer/self/zero/exact-nonoverlap rows are tested neutral. OPEN/current versus abandoned/terminal selection cannot be inferred from this interface and remains adapter authority. Prior claims are already-selected same-issuer/self-excluded obligations. No digest/membership provenance claim is made here.

Where huge admitted inputs simultaneously exceed a size bound and have incomplete domains, the parent confirmed no reason-precedence law: tests require bounded `UNCERTIFIED` and allow only `sizeLimit` or `domainIncomplete`. They do not force optional direct-upper-bound optimization on a different complete huge domain. This does not relax ordinary complete negative proof, isolated cap reasons, positive certificate conditions, or work-limit semantics.

## Explicit limits and next action

All test bodies, type compatibility and finite fixture guards remain UNEXECUTED. No timing guarantee, raised timeout, natural-case performance result, whole-domain polynomial claim or solver acceptance is asserted. Owner schedule enumeration, staffing/admission completeness, screenplay/production joins, real employment/hold provenance, exact digest integration, final seating, class policy, bridge activation, save cutover and actual outcome causality remain separate obligations. Additional adversarial cases may be independently added after the first tranche; this draft is not whole-B4 coverage.

Next: independent review of exact draft and clarifications, parent-authorized exact installation, serialized recorded missing-module RED, then sole-writer implementation. Re-run these bodies on the candidate and investigate actual failures without changing contracts, tolerances, validators or budgets. Parent remains sole runtime/publication owner.
