# 410 — Genuine Post retention/release public control

2026-09-20. Native test-author; authorized additive control alongside407's sole
production writer. No implementation inspection, runtime/probe/typecheck, Git
command, network or delegation. Only the assigned existing test and this brief
were changed. Parent owns all execution.

## Baseline and frozen test

`tests/p14b4-started-owner-replay.test.ts`:

- Baseline source: parent-designated `e7aa158`.
- Original570-line SHA256:
  `d3fc0c003a1e4e58c2b9164e2e7d0cf8d6a9eccd3877037fcb1e668cbb8da790`.
  Author hashed the pre-edit working file; parent independently git-showed the
  complete file at e7aa158 and confirmed the same hash by message.
- New665-line SHA256:
  `e20912644615dc94a4d9e48d47143b4f82a938be150ce022c472b8abbbae7d32`.

Exact diff scope: one95-line additive block containing one default-timeout case,
between existing wrap and zero-horizon cases. No original line/assertion, helper,
import, fixture, cap or timeout changed. There are now17 cases in this file.

## Genuine construction and independent oracle

The unchanged `scheduled(1)` historical-control founding/action fixture supplies
the initial real scheduled5. Two real ticks create actual take4 then Post3.
Strict saves validate the filmed and Post source states; the full existing
supported-root/foreign-relevance guard validates the new source. No countdown,
task, reservation, history, technology, cash, identity or capacity is edited.

Two further real ticks independently establish Post3→2 with the identical Post
reservation, then2→1 Release Ready with no reservations. Both are strictly saved.
The single test invokes public replay at H=source+1 and H=source+2, each under the
unchanged200000/220/1024 limits. This explicitly checks the intermediate retained
projection rather than inferring it only from the final state.

Both complete projections match the actual production/operations/Set/release
outputs; technology keeps original foreign rows and exact actual own rows.
Exact call clocks and raw event order are pinned. The fixed Post hold is derived
from the real source reservation: no suffix replacement at the first horizon;
at the second, exactly one replacement ends at the actual Post release's replay
boundary, after that frame's sweep start and before Release Ready phase entry.
Company identity and total fixed-hold count remain exact, with no new holds, duplicate wrap,
release admission or first take. Existing filmed history remains byte-equal for
the affected production; it is not relabeled as a future take. Source and original
shooting inputs remain unchanged.

## Evidence limits / handback

UNEXECUTED retained-property control, not a verified RED regression. Actual
constructibility, public parity, typechecking and affordability are parent-owned
checks; preserve any resulting failure. This case does not prove selection of a
particular private tariff branch, numeric prepayment sufficiency, blocked-Post
coverage, general numerical fit or B4 acceptance. It introduces no private cost
oracle or synthetic blocked source. Existing Shooting/H5-specific ledger helper
is untouched; the new case uses its own Post-specific expectations.

Test and brief frozen at handback. Test-write ownership returned; parent may
verify the additive diff and run after the production writer yields.
