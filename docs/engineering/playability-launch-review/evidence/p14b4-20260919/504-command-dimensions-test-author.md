# 504 — same-week duplicate commitment public control

Independent native test-author handback. **UNEXECUTED**: parent owns the unchanged-source baseline before the next production writer. No runtime, production edit/next-implementation inspection, Git, network or delegation was performed.

## Authority and exact delta

Parent supplied committed source `80aec8b` and authorized this one additive public control under the existing 137/176 command law. Publication verification is parent-owned, not asserted by this handback.

Sole executable edit: `tests/p14b4-started-replay-background-command.test.ts`. Add one public release-owner namespace import and one test using the existing genuine `readyForRelease()` fixture, `input()` and `observed()` helpers. All original lines remain intact; no old assertion, fixture, helper, import, cap or timeout changed.

- Baseline matched before editing: 373 lines, SHA-256 `b26abdcf492cdf66cbb2c8fd4d514c382f25fe5b12cd0d5c209f6caece03794a`.
- Frozen candidate: 430 lines (+57), SHA-256 `dcefec0fa621def52af013cbe11bdda5e94b1671252d6e1b059b8676167b76ee`.
- Removing only the two marked `504 BEGIN` / `504 END` blocks in a read-only stream reconstructs the exact baseline SHA-256. The existing four cases remain unchanged; the file now has five cases.

## Genuine reference and independent assertions

The existing real-action/tick fixture supplies a strictly admitted Release Ready picture with no commitment. The actual public commit creates one authority row without advancing the clock, production or operations. Its resulting state passes all existing supported-root/foreign-work guards and strict save validation. The public `commitPictureToReleaseRefusal` then supplies the exact duplicate diagnostic, independently checked to name the actual commitment and duplicate-release reason. A second actual public action must throw that complete diagnostic with the established public-action prefix and leave the committed state unchanged.

Replay receives the original genuine source and two same-week original-production `commitRelease` commands with ordinals 0 and 1. A transparent pass-through spy calls the real `withReleaseCommitment` owner and records its actual return. Assertions require exactly one invocation with the original authority, genuine ID and actual week; its returned authority must equal the real first-commit reference. The second command must reach `commandRefused` with the exact public-owner diagnostic before any sweep, admitted release or take. First-command provenance remains present exactly once; the cut cannot present a complete trace/projection. Source, full input and real reference remain unchanged, with the original shared 200000 cap and default timeout.

## Scope limits and next action

This covers a current-branch authority transition before a second command, distinct from the existing post-tick stale-ID test. It does not introduce fake workflow/setup/binding/task fields, relabel production IDs, add a long-ID campaign or provide a private dimension/tariff oracle. It cannot prove full copy/width discovery or prepayment sufficiency when real owners still return correct results; those remain independent source-review obligations. Existing long Ready stale-route acceptance failure is untouched.

No RED/green result is claimed for the new case. Test and brief are frozen; test-write ownership is yielded. Parent next: review and run the unchanged-source baseline, then release any separately adopted production candidate.
