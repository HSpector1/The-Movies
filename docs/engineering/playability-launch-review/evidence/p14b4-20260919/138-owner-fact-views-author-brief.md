# 138 — existing owner fact-view tests: inert handback

2026-09-20. UNEXECUTED / native independent test-author. Only this brief and
`138-owner-fact-views.test.ts.draft` were written. No live tests/source/config/
fixtures, runtime/probe/typecheck, Git/network or delegation. No producer API or
new export is guessed. Parent owns installation and separate runtime/type RED.

Authority:125's type-only genuine owner seams; existing owner laws and accepted
132 scope correction. Parent reported protected code frozen during136; last
communicated published kernel base was `5cae2da841d6d50d2c460dc87aba3b603435d00e`.
No claim of a newly measured HEAD/source identity is made; parent must record the
actual installation/execution source and patch. Mutable staffing implementation
was not inspected. Existing named owner signatures, narrow reads and fixture
recipes were read to establish constructibility and expected distinctions.

## Frozen artifact and intended type RED

- Draft355 lines; seven intended cases (five ordinary plus two nullable/default
  player parameter cases), default timeouts. Intended path:
  `tests/p14b4-owner-fact-views.test.ts`.
- SHA256 `5ba6332a3dbdf2e7d81b6956faae86036ab5b3400c894bece4ce447ad5038389`.
- Every called owner already exists. Runtime may already pass; that would NOT
  erase the independently intended compile failures from current GameState/
  full Production/FilmConcept signatures. No missing-export/module RED is sought.
- Technology arguments contain exactly technology, market.tick, nullable
  Hollywood/playerStudioId, placement.facilities. Setup omits market; adoption
  chain omits technology too; installation reads only placement.facilities.
  Geometry has property and placement.facilities. Release has real narrow
  id/conceptId/remainingTicks records, id/title concepts, operations and authority.
  Existing generic clock owner receives the real four-field production view.
- No cast to GameState/Production, `any`, mock owner, unsafe missing-field
  stand-in, skip/todo or timeout extension hides the intended type boundary.

## Independent behavioral controls (not equivalence alone)

1. Real action/tick picture at6 advances to5 with NO take. Independently created
   full/narrow policy collectors and full/narrow clocks agree on operations,
   Sets and returned clock. Default founded player appends exactly its silent
   lock at the sweep week, preserving all preexisting other rows; null player
   appends none. Inputs stay deeply frozen and equal.
2. An actually emitted selected sound adoption/choice permits its exact stage
   and Post, refuses the other real stage, and never leaks the player's choice
   into a query under an actual distinct rival identity. Installation pending,
   operational, wrong-target and null-target outcomes are literal. An explicitly
   controlled query before the operational week refuses the selected stage.
3. Ballroom setup is conventional4 before installation and lighting2 only on
   the exact operational stage with its emitted held asset. Wrong stage,
   explicit null studio, other real studio, missing holder and cancelled
   adoption return conventional4; cancellation also invalidates the chain.
   Both direct provenance and resolver factory receive narrow views. Lighting
   retains truthful null Post; no dummy Post is invented.
4. Authored centres are stage(18,3), scenery(19,18): Manhattan16, duration2.
   Real lower Director-call output is observed BEFORE action arrival settlement,
   so the still-standing blocker is arrived-pending, not falsely in transit.
   The disclosed far-body control moves only the queried geometry to centre(1,5):
   distance31, duration4, remaining2; missing body is withheld, never guessed.
5. Real not-ready production refuses with its title/countdown; missing identity
   refuses; real Release Ready permits; actual commit produces the exact existing
   commitment identity and then refuses a duplicate. Full/narrow records agree.
   A separately labelled invalid detached missing-workflow probe fails closed.

## Fixture provenance and hard limits

`operationsStudio('p13a-production-consumer')` is the existing historical-control
founding fixture, not ordinary natural founding. Real greenlight, Director call,
take scheduling and bounded ticks produce the clocks, workflows and release
facts. Its managed OPERATIONS / legacy SCRIPT DEVELOPMENT combination is lawful.
The20-step guard cannot skip; failure is a fixture finding, not permission to
hand-stamp a countdown or increase the timeout. The scenery test calls the real
lower assign owner, then reads its actual pre-arrival intermediate workflow;
it does not claim that intermediate is the settled post-action saved state.

The short consumer installation helper is EXPLICITLY CONTROLLED, adapted from
the existing `p13a-production-technology.test.ts` consumer recipe. A30m balanced
cash/ledger bootstrap and synthetic research access isolate consumer facts;
sound physical completion uses existing P09 owners at6/12. The lighting variant
sets a disclosed780 query clock and completes its actual adopted installation at
784. These are NOT earned research or strict-admitted campaign/save histories.
Adoption, equipment, components and placements themselves come from existing
owners; no hand-authored adoption/equipment record is passed off as producer data.
Lighting's exact new conjunction remains unexecuted with a hard operational/asset
guard. Far/missing geometry, unheld/cancelled records and early query clock are
labelled pure consumer controls, never legal command histories.

Remaining proof: actual earned lighting research, natural rival adoption/positive
lighting/filming, placement-built rather than authored geometry, full source-backed
replay budgets/cuts, background chronology, unstarted Ready/queue admission and
whole trace completeness. Explicit rival query isolation is NOT rival campaign
execution. These seven cases narrow existing reads only and do not replace77's
clock coverage, other owner regressions or137's separately settled replay contract.

All new calls and fixture conjunctions remain UNEXECUTED. Next parent action:
install exact frozen draft, capture separate actual runtime and strict typecheck
results, investigate guards honestly, then release only reviewed type narrowing.
