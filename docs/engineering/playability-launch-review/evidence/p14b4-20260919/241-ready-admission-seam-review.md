# 241 — Ready-admission seam review

Native Fable contract-auditor: DONE — qualified KEEP for bounded interface and
independent-test gate. Both ambiguities resolved; NOT implementation acceptance.
Reviewed final240 SHAe81667c030bd0ed4c0eccb37fc96f4ec1e860a216e33c838cabd9f2cde9c160a.
No files changed/runtime executed. Parent persisted full report.

## MET WITH SOURCE EVIDENCE

Admission versus finance: explicitly operational-capacity projection, not payable
greenlight/affordability certificate. Real Ready/header/staffing law reused without
inventing budgets/forecasts/participants/GameState. Actions retain financial gate;
future consumers must preserve distinction.

Existing owner sequence:

- productionAdmission supplies Ready/assessment, screenplay, casting, founding,
  discipline, uniqueness and staffing law.
- actions341–345 uses production-company plus active-writing occupancy, not
  permanent writer credit or invented broader busy rule.
- Source-now isContracted/freelancerMarketIds preserve actual eligibility.
  Freelancer sampling uses its deterministic derived stream, not campaign RNG.
- actions511–549 confirms initial allocator script/casting/Set occupancy union,
  actual nextSetId marker and absence of technology/setup policy at that call.
- scriptDevelopment568–594 supplies actual Ready→inProduction link.

Identity: exact allocator move from actions255–261 to productionIdentity narrow,
no evident import-cycle requirement. persistedProductionIds remains authority for
full collision union including cancelled/history IDs. No history scheduling oracle.

Chronology/compulsory occupancy: successful(now,1) admission before commands matches
public action-before-tick order. Company starts there; facility holds follow real
grant events. Original source fixed holds remain(now,0). New holds/calendars belong
only to branch. Refusal yields no admitted picture, queue or subsequent execution.

One opportunity: screenplay path['screenplay',issuerId,projectId] across staffing
alternatives. Would-be productionID is execution identity, not another opportunity.
Expanded company/foreign-relevance guards required before claimsAndHolds complete.

## Clarifications resolved

1. Same Ready project legal across independent sibling staffing choices, including
   same independently allocated would-beID. Refusal is non-Ready/already-linked
   source project, not cross-branch consumption.
2. admissionScriptDevelopment is immediate linker snapshot, not later assessment/
   Review/Produced status. Correctly distinct from tick766–773 release bookkeeping.

Exact readyAdmitted observation/existing commandRefused cut also settled.

## Code/test acceptance still required

Actually preserve ONE cumulative Work counter, prepay admission/calculator work,
include added picture in dynamic joins/workload dimensions/whole-slate sweep.
Employment derivation, full persisted-ID/collision walk, eager occupancy/allocation,
linking/output construction billable. Source-justified all-contracted shortcut
may avoid unused freelancer derivation, never replace eligibility with caller claims.

No numerical cost sufficiency/runtime constructibility certified. Require unchanged
26 controls, independent Ready tests, useful real shared-kernel execution<=200000,
source/type reviews. Proceed concrete test/API freeze and ONEwriter implementation;
no extra product decision or prerequisite campaign identified.
