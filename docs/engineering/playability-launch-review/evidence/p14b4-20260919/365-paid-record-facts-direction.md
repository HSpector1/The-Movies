# 365 — Paid dimensions record facts: adopted bounded direction

2026-09-20. Parent adopts convergent native sim-core and contract-auditor read-only
recommendations. Base HEAD/exact remote2ccd87cca17388905a85a27dddd3ff5ab9c8bc66,
replay393d4dc7f8997183468c84b6b6c6e31ff839c626f765607b5391368a20996486.
364/366 preserve actual results. No fit/Ready/B4 acceptance claim.

## Concrete scope for367

Only dimensions discovery and its private Prepared cell/fact helpers:
a paid invocation-local linear table keyed by EXACT object reference. Retain only
intrinsic numeric shallow copy footprint and/or own-string maximum. No IDs,
hashes, parsed keys, global/cross-source cache or combined Dimensions snapshots.

Discovery modes remain distinct:
- P and operations/technology roots: copy-only. Enumerate all own string keys,
  including arbitrary P fields, but do NOT start inspecting opaque values.
- Reservations: string-only as before, no unnecessary copy discovery.
- Workflow/bindings/task/setup/Set/technology rows: both current discoveries may
  be fused into one incremental cold walk.
- Existing immutable static-facts discovery can retain its present helper.
- Generic Work.copyCost everywhere else remains UNCHANGED. No mutable replay
  branch/ledger/holds/calendar/counters/cells cached.

Every current collection traversal, membership and count remains. Each fresh
Dimensions composes maxima from CURRENT members, never from historical cached
maxima. Removed rows contribute nothing. Genre IDs, current P ID/director
discovery, root footprints, reservations/tasks/setup, Sets, technology rows,
allSilent and every repeated method comparison remain represented/paid.

## Identity/lifetime proof

Actual operations commands/advances replace changed P/workflow/task/bindings/setup
records; Set wear/novelty maps replace changed Set records; technology collector
replaces changed production row/root. Exact unchanged references therefore retain
intrinsic shallow facts during this synchronous plain-data invocation. New record
reference always misses even if same semantic ID/shape. Retain original references;
never mutate or clone caller records for this memo.

The existing private Prepared cell may share completed facts across this
invocation's siblings with ONE cumulative Work counter. Cold creation occurs only
within already-admitted preparation; zero/tiny fixed administration still no scan/
owners. No refund, per-plan reset, leaked partial entry or reuse on later calls.

## Required prepayment inventory

Pay table/header/entry literal/key creation and initialization before allocation.
Pay call arguments/dispatch, each actual visited reference comparison, miss/result
reads, arithmetic/max updates, field writes, append capacity and controls before
execution. Linear search means no assumed constant-time native hash lookup.

Cold scan is incremental for-in with own-property checks and full key spans,
matching plain own-string-key JSON premise. Preserve required direct-string
spans/type checks and exact copy-price formula1+sum(3+keyLength). Saturating
arithmetic/calculators remain prepaid. Fusion removes a real repeated enumeration;
it does not discount executed work or change actual owner-copy reservations.

Publish only COMPLETE numeric facts after discovery AND literal/write payment.
Represent copy-only/string-only/both validity explicitly; a later missing mode
must be separately paid and atomically completed. Never store a prior-max-seeded
string maximum. No deep traversal or Object.keys allocation before payment.

## Evidence and verification

363 nine dimensions calls total29,597 units; repeated unchanged Set discovery
exposes at least7,504 before string spans based on actual12-key/span100 shape,
before new lookup/composition overhead. These are affected-work bounds only,
NOT net-savings or fit predictions. New immutable records still cost real misses.

367 one sim production writer ONLY replay+handback with exact source/payment
inventory. Test-author may independently add one bounded public regression file
and368 brief; no existing-test changes or runtime while source mutable.
After all freeze: parent FULLread/pins,369 independent actual-source review,
serialized370types/371lookup5/372ordinary66/373Ready15/374started26/
375adjacent137/376bridge/377newfacts tests. Existing red full routes remain gates.
No caps/validators/test weakening. T4/B2/B3/B-F2 retained; keep B4/rest program
moving, Unity/native/Owner acceptance deferred and backlog retained.
