# 288 — Minimal bounded shared employment lookup

2026-09-20, native contract-auditor. Recommendation KEEP bounded stable-sort
direction as smallest robust shared-owner adjustment; NOT implementation or
budget acceptance. Parent adopts this bounded mechanical direction for independent
tests/planning. No hollywood source-write release yet;287 writer owns ONLY replay
firsttake billing and does not implement this dependency.

284 proves35357+192157=227514 entry+owner reservation before calculator work.
Aggregation alone cannot fix it. No demonstrated remaining tightening of the
unchanged owner was found sufficient for whole-branch budget; this is narrower
than proving every conceivable bound impossible.

## Exact preserved owner law

hollywood50–59 builds per-person arrays in original employment-row order and
returns first row satisfying the existing player-studio, start/end-week and
ended-week predicate. Local replacement:

- Keep rivalEmployment signature and employment-array-identity WeakMap.
- Cache boundedStableSort shallow array of ORIGINAL row references, ordered
  lexically by terms.talentId.
- Find first matching ID with bounded lower-bound search, then scan its stable
  equal-ID run with UNCHANGED eligibility predicate.
- Preserve exact returned reference, original equal-ID order, null behavior and
  source-array immutability. No cached eligibility/week/player-specific results.

This changes the REAL SHARED owner used by ordinary employment/talent-market
callers and replay's actual freelancerMarketIds. No replay-only alternative
policy, new public export, domain extraction, schema/evaluator change or financial
rule. For35 rows the existing sort supports conservative210 comparisons and
lower-bound at most6 iterations/query. These support a smaller explicit bill,
NOT an already-proven total-budget result.

## Alternative considered

Filtering player rows from cold index is viable only with playerStudioId-aware
cache validation and full filter/rebuild cost. It adds invalidation bookkeeping
and data-dependent savings; the24-key premise must come from actual rows.
Sorting ALL original references avoids that dependency, retains query-time
predicate and provides a general bounded lookup improvement. Preferred here.

## Required bounded checks

- Prepay cold sorting/copies, BOTH lexical comparator operations, query spans,
  binary-search iterations, equal-run boundary comparisons and predicates,
  cache work AND calculators. Never assume warm construction.
- Independently compare original-order first-eligible lookup for absent IDs,
  interleaved duplicate IDs, competing eligible rows, player exclusion,
  inclusive/exclusive dates and endedWeek.
- Verify original-reference returns, source unchanged, repeated same-array queries
  with changed week/playerStudioId, and replacement employment arrays.
- Re-run unchanged freelancer Ready case plus relevant ordinary caller controls
  under same200000; no cap/timeout/assertion/policy/schema/version changes.

Actual affordability remains implementation/verification obligation. Auditor
made no edits or runtime calls; parent read full recommendation and adopts only
this behavior-preserving direction.289 independent test-author draft next;
implementation waits its reviewed tests/parent serialized baseline and a later
explicit sole-writer release.287 firsttake source work proceeds independently.
