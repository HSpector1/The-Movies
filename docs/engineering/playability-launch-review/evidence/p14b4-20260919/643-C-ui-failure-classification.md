# 643-C — contract-auditor classification of the 28 UI failures (read-only)

**Status: DONE (classification) / PARTIAL (verification — nothing executed).** No changes made.

Paths: EV = `/Users/zacheryspector/The-Movies-headless-program/docs/engineering/playability-launch-review/evidence/p14b4-20260919/643-b4-ui-project.txt` (HEAD 0e330da0, `fixedSource: true`, exit 1, 9 failed files / 28 failed / 2657 passed / 5 skipped, plus **1 unhandled error**, duration 896.9 s). UI = `/Users/zacheryspector/The-Movies-headless-program/ui/src`.

## Table — 28 rows

| # | File > test | Exact error line (EV line) | Class | P14B.4 reachability (evidence) | Earlier evidence |
|---|---|---|---|---|---|
| 1 | lot/WorldFirstLotNativeCastingReviewApp > greenlights the canonical Package into exact formation… | `Test timed out in 5000ms.` (EV:2313; 7189 ms) | TIMEOUT | NOT REACHABLE (semantic). Test at `UI/lot/WorldFirstLotNativeCastingReviewApp.test.tsx:422-485`; every state check is a `waitFor`/`findBy` with the default 1000 ms budget (no `asyncUtilTimeout` in `UI/test/setup.ts`, no per-test timeout), so a broken greenlight would surface as an assertion, not a 5 s timeout. Greenlight/formation is green elsewhere in the same run: `App.test.tsx` "assembles, greenlights…" (EV:3299), `…CastingReviewAppAuthority` "commits the clear successor…" (EV:221), "keeps the committing successor… publishes one live formation atomically" (EV:225). | None (no prior full-UI run) |
| 2 | lot/WorldFirstLotNativeCastingReviewAppAuthority > never lets stale Casting review closures demote or mutate a newer exact event | `Test timed out in 5000ms.` (EV:232; 8663 ms) | TIMEOUT | NOT REACHABLE (semantic). `UI/lot/WorldFirstLotNativeCastingReviewAppAuthority.test.tsx:917-984`: two UI sim-to-next-event runs + one adapter `advanceToNextEvent` + 4× `exportSaveJson` of a full V30 world (sync, heavy); stale-closure law is asserted synchronously (:974-983) and the script-review twin passed (EV:3442). Exact casting variant NOT VERIFIED in this run. | None |
| 3 | lot/WorldFirstLotNativeNextEventApp > orients a cash stop to Administration and focuses the supporting Finances section | `Test timed out in 5000ms.` (EV:209; 14166 ms) | TIMEOUT | NOT REACHABLE (semantic); RUNTIME-SENSITIVE. `UI/lot/WorldFirstLotNativeNextEventApp.test.tsx:1637-1702`: roster on 400-week contracts (`UI/test/founding.ts:35`), `advanceToNextEvent` run 2× in setup + 2× in UI until `cashNegative` — hundreds of Hollywood ticks per run. `hollywoodTick.ts` is a P14B.4 file, so a per-tick cost regression could push this over 5 s; the assertions themselves (stopReason, Administration focus) are green elsewhere: `d12-sim-event-stop` "(9) first negative-cash crossing" (EV:3113), `next-event-destination-focus` "focuses finance…" (EV:3424). | None |
| 4 | lot/WorldFirstLotNativeNextEventApp > clears every next-event transient across an accepted same-seed same-week whole-studio replacement | `Test timed out in 5000ms.` (EV:218; 8081 ms) | TIMEOUT | NOT REACHABLE (semantic); RUNTIME-SENSITIVE. `:1984-2076`: 2× `advanceToNextEvent`, a week-sync loop, 3× `exportSaveJson`, then a V30 export→`saves-import` round trip (:2033-2048). The V30 import path is green in this run (`session.test.tsx` 17/17 EV:2732; `d17-save-migration` EV:3443; sibling "preserves the exact live-world reaction after a rejected import" EV:216) and in 615 (`saves.test.tsx` 15/15). | None |
| 5 | lot/WorldFirstWorldInspectorDefault > reaches the canonical deep screen ONLY through the explicit details action | `Test timed out in 5000ms.` (EV:328; 5239 ms) | TIMEOUT | NOT REACHABLE. `UI/lot/WorldFirstWorldInspectorDefault.test.tsx:339-355`: 9 full Lot mounts in one 5 s budget; the identical-shape sibling "never navigates from any of the nine places" passed at 4927 ms (EV:325). Pure UI routing; no P14B.4 file in the path. | None |
| 6 | lot/WorldFirstWorldInspectorDefault > never lets any place print its blocks out of the canonical order | `Test timed out in 5000ms.` (EV:589; 5590 ms) | TIMEOUT | NOT REACHABLE. `:609-629`: 3 states × 9 buildings = 27 mount/unmount cycles under the default 5 s. Root of rows 9-15. | None |
| 7 | lot/livingTurn.scheduler > auto-pauses on the FIRST PAUSE-class stop, at exactly the hand-advanced week | `Test timed out in 5000ms.` (EV:2653; 5854 ms) | TIMEOUT | NOT REACHABLE (semantic); RUNTIME-SENSITIVE. `UI/lot/livingTurn.scheduler.test.tsx:443-462`: `pauseClassRunway` sim + `spendWeeks(2R+4)` in 500 ms fake-clock chunks (`:232-256`, one `act` per chunk). Wall time scales with R = first pause-class stop week; `SimStopReason` (`UI/engine/adapter.ts:2567-2586`) has no promise/market member and ui/ was untouched, so R moves only if a P14B.4 tick change moved a stop week. Sibling partition tests passed (EV:2648, 2655, 2658). Exact auto-pause claim NOT VERIFIED. | In-source only: `livingTurn.parity.test.tsx:90-98` records "5.1s under a full parallel suite, which is how the default first bit" |
| 8 | lot/WorldFirstWorldInspectorDefault > routes canvas intent and semantic companion activation to the same owner | `Found multiple elements by: [data-testid="lot-nav-theater"]` (EV:331; test:365) | CASCADE (secondary to row 5) | NOT REACHABLE. Row 5's orphaned loop keeps mounting `StudioLotScreen` after vitest moved on; two `studio-lot-screen` trees coexist. Not a component duplication: every other `getByTestId('lot-nav-*')` in this file and in `WorldFirstLiveWeekAdvance`/`StudioLotScreen.test` passed. | None |
| 9 | …InspectorDefault > offers Development the Commission verb at Week 0 | `Found multiple elements by: [data-testid="lot-nav-writers"]` (EV:592; test:635) | CASCADE (secondary to row 6) | NOT REACHABLE. Proof of cross-test leak: this test renders a Week-0 studio with nothing drafting, yet the second match reads `Drafting: A Study in Insurgent` / `att-active` (EV:632-659) — that is row 6's `draftingStudio('m-b-order-sweep-drafting')` tree. | None |
| 10 | …InspectorDefault > routes the Commission verb to the commission intent, and the deep ghost to Development | same string, `lot-nav-writers` (EV:838; test:663) | CASCADE | NOT REACHABLE (as row 9) | None |
| 11 | …InspectorDefault > F4: offers the Commission verb while a screenplay is already being written | `lot-nav-writers` (EV:1083; test:697) | CASCADE | NOT REACHABLE | None |
| 12 | …InspectorDefault > F4: offers it with an accepted screenplay waiting on casting, too | `lot-nav-writers` (EV:1328; test:711) | CASCADE | NOT REACHABLE | None |
| 13 | …InspectorDefault > names the picture in Casting's audition verb, and prefers the retained in-world planner | `lot-nav-casting` (EV:1573; test:733) | CASCADE | NOT REACHABLE | None |
| 14 | …InspectorDefault > falls back to the existing deep Casting path rather than leaving a dead button | `lot-nav-casting` (EV:1818; test:760) | CASCADE | NOT REACHABLE | None |
| 15 | …InspectorDefault > reads the deep ghosts in plain language, never in internal screen names | `lot-nav-writers` (EV:2063; test:770) | CASCADE | NOT REACHABLE | None |
| 16 | lot/authored-rgba-export > 3. the RGB channel is reduced to the recovered 128-colour contract | `ModuleNotFoundError: No module named 'PIL'` via `execFileSync('python3', …)` (EV:3332-3342; test:91) | ENVIRONMENTAL | n/a | Same class in every core run since `headless-baseline-20260916/test-core.txt:1137` (tools/hollywood/export_district.py); named in `p14b3-20260919-final/14-independent-core-review.md:57` and `p14b4-20260919/150-owner-fact-source-review.md:40` |
| 17 | authored-rgba-export > 4. alpha is bit-identical between the two finishes | PIL (EV:3344; test:99) | ENVIRONMENTAL | n/a | as 16 |
| 18 | authored-rgba-export > 5. alpha is genuinely lossless… | PIL (EV:3356; test:109) | ENVIRONMENTAL | n/a | as 16 |
| 19 | authored-rgba-export > 6. output is truecolour RGBA with no PLTE/tRNS… | PIL (EV:3368; test:137) | ENVIRONMENTAL | n/a | as 16 |
| 20 | authored-rgba-export > 7. repeated runs are byte-identical | PIL (EV:3386; test:160) | ENVIRONMENTAL | n/a | as 16 |
| 21 | authored-rgba-export > 8. the historical PNG-8 path is still available… | PIL (EV:3404; test:177) | ENVIRONMENTAL | n/a | as 16 |
| 22 | lot/authored-stage-a > 4. the RGB channel carries the recovered 128-colour contract | PIL (EV:3145; test:113) | ENVIRONMENTAL | n/a | as 16 |
| 23 | authored-stage-a > 5. alpha is bit-identical… | PIL (EV:3157; test:124) | ENVIRONMENTAL | n/a | as 16 |
| 24 | authored-stage-a > 6. alpha is lossless… | PIL (EV:3169; test:133) | ENVIRONMENTAL | n/a | as 16 |
| 25 | authored-stage-a > 7. registration is the approved one — inset, with a forecourt | PIL (EV:3181; test:139) | ENVIRONMENTAL | n/a | as 16 |
| 26 | lot/livingTurn.parity > (a) by hand at the seam — the manual verb, pressed twelve times | `Unable to find an element by: [data-testid="studio-lot-screen"]` — DOM shows `recovery-notice "Continuing your studio — Week 2."` + `studio-lot-lazy-loading "Opening the Studio Lot…"` (EV:2963-3043; `mountLot` :110, test :225; 1550 ms) | TIMEOUT (findBy 1000 ms, lazy-chunk cold mount) | NOT REACHABLE. The V30 session restored (recovery notice rendered); only the lazy Lot chunk had not resolved within 1 s on the file's first mount. Arms (b), (b′), (c) and "exports FOUR byte-identical saves" use the same `mountLot`/fixture and passed (EV:3044-3047). | In-source: `livingTurn.parity.test.tsx:90-98` (load sensitivity recorded; the 60 s `MOUNTED_ARM_TIMEOUT_MS` does not cover `findBy`) |
| 27 | test/contracts/p05a-w2-closed-production.contract > the guidance card agrees with the rail on the wrapped-waiting oracle fixture — never SHOOTING (P06C §5) | `Error: studioLotSnapshot: invalid or ambiguous Gate Hiring authority` at `UI/engine/adapter.ts:7264` via `managedSnapshot` (test:86) from test:335 (EV:5952-5962) | ASSERTION (thrown) | NOT REACHABLE by the P14B.4 subjects; INHERITED from P14A.1. Read-trace: the test loads `ui/e2e/p06-visual-oracle-v1/s2-wrapped-waiting-for-post.save.json` (`"saveVersion":16`, no `hollywood` key) through `loadSave` = `validateSave` (`src/core/save.ts:6411-6413`, version-dispatched validation, **no migration**) and casts the V16 state to `GameState` (test:331). `gateHiringEligibleCards` (adapter.ts:4124-4130) wraps `hiringMarketCards` in try/catch → null → throw at :7264. Inside that try, `employmentInfo` (adapter.ts:4032) → `playerOfferOptions` → `playerOffer` (`src/core/talentMarket.ts:260-262`) tests `state.hollywood === null` and otherwise reads `state.hollywood.playerStudioId`; on the V16 state `hollywood` is `undefined`, so this dereference throws. `playerOffer` and the adapter re-route landed at P14A.1 R1 `c25afd6` (plan `P14-HEADLESS-PLAN.md:446`, "the ui adapter option lists … route through it"; same entry records "`--project ui` not run (typecheck-clean only)"). Every other `managedSnapshot` call in this file runs on live-version fixtures and passed. | None (no full-UI run since P14A.1; 614/615 did not include this file) |
| 28 | p05a-w2-closed-production.contract > the rail, the world Stage card, the workspace source and the guidance card all agree for the wrapped-waiting picture (P06D item 2) | same error; test:86 from test:363 (EV:5966-5976) | ASSERTION (thrown) | NOT REACHABLE; INHERITED (same V16 fixture, same path as row 27) | None |

Not in the 28 but material:
- **Unhandled error** `TypeError: viewRef.current?.hollywoodPerformance is not a function` at `UI/lot/StudioLotScreen.tsx:4854` (500 ms interval, gated by `hollywood && identityProof && canvasReady`), origin `UI/lot/StudioLotIdentityReview.test.tsx` "each mode drives setIdentityMode…" (EV:5985-5994). The file's `Fake` view (`:25-46`) has no `hollywoodPerformance`; line 4831 uses `getDebugState?.()` but 4854 (and 4867) call without `?.()`. ui/ untouched by P14B.4 → inherited test-double gap. Vitest counts it as `Errors 1` and it alone forces exit 1.
- `Not implemented: HTMLCanvasElement.prototype.getContext` (EV:2749) is stderr from Phaser's `CanvasFeatures` init inside a passing file (`rt-audio-edges` 18/18, EV:2761) — noise, not a failure.

## Summary

Counts per class: ENVIRONMENTAL 10 (PIL) · TIMEOUT 5000 ms 7 · CASCADE from a timed-out sibling (DOM leak, "Found multiple elements") 8 · TIMEOUT findBy 1000 ms (lazy chunk) 1 · ASSERTION 2. Total 28.

Failures not attributable to environment/timeouts: rows 27, 28 only (one root cause). Both NOT REACHABLE by promise roots / cast class / seatClass / projection 47 / LIVE_SAVE_VERSION 30 / migrateToV30 / rival seating / cancel seam: the throw is on an unmigrated V16 oracle fixture hitting the P14A.1 `playerOffer` dereference, swallowed by the Gate Hiring fail-closed boundary. Read-trace, not executed — the exact first-throwing statement is inferred; the mechanism (V16 state, `hollywood === undefined`, try/catch → null → 7264) is certain from the sources cited.

Cascade attribution (rows 8-15) is demonstrated, not assumed: row 9's second `lot-nav-writers` carries row 6's drafting fixture text; row 8 follows row 5 immediately and the file's other nav queries pass.

Load signature supporting the timeout reading: `session.test.tsx` 6167 ms single-worker (615) vs 13403 ms here (EV:2732); `d17-save-migration` 3849 vs 7082 ms; `m5-determinism` single test 232 s (EV:3513); `StudioLotScreen.test` 61 tests 42.8 s (EV:58). Rows 3, 4, 7 remain RUNTIME-SENSITIVE to a P14B.4 per-tick cost change (`hollywoodTick.ts` is in the touched set) — not excluded by this evidence.

**Can 643 be recorded as the UI baseline?** Yes, as the first full-UI record, with these caveats written into the record: (1) deterministic inherited set = rows 16-25 (env) + rows 27-28 (V16 fixture) + the `hollywoodPerformance` unhandled error — this is the comparable set; (2) rows 1-15 and 26 are load-dependent and will not reproduce as the same names — comparing them by name next time is unsound; record them as "timeout class in 6 files" (NextEventApp, CastingReviewApp, CastingReviewAppAuthority, WorldInspectorDefault, livingTurn.scheduler, livingTurn.parity) pending a single-worker rerun; (3) exit code stays 1 while the unhandled error exists, so exit code is not a comparator; (4) the last recorded green full-UI run in docs is `docs/PRODUCTION-OPERATIONS-V1-CLOSURE.md:106` (61 files / 558 tests, P06 era) — not comparable in size.

Next concrete action (bounded reproduction, parent/test-owner): `vitest run --project ui --minWorkers=1 --maxWorkers=1` on the 6 timeout files at 0e330da0 (600-T3 idiom). Green → record rows 1-15, 26 as load; red → measure `advanceToNextEvent` / `pauseClassRunway` wall time at 89b5ad2 vs HEAD in a throwaway worktree. Rows 27-28 need a test-side fix outside P14B.4 (route the oracle fixture through the adapter's `importSaveJson` migration chain as `d17-save-migration.test.ts` does, or regenerate at V30); the `hollywoodPerformance` error needs `?.()` at `StudioLotScreen.tsx:4854/4867` or the method on the `Fake` — both ui/ writes, not authorized here.

Sources read: EV (headers, all FAIL blocks, summary); `UI/lot/{WorldFirstWorldInspectorDefault,WorldFirstLotNativeNextEventApp,WorldFirstLotNativeCastingReviewApp,WorldFirstLotNativeCastingReviewAppAuthority,livingTurn.scheduler,livingTurn.parity,StudioLotIdentityReview}.test.tsx`; `UI/test/contracts/p05a-w2-closed-production.contract.test.ts`; `UI/test/{setup.ts,founding.ts}`; `UI/engine/adapter.ts` (:120-199, 3940-4190, 4600-4675, 2567-2586, 2849); `UI/lot/StudioLotScreen.tsx:4830-4870`; `src/core/{save.ts (5248-5286, 6396-6437), talentMarket.ts (1-300), employment.ts (91-103, 160-171, 230-335, 340-459), hollywood.ts (1-100)}`; `ui/e2e/p06-visual-oracle-v1/s2-wrapped-waiting-for-post.save.json` (grep only); `vitest.workspace.ts`; `P14-HEADLESS-PLAN.md:444-456`; evidence greps across `docs/engineering/playability-launch-review/evidence/*`.

Evidence limits: no execution; no git history (cannot pin the commit that first broke rows 27-28 beyond the plan record); the orphan-loop interleaving for rows 8-15 is inferred from DOM content and ordering, not from a stack; per-tick cost regression neither shown nor excluded.