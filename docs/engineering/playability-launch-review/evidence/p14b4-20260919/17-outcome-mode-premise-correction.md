# B4 outcome premise correction — two independent modes

Parent read-only check2026-09-19 after original RED publicationd39a9a0.
This corrects the overly broad commissioning concern in15-first-red-disposition
and earlier progress notes. Those records and original RED remain preserved.

Actual decompression of genuine-v29-bound-open-p1.json.gz shows week52,
scriptDevelopment.mode='legacy', projects=[], operations.mode='managed'.
The source p13aGeneratedStudio helper activates StudioOperations only; it does
NOT activate ScriptDevelopment. Thus a legacy development door and managed
shooting workflow coexist lawfully.

actions.ts:334 requires an authoritative assessed Ready script only when
scriptDevelopment.mode==='managed'. The direct stock arm is legal for legacy
development. operations.ts:1585–1604 suppresses first-take emission only when
operations.mode!=='managed', NOT when screenplay development is legacy.

Accordingly, playerPayload/playerToFive direct stock is NOT established as an
invalid fixture. Do not rewrite it into managed commissioning merely on the
parent's earlier assumption or change production admission. Its other actual
runtime prerequisites remain unexecuted after the Save30 gate; investigate those
only when reached. realPlayerPromise likewise begins from the mixed-mode helper,
so absence of a commissioned project is not by itself proof its draft is invalid.

The independent outcome341 type error remains real: importSave returns the full
historical union and must be explicitly narrowed/validated before .state.promises.
No test/source/fixture was changed by this read-only correction.

Future capacity-owner implementation must distinguish BOTH mode axes. Managed
development requires real script work/acceptance; legacy development may have a
real direct-stock path if operations are managed. Fully legacy operations still
emit no first take. The new inert capacity draft intentionally activates managed
development and therefore legitimately constructs actual Ready scripts.
