# 479 — Independent measurement review and unadopted candidates

2026-09-20. READ-ONLY specialist conclusions; no optimization adopted.
Written479 direction SHA256:
`459ea8e2e8fc9baa25b5b8996477872b9c693b7779563b2b016fdadb5447b627`.

## Measurement direction

Native contract-auditor qualified KEEP. Preserve meter units/order/cuts/owners,
successful-versus-failed charges, exclusive/exhaustive categories and original-
entry shadow semantics. Reconcile the meter's ACTUAL initial used, recording raw
requested preparation separately if different. Count31 once per relevant cell,
not once across aggregated invocations.481 actual instrumentation review remains
required; written acceptance is not observer-integrity or runtime evidence.

## Local scalar reuse — not adopted

Two exact repeated expressions exist in frozen466:

1. sweepBill common expression computes keyBill(d.n,d.dp) twice. A local result
   could supply both unchanged multipliers, but must be introduced at the first
   original evaluation point, preserving earlier orderBill evaluation, and pay
   binding/reads. It is NOT the separate commitments.size key bill.
2. allocationBill composite arm computes keyBill(d.n,d.d) twice. A local must
   remain inside the existing non-wrap/non-retainedDevelopment/non-PostExit
   branch and pay dispatch/binding/reads. Hoisting outside adds absent work.

Neither is a cache across calls or reduction of real owner prices. Successful
singleEarlySweepBill bypasses both general formulas, and already hoists its main
values. These cannot be claimed as remaining7/6 fast-path savings. No frequency,
net benefit or fit is established; do not implement merely because reuse exists.

## Narrow capability width — not adopted

Under existing singleWrapSlot proof, the general requirement-facility component
of allocationBill is d.f × (2 + equality(d.d)). Actual Post has one requirement,
'post' (productionPhases), and this walk compares facility.capability to capability
(operations allocator). Both are fixed-domain capability strings of width<=19.
The stage-only comparison against boundSet.mountedOn cannot execute in this arm.

Only this guarded component could use separately prepaid equality(19). Retention
facilities.some compares facility IDs and MUST NOT be narrowed this way; neither
may general stage selection, slot-key construction/lookups, policy or sorting.
Pay selector/computation/local/reads and keep alternatives lazy. No measured net
benefit or priority follows; this may be too small to justify a source change.

## Independent test-author findings

Existing record-facts A/B/A/B tests establish repeat results/work for genuine
same-ID/different-package invocations, opaque-value neutrality, changed-own-key
cost and two-tick actual-owner parity. Started two-picture/standing-Set loops
exercise alternating role members; Ready siblings exercise shared inputs plus
separate new records and public isolation.

Ten roles have fixed modes: copy-only operations/technology/Production;
copy+strings workflow/bindings/task/setup/Set/technology-row; string-only
reservation. Genuine differently shaped owner records do not cross-alias these
roles, so they do not independently exercise same-record mode upgrades.
Do not fabricate campaign aliases to obtain a private accounting oracle.

Optional fresh-reference control: structuredClone the existing genuine A state,
validate with supported/makeSave, assert distinct relevant references/equal
values, replay against the existing genuine two-tick reference, and compare exact
result/work/purity. It is supplementary and NOT authored or required currently.

Exact-ref hits, complete miss scans, original shared entry/null-mode/atomic
upgrade/publication and tariff correctness remain static review obligations.
Keep production selection dependent on482/483 measurement, not these possibilities.

## Alternative exact-reference Map — architecture only, NOT adopted

Independent auditor found an admissible accounting architecture, not a tariff:
162 section2 explicitly uses an abstract complete-key-universe footprint for
native Map/Set operations; it does not prove the host hash implementation's
instructions or wall time.176 retains that model. Parent fully re-read162,
176 and365 after this assessment.

An invocation-local Map<object, DimensionRecordFacts> can compare exact object
references without inspecting opaque property values or stringifying keys.
Any proposal must explicitly reserve ALL potentially compared live references,
native invocation/access/result/write costs, constructor/cardinality work and
every calculator. Each get and reached set must be paid separately, including
cold insertion. Removing the existing real JavaScript search loop may remove
its loop/field/control costs; it does not permit lowering comparison prices.

365 specifically adopts a linear table, so changing it needs a scoped written
amendment and actual source-counted object-key footprint before implementation.
Preserve complete original shared entries, separate modes, exact identity,
atomic completed publication and invocation-only lifetime. No free/native
constant-time hash claim, global ID cache, omitted insertion or numeric snapshot.
If a literal native-engine worst-case bound were required,162 does not supply
it; an explicitly bounded lookup implementation would instead be needed.

No numerical tariff, benefit, priority or whole-route fit is established.
480 may record table cardinality at entry alongside actual visited count to
make later cold-growth comparisons possible. No Map source change is authorized.
