**629-B contract-auditor report — breakPromisesOnCancel causal-coupling correction (READ-ONLY; nothing run, nothing edited)**

Reviewed at HEAD cb13a457 in /Users/zacheryspector/The-Movies-headless-program. Candidates: the 629-A design note (scratchpad, 374 lines) and the untracked RED /Users/zacheryspector/The-Movies-headless-program/tests/p14b4-cancel-causal-proof.test.ts (592 lines). Parent baseline confirmed from /Users/zacheryspector/The-Movies-headless-program/docs/engineering/playability-launch-review/evidence/p14b4-20260919/629-cancel-red-baseline.json: sourceSha cb13a457, testedDiffSha256 104739c5…, exit 1, 7 failed / 9 passed, fixedSource true; every failure is the `untouched()` line (baseline.txt :6-20, :359-360).

## Q1 — Does the design implement exactly the plan/26 §2 law?

MET WITH EVIDENCE on every named element except one REFINE (the window-start clamp, Q2):
- Separate proof, not the offer result: §2.4 replaces promises.ts :753 (`reclassifyPromise(next,…).classification !== 'IMPOSSIBLE'`) with `targetSpecificImpossibility(state, …) === null`; `reclassifyPromise` :448-460 and `promiseFeasibility` :383-441 untouched (plan :354-355; 26 §2 :69-76).
- Remaining from actual class-qualified distinct takes, never progress: `remaining = max(0, count − |qualifyingTakes(state, promise)|)`; `qualifyingTakes` :620-634 is exact issuer, half-open window, mask seat, distinct productionId (plan :324-326). Zero remaining → `null`, no settle (plan :327-328).
- No reservation subtraction (`reservedByActivePromises` :300-304 not read), no family refusal (:389), no classless refusal (:394), no FRAGILE term (:432-440), no receipt minted (settle :639-661 writes outcome fields + one `promiseOutcome` receipt only).
- Class relevance: `promiseCastSlots(promise).some(slot => cancelled.cast[slot] === beneficiary)` replaces the class-blind `seated` set :748 (26 §2 "check whether the cancelled picture could satisfy this class"); legacy count-only roots keep generic-cast relevance through :595.
- Immunity :747 kept verbatim; termination owner :720-733 and its caller actions.ts :2637 untouched; due-week owner :673-713 untouched; no lifecycle rewrite; only actions.ts :599 reaches the function (grep, Q6).
- Greenlight seam: note §1.11 paper review is sound — a non-mask seat hold is reversible by the same studio's `cancel` (actions.ts :567-590; busy set derived from `activeProductions`, employment.ts :125-127), so it is not PERMANENT path loss; SPECIFIC_PROJECT never binds live (talentMarket.ts :1279-1281 authors P1/tagged P2). No greenlight integration is correct under plan :246-247 and :254.

Added beyond the law: module export of the helper (harmless, Q5) and a type-only narrowing of :352 (Q7). Missing: the `from` clamp defect in Q2 (D1). Stale comment actions.ts :591-595 will describe the old rule after the change (RECORD-ONLY, Q7).

Rulings on note §5 (i)–(viii):
- (i) Post-cancel `activeProductions` already excludes the cancelled picture (actions.ts :579-590 precedes :599). Confirmed. The suggested "second seated picture present" RED variant is unlawful for one person (M16, productionAdmission.ts :210-220); case 6c already pins that the cancelled picture's own clock is not a path. Nothing further needed.
- (ii) Employment end is the termination owner's; validator :1029 keeps bound windows inside the contract. Out of slice. Agreed.
- (iii) A bound DIRECTING_COUNT root is validator-lawful (:976-977, :993-997) and every owner reads it with generic-cast evidence (validator :982/:1047-1049; `qualifyingTakes` :630). The proof judging it over the cast mask is consistent with the source; that P3's director-of-record semantic is unmodelled is a pre-existing analysis limit (companion :321). RECORD-ONLY.
- (iv) Independent per-promise judgment, no reservation term: matches plan :249-256. Agreed.
- (v) Proof on `state`; settle touches only `promises`/`talentMarket` (:654-660); receipt order = root order. Agreed. `from ≥ due` is unreachable live (tick.ts :1102-1110 settles at due before any action); synthetic edge → BROKEN with the cancel cause. RECORD-ONLY.
- (vi) Stored progress never read by the proof; on the live route it is ≤ actual takes, so validator :1077 cannot be tripped by a cancel BROKEN (proof requires takes < count). Agreed.
- (vii) (a) SEAT_CYCLE_WEEKS is a HYPOTHESIS (:55-58) — ruled in Q2. (b) 26 §2 line drift (:656/:321/:739-746 → :744/:286/:567-600): RECORD-ONLY. (c) Optimism about a blocked picture is the safe direction. Agreed.
- (viii) Do not write `progress` at the cancel settle; termination owner precedent (:727-731). Agreed.

## Q2 — Cadence ruling: B, with one correction to t0

Busy law verified: `activeProductionCompanyTalentIds` = director + three cast + craft of every active production (employment.ts :125-127, productionPeople.ts :4-14); `assertGreenlightStaffingIdle` throws for any engaged id in that set (productionAdmission.ts :210-220, called at actions.ts :336-338). No recast verb exists (grep `\brecast\b` in src/core: none; companion :346 "there is no recast verb"), so a take's cast is the greenlight cast. First-take timing: `startTick >= currentTick` skips the first advance (operations.ts :1658-1661); `remainingTicks` decrements at most once per advance (:1431, :1614, :1751-1783, every branch adds to `settled`); the take is the 5→4 branch only (:1680-1695); greenlight sets remainingTicks 8, startTick now (actions.ts :344-345, tuning.ts :59). Hence greenlight at g → take ≥ g+5 (WEEKS_TO_FIRST_TAKE :53 is a floor; setup holds :1709-1749 only delay).

Proof that t_k = t0 + 5k is a true upper-bound chain: take_k occurs while P_k is active; P_{k+1}'s greenlight g_{k+1} needs the person idle, so P_k left `activeProductions` at a week ≥ take_k (cancel action in that week at the earliest); take_{k+1} ≥ g_{k+1} + 5 ≥ take_k + 5. Cancel-after-take-then-greenlight-same-week yields exactly +5, so the bound is tight, never violated. A person on two running player pictures is unreachable through the only door (M16); a hand-authored save could carry it (validateSaveV30 has no exclusivity check — grep "exclusivity|already engaged" in save.ts: none) and both A and B under-count it; that is outside the lawful contract. RECORD-ONLY.

A is unsafe for remaining ≥ 2 (DEMONSTRATED on paper from source law): no running picture, from = w, remaining 2, due ∈ (w+10, w+13]. Lawful path: greenlight w → take w+5 → cancel w+5 (immune, take retained) → greenlight w+5 → take w+10 < due. A's k=1 event is w+13 (:364) → nMax 1 → false permanent BROKEN. Plan :341-343 admits "a valid hard upper bound" and :350-351 "a valid hard bound"; B is one, A is the quote hypothesis. A and B coincide for remaining = 1 (both reduce to t0 < due): every existing pin and every RED case (count-1, or count-2 with one take).

D1 (REFINE, demonstrated on paper): the note's `from = max(windowStartWeek, week)` fed to `expectedFirstTakeWeek` makes t0 pessimistic when the window has not opened. Counterexample: week w, windowStart w+2, due w+7, P lead on the cancelled greenlit-this-week picture. Lawful path: greenlight at w → take w+5 ∈ [w+2, w+7) qualifies (:629). Note's proof: t0 = (w+2)+5 = w+7 ≥ due → BROKEN. A future windowStart is player-authorable (attachPromise :492-521 takes the draft's windowStartWeek; :401 refuses only < startWeek). The physical clock must run from `week`; the window start is a floor because a picture may be held at ticks 5 (operations.ts :1683; companion :325). Correct t0 = max(windowStartWeek, expectedFirstTakeWeek(state, promise, week, 0)). Identical to the note whenever windowStart ≤ week (all pins).

Formula the writer must implement (boundary: t0 ≥ due → 0; week ≥ due → t0 ≥ due → 0):
```ts
const remaining = Math.max(0, promise.predicate.count - qualifyingTakes(state, promise).length)
if (remaining === 0) return null
const t0 = Math.max(promise.windowStartWeek, expectedFirstTakeWeek(state, promise, week, 0))
const due = promise.dueWeekExclusive
const nMax = t0 < due ? Math.ceil((due - t0) / WEEKS_TO_FIRST_TAKE) : 0
return remaining > nMax ? 'no filming week inside the window can reach that many pictures' : null
```

## Q3 — Relevance ruling: mask-seat relevance only (note §2.3)

Because the proof is a hard bound, the filter cannot change any outcome (an impossibility proven from the earliest lawful greenlight persists to due); it decides only attribution (cancel cause at `week` vs due cause at due). Evidence for the note's rule: 26 §2 :75-76 says "could satisfy this class" (class, not timing); plan :252 requires impossibility "after the studio action" (evaluated on the post-action state, not a before/after differential); the committed pin p14b1 :819-822 already rules "the meaningful claim this test pins is the STUDIO-CAUSED cancel producing the BROKEN outcome, not that cancelling is what tips it into IMPOSSIBLE by itself"; companion :378 "the causing event's receipt". The stricter filter would make p14b1 :833-836 throw at :835 (no outcome within one tick; due-week BROKEN lands at W+2), i.e., a natural-chain control edit outside this slice. Consequences: 2b, 4c, 6a, 6b BROKEN at the cancel; case 4 ×2 untouched (support seat outside the mask); p14b1 pin holds. The attribution question (a cancel credited with a pre-existing impossibility) is RECORD-ONLY for the Owner, not a defect.

## Q4 — The RED

Every expectation cites a plan/26 §2 sentence (:279, :299-303, :337, :350, :374, :389, :398, :416, :439, :477-478, :488, :494, :548). Under Q2/Q3 the 7 RED cases are correct in law: 1 (t0 = w+5 < w+8, nMax 1, remaining 1 each → untouched; due-week BROKEN both); 2a (generic cast, nMax ≥ 1 → untouched); 3 (lawful subject via validator :976-977, judged over generic cast, nMax ≥ 1 → untouched — correct under any reading); 4 ×2 (support outside mask → not judged; due-week BROKEN at w+2); 5 and 5-stale (remaining 2−1 = 1 ≤ 1 → untouched). The 9 GREEN cases are genuine current-law controls and stay GREEN under the correction (paper): 1b (cross-beneficiary, no portfolio rule), 2b/4c/6 ×2/6c (t0 ≥ due → BROKEN; 6c is the true before/after differential), 7a (immunity :747), 7b (termination), 8 (no seated beneficiary).

Nothing synthesized: takes come from the real 5→4 (:182-199), outcomes only from `advancePromisesWeek`/`tick`/actions. `bind` (:209-236) is lawful: it follows the committed p14b1 :795-818 precedent (root installed with the real employment row's contractId, "looked up, never assumed") and goes further by passing `makeSave`/validateSaveV30 (:238-240). It is NOT the outcomes-file `variant()` precedent (:253-271 edits a genuinely bound root's material); the 616 R-6 "never an invented binding" rule governs the natural rival seed's :438 fix, not labeled player-issuer fixtures. RECORD-ONLY: the placeholder receipt is FRAGILE on a bound root (not live-mintable, talentMarket.ts :1112-1114; validator-lawful :1008-1013; unread by owners); `fundTo` :95-103 writes a labeled ledger row.

Schedule transcription: `nMaxAfterCancel` (:75-79, A's cadence) is test-local arithmetic on exported constants, never the source's output; its values (:288 1, :318 1, :344 2, :360 0, :387 2, :410 0, :429 0, :471 1, :504 0, :535 0) coincide with B except :344/:387 (B gives 3), where the expectation needs only nMax ≥ 1. No assertion depends on A. The header :31-40 and :74 describe A as "the bound" — relabel in T2. Trivia: header :42 says ":755"; the literal is :756.

629-T2 follow-ups (after the writer): (1) case-5 negative: took-variant with P in SUPPORT on A, tagged lead count 2, window [21,38), cancel A, greenlight B with P lead at 30, cancel B → BROKEN at 30 with CANCEL_CAUSE (remaining 2 > nMax 1); (2) A/B separator: count-2 tagged lead, P lead on the greenlit-this-week picture, window [w, w+12) (any due in {w+11, w+12, w+13}), pre-cancel premise reclassify IMPOSSIBLE/PHYSICAL_BOUND, cancel → untouched under B (A would BROKEN at w); (3) D1 clamp case: count-1 tagged lead, P lead on the greenlit-this-week picture, windowStart w+2, due w+7, cancel at w → untouched (HEAD and the unclamped note both BROKEN it), then due-week BROKEN at w+7; (4) relabel `nMaxAfterCancel`/header as the quote-law estimate or add `ceil((due − t0)/5)`; fix ":755"; (5) optional direct pins on the exported helper (t0 ≥ due → 0; due = t0+5 → 1; due = t0+6 → 2). Note the tails of cases 1 and 4 (post-`untouched` due-week assertions) have never executed; a first failure there must be attributed (premise vs law) before blaming the writer.

## Q5 — Design vs RED consistency

Strings identical: CANCEL_CAUSE = :756, DUE_CAUSE = :703, TERMINATION_CAUSE = :729, reason :515 = :758. `untouched()` (:266-269, promises AND receipts toEqual) matches the design (no-break path returns `state`; each BROKEN appends exactly one receipt; nothing else). The RED imports only PROMISE_RULES_VERSION, WEEKS_TO_FIRST_TAKE, advancePromisesWeek, qualifyingTakes, reclassifyPromise (:52-54) — the new helper's export is optional; `reclassifyPromise` must stay exported and untouched (RED premises :296, :348, :386, :414, :474, :486, :508, :538, :540). Disagreements in expectation: none. In description: the note's §2.1 fragment is the A loop; the writer must use the Q2 formula, not the fragment.

## Q6 — Natural-chain impact: none

Grep over {src,tests,scripts,bridge,ui}: `breakPromisesOnCancel` only at src/core/index.ts :1405 (re-export), actions.ts :61/:599, promises.ts :744, and RED comments; `reclassifyPromise(` only promises.ts :753 and the RED. `kind: 'cancel'` in src only at types.ts and actions.ts; none in bridge/ (bridge cancels are `cancelInstallation`, bridge/office.ts :279); hollywoodTick/hollywoodPolicy/hollywood: no cancel; talentMarket.ts 'cancel' only in comment :1056. Rivals never reach the seam. Of the 625 control set (17 files, 625-seating-natural-chain-controls.txt), only p14b1-promises.test.ts contains a player cancel (:830), unchanged on paper (§3.1: t0 = W+5 ≥ W+2 → BROKEN at W). No test outside the RED pins either cancel string or calls the function. Reconciliation class none.

## Q7 — Writable/frozen and writer plan

Writable: src/core/promises.ts only — (a) new exported helper after :634 with the Q2 formula; (b) :735-761 doc comment and body per note §2.4 (issuer test, mask-seat test, proof on `state`, strings :756/:758 byte-identical); (c) :352 parameter narrowed to `Pick<PromiseDraft, 'issuerStudioId' | 'beneficiaryPersonId' | 'predicate'>` (ruled over the synthesised draft: private function, two callers :417/:427, type-sound against types.ts :2179-2233, zero runtime change). Frozen: index.ts, actions.ts, all other owners, validators, version literals, tests, fixtures. ONE cumulative `git diff HEAD --binary`. Self-check: RED 16/16, p14b1-promises 15/15, outcomes 23/23, typecheck exit 0. Conditions: C1 Q2 formula with the clamp, no SEAT_CYCLE_WEEKS, no loop; C2 mask-seat relevance only; C3 proof called on `state`; C4 helper doc comment states the bound presumes M16 one-seat exclusivity and fixed cast, is a floor on take weeks (hence a ceiling on count) and is deliberately looser than the quote law; C5 diff shows no hunk in :383-460, :673-733; C6 the parent records the stale actions.ts :591-595 comment for the next comment sweep (recommended) or authorizes a comment-only hunk explicitly — the writer must not touch it silently.

## Q8 — Verdict: RELEASE WITH CONDITIONS (C1–C6 above)

DEMONSTRATED (paper, from source law; not executed): D1 window-start clamp (false BROKEN under a lawful path, Q2); D2 alternative A false BROKEN for remaining ≥ 2 (resolved by choosing B). REFINE: none beyond C4 wording. RECORD-ONLY: actions.ts :591-595 stale comment; 26 §2 line drift; RED ":755"; RED A-cadence labels; DIRECTING_COUNT generic-cast semantic; FRAGILE placeholder receipt on labeled bound roots; synthetic double-seat and `from ≥ due` states outside the lawful contract; cancel-attribution question for the Owner.

## Rulings for the parent
- Cadence: B. `t0 = max(windowStartWeek, expectedFirstTakeWeek(state, promise, week, 0))`; `nMax = t0 < due ? ceil((due − t0) / WEEKS_TO_FIRST_TAKE) : 0`; BROKEN iff `remaining > nMax`, `remaining = max(0, count − |qualifyingTakes|)`, zero → null.
- Relevance: evaluable ∧ same issuer ∧ mask-seat on the cancelled picture; no take-inside-window precondition.
- :352: parameter narrowing (not the synthesised draft).
- Export: yes, module-level from promises.ts; no index.ts re-export.
- 629-T2: the five items in Q4.

## Sources read
Design note and 629-T report (scratchpad); RED test; baseline .json/.txt; P14B4-HEADLESS-PLAN.md :230-359; 26-live-cutover-implementation-map.md :60-99; P14-PREPARATION-COMPANION.md :315-388; 616-cutover-qualified-checkpoint.md :72-85; src/core/promises.ts :30-90, :186-470, :492-521, :580-770, :960-1084; actions.ts :286-355, :560-604, :2622-2643; employment.ts :110-139; productionAdmission.ts :195-229; productionPeople.ts :4-14; operations.ts :1420-1436, :1600-1624, :1640-1794; tick.ts :1090-1119; tuning.ts :55-62; types.ts :2176-2235; talentMarket.ts grep (:465, :548, :771, :1278, :1056); tests/p14b1-promises.test.ts :565-604, :770-841; tests/p14b4-cast-class-outcomes.test.ts :240-279, :420-509; greps listed in Q6.

## Evidence limits
Paper only: no vitest/tsc/node run; all "GREEN after the writer" and natural-chain claims are hand-derived from test text and source. The two counterexamples (D1, D2) are derived from the source's timing/busy law, not executed. Whether a 5-week greenlight→take path is constructible in the p13a fixture (the RED's setup-recipe route reaches the take 9 weeks after greenlight) is unverified; it does not affect the bound's validity (floors only tighten), only T2's ability to build a lawful two-take witness. Not an Owner acceptance; no merge instruction.