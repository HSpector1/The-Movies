# 600-B — contract-auditor review of 600-A (design note) and 600-T (test reconciliation)

Status: DONE (read-only). Nothing executed; no file edits. Every GREEN/RED statement below is a paper reading of committed test bodies and source at HEAD 39a64ef0 (source identity unchanged since 48a43511). Model observed: harness banner only; lean-ctx MCP tools were not available (Read/Glob/Grep only).

Verdict up front: **RELEASE WITH CONDITIONS** (conditions C1–C12 in the rulings block). The S1–S5 design implements exactly the evaluator-4 law of record 600 §2 / plan :18-40 with nothing smuggled from evaluator 5; the 600-T reconciliation weakens nothing and is lawful under 600 §3 step 2. The demonstrated defects are in the note's bookkeeping (a missed typecheck residual; acceptance rows written before 600-T landed), not in the law or the source design.

---

## Q1 — S1 implements exactly the evaluator-4 law: MET (paper), with two record-only observations

Verified against `src/core/promises.ts` at HEAD (line numbers unchanged since the note; all citations re-read):

- (i) Mask law: `seatedPreFirstTake` :262-267 currently uses `CAST_SLOTS` (:265); the design threads `promiseCastSlots(draft)` (:548-553, exported, legacy→3 seats / lead→lead / leadOrAntagonist→lead+antagonist) into it and into `expectedFirstTakeWeek` :335-348 (k=0 event) and `existingPath` :398. A fixed seat outside the mask therefore contributes no event 0 (falls to `from + 5`, :347) and no existing path. The recorded-take exclusion :263 (`recorded.has(p.id)`) is untouched. Matches plan :23-27 and :148-150.
- (ii) Buffer: :403 `X > nMax - promiseBuffer(nMax)` becomes `reserved + X > nMax - promiseBuffer(nMax)`; :395-396 (IMPOSSIBLE bounds, incl. `reserved + X > nMax` :396) and :406-411 (existing path already `reserved + X`; slack) stay. This is the amended plan's own sentence (:19-23, :169-173). The amendment's parenthetical "reserved plus requested exceeding the residual capacity" (plan :25-27) is ambiguous; the only reading consistent with §4.3.2 (:354-360) and the installed RED (`p14b4-cast-class-capacity.test.ts:246` eligible → FRAGILE at reserved 0 + X 1 > nMax 1 − buffer 1; `:270` withoutSelfExclusion → IMPOSSIBLE at reserved 1 + X 1 > nMax 1) is: `reserved + X > nMax` → IMPOSSIBLE (:396), `reserved + X > nMax − buffer` → FRAGILE (:403). The design uses that reading. RECORD-ONLY: parent to state it in the record (C12).
- (iii) Refusals: delete only :205 of `NOT_OFFERED_IN_B1` (:204-209); :206-208 (P3–P5) and the application :371-372 stay; two shape refusals inserted after :372 and before :373-378. P1 order unchanged (P1 is not in the map, count-only, so both new checks are skipped and :373 is reached exactly as today). P3–P5 count-only still refuse at :371-372 with their sentences (the bridge control `tests/bridge-p14b1-promises.test.ts:397` "a directing promise is not offered in this slice" is preserved). The classless-P2 refusal returns the same classification (IMPOSSIBLE) the deleted line returned, so freeze (`talentMarket.ts:1090-1092` `promiseNotFeasible`) and `breakPromisesOnCancel` :685 behave as today for a reader-admitted legacy P2 (plan :119-121). The deletion of the P2 NOT_OFFERED line is the authorized law replacement (26 §2 row 3, plan :27-28), not a refusal weakened to obtain a pass.
- (iv) Inputs digest: append `[kind, seatClass]` after the reservations row :323-325 only when tagged; every count-only tuple :305-325 stays byte-identical; lead vs leadOrAntagonist differ (RED `:326`). The productions row :309 already carries `p.cast`, so the mask reads no fact the digest omits (plan :124-126).
- (v) `attachPromise` :463-524: full detached copy replaces :495; `version: PROMISE_RULES_VERSION` :492 unchanged in text; tagged-non-P2 throw inserted after :474-478 and before :479 (the V30 validator :918-920 would refuse the staged state, so fail-loud is correct); nonofferable drafts still staged (no blanket refusal, 26 §2 row 2; huge cases `:345-365` and the `refused-p2-count-only-current-draft` fixture depend on it). At-most-one :474, ordinal id :490, redigest :507-516, no RNG: untouched.
- (vi) `settle` :571-593: `next: Partial<ProfessionalPromise>` :574 narrowed to the five outcome fields; callers :614-628, :631-636, :659-663, :686-690 pass exactly those. 26 §2 row 5 satisfied.
- (vii) `PROMISE_RULES_VERSION = 4` :42 with the law named and evaluator 5 named as later; lands only inside the S1 patch (record 23).

Nothing from evaluator 5 is present: no kernel/replay/adapter/enumerator import (grep of src/bridge/ui: `promiseCapacityKernel` is imported only by `promiseCapacityOwnerReplay.ts`), no UNCERTIFIED string, no cross-beneficiary reasoning. Nothing evaluator-4 is missing: "class-restricted fixed-seat paths" at evaluator 4 is exactly the mask on started pictures (`seatedPreFirstTake`); unstarted paths (Ready scripts, stock door) are class-agnostic because an unstarted picture can cast the person in any seat; the feasibility-contract items that need joint reasoning (plan :144-145 same-studio shared capacities, :148 inProduction dedup, :155-159) are evaluator-5 law under D1 (a) (note Q9 confirmed below).

RECORD-ONLY (parity artifacts accepted by D1 (a), "same minimum-countdown heuristic P1 has"): (a) a non-mask running seat is neither an event nor a person hold at evaluator 4, so `expectedFirstTakeWeek` treats the person as free for event 0; (b) `unproducedScripts` :231-237 counts `inProduction` projects as paths alongside the seated picture. Both pre-exist at rules 3 for P1 and are not part of the amended evaluator-4 definition.

REFINE: the note's own recommendation to mint the root in two typed branches (or a `mintPredicate` helper) rather than one literal against the union; the one-literal form compiles only through structural looseness against the V29 member.

## Q2 — S2 `authorRivalPromise`: MET (paper); disclosure hunks belong in S5

`talentMarket.ts:1260-1277` at HEAD authors P1/count 1 only (:1263-1268). The design's loop `isProven ? [p1] : [flexible, p1]` with `attachPromise` on the first REASONABLY_ACHIEVABLE read matches plan :199-207 (flexible P2/count 1 over the full proposed term, then P1; proven P1; attach first achievable only; neither → no attachment; no staged failed root; no lead-only selection, count escalation, gate or RNG) and the RED: `tests/p14b4-cast-class-policy.test.ts:413-416` (`candidates = proven ? [P1] : [FLEX, P1]`; `expectedCount = 2` only when unproven and the first read is not ACHIEVABLE), `:418` both reads on the identical input, `:421-423` read draft keys exactly `{family, predicate, issuerStudioId, beneficiaryPersonId, startWeek, termWeeks, windowStartWeek, dueWeekExclusive}` (no `promiseId`), `:476-477` attach draft exactly `{family, predicate, windowStartWeek, dueWeekExclusive}`, `:492` `root.feasibilityReceipt` equals the chosen read (deterministic re-evaluation inside `attachPromise` :479). `isProven` :680-683 is the existing archetype (`realProven` :42-47 in the test is the same formula). Windows: `[proposal.startWeek, startWeek + termWeeks)` ✓ (:1266-1267 today).

Disclosure (`DisclosedPromise` :458-464, `disclosedPromise` :535-547): `seatClass` is wire-visible at once through `bridge/people.ts:975` (`promise: unboxPromise(row.promise)`) into `StudioMarketOwnProposalSnapshot.promise` (`bridge-schema.ts:2250`, closed object :2219-2226). Ruling: the two disclosure hunks land in S5 with the schema change (F-d correct). `caseDisclosure` copy and the whole-`UNKNOWN` rival row (:526, schema :2260) untouched ✓.

## Q3 — S3/S4: MET (paper); D17 drivers included; fixture widening lawful

- `save.ts:6396` `LIVE_SAVE_VERSION = 29`, `makeSave` :6400-6405 → 30 / `validateSaveV30` :8679-8692 / `SaveFileV30` :499; validation-before-detachment preserved; `exportCurrentState` :6423-6425 and the dispatch :5282 need no edit; the 19 downgrade refusals and `migrateToV29` :8669-8673 (V30 arm :8670) stay. Doc-only refreshes at :497-498, :8712-8713, `index.ts:1292`, `types.ts:2231-2232`.
- Load consumers (grep, all present exactly as the note lists): `bridge/session.ts:41,136` (`converted` :135 keeps comparing to `LIVE_SAVE_VERSION`), `bridge/runtime/campaign-library.ts:5,48,127`, `ui/src/engine/adapter.ts:112,3796,3808,3822` (plan :137-138: serialization plumbing, not rendered-UI work; no other `ui/src` file references the save version).
- `runtime-checkpoint.ts`: import :6-7; `CurrentEnvelopeSave` :439; strict check :454-458 → V30 (26 §3 :107); prior chain :843-846 → `migrateToV30` (its re-serialisation :850 goes through `exportSaveJson` → `makeSave`, so the slot bytes equal `exportSave(migrateToV30(importSave(slot)))` for `makeSave`-minted slots — runtime47 `:194`); R05 annotation :992 type only, guard :993-994 not widened ✓; `migratePriorProtocol4Checkpoint` reused (two calls :984-987, reset :1008-1022).
- **F-a verified**: `loadBridgeRuntimeCheckpoint` :1268-1270 consults `SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` before the current decode :1281. Registering `584bdd…` while `SCHEMA_ID` still equals it would route every current checkpoint into migration. The registry line belongs in S5 (C3). After it: 35 entries, matching runtime47 `EXPECTED_PRIOR_IDS` :39-75 and the exclusion `:167`.
- Note Q4 (D17 drivers `src/harness/d16/run-d17b-continuation.ts:37,183`, `run-d17b-week86.ts:35,124`): both only read `.state` (:184-193, :124-125); root tsc passes untouched (`GameStateV29` is assignable to `GameStateV30`, `types.ts:2227-2229`). Ruling: INCLUDE the four tokens in S4. They are inside 26 §3's enumerated consumer list, hence inside the "coordinated core/save/runtime/wire cutover (26 §2–§4)" the Owner ordered; the parent's authorization is the "narrow consumer sweep" 26 §3 :100-101 asks for; behaviour is identical for every historical file and admits a tagged V30 file instead of failing loud. No separate Owner authorization needed (routine implementation detail, not a product conflict).
- Note Q5 (`src/harness/p14/legacy-v28-fixtures.ts:25,40`): `emit(file, save: SaveFileV28 | SaveFileV29, …)` receives `makeSave(state)` at :79, :104, :208 and is in the root include (`tsconfig.json:20` `src/**/*.ts`). Without the widening the root `tsc` fails. Ruling: lawful; a two-line type annotation is not a "rewrite" of the minter (26 §3 :102): the FROZEN header :13-17 and the `!== 28` guard :47 stay, behaviour unchanged, precedent 8bb57388 (C7).

## Q4 — S5 wire: MET (paper)

- Closed family-discriminated union per 26 §4 :129-134: member A `family: literal('LEAD_OR_SIGNIFICANT_ROLE_COUNT')` + required `seatClass: enumeration(['lead','leadOrAntagonist'])`; member B `family: enumeration(four others)` with no class; P3–P5 stay enumerated (engine-refused at :371-372). DSL/generator support verified: `scripts/bridge-contract-csharp.ts:660-667` (const or string enum domains), `:669-725` (shared REQUIRED property, pairwise-disjoint values; `count`/`windowStartWeek`/`dueWeekExclusive` are integers so `family` is the unique candidate → no `x-csharp-discriminator` needed, `:873-883`), `:906-914` (members must be named `$ref` objects — the design uses `reference()`), `:915-927` (promotion of shared properties to the abstract base). Precedent in the same file: `StudioMarketProposalSnapshot` :2262-2265. Runtime `anyOf` is first-match with all failures reported (`bridge/schema/runtime.ts:99-110`), so disjoint `family` domains make order irrelevant. DSL object types are mutable (`dsl.ts:34-38`), so the RED's `canonical.promise.seatClass = …` after `'seatClass' in` narrowing (`bridge-p14b4-cast-class.test.ts:196-197`) compiles.
- The eleven G1 cases (`:131-138`) stay refused for a real reason: missing class → A "required missing", B "expected one of"; bad class values → A enum, B additionalProperties; extra `kind`/`predicate` → additionalProperties; class on a non-P2 family → A literal, B additionalProperties. G2 ('P2 missing class', wrongly accepted today) → INVALID_COMMAND. All before mutation (`validateQuote`, :158).
- Own snapshot :2219-2226 and history row :2229-2241 gain `seatClass: nullable(enum)`; `promiseHistoryFor` (`bridge/promises.ts:93-103`) emits `null` for P1 and legacy P2 — the RED's `history()` helper `:326-331` requires exactly that key set (`toEqual`), and `:387` requires `seatClass: null` on the own snapshot. Preferences :2283-2291 gain `preferredOpportunity` from `publicPreferredOpportunity` (`talentMarket.ts:722-724`) through the one emitter `people.ts:986-990` (workspace `market.ts:223` reuses it; RED `:409-414`).
- ONE conversion `corePredicateOf` in `bridge/promises.ts` used by `promiseQuoteSnapshot` (:124) and `prepareMarketProposal` attach (`contract.ts:469-474`); `MarketPromiseDraft` :338-343 becomes the wire union; `copyMarketProposalDraft` :358-361 and `playerProposalDraft` :596 already spread the nested draft; the session clones and hashes the payload (`session.ts:1903,1910`), so lead vs flexible mint distinct intents (`:185`). Quote verdict shape unchanged (no class echoed).
- `PROJECTION_VERSION` :222 → 47; `protocol.ts` derives identity; generator (`scripts/generate-bridge-contract.ts:160-215`) owns exactly the three artifacts :190-203; no `--unity-project`. F10/F11 reference the WHOLE `BRIDGE_SCHEMA` (`tests/fixtures/bridge-contract-union-fixtures.ts:226-233`) so their pins (`bridge-contract-generator.test.ts:656-657`) and the projection-46 identity pins (`:554-560`) move together; the frozen F01–F04/F09/F12 aggregate (`:260-267`) does not reference `BRIDGE_SCHEMA` and must not move.
- Note Q1: `seatClass` spelling confirmed (the RED pins it at `:31,:100,:128,:326-331,:387`; 26 §4 :152 makes spelling routine). C# consequence (sealed DTO → abstract base + two sealed members + converter) is Unity backlog only (26 §4 :168-169). Note Q2: `preferredOpportunity` on the preferences snapshot confirmed (plan :190, 26 §4 :150-151, RED :409-415); `line` text unchanged.

## Q5 — Per-step plan and atomicity: MET, with corrections

F-a…F-e: F-a verified above. F-b: V29 branch `promises.ts:928` `exact(predicate, ['count'])` refuses tagged roots, so S2 (which mints them in natural chains) must follow S3 ✓. F-c: strict V30 at :454 requires `makeSave` at 30 ✓ (session save route → `exportSaveJson` → `makeSave`). F-d: row type `MarketPromiseHistoryRow = BridgeMarketPromiseHistoryRow` (`bridge/promises.ts:38`) and `DisclosedPromise` flow to the wire ✓. F-e: 26 §3 :123 governs what is exposed; record 600 §3 step 3 already records the reading "per-step patches for bisectable commits; the whole set lands as one pushed boundary", and the Owner adopted record 600. Ruling: five commits are lawful; S3+S4+S5 need not be one commit (C5 conditions). Order S1 → S3 → S4 → S2 → S5 is correct.

Designated typecheck residuals (note Q6): the four core files verified (`tests/contracts/cross-owner-refusal.contract.test.ts:70-74`, `phase-table-agreement.contract.test.ts:102-106`, `determinism-floor.contract.test.ts:84,125`, `tests/legacy-parcel-ground.test.ts:343-356`) and `tests/bridge-p06-checkpoint-recovery.test.ts:54` (`after: SaveFileV29`) called at `:167,:174` ✓. **DEMONSTRATED omission: `tests/save.test.ts:229-233` `function wellFormedSave(): SaveFileV29 { … return makeSave(state) }` is in the root include (`tsconfig.json:20`, not excluded by :24-26) and becomes a fifth root diagnostic after S3** (its dependents :279, :293, :300 are typed from it). Grep of every `SaveFileV29` in `tests/` finds no other `makeSave` consumer; src/bridge/ui have none beyond the design's own sites. C2 restates the acceptance.

## Q6 — Paper effect spot-checks (eight+), all consistent with the design; one class NOT VERIFIED

1. Bridge G1 offerability (`current-p1`, t-act-09, window [52,92) read at 45): from 52; nMax 5 (52+8k+5 < 92 → k ≤ 4); buffer 2; reserved 0 (promise-0 is on a withdrawn proposal, `contractId: null`, so not a reservation under :274-283); existingPath ≥ 1 (legacy development → stock door :249-258); slack 35 → REASONABLY_ACHIEVABLE. cap16/17 windows [52,80+i): nMax 3, buffer 1, residual 2, slack ≥ 23 → all offerable (`:299-321`). Whole-quote refusal: due 105 > 104 → `:376-377` (`:217-220`). ✓
2. Capacity matrix (`:237-253`): eligible → `seatedPreFirstTake` finds the film (remainingTicks 5) in a mask seat → event 0 at tick+1 < due tick+2 → nMax 1; reserved 0 (self-excluded); `1 > 1−1` → FRAGILE "the schedule leaves no spare picture inside the window" ≠ UNKNOWN_CAP ✓. Ineligible → no mask seat → event 0 = tick+5 ≥ due → nMax 0 → IMPOSSIBLE "no filming week…" (an actual bound) ✓.
3. Joint (`:255-279`): three FRAGILE; `withoutSelfExclusion` → reserved 1 + X 1 > nMax 1 → IMPOSSIBLE "promises already made…" (:396) ✓; tick → one take, all three SATISFIED by mask; `live(actual)` at V30 validates masks (:979-984) ✓.
4. Revise/withdraw/nonoverlap/digests (`:289-343`): `w.opened` read → nMax 5, reserved 0, existingPath ≥ 2 (two Ready scripts, managed development), slack 35 → ACHIEVABLE ✓; self-excluded read equals baseline (inputs carry no `promiseId`; reservations row unchanged) ✓; nonoverlap root at `dueWeekExclusive` excluded by `windowStartWeek < draft.dueWeekExclusive` :282 ✓; lead vs flex digests differ only through the appended row ✓.
5. Huge (`:345-365`): :376 refusal precedes the nMax loop; `Number.isSafeInteger(MAX_SAFE_INTEGER)` admits the staged root at V30 (:841); unbound root skipped by `evaluable` :544-546 ✓.
6. Save-v30 `:156` `makeSave(migrated.state)` toEqual `migrated`: holds iff `broadcastCache` deep-equals `state.broadcastItems` and seeds match — true by provenance for `makeSave`-minted V29 files and the synthetic variants derived from them (note Q12: sound inference, not executed).
7. Runtime47 two-slot (`:185-205`) and reset/reopen (`:207-234`): slot bytes from :850 equal `exportSave(migrateToV30(importSave(slot)))` for the same reason; `SCHEMA_ID ∉ registry` → no re-migration ✓.
8. G8 (`:381-397`): `promiseHistoryFor` → `null` for P1 and for the reader-admitted legacy P2 variant (validated V29 first, :395) ✓; own snapshot of `refused-p2-count-only-current-draft` reads the STORED receipt (:545), no re-evaluation ✓.
9. Outcomes `realPlayerPromise` (`:303-332`): attach at start−7 over [start,start+40): nMax 5, reserved 0, existingPath 1 (stock; legacy development per record 17), slack 35 → ACHIEVABLE, version/rules 4 ✓; binding at `start` is the same natural premise B-F2 :195-199 relies on (UNEXECUTED).

Predicted GREENs I cannot confirm (not wrong, NOT VERIFIED): (a) policy D3 ×2 `:286` and trust-chooser test 6 `:405/:458` — ACHIEVABLE for the RIVAL incumbent's LEAD/FLEX at week 207/208 needs a mask-qualified or class-agnostic rival existing path (rivals have no stock door :250; a rival Ready script counts for either class, a support seat on a running rival picture would count for P1 only). Plausible, unproven; a miss is a fixture finding. (b) B-F2 `:195-199` and `realPlayerPromise` binding: under S2 a rival can author FLEX P2 for an UNPROVEN subject and earn D3 (`bandsFor` :797-802) where the player's P1 earns none, so "the player wins at windowStartWeek" can move if the subject is unproven — a G9-class moved premise (Q7), not a residual-buffer one. (c) G9's four witnesses and G10's nine natural cases.

Arithmetic correction to the note §4.1 G3: the policy fixture's three nonpair drafts have windows [415,416) (`:232-233`) which do not overlap [208,415) (`:282` half-open test), so reserved is 0, not "≤ 3"; the conclusion (no move at a 207-week window) stands.

## Q7 — Moved premises and the sweep: LAWFUL, with the G9 class recorded separately

(a) Sweep 29→30 by the TEST-AUTHOR, values only, own commit, after the writer: lawful and preferable to a parent sweep (plan T2 :396-400 "tests remain test-author-owned"; 600 §4 one production writer; precedent 6948e31). It must run BEFORE the parent's step-6 record-check of the controls, because `validateSaveV29(makeSave(...))` sits inside shared fixture helpers (`tests/helpers/p14b2-fixtures.ts:122,210,225`, `p14bf2:69` inside `fixture()`) and would turn every consumer RED at construction. Each grep hit classified live pin vs historical read (`bridge-p14b4-runtime47:143-144` and `bridge-p14b2-checkpoint:18-19` are historical and stay). (C9)

(b) Meaning pins reconciled from evidence, never loosened: byte-identity pins (`p14bf2:303`, `p14b3-rule-revision:109`, `bridge-p14b2-checkpoint:34-37`) re-express the same invariant as "writer output equals the governed migration of the raw" (runtime47 `:194` pattern) — lawful under T2. Generator pins: explicitly the test-author's under 26 §4 :170-173.

The S2-driven movements (rival FLEX P2 for unproven persons changing D3 opportunity, material digests, `rivalFixture` timing, poaching premise, and the B1 natural-authoring pins) are NOT "natural-chain reconciliation for residual-capacity behavior": they are a distinct class caused by the delegated rival-order hypothesis (plan :199-207; 537-B G9; 600 §3 step 3 names it in the design scope). Ruling: no separate Owner authorization is required. The causing law is Owner-ordered implementation ("Execute the coordinated P14B.4 … cutover", 600 §1 step 3, whose recorded scope in 600 §3 step 3 includes G9); plan T2 :396-400 grants independent explicit reconciliation for ANY legitimate moved current-law premise; precedent 554/556 applied exactly that to D3-driven winner/fixture movements without an Owner ruling. Conditions (C10): record the class by name, re-derive from receipts with the proven/unproven fact observed, re-express purposes rather than drop them (e.g. `p14b1-trust-chooser.test.ts:691-692` "first actor … P1" becomes "P1 if proven / FLEX if unproven"), never loosen, and state in 600-R which natural-chain winners moved so the Owner sees the launch-behaviour change the plan itself flags as "subject to expansion review" (:187).

Precision corrections to the note's inventory: the first stop under S2 in both trust-chooser test-7 cases is the spy's draft assertion `:570-575` (`family: 'APPEARANCE_COUNT'` on every rival authoring read), before `:641-648`/`:691-692`. Test 6 `:360` cannot move: FLEX ACHIEVABLE implies P1 ACHIEVABLE for the same rival at a 208-week window (mask subset → existingPath and nMax no larger; slack no better), and today no rival P1 is authored for that subject by 207, so no FLEX will be either.

## Q8 — 600-T reconciliation: LAWFUL; no defect to fix before the writer

Diff (`600-T.patch`, four files) plus the new file: only 3→4 on FRESH pins (`p14bf2` :98,:120,:133,:151,:193-194,:198,:234,:325,:332; `p14b3` :98,:144-147,:180), titles, header notes, three comment lines above unchanged `!== UNKNOWN_CAP` assertions, the conflicting-claim case removed from the live file and reproduced verbatim (`evaluator5.test.ts:236-245` vs HEAD `:279-288`), and test 6's P1 → tagged FLEX attach with two added assertions (`:393-399`). `p14bf2:234` (`kept version 4`) is the same fresh root minted at `:191`, so it is a fresh pin outside 600 §3 2(a)'s enumerated lines but inside its class — correct. Historical pins (`p14bf2:271,:284-285`; `p14b3:61,:77,:112-113,:195-196`; runtime47 `:150-155`) untouched. No classification, bottleneck, digest, fixture, seed, cap or timeout changed. No `@ts-expect-error`; the typed local `CastRoleCountPredicate` is assignable to today's `{ count: number }` and to the union (run 4 typecheck clean).

Evaluator-5 disposition: a separate file with the case LIVE and designated, `pureRead` pin kept at 4, plus an `it.todo` naming the certified-bottleneck intent — matches 600 §3 2(b) ("separate evaluator-5 file … never a weakened expectation; the shared `rulesVersion 4` pin (:201) stays") and D2 (i-c) (revisit at the evaluator-5 design; the pin moves then). Post-cutover it fails at `:239` (scalar FRAGILE vs expected IMPOSSIBLE), the informative line.

Trust test 6 vs records 110/554: same seed (`p13-public-commercial-adoption`), weeks (195/196/207/208), tie shape (1.25 vs 1.0, equal terms, standing band, both Reliable, three nonpair IMPOSSIBLE P1 drafts at [415,416)), winner meaning (baseline player on compensation `:452-453`; treatment incumbent on opportunity `:454-455`); the opportunity is now a real `leadOrAntagonist` P2 that `promiseMatchesPreferredOpportunity` accepts for the unproven subject (`talentMarket.ts:736-738`). Faithful to 110's "independently migrate this designated D3 proof to real tagged P2 at coordinated activation".

601 baseline (`601-reconciled-red-baseline.txt:6,25,54,57,69,508-509`): 6+14+1+3+1 = 25 failed, 7+0+0+3+10 = 20 passed, 1+2 = 3 todo — consistent with runs 1–3 (9/10, 15/0/1, 1/10/2); `fixedSource: true` on 5e6ac5dc; the 601 patch includes the new file (`:277-278`), which is why its SHA differs from `600-T.patch` (tracked diff only). Stop points as reported (`:8,:27,:41,:56,:72`).

## Q9 — Frozen list: complete and correct

Nothing writable that must not be; nothing frozen that the cutover needs (no new `index.ts` export is required: bridge imports `talentMarket.ts`/`promises.ts` directly and `index.ts:1293-1296` already exports the V30 API; `SaveFileV30`/`migrateToV30` comments are outside the frozen function ranges). No cap, tariff, timeout, historical fixture, replay module (`promiseCapacityOwnerReplay.ts`) or stale test (`p14b4-ready-replay-stale-target.test.ts:234`) is touched. The only refusal-string change is the authorized replacement of the P2 NOT_OFFERED line.

## Q10 — Remaining note questions

- Q3 (UI adapter arms): in scope; plan :137-138 and 26 §3 :98-99; three sites + import; no other `ui/src` reference.
- Q7 (S2 after S3; no live producer mints a V29-unsaveable root once S1–S5 land): confirmed — grammar refuses classless P2/misplaced class before mutation; core attach throws on tagged non-P2; V30 admits exactly what attach mints.
- Q9 (plan vs 26): follow the amendment; 26 §2 row 3 reads as the class-aware scalar; plan :144-145/:148 (shared capacities, dedup) are evaluator-5 law; the `existingPath` formula stays as at rules 3 apart from the mask (record the parity artifacts above).
- Q10 (`breakPromisesOnCancel` + classless legacy P2): record-only; same classification, new sentence; the coupling itself is 600 §3 step 8.
- Q11 (sentences): routine; meanings fixed, wording free; the quote message reaches only the issuer's own client (`bridge/promises.ts:134`), never Pulse. REFINE: "count-only P2" is engineering jargon on a player-facing line; e.g. "a seat-class promise needs its seat class selected (lead, or lead-or-antagonist)".
- Q14 (generator timing): confirmed — writer's S5 patch frozen → parent's `npm run generate:bridge-contract` (no `--unity-project`) → `check:bridge-contract` clean, `check:bridge-contract:fixtures` unchanged → hash the whole candidate → test-author repins F10/F11 and the projection-46 identity to actual bytes.
- Q15 (natural premises): agreed; a miss is a fixture finding for the test-author, never license to synthesize.

## Q11 — Verdict: RELEASE WITH CONDITIONS

DEMONSTRATED (defects in the note's bookkeeping, not in the law):
- D1. Note §6 Q6 / §3 S3 row: the root-tsc residual list omits `tests/save.test.ts:229-233` (fifth `SaveFileV29`-typed `makeSave` consumer in the root include).
- D2. Note §3 S1/S3 acceptance rows predate 39a64ef0: after 600-T, at S1 (V29 writer still live) `p14bf2` 13/13 and `p14b3` 6/6 are expected GREEN (pins now 4), the live capacity file has 14 cases (13 stopping at the V29 writer, "class digests" GREEN), the evaluator-5 file stops at the V29 writer; at S3 the capacity file is expected 14/14 and the evaluator-5 case RED at `:239`.
- D3. Note §4.2 trust-chooser row: the first S2 stop is `:570-575` (both test-7 cases); test 6 `:360` cannot move; §4.1 G3's "reserved ≤ 3" is 0.

REFINE (preferences): two-branch/typed-helper root mint in `attachPromise`; plain-English classless-P2 quote sentence.

RECORD-ONLY: the amendment's "residual capacity" reading (C12); evaluator-4 parity artifacts (non-mask running seat is neither event nor hold; inProduction script counted as a path); Unity backlog (abstract draft base + converter; class selector; preference/legacy display; V30/47 loading); the G9-class movements as a named reconciliation class with Owner visibility.

NOT VERIFIED: every paper GREEN that rests on a natural premise (Q6 list); root/UI/bridge `tsc` cleanliness at each step; that no additional bridge-tsc diagnostic appears at S4.

---

## Rulings for the parent (each one checkable)

- C1. The writer's S1 acceptance is: `p14bf2-acting-discipline` 13/13 and `p14b3-rule-revision` 6/6 PASS; `p14b4-cast-class-capacity` "class digests" PASS and the other 13 stop at `validateSaveV29 … predicate.kind` / `live()`; `p14b4-cast-class-capacity-evaluator5` stops at the same V29 refusal; `p14b4-material-evidence-core` 17 and `p14b4-d3-matching` 8 unchanged; any other failing line is a movement to report, not to fix.
- C2. After S3 the root `tsc` may report diagnostics only in these test lines: `tests/contracts/cross-owner-refusal.contract.test.ts:70-74`, `tests/contracts/phase-table-agreement.contract.test.ts:102-106`, `tests/contracts/determinism-floor.contract.test.ts:84,125`, `tests/legacy-parcel-ground.test.ts:343-356`, `tests/save.test.ts:229-233`; after S4 the bridge `tsc` only `tests/bridge-p06-checkpoint-recovery.test.ts:167,174`; the full `tsc` output is archived and any diagnostic in src/bridge/ui is a defect.
- C3. The `584bdd…` → `projection-v46` registry line is in the S5 commit together with `PROJECTION_VERSION = 47` and the schema change, never in S4.
- C4. `DisclosedPromise.seatClass` and the `disclosedPromise` hunk are in the S5 commit, not S2.
- C5. The five commits are pushed in one `git push`; each of S1–S4's commit messages states it is inside the record-600 cutover boundary and not a standalone runnable checkpoint; no record-check, generator or native run is made against an S1–S4-only tree beyond the writer's listed self-checks.
- C6. S4 includes the four `migrateToV29` → `migrateToV30` tokens in `src/harness/d16/run-d17b-continuation.ts:37,183` and `run-d17b-week86.ts:35,124`.
- C7. `src/harness/p14/legacy-v28-fixtures.ts` changes exactly two type-only lines (:25 import, :40 parameter); header :13-17 and guard :47 are byte-identical.
- C8. The generator runs once by the parent after the S5 patch is frozen; `npm run check:bridge-contract` is clean; `npm run check:bridge-contract:fixtures` and `tests/fixtures/bridge-contract-union-fixtures.ts:260-267` are unchanged; the test-author repins `bridge-contract-generator.test.ts:554-560,656-657` to the actual new bytes, never self-equality.
- C9. The 29→30 sweep is a test-author commit, values only, landed after the writer's five commits and before the parent's step-6 record-check of any control; it includes `tests/helpers/p14b2-fixtures.ts`, `tests/_historicalCurrent.ts` and every `validateSaveV29(makeSave(…))`/live-pin site, classifies each hit as live pin or historical read in its record, and excludes the meaning pins (`p14bf2:303`, `p14b3-rule-revision:109`, `bridge-p14b2-checkpoint:34-37`, generator pins).
- C10. Movements caused by S2 (rival flexible-first authoring) are reconciled by the test-author under plan T2 as a named class distinct from residual-buffer movements, re-derived from receipts with the proven/unproven fact observed, re-expressing each test's purpose (P1 for proven / FLEX for unproven) rather than dropping it; a missing natural witness is recorded as a fixture finding; the 600-R record lists which natural-chain winners moved.
- C11. The classless-P2, tagged-non-P2 read refusals and the attach-time throw keep the stated meanings; the deletion of the P2 `NOT_OFFERED_IN_B1` line is recorded as the authorized law replacement.
- C12. The record states the reading of plan :25-27: `reserved + X > nMax` → IMPOSSIBLE (:396), `reserved + X > nMax − buffer` → FRAGILE "no spare picture" (:403).

## Sources read (line ranges)

Scratchpad: `600-A-design-note.md` 1-765 (whole); `600-T.patch` (whole); `600-T-report.md` (whole); `600-T-run4-typecheck.log`. Records: `600-owner-ruling-578-reopened-program.md` (whole); plan `P14B4-HEADLESS-PLAN.md` 1-290, 380-416; `26-live-cutover-implementation-map.md` (whole); `23-…` (whole); `17-…` (whole); `537-…` 1-240, 380-570; `110-…` 30-59; `554-…` 25-59; `578-…` (whole); `601-reconciled-red-baseline.txt` 1-80 + summary lines, `.json`, `.patch` headers. Source: `src/core/promises.ts` 1-700, 800-1049; `src/core/types.ts` 2170-2239; `src/core/save.ts` 490-505, 6385-6439, 8598-8722 + grep of V30 sites; `src/core/talentMarket.ts` 450-549, 675-814, 1045-1134, 1245-1284; `src/core/index.ts` 1286-1297, 1394-1428; `src/core/promiseCapacityOwners.ts` (grep); `bridge/promises.ts` 1-143; `bridge/contract.ts` 330-369, 436-485, 583-599; `bridge/people.ts` 36-45, 966-1002; `bridge/session.ts` 128-141, 1894-1927 + grep; `bridge/runtime-checkpoint.ts` 1-70, 160-179, 432-465, 836-855, 968-1027, 1238-1282; `bridge/runtime/campaign-library.ts`, `ui/src/engine/adapter.ts` 3776-3805 (+ grep); `bridge/schema/bridge-schema.ts` 200-225, 1724-1795, 2215-2294, 3255-3270, 3468-3481; `bridge/schema/dsl.ts` 1-138; `bridge/schema/runtime.ts` 90-119; `scripts/bridge-contract-csharp.ts` 650-729, 830-929; `scripts/generate-bridge-contract.ts` 156-217; `src/harness/p14/legacy-v28-fixtures.ts` 1-110; `src/harness/d16/run-d17b-continuation.ts` 176-195; `run-d17b-week86.ts` 118-133; `tsconfig.json`, `tsconfig.bridge.json`. Tests: `p14b4-cast-class-capacity.test.ts` (whole); `p14b4-cast-class-capacity-evaluator5.test.ts` (whole); `p14b4-cast-class-policy.test.ts` 1-532; `p14b4-cast-class-outcomes.test.ts` 60-359; `p14b4-save-v30-compatibility.test.ts` 100-199; `bridge-p14b4-cast-class.test.ts` (whole); `bridge-p14b4-runtime47-compatibility.test.ts` 1-40, 76-236; `p14bf2-acting-discipline.test.ts` 55-99, 160-209, 290-339; `p14b1-trust-chooser.test.ts` 286-460, 548-708; `bridge-p14b2-checkpoint.test.ts` 14-43; `tests/helpers/p14b2-fixtures.ts` (grep); `bridge-contract-generator.test.ts` 548-565, 648-661; `tests/fixtures/bridge-contract-union-fixtures.ts` 220-269; `tests/save.test.ts` 224-303; `tests/contracts/*.contract.test.ts` cited lines; `tests/legacy-parcel-ground.test.ts` 340-359; `tests/bridge-p06-checkpoint-recovery.test.ts` 50-59, 160-179; `tests/bridge-p14b1-promises.test.ts` 224-247, 380-399; `tests/p14b1-leak.test.ts` 88-109; greps across tests/src/bridge/ui for `SaveFileV29`, `GameStateV29|ProfessionalPromiseV29`, `saveVersion [!=]== 29`, `migrateToV29|LIVE_SAVE_VERSION`, `DisclosedPromise|promiseHistoryFor`, `not offered in this slice`, `caseDisclosure(`.

## Evidence limits

Nothing executed: no `tsc`, vitest, generator, gunzip or git. Fixture facts (t-act-09 holds no other reservation; `makeSave` provenance of the genuine corpus and the projection-46 checkpoint; rival development mode at week 207) are taken from the note, the provenance pins in the tests and record 17, not observed. Paper `nMax`/buffer arithmetic uses `WEEKS_TO_FIRST_TAKE 5`, `SEAT_CYCLE_WEEKS = TUNING.PRODUCTION_TICKS` (8 per the note; `tuning.ts` not re-read), `promiseBuffer` :63-65. The 601 baseline and the 600-T logs were read, not reproduced. TypeScript assignability claims (union widening, `in` narrowing, discriminated-union excess-property behaviour) are by reading the types, not by compilation. No native/Unity, usability or Owner-acceptance claim; a rendered mockup or green suite would not be one either.