# 113 — next real-owner trace tests: bounded fixture matrix

2026-09-20. INERT preparation only. No producer API or executable test is proposed;
no runtime, probe, typecheck, Git/network, kernel implementation inspection, live
edit, fixture generation or delegation occurred. Parent owns the next producer
interface/implementation. This is not owner-adapter or B4 acceptance.

Read current `next-owner-extraction-notes.md` including its post-clock section,
frozen89/91/94, full installed77 clock tests and actual A/W/B regression, plus the
bounded fixture bodies named below. Frozen authority hashes remain89 `9b484385…bbc6`,
91 `7c0a75a8…f7f7`,94 `cbbd9376…a025`. No mutable detached solver was consulted.

## Provenance categories

- **Real-action fixture:** existing actions/ticks emit the productions, projects,
  bindings and dated history. Preserve its disclosed bootstrap; this is not an
  automatic claim of ordinary natural player play or a newly validated save.
- **Controlled owner input:** explicit clock/workflow/capacity/layout or consumer
  setup for a narrow owner law. Never relabel it a genuine admitted campaign or
  pass it off as a lawful fully staffed trace.
- **Unconstructed next proof:** a source-backed route exists, but the exact new
  conjunction/observation has not been built or executed. Retain a hard premise
  failure and honest incomplete coverage rather than forge missing facts.

## Next discriminants and exact available routes

| # | Owner obligation / required distinction | Existing route and evidence limit |
|---|---|---|
| 1 | **Mandatory priority and one full context.** Preserve A/W/B waiting order3/2/1, countdowns7/4/7: after W wraps, restart gives the stage to A, not B. Trace must keep the waiting/uncredited paths and ordered release-before-grant facts; a nonoverlapping W→B→A splice is still not this owner execution. | `tests/c2a-m4-release-law.test.ts`, “gives capacity released MID-SWEEP…”: original fixture clones ONE production template, rewrites IDs/start/countdowns and hand-builds workflows; it is a CONTROLLED allocator regression, not three legally staffed engine productions. It can anchor a detached narrow-clock owner check without fabricating a new full Production. `_m4Fixtures.ts::contendedStudio(seed)` supplies four actually commissioned/accepted scripts and two actual greenlights for real-context follow-up. A genuinely admitted three-picture instance of the exact A/W/B clocks/rosters is NOT yet constructed. Do not reuse the cloned shared cast as staffing proof. |
| 2 | **Same-state joint replay versus solo projection.** Replay all current pictures and relevant background together; compare actual ordered sink releases/grants with initial/final reservations. Two legal branches must have distinct trace identities; never concatenate dates, locks or holds from their solo runs. Retained slots produce no spurious release/regrant. | Installed `tests/p14b4-managed-clock-owner.test.ts` (`greenlit(true)`, `paired`) provides two real greenlights and full/narrow equivalence with fresh collectors. Its `operationsStudio('p13a-production-consumer')` uses historical-control founding and real hire/found/activate actions; script development remains legacy. These are actual productions under a disclosed controlled founding route. The new multi-week contention/trace extraction assertions remain unexecuted;77 alone is not a forward-domain proof. |
| 3 | **Caller-specific command order.** Player due-scenery arrival only reaches READY; absent a subsequent schedule command the same sweep must not invent a5→4 take. Rival assign/abstract-clear/schedule occurs before its sweep. Queue admission remains after the player sweep; no same-step greenlight work or arbitrary queue admission order. | `tests/p14b1-first-take.test.ts::buildScheduledPlayerProduction` supplies public-action player scheduling; its rival test searches `p13aGeneratedStudio()` up to60 ticks for a real rival countdown5, then ticks to4. `tests/p05a-w1-scenery-truth.test.ts::pictureBeforeCall/callDirector/inTransit` supplies the actual scenery classifier/action sequence, but `inTransit` HAND-MOVES property geometry and is a CONTROLLED layout test.77's direct `clearSceneryLoadIn` is owner-unit behavior, not player geometric transport. A genuinely action-moved far-stage campaign plus producer command-plan branch is still an explicit construction obligation. |
| 4 | **One arrived first-take time, not shooting entry or sweep time.** At pre-week w, entry6→5 may lock technology but earns no take; scheduled5→4 yields the durable event at w+1. Verify production/studio/director/exact cast joins for player and rival, no duplicate on the next tick. Internal ordered steps must reconcile reservation events at pre-week, setup admission at arrived-week and the take at arrived-week. | The PLAYER case in `p14b1-first-take.test.ts` already pins `take.week === scheduled.market.tick + 1` and exact identities; the RIVAL case supplies a natural generated-industry witness but does not itself pin that week equality. Add the rival week assertion in the future adapter test without assuming a magic week. `retentionFixture().scheduled/kept` also has an exact actual week+1 take and strict-save provenance; it carries a disclosed30m cash/ledger bootstrap and real contracts/ballroom work. None of these dates may be restamped to the sweep argument. |
| 5 | **Wrap resources versus person release.** At completed shooting, stage and Set occupancy end before blocked Post; retained Set/setup facts remain historical, and wear occurs exactly once. Director/cast/craft person occupancy continues until actual legal release/commit boundary, not until stage wrap. Countdown1 alone does not authorize a commitment or earlier person reuse. Unknown release retains path-owned coverage through computed H. |77 has a real scheduled take followed by a detached occupied-Post owner input, and a separate actual `commitPictureToRelease` route. `tests/p14b2-setup-wrap-regressions.test.ts::wrapped()` uses real `retentionFixture()` take→wrap/Post with unchanged setup and strict roundtrip; its blocked-Post case uses real script/production actions with a disclosed one-slot capacity override. The extra proof that people remain unavailable throughout blocked Post and uncommitted Release Ready must be newly observed, not inferred from stage bindings. Use the common release-refusal owner; do not hand-mint commitment rows. |
| 6 | **Technology/setup/genre/Set state is branch-local and evolves jointly.** Every branch/sweep gets a fresh actual technology/event collector; all pictures within that sweep share it. Carry returned technology/Set wear only within that branch. Real genre lookup, exact stage/Post chain, setup route provenance and current installation/equipment facts remain present. Admission credits zero setup work; later admitted weeks credit once. Branch B must not inherit A's lock/wear/setup or re-read one stale live root forever. |77 pins silent entry lock and input purity, but no nonempty setup or sound branch. `retentionFixture()` and `p13b-r07-timeline.test.ts::conventionalBallroomAtRehearsal` supply real selected setup/ticks with disclosed cash bootstrap. `p13a-production-technology.test.ts::installConsumerFixture` is explicitly SYNTHETIC technology-access provenance plus controlled completion and is NOT a genuine research/adoption save route. A real available sound route is `p13a-technology-validation.test.ts` setup: `p13aGeneratedStudio('p13a-validation-purchase')`→actual ticks416→purchase→`adoptExistingSoundChain`→actual ticks428, with `makeSave` in that existing test. The R07 lighting timeline uses real research/adoption/Set build, with disclosed funding. Both are longer routes: exact new production staffing and cross-branch conjunctions remain unconstructed; no acquisition flag may be fabricated to shorten them. |
| 7 | **Writing work versus completed credit.** Draft/Rewrite occupy every pooled writer through persisted due/completion; Review/Ready do not continue that assignment. A Ready screenplay credited to writer W may be greenlit while W truly drafts another screenplay, subject to all other real crew/contract/casting/capacity laws. Commissioning's broader busy predicate must not be silently substituted for greenlight's. | `_p04a2WriterCreditFixtures.ts::buildScenario(SEED)` exports `readyOnly` and `deadlock`: real commission A→review→accept, then same writer drafts B; historical-control founding, no hand-edited contracts/cash in that builder. `tests/script-projects-actions.test.ts`, seed `script-actions-lifecycle`, supplies real Draft→Rewrite→Review→Ready and persisted due values. `tests/c2a-m3-rename-and-pooling.test.ts`, seeds `m3-pool-speed`/`m3-pool-busy`, uses real `commissionOriginalScreenplay`/`assignScreenplayWriter` and checks the complete writer pool/due update. No detached adapter may release only the credited first writer or recalculate an already-persisted due from today's profile. These are real-action controlled-founder routes, not natural-roster guarantees. |
| 8 | **Ready/queue/production identity is one path, not two opportunities.** From actual Ready package, capacity-only refusal creates a queued intent without new production, money, ledger or crew commitments; admission links the real project and production. An inProduction screenplay cannot also remain an unstarted background opportunity or yield a second take. Preserve all-role uniqueness, actual has-discipline/contract/free-agent eligibility and credited-writer exception. | `tests/p04a3-greenlight-law.test.ts` uses `buildScenario` plus an explicitly controlled one-development-slot override to prove queue/no-commit and the same free-slot immediate admission. `script-projects-actions.test.ts` asserts the actual `project.productionId`, inProduction status, assessment and later cancellation/Produced history. `_m4Fixtures.ts::contendedGreenlightStudio` additionally supplies Ready/audition evidence with real free crew. Queue-capacity controls are not new owner history; actual linked IDs must be read from results, not guessed or minted by the planning adapter. |
| 9 | **Full external/context ledger.** Installation, active script, casting and Set work must all occupy their real slots in the relevant sweep; uncredited background cannot disappear. Initial holds and exact sink release/grant sequence must explain the final reservations without merging alternate traces. A fixed hold's owner path and immutable prefix survive every permitted suffix release. | Use the real active-writing state from row7 and actual production/Set state from rows2/5 as the first bounded contexts. Existing notes identify the player union and post-load-in Set read. A SINGLE engine-admitted conjunction with installation+writing+casting+Set work has not been constructed here; do not splice independently lawful snapshots. Additional combined cases should fail a finite premise guard or report incomplete coverage if a legal combination is not yet enumerated. |
| 10 | **Before-replay budget and truthful coverage.** Charge a conservative justified owner-step/input bound BEFORE each replay; include sorts, fixed-point retries, callbacks, initial normalization and every attempted/discarded branch. With insufficient allowance, observe no uncharged owner call; retain incomplete omissions. With sufficient finite ordinary input, observe useful actual traces, not permanent uncertainty. Canonical choice/input permutation must preserve complete result and global work; windows beyond governed span never cause a per-week loop. |77's one/two-picture real-action states are the small first measurement route, with row1 controlled priority as a separate worst-case-restart discriminant. Existing91/94 cap/completeness laws apply, but no concrete producer meter/interface exists yet: call observation and exact charging assertions MUST wait for that settled seam. No invented per-call cost, wall-time threshold, timeout increase or whole-GameState cloning shortcut. Future work that can alter an existing calendar makes global existingCalendars incomplete; empty/background branches still cost rows/work. |

## Minimal first tranche and remaining proof

After the producer interface is settled, begin with rows2/4/5 on the existing
real-action player route plus the actual rival take witness, row7's writing
state and row10's pre-replay guard. Keep row1's controlled priority oracle separate
from any genuine full-context trace. Add actual technology/setup conjunctions only
with explicit fixture evidence; their source recipes do not claim current success.

All future positive traces must join back to actual caller-owned clocks, paths,
cast/employment, settings, sink transitions and branch roots. Independent disposable
action/tick runs may supply the test oracle; the planning producer must not commit
hypothetical history or copy an entire action/tick pipeline. The lower detached
kernel remains only the consumer of certified facts. Coverage of natural offers,
ordinary performance, managed scripts, all legal staffing/command choices and
full B4 qualification is still owed. This matrix changes no source, policy,
fixture bytes, validator, timeout or current execution evidence.
