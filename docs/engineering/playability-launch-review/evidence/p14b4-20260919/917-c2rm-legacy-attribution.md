# 917 — selected legacy RED closed and attributed

916 CLOSED/fixedSource:true on published17e65c36ddcb2d67f174bb413ec927245db1021a,
empty sourcepatch.914 selector observed exactly47 cases:45FAIL/2PASS,405unselected,
32files. No import/premise/timeout failures. No permanent skip or timing changes.
The two controls retain no extension promises and one-final-extension wording.

All45 failures are accounted for in917 JSON:37 current projection metadata pins,
three current prior-registry roster/count assertions, one current generated schema
literal hash, four superseded W10 bridge exclusions. 875 requires current52 with
Save37/protocol4 unchanged and40 prior identities, adding only genuine outgoing51.
The generator diagnostic exposes its test-local old projection51 argument; the
checked-in generated contract itself is52. No generator production correction needed.

Parent inspected complete primary diagnostics via916-vs-861-diagnostics.json.
That is a SELECTED-vs-full comparison; its70 vanished failures are unselected,
not fixes. It is not the required matched full qualification. Raw logs preserved.

Release the same independent test author to bounded maintenance of the32 exact914
files only. Keep frozen35/36 prior lists and append actual outgoing51 only to current
aggregate expectations. Current hash is independently recorded by914. Update the
four W10 expectations to875 list/profile/open-world/own-issuer attention disclosure,
retaining real natural input and both existing controls. Preserve numeric-tolerant
selector applicability and W10 suite prefix. No production, historical corpus,
save-conversion, timeout, skip/todo or behavioral guard changes.

The selected B2 case also contains the old seven-cause attention enum, masked by its
first projection assertion in916. 875 explicitly adds retirementAnnounced,
finishingCommitments and retirementExtensionOpen, so update that wire enumeration
as attributed current-schema maintenance. The separate seven-member preference
priorityOrder and unchanged intent vocabulary MUST remain exact. Any additional
failure after maintenance requires fresh attribution, not weakened assertions.

After author freeze: independent stable-diff review, exact same selected GREEN,
appropriate types, then required full core verification matched against861.
