# 191 — started-owner replay implementation

2026-09-20. Initial implementation candidate **SOURCE FROZEN** for parent-owned
compiler, independent behavior and bounded source review. Base:
`0bc641a10f995a418a180b1d4fe4e6018aa4a14a`. Native sim-core; no delegates.
No runtime, tests, probes, typechecks, Git or network commands were run here.
This is not a passing-test, accepted-budget or live-integration claim.

## Exact writable scope / source identity

| File | SHA256 |
| --- | --- |
| src/core/promiseCapacityOwnerReplay.ts |6e681ce3a23e236e638efe1db2ee0ec340b17633133e70c3a187cf7d8c48662f|
| src/core/scriptDevelopment.ts |a82629e70d467c4716a280c7cac6fcaa67ce26137accc0d5d9e7835fefd63a49|
| src/core/castingSessions.ts |497a8a32152e556378db96d8363be6de9a0719f2eed1fbec4a4fcfe222cf3a3b|
| src/core/productionPeople.ts |2c6ea9affd6811d25cd36d85808d9470441f579153a7f0a2fb5b6a2c8a118d58|

The new module implements137's public types/function. The two completion owners
delegate only their original eligibility expression to the new pure predicate:
the negated status/null/`> arrivalWeek` expression is exact, including its narrow
NaN behavior. Mode/week validation, assessment and completion bodies stay in
their original owners. productionCompanyTalentIds's body is unchanged; its
input is the genuine readonly director/cast/craft view.

Rechecked unchanged protected identities:

- kernel: `1faa6fcac443dac046bd97fa3e27c61bada80c7297f8e69ea4ebcff76d899332`
- operations: `9935b09495df1d11f39bf3b1a37b244c3d7bf7d0994cd4c8ce286571b3be3411`
- bounded sorter: `87861f3ceefe2b2a9163a34bd9762a991d5c8a7f1640b84fbe6d1133f33ff964`
- release owner: `9ab259623d3b5f5f75f1d750168e68ccb5c14d27d96f06f4656985db8064b218`
- installed182: `d3fc0c003a1e4e58c2b9164e2e7d0cf8d6a9eccd3877037fcb1e668cbb8da790`

Only the four authorized production files and this handback were edited.
No new index export, live caller, version, schema, validator, kernel, Unity,
test/helper/fixture or configuration change. Read frozen194 requirements only;
parent owns its eventual installation. A scoped trailing-whitespace search found
none. Source hashes above are the static handback, not an execution checksum.

## Implemented execution and certificate

`prepare` reads genuine roots and exact joins, not caller harmlessness flags.
It checks canonical plans/commands, current picture/workflow/phase/facility joins,
actual company people and pooled writers, indexed foreign screenplay facts,
relevant foreign company/research seats, and the adopted queue/physical work/
installation/Set/research/adoption cuts. Retained disjoint foreign work is not
removed. Context cuts name their reason; contradictions throw the replay prefix.

Every plan starts from the same source/fixed ledger and immutable roots. `frame`
closes actual due background holds before the sweep, executes real scenery
arrival, builds fresh technology/setup collectors, and calls the real whole-slate
advance once. No tick/actions import, copied allocator/countdown, forecast,
fake Production/GameState or observer-result substitution exists. Command dispatch
uses current branch lookup and actual assign/clear/schedule/release owners.
An earlier released picture cannot be resurrected by a later command.

`drainEvents` preserves actual append order and raw ownerWeek. Exact reservation
joins use original/returned reservation facts rather than splitting facility IDs.
Wrapped(stage,set) supplies Set identity; matching stage reservationReleased
supplies BOTH stage and bound-Set endpoints. Scenery closes at its own release;
Post starts at its own grant. Retained slots have no synthetic event. Full returned
reservations are reconciled against the ledger. Standing mounts remain separate
background resources; historical setId without a stage holds nothing.

The actual admitted-release/zero-clock sets must agree. Company holds close at
the pre-increment release boundary, real commitments are pruned, and actual Set
novelty depletion uses pre-sweep retained bindings in production-ID order. Returned
first takes receive the arrived boundary and cannot repeat historical/branch
first takes. Unknown actual person release stays null with full-H company holds.

Every picture and supported background survives in the trace, credited or not.
No durable receipt/event sequence or released-film history is minted. Generic P
spreads are the real owner's; opaque marker/forecast values are never traversed.
H==now uses the unchanged projection/zero-length current holds without owner
execution, subject to paid preparation. Prefixes are cuts, never proof traces.

## Before-call work inventory and reservation sites

This is a source-work model, not a host instruction/allocation/time theorem.
Plain engine JSON containers plus ordinary own-string-key P extensions are the
internal premise; no free dynamic symbol/getter/proxy validation is claimed.

`Work.pay` owns the one cumulative allowance initialized by preparationWork.
`plus`/`times` saturate before overflow at200001. Exhaustion saturates the shared
counter and stops before the unpaid operation. No branch refund/reset exists.
The input ceiling/span/global header+path occurrence limits are200000/220/1024.
Producer output transfers preparationWork to the unchanged trace kernel; this
module does not invoke a fresh-budget kernel or assert complete choice coverage.

| Billing component | Source variables and covered operations | Pre-call site |
| --- | --- | --- |
| `Work.copyCost`, `dimensions` | Incremental own-string-key enumeration/read/write footprint, no Object.keys expansion; all actual P/workflow/task/bindings/setup/Set/technology root/row copies. Repeated copies pay the measured footprint again. Named consumed string fields, capacities, structures/provided IDs/placement cells are visited before selecting a branch bill. | Preparation and each command/frame, before external owners |
| `sortBill`, `orderBill`, `sorted` | Actual168 arithmetic merge passes/runs/head comparisons/tail writes;176 scalarization. Production decoration pays unchanged regex and digit spans once per row; comparator pays both lexical operands each time. Other ordered outputs use the same bounded sorter. | Every actual local sort and inside sweep/commit reservation |
| `company`, `writers` | Actual company Set and cast/craft loops; key universe and repeated operands; original attributed-writer includes/fallback copy. Credited writer never becomes a company seat. | Before shared company/writer helper calls |
| `geometryBill`, `arrivalBill` | Every workflow guard, each potential trip's two body queries, authored filter/includes, placement/cell scans, finite geometry/result blocks, arrival sink/task/workflow/operations copies and replacement maps. Pending trips are counted before invoking arrival. | Immediately before real arriveDueScenery / manual-clear classifier |
| `restrictedSweepBill` |162's new/5/3/1 branch inventory: actual A/Q/T/C/O/R classification, rounds, all associative queries, workflow finds/maps/filters, full generic copies, phase/task strings, output/control and empty release transition. | `frame` before actual advance |
| `allocationBill` | Eager raw production claims (including filtered rows and second Set pass), filter/index/key universe, external union, all facility policy callbacks BEFORE bounded sorting, retained-capability/facility lookup, capacity slot loops/key construction, stage/Set candidate loops and finite result literals. General capacity loops use all supplied capacities, not expected successful slots. | Included in general `sweepBill` before actual advance |
| General phase/transition bill | Release-phase copy/filter, both ordered reservation transitions and repeated includes operands, wrap/wear, workflow updates, allocation success/failure, setup resolution/record copies, actual technology selection/adoption/two installation scans/lock, Set binding/genre lookup, task/result/P copies. General visit bound N(2N+1); no unsupported-phase shortcut. | Included in `sweepBill` |
| Restricted all4 Post bill | All pictures genuinely at4; Post cannot release within this sweep. Failed-attempt bound N(N+3)/2, with division before saturation. Only actual all-silent selection, no external occupancy and enough current Post slots select N successful attempts. General common visits remain conservatively reserved. | Facts inspected/charged by `sweepBill` before its reservation |
| Commands | Current lookup, actual command guard/refusal strings and all immutable updates. Release refusal includes production/workflow/commitment/title/concept scans; successful append separately reserves row/ID/copy and bounded sort. | `executeCommand`, immediately before each real owner |
| Factories / collector access | Genuine fact literal, fresh closure/object construction, sink creation, actual technology result getter. Full callback traversal is in the sweep bill, not omitted by factory creation. | `frame` before each factory; getter separately paid |
| Release aftermath | Actual zero/admission equality and sort; original workflow join; real Set some/map/copy/novelty; released-ID Set creation, full commitment filter and production pruning. | Each actual release/depletion/prune/filter call |
| Ledger/proof/projection | Per-row discovery, exact repeated path/subject/ID operands, actual copies, source-derived JSON token spans (including worst-case escaping), closes/grants/reconciliation, canonical output arrays and full branch projection. | Incremental payment before each operation; later exhaustion leaves a cut |

Finite calculator bodies have separate charges; they are not free tariff oracles.
Variable scans in calculators charge each visit/string/key. Their scalar formulas
use bounded-arity checked sums/products; calculator allowances and all owner bill
constants still require the independent source audit against this exact candidate.
Constants cover named finite source blocks, not an unmeasured whole-replay
residual. In particular the old67,904 estimate is neither used nor revived.

Exact-ID joins in the new producer are explicitly charged linear walks rather
than a new general index framework. Canonical sorts/duplicate checks use the real
bounded sorter. This is an implementation choice with visible cost; no free
constant-time lookup is assumed, and actual useful-cap tests decide whether it
is sufficiently efficient. No cross-state ID cache exists.

`frame` reserves sweep bill + sink/start-observation/callback literal overhead
atomically before appending sweepStarted and calling advance. Thus threshold
tests can observe no unexecuted start or unreserved advance. If later charged
reconciliation/output fails, the already-paid sweep is retained only as a cut
prefix. Constant administrative exhaustion results do not scan omitted suffixes.

## Verification still owed — not inferred green

The candidate has not been compiled or executed by this writer. Parent next:

1. Install exact independently frozen194; run compiler feedback and installed182
   plus194 real parity/ledger/budget/command cases serially.
2. Confirm BOTH genuine one/two H6 traces finish, pass the unchanged trace kernel,
   and remain below the SAME200000 allowance with transferred preparationWork.
   Neither construction179 nor this handback proves that numerical result.
3. Check original due-domain/owner regressions and root/UI/bridge typechecks.
4. Independently review all fixed calculator/finite owner blocks, variable factors,
   operation order, string/copy repetition and reservation sites. Correct actual
   undercharges or excessive ordinary-case bills without altering the tests,
   limit, validators, endpoint semantics or supported owner law.

Risk concentration: the new module is substantial and its source-accounted
general-phase bills have not yet received compiler/runtime/auditor feedback.
Early complete candidate publication is deliberately not certification. No
specific new source-owner dependency or product-law blocker was found.
Rival replay, future admission/enumeration, complete natural feasibility and live
save/wire/policy cutover remain outside this tranche.

Source ownership yielded; all four files are frozen pending parent verification.
