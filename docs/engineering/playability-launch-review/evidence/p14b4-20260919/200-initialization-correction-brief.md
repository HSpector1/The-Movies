# 200 — preserve the real fresh-industry initialization boundary

2026-09-20. Test-only INERT correction, no runtime/typecheck/probe, protected edit,
replay implementation inspection, Git/network or delegation.193/194 and the
installed test remain unchanged. Parent owns actual execution and installation.

Read the complete198 raw output: source `0bc641a10f995a418a180b1d4fe4e6018aa4a14a`,
tested diff `40b27b819d98693c90b53157db2f6402a90298dee0523b2fca0e68ee0cb3d2ed`,
fixedSource true, real exit1,19PASS/1FAIL. The writing case fails in `supported`
→ `makeSave` before replay. The exact refusal is `Hollywood save: fresh campaign
starts at week zero`. The other three command cases and all sixteen182 cases pass.
This is read evidence from the parent's run, not an execution by this author.

## Factual cause and law-preserving correction

`initializeHollywood` stamps `originWeek` from the supplied current week;
`hollywoodValidation.ts81` requires a fresh origin at0. The old `buildScenario`
already performs a real script tick before returning `.deadlock`. Initializing
that returned week1 state as fresh was therefore invalid.

The new copy reconstructs ONLY that short real action recipe using the existing
exported `foundedStudio`, `contractedByRole`, `commissionPayload`, `writingIds`,
`projectById` and `remainingPackage` helpers. It performs fresh initialization
immediately after the genuine historical-control founding at actual week0, then
commissions A, ticks, accepts A and commissions B. No saved root/clock/history is
restamped, no past is invented, no validation bypass occurs, and the generated
campaign is not reclassified as a migration to excuse a late fresh origin.

New strict guards pin week0/fresh origin0 and validate the initialized, Ready-A
and drafting-B states. Existing one-writer/actual-status/pool guards are retained
in the short recipe. The existing writing test still requires all original
production/project joins, strict save validations, root cuts, different slots,
8-skip/7-sticky transition, exact background/person holds and before-sweep order.
All other three command cases, default timeouts and every existing194 assertion
are unchanged. Read-only diff confirmed only imports, the helper and first-case
fixture setup changed. Frozen194 hash still
`11d607b24dc0bd22fb622a5ee0cd16a92705ce17bf4223c28a957864968d3add`.

Corrected inert copy: `200-started-replay-background-command.test-draft.ts`,
362 lines/four cases, SHA256
`4b4f2e7cf06b71669b1e1cc73444306738925645618b6ae6028beabd1582dcc8`.
Intended installed path remains
`tests/p14b4-started-replay-background-command.test.ts`.
Copied193 provenance header remains intact;200 provides the corrective record.

The same genuine early-initialization recipe was applied to the still-unexecuted
199 casting draft before its handback. Neither corrected fixture has been run.
Actual industry advancement now accompanies each real script tick; all foreign
relevance/root-cut guards remain mandatory. If that reveals another real
conjunction or budget finding, record it without root repair or assertion relief.

Next: independent parent review, exact scoped installation, serialized runtime
and typechecking. No production change is requested by this fixture correction.
