# 309 — Two approved pre-execution302 oracle corrections

2026-09-20. Native test-author; FROZEN INERT corrected copy only. One case,
280 lines, still UNEXECUTED. Original302 and its brief remain unchanged.

- Corrected draft: `309-ready-stale-target.test-draft.ts`.
- SHA256: `34e99a3c0be9f3bdd8f417889010494642de6c615cc71584fac81d1bb7b86a48`.
- Intended target: `tests/p14b4-ready-replay-stale-target.test.ts`.
- Preserved302 SHA256:
  `4c903429536054ff471dc954b0e49be76ecfab852900ae2f30747560ba0b6a4d`.

Parent adopted308's two bounded findings. Exactly these two sites changed:

1. ReplayObservation.command carries the RESOLVED StartedProductionCommand,
   not the caller's Ready-project target arm. The expected array now independently
   maps original literal kind/week/ordinal plus the productionId allocated by
   the REAL reference greenlight. Full equality and exact command order remain;
   the expected identity is not read from the replay observation.
2. Parent's reviewed shared-executor refusal is exactly
   `production already released in this branch`. Pin that sentence instead of
   the unsupported demand that this particular prose contain productionId.
   All exact identity joins, owner-call equality, release/take/admission
   provenance and the separate actual public authority/action ID-bearing
   refusal remain unchanged.

The historical302 header is retained. Read-only diff confirms no other fixture,
assertion, input, cap, default-timeout or semantic edit. This corrects two
pre-execution interface/prose overconstraints, not a response to a failed
implementation. No runtime/typecheck or parser execution, live installation,
production/replay inspection, protected write, Git/network or delegation.

All original fixture-construction and longer-route/cap uncertainty remains.
No RED/PASS or full-budget claim; parent owns review, exact installation and
serialized execution. The cut still must reach the genuine post-release stale
command within200000 and cannot supply a complete trace or resurrect a clock.
