# 182 — independent started-owner replay tests: frozen inert handback

2026-09-20. Native Fable test-author. AUTHORING ONLY / UNEXECUTED. Only the new182
draft and this brief changed. No protected source/test/config/fixture write,
runtime/probe/typecheck, Git/network or delegation. Parent owns installation,
actual RED and subsequent serialized verification/source release.

Draft `182-started-replay.test-draft.ts`,569 lines,16 intended Vitest cases.
Intended installed path: `tests/p14b4-started-owner-replay.test.ts`.
SHA256 `2345394f2a20dde524630dd29b9d48a18cf3540af75c8e4004d8b300eb304565`.

## Authority and actual fixture provenance

Read complete137 API/architecture,148,169,178 probe,180 qualification and181
independent KEEP. Later read COMPLETE FROZEN176, SHA256
`977c19e603e865d04d83be011e0257f3b02d3fb415e2db07ef2d7abf354336d9`,
only after sim handback. Parent adopted176 §8 and reported185 qualified KEEP;
no whole-cost proof is inferred. No mutable176 or replay implementation inspected.
Parent's186 precision is explicit: wrapped supplies Set identity; MATCHING stage
reservationReleased supplies BOTH stage and exclusive-Set endpoints. Scenery
ends at its own release; Post begins at its grant; company/mount holds persist.

178's actual source is published `d9ebb81090dafc0d18d7c0f3b86e5f92d66ae123`;
179 validated both one/two constructions, source week4, take5, wrap/Post6,
all source-cut guards, genuine current save validation and input purity. The
new tests reuse that route, not fabricated old fixture bytes. Historical-control
founding is disclosed. All film/person/contracts/actions/ticks are real, with
managed operations and legacy development. No manual lower-owner scenery clear,
cash/RNG/clock/history changes. Both Ready tasks are scheduled in the SAME week.
The cached fixture is cloned between tests; no producer result becomes an oracle.

## Assertions

- Four local-parity cases: one/two pictures to H5 and H6. Real `tick` reference
  states are computed BEFORE transparent observers. Actual original whole-slate
  sweep executes once per frame. Exact clocks, productions, operations, Sets,
  owned technology, retained foreign technology, release authority, take boundary,
  wrap/reservation/phase event order and raw ownerWeek agree. No fake durable
  first-take receipt is returned. Actual used Sets wear100→91 once; company stays.
- H==now: zero scenery/factory/sweep calls, original projection/current picture
  paths, canonical zero-length fixed holds, no take/release/provenance invention.
- Full generic P: exact inferred full return type, preserved extra fields/nested
  marker references, no fake GameState/save. Opaque nested marker expansion does
  not alter work; added ordinary own keys increase measured expenditure. No
  getters/proxies/symbols are submitted to176's plain own-string-key domain.
- Long trace identity: short useful control first; a caller key over200000 code
  units alone exceeds the cap, so honest saturated work cut before any sweep.
  No genuine production/person/facility ID is reminted. No arbitrary requirement
  that an unbounded long string still earns a complete certificate.
- Two ledger/kernel cases: every production and standing Set-mount background
  path remains. Independent expected fixed subjects derive from actual company,
  reservations, occupied Set and mounts. Exact initial/end intervals, legal suffix
  changes, exact186 resource endpoints, new Post holds, unique IDs and pairwise
  subject compatibility; no uncredited-path deletion. Original/matching cast and
  source start/take calendars are pinned, unknown person release remains null.
- Strict unchanged joint kernel consumes the ACTUAL producer traces/fixedHolds
  and returned preparationWork under the SAME200000 allowance. Relevant promise
  roots are independently asserted absent; actual source cuts/ledger are checked
  before claimsAndHolds complete is supplied. Existing/all owner-choice coverage
  remains incomplete. Exactly one genuine in-window target event is counted
  directly; expected verdict is UNCERTIFIED/domainIncomplete, NOT workLimit or
  ACHIEVABLE. There is no invented countWitness in that result.
- Zero/tiny caps guard no actual scenery, policy/setup factory or sweep calls;
  zero returns no fixed holds/attempts and a nonempty honest omission. No invented
  exact diagnostic strings. Two ≤18-step binary searches locate actual first and
  second sweep admission thresholds, compare immediate neighboring integer caps,
  full real owner calls and sweepStarted provenance. No private tariff self-oracle.
- Two canonical plans share expenditure (including initial17), preserve exact
  results/work under plan permutation and do not reset the allowance for planB.
  Threshold tests are behavioral pre-call observations, NOT a substitute for
  independent conservative source-bill review.
- Three seam cases: exact old negated-refusal due predicates (including explicit
  invalid-save NaN pure-number probes; no validator change), and readonly OUTER
  narrow company records preserving the genuine craftIds reference. No new
  deep-readonly nested-array API, writer-company seat, mode or caller-week change.

## Honest limits / next action

Static authoring only: no claim182 imports, typings or bodies have executed.
Absent replay module should cause actual collection RED; record real reach rather
than assuming it. Due exports/readonly narrowing are intentional future seams.
New kernel/no-relevant-promises, ledger, lower-budget and shared-plan conjunctions
are independently specified but UNEXECUTED;179 proved fixtures, not these results.
Default timeouts remain unchanged. No skip/todo, fake result stub or unsafe cast.

The suite is a first useful tranche, not full137 coverage. Due-background132,
nonempty command plans/current-branch post-release refusal, context/refusal branch
tables, active setup/sound/geometry choices, Post-blocked/mixed-phase cases and
general long historical-ID provenance remain further tests/implementation duties.
The H5 event is exactly its horizon and receives no claim credit; H6 admits count
progress only. Neither two disjoint companies nor finite commands proves B2,
eight-week slack, full prior optimum or complete future choices. Rival replay,
new-film enumeration and live policy/Save30 activation remain outside this file.

Parent next: read/freeze exact bytes, install after adoption, record actual RED,
publish recoverable evidence and release the one scoped writer. Keep the real
useful H6+kernel200000 gate and independent source-prepayment review; failures
must not be repaired by weakened guards, cost caps, validators or owner laws.
