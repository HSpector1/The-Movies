# 253 — bounded Ready-command boundary supplement

2026-09-20. FROZEN INERT / UNEXECUTED. Three cases, 201 lines.

Draft: `253-ready-command.test-draft.ts`

SHA256: `981a54182ba5986e7c440e75b6e65e0baa993986a7cb783dc23ff9bb61c68628`.

Intended installation: `tests/p14b4-ready-replay-command.test.ts`.
Authority: frozen240/241, shared137 malformed-input/command law and176 accounting;
immutable source reads used `git show 78796c1:...` for actions and the operation
owner, not the mutable replay or249 changes. Existing244/250 and live tests remain
untouched. No runtime, probe, typecheck, installation, protected edit, Git mutation,
network, delegation, private bill, raised200000 cap or timeout change.

## Exact cases

1. Real baseReady → real immediate greenlight proves a Development picture with
   remaining8 and no shooting task. The existing public scheduleShootingTake
   action genuinely refuses this phase with the literal owner diagnostic naming
   the actual allocated ID. A Ready-target replay command at source-now ordinal1
   must first admit and link, then resolve readyProjectId to that same new clock.
   Transparent pass-through observers require order admit→link→schedule and the
   exact immediately admitted operations passed to the original scheduler.
   Refusal is commandRefused with the same diagnostic, successful readyAdmitted
   provenance but no complete trace/projection, arrival or sweep. Source remains
   empty-slate/Ready; no guessed future ID, branch resurrection or persisted film.
2. H==now with a genuine Ready choice is contradictory input for the Ready entry,
   unlike the valid started-only zero-span control. Assert Error only, no invented
   message, and zero observed header/staffing/admission/link/schedule/arrival/sweep.
3. Source-now ordinal0 is reserved for admission. A normal Ready-target schedule
   command in that slot likewise errors before those owners, with no diagnostic
   wording prescribed. Horizon is otherwise lawful and budget is the ordinary
   full cap, so these cases do not accidentally test administrative work0 cuts.

The three boundary records are correctly typed requests; invalid H/ordinal are
explicit API-domain probes over a genuine state, not supposedly valid gameplay
commands or invalid saves admitted by a weakened validator. All spy calls retain
the real implementation/result or real throw; restore and whole-input purity
checks are in finally. No synthetic successful owner receipt is introduced.

## Fixture provenance and limits

The supported-root guards and baseReady helper are reused verbatim from frozen250,
which reuses244's disclosed historical-control founding/actions: real hires,
fresh industry at actualweek0, real managed commission→tick→Review→accept. Full
strict makeSave and proposed-company/foreign-work relevance guards remain.
This is not ordinary natural founding, a historical imported campaign or a
fixture proved by this task. Every new command/guard conjunction is UNEXECUTED.

Positive source-now admission, real finance and current ID come from the actual
action oracle; tests do not claim the capacity replay authorizes affordable
greenlight. Current-law scheduler diagnostic is backed by immutable
operations787–801 and actions1858–1866 at78796c1.

Explicit remaining gaps: successful future Ready-target take/director commands,
original-production target alongside a Ready target, both/missing target-arm
structural rejection, target after actual committed release, and original-ID
resurrection boundaries. These would need separate exact typed/real routes;
no unsafe cast or longer campaign was invented here. Existing started controls
remain the independent positive command evidence, not proof of these new arms.

No RED/GREEN or performance result is claimed. Additional coverage is NOT a
prerequisite or delay to249. Parent owns review, installation and serial execution.
