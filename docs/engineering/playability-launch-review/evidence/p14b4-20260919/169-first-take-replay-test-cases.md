# 169 — first useful started-owner replay: bounded independent case matrix

2026-09-20. PAPER ONLY / UNEXECUTED. Native Fable test-author; parent supplied
published base `f290536`. Read full frozen137/148/132/162 and157 §§4–6;
read unchanged action/tick, actual fixture/tests and joint-kernel contracts/types.
Did NOT inspect mutable operations.ts, releaseAuthority.ts or boundedStableSort.ts
during168. No executable draft, runtime/probe/typecheck, source/test edit, Git,
network or delegation. Only this169 note changes.162 is a conditional ONE-owner
bill, not an adopted complete replay bill or a numerical execution result.

## Required endpoint correction — adopted by parent during169

For source week **w**, the real scheduled5→4 sweep emits a first take at
**(w+1,0)**. If the one-sweep horizon is **H=w+1**, this is exactly the horizon.
A demand's half-open window requires `take.week < dueWeekExclusive`; the kernel's
effective H is at least that due week. Thus an in-window demand for that event
requires `dueWeekExclusive >= w+2`, hence H>=w+2. One cannot lengthen the demand
alone: unknown-release company holds and the compulsory physical ledger must
then be complete through the larger computed H.

Parent adopts this correction to157 §5's claim that a one-sweep event is a
"qualifying future event." It is a REAL event and useful local projection/ledger
control, but NOT an in-window claim witness at the same one-sweep H. Keep the
clock, half-open law, computed-H rule and unchanged kernel. No invented release,
hold tail, stationary fast-forward or restamped event repairs this boundary.

The minimum in-window extension is a real second sweep, H>=w+2, including actual
4→3 wrap/Post transition, stage/scenery release, Set wear and continued company
holds. Its before-call bill is NOT162's restricted5/3/1 bill. Even then, positive
COUNT is not CERTIFIED_ACHIEVABLE: fresh X=1 needs B=2 distinct same-person picture
credits AND eight-week slack on the Xth take, together in one legal witness.
The two simultaneous current pictures below have disjoint actual companies, so
they cannot supply the same beneficiary's second picture. Do not duplicate cast,
combine plans, or call a finite explicit command menu complete to force FRAGILE.

## Genuine fixture route — smallest useful source, hard guards

Reuse the real historical-control founding route in
`tests/contracts/_contractFixtures.ts::operationsStudio/productionPayload` and
the actual action path in `tests/p14b4-owner-fact-views.test.ts::atRemaining`.
Initialize actual Hollywood (`initializeHollywood(...,'fresh')`) BEFORE greenlight
so the source has a real player issuer. This is disclosed controlled founding,
not ordinary natural recruitment; actual people/contracts/films/workflows/ticks
and first-take history are never manufactured. Managed OPERATIONS plus legacy
DEVELOPMENT is lawful; no invented Ready/commission is needed in this mode.

For one picture, real greenlight offset0. For two, real greenlights offsets0/1;
guard two actual distinct productions, six distinct cast people, distinct engaged
director/craft people, real stage/Set bindings and enough actual capacities.
Advance through real ticks to first Shooting week. Call each real locked director
through `applyActions`. Let actual arrival settle with real ticks until BOTH tasks
are Ready; do not schedule the first while still waiting for its sibling. Once
both are Ready at the SAME visible week, issue actual scheduleTake actions for
both without an intervening tick. Never manually clear a current derived trip.

Use finite guards (e.g.20 setup/entry ticks and8 arrival ticks, no enlarged test
timeout), reporting a failed conjunction rather than changing clocks/workflows.
At chosen source w require all current pictures remaining5, startTick<w, task
scheduled, blocker null, no pending scenery, and no existing firstTake for those
IDs. Guard exact full137 root cuts: founded managed player; empty current queue;
no active physical/construction/Set/research/adoption work; no relevant foreign
company/pooled-writing/research conflict. Preserve disjoint rival state, source
history and all original current roots; do not delete rows to satisfy a cut.

The two-picture conjunction is a source-backed CANDIDATE, not a reached witness:
77 already has real two-greenlight and single-scheduled fixtures, but that does
not prove this combined simultaneous-ready/cut-free state. Parent executes it
only after the producer interface/budget gates settle.

## Minimal matrix

| Case | Exact discriminant and independent expectation | Limit / additional gate |
|---|---|---|
| A. One scheduled picture, one empty-command plan, H=w+1 | Exactly one actual whole-slate sweep; returned picture5→4 and shooting task scheduled→completed. Projection weekH; same IDs/company/cast/other production fields, unchanged reservations/Sets/technology/release authority for this branch. Exactly one firstTake observation at(H,0), not(w,k), and no release. Compare to actual `tick(source)` LOCAL player projection and its durable take's production/studio/director/cast/week, not by inventing an event ID inside replay. | Whole-world equality is false authority: actual tick advances money/rivals/market/history. Replay must not fabricate these roots or permanent receipts. Compare issuer-owned technology locks, not unrelated foreign technology rows. Full source unchanged. |
| B. Two scheduled pictures, one empty plan | Exactly ONE sweep receives the complete two-picture slate. Both5→4, both events share(H,0), distinct production IDs. Returned production order remains caller order while service ordering stays the real owner. Every picture remains represented even when only one beneficiary is of interest. Compare complete local operations, Sets, production projection, exact take-ID set and player technology to actual tick. | Do not replay each picture separately or splice their outputs. Real staffing is disjoint; neither picture is a spare second credit for the other's person. No input reorder of workflows to manufacture priority. |
| C. Compulsory full-H ledger and unchanged joint-kernel admission | Independently derive all current company people (director/cast/craft, NOT merely credited writer), exact initial facility/slot reservations, occupied bound Sets and mount backgrounds. Exact source-derived path-key membership, including uncredited picture/background paths. Person releases null; company holds cover[now,H) with no gaps or duplicated overlap. One-take sweep retains physical holds and needs no false suffix release. Feed actual producer fixedHolds/complete traces unchanged to `searchPromiseCapacityTraces`, with the true upstream coverage flags and cumulative preparationWork. | One-sweep H event is not in-window. Acceptance here means successful strict trace/ledger normalization and truthful verdict, NOT positive opportunity certification. Final literal verdict depends on settled prepared claims/coverage; do not infer completeness from one finished plan. No hand-repaired trace or dropping background paths. |
| D. H==now | Same original roots and current paths; canonical zero-length[now,now) current holds; unchanged projection. No commands allowed by now<=week<H, no actual owner sweep/arrival/command invocation, take/completion/release observation, or invented release endpoint. Input/context/budget checks still occur. | A real path at5 is retained, not deleted for having zero execution span. This cannot be billed or represented as a completed take. Kernel adapter must use consistent effectiveH rather than secretly lengthen the horizon via a later demand. |
| E. Zero and tiny global work budgets | Transparent pass-through observers around EXISTING real owner entry(s), always restored. limits.work0 with preparationWork0, plus small positive caps below independently proved minimum whole-call charge, return truthful workLimit/prefix-or-no-attempt semantics and ZERO unreserved advance invocations. Source remains byte/equality/identity pure and no complete trace is emitted after an unaffordable call. | Exact cut/detail/preparation boundary comes from settled API/bill, not a guessed counter. Spies do not stub results. Merely no sweep at budget0 does not prove every intermediate factory/arrival call was prepaid. |
| F. Binding before-call threshold and shared two-plan budget | Once source composition is frozen, independently calculate preparation+next-owner allowance. At one unit short of required whole call: no invocation; at enough: exactly one real call and exact real output. Two distinct traceKeys for the SAME empty command plan share one counter; canonical reversed plan input has identical full result/work. Choose a limit that funds one complete plan but not both; second cut retains consumed first-plan expenditure, never receives a fresh counter/refund. | 137 exposes only cumulative result preparationWork, not per-call budget access. Need settled independently calculable block costs or an approved truthful observation seam before executable threshold assertions. Never measure successful runtime then use that output as the expected cost oracle. All proof/prefix construction work also belongs to the same budget. |
| G. Full generic P and opaque markers | Construct typed StartedOwnerSource<P> from the genuine source with actual production facts plus caller-only enumerable marker fields; DO NOT make a forged persisted GameState/save. Pair replay outputs with unmarked real-tick local facts. Preserve every extra key/value, nested marker reference and original input; clock/status changes remain exact. Extra own-property keys/long key names incur independently computed repeated shallow-copy footprint. | Do not recursively charge/traverse opaque forecast/marker contents solely because spread retains a reference. Same own keys with larger opaque nested values should not acquire a hidden deep-copy bill; exact counter assertion awaits final consumed-view preparation law. No partial-as-Production cast, getter/proxy, fake history or validator change. |
| H. Long consumed strings without reminting history | Use a caller-chosen genuinely long traceKey (and ordinary typed extra-property name fromG), preserving actual production/person/facility IDs. Canonical results/work must account for repeated consumed characters and remain bounded; lower cap may cut before allocation/owner invocation. Original IDs, facts, roots and receipts unchanged. | A310-digit production ID cannot be manufactured by renaming a genuine generated campaign. Such detached ordering semantics already belong to160, not a claimed real replay witness. A replay-specific long historical-ID test needs genuine provenance or separately authorized explicit reader-admitted variant; neither is fabricated here. |
| I. Minimum actual two-sweep extension, H=w+2 | Same scheduled source, actual first take atw+1, actual next4→3 wrap/Post visit. Compare both real ticks; reconcile exact owner-event order and stage/scenery release, Set wear ONCE, completed task removal, Post allocation/hold and company retained throughH; personRelease remains null unless an actual release admission occurs. This makes the first event in-window for dueH. | Requires source-accounted wrap/Post/factory/geometry/callback/certificate costs beyond162. It proves a positive count candidate, not B/slack/completeness. Keep actual uncredited sibling paths and physical contention. Do not extend ledgers without executing that second owner step. |
| J.132 due-background chronology companion (later bounded branch) | Existing `_p04a2WriterCreditFixtures.buildScenario(...).deadlock` + realgreenlightA: B draft duew+1 releases its ORIGINAL dev slot before sweep; A owns a DIFFERENT slot and first-week skip keeps8. Exact due provenancew+1, no fake durable completion event/Ready state. Future8→7 retains A's slot. | This is NOT same-slot development waiter acquisition. Queue handoff is later after-sweep admission, outside137 and prohibited by its current-queue cut.162 can cover new-picture skip body, but preparation/background/ledger and later8→7 bills remain separate. |

## Budget and oracle discipline

For A/B,162 paper classification is A=Q=T=N, C=O=R=0, one round. Its owner-body
subtotals962/2436 explicitly EXCLUDE actual SORT_AND_KEYS, preparation, arrivals/
factories/background inspection, plan/ledger/provenance, result canonicalization
and kernel work. Do NOT pin these numbers as whole-call or total costs. Parent
and independent auditor must first verify literal key/property sums, actual
bounded-sort implementation blocks and the complete composition. No arbitrary
remaining allowance,137's withdrawn67904, native-sort tariff or elapsed timing.

Keep200000 unchanged. A useful test requires both real owner reach and an honest
within-cap total, but success of A/B does not license the longer I branch or
complete choice enumeration. Pass the producer's actual cumulative preparation
work into the unchanged kernel; never reset it to0 for a second proof budget.
One passed trace neither proves the prior optimum nor existing/all calendars
complete. Genuine positive feasibility needs B/slack, full relevant holds and
proved prior profile under91/94; its additional domain is NOT supplied here.

Before executable drafting: settle the full A/B pre-call budget, tiny-cut output
precision, observer strategy and prepared-kernel coverage/claim seam; execute the
finite real source-construction guards under parent runtime ownership. Then author
small tests with exact expectations and no skip/todo/time-limit weakening. Retain
I/J and any unsupported phase as explicit additional bills/coverage, not permanent
product exclusions. No test reach, RED, positive certificate or full coverage is
claimed by this paper matrix.
