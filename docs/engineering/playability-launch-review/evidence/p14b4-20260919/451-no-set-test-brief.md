# 451 — genuine no-Set public replay control

Frozen independent test-author handback. **UNEXECUTED**: no test, probe, typecheck or production inspection was performed for this addition. Parent owns the unchanged-438 baseline run before releasing the 452 production writer.

## Authority and exact scope

- Published source supplied by parent: `ba1a0ba1f0164dfd8a1c86312c6daf6ae5856e06`.
- Fully read adopted `451-rehearsal-success-direction.md`; SHA-256 verified as `bf1d844e8fb6ce3196c541eb978a44e47177409f3c7158fc5c1af894d50db6fe`.
- Only executable edit: one additive case in `tests/p14b4-ready-owner-replay.test.ts`, lines 455–565 (111 added lines). No original import, helper, fixture, assertion, cap or timeout changed.
- Baseline: 608 lines, SHA-256 `5e2f28239e675f62d4f057eef609162ef6292cc86fc87d449d406840fd54e057`, matched before editing.
- Frozen candidate: 719 lines, SHA-256 `0e1a25ed6e9b5c1c88d8e4459845636ca7caef6e131f78ff823c5d3cc542f864`.
- Removing only the additive lines from the candidate reconstructs the exact baseline hash. No Git command was used.

## Independent construction and oracle

The existing genuine `baseReady()` route supplies an assessed Ready project. Real public `strikeSet` actions retire both actual standing house Sets before admission. The test requires their original positive-capacity soundstage mounts and preserves each retired row's actual `mountedOn` history; no Set, clock, cash, capacity, history or root is rewritten.

Real public greenlight and three ordinary ticks provide the independent reference: initial 8, then 8/7/7; the last frame is `preProduction`, blocked by `set-unavailable` targeting `rehearsal`, with no stage/Set binding and no retained Development reservation. Every reference state passes the existing strict save and full supported-root/foreign-company guards. The complete H+3 public replay must fit the unchanged 200000 work cap and match whole local projection, actual sweep chronology and complete raw owner-event history.

The compulsory ledger must contain exactly the five actual company people through H and the one real Development slot from its actual grant to its actual release. Retired mounts are not fixed occupancy; no stage, Set or mount hold may appear. The single real screenplay path has no first take or release, and all reference/source inputs must remain unchanged. The existing helpers continue to assert immediate admission-link identity, complete-attempt status, no omissions and production/technology/operation projection parity.

## Evidence limits and handback

This is a retained public-behavior/ledger control, not a claimed RED regression or a private witness/tariff oracle. Public parity alone cannot prove payment sufficiency or detect an undercharged attempt multiplier when real owners still execute unchanged. The direction's bounded-work proof remains an independent source-review obligation; the original longer-route acceptance failure remains separate evidence.

The combined fixture and all new assertions remain unexecuted until the parent's baseline. There is no recovery construction, future Set commissioning, changed timeout/cap, extra campaign or claim of complete Ready-domain coverage. Both assigned files are frozen; test-write ownership is yielded.
