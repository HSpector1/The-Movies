# 771 — the per-case attribution evidence, as the repair pass consumes it

Extracted verbatim from the attribution run behind record 768. This is EVIDENCE, not instruction:
a repair that disagrees with a finding here reports the disagreement rather than silently overriding it.

## Category: inconsistent_fixture (production is right; the test setup is not)

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays development/none byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-act-13: native 48→49 at scripted week 1; the migrated twin stays 48. Native anchors on the EXACT drawn age at week 0 (worldgen.ts:809 `buildTalentProvenance(drawn, 0, 'authored_exact_week')`, with `talent` holding `Math.floor` of it at worldgen.ts:718-719), so its birthdays spread across the calendar. The twin carries only the floored 48, and tests/_historicalCurrent.ts `liftHistoricalState` re-anchors it at the fixture's `market.tick`, putting the next birthday at migrationWeek+52 — outside the 30-week window.
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Re-ran `node_modules/.bin/vitest run tests/v14-migration.contract.test.ts --minWorkers=1 --maxWorkers=1` on the current tree (HEAD 2d3a00fd): 9 failed / 21 passed, the same 9 cases at the same weeks as run 767. Parsed the Expected/Received strings out of 767-c1-full-core-confirm.txt and compared them character by character: lengths equal at 224801, exactly ONE differing character, at index 144136, `"age":49` (native) vs `"age":48` (migrated), inside the object whose own `"id"` is `t-act-13`. Nothing else in the 224,801-char V13 projection differs — cash, rngState, ledger, phases, reservations, shooting tasks and releases all agree. Baseline 755-b8-full-core.txt line 1572 shows this file at `✓ (30 tests)`, so not inherited. Production side proven right: `node_modules/.bin/vitest run tests/p14c1-materialized-aging.test.ts` is 46/46 green on the current tree, including §4 (real `migrateToV33` on all six genuine V32 worlds keeps `ageAtMigration` bit-exact and unrounded) and §5 (V32→V33→V32 byte-identical on every held world).

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays preProduction/none byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: Four people at once, all native-one-higher at scripted week 1: t-wri-01 55 vs 54, t-act-06 48 vs 47, t-act-09 28 vs 27, t-cra-09 49 vs 48. Same cause as development/none: native anchored at week 0 on the exact drawn age, twin re-anchored at the fixture's migration week on the floored age.
- consumer: none reached
- downstream: none observable beyond the age fields themselves
- evidence: Shares the one traced cause with all nine cases (see development/none). Character-level diff of the run log's Expected/Received: both 225078 chars, exactly 4 differing characters at indices 34549, 121797, 131491, 221931, each the single digit of one person's `"age"`, native always +1. No other byte in the projection moves. Re-ran on the current tree: still fails at week 1. Absent from baseline 755 (file passed 30/30 there).

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays preProduction/facility-capacity byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: Identical to preProduction/none — t-wri-01 55 vs 54, t-act-06 48 vs 47, t-act-09 28 vs 27, t-cra-09 49 vs 48 at week 1. The cell is `withBlocker(atPreProduction, …)`, i.e. the same world with a hand-set blocker, so it carries the same people and the same anchors.
- consumer: none reached
- downstream: none observable beyond the age fields themselves
- evidence: Shares the one traced cause with all nine cases. Diff is byte-for-byte the same as preProduction/none: 225078 chars, 4 differing characters at 34549 / 121797 / 131491 / 221931, each an age digit. Re-ran on the current tree: still fails at week 1. Not in baseline 755.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays rehearsal/none byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-act-17: native 23→24 at scripted week 1; the migrated twin stays 23.
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Shares the one traced cause with all nine cases. Run-log diff: both sides 225489 chars, exactly ONE differing character at index 157736, `"age":24` vs `"age":23`, in the object whose `"id"` is `t-act-17`. Re-ran on the current tree: still fails at week 1. Not in baseline 755.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays rehearsal/facility-capacity byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-act-17: native 23→24 at scripted week 1 — the same person and week as rehearsal/none, because this cell is that same state with a hand-set blocker.
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Shares the one traced cause with all nine cases. Run-log diff identical to rehearsal/none: 225489 chars, one differing character at 157736. Re-ran on the current tree: still fails at week 1. Not in baseline 755.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays shooting/none byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-wri-08: native 34→35, here at scripted week 2 because this cell starts one tick earlier than shooting/facility-capacity. Both cells name the SAME person at the SAME absolute campaign week — direct confirmation that the divergence tracks a calendar birthday on the native side, not the cell.
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Shares the one traced cause with all nine cases. Run-log diff: both 225526 chars, exactly ONE differing character at index 57611, `"age":35` vs `"age":34`, object id `t-wri-08`. Re-ran on the current tree: still fails at week 2. Not in baseline 755. The cross-check against shooting/facility-capacity (built from `atShootingCompleted`, one tick later, failing at week 1 on the identical index and person) is the measured proof that the native side's birthdays are spread across the calendar while the twin's are not.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays shooting/scenery-load-in byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-wri-08: native 34→35 at scripted week 2, same absolute week as shooting/none (this cell is `atShootingBlocked`, the same tick).
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Shares the one traced cause with all nine cases. Run-log diff identical to shooting/none: 225526 chars, one differing character at 57611. Re-ran on the current tree: still fails at week 2. Not in baseline 755.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays shooting/facility-capacity byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: t-wri-08: native 34→35 at scripted week 1. Built from `atShootingCompleted`, one tick past the other two shooting cells, so the same birthday lands one run-week earlier.
- consumer: none reached
- downstream: none observable beyond the age field itself
- evidence: Shares the one traced cause with all nine cases. Run-log diff: 225526 chars, one differing character at index 57611, same person `t-wri-08`, as in shooting/none and shooting/scenery-load-in — but reached at week 1 instead of week 2, which pins the divergence to an absolute campaign week rather than to the cell. Re-ran on the current tree: still fails at week 1. Not in baseline 755.

### tests/v14-migration.contract.test.ts > C2a-M1 · T9 (B) — every held phase × blocker kind migrates and plays true > plays postProduction/none byte-identically to its V13 twin for 30 weeks
- confidence: **measured** · still reproduces: True
- first changed age: Two people at scripted week 1: t-wri-04 native 43→44, t-dir-03 native 38→39; the migrated twin holds both at the old value.
- consumer: none reached
- downstream: none observable beyond the age fields themselves
- evidence: Shares the one traced cause with all nine cases. Run-log diff: both 225676 chars, exactly 2 differing characters at indices 44866 and 80405, each an age digit (ids `t-wri-04`, `t-dir-03`), native +1 in both. Re-ran on the current tree: still fails at week 1. Not in baseline 755.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > capacity-only affordable package: quote accepted, queues, then ONE queue row and zero commitment
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree: `node_modules/.bin/vitest run tests/bridge-p05a1-owner-greenlight.test.ts --minWorkers=1 --maxWorkers=1` -> 6 failed / 1 passed, identical to run log lines 2107-2120. This case dies at `new BridgeSession(state,'p05a1-queue-green')` (test line 97) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`; stack `Object.stateDigest (bridge/snapshot-build-context.ts:103) -> exportSaveJson -> makeSave (src/core/save.ts:6442) -> validateSaveV33 -> validateTalentProvenanceRoot (src/core/save.ts:9121)` = validator condition 2. TRACED CAUSE, SHARED BY ALL 10 CASES IN THIS GROUP: `ownerState()` (test line 48-51) is `liftV18Control(migrateToV18(importSave(OWNER_SAVE_JSON)).state)`. I read the fixture: `tests/fixtures/p05a1-owner-profile-rev2.save.json` is saveVersion 15 at market.tick 5 with all 60 stored ages fractional (t-wri-00 = 24.759120920566843). C.1 added to `src/harness/roster-wall/historical-control.ts:14-34` a `talentProvenance` root built by `buildTalentProvenance(state.talent, state.market.tick, 'legacy_age_anchor')` and, per record 762 §12 F4, DELIBERATELY NOT FLOORED, so `ageAt(row,5)=floor(24.759…)=24` while `talent[i].age` stays 24.759…. Production is right for the helper's documented purpose — its own header reads 'Historical V18 observatory only … This is not gameplay migration', and the roster-wall observatory saves via the re-exported `makeSaveV18`, so no V33 validator sees it there. The supported live route for this fixture is `migrateToV33` (save.ts:9234 -> convertV32ToV33 at :9166), which floors every age while keeping the float in `ageAtMigration` and therefore produces a conforming root. The test builds a LIVE bridge state through the observatory lift, and that setup is now internally inconsistent. Clean discriminator measured in both files: every case that constructs a `BridgeSession` fails, every read-only case (`castingProjection`/`projectView`, and here the raw-bytes digest pin at line 86) passes. NOT inherited: baseline 755 line 2055 shows `✓ tests/bridge-p05a1-owner-greenlight.test.ts (7 tests)`. NOT approved_behavioral_change: the throw happens at session construction before any age-priced value is read, and no assertion on a moved number failed.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > unaffordable package: EXACT D-12 sentence rides in the reject message
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run (6 failed / 1 passed). Dies at `new BridgeSession(ownerState(),'p05a1-unaffordable')` (test line 156) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`, same stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (src/core/save.ts:9121)`. SAME TRACED CAUSE as the other 9 cases in this group: `ownerState()` lifts a saveVersion-15 fixture (tick 5, all 60 ages fractional) through `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`), which per record 762 §12 F4 builds `legacy_age_anchor` rows from the EXISTING ages and deliberately does not floor them, so condition 2 (`talent[i].age === ageAt(row, tick)`) cannot hold. The helper is documented 'Historical V18 observatory only … This is not gameplay migration' and the observatory saves via `makeSaveV18`; the supported live route `migrateToV33` -> `convertV32ToV33` (save.ts:9166) floors the ages and would produce a conforming root. Production is right for its purpose; the test's live-state setup is inconsistent. NOT inherited: baseline 755 line 2055 has this file at `✓ (7 tests)`. NOT a behavioural change: nothing age-priced is reached before the throw.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > duplicate actor: exact sentence names the duplicated slots
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a1-duplicate')` (test line 178) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `ownerState()` = `liftV18Control(migrateToV18(importSave(...)).state)`; `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`) attaches a provenance root built from the fixture's unfloored fractional ages (measured: saveVersion 15, tick 5, 60/60 ages fractional), so validator condition 2 refuses the state the moment the bridge digests it. The lift is documented observatory-only and its own saves go through `makeSaveV18`; `migrateToV33` is the supported route and floors ages, so production is right and the test's live-state setup is the inconsistent part. NOT inherited (baseline 755 line 2055: file passed, 7 tests). No age-priced value is reached before the throw, so this is not repricing.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > non-candidate person: exact sentence names the id, role, and title
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a1-alien')` (test line 192) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: the fixture (`tests/fixtures/p05a1-owner-profile-rev2.save.json`, saveVersion 15, tick 5, 60/60 fractional ages) is lifted into a LIVE state by `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`), which C.1 gave an unfloored `legacy_age_anchor` root by design (record 762 §12 F4), so `ageAt(row,5)=24` never equals the stored 24.759…. The helper is 'Historical V18 observatory only … not gameplay migration' and the observatory writes V18 saves; the supported route `migrateToV33` -> `convertV32ToV33` (save.ts:9166) floors the ages. Production is right for its documented use; the test setup is inconsistent. NOT inherited: baseline 755 line 2055 shows the file passing.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > unpublished budget amount: exact sentence names the budget
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a1-budget')` (test line 205) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `ownerState()` builds the live state with `liftV18Control`, whose C.1 addition (`src/harness/roster-wall/historical-control.ts:14-34`) writes `legacy_age_anchor` rows from the fixture's raw fractional ages without flooring them, which validator condition 2 refuses. The lift is documented observatory-only and never itself saved at V33; the supported live migration `migrateToV33` floors ages and produces a conforming root, so production is right and the fixture's live-state route is what needs to change. Note this case asserts only a rejection SENTENCE about a budget, with no age-priced quantity in it — further evidence that the failure is the setup, not repricing. NOT inherited: baseline 755 line 2055, file passed.

### tests/bridge-p05a1-owner-greenlight.test.ts > P05A.1 — the Owner-profile Queue Greenlight state, replayed > stale revision and foreign session are their own reason codes, never ENGINE_REJECTED
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a1-envelope')` (test line 216) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 5`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`) attaches an unfloored provenance root to the saveVersion-15 Owner fixture (tick 5, 60/60 fractional ages), violating condition 2 as soon as the bridge digests the state. This case is about ENVELOPE reason codes (stale revision, foreign session) and touches no priced quantity at all, which rules out repricing decisively: it fails purely because the state it was handed cannot be serialized. The helper is observatory-only by its own header and the observatory saves at V18; `migrateToV33` is the supported route and floors ages. NOT inherited: baseline 755 line 2055 shows `✓ (7 tests)`.

### tests/bridge-p05a3-roster-liveness.test.ts > P05A.3 — sign quote → commit breaks the deadlock (§17-A/E/F/H) > A+H: quotes exact figures, commits, and the signed actor joins the legal pool
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree: `node_modules/.bin/vitest run tests/bridge-p05a3-roster-liveness.test.ts --minWorkers=1 --maxWorkers=1` -> 4 failed / 5 passed, identical to run log lines 1897-1906. Dies at `new BridgeSession(state,'p05a3-sign-happy')` (test line 208) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 8`, stack `Object.stateDigest (bridge/snapshot-build-context.ts:103) -> exportSaveJson -> makeSave (save.ts:6442) -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `ownerState()` (test line 59-61) is `liftV18Control(migrateToV18(importSave(OWNER_SAVE_JSON)).state)`; I read `tests/fixtures/p05a3-owner-profile-rev10.save.json` — saveVersion 15, market.tick 8, all 60 ages fractional — and `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`) gives it a `legacy_age_anchor` root built from those unfloored ages by design (record 762 §12 F4), so `ageAt(row,8)=24 != 24.759…`. Discriminator measured in this file: the 5 passing cases use `castingProjection`/`projectView` only; all 4 failing cases construct a `BridgeSession`. NOT inherited: baseline 755 line 1854 shows `✓ tests/bridge-p05a3-roster-liveness.test.ts (9 tests)`. NOT approved_behavioral_change: this case does pin exact figures (`GLORIA_1YR = {weekly:6040, guaranteed:314080, bonus:56536}`), but none of them was reached — the session throws before the quote is issued, so no figure was measured as moved.

### tests/bridge-p05a3-roster-liveness.test.ts > P05A.3 — sign quote → commit breaks the deadlock (§17-A/E/F/H) > E: an unaffordable sign refuses at quote time with the D-12 sentence and mutates nothing
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run (4 failed / 5 passed). Dies at `new BridgeSession(ownerState(),'p05a3-sign-broke')` (test line 250) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 8`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`) attaches a provenance root built from the saveVersion-15 fixture's unfloored fractional ages (tick 8, 60/60 fractional), which validator condition 2 refuses. The helper's header is 'Historical V18 observatory only … This is not gameplay migration', the roster-wall observatory saves via the re-exported `makeSaveV18`, and the supported live route `migrateToV33` -> `convertV32ToV33` (save.ts:9166) floors every age while keeping the float in `ageAtMigration`. Production is right for its documented purpose; the test's live-state setup is now internally inconsistent. NOT inherited: baseline 755 line 1854, file passed with 9 tests.

### tests/bridge-p05a3-roster-liveness.test.ts > P05A.3 — sign quote → commit breaks the deadlock (§17-A/E/F/H) > F: an accepted unrelated sign invalidates a live offer; the same draft is quotable fresh
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a3-sign-stale')` (test line 263) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 8`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `ownerState()` lifts the saveVersion-15 Owner fixture (tick 8, 60/60 fractional ages) into a live state with `liftV18Control`, whose C.1 provenance root is deliberately unfloored (record 762 §12 F4), so condition 2 (`talent[i].age === ageAt(row, tick)`) fails at the first digest. This case asserts INTENT_NOT_AVAILABLE and re-quotability, not any priced quantity, so repricing is ruled out on its own terms. The observatory lift is documented not-gameplay-migration and saves at V18; `migrateToV33` is the supported route and floors ages. NOT inherited: baseline 755 line 1854 shows the file passing.

### tests/bridge-p05a3-roster-liveness.test.ts > P05A.3 — sign quote → commit breaks the deadlock (§17-A/E/F/H) > refuses an unknown or non-signable talent and a missing term in plain language
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree in the same single-file run. Dies at `new BridgeSession(ownerState(),'p05a3-sign-refusals')` (test line 293) with `validateSaveV33: stored age 24.759120920566843 for t-wri-00 disagrees with its provenance, which derives 24 at week 8`, stack `stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 -> validateTalentProvenanceRoot (save.ts:9121)`. SAME TRACED CAUSE as the other 9 in this group: `liftV18Control` (`src/harness/roster-wall/historical-control.ts:14-34`) builds `legacy_age_anchor` rows from the fixture's raw fractional ages and does not floor them, so the lifted live state cannot satisfy validator condition 2 and the bridge refuses to digest it. This case asserts only plain-language refusal sentences for an unknown/non-signable talent and a missing term — no age-derived number appears in it — so the failure is the setup, not repricing. Production is right for the helper's documented observatory purpose (it saves via `makeSaveV18`); the supported live migration `migrateToV33` floors ages and yields a conforming root. NOT inherited: baseline 755 line 1854 shows `✓ (9 tests)`.

### tests/d17b-publicity.test.ts > D-17B §2/§6 — save round-trip and replay determinism > a bought campaign survives export → import byte-identically, stamps and all
- confidence: **measured** · still reproduces: True
- first changed age: n/a — no age moved for a legitimate consumer; the fixture's clock moved without the engine
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree: `validateSaveV33: stored age 34 for t-wri-00 disagrees with its provenance, which derives 35 at week 9`. Cause is the file-local `studioAt` helper at tests/d17b-publicity.test.ts:57-83, whose own doc comment says "no ticking needed": it returns `market: { ...s.market, tick: over.week }` and never calls tick(). materializeAges runs ONLY at the tick tail (src/core/tick.ts:1056, `materializeAges(provenanced, currentTick + 1)`), so a jumped clock leaves every stored age at its week-0 value while the provenance formula moves on. MEASURED with a vite-node probe: t-wri-00's row is {kind:'authored_exact_week', ageAtEntry:34.89983411815981, entryWeek:0}, ageAt(row,0)=34 and ageAt(row,9)=35, and ticking that same world 9 real weeks stores 35 — production materializes correctly. The three sibling cases in the same describe block that use real tick() loops all pass. NOTE, correcting record 767's table: this is NOT the "A13 hand-written age" shape — no age is hand-written here, the WEEK is. Shared cause with the t-wri-01 case below.

### tests/d17b-publicity.test.ts > D-17B §2/§6 — save round-trip and replay determinism > the cooldown clocks survive a reload — a reload cannot buy a second campaign early
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced on the current tree: `validateSaveV33: stored age 24 for t-wri-01 disagrees with its provenance, which derives 25 at week 30`. Same single traced cause as the case above — `studioAt` (tests/d17b-publicity.test.ts:57-83) sets market.tick by hand and never ticks, so materializeAges (src/core/tick.ts:1056) never runs. MEASURED: t-wri-01's row is {kind:'authored_exact_week', ageAtEntry:24.802612004578172, entryWeek:0}; ageAt(row,30)=25 while the fixture leaves 24, and ticking the same world 30 real weeks stores 25, matching the derivation exactly. The later hand-set `market.tick: 31` in this test body is after makeSave and is not implicated.

### tests/p13b-s1-validation.test.ts > P13B-S1 validator refusals on the live save (test 9) > rejects a receipt naming a Scientist who was never employed that week, even with a seat forged to cover it
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced: expected /unemployed Scientist/, got `validateSaveV33: no talent provenance row for t-sci-04`. The mutation pushes `generateScientist(state.seed, 't-sci-04')` straight onto state.talent with no provenance row, so condition 1 refuses first and masks the P13B refusal under test. MEASURED with a probe on the same base2 fixture (week 263, 86 people, 86 rows): repeating the mutation in the shape production's own append site uses — src/core/actions.ts:2854-2856 `recruitScientist`, which stores `{...scientist, age: Math.floor(scientist.age)}` and anchors the row on the EXACT age via `withTalentProvenance` — makes the intended refusal fire: "Technology save: research receipt names an unemployed Scientist". Both halves are needed: `generateScientist` returns a fractional age (46.03695448152847), so appending a row alone still trips condition 2. Production is right; `recruitScientist` already floors and anchors correctly. Shared cause with the two cases below.

### tests/p13b-s1-validation.test.ts > P13B-S1 validator refusals on the live save (test 9) > rejects a fifth unreleased seat on a four-seat Laboratory
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced: expected /exceed capacity/, got `validateSaveV33: no talent provenance row for t-sci-04`. Same single traced cause as the case above — an unprovenanced `generateScientist` push onto state.talent. MEASURED on the base4 fixture: with the production-shaped setup (floor the stored age, anchor the row on the exact age via withTalentProvenance, as src/core/actions.ts:2854-2856 does) the intended refusal fires: "Technology save: Laboratory seats exceed capacity". Ruled out as a production defect: validateSaveV33 running its own root check before delegating to the frozen chain is a defensible order (the validator's own comment at src/core/save.ts:9066-9069 explains the ordering it does fix, checks 4/5 before 2, for attribution), and the forged state is genuinely invalid either way — the test just asserts on the older of two true refusals.

### tests/p13b-s2-validation.test.ts > P13B-S2 validator refusals for the per-Laboratory facts (test 7) > (d) rejects a fifth unreleased seat on one Laboratory when it is spread ACROSS two different technology projects
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced by running the file on the current tree: expected /Laboratory seats exceed capacity/, got `validateSaveV33: no talent provenance row for t-sci-outsider` (1 failed, 14 passed). Identical shape and identical traced cause to the two p13b-s1 cases: tests/p13b-s2-validation.test.ts:89 does `state.talent.push(generateScientist(state.seed, 't-sci-outsider'))` with no provenance row and an unfloored fractional age. The remedy measured on the s1 fixtures — store Math.floor(age) and append the row via withTalentProvenance, exactly as src/core/actions.ts:2854-2856 `recruitScientist` does — applies unchanged here.

### tests/p13b-r07-save-v25.test.ts > P13B-S5-R07 Save V25 (test 5) > validates a reconstructed V25 envelope with a real rehearsing production and setup: null on a nonempty workflow
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced: `validateSaveV25: frozen V24 state is invalid — … — validateSaveV12: state has unknown field "talentProvenance"`. Cause is the file-local `asV25Envelope` (tests/p13b-r07-save-v25.test.ts:149-178), which hand-builds a V25 envelope out of a LIVE world and deletes each newer root by name — it already carries a sweep line for `talentMarket` (P14A.1), `firstTakes`/`promises` (P14B.1) and `relationships` (P14B.5). C.1 adds `talentProvenance` and the matching line is missing, so the frozen V12 exact-key law correctly refuses a root V25 never had. MEASURED with a scratchpad replica of the helper: with `delete stripped.talentProvenance` added, migrateToV25 succeeds (saveVersion 25, 1 workflow, setup === null) — the whole case passes. Production is right: no supported path down-projects a live V33 world to V25, and convertV33ToV32 (src/core/save.ts:9203) already refuses a native downgrade for exactly this reason. Shared cause with the downgrade case below.

### tests/p13b-r07-save-v25.test.ts > P13B-S5-R07 Save V25 (test 5) > migrateToV24 / migrateToV23 / migrateToV22 / migrateToV21 / migrateToV20 refuse a V25 save
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Reproduced with the same error text; same single traced cause as the case above — the missing P14C.1 sweep line in `asV25Envelope`, which fails at the setup call `withV25.migrateToV25(asV25Envelope(...))` before any downgrade is attempted. MEASURED in the same replica: with the strip line added, all five downgrades throw as the test expects — "migrateToV24: cannot downgrade SaveFileV25 or discard production setup", and likewise for V23/V22/V21/V20. Nothing about the downgrade refusals themselves moved under C.1.

### tests/production-operations-save-v8.test.ts > Production Operations V1 — frozen V7 to live V8 migration > preserves an active V7 production countdown and continuation exactly
- confidence: **measured** · still reproduces: True
- first changed age: n/a — restoring the fixture's own floats removes every diff
- consumer: n/a
- downstream: n/a
- evidence: Reproduced (2 failed, 18 passed). VERIFIED RATHER THAN ASSUMED, per the group guidance: I rebuilt the case in a probe and did a full recursive field diff of the two compared V16 projections — 12 differences, and every one of them is `state.talent[].age`. Nothing else moved. Boundary week M=0. Cause: `toV7` (tests/production-operations-save-v8.test.ts:67) is `makeSaveV10(live).state`, so the forged V7 file receives the LIVE world's already-INTEGER ages; `liftHistoricalState` (tests/_historicalCurrent.ts) then anchors legacy_age_anchor {ageAtMigration: 47, migrationWeek: 0} for t-act-00 where the uninterrupted world holds authored_exact_week {ageAtEntry: 47.553943954060344, entryWeek: 0}. The birthday phase is destroyed by the fixture's manufactured legacy save, not by production — 12 of 60 people cross a birthday in weeks 1..10 in the continuous world and none do in the resumed one (10/52 = 19.2%, matching 12/60). DECISIVE: injecting the genuine fractional ages a real pre-C.1 V7 file would carry makes the twin byte-identical again — 0 diffs, `V16 bytes equal: true`. The test file's own header (line 132-155) already states the discipline this needs: a frozen builder may write a historical format only when the migrator can put the identical bytes back, and facts that cannot must be given up deliberately by name.

### tests/bridge-p14b7-promise-waiver.test.ts > P14B.7 group11 — a waived promise is public as an OUTCOME receipt but mints NO public industry activity > the promiseOutcome receipt exists after a waiver, and no industry Pulse activity carries its eventId
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: The fixture helper boundOpenP1() (tests/bridge-p14b7-promise-waiver.test.ts:93) returns convertV31ToV32(genuineV31).state and uses it as a LIVE GameState. Measured: that state has Object.hasOwn(state,'talentProvenance') === false, week 52, 84 people with fractional ages (43.40522199102273, 33.00749945884644, ...) — correct, since V32 has no such root. The test's allActivities() -> industryPage -> stateDigest -> exportSaveJson -> makeSave -> validateSaveV33 then throws 'validateSaveV33: talentProvenance root missing' (save.ts:9143). Measured fix-shape: makeSave(convertV32ToV33({saveVersion:32,...}).state) succeeds. Production is right — a live state must carry the root (contract 762 §6) — and the other five cases in this file pass because only group11 reaches makeSave. Not inherited: 755 line 1773 shows 6/6 green.

### tests/facility-move-demolish.test.ts > C1-M3a (F) — saves, boundaries, and determinism > keeps the refund row out of every historical save format (law 19)
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: The test forges a V11 envelope from a LIVE save (tests/facility-move-demolish.test.ts:794 JSON.parse(exportSave(makeSave(razed)))) and hand-deletes every root newer than V11 so the demolition-refund guard is the first violation — the file's own comments record doing exactly this for the V14, P06A, P08A, P09, R05, S3, P14A.1, P14B.1 and P14B.5 roots. talentProvenance is the one root not on that list. Measured by replicating the forgery: keeping the root reproduces 'validateSaveV11: state has unknown field "talentProvenance"'; adding one more delete yields exactly the expected 'validateSaveV11: state.ledger[15] contains SaveFileV13 facility demolition authority', which matches the test's regex. So the guard under test is intact, the closed-world V11 refusal is correct, and the strip list is one line stale. No age path is involved beyond the root's existence. Not inherited: 755 line 728 shows 30/30 green.

### tests/p13b-s8-finance.test.ts > P13B-S8 rival finance: typed kinds and interval Opex reconcile, migration frozen, legacy grandfathered (test 4) > migration basis frozen: a GENUINE V26 period (ten keys), lifted through the real migrateToV31, carries the four new keys at zero and its nine original movements byte-identical
- confidence: **measured** · still reproduces: True
- first changed age: n/a (the refusal is on the row KIND, not on any age value; stored ages are consistent — the envelope validates as V33)
- consumer: n/a — no production caller. grep over src/, bridge/, ui/src/, scripts/ finds migrateToV26 / migrateToV31 / convertV33ToV32 only inside src/core/save.ts and as re-exports in src/core/index.ts, so the downgrade chain has no consumer outside tests.
- downstream: n/a
- evidence: The test mints its 'genuine V26' fixture by DOWNGRADING a live world: makeSave(p13aGeneratedStudio('p13b-s8-finance-01')) then save.migrateToV26(...). Measured: that envelope is saveVersion 33, market.tick 0, boundaryWeek 0, 84 provenance rows ALL kind 'authored_exact_week' (row0 {ageAtEntry:47.445061789257494, entryWeek:0}, stored age 47); migrateToV26 -> convertV33ToV32 (save.ts:9215) throws 'cannot downgrade SaveFileV33 — t-wri-00 entered at a known week under V33 and has no V32 ancestor age to restore'. tick === boundaryWeek, so only the row-kind clause bites. That refusal is pinned by contract 762 §6 and §12 F3 ('a fresh V33 campaign is therefore NOT downgradable'), so production matches its own law and the fixture route is what must change (mint the V26 bytes at the old writer, per the standing fixture rule). Not inherited: 755 line 1974 shows 5/5 green. Noted, not asserted as a defect: ageAtEntry still holds the exact pre-C.1 float, so a boundary downgrade COULD reproduce the old bytes — the refusal is an identity rule, not a data limitation (see openQuestion).

### tests/property-state-v13.test.ts > C1-M1a (a) — the property IS the constants, in a new representation > plays a migrated V12 world byte-identically to a native V13 world for 30+ weeks
- confidence: **measured** · still reproduces: True
- first changed age: n/a — measured: every stored talent age is identical in the two twins (JSON of native ages === JSON of migrated ages). Only the provenance ANCHOR moved: native {authored_exact_week, ageAtEntry 48.33820189320287, entryWeek 0} vs migrated {legacy_age_anchor, ageAtMigration 48, migrationWeek 0} for t-wri-00.
- consumer: n/a at the failing assertion (it is a whole-state stableStringify pin at week 0, before the 30-week run).
- downstream: n/a for the failing assertion, but recorded: the two twins would age on different schedules — native `due` is spread (week 1 t-act-02, week 2 t-act-12, week 3 t-dir-04, ...), the migrated twin's `due` is one bucket at week 52 holding all 60 people.
- evidence: Measured by replicating the test's chain: stableStringify differs first at offset 259367, inside "talentProvenance"; stripping that one root from both makes the two states byte-identical ('EQUAL WITHOUT ROOT true'). Cause: worldgen stores floor(age) and keeps the float only in the root (worldgen.ts:719,809); makeSaveV12 projects the root away; tests/_historicalCurrent.ts:liftHistoricalState rebuilds a legacy_age_anchor from the already-floored age, so the phase is unrecoverable and every birthday collapses onto week 52. This is the same cause the 767 record calls 'birthday PHASE loss in a projected twin' for v14-migration.contract. Evidence it is test-only here: the loss needs an integer source age, which only a TODAY-generated world has; a genuine pre-C.1 file still carries floats (measured 43.40522199102273 in the genuine V31 corpus fixture), so migrateToCurrentControl of a real old save keeps its phase. Production (worldgen root + the lift mirroring convertV32ToV33) is doing what 762 §6/§12 F3 pins; the test's byte-identity premise across a lossy V12 projection is what no longer holds. Not inherited: 755 line 734 shows 24/24 green.

### tests/p14b7-promise-waiver.test.ts > P14B.7 group9 — Save V31 -> V32: supersededByPromiseId opens null, downgrade refuses non-null > opens supersededByPromiseId: null on EVERY existing promise record, recomputing nothing else
- confidence: **measured** · still reproduces: True
- first changed age: n/a
- consumer: n/a
- downstream: n/a
- evidence: Line 661 is `expect(v32.saveVersion).toBe(33)` on the output of convertV31ToV32. Measured: convertV31ToV32 returns saveVersion 32 (save.ts:8949-8957 stamps 32 and validates through validateSaveV32); LIVE_SAVE_VERSION is 33. `git log -p -- tests/p14b7-promise-waiver.test.ts` shows commit a04fa398 ('P14C.1: materialized aging implemented') changed three literals in this describe block 32 -> 33: the LIVE_SAVE_VERSION pin (correct), this converter-output pin (wrong), and the staged envelope of the downgrade test (harmless, since convertV32ToV31 refuses on the non-null link before any version check). Stamping 33 here is not merely unpinned but unsatisfiable: a V33 envelope must carry talentProvenance and this converter builds none, so validateSaveV33 would refuse it. Production is right; the sweep over-applied. Not the A13 hand-written-age idiom, and not inherited: 755 line 6 shows 32/32 green.

## Category: approved_behavioral_change (the intended repricing)

### tests/p14b4-rival-seating-preference.test.ts > default seed: the first screenplay-planning call and first film decision are byte-identical to the accepted values; the transcribed ordinary score reproduces the real no-preference choice
- confidence: **measured** · still reproduces: True
- first changed age: Every studio-aca408ec-r01 entry contract at week 0: enterRival (src/core/hollywood.ts:236-238) now commits Math.floor(entryAge) and records the unrounded age as the provenance anchor (contract 762 §12 F3 — a fresh V33 campaign stores INTEGER ages from genesis, 43.40522 -> 43).
- consumer: ageFactor (src/core/employment.ts:262-266) inside offerForTalent (:283-291) -> annualSalary -> rivalWeeklyOperatingCost (src/core/hollywood.ts:104-108) -> the weeklyCost/cashAvailable handed to chooseIndustryPackage (src/core/hollywoodTick.ts:231-235).
- downstream: The first screenplay-planning call's pinned options move: weeklyCost 97680 -> 97705, cashAvailable 23362807 -> 23362209. holdOperatingMargin = -weeklyCost * (PRODUCTION_TICKS + THEATRICAL_WEEKS) moves with it.
- evidence: Reproduced on the current tree (18 failed | 3 passed | 1 todo, identical digests to run 767). The assertion is a hard-coded literal at tests/p14b4-rival-seating-preference.test.ts:390 pinned at HEAD 62ca561a; the received object differs only in weeklyCost (+25) and cashAvailable (-598), with seed/key/lockScreenplay identical. Baseline 755-b8-full-core.txt shows this file at '✓ (22 tests | 1 skipped)', so nothing here is inherited. weeklyCost is payroll only: rivalWeeklyOperatingCost sums weeklySalary(terms.annualSalary), and annualSalary is salaryCurve * ANNUAL_MULT * lengthFactor * ageFactor(talent.age) * jitter.

### tests/p14b4-rival-seating-preference.test.ts > p13a-core-causal-01: every real film decision without members equals the pre-existing choice and pool; every decision seats the law pick over the pool the seam law derives; a null choice only when no permutation is viable
- confidence: **measured** · still reproduces: True
- first changed age: Same as the default-seed case: genesis flooring of every rival's entry age at enterRival/worldgen (contract §12 F3), then one materialized year per 52 weeks, repricing every retention contract signed later in the chain.
- consumer: ageFactor -> offerForTalent -> annualSalary -> rivalWeeklyOperatingCost -> weeklyCost/cashAvailable at chooseIndustryPackage -> holdOperatingMargin and the affordability gate -> the marketing menu pick.
- downstream: The 1015-row chain digest over [week, studioId, key, pool, chosen, budget] moves 2aec0184... -> 699435479e... The structure does not: same 1015 decisions, same 51 pictures, same 724 control rows.
- evidence: Measured: the failure is at the sha pin, :461. Every law assertion ABOVE it passed — r.ok on all 1015 rows, pool === seam, pool === firstThree on controls, and the full G10-b admissions table (r01 w208 script-0006/script-0011 admitting r02-0 on promise-12, r02 284 rows w208..w349 on promise-1). So the seating law still holds row for row; only pinned bytes moved. The concrete moved byte is visible in the same run's SEAM WITNESS print for w208: cashAvailable 8686605.262935068 -> 8716950.262935068, score -1366501 -> -1344278, marketing 594746 -> 594737, cast unchanged. Baseline 755 has this file fully green.

### tests/p14b4-rival-seating-preference.test.ts > seed-b: every real film decision without members equals the pre-existing choice and pool; every decision seats the law pick over the pool the seam law derives; a null choice only when no permutation is viable
- confidence: **measured** · still reproduces: True
- first changed age: Same payroll chain on seed-b's rivals (studio-bc14baf6-*), whose entry ages were floored at week 0 and then advance one year per 52 weeks.
- consumer: ageFactor -> offerForTalent -> annualSalary -> rivalWeeklyOperatingCost -> weeklyCost/cashAvailable at chooseIndustryPackage.
- downstream: The 134-row (w < 211) chain digest moves d3218295... -> f9cd2a8636... with no structural change: 1083 decisions, 98 pictures, 669 without members, identical to baseline 755.
- evidence: Measured: failure is the sha pin at :461; every r.ok / pool === seam / admissions assertion above it passed, and `expect(admissions).toEqual([])` for seed-b still holds, so no pool departed from the historical first three. The three printed counts are byte-identical to baseline 755 line 63-64. Baseline had this case green, so not inherited.

### tests/p14b4-rival-seating-preference.test.ts > search table: the first seed-b decision whose seating matters holds two tagged leadOrAntagonist beneficiaries and one P1 beneficiary in a three-actor pool
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 (and, by the same route, -r01-2): minted by enterRival at week 0 with a drawn age under 30 and zero workHistory, frozen there before C.1 because pre-C.1 tick.ts never wrote talent.age (checked on 975fe6c7). C.1 materializes age(w) = floor(entryAge + w/52), so by w202 (+3.88 years) their stored age is >= 30.
- consumer: isProven (src/core/talentMarket.ts:690-692, `careerIdentity(...).identityDisciplines.length > 0 || talent.age >= 30`) read by authorRivalPromise (:1325), which offers a PROVEN person only APPEARANCE_COUNT and an UNPROVEN person the flexible LEAD_OR_SIGNIFICANT_ROLE_COUNT/leadOrAntagonist first.
- downstream: promiseCastSlots turns APPEARANCE_COUNT into the full ['lead','antagonist','support'] mask, so benefitOf returns the same count for every BILLINGS permutation. The scan's witness predicate `viable.some(p => p.benefit < max)` can never be true, so scan('seed-b').witness is undefined across all 350 ticks and witness() throws UNEXECUTED natural premise.
- evidence: MEASURED: the archetype flip itself is visible in the sibling failure at :599 — person-studio-bc14baf6-r01-4's own natural CURRENT retention promise at w202 now carries mask ['lead','antagonist','support'] where baseline had ['lead','antagonist'], which is exactly and only the isProven branch of authorRivalPromise. MEASURED: no witness exists anywhere in the scan (this test, and :643's bare `assert.ok(s.witness)`, both fail), while membership itself is untouched — the sibling RED still prints 'SEED-B MEMBER DECISIONS 414 (8 pictures); MISSES 0', identical to baseline 755. REASONED: that the specific failing witness condition is benefit uniformity rather than inPool or viability — I could not print the w211 masks, because witness() throws before the table is logged. Baseline 755 had this case green.

### tests/p14b4-rival-seating-preference.test.ts > RED: the real seating serves the maximal DISTINCT-beneficiary count (plan :215-218), the picture is viable and affordable (:223-225), and the real first take then satisfies all three through advancePromisesWeek/qualifyingTakes
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (see the search-table finding — same single traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: Every pool member generic, so benefit no longer discriminates and no natural witness is found; witness() throws at :317 before this case reaches any of its own assertions.
- evidence: Fails at tests/p14b4-rival-seating-preference.test.ts:491 through witness() with 'UNEXECUTED natural premise'. Shares one traced cause with the other 12 witness-gated cases: the measured mask flip at :599 plus unchanged member counts (414 member decisions, MISSES 0). Green in baseline 755, so not inherited. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > CONFLICT: the ordinary economic score prefers a two-beneficiary permutation; benefit count wins over score (:216-217) and the real take proves it
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause as the search-table finding).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: The whole premise of this case is a benefit/score conflict; with every member generic there is no permutation that serves fewer beneficiaries than the max, so the conflict cannot occur naturally anywhere in seed-b.
- evidence: Fails at :517 through witness() at :317. Same single traced cause as the other witness-gated cases (measured mask flip at :599). This case is the clearest loss of coverage, not just of a fixture: the benefit-over-score law now has no natural witness on this seed. Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > TIE FALLBACK (labeled reception-input variant): two reception-identical tagged beneficiaries tie on score; the first strict-greater winner in the inherited BILLINGS order is seated — inherited tie behaviour, not fairness (:218-221)
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before twinVariant runs, so the tie variant is never built.
- evidence: Fails at :550 through witness() at :317; same traced cause as the other witness-gated cases. NOTE FOR WHOEVER REPAIRS THIS ONE: a second, latent A13-shaped hazard sits behind the premise. twinVariant (tests/p14b4-rival-seating-preference.test.ts:300-311) copies person-studio-bc14baf6-r01-4's whole Talent record — including `age` — onto -r01-2's id and then calls live() -> makeSave -> validateSaveV33, whose condition 2 requires talent[i].age === ageAt(row_i, market.tick) against -r01-2's OWN provenance row. If the two anchors floor to different ages at w211 that save is refused. It is not the current cause and I did not reach it. Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > CURRENT unaccepted offers never count: seed-b w202, three rivals decide while their own retention offers carry promises with contractId null; re-authored offers make the negative discriminating
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4: entered r01 at week 0 under 30 with zero workHistory; C.1's materialized age puts him at >= 30 by w202. Pre-C.1 his stored age never moved (no talent.age write exists in tick.ts at 975fe6c7).
- consumer: isProven (src/core/talentMarket.ts:690-692) read by authorRivalPromise (:1325): PROVEN -> APPEARANCE_COUNT only; UNPROVEN -> LEAD_OR_SIGNIFICANT_ROLE_COUNT/leadOrAntagonist first.
- downstream: r01's own w202 retention promise for -r01-4 is now minted APPEARANCE_COUNT, so promiseCastSlots returns ['lead','antagonist','support'] instead of ['lead','antagonist'].
- evidence: THE MEASURED ROOT OF THIS WHOLE GROUP. The assertion diff at :599 is a single added 'support' on person-studio-bc14baf6-r01-4's mask; -r01-3 (re-authored p2lead) and -r01-2 (re-authored p1) match exactly, so only the one NATURAL promise moved. Everything upstream passed: the same three roots promise-4/6/8 at w202, the same three deciding studios, membersAt still {certain: [], ambiguous: []}, r.ok true — i.e. the 'CURRENT offers never count' law itself is intact. Only the pinned family of a naturally minted promise moved. Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > another issuer's promise never counts: r02's offers to r01's three actors are present at w202 and at the witness and never enter r01's member set; re-authored r02 offers make the negative discriminating
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause as the search-table finding).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: The case checks the foreign-issuer exclusion at both w202 and the witness; the witness half throws first.
- evidence: Fails at :621 through witness() at :317, after its own w202 half had already run. Same single traced cause (measured mask flip at :599). Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > a met promise never counts (control, r02 chain): after r02's w211 picture satisfies its three P1 roots at w216, its next real decision has no member and takes the ordinary pick
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: scan('seed-b').witness is undefined, so the bare `assert.ok(s.witness)` fails with 'undefined == true'.
- evidence: Fails at :643 with a bare assert (no witness() message), which is why it reads 'undefined == true' rather than 'UNEXECUTED natural premise' — same missing object, different assertion site. Ironically this case is the r02 control, whose own members were already all-generic in baseline; it only borrows the witness state as a starting point. Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > a met promise never counts (r01 chain, RED today only through the witness defect): after the witness take the SATISFIED roots leave r01's member set at its next real decision
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws; the r01 met-promise chain is never walked.
- evidence: Fails at :661 through witness() at :317. Same single traced cause (measured mask flip at :599). Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > a promise whose window excludes the prospective take never counts — 'window starts after every plausible t…'
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before the window variant is constructed.
- evidence: One of two .each rows failing together at :683 through witness() at :317. Same single traced cause (measured mask flip at :599). Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > a promise whose window excludes the prospective take never counts — 'window ends before any take can land …'
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before the window variant is constructed.
- evidence: The second of two .each rows failing together at :683 through witness() at :317; vitest stacks both headers under one trailing error. Same single traced cause (measured mask flip at :599). Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > mask derivation through the shared reader — 'tagged lead stays exact: r01-2 counts…'
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before the mask variant is constructed.
- evidence: One of three .each rows failing together at :707 through witness() at :317. Same single traced cause (measured mask flip at :599). Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > mask derivation through the shared reader — 'legacy count-only LEAD_OR_SIGNIFICANT…'
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before the mask variant is constructed.
- evidence: The second of three .each rows failing together at :707 through witness() at :317; vitest stacks all three headers under one trailing error. Same single traced cause (measured mask flip at :599). Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > mask derivation through the shared reader — 'APPEARANCE_COUNT is generic cast'
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws before the mask variant is constructed.
- evidence: The third of three .each rows failing together at :707 through witness() at :317. Same single traced cause (measured mask flip at :599). Green in baseline 755. Reproduced on the current tree.

### tests/p14b4-rival-seating-preference.test.ts > promised non-primary-actor beneficiaries (the witness rival's writer, director and craft, each bound OPEN P1) are never seated: one person cannot double, and the sole holder of a role is a capacity constraint, not a forged package
- confidence: **reasoned** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 / -r01-2 crossing 30 under materialized aging (same traced cause).
- consumer: isProven (talentMarket.ts:690) -> authorRivalPromise's promise family (:1325) -> promiseCastSlots mask.
- downstream: witness() throws; the writer/director/craft capacity check is never run.
- evidence: Fails at :728 through witness() at :317. Same single traced cause (measured mask flip at :599). The companion positive seam case (:790, default seed w208) still finds its seam witness and still admits r02-0 with the identical cast, so the seam law itself is unaffected — only seed-b's witness premise is gone. Green in baseline 755.

### tests/p14b4-rival-seating-preference.test.ts > the positive seam witness (plan :225-227, :233; record 621 R2 / G10-b): the first default-seed picture whose pool seats a promised person outside the historical first three admits r01's sole WRITER — a non-primary-actor beneficiary — and the real package and first take serve his promise
- confidence: **measured** · still reproduces: True
- first changed age: studio-aca408ec-r01's w208 team (r02's people after the w208 settlement): their retention contracts are priced at w208 on ages that are now floored at genesis (contract §12 F3) and then advanced ~4 years by materialization, so weeklyCost falls.
- consumer: ageFactor (src/core/employment.ts:262-266) inside offerForTalent -> annualSalary -> rivalWeeklyOperatingCost (hollywood.ts:104-108) -> weeklyCost and cashAvailable at hollywoodTick.ts:190 -> holdOperatingMargin at hollywoodPolicy.ts:63.
- downstream: The w208 package's ordinary score rises from -1366501 to -1344278 and the marketing menu pick drops from 594746 to 594737; the negative (3033668) and the whole cast are unchanged.
- evidence: MEASURED end to end from the printed SEAM WITNESS tables. Baseline 755 line 420: cashAvailable 8686605.262935068, score -1366501, budget 3033668+594746. Current run: cashAvailable 8716950.262935068 (+30345), score -1344278, budget 3033668+594737. All six permutation rows keep benefit=3 and the same five NOT VIABLE, and law === ordinary === the same cast. Critically, the assertion at :795 `expect(decision.result.budget).toEqual(law.ordinary!.budget)` PASSED — the engine still agrees with the transcribed law — and only the hard-coded literal at :796 (pinned at fc1c7b07) disagrees. This is a byte pin on a repriced number, not a law break. Green in baseline 755.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 genuine submit/settle/take routes, no synthetic commitments > RIVAL: a genuinely naturally authored tagged commitment binds and qualifies through the real scheduled owner transition
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-4 (the w215 lead) and person-studio-bc14baf6-r01-2 (the support): provenance row {kind:'authored_exact_week', ageAtEntry:28, entryWeek:0} — the exact integer 28 comes from the pre-existing authored-rival-template raise at src/core/hollywood.ts:230 (Math.max(28, person.age)). Pre-C.1 the stored age stayed 28 for the whole campaign; under C.1 materializeAges writes floor(28 + w/52), so 29 at w52, 30 at w104, and 32 at w208. Flooring at genesis is NOT the trigger: I measured all 60 worldgen people on seed-b and isProven flipped for exactly 0 of them at week 0 (floor(x)>=30 iff x>=30). Aging is the whole cause.
- consumer: isProven, src/core/talentMarket.ts:693 (`talent.age >= 30`), read by authorRivalPromise at src/core/talentMarket.ts:1332: `for (const attachment of isProven(state, talentId) ? [p1] : [flexible, p1])`. A proven beneficiary is never offered the flexible LEAD_OR_SIGNIFICANT_ROLE_COUNT / castRoleCount candidate at all.
- downstream: Both beneficiaries are proven (by age, with empty identityDisciplines) at the w208 authoring, so every promise the rival authors on studio-bc14baf6-r01:film:23 is APPEARANCE_COUNT {count:1}. Measured at w215: lead person-studio-bc14baf6-r01-4 -> promise-8 APPEARANCE_COUNT; antagonist person-studio-bc14baf6-r01-3 -> promise-6 APPEARANCE_COUNT; support person-studio-bc14baf6-r01-2 -> promise-4 APPEARANCE_COUNT. The test file's own comment pins the pre-C.1 state of this chain: 'lead promise-8 (r01-4, tagged leadOrAntagonist) ... support promise-6 (r01-3, P1)'. So `genuine` — a LEAD_OR_SIGNIFICANT_ROLE_COUNT root with a castRoleCount predicate on a lead seat (or an antagonist seat whose seatClass is leadOrAntagonist) — has no natural witness anywhere in 350 ticks. This case is the ONLY one of the nine that actually needs `genuine`; its premise is genuinely unreachable.
- evidence: Reproduced on the current tree (HEAD 2d3a00fd): `node_modules/.bin/vitest run tests/p14b4-cast-class-outcomes.test.ts --minWorkers=1 --maxWorkers=1` -> 9 failed / 14 passed (23), identical case names and identical error to run 767. Baseline 755 (755-b8-full-core.txt line 890) shows this file green at 23/23, so not inherited. I then re-ran the exact rivalWorlds() search predicate against a live seed-b chain (esbuild stdin bundle piped to node; no files written, nothing in the repo touched) and measured: all three slots ARE still found, at week 215, on the same film studio-bc14baf6-r01:film:23, but `genuine` is null at every one of the 6 qualifying (film,slot,week) hits because all six promises are APPEARANCE_COUNT. A second probe printed each cast member's provenance row, stored age, careerIdentity disciplines and the promise's feasibilityReceipt.week (208). Arithmetic verified: floor(28 + 215/52) = 32, matching the stored 32.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'lead' with actual 'lead' seat: qualification=true
- confidence: **measured** · still reproduces: True
- first changed age: Same chain as the RIVAL genuine case: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry exactly 28 at entryWeek 0 (src/core/hollywood.ts:230), frozen at 28 pre-C.1, now floor(28 + w/52) = 30 at w104 and 32 at the w208 authoring.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral, and this is the load-bearing distinction: this case's OWN premise is still met. It calls atFive('rival','lead'), which needs only a bound OPEN root on the lead seat of a rival film at remainingTicks 5 — measured present at w215 on studio-bc14baf6-r01:film:23 (promise-8) — and it rewrites the family itself via variant(..., p2('lead')), so it never needed a naturally tagged root. It dies because rivalWorlds() (tests/p14b4-cast-class-outcomes.test.ts:239-247) will not return unless slots.lead && slots.antagonist && slots.support && genuine are ALL satisfied, and `genuine` is the one that is now unreachable. One conjunction takes eight otherwise-satisfiable cases down with it.
- evidence: Same two probes as the RIVAL genuine finding, which is the shared traced cause: the live seed-b search reports firstFound.lead = {w:215, film:'studio-bc14baf6-r01:film:23', promise:'promise-8'} while genuine stays null through all 350 ticks. Vitest re-run on HEAD 2d3a00fd reproduces this exact case name (9 failed / 14 passed), with the stack atFive (line 250) -> rivalWorlds (line 247) from line 378. Baseline 755 has the file green at 23/23, so not inherited.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'lead' with actual 'antagonist' seat: qualification=false
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause. Its own premise (a bound OPEN root on the antagonist seat) is measured present at w215 — person-studio-bc14baf6-r01-3, promise-6 — and the test supplies its own tagged material through variant(). It fails only because rivalWorlds() refuses to return without `genuine`.
- evidence: Same two probes as the RIVAL genuine finding (shared cause): firstFound.antagonist = {w:215, film:'studio-bc14baf6-r01:film:23', promise:'promise-6'}, genuine null through 350 ticks. Vitest re-run on HEAD 2d3a00fd reproduces this case name; baseline 755 green at 23/23.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'lead' with actual 'support' seat: qualification=false
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause. firstFound.support is measured present at w215 (person-studio-bc14baf6-r01-2, promise-4); the case dies on rivalWorlds()'s `genuine` conjunction, not on its own premise.
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name; baseline 755 green at 23/23, so not inherited.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'leadOrAntagonist' with actual 'lead' seat: qualification=true
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause; its own lead-seat premise is measured present at w215 (promise-8) and its tagged material is supplied by variant(). Dies on the `genuine` conjunction in rivalWorlds().
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name among the 9 failures; baseline 755 green at 23/23.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'leadOrAntagonist' with actual 'antagonist' seat: qualification=true
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause; the antagonist-seat premise is measured present at w215 (promise-6). Dies on the `genuine` conjunction.
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name; baseline 755 green at 23/23.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > 'rival' 'leadOrAntagonist' with actual 'support' seat: qualification=false
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause; the support-seat premise is measured present at w215 (promise-4). Dies on the `genuine` conjunction.
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name; baseline 755 green at 23/23.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > rival support qualifies for P1 and shape-legacy count-only P2, not tagged P2
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause. This case (line 388) only needs atFive('rival','support') and installs p1()/legacyP2() material itself, so a naturally tagged root is irrelevant to it; its support-seat premise is measured present at w215. It fails inside rivalWorlds() before its body runs.
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name; the stack in both the run log and my re-run is atFive (line 250) -> rivalWorlds (line 247) from line 388. Baseline 755 green at 23/23.

### tests/p14b4-cast-class-outcomes.test.ts > P14B4 labeled outcome-owner probes on real bound roots and actual casts > rival half-open window counts start, excludes before-start and excludes due
- confidence: **measured** · still reproduces: True
- first changed age: Same chain: person-studio-bc14baf6-r01-4 / -r01-2, ageAtEntry 28 at entryWeek 0, frozen pre-C.1, now 30 at w104 / 32 at w208.
- consumer: isProven (src/core/talentMarket.ts:693) via authorRivalPromise (src/core/talentMarket.ts:1332).
- downstream: Collateral on the same traced cause. It needs atFive('rival','lead') and rewrites the window and family itself; its lead-seat premise is measured present at w215 (promise-8). It fails inside rivalWorlds() at line 409 before touching any window logic.
- evidence: Same two probes as the RIVAL genuine finding (shared cause). Vitest re-run on HEAD 2d3a00fd reproduces this case name; stack atFive (250) -> rivalWorlds (247) from line 409. Baseline 755 green at 23/23.

### tests/p14b4-cast-class-policy.test.ts > P14B4: natural rival policy uses the real shared service, never staged failed candidates > observes flexible-first, actual P1 fallback, proven P1, and a genuine zero-attachment refusal without extra RNG
- confidence: **measured** · still reproduces: True
- first changed age: On the DEFAULT seed's 220-tick scan, three of the twenty-four rival beneficiaries flip from unproven to proven purely by aging, with no credit change: person-studio-aca408ec-r01-5 (anchor 29.927234713302248 -> age 33 at w196), person-studio-aca408ec-r03-2 (anchor exactly 28 from src/core/hollywood.ts:230 -> 31), person-studio-aca408ec-r03-4 (anchor 28.588303918345026 -> 32). All three have empty identityDisciplines, so pre-C.1 (ages frozen at the anchor) isProven was false for each and is now true. r01-5 proves flooring alone is not the cause: 29.927 floors to 29, still unproven at genesis; it is the w52 birthday to 30 that flips it.
- consumer: isProven (src/core/talentMarket.ts:693) read by authorRivalPromise (src/core/talentMarket.ts:1332), and mirrored by the test's own realProven at tests/p14b4-cast-class-policy.test.ts:61-65.
- downstream: Every rival proposal the default-seed scan observes now has a proven beneficiary, so the candidate list is [p1] only and every authored root is APPEARANCE_COUNT -> the only witness kind the scan can record is 'provenP1'. The 'flexibleP2' witness requires an UNPROVEN beneficiary whose FLEX candidate reads REASONABLY_ACHIEVABLE, and the 'neither' witness is explicitly gated on `if (!proven)` at the zero-attachment branch — both are now unreachable on that seed, which is exactly the assertion's diff (received ['provenP1'], missing 'flexibleP2' and 'neither'). The in-loop assertions that check the policy's own shape (expect(calls).toHaveLength(expectedCount), the per-index draft equality) all PASS, so the production policy and the test's model of it still agree; only the population of beneficiaries moved.
- evidence: Reproduced on HEAD 2d3a00fd: `node_modules/.bin/vitest run tests/p14b4-cast-class-policy.test.ts --minWorkers=1 --maxWorkers=1` -> 1 failed / 6 passed (7), same AssertionError and same stdout witness line ('default seed w196 provenP1: studio-aca408ec-r01 -> person-studio-aca408ec-r01-0'). Baseline 755 (line 759) shows the file green at 7/7, so not inherited. I then enumerated every rival proposalSubmitted receipt over the default seed's 220 ticks (72 receipts, 24 distinct beneficiaries, all in week 196) and printed each one's stored age, provenance anchor, careerIdentity disciplines, proven-now and proven-if-ages-frozen: exactly 3 differ, the 3 named above. A separate probe measured the unproven pool over time on both worlds (population constant at 84): seed-b 13 unproven at w0 -> 6 at w215 with 7 flipped by aging; default seed 13 -> 7 with 6 flipped. Same root cause as the nine outcomes cases, reached on a different seed.

### tests/p14b5-relationships.test.ts > family 1 — EDGE MINTING at the tick tail from the advance's delta (scope (2), (2a)) > no RNG reaches the seam: rngState is byte-equal before/after the seam and the tick's rngState equals the frozen control; everything but the root is byte-identical to the frozen post-tick state
- confidence: **measured** · still reproduces: True
- first changed age: convertV32ToV33 flooring at the migration boundary (week 60) of tests/fixtures/p14/genuine-v30-pre-b5/genuine-v30-first-take-at-five.json.gz: 83 of the 84 people move, every row legacy_age_anchor, max |delta| 0.9412217398847034 (pure flooring — nobody crosses a birthday in the one tick). No person is singled out; the whole population floors at once.
- consumer: none reached in this tick. The changed values are the stored talent[i].age fields themselves plus the added talentProvenance root; the test's own bytes() helper (tests/p14b5-relationships.test.ts:216-220) strips only `relationships` and promises' `supersededByPromiseId`, so both land inside the digest.
- downstream: FROZEN.postTakeDigestStripped (line 496) moves from 6403ac2b… to 9702aa68…. Nothing economic moves: the counterfactual proves the rest of the state is byte-identical.
- evidence: Reproduced on the current tree with a scratchpad-only vite-node probe (no repo file created or changed) that rebuilds takeWorld() exactly: lifted fixture → migrateToV33 → assignShootingDirector + scheduleShootingTake → one tick. Measured: week 60→61; after.rngState === FROZEN.rngAfterTake (line 495 passes, materializeAges consumes no RNG per contract §3); sha(bytes(after)) = 9702aa6869cf80f82d5133f68137427a0ed44c07ce987e8fe2be66bbb60f3d78, byte-for-byte the value in 767-c1-full-core-confirm.txt. DECISIVE COUNTERFACTUAL: deleting `talentProvenance` and restoring every talent[i].age to anchorOf(row).age (= ageAtMigration, the exact pre-C.1 stored float, since pre-C.1 nothing aged — see the worldgen/tick hunks of a04fa398) reproduces the frozen pin EXACTLY: 6403ac2bb1dd59249db054732115f9227a104f5f2afd9f699e9f89d393388d21. Stripping the root alone gives a3c996be… (not the pin), so both halves are needed and nothing else in the state moved. Contract 762 §6 discloses the flooring as "the migration's visible cost"; §11 makes the new root unavoidable. Baseline 755 line 9 shows the whole file green (46 tests), so not inherited.

### tests/bridge-p14b5-relationships.test.ts > family 12 — the R-D5 natural-chain LEDGER … > p13a-core-causal-01: chain digests, row counts, churn rosters empty under D1, zero exposed rows through 416; the nine frozen drop templates only
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-aca408ec-r01-0 at week 0: ageAtEntry 44.36540781416331 committed as stored age 44 (the enterRival/worldgen floor added by a04fa398). It is the first row of state.hollywood.employment and therefore the first bytes the digest covers. 43 of the 44 employment rows on this seed are priced from an age the floor moved.
- consumer: ageFactor (src/core/employment.ts:262-266) inside offerForTalent (:281-287), which prices annualSalary = iround(salaryCurve × ANNUAL_MULT × lengthFactor × ageFactor(talent.age) × jitter) and signingBonus = iround(annual × CONTRACT_SIGNING_BONUS_FRACTION). Measured for that person: ageFactor 0.9667019589399015 → 0.96900826446281.
- downstream: terms.annualSalary 394607 → 395548 (+941) and terms.signingBonus 71029 → 71199 (+170) on that employment row, so sha(JSON.stringify(state.hollywood.employment)) moves 09bcc35b… → 4cffcb41… at line 460. On THIS seed the movement stops there.
- evidence: Reproduced by running the file (node_modules/.bin/vitest run tests/bridge-p14b5-relationships.test.ts --minWorkers=1 --maxWorkers=1 → 5 failed / 10 passed, received digest 4cffcb410893395f8ee93da2d7d6146f35db2369962fe333e02de8d2fc7c0b58, identical to the run log). A scratchpad-only probe (no repo file created or changed) re-ran runLedger's exact chain (p13aGeneratedStudio(seed) + 416 ticks) and measured against the 654-T controls: settlementDigest MATCH, talentMarket.receipts MATCH, firstTakes MATCH, rngState MATCH ('2598418427,508725886,1318803286,3129010527'), employment the only pin that moved. So no settlement, no case, no roster, no take and no RNG draw moved on this seed. The pricing law itself was validated rather than assumed: offerForTalent(seed, talentAtWeek0, termWeeks, 0) reproduces all 24 genesis rows' terms EXACTLY (24/24), and re-pricing the same person at the raw anchor age gives the pre-C.1 numbers quoted above. Contract 762 §12 F3 states this consequence in advance ("ageFactor(43) and ageFactor(43.405) differ … that is Trap 1 and Trap 2 arriving together at world generation, and it is intended"). Baseline 755 line 705 shows this file green (15 tests), so not inherited.

### tests/bridge-p14b5-relationships.test.ts > family 12 — the R-D5 natural-chain LEDGER … > seed-b: chain digests, row counts, churn rosters empty under D1, zero exposed rows through 416; the nine frozen drop templates only
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-bc14baf6-r01-0 at week 0: raw anchor 51.56321580896011 committed as 51. 73 of the 76 employment rows on this seed are priced from a moved age.
- consumer: ageFactor in offerForTalent (src/core/employment.ts:262/281-287); the signing bonus then reaches rival cash through moveRivalMoney(b.account,'signing',…) at src/core/hollywoodTick.ts:118/159 and src/core/talentMarket.ts:999, and rival cash is read as cashAvailable = b.account.cash − operatingReserve(…) at src/core/hollywoodTick.ts:190/235 and as the greenlight gate at :211.
- downstream: employment digest ba5ab89e… → 6c7f608c… at line 460, AND — not reported by the run because it fails one line earlier — state.firstTakes also moves (line 461 would fail): the repriced cash changes which package chooseIndustryPackage can afford, so rival casting moves. Same cause class as the ~28 'natural-chain repricing' lines record 767 attributes to p14b4-rival-seating-preference / p14b4-cast-class-outcomes.
- evidence: Same run and same scratchpad probe as the p13a seed. Measured against the 654-T controls at week 416 (76 employment rows): settlementDigest MATCH, talentMarket.receipts MATCH, rngState MATCH ('1640490702,2161102015,891615888,2071390822'), employment MOVED (6c7f608cea8d3ceafef0f844f949805c252f21ebc88d22d36dd09c8a6f7ef855 — identical to the run log), firstTakes MOVED. The settlement/receipt identity rules out any change of who was hired by whom, when, or with what reasons; the price chain above is measured exactly on the p13a seed (24/24 genesis rows reproduced by the live law, pre/post ageFactor and salaries printed). Baseline 755 has this file green, so not inherited.

### tests/bridge-p14b5-relationships.test.ts > family 12 — the R-D5 natural-chain LEDGER … > p13b-s8-bridge-probe-01: chain digests, row counts, churn rosters empty under D1, zero exposed rows through 416; the nine frozen drop templates only
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-efb645e3-r01-0 at week 0: raw anchor 29.222265871549247 committed as 29. 72 of the 76 employment rows on this seed are priced from a moved age.
- consumer: ageFactor in offerForTalent (src/core/employment.ts:262/281-287), then the signing bonus into rival cash (moveRivalMoney 'signing') and cash into chooseIndustryPackage's cashAvailable (src/core/hollywoodTick.ts:190/235).
- downstream: employment digest 6e39a55c… → d4f19ea4… at line 460, and state.firstTakes also moves (line 461, unreported because 460 fails first).
- evidence: Same run and probe. At week 416 (76 rows): settlementDigest MATCH, receipts MATCH, rngState MATCH ('2343039306,887634093,2940629248,1402597496'), employment MOVED to d4f19ea4618c680f60d9b7f4ea395ddc6813291ed261f939e06c4178a86fe8b1 (identical to the run log), firstTakes MOVED. Pricing law validated on the p13a seed (24/24 genesis rows reproduced exactly by offerForTalent at the stored age; re-pricing at the raw anchor age yields the pre-C.1 salary/bonus). Baseline 755 green for this file, so not inherited.

### tests/bridge-p14b5-relationships.test.ts > family 12 — the R-D5 natural-chain LEDGER … > p13-public-commercial-adoption: chain digests, row counts, churn rosters empty under D1, zero exposed rows through 416; the nine frozen drop templates only
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-5a47d054-r01-0 at week 0: raw anchor 31.172746805447233 committed as 31. 62 of the 64 employment rows on this seed are priced from a moved age. (This is the same world as the poachingFixture case below, whose subject person-studio-5a47d054-r04-3 anchors at 29.84081390477671.)
- consumer: ageFactor in offerForTalent (src/core/employment.ts:262/281-287), then signing bonus → rival cash → chooseIndustryPackage cashAvailable (src/core/hollywoodTick.ts:190/235).
- downstream: employment digest bb775d62… → 94e3a106… at line 460, and state.firstTakes also moves (line 461, unreported).
- evidence: Same run and probe. At week 416 (64 rows): settlementDigest MATCH, receipts MATCH, rngState MATCH ('3069080245,1730081600,660681499,2741201056'), employment MOVED to 94e3a1066c95b3b3dc0046b2a09ae32109b2d529297af5eb0d2f7e06bac51049 (identical to the run log), firstTakes MOVED. Pricing law validated on the p13a seed as above. Baseline 755 green for this file, so not inherited.

### tests/bridge-p14b5-relationships.test.ts > family 12 — the R-D5 natural-chain LEDGER … > the MOST EXPOSED shared fixture, poachingFixture (p14b2-fixtures.ts :145-211; consumers bridge-p14b2-trust, p14b2-fixture-preconditions): the week-208 reasons pin and endedWeek 208 hold, and under D1 no survivor holds a shared-take counterpart
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-5a47d054-r04-3 (the fixture's subject) in world p13-public-commercial-adoption. Provenance row: {kind:'authored_exact_week', ageAtEntry:29.84081390477671, entryWeek:0}. Pre-C.1 the stored age was that static float forever; post-C.1 it is 29 at week 0, crosses to 30 at week 9 (floor(29.84081390477671 + 9/52) = floor(30.0139…) = 30, exactly the contract §1 formula), and is 33 at the fixture's week 196. This is the AGING half of C.1, not the flooring — flooring alone would have made the person younger.
- consumer: isProven (src/core/talentMarket.ts:690-693) — `careerIdentity(talent).identityDisciplines.length > 0 || talent.age >= 30`. Measured at week 196: identityDisciplines is [] both ways, so the age clause alone flips the branch (isProven pre = false, post = true). aging.ts's own docstring names this consumer by line.
- downstream: preferredTerm (talentMarket.ts:702-705) returns CONTRACT_TERM_OPTIONS[last] instead of [0], so publicPreferredTerm(state, talentId) is 208 where the fixture pins 52 — the failure at tests/helpers/p14b2-fixtures.ts:164 reached from bridge-p14b5-relationships.test.ts:468. priorityOrder also flips from the unproven order to ['compensation','term','trust','relationships','incumbency','standing','opportunity'] (measured), which is the D3 route the fixture's own :150-157 comment is built on.
- evidence: Reproduced by running the file (assertion reports 'expected 208 to be 52', identical to the run log). Traced with a scratchpad-only vite-node probe (no repo file created or changed) replaying the fixture prefix exactly — fund(p13aGeneratedStudio('p13-public-commercial-adoption')), sign an actor for 52, advanceTo(196) — and printing the provenance row, the stored age, the anchor, both isProven clauses, publicPreferredTerm and publicPriorityOrder. Output: ageAtEntry 29.84081390477671 / entryWeek 0 / stored age 29 at week 0 / 33 at week 196 / identityDisciplines [] / isProven post true, pre false / publicPreferredTerm 208 / proven priorityOrder / first week ageAt(row,w) >= 30 is week 9. NOT the A13 shape: nothing in poachingFixture hand-writes an age, the person ages under the law. SHARED CAUSE: the bridge-p14b2-trust failure at [9/104] in the run log is the same assertion at the same fixture line and has the same trace.

### tests/bridge-p14b2-trust.test.ts > P14B.2 group5 — independent promise attention after cases close > raises due at minus8 but not minus9 for an actual player poaching winner with no proposals
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-5a47d054-r04-3 (seed p13-public-commercial-adoption): provenance row {kind:'authored_exact_week', ageAtEntry:29.84081390477671, entryWeek:0}. Pre-C.1 the stored age was that 29.8408 forever. C.1 floors it to 29 at genesis and materializes 30 at WEEK 9 (floor(29.84081390477671 + 9/52) = floor(30.0139) = 30), then 31/32/33 at weeks 61/113/165. Measured age track: [[0,29],[9,30],[61,31],[113,32],[165,33]].
- consumer: isProven (src/core/talentMarket.ts:690) — measured on the `talent.age >= 30` arm alone: careerIdentity(person).identityDisciplines is [] at week 196 despite workHistory.acting = 13, so the identity arm contributes nothing. isProven feeds preferredTerm (talentMarket.ts:710) and its public read publicPreferredTerm (:725).
- downstream: publicPreferredTerm(state,'person-studio-5a47d054-r04-3') returns TUNING.CONTRACT_TERM_OPTIONS[last] = 208 instead of options[0] = 52, from week 9 onward. tests/helpers/p14b2-fixtures.ts:164 asserts 52 as the shared poachingFixture's premise, throws, and kills this case in setup before any trust or attention assertion runs.
- evidence: Ran `node_modules/.bin/vitest run tests/bridge-p14b2-trust.test.ts --minWorkers=1 --maxWorkers=1` on the current tree (2d3a00fd): 3 failed | 19 passed, identical `expected 208 to be 52` at helpers/p14b2-fixtures.ts:164 via tests/bridge-p14b2-trust.test.ts:292. Then replayed the same world with an in-memory probe (esbuild stdin -> node, no file written, no suite run) printing the age track, the provenance row and publicPreferredTerm per week — values quoted above. NOT inherited: 755-b8-full-core.txt line 915 shows `tests/bridge-p14b2-trust.test.ts (22 tests)` all passing. Trust did not move: the preceding assertion at :162, trustDescriptor(...) matching {label:'Reliable',scope:'studio'}, passes, and `grep -n '\bage\b|\.age' src/core/promises.ts` returns 0 hits — no trust driver reads age on any path.

### tests/bridge-p14b2-trust.test.ts > P14B.2 group5 — independent promise attention after cases close > raises the poaching winner’s actual outcome only in its recorded week, once per cause/person
- confidence: **measured** · still reproduces: True
- first changed age: Same chain as the case above — person-studio-5a47d054-r04-3, ageAtEntry 29.84081390477671 at week 0, materialized to 30 at week 9.
- consumer: isProven (talentMarket.ts:690) -> preferredTerm (:710) -> publicPreferredTerm (:725).
- downstream: publicPreferredTerm 52 -> 208; the shared poachingFixture premise at tests/helpers/p14b2-fixtures.ts:164 throws. poachingFixture only populates its cache on success, so each of the three group5 cases re-runs the fixture and re-throws independently — one traced cause, three failing cases.
- evidence: Same run as above; this case fails at tests/bridge-p14b2-trust.test.ts:311 with the same `expected 208 to be 52` at helpers/p14b2-fixtures.ts:164. Shares one traced cause with the other two bridge cases (fixture premise, not the case body). Passing in baseline 755.

### tests/bridge-p14b2-trust.test.ts > P14B.2 group5 — independent promise attention after cases close > does not require any case entry to retain a real promise reminder (explicit read-only case-carrier probe)
- confidence: **measured** · still reproduces: True
- first changed age: Same chain — person-studio-5a47d054-r04-3, ageAtEntry 29.84081390477671 at week 0, materialized to 30 at week 9.
- consumer: isProven (talentMarket.ts:690) -> preferredTerm (:710) -> publicPreferredTerm (:725).
- downstream: publicPreferredTerm 52 -> 208; poachingFixture throws at tests/helpers/p14b2-fixtures.ts:164. The case's own read-only carrier probe never executes, so nothing about promise reminders or case carriers is implicated.
- evidence: Same run as above; fails at tests/bridge-p14b2-trust.test.ts:339, same assertion and same frame. Shares one traced cause with the other two bridge cases. Passing in baseline 755.

### tests/p14b1-trust-chooser.test.ts > P14B.1 test 6: trust, the widened chooser and the priority order > opportunity changes the winner of an otherwise compensation-versus-incumbency 1-1 tie, with two real surviving proposals
- confidence: **measured** · still reproduces: True
- first changed age: Same person and world as the bridge trio: person-studio-5a47d054-r04-3, seed p13-public-commercial-adoption, ageAtEntry 29.84081390477671 at week 0, materialized 30 at WEEK 9. The test reads at week 196, where the measured age is 33.
- consumer: isProven (talentMarket.ts:690) -> priorityOrder (:702) -> publicPriorityOrder (:721). Not preferredTerm this time, which is why the two files fail on different symbols from one flip.
- downstream: priorityOrder returns the PROVEN order ['compensation','term','trust','relationships','incumbency','standing','opportunity'] instead of the unproven ['opportunity','compensation','relationships','term','trust','standing','incumbency'], so publicPriorityOrder(...)[0] is 'compensation', not 'opportunity'. The test's whole construction — an opportunity-decided 1-1 tie for a capable-but-unproven subject — has no unproven subject left to construct it on.
- evidence: Ran `node_modules/.bin/vitest run tests/p14b1-trust-chooser.test.ts --minWorkers=1 --maxWorkers=1` on 2d3a00fd: 2 failed | 9 passed | 2 todo; this case fails at :338 `expected 'compensation' to be 'opportunity'`. My probe printed publicPriorityOrder(state,talentId)[0] per week for the same seed: 'opportunity' at week 0, 'compensation' from week 9 through 196 — the value the test reads. The assertions immediately before it (employment row endWeekExclusive 208 at :336, incumbent cash > 0 at :337) both pass, so the incumbent's live bid and solvency premise are intact and only the archetype moved. NOT inherited: 755-b8-full-core.txt line 476 shows `tests/p14b1-trust-chooser.test.ts (13 tests | 2 skipped)` with no failures.

### tests/p14b1-trust-chooser.test.ts > P14B.1 test 7: rival symmetry > natural rival authoring preserves the first writer IFF, the first actor positive (P1 if proven, flexible P2 if unproven), an unproven flexible-P2 witness, and actual non-achievable zero-attachment witnesses for a proven and an unproven person
- confidence: **measured** · still reproduces: True
- first changed age: Seed p13a-core-causal-01 (the default p13aGeneratedStudio()). Three subjects supplied every unproven witness this scan ever had, and all three cross 30 under C.1: person-studio-aca408ec-r01-5 (craft, ageAtEntry 29.927234713302248, crosses 30 at WEEK 4 — the first changed age that matters here — and is 33 at week 196); person-studio-aca408ec-r03-4 (actor, ageAtEntry 28.588303918345026, crosses 30 at week 74, is 32 at 196); person-studio-aca408ec-r03-2 (actor, ageAtEntry exactly 28 from the hollywood.ts:221 Math.max(28,…) raise, crosses 30 at week 104, is 31 at 196).
- consumer: isProven (talentMarket.ts:690) -> publicPreferredOpportunity (:731), which is literally what the test computes as `observed.proven` (tests/p14b1-trust-chooser.test.ts:602) and what the rival authoring policy reads to choose the flexible-P2-first read sequence.
- downstream: Measured over the full 220-tick scan: all 72 natural rival proposal submissions land in exactly ONE week, 196, and under C.1 every one of the 72 has publicPreferredOpportunity === 'anyCastAppearance' (proven). Scored against the provenance anchors — the ages that were stored pre-C.1 — 9 of those 72 were unproven, all from the three people above. With zero unproven subjects the `flexible` witness (!proven && reads[0] REASONABLY_ACHIEVABLE, :758) and the `negativeUnproven` witness (!proven && nothing achievable, :774) are unreachable, the visitor can never return true, and scanNaturalRivalAuthoring exhausts 220 weeks and throws at :647.
- evidence: Same vitest run as test 6 on 2d3a00fd: fails with `search premise failed: required natural authoring witnesses absent within 220 weeks`. Two in-memory probes (esbuild stdin -> node; no file written, no suite run) replayed p13aGeneratedStudio() for 220 ticks collecting every rival proposalSubmitted receipt: total 72, weeks-with-submissions = [[196,72]], provenNow {true:72}, provenPre {true:63,false:9}, and the 9 flipped rows are the three people and ages quoted above. The sibling case at :721, which needs only the FIRST submission and therefore only the proven [APPEARANCE_COUNT] sequence, still PASSES in the same run — corroborating that the chain still authors normally and that only the unproven branch lost its witnesses. NOT inherited: baseline 755 shows this file with no failures.

### tests/bridge-owner-ux-projection20-migration.test.ts > Owner UX outgoing projection20 migration > migrates 'frozen Week105 null slot' without changing either V18 gameplay slot
- confidence: **measured** · still reproduces: True
- first changed age: `convertV32ToV33` floors every stored age at the migration week. Measured on this fixture at week 105: all 60 of 60 ages moved, e.g. t-wri-00 47.51480486320066 -> 47, t-dir-* 50.72720958059061 -> 50, t-act-* 24.597620989679243 -> 24. The provenance row written alongside keeps the float unrounded: {"ageAtMigration":47.51480486320066,"kind":"legacy_age_anchor","migrationWeek":105,"personId":"t-wri-00"}.
- consumer: `ageFactor` / `offerForTalent` (src/core/employment.ts:263) and the projected `StudioPersonProfileSnapshot.age` (bridge/schema/bridge-schema.ts:2725), which the migrated slot feeds once BridgeSession hydrates it.
- downstream: Measured: `offerForTalent('q', person, 52, 105)` on t-wri-00 moves annualSalary 1568058 -> 1575089 (+7031, +0.45%). No person in this fixture crosses the age-30 `isProven` boundary (measured: 0 of 60), so no talentMarket priorityOrder branch flips. The slot's state digest therefore changes, which is exactly what the test's own `afterDigest` line expects — the failure is one line earlier.
- evidence: Reproduced on the current tree: `node_modules/.bin/vitest run tests/bridge-owner-ux-projection20-migration.test.ts --minWorkers=1 --maxWorkers=1` -> 3 failed | 7 passed (10), same three cases as the run log. Both files pass in baseline 755 (755-b8-full-core.txt:1861 `✓ tests/bridge-owner-ux-projection20-migration.test.ts (10 tests)`), so not inherited. Classified every `-`/`+` line of the run-log diff (767 lines 2730-25295) after numeric normalisation: the ONLY differences are 60 floored `age` values and the added `talentProvenance` root (boundaryWeek / rows / due). No other state field moved. Two independent contracted causes land in one assertion at tests/bridge-owner-ux-projection20-migration.test.ts:139, `expect(oldRoots).toEqual({ ...before.state, ... })` — line 133's rest-spread strips hollywood, technology, physicalPlans, talentMarket, firstTakes, promises, relationships but not `talentProvenance`, so C.1's new root falls into `oldRoots`; and the floored ages break the equality on `oldRoots.talent`. Contract 762 §6 authorises both: `convertV32ToV33` writes one `legacy_age_anchor` per person holding "the original float, unrounded", "then FLOORS each `talent[i].age`. That first flooring is Trap 1's value-shape change and it is the migration's visible cost." LOSSLESSNESS PROVED, not assumed: ran `convertV33ToV32` on the migrated slot and compared against the direct V18->V32 chain — full state key-normalised equal `true`, ages restored exactly `true`. The fixture carries 0 integer ages in 60 people (measured on both p20-before-hire and p20-after-hire), so record 767's open birthday-phase-loss question does not bite this path: every anchor here is fractional and recovers its own float.

### tests/bridge-owner-ux-projection20-migration.test.ts > Owner UX outgoing projection20 migration > migrates 'frozen R12 Week106 null slot and genuine two-command journal' without changing either V18 gameplay slot
- confidence: **measured** · still reproduces: True
- first changed age: Same cause, one week later: `convertV32ToV33` floors all 60 stored ages at migration week 106, writing `legacy_age_anchor` rows with `migrationWeek: 106` and the unrounded float in `ageAtMigration` (the run-log diff shows e.g. ageAtMigration 27.38144414103581 / 53.08841319122058 / 66.59623308813678 for t-cra-06/07/09 at migrationWeek 105 and 106 across the two slots).
- consumer: `ageFactor` / `offerForTalent` (src/core/employment.ts:263) and projected `StudioPersonProfileSnapshot.age` (bridge/schema/bridge-schema.ts:2725).
- downstream: Same ~0.45%-scale contract-quote shift measured on the sibling slot; the migrated slot's state digest changes. This fixture also carries 0 integer ages in 60 people, so no birthday phase is lost.
- evidence: ONE TRACED CAUSE SHARED WITH THE OTHER TWO CASES in this file — it.each over the same `cases` array runs the identical body, failing at tests/bridge-owner-ux-projection20-migration.test.ts:139. Reproduced on the current tree (3 failed | 7 passed). Absent from baseline 755 (755-b8-full-core.txt:1861 shows all 10 passing). The only diffs are the un-enumerated `talentProvenance` root plus floored ages; verified by normalising every diff line in the run log. The V33->V32 downgrade was proved lossless on the sibling fixture under the same code path (`market.tick === boundaryWeek`, all rows `legacy_age_anchor`, contract 762 §6 predicate), and `p20-after-hire` likewise has 0 integer ages in 60 people, so the same recovery holds here. Not a production defect: the migration writes the contracted root, preserves the exact anchor float, and floors as 762 §6 specifies.

### tests/bridge-owner-ux-projection20-migration.test.ts > Owner UX outgoing projection20 migration > migrates 'constructed distinct-slot envelope from frozen Week106/current and Week105/saved bytes' without changing either V18 gameplay slot
- confidence: **measured** · still reproduces: True
- first changed age: Both slots of the constructed envelope floor independently at their own migration week — the current slot at week 106 and the saved slot at week 105 — because the test body loops `[[currentSaveJson…],[savedSaveJson…]]`. 60 ages per slot, e.g. t-wri-00 47.51480486320066 -> 47 at week 105.
- consumer: `ageFactor` / `offerForTalent` (src/core/employment.ts:263) and projected `StudioPersonProfileSnapshot.age` (bridge/schema/bridge-schema.ts:2725).
- downstream: Measured +7031 on t-wri-00's annual quote at week 105; both slot digests change. 0 of 60 people cross the age-30 `isProven` boundary in either slot, so no market branch flips.
- evidence: ONE TRACED CAUSE SHARED WITH THE OTHER TWO CASES in this file. This case is the only one exercising a non-null `savedSaveJson`, so it proves the migration floors and roots BOTH slots rather than only the current one — which is correct behaviour, not a defect: each slot is its own save and gets its own `boundaryWeek`. Reproduced on the current tree (3 failed | 7 passed); absent from baseline 755 (all 10 passed at 755-b8-full-core.txt:1861). The three sibling cases in the same file that do NOT compare old roots — 'persists … once, then preserves the new session on restart' for all three fixtures — still pass, which measures that the V33 envelope loads, validates and persists correctly; only the frozen-old-roots equality at line 139 fails. Downgrade losslessness verified on the same code path (`convertV33ToV32` output key-normalised equal to the direct V18->V32 chain, ages restored exactly).

### tests/ruling-a-development-in-play.test.ts > RULING A — development applies exactly once per released production > advancing PAST a release does not re-develop (the released production is gone from active)
- confidence: **measured** · still reproduces: True
- first changed age: Seed 'A-once-2'. `released` sits at week 9 (TICKS_TO_RELEASE = TUNING.PRODUCTION_TICKS + 1 = 9, measured), then `advanceDev(released, 10)` runs to week 19. The first changed age is t-cra-02, stored 25 on an anchor of 25.803859853378853 at week 0, whose scheduled birthday week is 11 — inside the window. Exactly 9 people have a scheduled birthday in weeks 10..19 (measured from `generateWorld('A-once-2').talentProvenance.due`: week 11 t-cra-02; week 12 t-wri-02, t-dir-02; week 13 t-act-05; week 15 t-act-11, t-act-15, t-cra-04, t-cra-05; week 16 t-cra-09), and the run log shows exactly 9 age pairs, each +1 (25->26, 26->27, 42->43, …).
- consumer: `materializeAges` at the tick tail (src/core/aging.ts, called from src/core/tick.ts:1049) writes `ageAt(row, currentTick + 1)` onto `state.talent[i].age` — which is precisely the field this test compares.
- downstream: `expect(after.talent).toEqual(released.talent)` at tests/ruling-a-development-in-play.test.ts:215 fails on `age` and nothing else. Stated plainly rather than dressed up: inside this test the moved age reaches NO priced consumer — no offer is quoted and no market case is decided, so there is no repricing here. The change is the intended one on its own terms: C.1 made `Talent.age` time-varying, and the test's premise that talent is byte-frozen across 10 simulated weeks conflates 'no development' with 'nothing about a person changes'. The ruling the test actually guards is intact.
- evidence: Reproduced on the current tree: `node_modules/.bin/vitest run tests/ruling-a-development-in-play.test.ts --minWorkers=1 --maxWorkers=1` -> 3 failed | 15 passed (18), the same three cases as the run log. NOT inherited: 755-b8-full-core.txt:1157 reads `✓ tests/ruling-a-development-in-play.test.ts (18 tests)`. Normalised every `-`/`+` line of the run-log diff block (767 lines 26320-46798): 9 `"age"` pairs and NOTHING ELSE — no skill, ceiling, devRate, workHistory or workEthic moved, so development genuinely did not re-run and the ruling still holds. The count matches the birthday schedule to the person (9 predicted, 9 observed). Not a production defect: the derivation checks out against contract 762 §1 — the sibling person t-wri-00 in seed 'A-cancel-1' anchors at 37.62277198892757 week 0, and `nextBirthdayWeek(row, 37)` returns 20, which is exactly `ceil((38 - 37.62277…) * 52) = ceil(19.616) = 20`. Not an inconsistent fixture: nothing hand-writes an age; the world comes from `generateWorld(seed)` and its provenance is internally consistent.

### tests/ruling-a-development-in-play.test.ts > RULING A — canceled and unfinished work develop nobody > a canceled production develops nobody (no release, no growth)
- confidence: **measured** · still reproduces: True
- first changed age: Seed 'A-cancel-1', person t-wri-00 (the payload's writer, first in `performerIds`). Provenance row measured: {"personId":"t-wri-00","kind":"authored_exact_week","ageAtEntry":37.62277198892757,"entryWeek":0}, stored age 37 at week 0. `nextBirthdayWeek(row, 37)` = 20 (measured), and `ageAt(row, 23)` = 38 (measured). The test advances 3 + 20 = 23 weeks, so week 20 falls inside the window and the age becomes 38.
- consumer: `materializeAges` at the tick tail (src/core/aging.ts via src/core/tick.ts:1049) writes the new age onto `state.talent[i].age` — the field compared at tests/ruling-a-development-in-play.test.ts:250.
- downstream: `expect(s.talent.find(t => t.id === id)).toEqual(before.talent.find(t => t.id === id))` compares a WEEK-23 person against the week-0 `generateWorld` person, so it now spans a birthday. Honest limit: the moved age reaches no priced consumer inside this test; the only observable effect is the equality failure. The intended change is that `age` now advances with the clock.
- evidence: ONE TRACED CAUSE SHARED WITH THE OTHER TWO CASES in this file: `advanceDev(state, N)` ticks N weeks and `materializeAges` fires every scheduled birthday in that window, while each test asserts talent is byte-identical across it. Reproduced on the current tree (3 failed | 15 passed). NOT inherited — 755-b8-full-core.txt:1157 shows all 18 passing. Normalised the run-log diff block (767 lines 46808-47157): exactly ONE changed line pair, `"age": 37` -> `"age": 38`. Every other field of the Talent object is identical, so the cancelled production developed nobody and the ruling under test is unbroken. The birthday week is the contract formula to the week: 37.62277198892757 + 20/52 = 38.0073… ≥ 38, and 37.62277… + 19/52 = 37.9880… < 38, so week 20 is the unique crossing. Not an inconsistent fixture: the world is `generateWorld('A-cancel-1')`, no hand-written age anywhere.

### tests/ruling-a-development-in-play.test.ts > RULING A — development survives save round-trips without duplication > load → advance-with-no-release → the loaded talent is unchanged (no phantom development)
- confidence: **measured** · still reproduces: True
- first changed age: Seed 'A-save-3'. `released` sits at week 9; the state is exported, re-imported and advanced 5 weeks to week 14. Two people have a scheduled birthday in weeks 10..14 (measured from `generateWorld('A-save-3').talentProvenance.due`): t-act-14 at week 13 (stored 22, anchor 22.766675596633227) and t-act-07 at week 14 (stored 31, anchor 31.74947995978345). The run log shows exactly two age pairs, 22->23 and 31->32.
- consumer: `materializeAges` at the tick tail (src/core/aging.ts via src/core/tick.ts:1049) writes the new age onto `state.talent[i].age`, the field compared at tests/ruling-a-development-in-play.test.ts:428.
- downstream: `expect(advanced.talent).toEqual(reloaded.state.talent)` fails on `age` alone. Worth naming because it is the one case in this group where a repricing boundary is nearby: t-act-07 crosses 31->32 and t-act-14 crosses 22->23, so NEITHER touches the age-30 `isProven` boundary at talentMarket.ts:690 and no priorityOrder branch flips. No skill moved, so no phantom development occurred.
- evidence: ONE TRACED CAUSE SHARED WITH THE OTHER TWO CASES in this file. Reproduced on the current tree (3 failed | 15 passed), failing at line 428 as in the run log. NOT inherited — 755-b8-full-core.txt:1157 shows all 18 passing. Normalised the run-log diff block (767 lines 47167-67638): exactly 2 `"age"` pairs and no other field. Predicted count from the birthday schedule (2 in weeks 10..14) matches the observed count exactly. THE SAVE PATH WAS CHECKED FOR PHASE LOSS AND IS CLEAN, since this is the one case that round-trips through a save: `importSave(exportSave(makeSave(w)))` on this world returns saveVersion 33 with the provenance root key-normalised IDENTICAL to the pre-save root (measured `true`) and all stored ages identical (measured `true`) — the exact `ageAtEntry` floats such as 48.09926078702489 survive serialisation, so no birthday phase is lost and the second birthday is still computed from the original anchor. `migrateToCurrentControl` takes the `save.saveVersion >= 19` arm (`migrateToV33`) and does not re-anchor. Not a production defect and not an inconsistent fixture.

### tests/d17b-save-v7.test.ts > D-17B/E4 — migrateToV7 lifts every known version, and the chain still holds > a V7 save reloads and continues identically to an uninterrupted run
- confidence: **measured** · still reproduces: True
- first changed age: t-wri-03, authored_exact_week {ageAtEntry: 42.8461210480575, entryWeek: 0}: the uninterrupted world derives 43 at week 12, the world reloaded at week 6 derives floor(42.8461 + 6/52) = 42. Also t-wri-08 (33.7929…, 34 vs 33) and t-wri-09 (55.8068…, 56 vs 55, which straddles ageFactor's flat boundary at 56).
- consumer: makeSaveV16's `state.talent[].age` is inside the compared bytes; in the live engine ageFactor (src/core/employment.ts:263), a smooth bell centred 34 with spread 22, so any integer step moves the salary quote, and isProven (src/core/talentMarket.ts:692, `talent.age >= 30`).
- downstream: A campaign resumed from a pre-provenance envelope prices those people one year younger for up to 52 weeks after the load, and priorityOrder branches on isProven, so a person sitting on the 30 boundary can change which studio wins a contested talent-market case.
- evidence: Reproduced (1 failed, 21 passed). Full recursive field diff of the compared V16 projections: 5 differences, all `state.talent[].age`, nothing else — verified, not assumed. Boundary M=6, compared at week 12. Two causes stack, and I measured them apart. (1) the same integerized-legacy-save artefact as the prod-ops M=0 case; (2) a residue that NO fixture fix removes — injecting the genuine fractional ages leaves the count unchanged at 5. That residue is C.1's specified legacy law: convertV32ToV33 (src/core/save.ts:9155) and liftHistoricalState both write legacy_age_anchor {ageAtMigration, migrationWeek: market.tick}, re-basing the birthday clock at the load week because a pre-provenance envelope carries no birth week. Contract 762 §6 chooses this explicitly ("recorded at market.tick — never hollywood.originWeek"), and the alternative (anchoring at week 0) would age an entire legacy population at load. CONFINEMENT MEASURED: the identically-shaped LIVE-version twin passes — d17b-publicity's "a reload mid-run continues identically to an uninterrupted run (campaign included)" round-trips through makeSave/importSave/migrateToCurrentControl at V33 and is byte-identical, because authored_exact_week rows survive a V33 save. Only a reload through an envelope older than V33 re-bases. Since the test's claim cannot be restored by any internally-consistent fixture, the expectation is what has to move.

### tests/production-operations-save-v8.test.ts > Production Operations V1 — managed state validation and continuation > round-trips a blocked shooting task with its real director and reservations
- confidence: **measured** · still reproduces: True
- first changed age: state.talent[22].age, 29 in the resumed world against 30 in the uninterrupted one at week 9 — it sits exactly on the isProven threshold. Also talent[35] 35 vs 36 and talent[39] 37 vs 38, all three surviving the fixture fix.
- consumer: makeSaveV16's `state.talent[].age` is inside the compared bytes; ageFactor (src/core/employment.ts:263) and isProven (src/core/talentMarket.ts:692, `talent.age >= 30`) read it in the live engine.
- downstream: The resumed campaign prices those three people a year younger for up to 52 weeks, and the 29-vs-30 person crosses isProven exactly, so priorityOrder can seat a different studio in a contested market case.
- evidence: Reproduced. Full recursive field diff of the compared V16 projections: 5 differences, all `state.talent[].age`, nothing else — verified, not assumed. Boundary M=4, compared at week 9. Same two stacked causes as the d17b-save-v7 twin, measured apart on this case's own world: restoring the genuine fractional ages to the forged V8 file removes 2 of the 5 diffs and leaves 3, so the fixture artefact is real but is NOT the whole cause. The residue is the legacy_age_anchor re-basing at market.tick written by convertV32ToV33 / liftHistoricalState, which contract 762 §6 specifies. Because no internally-consistent fixture restores byte identity here, this case's expectation must move rather than its setup. Confinement evidence as above: the live-V33 reload twin in d17b-publicity passes, so an ordinary player save/load preserves phase exactly; only a reload through a pre-provenance envelope re-bases.

### tests/p14b2-fixture-preconditions.test.ts > P14B.2 lawful engine fixture preconditions (not bridge verification) > wins a real rival-owned case and reaches reminder/outcome weeks after proposals are gone (fails inside tests/helpers/p14b2-fixtures.ts:164, poachingFixture)
- confidence: **measured** · still reproduces: True
- first changed age: person-studio-5a47d054-r04-3 (actor of rival studio-5a47d054-r04, world p13aGeneratedStudio('p13-public-commercial-adoption')). Measured provenance row: {kind:'authored_exact_week', ageAtEntry:29.84081390477671, entryWeek:0}. Pre-C.1 the stored age was that gaussian and never moved, so it was 29.84 at every week. Post-C.1 the stored age is 29 at week 0, crosses 30 at week 9 (nextBirthdayWeek(row,29)=9; ageAt@8=29, ageAt@9=30) and is 33 at the fixture's week 196 (ageAt@196=33, confirmed by running the world forward to 196 and reading state.talent).
- consumer: isProven (src/core/talentMarket.ts:690) — its other input is unchanged: careerIdentity(person).identityDisciplines measured [] at week 196, so isProven is decided by age>=30 alone. isProven feeds preferredTerm (:710) and priorityOrder (:697).
- downstream: publicPreferredTerm(state, talentId) goes 52 -> 208 (TUNING.CONTRACT_TERM_OPTIONS first -> last), measured 208 at week 196; publicPriorityOrder[0] goes 'opportunity' -> 'compensation' (the proven archetype order). That is precisely the assertion that failed (expected 52, received 208). The fixture's own comment at p14b2-fixtures.ts:150-157 states its premise is 'a publicly unproven person', so the premise, not the engine, is what aging invalidated.
- evidence: Measured by probe against the current tree: fresh world gives the row above with storedAge 29 at week 0; advanceTo(196) gives storedAge 33, disciplines [], publicPreferredTerm 208, publicPriorityOrder[0] 'compensation'. Baseline 755 line 2743 shows this file green, so the pre-C.1 value really was 52 (i.e. unproven, which with an empty discipline list can only mean age<30). The whole chain 29.84 -> 33 is exactly floor(29.84+196/52) — the C.1 formula applied correctly, no defect in the derivation. SAME TRACED CAUSE as the failure of tests/bridge-p14b5-relationships.test.ts family 12 (it calls the same poachingFixture and dies on the same helper line 164), and it is consistent with the p14b1-trust-chooser test-6 failure in another group (same isProven flip, priority-order half).

