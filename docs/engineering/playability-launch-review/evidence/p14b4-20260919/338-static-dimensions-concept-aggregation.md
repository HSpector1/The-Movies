# 338 — Exact concept-map aggregation and lazy static dimensions

2026-09-20. Native sim-core bounded design recommendation and independent
contract-auditor qualified KEEP; parent adopts both directions. No source edits,
runtime, budget-fit proof or339 write release in this document.
Reviewed restored321 replay1f5ea6e7a7def99e43fef0649d74744d4e49604bc4dba5f7857fbdd9cf820242.

335 actual market now returns at197631 but later refusal preparation cuts.
336 director/scheduling now execute; frame5 dimensions193602→197994 then
restricted/order calculator cuts199992/16 BEFORE actual take. These are measured
remaining failures, not proof the next change fits.337 preserves full evidence.

## 1. Concept Map: identical owner reservation, fewer calculator operations

For original concept row i (zero-based), length Li and preceding character sum Si:

K(i,Si,Li) = 1 + Li + i*(1+Li) + Si
sum(10+K) = 10*N + N*(N+1)/2 + N*sum(Li)

Each Li occurs once directly, i times as query length and N-1-i times in later
prefixes, hence N times total. N counts ALL concept rows, not distinct IDs.
Retain all actual ID/title discovery, title maximum, character/count discovery,
tuple/Map owner construction prices and later writer lookup/label costs.
Only repeated calculator expression trees disappear.

Prepay every new arithmetic operation. Halve the EVEN factor of N*(N+1) before
the saturating product. Zero remains zero; never saturate then divide.
Nonnegative saturated algebra preserves the prior capped owner reserve.
No market/owner/policy/cap changes or free repeated comparisons.

## 2. A: lazy invocation-local static facts, never a cached combined Dimensions

Static-only record contains ten numeric fields:
f, capacity, d, adoptions, access, equipment, placements, structures, provides, cells.

Discover unchanged facilities/capacity/string maximum, player studio ID,
technology adoption/access/equipment strings/counts, source property structures/
provided IDs and placement/installation/cell facts using their complete paid walks.
Current permitted owners preserve those roots: operations replacement preserves
facilities; technology collectors change productions only; source property/
placement/player identity remain unchanged throughout a synchronous invocation.

On EVERY call construct a fresh full Dimensions record, combining static-only
maximum with newly measured dynamic values. Keep fresh:

- Full original/Ready production ID/director spans and ALL opaque own-string-key
  production-copy footprints; do not traverse opaque values.
- Workflows, reservations, tasks, bindings, setup and Sets.
- Operations/technology ROOT copy footprints; technology production rows/copies.
- Current n, external occupancy, t and allSilent.
- Genre lookup row count/ID maximum from CURRENT prepared pictures, including
  Ready additions and retained prepared pictures, not only active production IDs.

The technology method-versus-silent comparison must pay its own repeated string
span (Work.equal), independently of discovery and subsequent owner comparisons.
Actual owner traversal/callback/copy reservations and calls remain unchanged.

## Minimal local architecture

Sim recommendation adopted as an implementation detail within320A:
one private paid {value: StaticDimensions|null} cell on Prepared, created only
with prepare's existing final paid result construction. Extend/pay the Prepared
literal field and cell literal/initial value. Ready's explicit branchPrepared
clone shares the SAME cell; it never snapshots sibling dynamic dimensions.

Pass the cell to the three existing dimensions callers (command/frame/admission).
No new Work administrative-envelope state, module cache or source-identity cache.
First static discovery still occurs at the FIRST EXISTING dimensions call, after
existing refusal and zero-work paths. Publish the reusable readonly numeric
record ONLY after complete paid construction; partial discovery is never reused.
Pay the first cell assignment and EVERY later lookup/read/composition/copy.
One cumulative allowance across branches: no refund/reset or dynamic sibling leak.

No new index rebuilding or broader extraction. Static cardinalities are reused
only because these actual owner paths preserve them; dynamic roots remain fresh.
Independent actual-source review must check literal keys, scalar blocks, all
three callers, Ready sibling cell sharing and lazy publication order.

## Scope and verification

Independent architecture addendum: qualified KEEP, no concrete concern. The
Prepared cell stays invocation-local, shares only static facts, and publishes
only the fully paid record. New Prepared field/cell/record literals/assignment/
accesses must all be paid; no extra architecture gate is needed. No runtime.

After337 publication/exactremote340,339 may be sole writer ONLY replay module
and its handback. Preserve Hollywood, all actual owners, original26/Ready15/
employment5/ordinary66 and all validators/limits/timeouts. Parent full source read,
immutable pins, bounded source review and serialized unchanged checks remain.
No numerical usefulness, complete owner proof or B4 acceptance follows here.
