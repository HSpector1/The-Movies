# 108 — independent D3 preference source review

Disposition: **KEEP the bounded source candidate.** No concrete source defect
found against the adopted B4 D3 match table. This is not full D3/P2 acceptance:
two matrix cases still stop at the unimplemented tagged-P2 attachment premise,
one superseded old chooser expectation remains failing, and the bridge's future
predicate type obligation remains open.

## Exact review scope

Read97 completely, the actual `src/core/talentMarket.ts` diff against HEAD,
the relevant preference/scoring/freeze/binding code, B4's match table, all99/101
raw output, all102/103 raw output and all four terminal metadata records.
Read the unchanged test control flow needed to identify reached assertions.
No tests, typechecks, probes, engine imports, Git writes, network or delegation
were performed; only this authorized report was written.

- Base in all four records: `c04f4af500c37c726fc7b292db7fac2050ee04d9`.
- Independently hashed source: `dd96a993a61ef50395c075a95505d5b7aa3c5c90d5ae4c66925bad4a28062c51`.
- Independently hashed all four protected patches: `0e3591aa097fc90b3e21f7dc077ea40d06dc45b5d6d23dab938d504afe71d381`.
- Production delta is only `talentMarket.ts`: one type import, the small matching
  helper and the D3 calculation. Other visible tracked changes are parent-owned
  documentation; this review does not claim the entire checkout is clean.

## Source requirements

MET WITH SOURCE EVIDENCE:

- `talentMarket.ts:728–741` implements the adopted table: P1 matches only
  `anyCastAppearance`; explicitly tagged P2 lead/flexible class matches either
  of the two public preferences. Untagged count-only P2 and other families do
  not acquire a significant class. The helper uses family/predicate shape, not
  historical root/evaluator version as a substitute for terms.
- P1 reads the existing pure `publicPreferredOpportunity`, which shares the
  existing `isProven` owner with public priorities and term preference. No new
  age/credit threshold, hidden personality, role restriction or RNG is added.
  P2 need not branch on the accessor because both possible public preferences
  accept both explicit P2 classes under the adopted table.
- `talentMarket.ts:797–802` joins the actual attached promise and requires BOTH
  class match and current REASONABLY_ACHIEVABLE feasibility. The feasibility
  read occurs before the Boolean expression, so a mismatch does not skip it.
  All other descriptor scores, Copeland/dominance logic and tie order remain
  unchanged.
- Preference matching is referenced only by this scoring seam. The unchanged
  freeze refusal at1083–1093 still checks material integrity, actual feasibility
  and affordability, not taste. The unchanged1121–1125 freeze map preserves its
  pre-commit receipt and survivor decisions. The unchanged1100–1111 binding
  owner requires actual employment and the frozen achievable receipt, not a
  preference match. Thus a mismatch is not a new legal/drop/binding condition.
- No material/digest formula, fee, attachment writer, validation, saved-history
  migration, root/evaluator version, projection, rival authorship or capacity
  implementation is changed. A type-only import of the additive V30 shape does
  not activate a new live save/wire version. Previously settled choices are not
  rewritten by this read-time scoring change.

## Actually recorded verification and exact limits

All four completed metadata records report fixedSource:true, the same base and
protected patch at both endpoints, and empty untracked protected-source arrays.

| Record | Observed result | Limit |
| --- | --- | --- |
| 99, 00:16:47.204–00:17:00.550Z | Exit1; 1 passed, 2 failed, 4 unselected | Both age29/30 cases now stop at `attachCandidate:284`: expected tagged lead P2, received `{count:1}`. |
| 101, 00:17:47.982–00:18:08.808Z | Exit1; 14 passed, 1 failed, 3 existing TODO | Only the designated old unproven-P1 opportunity-winner pin fails at `p14b1-trust-chooser.test.ts:439`. |
| 102, 00:18:20.342–00:19:33.606Z | Exit0, root then UI typechecks | The chained command completed; not a native UI check. |
| 103, 00:20:22.666–00:20:49.677Z | Exit2 | Sole TS2353 at `bridge-p14b4-cast-class.test.ts:364`: future predicate `kind` is not in live `{count:number}`. |

99's loop orders P1, lead, flexible. Reaching the lead attachment proves that
the preceding P1 iteration's settlement, unchanged drop set, attachment identity,
achievable receipt, public preference and winner/reason assertions completed for
both ages. Age30's matching P1 binding assertion also completed. This does NOT
mean either whole matrix case passed: lead/flexible scoring/binding was not
reached, nor was age29's later separate player-P1 mismatch-wins-and-binds branch
at325–332. The latter remains source-supported but not established by99.

101's old fixture supplies P1 to a publicly unproven person and previously
required opportunity to change the winner. It now reaches the unchanged
two-survivor/drop/attachment and baseline compensation assertions before439;
player remains winner. This is the intended policy change, not a historical
baseline failure to relabel as inherited. Preserve its failure evidence and
independently re-express the positive opportunity witness with real tagged P2
at activation; do not weaken it or restore the superseded preference rule.

The inspected outputs contain no failed-suite or unhandled-error diagnostic.
No full suite, completed six-way P2 matrix, rival policy, real capacity adapter,
Save30/projection47 activation, native verification or Owner acceptance is
established by this candidate. Continue those independent obligations with the
remaining exact test/type failures retained.

## Separately frozen106 test-contract supplement

Read all83 lines of106 and its complete brief after handback. KEEP as independent
POST-patch pure-reader coverage: the six literal age/preference/P1/tagged-P2 pairs
and count-only P2/other-family negatives follow the adopted table, not the source
implementation as oracle. The generated uncredited subject is guarded; age29/30
are disclosed pure-read inputs, with no invented credit or claimed birthday.
Whole-state/predicate freeze and before/after equality retain history and RNG.
These cases do not replace actual attached-feasibility, freeze or binding tests.

Independently hashed draft `39e247685562a91422a9afd2a41d6b0a9bc45740c401f74663511fc8554f5adb`,
brief `195af0cb3835228c09a0411a2a6b60eab49d5c5ea45046f4fa4bab696ee517ff`,
installed test `337c0b087665a714053533ea1b1873b862d315f6b8ba977b03a5d31219e4bdfd`.
A read-only byte comparison establishes that installation adds only the first
provenance-comment line to the frozen draft.

After the parent's handback, read the complete109 raw output and terminal
metadata: all8 intended cases PASS, exit0, 00:24:02.960–00:24:07.116Z, fixedSource:true.
The actual fixture guards and table assertions therefore completed in that run.
The base remains c04f4af; the combined protected patch, independently hashed,
is `30308068752a42e41e3f452095eaf62ad66aac2e53ff6df1bcafa3a78227627c`.
Its three stable untracked tests are the newly installed95/100/106 files, not
unexplained drift. Independently rehashed `talentMarket.ts` remains dd96a993….
This is actual POST-patch pure-reader GREEN, not a retrospective API RED claim
or evidence that the still-blocked attachment/settlement matrix passed.
