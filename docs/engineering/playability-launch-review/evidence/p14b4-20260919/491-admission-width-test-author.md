# 491 — long unknown-director public refusal control

Independent native test-author handback; **UNEXECUTED** retained-behavior control. Parent owns `491-admission-width-refusal-baseline` on unchanged production before any candidate writer. No runtime, Git, network, delegation or production edit/temporary-patch inspection was performed.

## Authority and exact preservation

- Published source supplied by parent: `1baf982520248d15731950d5bda48a6e93bba5ea`; restored replay466 SHA-256 supplied as `956a0245872f02e625c93a41cc2cc2033c4279129b85e100f315045d4d4f8ca4`.
- Fully read `486-admission-width-direction.md` during the immediately preceding assessment; 490 was not present when checking the assigned evidence directory at write-task entry. This test follows the parent's explicit one-control release, not an inferred production-change authority.
- Sole executable edit: one additive case in `tests/p14b4-ready-owner-replay.test.ts`, between marked `491 BEGIN` / `491 END` lines. No original byte, assertion, helper, import, fixture, cap or timeout changed.
- Baseline independently matched before editing: 745 lines, SHA-256 `af5d87eeb80da9da0dc7ee5d20c195f89ce1162e82238a40180879afd93db15b`.
- Frozen candidate: 773 lines (+28), SHA-256 `79de5329befd79263496f5c3b895604755471aec1175ecba09d819f8696f5fa2`.
- Removing only the marked insertion in a read-only stream reconstructs the exact baseline SHA-256.

## Construction and independent expectations

Reuse genuine `baseReady()` without altering any campaign fact. The invalid choice alone names `491-missing-director-` followed by 48 `x` characters. Guards require its length to exceed 22 and confirm no actual source person has that identity. Existing `supported` performs the strict save and full root/foreign-work checks on the unchanged source.

The real `applyActions` call must throw the exact literal diagnostic `greenlight directorId references unknown talent id "<supplied ID>"`; Node's strict object-form `assert.throws` compares its complete message, not a substring. Public replay must reach header and staffing owners, return `commandRefused` with exactly the same complete diagnostic, and perform no workflow allocation, link, sweep or Ready admission. Existing call-through observation retains the actual owners, input-purity checks and unchanged shared 200000 cap. The new case additionally checks full source purity before and after replay.

## Limits and handback

No assertion is made that the unknown ID is a genuine person, selected freelancer or stored employment key. The source is genuine; the command is deliberately invalid. No second hypothetical long-person witness or new campaign is added.

This public control can detect diagnostic truncation/substitution, wrong refusal, premature admission/mutation, or a work cut replacing the required reached refusal. It cannot by itself detect under-reservation while unchanged owners still produce the right diagnostic, nor prove the three diagnostic floors, complete key discovery or payment sufficiency. Those remain independent source-review obligations. Existing first-take/stale acceptance REDs remain unchanged; no new RED or green result is claimed.

Test and brief are frozen; test-write ownership is yielded. Parent next: verify add-only reconstruction, review and execute the fresh unchanged-source baseline before the sole production candidate.

## Corrected author expectation after actual baseline

Parent's `491-admission-width-refusal-baseline` completed with exit 1, fixed source true, from `2026-09-20T12:18:28.399Z` to `2026-09-20T12:18:35.144Z`. The actual public owner includes the leading `applyActions: ` prefix. The original exact expectation omitted it; this is an author-expectation mismatch, **not a production RED**. The original candidate hash and mistaken expectation above remain historical evidence; the parent retained the complete raw baseline.

The only executable correction is that added message literal, now `applyActions: greenlight directorId references unknown talent id "<supplied ID>"`. Exact object-form `assert.throws`, exact replay-detail equality and every other assertion remain unchanged. No runtime or production change was performed by this author. The parent owns the fresh `491-admission-width-refusal-corrected-baseline`; no corrected execution result is yet claimed.

Corrected frozen test SHA-256: `c012c1c65ec55df13e941df874ac14ed60e1b7d5510364d3094d13acde284733` (still 773 lines). Removing only the marked 491 addition again reconstructs original SHA-256 `af5d87eeb80da9da0dc7ee5d20c195f89ce1162e82238a40180879afd93db15b`. Corrected test and appended brief are frozen; ownership is yielded.
