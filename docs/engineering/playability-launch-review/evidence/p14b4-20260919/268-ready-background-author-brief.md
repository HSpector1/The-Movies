# 268 — exact unknown identity and real independent audition

2026-09-20. FROZEN INERT / UNEXECUTED. Two tests, 247 lines.

Draft: `268-ready-background.test-draft.ts`

SHA256: `ac14a1899e3172cf8d733fb9656784936a6192fe662d033e2bb0940ce6ed20b3`.

Intended installation: `tests/p14b4-ready-replay-background.test.ts`.
Authority:240/176, full255 bounded REFINE, unchanged public admission/casting
owners and frozen199/244/250 fixture law. No evolving replay implementation,
267 source or runtime-derived expected values were inspected.

## Independent cases

1. A correctly typed choice names `script-9999`, explicitly absent from the real
   generated project root. The actual public greenlight action establishes the
   owner diagnostic; the test independently pins its full literal sentence with
   the supplied ID in quotes. Replay must return commandRefused with EXACT detail,
   no admitted/sweep provenance, zero header/staffing/allocator/link/sweep calls,
   and unchanged input. This is a direct behavioral regression of255 finding2,
   not the different non-Ready/header diagnostic.
2. Genuine Ready A coexists with an independent active audition for Ready B.
   Real commission→tick→Review→accept creates B; a real startCastingSession uses
   A's currently free three cast people. Existing casting law engages none of
   those candidates and charges no cash/RNG/ledger mutation. Actual greenlight A
   succeeds in the other Development slot. Actual next tick completes B's
   audition and new-start-skips A. Replay must reproduce the exact local horizon
   projection, immediate admission linker snapshot, one whole-slate sweep and
   completed background path. Its original casting hold is exactly ONE resource
   with NO person holds; the suffix closes at actual background completion before
   sweep. Only A's new five company holds plus its slot enter additionalHolds.
   No fabricated audition Review/results root, duplicate company, instant second
   screenplay opportunity, forged Ready state or discarded background is allowed.

Both cases use transparent original owner calls and whole-input comparison with
finally restoration. Strict makeSave is retained for genuine source and action/
tick oracles. The ordinary cap remains200000; no timeout/config/budget change.

## Accounting evidence limit — deliberately not a private tariff test

Finding1 is an unpaid OUTER traversal even when each background has zero people.
The second test ensures that exact lawful resource-only situation is actually
reachable and semantically handled; it does NOT independently prove payment for
each internal visit. Public cumulative preparationWork combines source scans,
identity, allocation, background completion and trace construction. Comparing
unrelated scenarios or prescribing a private one-visit constant would not isolate
this charge. No such false cost oracle or fabricated instrumentation is added.

The outer-visit prepayment must therefore remain an explicit bounded source-
accounting review obligation. Passing this semantic/cap case alone does not close
255 finding1 or certify a whole replay work bound. No huge synthetic background
array or supposedly valid hand-stamped casting state is introduced.

## Provenance and handoff

Helper baseReady/supported facts are copied unchanged from frozen250/244: named
historical-control founding, actual hires and commissions, fresh industry at real
week0, strict saves and expanded foreign-person relevance guards. This is NOT
ordinary natural founding. The additional B audition/Ready-admission conjunction
is UNEXECUTED; all availability, root and cap guards remain hard requirements.
Old199 independently exercised the corresponding already-started audition route,
not this new admission entry; that evidence is not silently transferred here.

Original26 and Ready11 controls, live tests, source, fixtures and configs remain
unchanged. No runtime/probe/typecheck/installation/Git/network/delegation performed;
no RED or GREEN claim. Parent owns frozen-byte review and serialized execution.
