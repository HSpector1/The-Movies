# 407 — Certified one-picture wrap slot bound

2026-09-20. ADOPTED after native sim-core source proposal, contract-auditor
qualified design KEEP and parent full read. Actual source/payment review and
runtime remain required. No fit/Ready/B4 acceptance.

396 remains frozen for408/409 publication.406 measured same wrap prepayment
used184803/request17048,1851 short just to pay, before later work. Candidate
paper4599 reserve saving is not a full-route certificate.

## Exact bounded implementation

Only replay allocationBill/sweepBill plus410 handback. Keep existing wrapOnly
proof (each production older this week and remaining4), paid Post/heldPost scans,
and successful allSilent && external===0 && post-heldPost>=n arm.

Inside that successful arm additionally certify:

- n===1, exactly one production and exactly one workflow;
- sole workflow production ID exactly equals the production ID;
- phase shooting; nonnull shootingTask with status completed;
- exactly two reservations, forming soundstage/set-scenery in either order.

Use paid Work.equal for each reached ID/phase/status/capability comparison, at
most seven. Conditions select a bound, never refuse otherwise lawful execution.
On any failed guard retain existing general capacity multiplier/formulas.

Initialize private boolean false. Defer ONLY the enter calculator construction
until after the existing wrap proof/selector, then pass that boolean privately
to allocationBill. For wrapOnly AND certified one-slot, replace its slot-loop
multiplier d.capacity with1. No changed coefficient, per-slot body or other term.
Other allocationBill callers retain false/default general behavior.

Keep attempts1 from existing successful wrap proof, common3visits (not narrowed
to2), all existing fallback retry formulas, all literal/copy/owner prices, limits,
tests, sorting, policy/setup/binding and other calculators. No actual owner change.
No whole wholesale alternate wrap tariff or output-dependent cheaper branch.

## Owner proof (auditor qualified KEEP)

Shooting→Post requirements retain neither old soundstage nor set-scenery.
releaseCompletedPhase clears both reservations, task and stage binding before
allocation. Historical Set identity may remain, but raw occupancy emits a Set
claim only with a live stage reservation. Exactly one workflow and no external
slots therefore imply empty occupied slots at allocation.

Silent policy retains all supplied facilities. The allocator still copies,
filters, calls policy and sorts the complete facility array. It skips non-Post
and zero-capacity facilities. Positive currently unheld Post capacity means that
the first positive Post facility exists; its slot0 is free and satisfies Post's
one capability. Thus exactly one SLOT BODY executes. No resource consumption
or event is predicted from an observed owner output.

Retain full raw production/bound-Set occupancy walks and index setup even though
they emit no claims; facility scans/filter/sort/policy callbacks; requirements/
retention guards; original entire per-slot key/lookup/literal/insertion/result
charge; release/update/Set wear/events/copies; outer advance ordering/maps/
settled visits; later drain/reconcile/provenance/output/kernel.

Blocked Post, multiple workflows, sound policy, external occupancy, already
released reservations or any malformed guard does not qualify. The existing
general bound and retry behavior remain essential.

## Proposed complete added guard prepayment

| Finite block | Units, before evaluation |
| --- | ---: |
| boolean initialization |2|
| single-picture/workflow-count guards |16|
| production/workflow references |12|
| task/reservation-list references |12|
| nonnull task/two-reservation guards |16|
| two reservation references |8|
| predicate arguments, dispatch/short circuit and assignment |64|
| new allocation selector/argument controls |8|

Every reached string comparison pays its actual spans separately via Work.equal.
No added variable walk/literal/owner call. Exact implemented spelling must be
inventoried410 and reviewed411; these are not coupons for uncounted new work.
Existing calculator calls/payments remain except source-order movement of enter;
that movement cannot expose an unpaid owner call or successful proof on a cut.

At406 dimensions: unchanged per-slot657, capacity8→1 removes4599 owner units:
16951→12352 if all other terms unchanged. New selector/caller costs are additional.
Do not use this arithmetic as observed fit or completion evidence.

## Tests, ownership and next

Existing independent genuine one/two-picture H6 wrap tests cover positive and
multi-picture fallback, exact releases/grants/wear/projection. Other existing
controls cover new8, retained8→7, scheduled5→4 and committed1→0. No genuine replay
blocked-Post fixture identified; capacity-rewritten C2a-M4/F1 is not such evidence.
Optional genuine Post3→2→1 retained-then-released control remains unexecuted;
not required as private price oracle and no new fixture campaign authorized.

After409 exact publication,410 SOLEsim writer source+handback ONLY, then freeze,
parent full diff/pins,411 independent actual review,412–419 serialized original
gate matrix (no test edits). Preserve remaining failures and instrument only after
all runtimes close. Continue engineering beyond this checkpoint under logic-first.
Unity/native/Owner acceptance deferred; integration backlog retained.

