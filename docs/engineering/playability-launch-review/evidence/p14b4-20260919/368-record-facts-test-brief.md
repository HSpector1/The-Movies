# 368 — Independent record-facts public regression handback

2026-09-20. Native test-author; adopted365; immutable reference source
`2ccd87cca17388905a85a27dddd3ff5ab9c8bc66`. Authored only the assigned new test and
this brief. No implementation inspection, runtime/probe/typecheck, Git mutation,
source/config/fixture change, test weakening, delegate or network operation.

## Frozen test

`tests/p14b4-replay-record-facts.test.ts` — 221 lines, two default-timeout cases.
SHA256 `70d710859eb9f83baf5776101f4c30b1f1c4713db9cca50ea14c9a660a307dc9`.

1. A/B/A/B invokes the exact same original public input objects twice each.
   A and B are separately generated historical-control founding worlds, using
   real package offsets0 and1, actual greenlight/director/schedule actions and
   ticks. Hard guards require the same real production ID, different concepts,
   disjoint five-person companies, and separate operations/technology roots.
   Each result is compared in full against its first detached snapshot; work is
   also explicitly equal. Earlier returned objects and original worlds remain
   unchanged. There is no claim of different static facility counts or natural
   ordinary founding.
2. Legitimate generic full-P extensions preserve their complete static type and
   opaque marker reference. A one-character versus200001-character unknown
   top-level value must produce equal work, not become a consumed ID/string
   dimension. Renaming one opaque marker key to a longer own-string key, with
   equal property count and unchanged values, must increase work. Both shapes
   retain exact real owner projection. Extensions exist only in the typed lower
   owner view, never in GameState, saved campaign/history or fixture artifacts.

All seven replay calls retain200000/220/1024 and execute two actual-frame
projections: scheduled5→4 first take, then4→3 wrap/Post. Reference results come
from two real ticks, with strict saves of source and both resulting states.
Assertions compare the entire public projection (including original-root foreign
technology, exact real own technology rows and unchanged other technology
fields), actual first-take boundary, both sweep weeks and exact event sequence.
Source/argument purity, all supported-root/foreign relevant-person joins, scenery
readiness and complete non-cut results are mandatory. No owner stub/spy, private
cache helper, tariff constant or cost-threshold discovery is used.

## Evidence limits and next action

UNEXECUTED: neither candidate fixture branch nor these assertions/type pins have
run in this assignment. Offset0 follows the established178/182 route; offset1
as a separate one-picture branch is a new guarded combination. Preserve any
actual constructibility, budget, type or behavioral failure without lowering
assertions, cap or timeout.

This detects observable cross-invocation warming and same-ID result leakage;
it cannot prove absence of an observationally inert global table, all memo-mode
transitions, completed-entry publication/prepayment, or exact cold scan costs.
Those remain bounded source-review obligations. Existing sibling permutation,
zero/tiny pre-scan gates and broader actual dynamic owner tests remain unchanged.
No numerical-fit, complete-domain feasibility, B4 or native/Owner acceptance is
claimed. Parent owns serialized execution after both writers freeze. Tests are
frozen at the hash above; test-author returns idle.
