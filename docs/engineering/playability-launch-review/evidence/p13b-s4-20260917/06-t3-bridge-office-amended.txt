P13B-S4-T3 — amended run of tests/bridge-p13b-s4-office.test.ts
(coordinator adjudication of findings A/B, plus new finding C requirement and
a new finding D found-and-fixed same session, applied 2026-09-17)

Independent test engineer, 2026-09-17. Repo /Users/zacheryspector/The-Movies-headless-program,
worktree branch wip/headless-program-20260916-ts.

READINESS POLL (as instructed): waited for
  grep -c "installationAction" bridge/schema/intent-schema.ts  -> was 0, became 1
  grep -c "rejections" bridge/schema/industry-schema.ts        -> was 0, became 3
Both landed quickly: poll started ~03:07 local, READY at Thu Sep 17 03:09:36 CEST 2026
(well inside the ~15 minute budget; polled via a single backgrounded Bash loop,
10s interval, 900s cap — no Monitor tool was available in this session's toolset).

EXACT LINES CHANGED in tests/bridge-p13b-s4-office.test.ts (this task's one owned
file; no other file touched; not committed by this role — see PROVENANCE below):
  - Header comment: findings A and B marked ADJUDICATED with the coordinator's
    corrected rule text; added (C) as a new asserted requirement, not a finding;
    added (D) as a new finding, found and fixed within this same session.
  - `WireConversionRow` type: added `rejections: string[]`.
  - `WirePlanRow` type: added `reason: string | null`.
  - Case 1: added a second `it` asserting `intent.kind === 'installationAction'`
    on both immediate office-convert-* rows and `'physicalPlanAction'` on both
    plan-queue companions (finding C).
  - Case 2: mid-construction section — iii row's expected `refusal` changed from
    `'targetEngaged'` to `'alreadyInstalled'` (finding A, adjudicated), with a new
    `rejections` array-contains check for both `'alreadyInstalled'` and
    `'targetEngaged'`; ii row keeps `'targetEngaged'`, now also checked via
    `rejections`. "done" (post-completion) section: the `'standardAlreadyMet'`
    assertions on both rows were left UNCHANGED throughout (never weakened);
    they briefly went RED against an intermediate implementation state
    (finding D, below) and are GREEN again now that sim-core fixed it.
  - Case 3: "after" section — the ii row's `'standardAlreadyMet'` assertion, same
    story as case 2 (finding D).
  - Case 4: iii conversion row's expected `refusal` changed from `'targetEngaged'`
    to `'alreadyInstalled'` with a `rejections` array-contains check (finding A,
    recurring); the "both companions hidden" assertions were UNCHANGED (they were
    already correct under the coordinator's refined rule — finding B, adjudicated
    as "case 4 stands as written").
  - NEW `describe('case 4b: ...')`: the coordinator-named counter-scenario — a ii
    conversion running immediately leaves the iii plan companion offered and hides
    the ii companion; queueing iii through it and ticking once yields a `held`
    plan whose `reason` matches `/targetEngaged/` (finding B, the named test).

Command:
  npx vitest run tests/bridge-p13b-s4-office.test.ts --minWorkers=1 --maxWorkers=1 --testTimeout=200000 --reporter=verbose

RESULT (final, stable): 11 passed, 0 failed (11 tests total). Reproduced
identically across three consecutive clean runs (03:15:51, 03:16:25, 03:17:31
local) after sim-core's finding-D fix landed; bridge/office.ts settled at
01:13:53 UTC (checked immediately before each of those three runs, unchanged).

PER-CASE RESULTS (final):
  projection bump                                                    PASS
  case 1 (quote parity + laboratory-view null)                       PASS
  case 1 (intent kind, finding C)                                    PASS
  case 2 (mid-construction refusal/rejections, finding A)            PASS
  case 2 (post-completion refusal, finding D)                        PASS (see history below)
  case 3 (baseline + II->III quote)                                  PASS
  case 3 (post-completion ii refusal, finding D)                     PASS (see history below)
  case 4 (plan start, both companions hidden — finding B)            PASS (adjudicated: stands as written)
  case 4 (immediate rows lockout + refusal, finding A recurrence)    PASS
  case 4b (lower-running / higher-offered counter-scenario)          PASS (new, per adjudication)
  case 5 (insufficientFunds)                                         PASS
  case 6 (stale revision)                                            PASS
  case 7 (player-safe)                                                PASS
  case 8 (history)                                                   PASS

FINDING D — FULL HISTORY (found and fixed within this session, never silently
absorbed at any point): the FIRST amended run (03:11:29 / 03:13:42 local, 9
passed / 2 failed, see the two prior runs embedded further down and reported in
the prior handback) showed that implementing the coordinator's finding-A
amendment had an unannounced side effect: once a conversion blueprint reached
OPERATIONAL status (complete, not running), re-querying THAT SAME blueprint
against the SAME facility ALSO read `refusal: 'alreadyInstalled'` instead of
the ORIGINAL, never-amended T3 contract's `'standardAlreadyMet'` (the
coordinator's very first task message: "advance 16 weeks through the bridge ->
... ii row refusal 'standardAlreadyMet', iii row refusal 'standardAlreadyMet'").
Root cause: `alreadyInstalled`'s own check in `queryFacilityInstallation`
(src/core/placement.ts) carries no status filter, and the amended `refusalOf`
(bridge/office.ts) had been changed to unconditional `rejections[0]`, with no
exception left for a completed body. The coordinator's adjudication of finding
A explicitly scoped itself to a row "while a conversion runs"; a COMPLETED body
was never mentioned, so this was treated as a new, unadjudicated regression and
reported rather than matched. The original `'standardAlreadyMet'` assertions
were kept exactly as first written throughout — never touched, never weakened.
Between the first amended run and the run captured below, sim-core (bridge/
office.ts mtime advanced from 01:09:36 UTC to 01:13:53 UTC) added ONE targeted
exception to `refusalOf`: a COMPLETED record's own row yields to
`standardAlreadyMet`; an UNDER-CONSTRUCTION one still reads `alreadyInstalled`
first, preserving finding A's adjudicated behavior. Both original assertions
pass again, unmodified.

PROVENANCE NOTE: partway through this task, the coordinator (or another agent
with commit authority — not this role) committed this test file's FIRST-WRITTEN
state (7/9 passing, before this amendment) as commit e642212 on this branch.
This role never ran `git add`/`git commit`/`git push` at any point, per the
explicit "do NOT commit or push" instruction. The current working-tree file
(11/11 passing, this amendment) is UNCOMMITTED and differs from e642212 by
148 insertions / 43 deletions (`git diff --stat`); it is left for the
coordinator to commit or reject.
================================================================================


 RUN  v2.1.9 /Users/zacheryspector/The-Movies-headless-program

 ✓ |core| tests/bridge-p13b-s4-office.test.ts > P13B-S4-T3 office bridge page: projection bump > bumps PROJECTION_VERSION to the S4-T3 wire contract (36)
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 1: the founding office at week 12 — full quote parity on the wire; office null off the laboratory view > standard I, highest I, offline false, capacity 2, baselineWeeklyOperatingCost 0 (endowed); both conversion rows match the authoritative quote 1980ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 1: the founding office at week 12 — full quote parity on the wire; office null off the laboratory view > intent kind: both immediate rows publish installationAction; both plan companions publish physicalPlanAction (adjudicated finding C, 2026-09-17) 725ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 2: immediate iii conversion — engagement, reopening, standardAlreadyMet and the exact capex ledger row > commits, engages the body for 16 weeks, reopens at III, and writes one exact -$1,250,000 constructionCapex ledger row 1872ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 3: placed office money on the wire > $5,500 baseline continues through a real weekly charge; II->III quotes $850,000/8 weeks with standardDuringWork I 1466ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 4: plan companions — queue, start, hiding and the immediate-row lockout > plan-queue-office-convert-iii queues, starts, and offlines the body; the immediate rows lock out while it runs 1318ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 4b: a LOWER conversion running immediately leaves a HIGHER companion offered (coordinator-named counter-scenario, 2026-09-17) > while office-convert-ii runs, the iii plan companion is still offered and the ii companion is hidden; queueing iii holds on /targetEngaged/ during the ii work 2363ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 5: insufficient funds > both rows read insufficientFunds; plan companions still offered; a queued plan reads next.reason "insufficient cash" 1476ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 6: stale revision > an office intent submitted with a stale expectedStateRevision is refused with STALE_REVISION, and that attempt changes nothing 1601ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 7: player-safe — no rival facility id ever appears > a structurally injected rival facility id is refused as INVALID_CONTROL; no rival studio id leaks anywhere in the player's own office page 344ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 8: history — the existing P09 facility-completed row, no invented "office" kind > the completed iii conversion writes one Opened: <name> row through the existing facilityCompleted mechanism, never a new kind 1276ms

 Test Files  1 passed (1)
      Tests  11 passed (11)
   Start at  03:17:31
   Duration  20.42s (transform 4.04s, setup 0ms, collect 5.30s, tests 14.43s, environment 0ms, prepare 188ms)

