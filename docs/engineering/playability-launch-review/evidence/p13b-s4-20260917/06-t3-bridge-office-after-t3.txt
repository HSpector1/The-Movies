P13B-S4-T3 — after-T3 run of tests/bridge-p13b-s4-office.test.ts (per-case results)
Independent test engineer, 2026-09-17. Repo /Users/zacheryspector/The-Movies-headless-program,
worktree branch wip/headless-program-20260916-ts.

  $ grep -n "PROJECTION_VERSION = 36" bridge/schema/bridge-schema.ts
  84:export const PROJECTION_VERSION = 36 as const   -> matched, as instructed.

NOTE: T3 (bridge/office.ts itself) was already fully landed in the shared working
tree before this task's very first run (see 06-t3-bridge-office-first-run.txt's
header). There is therefore no distinct "before/after office.ts landed" transition
observable from inside this task for the office page's own logic. This capture
reflects the SAME office.ts/officeConversion.ts implementation as the first-run
file, taken after this test file's own case-6 test-construction bug (see the
other evidence file) was fixed, i.e. the FINAL, stable per-case result of the
completed test file against the delivered T3 implementation.

CONCURRENT-WRITE RACE OBSERVED (full disclosure, run-environment caveat): the
shared worktree's `bridge/session.ts` carries a live mtime of 2026-09-17 01:04:18
UTC — the concurrent sim-core specialist actively modified it partway through
this task (unrelated S4-T3B work per this task's own brief). One run at 03:03:48
local time (immediately after that write) produced 6 failures instead of 2,
including a spurious `INTENT_NOT_AVAILABLE` on case 8's very first, otherwise-
reliable dispatch — consistent with a torn read of a file mid-write, not a real
behavior change. Three OTHER clean runs this session — 03:01:42, 03:02:44 (BEFORE
the write) and 03:04:41 (AFTER it settled, with `bridge/session.ts`'s mtime
unchanged between that run and a follow-up check at 03:05:01) — all reproduced
the identical stable 7-passed/2-failed result, same two failures, same line
numbers. The tail captured below is the 03:02:44 run. Anyone re-running this file
against a shared, actively-written worktree should expect the same transient-
failure risk and re-run once to confirm stability before trusting a single
result.

Command:
  npx vitest run tests/bridge-p13b-s4-office.test.ts --minWorkers=1 --maxWorkers=1 --testTimeout=200000 --reporter=verbose

Result: 7 passed, 2 failed (9 tests total). The 2 failures are DELIBERATE,
contract-literal "CONTRACT CHECK" assertions (flagged inline in the test file's
own header and at each assertion site) — reproducible requirement-vs-implementation
gaps against the coordinator's fixed T3 wire contract, not test bugs:

  (A) case 2 — the row for the SAME blueprint currently under construction
      reports `refusal: 'alreadyInstalled'` on the wire instead of the
      contract-specified `'targetEngaged'`. Root cause: bridge/office.ts's
      `refusalOf()` returns `quote.rejections[0]`; `queryFacilityInstallation`
      (src/core/placement.ts) pushes `alreadyInstalled` before `targetEngaged`
      into `rejections` for a blueprint already committed on this exact body.
      The engine's own `rejections` array is correct and contains BOTH values
      (tests/p13b-s4-quotes.test.ts's own engagement test, lines 124-174,
      already asserts `.toContain('targetEngaged')`, which holds) — this is a
      bridge-layer single-string precedence choice, not an engine defect.

  (B) case 4 — once a plan-started conversion commits (a real installation
      record now targets the body), the contract says BOTH plan-queue
      companions (ii and iii) are hidden. Only the iii companion (the one whose
      OWN blueprint is now committed) is actually hidden; the ii companion (a
      DIFFERENT blueprintId) stays `enabled: true`. Root cause:
      bridge/office.ts's `companionHidden(state, own, facilityId, blueprintId)`
      is scoped to one exact blueprintId; it has no "any conversion committed
      on this body" branch.

Both findings are reproducible with the disposable vite-node probe commands
named in the test file's own header comment (never committed) and independently
via the test cases themselves, which fail deterministically at the exact lines
named in the tail below.
================================================================================


 RUN  v2.1.9 /Users/zacheryspector/The-Movies-headless-program

 ✓ |core| tests/bridge-p13b-s4-office.test.ts > P13B-S4-T3 office bridge page: projection bump > bumps PROJECTION_VERSION to the S4-T3 wire contract (36)
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 1: the founding office at week 12 — full quote parity on the wire; office null off the laboratory view > standard I, highest I, offline false, capacity 2, baselineWeeklyOperatingCost 0 (endowed); both conversion rows match the authoritative quote 1174ms
 × |core| tests/bridge-p13b-s4-office.test.ts > case 2: immediate iii conversion — engagement, reopening, standardAlreadyMet and the exact capex ledger row > commits, engages the body for 16 weeks, reopens at III, and writes one exact -$1,250,000 constructionCapex ledger row 1061ms
   → expected 'alreadyInstalled' to be 'targetEngaged' // Object.is equality
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 3: placed office money on the wire > $5,500 baseline continues through a real weekly charge; II->III quotes $850,000/8 weeks with standardDuringWork I 1072ms
 × |core| tests/bridge-p13b-s4-office.test.ts > case 4: plan companions — queue, start, hiding and the immediate-row lockout > plan-queue-office-convert-iii queues, starts, and offlines the body; the immediate rows lock out while it runs 1945ms
   → expected true to be false // Object.is equality
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 5: insufficient funds > both rows read insufficientFunds; plan companions still offered; a queued plan reads next.reason "insufficient cash" 1921ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 6: stale revision > an office intent submitted with a stale expectedStateRevision is refused with STALE_REVISION, and that attempt changes nothing 1847ms
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 7: player-safe — no rival facility id ever appears > a structurally injected rival facility id is refused as INVALID_CONTROL; no rival studio id leaks anywhere in the player's own office page
 ✓ |core| tests/bridge-p13b-s4-office.test.ts > case 8: history — the existing P09 facility-completed row, no invented "office" kind > the completed iii conversion writes one Opened: <name> row through the existing facilityCompleted mechanism, never a new kind 1616ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL |core|  tests/bridge-p13b-s4-office.test.ts > case 2: immediate iii conversion — engagement, reopening, standardAlreadyMet and the exact capex ledger row > commits, engages the body for 16 weeks, reopens at III, and writes one exact -$1,250,000 constructionCapex ledger row
AssertionError: expected 'alreadyInstalled' to be 'targetEngaged' // Object.is equality

Expected: "targetEngaged"
Received: "alreadyInstalled"

 ❯ tests/bridge-p13b-s4-office.test.ts:269:28
    267|     const midIII = required(mid.conversions.find(c => c.blueprintId ==…
    268|     expect(midIII.available).toBe(false)
    269|     expect(midIII.refusal).toBe('targetEngaged')
       |                            ^
    270|     expect(mid.actions.find(a => a.id === `office-convert-${facilityId…
    271|     expect(mid.actions.find(a => a.id === `office-convert-${facilityId…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL |core|  tests/bridge-p13b-s4-office.test.ts > case 4: plan companions — queue, start, hiding and the immediate-row lockout > plan-queue-office-convert-iii queues, starts, and offlines the body; the immediate rows lock out while it runs
AssertionError: expected true to be false // Object.is equality

- Expected
+ Received

- false
+ true

 ❯ tests/bridge-p13b-s4-office.test.ts:345:100
    343|     // the body. `companionHidden` is scoped to ONE blueprintId, so th…
    344|     // companion (a different blueprint) stays offered. Written to the…
    345|     expect(startedOffice.actions.some(a => a.id === `plan-queue-office…
       |                                                                                                    ^
    346|     const iiImmediate = required(startedOffice.actions.find(a => a.id …
    347|     expect(iiImmediate.enabled).toBe(false)

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯

 Test Files  1 failed (1)
      Tests  2 failed | 7 passed (9)
   Start at  03:02:44
   Duration  14.56s (transform 2.35s, setup 0ms, collect 3.20s, tests 10.92s, environment 0ms, prepare 114ms)

