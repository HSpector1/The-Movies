# Current Ops — Build50 input recovery and bounded native verification

**2026-09-15 · OPS-R3-N1-INPUT-RECOVERY-20260915-02**

**PRESERVE BUILD50 AS A SOURCE-CORRECTED / RENDERED-VERIFIED PARTIAL. AUTHORIZE A BOUNDED INPUT-ENVIRONMENT CHECK, THEN THE PREPARED NATIVE RUNS ONLY IF THEIR PREFLIGHT PASSES.** Same Fable coordinator, worktrees and existing specialist roles; no new team, setup campaign, Owner acceptance or P13B activation. This amends order10's verification stop and N1 reserve suballocation only as stated below.

## 1. Reviewed sources and limits

- The-Movies `103a73b126a551531bf3e4cf095d6ace349cdaf7`: K1–K7 atop `docs/engineering/playability-launch-review/06-FABLE-ADOPTION-AND-FRESH-SESSION-HANDOFF.md`.
- Private Unity publication `9e9d2ccc5b4fb4a9fd392cb5292b978217b440ce`: `docs/evidence/r3n1-native-correction-20260915-01/00-START-HERE.md`, its correction record and `evidence/Unity/r3n1-19/sleep-and-desktop-evidence.txt`.
- Prior authority: `3cf828a9f8853b1a524308c3567d7f47f75cddcc`, `10-R3-N1-NATIVE-DEFECT-CORRECTION.md`, and the retained selected R3/full-overhaul orders. Read only the new/affected sections; no historical handoff reread campaign.

Current Ops reviewed the published reports and power-log excerpts, not the live desktop, private permission databases, raw witness streams, withheld binaries or a replay of tests. Reported results remain source fixes plus 1751/1751 EditMode, 209/209 rendered PlayMode, 5277 TS passes/5 skips, and zero executed Build50 native task steps. None of F7–F15 is natively closed by this order.

| Identity | Preserved value |
|---|---|
| Unity candidate | `74c2141a241a11e43affd9dee909a74b4b1b9e2e` |
| TS source bound by seal45 | `43b3a6b1da3360feed31e5c5be421817a5d003aa` |
| TS later coordination record | `103a73b126a551531bf3e4cf095d6ace349cdaf7`; not a new player build |
| Build50 executable SHA-256 | `c9816921214127a74b93033b424fea191205e24d7be05cc05c88b3a11f31c177` |
| Build manifest SHA-256 | `319fb167eaffc4e163ed25e6dde26fd45a2714be432e7d811aee5dbad3778189` |
| Contract | Save20 / protocol4 / projection30; common DTO blob `9420d5ef5e5b7db5d9c18d2300f22a3d94074eb2` |
| Managed fixture | `r3n1-dense-02`, SHA-256 `bd64bbeb1dce2f39455e20ee1b562f1d16c464bd76d907624fe7774e8d251005`; actual counts/states remain those in K5 |

Preserve Build46's package, Build49's evidence, every interrupted attempt and Owner campaigns. Do not reset source or alter ProjectSettings while diagnosing input. The reported restored settings must still agree with the actual candidate binding before input.

## 2. Correct the cause attribution

The earlier sleep explanation is withdrawn. The published power-log excerpts do not support system sleep during Run A. The observation to retain is missing ordered acknowledgement of the injected event, followed by expiry of the 7200-second witness lifetime. The coordinator clock-discontinuity observation and blocked/idle duration remain recorded separately; do not turn either into a clean latency sample.

Accessibility/Input Monitoring, helper identity/recompilation and another desktop automation are hypotheses to check, NOT established root causes. In particular, the published Codex Computer Use assertions are timestamped after the first Run A failure. Their presence cannot by itself explain that earlier failure. No claim that sleep prevention, a permission toggle or stopping a service has repaired the path until the actual ordered acknowledgement is observed.

## 3. Desktop ownership and bounded diagnostic gate

No fifth full-run retry under the unchanged failing environment. The current coordinator remains sole native-input owner, but requires a CURRENT explicit desktop-availability statement. An order saying “reverify” does not itself renew an old availability statement. If Howard uses the desktop, the guard stops the run; do not automatically resume it.

Before any input, the coordinator may read only task-relevant local facts: exact helper/node/witness executable paths, hashes and code-signing identities; source/token/process ownership; current admission metadata; documented app permission state where visible without changing it; and the relevant comparison with the successful Build49 run. No private database/transcript search or broad environment dump.

Have Howard pause any other active computer-use task through its owning application after its work is preserved. Do NOT kill Codex, VS Code, SkyComputerUseService or other unowned processes, unload a service or log out/reboot the Mac automatically. Presence of a background service alone is not proof it is sending input or the cause of this fault. A verified exclusive input window is the requirement.

If a missing permission is actually identified, return the exact app/helper identity and the particular permission for Howard to review in System Settings > Privacy & Security > Accessibility or Input Monitoring. Do not assume a raw helper must appear as a separately grantable app. Grant requests must concern only trusted already-authorized tools; never enable every app, reset TCC globally, edit the permission database, disable SIP or change global security settings. Permissions changed by the Owner are recorded, not silently applied by Fable.

This is a diagnosis and controlled admission, not a new harness project. Existing source/report inspection and preparation can proceed while the desktop is unavailable; native input cannot.

## 4. Preflight must fail promptly, not wait two hours

Before another admitted attempt, fix ONLY the coordinator-side abort supervision whose zsh glob failed. Use explicit run paths and the existing ownership-bound lifecycle/cleanup interfaces, not wildcard process matching, bare PID killing, or a second input mechanism. Test that supervision on an inert, owned synthetic blocked child first. This may write only a small coordinator wrapper/check and evidence in the existing owned evidence/entry roots. No changes to driver, witness, ownerinput source, admission law, event acceptance criteria or game code are authorized. A necessary deeper change must return as a named proposal.

On the existing helper's no-ACK/failure result, stop scheduling inputs and invoke the existing interruption/cleanup immediately. Add an outer monotonic no-progress bound of at most 30 seconds for the first-event acknowledgement stage; it may abort earlier, never permit an otherwise rejected input. Do not wait for witness expiry, extend acknowledgement deadlines, replay the event, synthesize success or accept another helper's token. Record the actual command/token/helper/witness sequence. If safe bounded cleanup cannot be achieved through the existing interfaces, STOP before launching a player and report that exact lifecycle gap.

After Owner-side actions and current availability, permit ONE minimal preflight using the unchanged guard and the exact candidate/toolchain. It must establish the correct originating helper's ordered acknowledgement and one harmless, reversible local UI interaction with no gameplay commitment, followed by clean owned shutdown. Use an authorized generated fixture. This is diagnostic admission evidence, not closure of F7–F15. Each subsequent run receives its own fresh admission; no borrowing preflight tokens.

If the same acknowledgement failure recurs, stop this recovery attempt. Return the first failing boundary and useful trace; no additional full-run retry, second coordinator or long witness wait.

## 5. Conditional Run A / Run B and unchanged evidence standards

Only after the preflight passes, run the existing prepared A and B scripts sequentially on Build50, within the budget below, with current desktop availability and fresh guard/build/fixture admission. Check cleanup between them. One run per prepared script is released; an infrastructure failure stops later runs until disposition. A discovered product defect is reported with its actual failed task, not worked around and called passed. Preserve the existing per-run sleep assertion when used, but it is not a remedy for unacknowledged input.

Bind the actual compiler/settings/source/worker/player/tool/analyzer versions. Preserve analyzer v2 and raw evidence; no replacement samples, invented timing corrections or relabelling source assertions as native results. Record separately the prepared but previously unexecuted scripts and their new actual run identities.

The native goals are F7–F15, the 1280x720/200% compact cell, lawful picture-list overflow and the literal Schedule-take route's visible invalidation/fresh review or actual lawful refusal. Server tests remain server tests. Different fixtures remain different campaign proofs. Do not demand a second actor or manufacture a stale API request through native tooling.

R3.3's two-row tab-strip cap/footer treatment and the reported ten-pixel list-floor shortfall stay OPEN. This recovery does not waive them, authorize a silent geometry change, or convert 209/209 into whole-N1 acceptance. R1.5 with no applicable three-action footer remains a documented applicability statement, not an invented defect. Preserve full-overhaul coverage and later P13B obligations.

## 6. Reserve disposition and finite endpoint

The final user transmittal reports approximately 337.5 of 360 N1 reserve minutes used, leaving approximately 22.6 minutes; the published K6 seven-minute estimate predates final accounting. Use the actual stamped ledger/final tail at entry, append the difference in the next normal record, and do not rewrite the old sealed publication. Do not claim an exact locally verified balance from this remote review. Unattributed setup/smoke time remains NOT REPORTED.

**Increase N1's cumulative verification/correction/delivery suballocation from 6 to 7 hours, drawn from the EXISTING 36-hour whole-program reserve.** The program envelope remains 72 capability / 36 reserve; its final six reserve hours remain untouched. This is not another pool or a general extension. N1 capability maximum stays 20 cumulative hours.

**This entire recovery is capped at 75 productive minutes**, including coordinator reading, bounded diagnostics/supervision/preflight, actual runs, analysis, cleanup and compact publication: no more than 25 minutes through preflight, no more than 50 for A/B/review/return. The smaller of these caps and the remaining cumulative N1/program allowance governs. Unused stage time is not permission for more retries. No capability-to-reserve reclassification to obtain a pass.

On the reported figures, 75 further reserve minutes leave roughly 10.4 whole-program reserve hours, before unknown historical charges/tails; final six hours remain protected. These are projections, not completion estimates. Exclude only documented genuinely inactive waiting, record elapsed stall time separately, and retain all actual diagnostic/failed-attempt/reviewer work. Do not silently mix lead/session time and independent specialist-hour totals; keep the current declared charging basis with its qualification.

Return one small additive record in the existing private review folder and a pointer in the existing handoff: environment observations versus hypotheses; any Owner action; preflight and abort/cleanup result; A/B exact outcomes or NOT RUN; per-defect native disposition; remaining geometry/stale/overflow gaps; actual ledger/tail; and ownership state. No 84-file re-export, repeated model discovery or blanket full suites. Stop at that review result or the first material gate/budget failure. Source inspection is not native acceptance; P13A stays closed, Build50 remains partial until demonstrated, and no P13B/P14/P15/P16, hook activation, PR/merge or protected-ref promotion follows.

Reference for Owner permission review only (Apple, checked 2026-09-15):
- https://support.apple.com/guide/mac-help/allow-accessibility-apps-to-access-your-mac-mh43185/mac
- https://support.apple.com/guide/mac-help/control-access-to-input-monitoring-on-mac-mchl4cedafb6/mac
These explain permission controls, not the diagnosis of this specific missing acknowledgement.
