# Current Ops — R3-N1 execution release

**2026-09-14 · OPS-R3-N1-EXECUTE-20260914-01**

**READINESS REVIEW ACCEPTED FOR THIS INCREMENT. R3-N1 IMPLEMENTATION AUTHORIZED AFTER THE LOCAL ENTRY CONDITIONS BELOW.** This releases the first selected native experience, not the whole UI/UX program, its final acceptance, or P13B. No session or native run is started by publication.

## 1. Source and review disposition

Read the new S1–S7 header in the existing handoff at **HSpector1/The-Movies@31f6e70d558c16be17d547f316adbea84e4a765e**, `docs/engineering/playability-launch-review/06-FABLE-ADOPTION-AND-FRESH-SESSION-HANDOFF.md`. Current Ops read that header and verified the remote TS ref and one-file/203-line additive diff from 7dfe508b. Its F-A/F-B/F-C work is explicitly coordinator reading, not independent specialist review. Accept it as preparation; do not rerun those tasks merely because the specialists become available.

The received report identifies the new Fable coordinator's startup in `/Users/bruce` as the registration failure, with six valid byte-identical profiles. The original P13A/Playability delivery lead has already yielded and stays stopped. Do not return the task to it.

Controlling scope remains issued04 R3 order plus later05 whole-overhaul reconciliation and Owner `f2921730a6ff6cb5f0b8e952995978a8ecb8407f` (R3 HYBRID, 1A/2B/3A/4B). This release supersedes 07's readiness-only implementation hold for R3-N1 alone, and the older coordinator model/permission sample as described below. Read the already supplied FABLE-COORDINATOR.md and the relevant existing companion sections; do not merge a planning branch into the working game to obtain documents.

| Identity | Value and meaning |
|---|---|
| Last accepted product | Qualified P13A Core pair and closeout; not the overhaul |
| Reviewed gameplay foundation | TS `6e2c2ca10ce70f43f58e99193e052d4afa59bcfb` / Unity `e8c59d8672b6e32de73ed10628abeff49bc1a95e`, Build46 |
| Current TS coordination checkpoint | `31f6e70d558c16be17d547f316adbea84e4a765e`, `wip/playability-interaction-01-ts`; documentation/configuration descendants are not a new player build |
| Current Unity checkpoint | `e8c59d8672b6e32de73ed10628abeff49bc1a95e`, `wip/playability-interaction-01-client` |
| Incoming contracts | Save V20 / protocol4 / projection30; DTO blob `9420d5ef5e5b7db5d9c18d2300f22a3d94074eb2` |
| Exact coordination directory | `/Users/bruce/The Movies - Playability Interaction TS` |
| Approved Unity counterpart | `/Users/bruce/The Movies - Playability Interaction Unity` |
| Selected design archive | R3 at `b56088d63e5b8eb66c78584c7b0f40f915c08d8b`; existing verified local archive root `/Users/bruce/Desktop/Fable-Verified-Sources-20260914-01` |

Preserve newer lawful source, current evidence, Build46, P13A candidates/launchers and all Owner campaigns. Remote refs are not a claim about live local processes or uncommitted work.

## 2. Single restart and actual entry

Finish the already-saved coordinator continuation and exit the incorrectly home-launched Fable session before starting another. No process killing, lock deletion, reset, stash/discard, setup reinstall or additional outgoing-code-owner ceremony is required. Record the coordinator replacement locally; do not claim this document signed its acceptance.

The Owner explicitly selected Fable 5.1 for the coordinator and bypass permissions. Preserve those choices; do not force Opus/manual from the older sample. Specialist model/tool profiles remain unchanged. The command below omits a coordinator model override so the reported saved `claude-fable-5-1[1m]` choice can apply; verify the actual displayed model at startup. If the effective selection differs, report it rather than silently substitute or change global settings.

```bash
cd "/Users/bruce/The Movies - Playability Interaction TS" &&
/Users/bruce/.local/bin/claude --dangerously-skip-permissions \
  --settings '{"disableAllHooks":true}' \
  --add-dir "/Users/bruce/The Movies - Playability Interaction Unity"
```

No `--continue`, `--resume`, `--worktree`, safe/bare mode or concurrent replacement. Current official model documentation distinguishes saved settings from session-only `/model`; the local record specifically reports a saved default. Requested/displayed coordinator model does not verify specialist runtime models.

Before delegation, confirm this fresh runtime lists the actual six custom roles. Do not repeat the validator, the failed probe or a six-agent benchmark. The fresh registry observation closes registration only; it is not gameplay acceptance. If names remain absent, stop affected delegation and report the exact discrepancy; do not impersonate a named specialist with general-purpose.

Record the actual paired local sources, current instructions, ownership and budget. Complete the unfinished R3 source uptake: actual prototype and remaining early/waiting/busy/small/enlarged previews, including Back/focus behavior. Reuse the two already viewed previews. No new mockup competition or research survey. Safe read-only uptake and existing designer coordination may proceed without desktop-input admission.

Once these source/registry/ownership conditions are satisfied, routine implementation within this release proceeds without another Current Ops launch permission. Native input still requires the actual bound build, legal fixture, explicit current desktop availability and existing guard; bypass permissions does not waive those task boundaries.

## 3. First native outcome — no partial substitute

Deliver employees left from the complete employed roster, hybrid script/movie cards right, and a useful studio center. Keep exact subject identity, truthful stage/action/wait state, counts, reachable filters/Find/paging/history and selected R3 art/layout. Preserve useful existing command, navigation, text and campaign work.

Accept the F-B finding that the full employee roster already crosses the bridge. Reuse existing roster rows/counts, `population == employed`, stable talent IDs, roster filter/context owners and optional same-revision presence joins. Missing world presence must not hide an employee or manufacture Locate. Removing the display cap must not mean unbounded full-profile polling or rendering every offscreen row; use bounded visible rendering and existing read models.

For picture wait/attention/company/worksite facts, verify the existing fields before using them. A absent fact is not permission to infer a new gameplay rule. No new wire field, DTO/version, campaign migration or TS simulation change is authorized. If necessary, return one exact proposed read-only contract delta; continue independent permitted work.

**R3-N1 includes the minimum list-level keyboard focus, inspector/context access, non-overlapping studio-name/timeline and text reflow needed to pass its own native task.** The estimate places broader shared work in S2, but that cannot defer behavior required by N1. Do not mark N1 complete while both rails vanish under inspection, global tools are obstructed, or critical enlarged text is clipped.

D-1 remains a real design dependency: use the existing designer's compact inspector/shared state sheet and responsive rules for the risky small/enlarged states before implementing that materially different layout. Repeated states can share one sheet. Existing full-screen Profile/Production workspaces may be reused internally or as a labelled partial engineering increment, but are not an automatic substitute for the selected connected inspection experience. Missing D-1 blocks that layout's implementation and N1 acceptance, not independent roster/identity/return-state work.

Use the existing R3 stage-art provenance and approved native conversion path. Placeholder portrait treatment may support this early behavior increment only when explicitly labelled; it does not close final portrait/population-art requirements. Complete the applicable asset/font access check, not an asset search campaign.

## 4. Bounded task ownership and allowed changes

**Coordinator/integration owner:** the correctly restarted Fable successor after recording its acceptance. **Production writer:** registered unity-ui for this increment; Fable does not concurrently edit its files. **Test owner:** registered test-author with disjoint test/evidence paths. **Native-input owner:** Fable unless an explicit timed handoff is recorded; only one slot. At most two specialists concurrently. No nested delegation. Existing designer retains design ownership; do not dispatch a duplicate designer.

Implement the S5 proposal against actual paths, with these bounded adjustments:

- Unity presentation/infrastructure: employee and production rail HUD/contracts; their filter/focus/return context; current workspace/profile/production navigation owners; the small shared HUD/inspector/text layout helpers necessary for N1. Reuse existing context owners. New presentation-only helpers/enum members and matching `.meta` files are permitted; no persisted or wire enum changes.
- S5's named paths are source locators, not proof every proposed filename exists. Resolve them against the actual tree and record the exact affected-file list before editing. Do not create a parallel obsolete owner to match a guessed filename. A sibling client-only helper within the same narrowly assigned owner does not require a new Owner vote.
- Asset writes: only the approved R3-derived 2D stage-art import/atlas and related native metadata, kept separate from 3D lot/world geometry. Preserve provenance and licenses. No fonts from an assistant container, paid assets, blanket installs, engine/UI framework replacement or global configuration changes.
- Tests: the existing rail-contract, scroll-owner, roster, production/profile-navigation, workspace and client-text tests; add focused return-context and native UI regression tests where required. Derive expected results from the selected behavior, not existing output. Test-author may not repair production or weaken assertions.
- TS: no production edits. Existing generated-fixture/build scripts may run only in authorized disposable roots. A needed generator/harness source change requires its named bounded proposal; do not mutate fixtures by bypassing game law.
- Evidence writes: new uniquely named runs under the existing authorized `artifacts/playability-interaction-01/` and `Evidence/Playability-Interaction-01/` roots, plus the existing handoff/coverage record. These additive outputs are allowed; S5's evidence-root exclusion means preserve old/sealed evidence, not prohibit new evidence. Do not overwrite historical runs, Build46 package or Owner campaigns.

Use installed build/test/capture tools only under the actual task permissions and existing guard. Build/source/worker/schema binding must describe the new pair, not claim the configuration HEAD was previously tested. Targeted tests during iteration, applicable full verification at the increment's candidate review; do not rerun all evidence merely to establish a new session.

## 5. Budget disposition — staged delivery, no automatic 155-hour program

The S6 estimate is **78 remaining capability + 34 remaining reserve = 112 remaining productive hours**, not a total-program estimate. It exceeds the known remainder of approximately 48.41/17.28. Current Ops accepts the need to stage the full outcome; the old 108-hour envelope does not justify cutting it. **The proposed additional approximately 30 capability / 17 reserve hours is NOT yet a blanket authorization.** The designer inventory is not complete, and N1/shared/art overlap must be charged once.

**Release only R3-N1 now:** planning target 16 capability hours; **hard maximum 20 capability hours plus 5 protected verification/correction hours**, including its required source/design uptake, coordination, implementation, tests, native comparison, product critique and compact handoff. These are reservations inside the existing cumulative 72 capability / 36 reserve envelope, not added hours. Stop earlier if the remaining cumulative allowance is smaller. Verification reserve cannot finish unbuilt features.

Using the rounded reported starting balance, a full 20/5 charge would leave approximately 28.41 capability / 12.28 reserve hours before additional attributable historical charges. The last six reserve hours remain protected for final reverify/package/delivery. The existing below-18-reserve escalation is expressly disposed for this bounded increment only; no discretionary polish.

At 12 capability hours, record remaining N1 work and any design/data block. At 16, estimate-to-complete must fit the remaining four; otherwise stop affected capability and return the exact remainder rather than spend reserve or claim a stripped-down milestone. At 20 capability or five increment-reserve hours, stop at a safe checkpoint even if PARTIAL. Preserve all failed evidence and recoverable work.

The published readiness snapshot excludes its closing tail; carry the reported final approximately 16-minute readiness charge once and reconcile the actual local ledger before starting this increment. Do not reset the prior 150-minute startup window or rerun already answered F-A/F-B/F-C. Unattributed setup/smoke time remains NOT REPORTED, not zero; keep it as a separate outstanding accounting line. That uncertainty does not block the bounded release on the known balance, but it must remain visible and be resolved or explicitly dispositioned before claiming final program budget fit. No private-log archaeology.

For the next stage recommendation, deduct N1-delivered keyboard/inspector/HUD/art work from S2/S6, separate remaining designer delivery from native work, and price final art coverage rather than equating six portrait samples with completed population art. Include required S3–S7 scope and protected verification. Return this update with N1, not as another pre-implementation research cycle. S2–S7 beyond N1's necessary shared work remain held for their staged disposition; none is deleted.

## 6. Native acceptance task and stop line

Use lawful generated early/mature fixtures. Test at 1280×720 and 1440×900 with 100/150/200% text for changed surfaces. Larger/Retina admission remains a named later full-pass obligation; do not bypass the current driver's viewport rules. Target meaningful crowded lists using legal game capacity; record actual density rather than manufacture people/pictures to hit 40/20.

R3-N1 review starts with both rails at nonzero scroll offsets and records exact selected IDs, filters/search and invoking focus:

1. Inspect an employed person, including same-name and missing-presence controls; Back restores context in both rails. Locate occurs only by the legal explicit action.
2. Inspect an actionable picture, its supported company person and worksite; Back through the route preserves both lists and focus. One activation produces at most the existing legal effect; inspection alone spends nothing.
3. Inspect an ordinary waiting picture; show the real cause and only a supported remedy/inspection. Never turn stale historical blocker text into a current factual claim.
4. Exercise keyboard list entry/exit, all controls, live list changes/missing targets, library return, pointer/scroll versus lot camera, and reachable global tools. Text enlargement must preserve essential title/state/reason/cost/Back information and selected context.
5. Compare actual native output with R3 at comparable size. Record screenshots and actual input/state/receipt evidence separately. Measure changed-route acknowledgements using the existing methodology where applicable; final whole-pass matched Save/load and input obligations are retained, not silently claimed by this increment.

Keep UX-STALE-NATIVE-01 accurately labelled: test the changed commitment route's visible invalidation/required fresh review or an actual lawful refusal with independent command-owner rejection coverage. Prevention is not server refusal; no artificial race, API injection or weakened guard. Owner design selection is not native acceptance; registered agents and green tests do not prove usability.

Return one compact candidate review: exact TS/Unity/build/DTO/asset identities; changed paths; first product critique against this connected task; actual native settings/results and failures; remaining full-overhaul coverage; increment and cumulative charges; existing designer's remaining delivery; costed next stage and exact stop/ownership state. Keep the prior candidate and rollback source. Do not rebuild or republish thousands of old evidence files; link the existing pinned evidence and add only changed/new records with an index.

Stop at N1 review readiness or a real blocker/budget stop. No PR, merge, force/protected promotion, Owner-campaign access, hooks, goal-hook activation, or P13B/P14/P15/P16 coding is authorized. The whole-game UI/UX overhaul remains the outcome; this is its first selected native checkpoint, not completion.
