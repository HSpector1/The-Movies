# Current Ops — R07 disposition and bounded engine follow-up

**2026-09-16 · OPS-P13B-R07-DISPOSITION-20260916-01**

**ADOPT the Future Ops R07 clarification for headless implementation, including the standard-stage sequencing refinement and both corrections.** This closes the named R07 recipe decision, not its implementation dependencies or acceptance. The existing Fable coordinator owns uptake and execution. No new session, research campaign, review team or Unity work.

## 1. Sources and review boundary

Controlling clarification: The-Movies `e529c10f83e3fdab9c7911e1498c4f279ae64ce9`, `docs/engineering/playability-launch-review/P13B-R07-PRODUCTION-CLARIFICATION.md`, Git blob `77cf8211dcec17f431afd988106ef43c931258f3`. Read the supplied complete document; this disposition adopts its sections 2–6 with the execution placement below.

Current Ops re-read the engine branch at `877aad57f6c5356724ce4fa47c582800e46571ca` and inspected `src/core/technology.ts` (research completion, operational adoption and shared validation) and `src/core/operations.ts` (managed production sweep). This establishes the source findings below, not live process ownership, a finished full-suite result, or a reproduced engine failure. Reconcile any newer lawful local work before changing it. Preserve the current worktree, active run and unpublished work; do not reset to the reviewed pin.

The existing Owner logic-first directive remains controlling: engine implementation AND verification now; Unity compilation, native interaction and full R3 visual acceptance paused/open. All eight P13B Ready obligations remain. This document creates no blanket promotion, main merge or change in permission, hook, spending or campaign-access policy.

## 2. R07 specification — adopted candidate content, not final balance

| Planned production task | Conventional | Operational R07 on the exact bound stage |
|---|---:|---:|
| Ballroom reveal: foreground, entrance and background cues; usable `grand-ballroom` Set | 4 setup units | 2 setup units |
| Ordinary single-zone interior; qualifying `generic-interior` or `apartment-interior` Set | 1 setup unit | 1 setup unit |
| Legacy production with no new setup-plan record | Existing schedule | Existing schedule |

Use one eligible production-work week per setup unit as provisional authored tuning. Expose a real reviewed production-plan action and preserve the selected scene requirement; the same Ballroom recipe cannot skip its preparation by calling it ordinary. Choosing a simpler scene changes the plan. This does not impose new preparation retroactively on every existing film or invent a quality reward to justify the recipe.

Extend the existing production-operations/phase owners with the bounded post-rehearsal, pre-Shooting setup subtask. Preserve normal rehearsal, the current tick/phase invariants, fixed-point sweep ordering and stage-release law. At most one unit is credited per eligible interval, with no credit on admission, load, retry or a second sweep visit. Setup start at 820 has candidate completion 824 versus 822; ordinary setup completes 821 in either route, conditional on no other block. These are required scenarios to demonstrate, not runtime results from this review or guaranteed release/revenue leads.

The reduced workload requires the same studio's lawful technology access plus supplied equipment, completed fit-out and operational adoption on the bound stage. Research equipment in a Laboratory, knowledge alone, a different stage, unfinished installation and cancelled/restoring equipment do not qualify. Lighting requires neither synchronized sound nor a Post installation. Retain P09 target availability and occupancy; no fixture bypass.

Fix recipe/workload/equipment provenance at setup admission. Preserve the separate sound lock at actual Shooting entry before the first take. Setup does not complete a take or satisfy a future P14 filming promise. Same-binding continuation retains work; a changed stage, Set or incompatible recipe needs a new physical setup with prior performed work preserved. No instantaneous retrofit discount on partly worked setup, new generic job scheduler, setup surcharge, duplicate payroll, universal refund/undo or global production/quality multiplier.

Persist bounded live and terminal task evidence through the real production/history owner. Save/reload, exact IDs, stale command refusal, completed-task idempotence, retargeting, occupied-stage competition and campaign isolation are required alongside the positive example. A forged operational flag is not the proof of a lawful useful consumer.

## 3. Sequencing — standard first, oversized retained

**Adopt the standard-sized Ballroom as the first R07 proof.** Neutral standard/oversized descriptors belong to the existing Set/placement owners; legacy bodies receive the standard classification without invented setup history. This replaces only the earlier prerequisite that the first lighting demonstration wait for a new large stage.

Keep CAT-011, companion A12, large-stage compatibility, oversized content and the required oversized refusal/acceptance exercise OPEN with an owner and return condition. The K4 body/footprint/pricing choices are not selected here. A standard-stage R07 pass cannot close A12 or all P13B. Do not invent an oversized production to fill a checklist.

In the EXISTING headless plan, name a bounded **S5-R07 consumer task** after S5 supplies the actual per-technology equipment/installation/operational chain. Continue S3 and S4 in their existing sequence; do not fold the entire new production subtask into S2. S6 must subsequently exercise cancellation/restoration eligibility against that consumer, and S8 must bind comparable rival behavior through real rival producers before claiming symmetry. Missing dependent producers stay explicit; no parallel fake system.

Fable records the touched producer/consumer paths, migration/projection plan, requirement tests, effort forecast and later Unity backlog before that task begins. This is the existing delegated task expansion, not a new Owner approval gate. Allocate the next governed versions at execution; do not take S3's planned Save V23/projection 35 or silently reuse a version with different semantics. Preserve genuine earlier fixtures before a new writer changes them.

## 4. Timing and restart corrections — accepted

Use residual horizon **[789,841)**. The lawful prehistory is eight funded four-person weeks (48 units, $320,000 R&D) plus one zero-ceiling week (4 units), giving 52 verified units at 789. Opening 780 and commercial availability 936 stay unchanged.

Residual knowledge dates are sound/light **796/798** for cooperation then lighting and **800/791** for splitting; within-horizon R&D is **$720,000 / $520,000**. Preserve the $200,000 difference. Prior signing and prehistory costs are sunk before this residual window, not charged again. Keep full-project horizon [780,832), the separate Post-onset correction and the distinction between runtime research checks and conditional paper all-in amounts. Neither route's absolute costs are interchangeable with a different starting state. No broad spreadsheet re-review.

**Repeated cancellation/restart is lawful.** “Exactly one restart” means one resumed instance of the same project, not a lifetime quota. Keep identities, work, spending and receipts; recheck eligibility; no cloned work, refunded consumed research or repeated invention/prototype entitlement. Include at least two cancel/restart cycles with save/reload. This does not change S6 installation-cancellation law. Remove the alleged lifetime quota from the unresolved-product-choice list.

## 5. Material S2 finding — close the exact access gap

The inspected `advanceResearchWeek` has both of these selectors without technology identity:

```ts
access.some(a => a.studioId === p.studioId && a.acquiredWeek !== null)
access.findIndex(a => a.studioId === p.studioId)
```

**The predicate omission is confirmed in source; no runtime reproduction is claimed by this review.** Under the published algorithm, acquired access to one technology suppresses another technology's completion grant, and an unrelated pending row can be removed. This matters to S2's two-technology promise; passing its existing work/spend checks does not establish correct access.

At the next safe checkpoint, the existing test owner adds a targeted failing regression on the unchanged candidate, then the production owner fixes the shared access identity to the exact `(studioId, technologyId)` pair and checks related consumers. Required cases: sound then lighting; lighting then sound; simultaneous completion with order independence; unrelated pending/completed access survives; save/reload and later ticks do not duplicate or erase grants. Assert the exact technology/project provenance as well as work and dates. Retain existing same-technology access rules. S5 supplies the corresponding per-technology prototype/equipment tests; do not invent a second entitlement implementation in S2.

Do not edit files under an active matched run. Let it finish or take its existing safe stop, preserve the result, then test the correction on an exact fixed snapshot. Existing passes retain their original applicability. Do not call S2's two-technology logic verified while this specific defect remains; if a closeout already occurred, append a correction rather than erase it. Independent queue work may continue, but any release depending on correct multiple-technology access requires the fix. Rerun the affected S2/access/save/P13A boundary checks and satisfy the existing regression policy; no automatic repeated whole-repository audit.

For S5, the inspected `finishTechnologyWeek` and shared adoption validation still assume sound-stage plus Post. Generalize their technology-specific requirements, target identity and prototype accounting together. Do not just make Post optional everywhere or permit a lighting adoption to satisfy synchronized-sound requirements.

## 6. Return and continuity

The first return is a compact existing-record update: this disposition linked; restart/timing entries corrected; the access regression's before/fix/after evidence; S2 closeout status; S5-R07 task and retained A12/K4 dependency. Continue routine settled engine work without another prompt. Skills and the existing named specialists support that work; no new team or setup checks.

Carry actual usage forward under the logic-first directive; task forecasts are not fresh reserves or a promise the program fits the remaining hardware window. Keep heavy test commands serialized on the host. Use existing evidence roots, sanitized publication and immutable source/fixture identities. Do not publish secrets, private sessions or Owner campaign payloads.

The complete future recipe-review → setup → Shooting → take → result journey, client bindings, readable waits, portraits/art and full R3/UI work stay in `UNITY-INTEGRATION-BACKLOG.md`, not marked passed by engine tests. P13A stays closed. Future Ops has completed this requested clarification; there is no further broad research round here.
