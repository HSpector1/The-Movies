# UNITY-INTEGRATION-BACKLOG — work reserved for the replacement laptop

Every headless slice records here: producer/consumer versions, required UI action/feedback, C# binding
changes, exact future compile/render/native tests, and unresolved product choices. Nothing in this file is
verified in Unity. Generated C# produced by the TypeScript generator is text, not a compiled consumer.

## Standing obligations carried from the paused UI branch (unchanged by this window)

- Paused TS UI branch `wip/playability-interaction-01-ts` @ `e2e409e80eccb6a7fd49fa16aa0f750faeb51253`; paused
  Unity branch `wip/playability-interaction-01-client` @ `08c32c47f6faf223068d0999ec4a7a6c90649948`.
- Open rendered/native defects: **F26** (Development review control unreachable under `inspector-fallback` at
  1280×720 @ 100 % after a workspace closes), the card scroll-rect overrun (76 px), the 100 % body scroll not
  observable, Production clamp false for J1 at 1280×720 @ 200 %, Profile explanation 74 px; IMPL-29 / TEST-27
  were running when the laptop became unavailable — their local state is **not recoverable** from published
  work and must be re-derived on the new machine.
- `dense-03p32` genuine 3-action record remains a NAMED DATA DEPENDENCY.
- Last admitted player: Build56 (Unity 288c4ddb / TS 70a8c3ec, projection 31, seal 51, admission 46). Working
  bridge manifest on the pin: protocol 4, projection 32, schema
  `sha256:b3a76d197481175c3a4a615f634cf227ae00b95981625df3f67784b307fc9e5c`.
- Native +5 % Save/load and acknowledgement obligations remain for a matched later run.

## Per-slice entries

### P14B.4 — cast-class promises (implementation active; no native verification)

Headless checkpoint501 (2026-09-20), published/exactremote
`80aec8b348f7322da552ef3cd35ae9ba671ba488`, separates talent-key and diagnostic
widths without changing owner/comparison tariffs. Original Ready first-take
test NOW passes completely, including the unchanged shared-budget kernel's
UNCERTIFIED/domainIncomplete outcome. Producer uses190818, saving5650 measured
units; actual projection/chronology/wrap/ten holds/purity remain green.
Fresh494–499:254focusedPASS, sole original stale-release test still FAILS;
root/UI typesPASS, sole old futureP2 bridge-type error. Stale now advances through
Post3 but cuts at week8 before Post exit.500probes removed; exact492 tested
source restored.502 records independent exact-remote publication verification.
Genuine rehearsal/unassigned Shooting/no-Set and started Post controls remain
green. Full Ready/stale-release/live feasibility integration remains OPEN.

Headless checkpoint513/514 (2026-09-20, Claude Code runtime after the Owner
transfer), published/exact remote `9ed6e35670b91d207c2f548de4a8ad592c366012`:
505 command-only discovery (replay-internal work accounting) reviewed KEEP
twice (506), 507–512 fixed-source: 255 focused PASS, root/UI types PASS, sole
old futureP2 bridge-type error unchanged, sole original stale-release test
still FAILS. First-take producer189256 (net1562 saved, kernel GREEN). Stale
now cuts inside the week8 sweep bill (Post2→1) at199994; the refusal remains
the week8 remainder, week9 commit, week9 release frame and week10 stale command
away. 514 probes removed; exact505 source restored and hash-verified.

Headless checkpoint535 (2026-09-20): exact pre-call bills (517+526, reviews
518/527 KEEP) on replay source d5f8cb71…; 528–533 fixed-source: 255 focused
PASS, sole old futureP2 bridge-type error unchanged, sole original stale-
release test still FAILS (now at the week8 owner reservation). First-take
producer182925, kernel GREEN. Owner decision item (515§6) recorded: the stale
route is not shown to fit200000 under the work contract as written.

Headless checkpoint536/537 (2026-09-20), published/exact remote
`73c28e2d7385d5b9dc24040a1bb8152340761cf6`: fixed-source run of the eleven
non-replay B4 groups on the qualified checkpoint (110 PASS / 83 FAIL; kernel,
material-evidence, D3, hold-order and stable-sort GREEN; the live-P2 files stop
at the recorded Save29 / rules3 / projection46 / draft-grammar boundaries).
Three READ-ONLY reports classify every failure by true stop point and review the
proposed pre-cutover core slice: NOT lawful as proposed (a rules-4 stamp on a
scalar evaluator is a law the plan does not define, and it breaks the B-F2/B3
rules-3 pins). Two OWNER / Current Ops decision items recorded (537 §3):
D1 evaluator-4 definition and sequencing; D2 the kernel budget finding (one
Ready plan costs 182925 of 200000). No writer released. Consumer impact: the
projection47 / Save30 / class-selection wire contract stays future; its timing
now depends on D1.

These are private logic/work-accounting changes only. No consumer field/action,
protocol4, projection46, live Save29, evaluator3, schema or generated C# changed.
Do not expose partial traces as certified offers, impossible schedules or broken
promises. Existing future projection47/Save30/class-selection/refusal/intent and
native integration obligations below remain unchanged. Handwritten Unity,
compilation/rendering/native UI/UX and Owner acceptance remain deferred.


Headless foundation update2026-09-20: staffing/fact interfaces, detached joint-trace
kernel and shared bounded real-owner sorting are implemented in qualified slices.
Latest sort evidence174:55targetedPASS/rootUItypingPASS; bridge typing retains
the sole futureP2 class-contract TS2353. No consumer shape/version changed.
Actual whole-owner replay, full work-budget composition and coherent live-P2
integration remain OPEN; these foundations do not certify live offers or Unity.

Replay continuation: genuine one-/two-picture controls179/180 independently KEEP181;
test-only checkpoint0bc641a publishes16 independent cases and exact188/189 RED.
Internal replay191 is implemented and partially verified:197rootUIpass,19819/20
pass with one invalid test fixture before replay,20198adjacentPASS;202 retains
the sole known futureP2 bridge type error.196 accounting review requires fixes;
behavioral GREEN does not certify full prepayment or live feasibility. Corrected
fixtures205 now give22PASS and206rootUIpass;196REFINE drives the same-module207
accounting correction.207 then214rootUIpasses/21523of24passes with a real
ordinary writing-background budget failure.213 accepts the construction fixes
but requires remaining calculator payment corrections. Published16119d9 retains
the failure;216 diagnostic shows both real sweeps finish before final-trace
exhaustion.218 same-module payment/retained-Development correction is underway;
220 independent two-picture regression is now actualPASS on218, as is the prior
writing failure.224typingPASS/22523of25PASS; two existing two-pictureH6 replay
cases now exhaust before kernel.226 proves both real wrap sweeps finish before
the cut.227 calculator efficiency correction is now verified:232rootUItypingPASS,
233all25PASS including both repaired two-pictureH6/shared-kernel cases under
unchanged200000;234genuine migrated grandfathercommandPASS;235adjacent98PASS.
231source/237qualifiedevidenceKEEP.236bridge retains sole known futureP2 kind
type error. Next Ready/unstarted/livecapacity integration remains. No live capacity or
consumer readiness follows from partial verification. No
consumer field/action, generated C#, schema or live version changes
are authorized in this four-file tranche. Keep capacity explanations tied to the
future verified evaluator; incomplete bounded analysis must not be presented as
a proven impossible schedule or a causal broken promise.

Preservation checkpoint a76242f retains genuine outgoing Save29 and runtime46;
independent RED d39a9a0 is published. The current bounded data step adds explicit
V30 readers/migrations only. Live writer29/protocol4/projection46/rules3 remain
in place until the coordinated behavior/wire cutover; no paired consumer claim.

Planned consumer obligations at that cutover: explicit lead-only versus
lead-or-antagonist selection for P2, no silent class default, new quote after
material edits, whole refusal and emitted-intent-only commit. Own current/history
rows must show actual class or honest unknown for legacy classless P2; rival
terms/class remain UNKNOWN while public outcomes/trust remain visible. Display
the actual derived public opportunity preference, not a private invented taste.

After generator-owned projection47 and Save30 integration actually lands,
record their exact identities here. Future Unity work: adopt matching generated
DTOs; compile/round-trip absent or inapplicable class, both valid classes and old
unknown history; native actual offer→settlement→class-qualified first completed
take→history journey, class-change intent invalidation/replay/refusal, and genuine
outgoing46 recovery retaining distinct same-week current/saved gameplay slots.
Verify both display scales, long refusal/history text and cross-discipline
casting access. No handwritten Unity, rendering, native UI/UX or Owner acceptance
is performed by the headless tests; inherited schema32–44 gap remains separate.

### P14B-F2 — acting-discipline eligibility (qualified logic verified; Unity not verified)

One-file engine correction makes P1 feasibility use the existing casting law's
acting-profile presence, not primary profession. Fresh evaluator/root3; historical
versions/refusals unchanged. Save29/protocol4/projection46/schema584bdd… and C#
1b5c7e… unchanged. Candidate evidence p14bf2-20260919/11:36files404PASS/5unchanged
TODO, both typechecks/contracts PASS, independent04KEEP. Full89b5ad2:
core3351PASS/22FAIL/6TODO,bridge761PASS/12FAIL/2TODO; all failures exact historical
IDs+diagnostics, both typechecks/contracts PASS, fixedSource:true, independent
14/16KEEP. Qualified closeout p14bf2-20260919-final/17-attribution.md. Core was
about12% slower than B3; no performance-equivalence or all-green claim.

Consumer obligations: do not add a profession-label refusal to a P1 proposal
composer; use the emitted quote/refusal and current intent. A primary writer may
lawfully receive/keep a cast promise without relabeling. Native proof of that
proposal→renewal→actual cast/take→history journey remains deferred. This correction
does NOT broaden existing casting read-model/candidate pools or claim the native
UI already exposes cross-discipline casting. Inventory that integration gap when
building the real consumer path; no UI/chooser/rival-policy workaround here.
Show historical refused receipts as history, not silently repaired live verdicts.
Unity compilation/rendering/native/Owner acceptance remain unverified.

### P14B.3 — atomic P1 proposal command (qualified logic verified; Unity not verified)

Producer stays Save29 / protocol4 / projection46, existing schema584bdd… and
generated DTOs unchanged. New roots and fresh feasibility receipts use evaluator2;
historical roots/receipts remain unchanged on load. Three owners: promises.ts,
bridge/contract.ts, bridge/session.ts. Source425170e; evidence p14b3-20260919:
27targeted files335passes/3unchanged todos, both typechecks/contracts PASS,
independent review KEEP. Full tested359f636 fixedSource:true: core3338PASS/
22FAIL/6TODO, bridge761PASS/12FAIL/2TODO, all failures exact historical identifiers
and diagnostics; both typechecks/contracts PASS; independent final KEEP.
Qualified closeout p14b3-20260919-final/17-attribution.md removes the B1 preview-
only caveat for the HEADLESS P1 route, not for an unverified native composer.

Future consumer work: make the existing P1 draft composer submit ONLY the emitted
intent; whole-quote ok:false (including nested FRAGILE/IMPOSSIBLE) disables commit
and shows the nested refusal verbatim. Caller mutation after quote must not alter
the pending draft; editing a material term requires a fresh quote. Revise/remove/
withdraw must invalidate old intents without inventing outcomes for abandoned
unbound roots. Quote→commit attaches but does not bind or charge money; the later
winning settlement binds the actual contract. Show public outcome/trust history
while rival family/count/window remain UNKNOWN. Native tests owed: lawful P1
submit/revise/remove/withdraw, refusal at both contract edges, expired/replayed/
wrong-session intent, save/restart clearing pending intent, settlement→take or
due-week outcome, and 100%/200% readable refusal text. No new C# shape or hand edit.

Competing CURRENT cross-issuer reservation accounting remains separately OPEN.
B-F2 has-discipline correction is qualified closed in the entry above; its native
integration/candidate-pool gap remains deferred, not silently broadened here.
Unity compilation, rendering, native UI/UX and Owner acceptance all DEFERRED.

### P14B.2 — trust and promise read models (qualified logic verified, Unity not verified)

Producer: protocol4 / projection46 / unchanged Save29. Candidate schema
`sha256:584bdd8565030f049d548b1af4fcbf8c517ca7c9150016736f632f1ef8fcb98c`;
generated C# SHA256 `1b5c7e889ffe3454858afa8960b4a4c099d88cfe25a9553212ba67f11a4c3268`.
Source `3b978d77c29df2b5de038e3b1b7fc94778257f33`; generator, targeted bridge/
checkpoint/D3, both typechecks and contract checks passed with qualifications in
`evidence/p14b2-20260919/13-candidate-review.md`. Qualified closeout
`evidence/p14b2-20260919-final/18-attribution.md`: full frozenbee7e22,
22core/12bridge exact inherited failures plus one shared test-literal omission;
test-only correction7f89f75,238corrective passes/two unchanged todos, both
typechecks/contracts PASS. Not an all-green full rerun or compiled consumer/native
evidence. Final schema/generated bytes unchanged by that correction.

Required bindings: Profile `trust` (label/scope/top-three dated drivers/line) and
`promises` are required, never omitted. Empty history is `[]`; open outcome/week/
cause are null. Shared bound-history row now requires real nonnull `contractId`
and nullable `outcomeCause`, newest first, on Profile, case and workspace history.
Industry studio `trustLabel` is required; public Pulse `outcomeKind` is optional
and ABSENT on unrelated activity, not an invented empty/null enum. Add attention
causes `promiseDue`/`promiseOutcome`; preserve the original five and their actions.

Future integration: regenerate/copy matching DTOs only during authorized Unity
pairing; compile and round-trip empty/populated trust/history, null open outcomes,
and absent optional Pulse discriminator. Render long reason/year text and nonempty
history at 100%/200%, preserve selection/scroll and case navigation. Native real
journeys: retention→take/kept, release/broken, poaching→closed case→due reminder,
outcome clears next week, genuine45 checkpoint opens with both distinct save slots
unchanged and a new session. Test rival terms remain UNKNOWN/absent while public
rival outcome and aggregate trust remain visible. No new action/view/page/intents.

Projection45 runtime identity is explicitly carried forward; missing historical
32–44 identities remain the separate inherited recovery obligation below. B1's
promise quote is STILL PREVIEW ONLY until B3 command integration, not fixed by
these read models. Copy/reminder8 tuning are existing hypotheses; no new Owner
decision. Unity/rendering/Owner acceptance remain deferred.

### P13B-S1 — full named research staffing (engine)

| Item | Record |
|---|---|
| Producer version | **Save V21 is the live writer** (`makeSave`), technology root v2 (`seats`/`weeks`/`legacy`); engine commits `57980e95` → `b5b2412` → `5a52e1e` → `d74426a`. Bridge protocol 4 / projection 32 unchanged; contract manifest schema `sha256:b3a76d19…` unchanged (`check:bridge-contract` + fixtures PASS at b5b2412). `migrateToV20`/`V19`… refuse a V21 save (no silent downgrade); a V20 save is proved by the frozen v1 validator, then lifted |
| Consumer change | None on the wire. Laboratory page (projection 32) row changes an old client would notice: the single `recruit-<placementId>` row is replaced by **one row per free named candidate** `recruit-<placementId>-<t-sci-NN>` whose intent carries `scientistId` (an old client's stored `recruit-<placementId>` intent is refused as unknown, by design); per-seat `release-<placementId>-<talentId>` rows (`releaseResearchSeat`); `seatLabel` ("Seats assigned: n of 4 … Seated: …"), multi-line `scientistLabel` (one line per seated person), `provenanceLabel` names the completing seats. `scientistId` stays nullable = first unreleased seat |
| Required UI action/feedback | Candidate list of eight named people with a hire row each (offer terms, contract end, "seat retained" when the person still holds a seat); seat list (≤ 4) with per-seat release and per-seat employment status ("contract no longer active" after expiry, rehire of the same id); refusal reasons: four seats occupied, second Laboratory (S1), eight Scientists employed, unemployed person, already holds a seat; auto-pause explanation when no seat is eligible; the "n of 4 seats" count |
| C# binding change | None until S1b adds `seats` (talentId, assignedWeek, releasedWeek, employed) and per-week receipts to `StudioLaboratoryPage` (projection 33); DTO regeneration + paired Unity adoption then |
| Future compile/render/native tests | EditMode: Laboratory workspace binds up to 8 recruit rows + up to 4 release rows and the multi-line scientist label; PlayMode: seat rows/labels at 100 %/200 %, 1280×720 and 1440×900; native A1/A2 (companion §8) on generated worlds: hire/assign four, fifth refusal, expire one, rehire same id, idle salary/overhead; a future player must explicitly pair with Save V21 before loading engine-generated test saves (`CURRENT_ACCEPTED_SAVE_VERSION` in `scripts/bridge-contract-consumer-lock.ts` is still the consumer's attested value and is not bumped by the engine) |
| Migration / integration risks | The old executable (Build56) cannot read V21 saves; keep engine-only candidates away from its live saves (Owner rule). Campaign-library records written by this engine are V21. Evidence generators now stamp `makeSave(state).saveVersion`. Root + ui typecheck now PASS because the r3n1 bridge-class tests moved to `tsconfig.bridge.json`; the Unity-side r3n1 fixtures (`evidence/Playability-Interaction-01/fixtures/r3n1-dense-02*`) are not on this host |
| Unresolved product choices | "exactly one restart" after cancel — **resolved 2026-09-16 by OPS-P13B-R07-DISPOSITION-20260916-01 §4: one resumed instance, no lifetime quota; repeated cancel/restart lawful (P13A law retained)**. Previously: (proposed, not coded; P13A repeatable-restart law retained). Plan-text reading recorded: "seating mid-project changes n from the next boundary only" = the receipt written at the next tick counts the seat; earlier receipts are immutable |

### P13B-S1b — Laboratory seats read model (bridge projection 33)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 33**; schema `sha256:9ee4bcff04e06d47fa672f6091d3f9eac98c3a19829260fd9587ab22d06d55f6` (was projection 32 `sha256:b3a76d19…` at the pin); generated TS contract sha `076893b770110d77864504d46f0503aad574b33ede35702c505338e448217c3d`; `BRIDGE_SCHEMA.$id` `urn:project-studio:bridge:protocol-4:projection-33`. Save version unchanged (V21). Engine commit: the S1b commit on this branch |
| Consumer change | Additive DTOs on `StudioLaboratoryPage`: `seats: StudioLaboratorySeat[]` (`talentId`, `name`, `assignedWeek`, `releasedWeek?`, `employed`; every seat row incl. released history, stored order), `receipts: StudioResearchReceipt[]` (last eight worked weeks ascending: `week`, `seatTalentIds`, `spend`, `units` ≥ 1 in 1/20,000 work units), `weekly: StudioResearchWeek` (`ceiling`, `usable`, `seats`, `output` — the CURRENT quote, zero when not active). Labels retained unchanged. `snapshotVersion` literal 33 on every envelope: an unpaired projection-32 client is refused by the schema, by design |
| Required UI action/feedback | Seat rows by id with employment state and history; receipt table (last eight weeks) as the visible "actual advance receipts"; numeric weekly quote beside the labels |
| C# binding change | Three new generated DTO classes + three members on `StudioLaboratoryPage` in `generated/unity/StudioBridgeDtos.Generated.cs` (`ProjectionVersion = 33`); copy/generate into the Unity client (`--unity-project` check), regenerate P11 EditMode fixtures / PlayMode embedded wire for the Laboratory page; the manifest's `unityGeneratedContractSha256` is the required-copy expectation, NOT proof of adoption |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-33 Laboratory page with 4 seats / 8 receipts; Laboratory workspace binds seat rows + receipts; PlayMode 100 %/200 %; native: seat/receipt rows readable at 1280×720 and 1440×900; a stale projection-32 client must show the refusal, not a blank page |
| Migration / integration risks | Any checkpoint/evidence generated by this engine carries `snapshotVersion` 33; Build56 (projection 31) and the paused Unity branch (32) are both unpaired; `scripts/bridge-contract-consumer-lock.ts` attests the consumer's own values at runtime (not edited) |
| Unresolved product choices | none new |

### P13B-S2 — second technology, two Laboratories, cooperation (engine Save V22; bridge projection 34)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 34**; schema `sha256:eeebeedefeaa734290081eccb2b8e3701f95df3db2d66fd33a314581b99965b2` (was projection 33 `sha256:9ee4bcff…`); generated TS/Unity contract sha `9ef5d064d6400ccdf6f30e907f545c8f5592cf68bd4aef15e0b0e071d2360b47`; `BRIDGE_SCHEMA.$id` `urn:project-studio:bridge:protocol-4:projection-34`. **Save version V22** (T4, commit `5564542`): technology root v3 with `cooperationFromWeek`; every V21 save is lifted on import (`converted: true`); `migrateToV≤21` refuse V22. Engine commits: `e68de38` (T1–T3), `5564542` (T4), the T6 commit on this branch |
| Consumer change | New DTOs `StudioResearchLabRow` (`laboratoryFacilityId`, `seatTalentIds`, `spend`, `rawUnits` over 1/20,000 — one Laboratory's part of a stored week), `StudioResearchLabShare` (same for the quoted week, `seats` instead of ids), `StudioLaboratoryProject` (`projectId`, `technologyId` ∈ {synchronized-sound, lighting-control-01}, `technologyLabel`, `status`, `homeLaboratoryFacilityId`, `laboratoryFacilityIds` ascending, `verifiedWork`, `work`, `budgetPerWeek`, `progressLabel`, `bottleneckLabel`, `estimateLabel`, `cooperationLabel`, `seats`, `receipts`, `weekly`) plus generated value holders for `technologyId`/`status`. Changed: `StudioLaboratorySeat.laboratoryFacilityId` (required; inside `projects[].seats` it can name either Lab); `StudioResearchReceipt.labs` (**nullable** array — JSON `null` means a single-pool week written before `cooperationFromWeek`, never "no Laboratories worked"; the generator initialises it to an empty array, so the binding must keep `null` distinct from `[]`); `StudioResearchWeek.units` + `.labs` (empty when idle); `StudioLaboratoryPage.projects` (every player project homed on or seated at this body, stable project-id order). **Semantics: `StudioResearchReceipt.units` and `StudioResearchWeek.units` are the project credit over 1/160,000 (was 1/20,000; a view dividing by 20,000 reads 8× high); `output` stays the float credit.** The retained top-level S1b members (`scientistId`…`weekly`) keep their synchronized-sound meaning and are superseded by `projects[]` for the binding; a body carrying only a lighting project publishes empty/zero top-level members and one `projects[]` row. Row ids: `assign-<labPlacementId>-<talentId>-<technologyId>` (intent carries `technologyId`; a brief whose research has not opened is published with "Research opens <date>; the seat waits until then." — retained P13A seat-before-research law, engine-decided enablement); `release-<labPlacementId>-<talentId>` per seat across projects; per-project `begin/resume/pause/cancel/budget-<projectId>-<amount>` with presets 0 / 2,500 / 10,000 / 40,000 / **60,000 / 80,000** (the two new presets fund a 4+2 and an 8-seat two-Laboratory project at their usable ceilings); `instruments-<labPlacementId>` unchanged; lighting's `electrical-control-instruments` module and `lighting-control-stage` are P09 placement blueprints (existing placement routes). `snapshotVersion` literal 34 refuses an unpaired projection-33 client by design |
| Required UI action/feedback | Per-project block on the Laboratory workspace: technology name, status, progress `x of 64`, the cooperation sentence (one or two Laboratories with per-Lab seats/dollars/raw output and the 0.625 trailer), per-Lab rows of the last eight receipts, assign rows grouped by person then brief, release rows, budget presets to $80,000; a second Laboratory shows the same project with the same rows; a pre-V22 single-pool receipt must render as "no per-Laboratory split recorded", not as zero Laboratories |
| C# binding change | Three new DTO classes + two value holders + members on `StudioLaboratorySeat`, `StudioResearchReceipt`, `StudioResearchWeek`, `StudioLaboratoryPage` in `generated/unity/StudioBridgeDtos.Generated.cs` (`ProjectionVersion = 34`); `StudioBridgeUnionFixtures.Generated.cs` byte-identical; copy/generate into the Unity client (`--unity-project` check), regenerate P11 EditMode fixtures / PlayMode embedded wire for the Laboratory page; the manifest's `unityGeneratedContractSha256` is the required-copy expectation, NOT proof of adoption |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-34 Laboratory page with two `projects[]` rows (sound across two Labs with two-row receipts; lighting on one Lab), a receipt with `labs: null`, and the six budget presets; Laboratory workspace binds `projects[]` (not the superseded top-level members); PlayMode 100 %/200 %; native: two-Lab cooperation sentence and per-Lab rows readable at 1280×720 and 1440×900; a stale projection-33 client must show the refusal, not a blank page; a V21 save loaded through the client shows the lift (`converted`) and 8× rebased `units` without a visible jump in `verifiedWork` |
| Migration / integration risks | Every checkpoint/evidence generated by this engine is **V22** with `snapshotVersion` 34; Build56 (projection 31) and the paused Unity branch (32) are unpaired; V21 saves written by `e68de38`–`e24c860` with seats on two Laboratories lift with `labs: null` on those receipts (honest single-pool history; validated only before `cooperationFromWeek`); the `units` base change is silent on the wire (same field name) — the projection bump is the only guard; `scripts/bridge-contract-consumer-lock.ts` attests the consumer's own values at runtime (not edited, `CURRENT_ACCEPTED_SAVE_VERSION` still 19) |
| Unresolved product choices | none new. Companion §4 tuning stays provisional (0.625 second-Lab factor, $10k/seat, 4 seats/Lab, budget presets); the production consumer of R07 is **no longer design blocked**: the standard-stage Ballroom recipe (`ballroom-reveal-lighting-01`, 4/2/1 setup units) is adopted by OPS-P13B-R07-DISPOSITION-20260916-01 as the plan's S5-R07 consumer task (after S5's real deployment chain); CAT-011 oversized Sets, the K4 large-stage body and companion A12 stay OPEN separately; money scale OPEN-5 remains the Owner's. **S2 correction owed before its label:** research-completion access identity must be the exact `(studioId, technologyId)` pair (disposition §5; S2-T8) |

### P13B-S3 — persistent physical plans, dependencies and admission (engine Save V23; bridge projection 35)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 35**; schema `sha256:889c83c6278aaf54fc392282bb555ce03f61f8381e20cc62618e0106cf6f1da3` (was projection 34 `sha256:eeebeede…`); generated TS/Unity contract sha `4ea3b9d0b23ebc5d01ad0711d01a1149dba3a3f53b514ee6c6e29d1acdfd0b5d`; `$id` `urn:project-studio:bridge:protocol-4:projection-35`. **Save V23** (engine `30836b0`): root `physicalPlans {version 1, nextPlanId, plans[]}`; every V22 save lifts on import with an empty root; `migrateToV≤22` refuse V23. Bridge commit `c18abe8` |
| Consumer change | New view `plans` on the industry request AND response; `StudioIndustryResponse.plans` (nullable `StudioPlansPage`, null on other views). New DTOs `StudioPlansPage {title, notice, rows, actions (reuses StudioLaboratoryAction)}`, `StudioPlanRow` (planId, ordinal, status, statusLabel, reason, queuedWeek, statusWeek, workKind, blueprintId, workLabel, targetFacilityId, targetPlanId, dependsOn, approvedMaximumDebit, earliestStartWeek, admission, approvedQuote, pendingQuote, next, startedPlacementId, commitReceipt), `StudioPlanQuote`, `StudioPlanQuoteComponent`, `StudioPlanNext {outcome, reason}`, `StudioPlanCommitReceipt`; value holders for status/admission/workKind/outcome; history kinds `planQueued|planStarted|planHeld|planBlocked|planCancelled` ("Plan queued/started/held/blocked/cancelled", detail `Week n · plan <planId>. <reason>`, no Locate target); intent kind `physicalPlanAction`. Row ids: `plan-cancel|review|admission-<mode>|move-up|move-down-<planId>` on the plans page; `plan-queue-acoustic|electrical-<labPlacementId>` beside the unchanged `instruments-<labPlacementId>` on the Laboratory page (hidden once that module has a committed placement on the Lab in any status or a queued/held/started plan already targets it). `planId` = `<studioId>:plan:<n>` (contains colons; never split ids on `-` or `:`). **Binding hazards:** `next` is null exactly on `started`/`cancelled` rows (no next boundary; null must not render as "waiting"); `pendingQuote` only while `held`; `commitReceipt`/`startedPlacementId` only once started; `dependsOn` empty = no dependency (never null); `approvedMaximumDebit` is a ceiling, not a reservation — a queued plan holds no cash, burn, capacity or engagement. `snapshotVersion` literal 35 refuses an unpaired projection-34 client by design |
| Required UI action/feedback | Ordered plans list with status, reason, next-boundary outcome, dependencies and ordinal; "Review changed plan" row with approved vs pending quote side by side (cost, build weeks, weekly operating cost, differing components); move up/down; cancel; admission-mode toggle; the two Laboratory queue companions with the quoted cost and "nothing reserved until the plan starts"; history rows for the five plan events. Body-placement queueing needs the lot/placement quote surface (not delivered; future work with P09's placement route) |
| C# binding change | Six new DTO classes + four value holders + members on `StudioIndustryRequest`/`StudioIndustryResponse` view values, `StudioHistoryEventSnapshot` kind values and `StudioBridgeIntentOption` kind values in `generated/unity/StudioBridgeDtos.Generated.cs` (`ProjectionVersion = 35`); union fixtures regenerated; copy/generate into the Unity client (`--unity-project` check), regenerate P11 EditMode fixtures / PlayMode embedded wire; the manifest's `unityGeneratedContractSha256` is the required-copy expectation, NOT proof of adoption |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-35 plans page with queued/held/blocked/started/cancelled rows (null `next` on terminal rows, `pendingQuote` on the held row), a history page with the five plan kinds, a Laboratory page with the two companions; workspace binds the plans list and the review row; PlayMode 100 %/200 %; native: plans list and review row readable at 1280×720 and 1440×900; a stale projection-34 client shows the refusal; a V22 save loaded through the client shows the lift (`converted`) with an empty plans list and no invented plan history |
| Migration / integration risks | Every checkpoint/evidence generated by this engine is **V23** with `snapshotVersion` 35; Build56 (projection 31) and the paused Unity branch (32) are unpaired; admission is player-only until S8 supplies rival physical-commit authority (rival plan rows validate but never admit); campaign isolation is enforced at the plan identity (no per-studio facility root exists to check a target against — recorded limitation); `scripts/bridge-contract-consumer-lock.ts` attests the consumer's own values at runtime (not edited) |
| Unresolved product choices | none new. Delegated: default admission `reviewChangedQuote`; held plans re-evaluated every boundary and start without review only when the live quote again equals the approved quote; ceiling of a bridge-queued companion = the quoted price; quote fingerprint excludes the completion week |

### P13B-S4 — direct gap-aware Office conversion (engine Save V23 unchanged; bridge projection 36)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 36**; schema `sha256:c2247716204c53ecab8f23d0aa0fae64676fada87d76024487d5f69c56ffe74c` (was projection 35 `sha256:889c83c6…`); generated TS/Unity contract sha `e0d44f46c7a02ab8b236b058b5f7943702632f62d80f1afb443e6cbd428b3072`; `$id` `urn:project-studio:bridge:protocol-4:projection-36`. Save version **V23 unchanged** (no new persisted fact: a conversion is an ordinary P09 installation placement; the development standard and offline state are derived). Engine commits `dba78d0` + `c2d27d1`; bridge `8334a3f` |
| Consumer change | New view `office` on the industry request AND response, `targetId` = the development body's FACILITY id (the endowed founding office has no placement; its id comes from the operations registry); `StudioIndustryResponse.office` (nullable `StudioOfficePage`, null on other views). New DTOs `StudioOfficePage {facilityId, title, blueprintId (nullable — null for the endowed founding office), standard, highestOperationalStandard, offline, offlineUntilWeek (nullable), capacity (live registry number, 0 while converting), baselineWeeklyOperatingCost (0 for the endowed office), conversions[2], planIds, actions (reuses StudioLaboratoryAction)}` and `StudioOfficeConversionRow {blueprintId ∈ office-conversion-ii|iii, label, cost, buildWeeks, weeklyOperatingCost, fromStandard, toStandard, standardDuringWork, standardAfter, downtimeWeeks, available, refusal (nullable, the engine's PRIMARY rejection; a completed record's own row yields to standardAlreadyMet), refusalText (nullable), rejections (the engine's full list)}`; value holders for standards (I/II/III) and blueprint ids; intent kind `installationAction` (new; immediate P09 installation commit). Row ids `office-convert-<facilityId>-ii|iii` and `plan-queue-office-convert-<facilityId>-ii|iii` (facility ids contain hyphens — match the published id, never split); companions hidden under the reachable-standard rule (own blueprint committed/planned, or target standard ≤ current/committed/planned standard). Quotes: I→II $500,000/4 w (+$2,500), I→III $1,250,000/16 w, II→III $850,000/8 w (+$4,000) — O1 candidate tuning, not Owner-approved. **Binding hazards:** both conversion rows are always published (show `refusalText`, never hide); `blueprintId`/`offlineUntilWeek` are genuinely null on the endowed office / an open body; `capacity` is live, not a blueprint constant; `standardDuringWork` discloses the ladder drop while the sole high-standard body is offline; an office intent with a stale revision is refused STALE_REVISION |
| Required UI action/feedback | Office workspace: current standard and the studio's highest operational standard, offline banner with the reopening week, the two conversion rows with cost, downtime, standard during and after, and the refusal sentence when unavailable; immediate convert and plan-queue decisions; plan ids linking to the plans list; the history row for a completed conversion is the existing facility-completed kind |
| C# binding change | Two new DTO classes + value holders, `StudioIndustryResponse.office`, `Office` on both view value sets, `InstallationAction` intent kind in `generated/unity/StudioBridgeDtos.Generated.cs` (`ProjectionVersion = 36`); union fixtures regenerated; copy/generate into the Unity client (`--unity-project` check), regenerate P11 EditMode fixtures / PlayMode embedded wire; the manifest's `unityGeneratedContractSha256` is the required-copy expectation, NOT proof of adoption |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-36 office page for the endowed office (null blueprintId), a placed office mid-conversion (offline, capacity 0, both rows refused) and a completed III; workspace binds the rows and both decision kinds; PlayMode 100 %/200 %; native: office page readable at 1280×720 and 1440×900; a stale projection-35 client shows the refusal |
| Migration / integration risks | Every checkpoint this engine writes is V23 with `snapshotVersion` 36; Build56 (31) and the paused Unity branch (32) unpaired; the `conversionStandards` fallback for an effect-only standalone II/III body (no registry row) is unproven by tests (disclosed via `available:false` + refusal); rivals have no P09 lot, so no rival conversion exists until S8; `scripts/bridge-contract-consumer-lock.ts` unchanged |
| Unresolved product choices | **ENG-2** (new Office III without owning II) and the O1 durations 4/8/16 stay undisposed — the standalone-III route on a never-owned-II studio is not offered; document 03's $1,646,000 for that route presumes ENG-2 (the lawful engine total is $1,776,000). A true in-place capacity extension is unavailable and not offered |

### P13B-S5 — component inventor pricing and prototypes per technology (engine Save V24; bridge projection 37)

| Item | Record |
|---|---|
| Producer version | Engine **Save V24** (`2d41e85`): technology root v4 with `equipment: TechnologyEquipmentAsset[]` and `nextEquipmentId`; every adoption carries `components: TechnologyAdoptionComponent[]`, `equipmentAssetId`, `postFacilityId: string \| null`. Bridge **protocol 4 / projection 37** (`aba6761`): schema `sha256:2158ef1606497912f8589b5ae6496e55d4d73a2ee687e4b1867837c593af7aa8` (was projection 36 `sha256:c2247716…`); union-fixture identity `dc94ab3b…`; generated TS/Unity DTOs regenerated and contract-checked. |
| Consumer change | Laboratory page: per-technology adopt rows `adopt-<technologyId>-<stageFacilityId>[-<postFacilityId>]` with the new `adoptTechnology` intent kind (the sound rows keep their P13A meaning; lighting rows have no Post), a `StudioAdoptionQuote` per row (component list kind/label/cost/source/weeks, total, `reusedPostFacilityId`, `reusedEquipmentAssetId`, engine `rejections` + primary `refusal`), `plan-queue-adopt-*` companions (queue the stage fit-out through `physicalPlanAction`; never mint the adoption), and `adoptions: StudioAdoptionRow[]` (technology, route, committedWeek, operationalWeek, components, equipmentAssetId, postFacilityId nullable). |
| Required UI action/feedback | Adoption workspace per technology: the component quote as an itemised bill (access existing/0, equipment first-prototype 0 / later-inventor / commercial / reused asset 0, site, installation, capture, Post or "existing Post reused"), the total, the refusal text when not actionable, the queue companion beside the immediate commit; adoption progress to operational (lighting +4 weeks, sound stage + Post); equipment assets held by an adoption listed with their source and cost. |
| C# binding change | New DTOs `StudioAdoptionQuote`, `StudioAdoptionComponent`, `StudioAdoptionRow` (in `industryDefinitions`; no separate asset DTO — assets appear through `equipmentAssetId` and the components); nullable `Quote` on `StudioLaboratoryAction` (null on plans/office rows); `Adoptions` on `StudioLaboratoryPage`; `AdoptTechnology` intent kind — published for BOTH sound and lighting adopt rows (sound rows no longer carry `researchAction`; a client filtering `Install sound:` rows by kind must move to `adoptTechnology`); nullable `postFacilityId` on adoption rows (a lighting row publishes null — client bindings that assumed a Post must accept null); generated `generated/unity/*` regenerated at T4. |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-37 Laboratory page with one sound and one lighting adoption on different stages, a lighting row with null Post, a quote with `reusedPostFacilityId`, a refused row with `rejections`; PlayMode: commit an adopt row and see the itemised bill and the operational week; never marked passed by engine evidence. |
| Migration / integration risks | Every checkpoint this engine writes is **V24**; V23 clients cannot load it (eleven downgrade guards refuse with "cannot downgrade SaveFileV24 or discard equipment assets"); Build56 (31) and the paused Unity branch (32) remain unpaired. A client that keys adoptions by `stageFacilityId` alone breaks once a stage carries a lighting AND a sound adoption — key by adoption id. Equipment assets are never deleted; S6 will detach holders on cancellation, so a "held by" field can become null later. |
| Unresolved product choices | None new. The inventor access row at $0 and the `existing` source on every access component are delegated implementation decisions (recorded in the plan), not Owner choices; A12/K4/CAT-011 stay OPEN under S5-R07. |

### P13B S5-R07 — Ballroom-reveal lighting consumer (engine Save V25; bridge projection 38 — closed `06f05cf`)

| Item | Record |
|---|---|
| Producer version | Engine **Save V25** (`90e8307`): `ProductionWorkflow.setup: ProductionSetupRecord | null` + `planRevision` (validated then stripped to the frozen V24 shape; both operations roots; 12 downgrade guards; sentinel 26); `src/core/productionSetup.ts` (`SETUP_RECIPES` = `ballroom-reveal-lighting-01` 4/2 units, `ordinary-interior-01` 1/1; `setupForecast = admittedWeek + requiredUnits`); action `setProductionSetupRecipe {productionId, recipeId, expectedPlanRevision}`; history kinds `setupAdmitted`, `setupUnitCredited`, `setupCompleted`, `setupRebound`. Bridge **protocol 4 / projection 38** (`06f05cf`): schema `sha256:baecd21e5fbfd4f13bd08b7af132d4b05fe24a40f1b38cd19d2782d9e2d0851e` (was projection 37 `sha256:2158ef16…`); union-fixture identity `f811b325…`; generated TS/Unity DTOs regenerated and contract-checked; the manifest names projection 38 for the Unity spike — a Unity-side compile of `StudioBridgeDtos.Generated.cs` at this id is owed by the client integrator, never claimed here. |
| Consumer change | `productionOperations` rows gain `setup: StudioProductionSetup | null` {recipeId, recipeLabel, route, creditedUnits, requiredUnits, admittedWeek, nextUnitWeek, forecastShootingEntryWeek, completedWeek, adoptionId, stageFacilityId, setId, planRevision} and `setupRecipeActions[]` (`setup-recipe-<productionId>-<recipeId>` rows, intent kind `productionSetupAction`, engine `rejections` + primary `refusal`, `enabled` agreeing); a held rehearsal publishes phase `rehearsal` with a setup-wait status label and `weeksRemaining` = forecast − current week; the four setup history rows appear on `operationsEvents`; `converted` on load now compares against the live save version (was always true since V24). |
| Required UI action/feedback | Recipe review → setup progress (units, wait vs worked time) → Shooting entry → scheduled take → result; readable waits; the two-stage usefulness (4 vs 2 units) visible against the bound stage's operational lighting |
| C# binding change | New DTOs `StudioProductionSetup` and `StudioSetupRecipeAction`; optional-nullable `Setup` and optional `SetupRecipeActions` on `StudioProductionOperationsSnapshot` (every served row carries both; clients must accept absence on raw lot snapshots); `ProductionSetupAction` intent kind; four operations-event kinds (`setupAdmitted`, `setupUnitCredited`, `setupCompleted`, `setupRebound`) with name-resolved sentences; `converted` on load now compares against the live save version. OPEN for the client/ops owner: `executeLoad`'s "loaded and migrated" notice is unreachable because `load()` accepts only canonical current saves. |
| Future compile/render/native tests | The complete recipe-review → setup → Shooting → take → result journey, client bindings, portraits/art and the R3 visual acceptance of it — never marked passed by engine tests |
| Migration / integration risks | Existing saves receive no invented setup history or delay; legacy productions keep their schedule; standard classification on legacy bodies without setup history |
| Unresolved product choices | CAT-011 oversized Sets, K4 large-stage body/footprint/pricing, companion A12 refusal/acceptance (OPEN, own owners); 4/2/1 units and the one-unit-per-week clock are candidate tuning |

### P13B-S6 — Option-B installation cancellation, receipts, restoration (engine Save V26; bridge projection 39)

| Item | Record |
|---|---|
| Producer version | Engine **Save V26** (`055a907`, adjudications `941ceba`, sweep `00e8657`, validator `a123562`): `PlacementStatus` gains `'cancelled'`, placements carry `cancellation: CancellationReceipt \| null` ({projectId, week, components[{label, cost, weeks, status completed\|inProgress\|unstarted, paid, refunded}], refund, restorationProjectId}), adoptions carry `cancelledWeek`, ledger kind `constructionRefund` (positive; finance category 'Cancelled installation capital returned'), `RESTORATION_BLUEPRINTS` (`restoration-sound` 25,000/2 w, `restoration-lighting` 10,000/1 w, `restoration-office` CANDIDATE 25,000/1 w; `takesTargetOffline`; never catalogued); actions `cancelInstallation {projectId}` / `cancelAdoption {adoptionId}`; `LIVE_SAVE_VERSION = 26`; both leaves validated then stripped for V12–V25 readers. Bridge **protocol 4 / projection 39** (`25298f5`), schema `sha256:bdd3e666c84dce94c8ae65415fc5878e61c4616574c1a57d1f2951cb793d2213`, C# contract `sha256:fa34e626…`. |
| Consumer change | Action rows `cancel-adoption-<adoptionId>` (Laboratory page) and `cancel-<projectId>` (Office page for a running conversion; plans page for a started plan's own placement) with intent kind `cancellationAction`; the row's `quote` is the engine's `cancellationQuote` verbatim: components with derived progress/paid/refunded, `refund`, `restoration {blueprintId, cost, weeks} \| null`, engine `rejections` + primary `refusal`, `enabled` agreeing; rows absent for operational, cancelled and restoration placements; after a commit the Office page publishes the restoration `offline`/`offlineUntilWeek` through the existing members and a restart is quoted anew only after it completes. `StudioAdoptionRow.cancelledWeek`; `StudioLaboratoryPage.equipment[]` (`StudioEquipmentAsset` {id, technologyId, holderAdoptionId, acquiredWeek, cost, source}) discloses an unheld asset, and the next `adopt-*` row for that technology quotes the equipment at 0 with `reusedEquipmentAssetId`. `StudioFinanceFacility.status` now admits `'cancelled'`; the finance page carries the refund category line in the period of the refund week. The restoration's completion is an existing `constructionCompleted` operations event with a "back in service" sentence; **no cancellation-act history row exists** (OPEN: a `facilityCancelled` history kind beside `facilityDemolished` needs the frozen history validators widened — owner/companion input). |
| Required UI action/feedback | Cancel review (per-component completed/in-progress/unstarted with paid vs refunded, restoration cost/weeks) → confirm → refund receipt → body offline during the restoration → restart offer at full price; a cancelled adoption's row stays visible as history with its retained equipment asset shown as reusable |
| C# binding change | **Wire rename:** projection 37's `StudioAdoptionQuote` is replaced by one closed `StudioActionQuote` on `adopt-*` and `cancel-*` rows (required `components`, `rejections`, `refusal`; adopt-only `total`, `reusedPostFacilityId`, `reusedEquipmentAssetId`; cancel-only `refund`, `restoration`; each optional per verb, never defaulted — the generator refuses an undiscriminated object union, CF08). `StudioActionQuoteComponent` merges both verbs' component lines (`kind` nullable: `null` for an authored label the S5 classifier does not map, today the office conversion's single component; cancel-only `status`, `paid`, `refunded`; adopt-only `source`, `placementId`, `equipmentAssetId`). New DTOs `StudioCancellationRestoration`, `StudioEquipmentAsset`; nullable `CancelledWeek` on `StudioAdoptionRow`; `Equipment` list on `StudioLaboratoryPage`; `CancellationAction` intent kind; the three-value `Status` on `StudioFinanceFacility` (lot-view statuses stay two-value: bodies only). |
| Future compile/render/native tests | The cancel review → refund → restoration → restart journey, the finance line, the reusable-asset disclosure, client bindings and the R3 visual acceptance of them — never marked passed by engine tests |
| Migration / integration risks | Legacy saves publish `cancellation: null`-derived rows unchanged and no refund line; a V26 save read by a V25 reader refuses (downgrade guard); a client still binding `StudioAdoptionQuote` fails to deserialize projection 39 rows |
| Unresolved product choices | `restoration-office` 25,000/1 w is a CANDIDATE (5 % of the conversion, the sound ratio); the cancellation-act history row (above); the plan's S6 acceptance wording is drafted from its paraphrase of the companion `02-P13B-DECISIONS-AND-ACCEPTANCE.md`, which must be re-supplied |

### P13B-S7 — Forecast/replacement disclosure (engine Save V26 unchanged; bridge projection 40)

| Item | Record |
|---|---|
| Producer version | Engine unchanged at **Save V26** (`18ab734`: catalogue data only — `publicWindow { from, to, announceWeek }` (lighting 884..988 announced 884; sound degenerate 416) and `replacementLabel` per entry; pure `src/core/technologyDisclosure.ts` — `technologyForecast(entry, week)`, `technologyAnnouncements(week)`, `replacementDescriptor(entry)` — no `GameState` input, no receipt, no persisted announcement). Bridge **protocol 4 / projection 40** (`6622062`), schema `sha256:3283d0fd2117eeb2935711bbc633eb9659c1b228bc9db9ecdf5161b7b196e791`, C# contract `sha256:21f097ea…`. |
| Consumer change | Laboratory page `forecast: StudioTechnologyForecast[]` per catalogue technology {technologyId, name, kind `window` \| `exact`, windowFromLabel, windowToLabel, exactLabel, announcedWeek, basis}: a windowed technology publishes only its public window (lighting 884..988 before week 884; the exact commercial date appears nowhere on the page), the exact date and announce week from the announce week on; sound is exact at every week. `wait-<technologyId>` / `purchase-<technologyId>` rows per catalogue entry, present only while the forecast is exact (sound's rows unchanged; lighting's from 884), `enabled`/`disabledReason` from the engine dry run (`commercialAccessRefusal` until the commercial week; `This studio already has <id> access.` once held). `replacementLabel` on purchase/adopt rows and on `adoptions[]` rows, verbatim catalogue text. Industry page: one derived `announcements` activity per announced technology (`technology-announcement-<technologyId>`, week = announce week, `studioId: null`, standing — not retired by the 13-week pulse window; absent from every per-studio History view; identical in Save As worlds and after any load, since it is derived). |
| Required UI action/feedback | Forecast card per technology (window vs exact, announced date), the announcement in the industry feed, replacement sentence on the purchase/adopt review and on the adoption record; the lighting purchase/wait affordances appear only after the announcement |
| C# binding change | New DTO `StudioTechnologyForecast` (+ `StudioTechnologyForecastKindValues`), `Forecast` list on `StudioLaboratoryPage`; optional `ReplacementLabel` on `StudioLaboratoryAction`, required on `StudioAdoptionRow`; **`StudioIndustryActivity.StudioId` becomes nullable** (`Required.AllowNull`; receipt-derived rows keep their ids); intent kinds unchanged (`purchaseTechnology`/`waitForTechnology` already generic). The Unity-side contract copy is not in this worktree. |
| Future compile/render/native tests | Forecast/announcement rendering, the replacement sentence on review screens, the lighting purchase journey at 936, client bindings and the R3 visual acceptance — never marked passed by engine tests |
| Migration / integration risks | None on the save (V26 unchanged); a client binding `StudioId` as non-null fails on the derived announcement row; a client that hid purchase rows by absence must now read `enabled` |
| Unresolved product choices | The 884..988 window and 884 announce week (CANDIDATE, ±52 weeks around the commercial week; no corroborating transcription), both replacement sentences, the generalised purchase/wait/announcement wording (all companion text owed); the engine's own `commercialAccessRefusal` sentence still names the exact week to any direct client before the announcement (latent, no bridge row reaches it — OPEN minor); `validateTechnologyCatalogue` does not validate `publicWindow` (OPEN for a sweep) |

### P13B-S8 — Symmetric rival research and finance (engine Save V27; bridge projection 41)

| Item | Record |
|---|---|
| Producer version | Engine **Save V27** (`c609e0a`; live path `742fb1e`): `RivalMoneyKind` 10 → 14 (`researchSpend`, `researchCapacity`, `technologyRestoration`, `technologyRefund`; every rival finance period carries the four keys, lifted at 0); `IndustryReceipt` gains `laboratoryCommitted {planId, facilityId}`, `laboratoryOperational {facilityId}`, `instrumentOperational {facilityId, technologyId}`, `researchSeatAssigned {projectId, talentId}`, `researchCompleted {projectId}` (rival-only emitters); rival Laboratories are abstract `StudioOperations` facilities (`capability 'laboratory'`, capacity 4) appended on plan completion — no placement; the research law generalised per studio through a `StudioContext` (`src/core/technology.ts`), `src/core/rivalResearch.ts` (`RIVAL_RESEARCH_POLICY` CANDIDATE, `admitRivalPlans`, `advanceRivalResearchWeek`), `rivalCapacityOpex` pricing Laboratories/instruments, `deriveSetupProvenance(…, studioId)`, `considerRivalSoundPurchase` per technology and route (one inventor per technology, CANDIDATE); `projectHollywoodPreV27` refuses a V26 downgrade once any rival research fact exists; `LIVE_SAVE_VERSION = 27`. Bridge **protocol 4 / projection 41** (`87aac7d`), schema `sha256:16b8432222d8cccf08be3fa056db4b4c975f39648ac67cd57d103a4c74663365`, C# contract `sha256:be1f42ea…`; NO DTO change. |
| Consumer change | Industry page: one `studios` activity per rival `laboratoryOperational` receipt (`<studio> expands its research capacity`; detail states that seats, instruments, projects and spending stay private — no counts); the four other rival receipt kinds produce no row and no text; `technologyAdopted` rows and the S7 announcement row unchanged; rival finance only through the public standings (the four money kinds, rival cash and periods appear on no page). Player pages unchanged by rival research (no rival project/seat on the Laboratory page; the player's `researchSpend` finance channel unaffected). Save As and load: a V27 save round-trips; a V26 save loads converted with unchanged pages; a V27 save carrying rival research cannot be downgraded. |
| Required UI action/feedback | The research-capacity line in the industry feed; no new controls; rival research remains invisible by law |
| C# binding change | None in the DTOs (schema id and manifest move only). Receipt-kind consumers that switch on kind must tolerate the five new kinds (they never reach the wire as activities except `laboratoryOperational`). The live save version a client must write/expect is 27; a client still binding `SaveFileV26` on the checkpoint path downgrades and fails once rival research exists. |
| Future compile/render/native tests | The industry feed line, the V27 load path in the native client, and the R3 visual acceptance — never marked passed by engine tests |
| Migration / integration risks | A V27 save with rival research facts refuses every frozen reader V19–V26 (`cannot downgrade … discard rival research`); legacy zero-Opex rivals are grandfathered (no back-charges); the rival `researchSpend` money kind shares its literal with the player's pre-existing `MoneyKind` (different roots; consumers must not conflate) |
| Unresolved product choices | Every policy number (`RIVAL_RESEARCH_POLICY` seats/budget/interest timing at the bounds' maxima), the one-inventor rule and which rival innovates (today row order), cash-only Lab admission from week 0, the rival cancellation policy (`technologyRestoration`/`technologyRefund` validated at 0), the movement kinds' player-facing labels, the S8 acceptance wording — companion text owed |

### P14A.1 — One Contested Expiry Core: the contested market case on the Profile (engine Save V28; bridge projection 42)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 42**; schema `sha256:a3f8b03c00eee8e7c18b7f7c5d1f56c884f96d6173f5ab59ed7396911f56e981` (was projection 41 `sha256:16b84322…`); `$id` `urn:project-studio:bridge:protocol-4:projection-42`; manifest projectionVersion 42. Save version **V28** (`talentMarket` root `{cases, proposals, receipts, representation: null, legacyTerminations}` at the top level; `projectTalentMarketPreV28` strips it before the frozen V27 chain; 13 downgrade guards; sentinel 29; the lift never fabricates a case — a subject already inside its window at load is discovered on the next tick; every pre-V28 player termination is recorded as `legacyTerminations {contractId, endedWeek, amountPaid}` from the ledger's actual charge, no back-charge). Engine `562cdf2` (bridge) over `daaf95f` (engine); LOGIC VERIFIED · UNITY NOT VERIFIED label pending the T4 matched pass. |
| Consumer change | `StudioPersonProfileSnapshot.marketCase` (nullable) → `StudioMarketCaseSnapshot {talentId, subjectStudioId, status (7 values: discovered · proposals_open · decision_pending · settled · declined · expired · invalidated), decisionWeek, decisionWeekLabel (derived on read from the P12 row's `endWeekExclusive`, never a stored copy), preferences: StudioMarketPreferencesSnapshot {priorityOrder[compensation|term|standing|incumbency], preferredTermWeeks, line}, proposals[], attentionRows[], settlementReasons[]}`. Proposal rows are a discriminated union on `disclosure`: `StudioMarketOwnProposalSnapshot` (premiumTier, annualSalary, signingBonus — RE-DERIVED at the read week through the shared pricing entry, never the submission-week quote) and `StudioMarketUndisclosedProposalSnapshot` (term length and effective week public; premiumTier/annualSalary/signingBonus are the literal string `"UNKNOWN"`, never null, never omitted). New intent kind `marketProposalAction` through the accepted quote pattern: `StudioQuoteMarketProposalRequest {type:"quoteMarketProposal", draft:{verb: propose|revise|withdraw, talentId, termWeeks?, premiumTier?}}` (no issuer on the wire — the session always issues as the player) → `StudioMarketProposalQuoteSnapshot` (kind `marketProposalAction`) in `StudioQuoteSnapshot`; commit advances `stateRevision`, `STALE_REVISION` on the old revision. The renew row for a market-case subject reads `renewAvailable:false` with the new refusal `underMarketCase` (in-window `renewContract` is repealed: the incumbent renews by proposing for the decision week). Attention rows on the profile route for exactly five causes (closed enum): `decisionWeekNear` (stop horizon 1 week — an Owner call, see below) · `newCompetingProposal` · `termsRevised` · `settlementCompleted` · `proposalWouldFail`, each `{cause, talentId, reason}`. Settlement on the wire: the case reads its terminal status with the receipt's order-only reasons (no amounts) and the winner's employer/contract; a terminal receipt also carries `dropped[]` (one sentence per dropped proposal — CANDIDATE wording). Disclosure narrowing unchanged for everyone outside a case (`bridge/industry.ts` "Contract terms are kept private in Industry."; `bridge/people.ts` `contract: null` for rival-employed people). No Pulse fold of the two receipts (A.2), no workspace (A.2), no world route (A.3). ADDENDUM 2026-09-18 (P14A.1-F1 `a36449f`, values only, projection 44 unchanged): for a capable-but-unproven person `StudioMarketPreferencesSnapshot.priorityOrder` now reads `['compensation','term','standing','incumbency']` (was term-first, a transposition against companion §2.1.7) and the derived "Weighs … first, then …" line names compensation first; `preferredTermWeeks` unchanged for both archetypes; no field, type or enum moved. |
| Required UI action/feedback | Profile → Contract: the case block while a case is open (status, decision-week label, the person's public preference line, one row per proposal: the player's own with tier/terms/price, competing rows with term and effective week and an explicit "not disclosed" rendering of the UNKNOWN marker — never a blank or a zero); propose / revise / withdraw through the quote → confirm → commit flow with the engine's enabled/refusal sentence; the renew row disabled with the `underMarketCase` sentence and a pointer to the proposal; the five attention rows; after the decision week the settled/declined/expired/invalidated outcome with its order-only reasons and, when the player's own proposal was dropped, the drop sentence. |
| C# binding change | New DTO classes `StudioMarketCaseSnapshot`, `StudioMarketPreferencesSnapshot`, `StudioMarketProposalSnapshot` (abstract) with `StudioMarketOwnProposalSnapshot` / `StudioMarketUndisclosedProposalSnapshot`, the generated `StudioMarketProposalSnapshotJsonConverter` (registered), `StudioMarketProposalDraftPayload`, `StudioQuoteMarketProposalRequest`, `StudioMarketProposalQuoteSnapshot`; `marketCase` on `StudioPersonProfileSnapshot`; `MarketProposalAction` intent kind; `underMarketCase` in the contract refusal vocabulary; the attention-cause enum; `ProjectionVersion = 42` in `generated/unity/StudioBridgeDtos.Generated.cs`; union fixtures regenerated (`StudioBridgeUnionFixtures.Generated.cs`); copy/generate into the Unity client (`--unity-project` check), regenerate P11 EditMode fixtures / PlayMode embedded wire; the manifest. |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-42 profile with an open case (own row + one undisclosed row carrying the three `"UNKNOWN"` strings), a settled case, and a profile without a case (`marketCase` null); the quote/commit/stale sequence for `quoteMarketProposal`; the renew row's `underMarketCase` refusal. PlayMode 100 %/200 %: the case block and the five attention rows readable at 1280×720 and 1440×900; a stale projection-41 client shows the refusal. Native: nothing verified here — Unity work is deferred by the Owner directive. |
| Migration / integration risks | Every checkpoint/evidence this engine writes is **V28** with `snapshotVersion` 42; Build56 (projection 31) and the paused Unity branch (32) are unpaired. Save V28 was widened IN PLACE twice inside this slice (`legacyTerminations` on the root at `ad35a81`; `dropped[]` on every case-terminal receipt at `daaf95f`) — no V28 fixture existed before either widening, and the genuine V27 fixtures (`legacy-v27-natural-rival-labs-20`, `legacy-v27-renewal-window-456`) load converted with an empty market. A V27 envelope is also the downgrade projection of a live V28 campaign, so the frozen V≤27 chain accepts a termination priced under EITHER the historical 50% law or the 26-week-cap law (any third number is refused). The market engages iff `hollywood !== null`. A rival's seat is free only between a lost own case and the next weekly deficit fill, so rival challenges almost always seat-drop at a synchronized expiry (see the plan's OPEN consequence). The people/industry JSON never carries a competing proposal's figures; at premium tier 1.00 a rival's offer equals the public ask, which is published only where the same number is the player's own contract. |
| Unresolved product choices | Premium tier VALUES (1.00 floor, monotonicity and the below-1.00 refusal are pinned; the ladder is a hypothesis); the rival trigger numbers and the two-bit rival tier policy (incumbent bids lowest; `reserveWeeks ≥ 16`), which produce exact challenger ties and decline-all on some seeds; own-person priority in the fixed settlement order; the stop horizon for `decisionWeekNear` (applied 1 week; alternative 12 weeks recorded, not applied); a seat reservation for a pending challenge or the §3.6 rival release path; R2 (release refused for the busy set / founding) DEFERRED; representation (R10) null; the `perceived` genre-experience field can carry IEEE `-0` in memory, which any JSON round trip flattens (pre-existing, unrelated). The founding rivals' insolvency on the p13a seed is inherited from the S8 close (a P13 finance balance fact, recorded for the Owner). ADDENDUM 2026-09-18 (P14A.3-T1 finding, evidence `p14a3-20260918/01`): under the landed seat law a founding rival can never poach a player's employee — every founding roster holds exactly `RIVAL_TEAM_ROLES` with zero slack, and at the churn each rival's own incumbent retention settles earlier in the same weekly pass and refills its seat before the player's case is evaluated; rival → player contests are therefore impossible at founding roster sizes (roster slack, the intra-week settlement order, or a seat budget above the roster are Owner-side choices; nothing changed here). |

### P14A.2 — Talent Market workspace read models (engine Save V28 unchanged; bridge projection 43 — landed `cea19cf`/`e210513`; CLOSED LOGIC VERIFIED · UNITY NOT VERIFIED on the `a0611b8` matched pass, attribution `evidence/p14a2-20260918/13`; not Owner acceptance)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 43**; schema `sha256:95351b9d1af70fd7d890dc6d8dc75ac40430d059f87fec526a42707b1f7bb1c7` (was projection 42 `sha256:a3f8b03c…`); `$id` `urn:project-studio:bridge:protocol-4:projection-43`; `LIVE_SAVE_VERSION` 28 unchanged. Every checkpoint/evidence this engine writes carries `snapshotVersion` 43. |
| Consumer change | Industry route: request `view` enum gains `'market'`; new OPTIONAL request field `historyPage` (every pre-43 request stays valid); `targetId`/`page` reused. Response `view` enum gains `'market'`; new REQUIRED nullable `market: StudioMarketPage | null` (populated only on `view: 'market'`). `StudioMarketPage {attention: StudioMarketAttentionRowSnapshot[], cases: StudioMarketCases, selected?: StudioMarketCaseDetail}`; `StudioMarketCases {renewalWindow: StudioMarketCaseRow[], freeAgents: StudioMarketFreeAgentRow[], settling: StudioMarketCaseRow[], closed: StudioMarketClosedCases {rows, page, pageSize, total}}`; `StudioMarketCaseDetail {marketCase: StudioMarketCaseSnapshot, rail: StudioMarketCandidateRail {talentId, role, standingLine, preferences, profileRef?}, comparison: StudioMarketProposalSnapshot[] (own / undisclosed rows reused from projection 42), history: StudioMarketHistory {employers: StudioMarketEmployerRow[], credits: StudioPersonCareerRowSnapshot[], page, pageSize, total}, droppedReasons: string[]}`. Page sizes `MARKET_CLOSED_PAGE_SIZE = 20`, `MARKET_HISTORY_PAGE_SIZE = 10` — OPEN values. Pulse (`view: 'pulse'`): the two P12 receipts of one settlement (the subject's expiry and the winner's start) fold into ONE `StudioIndustryActivity` row carrying the new OPTIONAL `settlementKind` ∈ {`retained`, `moved`}; salary absent; existing pulse consumers see fewer rows (settled-208 page 0: 44 → 20 people rows, totalRows 56 → 32). ABSENCE IS MEANINGFUL: `StudioMarketEmployerRow.annualSalary` is OMITTED (not null, not a marker) on rival-owned intervals. |
| Required UI action/feedback | New Talent Market workspace (P14A.2 design, deferred): attention rows first (five causes, deduplicated, decision week ascending then discovery order); four buckets in the FIXED projected order — never re-sorted client-side (`renewalWindow`/`settling` by decision week then discovery; `closed` by close week descending, paged; `freeAgents` by role order then ask descending, instant-sign through the EXISTING hiring intent); the selected case detail with the candidate rail BY REFERENCE (`profileRef` opens the projection-42 Profile — no second copy of the profile), the offer comparison with every competing figure as the `"UNKNOWN"` string, paged employer/credit history (`historyPage`), and the settlement's order-only reasons plus `droppedReasons` sentences. Review & Submit / Revise / Withdraw reuse the P14A.1 `marketProposalAction` quote family unchanged (same `STALE_REVISION` / `INTENT_NOT_AVAILABLE` handling). Pulse renders the folded row by `settlementKind` (retained vs moved) with person, studio and term length, never a salary. |
| C# binding change | New DTO classes `StudioMarketPage`, `StudioMarketCases`, `StudioMarketClosedCases`, `StudioMarketCaseRow`, `StudioMarketFreeAgentRow`, `StudioMarketCaseDetail` (member `marketCase` — the schema member name `case` is a C# keyword and the generator refuses it: `CF08-IDENTIFIER-COLLISION`), `StudioMarketCandidateRail`, `StudioMarketHistory`, `StudioMarketEmployerRow`; `StudioIndustryActivity.settlementKind` (optional string enum); `StudioIndustryRequest.historyPage` (optional int); `StudioIndustryResponse.market` (nullable). Reused without new shape: `StudioMarketProposalSnapshot` union, `StudioMarketAttentionRowSnapshot`, `StudioMarketCaseSnapshot`, `StudioPersonCareerRowSnapshot`. Regenerate from `generated/unity/StudioBridgeDtos.Generated.cs` + manifest at e210513. |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-43 industry response with `view: 'market'` carrying all four buckets populated (the genuine `tests/fixtures/p14/legacy-v28-settled-208.json.gz` read yields 24 closed cases across two pages of 20), a `selected` detail with `marketCase`, a comparison holding one own row and one undisclosed row, an employer row WITHOUT `annualSalary`, and a pulse response holding a folded activity with `settlementKind`; a pre-43 request (no `historyPage`) still validates. PlayMode (deferred): fixed-order rendering of the buckets, `profileRef` navigation, the quote → commit → `STALE_REVISION` → revise → withdraw sequence from the workspace. None of this is claimed verified: Unity NOT VERIFIED. |
| Migration / integration risks | Build56 (projection 31) and the paused Unity branch (32) are unpaired with 43; Save V28 unchanged by this slice, so every V28 fixture from P14A.1 reads unchanged. The Pulse fold changes row counts for any consumer that pinned pulse totals (none in this repo did). The page-size values are hypotheses: a value change is a projection bump, not a save change. The two receipts do not carry each other's `eventId` (companion §2.1.8 prose vs the landed receipt — V29 candidate); the (talentId, week, kind) join is exact by one-contract-per-person. |
| Unresolved product choices | The two page-size values (20 / 10); whether the per-person EMPLOYMENT route folds a settlement as Pulse does (today it shows both rows — plan A.2 item 3 scopes the fold to Pulse); durable review marks; the decision stop horizon (1 week, inherited Owner call); the fold row's prose. None decided here. |

### P14A.3 — World route facts (engine Save V28 unchanged; bridge projection 44 — landed `b39e646`/`a3ebe95`; CLOSED LOGIC VERIFIED · UNITY NOT VERIFIED on the `f32d56c` matched pass, attribution `evidence/p14a3-20260918/13`; facts only — the three client deliverables below stay open; not Owner acceptance)

| Item | Record |
|---|---|
| Producer version | **Bridge protocol 4 / projection 44**; schema `sha256:05e53f126b28c601a2ed80fb614347da571c90946b65808f111f77bf7bdd5392` (was projection 43 `sha256:95351b9d…`); `$id` `urn:project-studio:bridge:protocol-4:projection-44`; generated C# sha256 `6929c399910002e906ab66dcdee8f4970ea862618165e9ab83e4b3f30d46b220` (both manifest fields); `LIVE_SAVE_VERSION` 28 unchanged. |
| Consumer change | Two NEW DTOs: `StudioWorldCaseRef {view: const "market", targetId}` and `StudioWorldRouteSnapshot {statusLine: string | null, caseRef: StudioWorldCaseRef | null, reach: "playerLot" | "industry"}`. Three changed DTOs (all `additionalProperties: false`, so every member must be bound): `StudioPersonProfileSnapshot.worldRoute` NEW REQUIRED non-nullable; `StudioRosterRowSnapshot.worldStatusLine` NEW REQUIRED nullable (never omitted; null = no open case); `StudioIndustryPerson.caseStatusLine` and `caseRef` NEW REQUIRED nullable, always null together (closed case or no case). `statusLine` copy (CANDIDATE, Owner wording OPEN): "Renewal window open · decides Week N" while the case reads discovered/proposals_open; "Decides next week · Week N" while open and `decisionWeek <= week + 1`; the middle dot is U+00B7. `caseRef` opens the exact case through the EXISTING `view: 'market'` request with `targetId`; the market page's `selected.marketCase` is byte-equal to the Profile's `marketCase`. No enum widened; no new view, intent or page; presence `engagement`/`credit` unchanged. |
| Required UI action/feedback | The companion's three client deliverables, DEFERRED and genuinely open: the lot status glyph on a player's person with an open case (restrained: the `statusLine` text, nothing else) and the world entry that opens the exact case from `caseRef`; keyboard/controller paths to the same entry; Unity parity. A rival's person is reached only from the Industry roster row (`caseStatusLine`/`caseRef`) and the Profile; no rival lot exists and none is to be drawn. Arrivals and departures render at the effective week only (presence follows the contract rows; no early move on submission, revision or settlement). No agency office, no meeting marker. |
| C# binding change | Re-vendor `generated/unity/StudioBridgeDtos.Generated.cs` at sha `6929c399…` and re-run the CF-09 attestation; add `StudioWorldCaseRef` (`view` Required.Always const "market", helper `StudioWorldCaseRefViewValues.Market`; `targetId` Required.Always minLength 1) and `StudioWorldRouteSnapshot` (`statusLine` Required.AllowNull + Include; `caseRef` Required.AllowNull + Include; `reach` Required.Always enum, helper `StudioWorldRouteSnapshotReachValues.PlayerLot/Industry`); bind the three new members above. F10/F11 whole-schema sentinel `48a31e2c252e3762f9cc3bf2074c19402e4fdcbc522bec939ebf6f883ff7a81b`. |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-44 profile with an open case (`worldRoute.statusLine` set, `caseRef.view == "market"`, `reach == "playerLot"`), a profile without a case (`statusLine` null, `caseRef` null), a roster row with `worldStatusLine` null, an Industry person row with the null pair and one with the set pair; a projection-44 industry response still parses every pre-44 view. PlayMode (deferred): the glyph appears only while `statusLine` is non-null; the world entry lands on the market page with the same `targetId`; a person's lot presence flips exactly at the contract's effective week. None claimed verified: Unity NOT VERIFIED. |
| Migration / integration risks | Build56 (projection 31) and the paused Unity branch (32) are unpaired with 44; Save V28 unchanged, every V28 fixture reads unchanged. The copy strings are hypotheses: a copy change is a test edit plus a projection bump. A future decision to fold the Employment route (A.2 OPEN) or to change the seat law (the A.1 addendum above) does not touch these DTO shapes. |
| Unresolved product choices | The status-line copy; whether the Profile's `renewal-open` attention text ("Renewal window open — contract ends Week N.") is retired under the market (three-way wording drift with the companion's case-block prose and A.3's line, flagged for the Owner track); the glyph and input design; the three deferred client deliverables. Product consequence recorded at A.3-T1 (see the P14A.1 addendum): rival → player poaching is impossible under the landed seat law at founding roster sizes. |

### P14B.1 — First Kept Promise Core (Save V29 / projection 45; LOGIC VERIFIED · UNITY NOT VERIFIED)

Current correction (2026-09-19): T3 landed at `e78dbc0`, evidence `c5c81c1`.
The historical PENDING T3 cells below are superseded by the actual manifest:
schema `sha256:5b2a4ca93d930e90a288db55bb5cc3fdc8eea070ef51fa1450a193a325bd755d`,
generated C# `3517aeaa241bf428f6d8935c757d811a8045e3bbc94218f1abf593b5f1e51907`.
Actual projection-45 required/nullability contract: case `trustLabel` and
`promiseHistory` are required/non-null; own proposal `promise` and quote `promise`
are required/nullable; request draft `promise` is optional but non-null when
present. History rows require all seven fields (`promiseId`, `family`, `count`,
`windowStartWeek`, `dueWeekExclusive`, nullable `outcome`, nullable `outcomeWeek`).
`contractId` and `outcomeCause` are not in projection 45; B.2 explicitly adds
them at 46 and changes both history carriers to newest-first. F10/F11 sentinel:
`c69088b3e114fdd6d801d9119a48db798f002ce749504855d031784b50f89222`.
T4 production corrections are published at `ee91913`; its fixed-source full
verification is complete with exact historical attribution. Seven test-only
compatibility corrections at `a93bf77` passed the fresh 13-file / 170-test target;
both typechecks and both contract checks passed. T4 is qualified CLOSED. See
`evidence/p14b1-20260919-t4-final/16-attribution.md`. The case carrier exists for terminal latest cases too, not
only open cases. Historical B1 limitation (superseded by qualified B3 above):
the quoted promise draft was preview-only:
`applyMarketProposal` submits the base proposal without attaching it. Future
command integration must preserve quoted promise terms through commit/revision,
revalidate stale drafts, and test actual attachment/binding before presenting
the composer as a functioning promise offer. B3 now verifies that command path
headlessly; these native consumer obligations remain deferred. No current UI/native
pass is claimed.

V29 compatibility constraint for T4: preserve truthful legacy submission-time
feasibility receipts and previously exported windows; do not fabricate a historic
freeze receipt during load. New settlements must retain the real freeze receipt
and enforce both contract-window edges. Exact identity/reference validation must
reject nonexistent or wrong-party contracts, outcome receipts and filming evidence.

P14B.2 prerequisite finding (2026-09-19, engine verified, Unity not verified): the inherited setup
validator refuses a lawful save after wrap because the completed setup retains
its original stage while occupancy correctly releases it. B2-F1 now validates
that completed history against the exact permanent wrap record, without changing
Save V29 / projection45 or scheduling setup in Post. Future native regression:
select setup → complete first take → wrap/release stage → save/reload in Post and
while Post-blocked; retained setup/provenance remains readable and stage stays
free. No current Unity/native result is claimed. Independent RED and contract:
`evidence/p14b2-20260919/02i-setup-wrap-red*` and `03-setup-wrap-review.md`.
Source `af455ef838b8c2227d784ebdc8bafb89e7a6835b`; bounded closeout `08-f1-closeout.md`:
179 targeted passes, both typechecks PASS, final KEEP; no full F1 suite claim.

Inherited engine compatibility gap found during the B2 producer refresh:
`SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` omits projections32–44. Registering
outgoing45 for the new46 bump does not fix or prove those older inputs (including
the paused32 consumer). Preserve old checkpoints; unsupported loads must remain
fail-closed, never restamp their schema or replace their bytes. Historical identity
restoration and corresponding migration proofs remain a separate engine recovery
obligation before pairing a new native consumer; no older compatibility pass is
claimed from B2's45 fixture.

| Item | Record |
|---|---|
| Producer version | Engine **Save V29**, corrected at `ee91913`; recovered bridge **protocol 4 / projection 45** landed at `e78dbc0` (not pending T3). Schema `sha256:5b2a4ca93d930e90a288db55bb5cc3fdc8eea070ef51fa1450a193a325bd755d`; `$id` `urn:project-studio:bridge:protocol-4:projection-45`. Exact generated identities and required/nullability split are recorded above. The forced bump adds `opportunity` and `trust` to the priority-order enum. T4 qualification is in the attribution record. |
| Consumer change | SAVE V29 (landed): two NEW GameState ROOT tables beside `talentMarket` — `firstTakes: FirstTakeReceipt[]` (`eventId 'first-take-event-<n>'`, `week`, `productionId`, `studioId`, `directorId`, `cast: Record<CastSlot,string>`; appended when a managed production's shooting advances 5 → 4 in both weekly callers) and `promises: ProfessionalPromise[]` (one promise family live, `APPEARANCE_COUNT`; `contractId` null until its proposal is committed; `outcome` SATISFIED/BROKEN with `outcomeEventId` naming the promise's OWN `promiseOutcome` receipt in every branch (ruling (ix), `faf45f3`; the causing take(s) live in `evidenceRefs`); `feasibilityReceipt {classification, bottleneck, inputsDigest, rulesVersion, week}`; the table also holds RIVAL-authored records — since `faf45f3` a rival attaches one appearance-count promise per contested actor case it can reasonably achieve — whose terms are never disclosed to another studio, so a client must not read `state.promises` directly for display, only the projection); `TalentMarketProposal.promises: readonly string[]` (promise ids, at most one); receipt kind `promiseOutcome`; the proposal digest gains a sixth element only when a promise is attached. The frozen V≤28 chain is byte-identical (V29 → V28 strips both roots and the per-proposal leaf); the engine REFUSES LOUDLY a state missing the V29 roots ("promises: the Save V29 roots are missing — migrate this state to V29 before acting on it"), so every client save path must run `migrateToV29` before acting (the web adapter pins moved at `6948e31`). PROJECTION 45 (PINNED by the RED bridge test `db13007`, landing at T3): `priorityOrder` enum six values `opportunity | compensation | term | trust | standing | incumbency` (a client switch over the old four must accept two new ones); the Profile case block's proposal rows gain `promise` (own row: family, count, window, classification; competing rows: the existing UNKNOWN marker string); `BridgeMarketCaseSnapshot` gains `trustLabel` (`Reliable | Mixed record | Distrusted`, viewer-scoped, no driver text) and `promiseHistory` (only promises the viewing studio issued, open and settled with outcome); the market page comparison carries the same promise rows; `StudioMarketProposalDraftPayload` gains OPTIONAL `promise {family, count, windowStartWeek, dueWeekExclusive}` on the EXISTING `quoteMarketProposal` intent; `StudioMarketProposalQuoteSnapshot` gains `promise {ok, classification, message}` with `message` null when ok, else "not offerable: <bottleneck>". No new view, page or intent kind. Absence semantics: `trustLabel`/`promiseHistory` exist only while the case block exists (null case block = no case; a terminal latest case also carries the block); the optional-vs-required split of every new member is recorded at T4 from the landed schema. |
| Required UI action/feedback | DEFERRED, none started: the promise row on the Profile case block and on the Market comparison (own terms vs the UNKNOWN marker); the draft composer's promise sub-form (family fixed to appearance count in B.1; count; window) and the quote refusal message shown verbatim; the trust label on the case block; the person's promise history rows with outcome. The trust driver text, the Pulse `promiseKept`/`promiseBroken` activities, the attention causes `promiseDue`/`promiseOutcome` and the workspace promise history are P14B.2 read models, not this entry. Unity NOT VERIFIED. |
| C# binding change | Generated producer is landed and contract-checked, NOT Unity-consumed: re-vendor `generated/unity/StudioBridgeDtos.Generated.cs` at `3517aeaa241bf428f6d8935c757d811a8045e3bbc94218f1abf593b5f1e51907`, pair projection 45 and re-run CF-09. Bind priority values `Opportunity`/`Trust` and all new members with the required/nullability split above; F10/F11 sentinel `c69088b3e114fdd6d801d9119a48db798f002ce749504855d031784b50f89222`. No Unity compilation, rendering or native acceptance is claimed. |
| Future compile/render/native tests | EditMode: DTO round-trip of a projection-45 case block with an own promise row, an UNKNOWN competing row, a `trustLabel` and a `promiseHistory` both empty and holding one BROKEN row; a quote response with `promise.ok == false` and its message; a projection-45 industry/market response still parses every pre-45 view; a V29 save load and a V28 save refused until migrated. PlayMode (deferred): the composer refuses to submit a draft whose quote said not offerable; the label changes only on the outcome week. None claimed verified: Unity NOT VERIFIED. |
| Migration / integration risks | Build56 (projection 31) and the paused Unity branch (32) are unpaired with 45; every V28 save must migrate to V29 before the engine acts on it (loud refusal, no silent default). The feasibility and trust constants are HYPOTHESES in `src/core/promises.ts` (`WEEKS_TO_FIRST_TAKE` 5, `SEAT_CYCLE_WEEKS` = `TUNING.PRODUCTION_TICKS` 8, `PROMISE_SLACK_WEEKS` 8, `promiseBuffer(N) = max(1, ceil(0.25 N))`, `TRUST_HORIZON_WEEKS` 260, `TRUST_DISTRUST_MIN_NEGATIVES` 2): a retune is a test edit, not a save bump; the bottleneck and label copy strings are hypotheses: a copy change is a test edit plus a projection bump. A promise BINDS only when its proposal is committed (ruling at T2): a losing, dropped or withdrawn promise is never evaluated and never drives trust. |
| Unresolved product choices | The trust thresholds and horizon; the buffer/slack numbers; the rival authoring policy (one appearance-count promise, count 1, only when reasonably achievable); the companion's has-discipline seat eligibility vs the landed M16.2 `actor` role gate (an appearance promise for a non-actor is IMPOSSIBLE under the engine's own law); Q1/Q3 recorded as recommendations; calendar/Upcoming rows; P2–P5 families, waiver, VOIDED, relationships → B.2+; the case-block-only visibility of the trust label and promise history in B.1 (the case-independent Profile trust block with driver text → B.2); the "not offerable" copy. The seat-law poaching finding (P14A.1 addendum) carries over. |
