# Current Ops — P13B launch review and next priority

Date: 2026-09-12.

**Recipient: the existing Future Ops / P13B preparation owner, not a new implementation agent.**

Read `01-CURRENT-OPS-DISPOSITION-AND-PREPARATION-ORDER.md`, then the bounded calculation correction in `02-PAPER-ECONOMICS-CORRECTION.md`.

This packet contains new Current Ops instructions and a reproducible paper check. It is not the original 43-file P13B launch-review archive and does not replace that archive. The preparation owner already holds the original candidate and its source packet. No new reading scavenger hunt is required.

Current Ops disposition:

- Preserve the complete P13B proposal and all eight Ready obligations. Correct the one Post operating-cost onset error; do not reopen the catalogue or broad reconnaissance.
- Schedule the whole-game Playability & Interaction Pass as the next implementation priority, after closed P13A and before P13B. This resolves companion decision D7.
- Prepare its finite launch order using the already-supplied usability direction and comparator work. This packet authorizes documentation and targeted immutable-source reads only. Neither UX nor P13B gameplay implementation starts under it.

Run the optional arithmetic check with Python 3:

```sh
python3 03-check-paper-economics.py
```

It prints JSON, writes nothing, calls no network service, opens no game, and accesses no user campaign. `04-CALCULATED-CORRECTIONS.json` is the captured result of that check.

`05-SOURCE-INDEX.md` distinguishes reviewed source identities from the new Current Ops decisions. `sources/` contains the three previously supplied, blob-verified P13A closeout documents and an explicitly marked excerpt of the UX priority already present in the planner's original packet. Local Mac links in historical sources are not authorization or a requirement to inspect mutable user data.

Finish at one handoff containing the corrected P13B publication/ZIP identities and the compact Playability & Interaction launch-review packet. Do not start another P13B catalogue review, create a reviewer fleet, reopen P13A, take native input, or start gameplay coding.
