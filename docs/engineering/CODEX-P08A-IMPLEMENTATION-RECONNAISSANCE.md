
    # P08A Implementation Reconnaissance — Revision 05

    **Status:** PROVISIONAL — CODING-AGENT CHANGED-PATH REFRESH REQUIRED
    **Product authority:** `codex/awards-standing-research-08@438708c5071097d8e1ddb2f97a3f7b6674b2a65e`
    **Accepted starting authority:** `2753e18ba8fb5f65b936c22cde9531646fecc6cd` / `c4c65db464ef9abcf3bdcc088f5c8a47cc9081b6`

    ## Current-law seams to inspect

    - `src/core/standing.ts`
- `src/core/types.ts StudioEvent/StudioEventLog`
- `src/core/tick.ts and publicity actions`
- `ui/src/engine/adapter.ts`
- `bridge/schema/bridge-schema.ts`
- `private Unity StudioWorkspaceHost/Admin/History owners`

    ## Required classification

    Every touched candidate path/symbol must be classified `REUSE`, `EXTEND`, `NEW`, `REPLACE AFTER PARITY`, or `DO NOT TOUCH`. Record one editing owner for shared save, schema, generated contract, workspace host, lot shell, and shared USS/UITK files.

    ## Known accepted contract

    - Save V16, protocol 4, projection 15, schema `sha256:ddce1c399ac4ff58327b296a0600428ac3f3346b84f3639e66e48e53a65fbe99`.
    - P07 result wire identity is `productionId`; titles can collide; participants/forecasts are optional; active totals remain projected.
    - TypeScript owns simulation/history/legality. Unity presents and dispatches exact intents.
    - Exact generated consumer verification is mandatory after every projection change.

    ## Likely version effect

    Expected V17 / projection 16; exact numbers require coding-agent preflight.

    These are planning expectations, not version authorization. The coding lead must inspect validators/migrations and may use a different monotonic final sequence only if all laws and profiles are preserved.

    ## Private Unity boundary

    **SOURCE INSPECTION NOT AVAILABLE TO FUTURE OPS — REQUIRES CODING-AGENT READ-ONLY PREFLIGHT.** The accepted Unity SHA is known; exact current files/owners must be inspected locally.

    Verify exact owners for the workspace host, lot route, focus/Back, element map, candidate builder, test assembly, proof runners, and generated DTO at `c4c65db464ef9abcf3bdcc088f5c8a47cc9081b6`. Record local/remote equality; do not claim Future Ops inspected it.

    ## Performance/reliability questions

    Measure collection/index behavior at campaign scale, avoid per-frame scene/global searches, preserve deterministic ordering, and prove save/load/reconnect/engine replacement. Never infer identity from title, name, list order, coordinates, or visual body.
