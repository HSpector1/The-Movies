# P13A decisions and acceptance companion

**LAUNCH-REVIEW PACKAGE · DOCUMENTATION AND READ-ONLY RECONNAISSANCE ONLY · P13 IMPLEMENTATION NOT AUTHORIZED.** Nothing here is an execution order and no number here is approved tuning. Index: [P13A launch review index](./P13A-LAUNCH-REVIEW-INDEX.md). Draft: [P13A launch prompt](./DRAFT-P13A-LAUNCH-PROMPT.md).

Prepared 2026-09-11 on `docs/p13-post-p12-launch-preparation-01`, planning parent `e48541b`. One documentation author, one independent read-only final checker. No game, build, test suite, native input or profile was touched, and the Unity repository was read through the API at its pinned shas only.

---

## 1. Source refresh against accepted P12

**Method.** Eight read-only inspections of the accepted runtime `592e926bfbf4574df94b38fc8dd594fc5df2ac8d`, the P12 closeout `13370d4`, the published evidence `d4e1915`, and the actual Unity player-build source `deca3952`, followed by one adversarial completeness pass that re-checked every REUSED and SHARED GENERALIZATION row against the source and hunted for gaps. Runtime, player-build, tools and documentation identities are kept distinct throughout; every TypeScript fact cites the runtime sha and every native fact cites the player-build sha.

**The rule that produced these classifications.** A design document naming an interface is not evidence the interface exists. Every REUSED and SHARED GENERALIZATION row carries a real path, exported symbol and line range you can open. The adversarial pass found **no row resting on a design document alone and no missing symbol**, which is the single most useful result in this section: the reuse claims hold.

**Five POST-P12 verification items were open when this preparation began. All five are now closed**, and §1.10 records what each answer was.

#### 1.1 Authoritative calendar and weekly scheduling

**Finding.** `market.tick` is the only clock and `tick()` in src/core/tick.ts is its only writer, so P13 hangs a dated technology milestone exactly where the existing fixed rival-arrival gate already hangs (tick.ts:1038-1041, reading `finalized.market.tick` after the single increment), but `EraConfig` is written once by worldgen and never advanced by anything, so era/technology progression is entirely unbuilt.

*NEW P13 WORK 1 · REUSED 8 · SHARED GENERALIZATION 3*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| The single authority that advances a week | `src/core/tick.ts`<br>`tick`<br>182, 999 | **REUSED** | P13 must not add an advance verb. A dated technology milestone is a step inside this pipeline, not a new one. Note the pipeline is fixed-order and documented at tick.ts:10-38 as insertions-not-reorderings; P13 inherits that rule. |
| Who may call the advance (the public verb and its callers) | `ui/src/engine/adapter.ts`<br>`advanceWeek`<br>2461-2478 (adapter); callers ui/src/App.tsx:2936, bridge/session.ts:351 | **REUSED** | P13 gets no new entry point. The batch verb (adapter.ts:2960-2985, `for (let i = 0; i < SIM_CAP; i++)` with stopReason fallback `'limit'`) also only calls the same `tick`; P13 must not add a stop reason without updating the exhaustive `never` switch in ui/src/lot/livingTurn.ts:134-156. |
| How campaignDate() maps week 0, and what campaign-calendar-1920-52/v1 actually is in code | `src/core/calendar.ts`<br>`campaignDate, CAMPAIGN_CALENDAR_POLICY`<br>2, 13-21 | **REUSED** | The policy is a bare string literal used as a version tag, not a config object: there is no era table, no leap handling, no month. Week 0 -> `1920 · Week 1`. P13 must derive every era/technology year from `campaignDate(week).year`, never from its own 52-week arithmetic. |
| Reaching the calendar from outside src/core | `src/core/index.ts`<br>`(absent), no calendar re-export`<br>whole file, 1311 lines | SHARED GEN. | Widening owned by whoever owns src/core/index.ts: add `campaignDate`/`CampaignDate`/`historicalDate`/`CAMPAIGN_CALENDAR_POLICY` to the barrel, or P13 repeats the deep relative import. Do NOT mint a second date formatter in ui/ or bridge/. |
| The existing fixed dated-milestone gate, the exact hang point for a dated technology milestone | `src/core/tick.ts`<br>`the scheduled-entry loop over hollywood.identities`<br>1038-1042 (with src/core/calendar.ts:3) | **REUSED** | This is the precedent and the place: a `const` week table in calendar.ts + a `<= finalized.market.tick` gate run once, after the single increment, inside tick's finalize. P13 must not duplicate the loop for rivals, must not reuse RIVAL_ARRIVAL_WEEKS as a technology table, and must not make a technology gate depend on a reserved rival id. |
| Deriving a recurring cadence without a second clock | `src/core/hollywoodTick.ts`<br>`finishHollywoodWeek`<br>293-316 | **REUSED** | A periodic industry observation is already modelled as pure arithmetic on the authoritative week plus a stored snapshot. P13's annual/era summary must copy this shape, not persist its own counter. Validator bound already exists at hollywoodValidation.ts:414. |
| Per-actor scheduled next-due-week (the stored-schedule precedent) | `src/core/hollywoodTypes.ts`<br>`RivalBusiness.nextDecisionWeek`<br>hollywoodTypes.ts:92; hollywoodTick.ts:140-141, 222; hollywoodValidation.ts:207 | **REUSED** | If P13 needs a per-studio research cadence, store a single absolute `nextXWeek` integer in state and bound it in the validator against `state.market.tick`, exactly as here. Do not store elapsed counters or intra-week positions. |
| Era state that a technology milestone would change | `src/core/types.ts`<br>`EraConfig (and GameState.era)`<br>types.ts:284-289, 479; worldgen.ts:657-663 | **NEW P13** | Nothing advances the era. P13 builds era transition itself, but must NOT duplicate the ~8 existing readers (candidates.ts:286, filmPackage.ts:97/755, forecast.ts:145, reception.ts:264, hollywoodPolicy.ts:45, studioRunRecap.ts:495, hollywoodTick.ts:63/199), they already consume `era.costScale` and the three flags. Changing `era` mid-campaign also changes save comparison (save.ts:2217 `v8Era`) and every frozen GameStateVn shape; that is P13's cost, not P12's. |
| Where a dated technology milestone is RECORDED | `src/core/studioHistory.ts`<br>`commitStudioHistory / appendStudioHistory / StudioHistoryEvent`<br>studioHistory.ts:206-216, 229-239; types.ts:1604-1652; save.ts:4906-4929 | SHARED GEN. | Widening owned by the P08A history owner: add a `technologyAdopted`-style member to the `StudioHistoryEvent` union, an arm in `classifyHistorySignificance` (it ends in `const _exhaustive: never = draft`, so omission is a compile error), and a row in save.ts's HISTORY key table (4906-4929) or saves fail to load. The week-ordering refusals already enforce 'no invented retrospective technology history', do not weaken them to backfill. |
| Does anything other than market.tick claim clock authority | `src/core/studioCalendar.ts`<br>`studioCalendar`<br>1-3, 833-868 (and 446, 711, 787, 846) | **REUSED** | studioCalendar.ts still exists at this sha and is unrelated to calendar.ts despite the name: calendar.ts is date LAW (year/label/policy/arrival weeks), studioCalendar.ts is a capacity-board READ MODEL over market.tick. P13 must not merge them and must not add a milestone by writing into this view. ui/src/lot/livingTurn.ts:6 and :257 likewise disclaim authority ('market.tick remains the authoritative integer week; Living Turn V1 persists no intra-week position'). |
| Per-campaign isolation of the clock | `src/core/types.ts`<br>`GameState.market (no campaignId anywhere in src/core)`<br>types.ts:276-282, 479; bridge/runtime/campaign-library.ts:21; bridge/industry.ts:28 | SHARED GEN. | Isolation of the clock is structural, not named: core has zero module-level mutable state (every top-level binding in src/core is a frozen const Set), so two Save As copies with identical entity ids cannot share a week. The one memo in the stack, bridge/industry.ts:28 `const indexes=new WeakMap<GameState,Index>()`, is keyed by GameState object identity and is therefore campaign-safe. Widening owned by whoever adds P13 caches: any technology cache MUST live in GameState or be a WeakMap keyed by GameState, never a module-level Map keyed by studioId or productionId, which Save As copies would collide on. |
| Whether the native player build holds or computes a clock | `Assets/Studio/Runtime/Infrastructure/StudioBridgeClient.Industry.cs`<br>`gameWeek (consumed as a staleness token)`<br>113, 123 | **REUSED** | The Unity player build never derives a date, it compares `gameWeek` as an opaque revision token and renders TS-supplied label strings. P13 must ship any era/technology date as a pre-formatted string field on the bridge DTO; it must not add year arithmetic in C#. Distinct from source-manifest HEAD 2bc8d30, which I did not inspect. |

**Hazards.**

- tick.ts:396 temporarily hands `admitQueuedIntents` a state whose `market.tick` is `currentTick + 1`, restored one line later at tick.ts:401 (`const admitted: GameState = { ...admission.state, market: state.market }`). Any P13 producer that reads `state.market.tick` from inside the queue-admission call path sees NEXT week, not the week being advanced. A technology gate placed there would fire one week early.
- src/core/studioHistory.ts:229/235 throw on any row whose week precedes `recordingStartedWeek` or the last recorded week. A P13 attempt to backfill retrospective research/expenditure/technology rows into studio history will not silently succeed, it will crash the tick. That is the invariant working; do not add a bypass.
- hollywoodValidation.ts:92 `requireFact(s.recordedFromWeek === null && s.eligibleWeek > state.market.tick,'overdue or partially entered studio')` means a reserved rival id is provably not an active studio. Any P13 technology state keyed by a reserved (unentered) rival id, or any per-studio research row minted for one, breaks this assertion at the save and tick boundary.
- calendar.ts:3 RIVAL_ARRIVAL_WEEKS is validated element-by-element at hollywoodValidation.ts:86 (`s.eligibleWeek === (index === 0 ? 0 : RIVAL_ARRIVAL_WEEKS[index-1])`). Editing that array to align it with an era/technology schedule invalidates every existing save. P13 needs a separate table.
- `campaignDate` THROWS on a negative or non-safe-integer week (calendar.ts:14-16) and `historicalDate` throws outside [1800,1920) (calendar.ts:27-29). There is no date type spanning the boundary. P13 code that formats a technology origin year must branch on the two functions the way bridge/industry.ts:45 already does, or it will throw on authored pre-campaign technology.
- src/harness/roster-wall/fixtures.ts:401 stamps `market: { ...generated.market, tick: ROSTER_WALL_MECHANICS_ENTRY_WEEK }` directly, bypassing `tick()` entirely. This is a harness fixture, not a runtime path, but a P13 fixture copying it would produce a state whose era/technology facts were never derived by any advance.
- `EraConfig` is embedded in the frozen save shapes (GameStateV2 onward, types.ts:478-479; validated by save.ts:2217 `v8Era`). Making `era` mutable per week is a save-chain change, not a field edit, and the frozen Vn anchors exist precisely to stop that from happening quietly.
- Adding a member to `SimStopReason` for a technology event breaks two exhaustive `never` guards: ui/src/lot/livingTurn.ts:153-156 and the significance classifier at src/core/studioHistory.ts:163-166. Both are compile errors, not runtime bugs, but both must be routed deliberately (pause vs notify; major vs standard).

**Open in this area.**

- Whether `docs/c2-planning/08A-TIME-MODEL-DOCKET-ADDENDUM.md` exists at 592e926 or says what ui/src/lot/livingTurn.ts:3 cites it as saying. I read only the code comment.
- Whether any test asserts that `tick()` is the sole writer of `market.tick`. I did not read or run any test file; my 'sole writer' claim rests on an exhaustive grep of src/, bridge/, ui/src/ and tools/ at 592e926, which found writes only at tick.ts:396 (scoped, restored) and tick.ts:999, plus harness fixtures.
- The source-manifest HEAD 2bc8d304b79a72bf20fda1d462ec3d96df253992. I inspected only the player-build source deca3952 for clock/date facts; the two identities may differ.
- Whether the main UI (ui/src/App.tsx and its components) ever renders a year at all. I confirmed `campaignDate` has zero occurrences under ui/src/ at 592e926, the label appears only via bridge/industry.ts, but I did not audit every UI string for an independently computed year.
- Whether the P12 Owner acceptance receipt covers the arrival-week schedule. 13370d42:docs/campaigns/P12-R05-OWNER-ACCEPTANCE-RECEIPT.md:15 states the passes 'do not claim he replayed every arrival date, long-horizon test, migration case, financial calculation or input device.' So the dated-milestone behavior P13 will extend is not Owner-replayed.
- Whether the migration path (save.ts:6796 `const migrationWeek = oldState.market.tick`, :7117 `migratedStudioHistory(oldState.market.tick)`) has any era/technology implication. I confirmed the clock is carried forward but did not trace the full V-chain.

#### 1.2 Studio identity, participation and scheduled rival entry

**Finding.** The accepted registry is `HollywoodState.identities`, exactly ten rows (player at row 0 plus nine reserved rivals), IDs derived from the campaign seed alone, arrival weeks pinned to `RIVAL_ARRIVAL_WEEKS = [0,0,0,0,520,988,1560,1872,2548]`, and a closed ten-field identity shape that the save validator rejects any extra key on and that carries no dormant/closed status.

*NEW P13 WORK 1 · REUSED 10 · SHARED GENERALIZATION 1*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| Canonical studio identity record carrying brand, founding, eligibility, entry and recording facts | `src/core/hollywoodTypes.ts`<br>`StudioIdentity`<br>6-17 | **REUSED** | P13 reads these ten fields as-is. It must not define a parallel studio record, and must not add an eleventh field (see hazards, the save validator pins the key set). |
| The one registry P13 must reference for studio participation (player + nine reserved rivals) | `src/core/hollywoodValidation.ts`<br>`validateHollywood`<br>52, 71-72, 102 | **REUSED** | `state.hollywood.identities` plus `state.hollywood.playerStudioId` IS the registry. P13 must not create a second studio list, a technology-studio table, or its own player/rival enumeration. |
| Fixed rival arrival weeks (handoff claim 520/988/1560/1872/2548, verified against code) | `src/core/calendar.ts`<br>`RIVAL_ARRIVAL_WEEKS`<br>3 | **REUSED** | Handoff VERIFIED: four opening rivals at week 0, then 520/988/1560/1872/2548. Nine entries, one per reserved rival. P13 must not reschedule, add, or era-gate an arrival; the validator throws 'fixed arrival policy mismatch'. |
| Stable, deterministic studio IDs P13 can key technology/era records against | `src/core/hollywood.ts`<br>`hollywoodWorldKey / uniqueIdentity / initializeHollywood`<br>107-109, 116, 121 | **REUSED** | IDs are `studio-<8-hex-worldkey>-player` and `-r01`..`-r09`, derived from the seed only. P13 keys by studioId but must also scope by campaign/session (see hazards): two Save As copies produce identical IDs. |
| How a reserved ID differs in code from an active studio | `src/core/hollywoodValidation.ts`<br>`validateHollywood (identity participation clause)`<br>91-93 | **REUSED** | A reserved ID is exactly: `enteredWeek === null`, `recordedFromWeek === null`, no `RivalBusiness` row, and `eligibleWeek > market.tick`. P13 must treat it as not-yet-existent, never as a dormant or closed studio. |
| How a reserved ID differs in code from an employer (a reserved rival is not an employer) | `src/core/hollywood.ts`<br>`studioEmployerId / rivalEmployment`<br>45-54, 60-65 | **REUSED** | Employment authority is the player's `state.contracts` plus `hollywood.employment` rows, which are only minted inside enterRival/staff. A reserved ID has zero employment rows, so it can never be returned as an employer. P13 must not add a second employer authority. |
| The participation predicate P13 needs (which studios actually exist this week) | `src/core/hollywoodTick.ts`<br>`finishHollywoodWeek (chart cohort), inline predicate, no exported selector`<br>307 (also bridge/industry.ts:23, 66, 73 at the same sha) | SHARED GEN. | Widening needed: extract one exported participation selector over `identities`, owned by the hollywood core module (src/core/hollywood.ts), and have P13 call it. P13 must NOT copy the predicate a fifth time and must NOT substitute a status enum for it. |
| A dormant/closed studio status field | `src/core/hollywoodTypes.ts`<br>`StudioIdentity, field absent`<br>6-17 (absence); enforced at hollywoodValidation.ts:75 | **NEW P13** | Nothing accepted provides dormancy or closure. The handoff's claim is VERIFIED. P13 must NOT build it either, the handoff assigns corporate fate/dormancy/closure/replacement to P15B/P15C, and must not infer closure from a missing RivalBusiness, which only means never-entered. |
| Brand tokens (name / mark / colour) for era and innovation presentation | `src/core/hollywoodStartingData.ts`<br>`HOLLYWOOD_STARTING_MANIFEST.studios`<br>8-38 (nine entries, rows 1-9); pinned at hollywoodValidation.ts:82 | **REUSED** | Brand is manifest-authored and validator-pinned per row. P13 may read name/mark/color for era displays but may not re-skin, rename, or add a per-era brand variant to the identity row. |
| Founding, entry and recording chronology P13 must honour for any era/technology history | `src/core/hollywood.ts`<br>`enterRival (identity stamping)`<br>189-192; validated at hollywoodValidation.ts:96-99 | **REUSED** | `recordedFromWeek` is the hard floor for any rival record. P13 must not author research, expenditure or technology history for a rival before its recordedFromWeek, and must not backfill `founding` for a migrated rival (validator throws 'fabricated migration past'). |
| The single driver that admits a scheduled rival entrant | `src/core/tick.ts`<br>`advanceWeek finalization loop -> enterRival(..., 'scheduled')`<br>1038-1043 | **REUSED** | Exactly two entry paths exist: initializeHollywood (hollywood.ts:132-134, fresh/migration due entrants) and this one. Both read `market.tick` of the campaign being advanced. P13 must not add a third driver, a second calendar, or any out-of-tick entry. |
| Authored opening history is restricted to rows 1-4 on a fresh campaign; rows 5-9 are genuine later entrants | `src/core/hollywood.ts`<br>`enterRival (authored gate)`<br>150, 195-199; validated at hollywoodValidation.ts:116-118 | **REUSED** | Only rows 1-4 (Bellwether, Rose Lantern, Night Orchard, Silver Current) carry pre-1920 founding years and two authored settled films each, and only when origin==='fresh'. P13 must preserve this and must not extend authored history to rows 5-9 or invent an era-era retrospective for them. |

**Hazards.**

- Save As copies share every studio ID. `hollywoodWorldKey(seed)` (src/core/hollywood.ts:107-109) derives the world key from `state.seed` alone, and hollywoodValidation.ts:70 pins `h.worldId === `world-${worldKey}``; bridge/campaign-origin.ts:4-6 treats identical seed+worldId+playerStudioId as the SAME native campaign origin. Any P13 technology ledger, research cache, or expenditure index keyed only by studioId (or worldId) will collide across Save As copies. Scope by campaign/session, not by studio ID.
- Industry receipt IDs are only locally unique. `eventId:`industry-event-${h.nextReceipt++}`` (src/core/hollywood.ts:184, 194; hollywoodTick.ts:36) counts within one HollywoodState. Two Save As copies emit the same eventId for divergent events. P13 must not build a cross-campaign receipt or research-history index on eventId.
- StudioIdentity has a CLOSED key set. hollywoodValidation.ts:75 calls `exact(s,['studioId','role','row','name','mark','color','founding','eligibleWeek','enteredWeek','recordedFromWeek'])`, and `exact` throws unless the key count matches exactly. Adding `status`, `techLevel`, `eraEntered` or any P13 field to StudioIdentity breaks every accepted save. P13 technology state must live in its own additive root, keyed to these IDs.
- Entry is driven strictly by the advancing campaign's own `market.tick` (tick.ts:1039, hollywood.ts:132-134, 143). Any P13 background/global sweep that enters rivals outside a campaign's own advance would advance an inactive campaign and break `requireFact(s.enteredWeek===Math.max(s.eligibleWeek,h.originWeek),'studio entry differs from its actual due boundary')` at hollywoodValidation.ts:98.
- Migration deliberately leaves rival `founding` null and stamps entry at the migrated week. hollywoodValidation.ts:99: `if (h.origin === 'migration' && index > 0) requireFact(s.enteredWeek >= h.originWeek && s.founding?.kind !== 'beforeCampaign','fabricated migration past')`. Any P13 backfill of a pre-1920 founding or era history onto a migrated rival throws on load.
- Inferring closure from data shape is wrong and unsafe. `businesses` absence means never-entered (hollywoodValidation.ts:93), not shut down; `RivalBusiness` has no lifecycle/status field (hollywoodTypes.ts:79-95) and nothing in hollywoodTick.ts ever removes a business. A P13 UI that renders 'inactive' from a missing business would mislabel reserved rivals 5-9.
- `state.era` is a single global `EraConfig` (src/core/types.ts:284-289: soundRequired, televisionCompetition, censorship, costScale) with no per-studio dimension, and rival reception already consumes it verbatim (hollywoodTick.ts:63 `era:state.era`, :199). Widening EraConfig silently changes every rival's reception and standing; it is not a P13-private knob, and there is no rival technology tree to attach it to.
- The participation predicate `enteredWeek!==null` is duplicated inline in four places (hollywoodTick.ts:307; bridge/industry.ts:23, 66, 73). A P13 fifth copy that drifts (for example filtering on a business row instead) would put reserved IDs into an era cohort or a comparison chart.

**Open in this area.**

- Whether bridge/session.ts, bridge/runtime/campaign-library.ts or bridge/runtime/runtime-coordinator.ts scope industry receipts or read-model caches by campaign/session, not inspected at 592e926bfbf4574df94b38fc8dd594fc5df2ac8d.
- Whether the save migration chain in src/core/save.ts (V18 -> V19) re-derives identities from the seed or trusts the persisted `identities` array, not inspected.
- Whether any Unity/native source consumes StudioIdentity, the nine reserved IDs, or RIVAL_ARRIVAL_WEEKS, the pinned manifest HEAD 2bc8d304b79a72bf20fda1d462ec3d96df253992 and player-build source deca39521da1baeca61898d156a43f4ae6a7e035 were not read.
- Whether `state.era` is ever mutated after world generation, no era-transition mechanism was found in calendar.ts, hollywood.ts, hollywoodTick.ts or tick.ts:1038-1044, but src/core/worldgen.ts and src/core/tick.ts were not searched exhaustively for era writes.
- Whether any test, harness (src/harness/*) or evidence fixture hard-codes the nine reserved studio IDs or the arrival weeks, not searched.
- Whether `hollywood.identities` or any studio identity is exposed through the public API barrel: src/core/index.ts (1311 lines) contains no case-insensitive match for 'hollywood', so consumers import directly from './hollywood.js' / './hollywoodTypes.js'. I did not verify whether P13 is expected to use the barrel.

#### 1.3 Rival finance, capacity and actual project lifecycle

**Finding.** A rival's capacity is frozen three ways at once, the facility list must byte-match rivalStartingFacilities, the capacity movement must equal the four-capex sum exactly, and facilityOpex reconciles as elapsed weeks x current capacity opex, so P13 cannot give a rival any new or upgraded capacity without a save-schema change and a migration.

*NEW P13 WORK 4 · REUSED 7 · SHARED GENERALIZATION 1*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| Managed operations represent development/casting, stage, scenery and post capacity for a rival | `src/core/hollywood.ts`<br>`rivalStartingFacilities`<br>98-105 | **REUSED** | P13 reads these four capabilities as the rival's whole plant. Do not build a second rival capacity registry; RivalBusiness.operations is a full StudioOperations (hollywoodTypes.ts:84) and already carries mode 'managed', facilities and workflows. |
| Rival physical operations are abstract, not lot occupancy | `src/core/hollywood.ts`<br>`rivalStartingFacilities / rivalCapacityOpex`<br>88-105 | **REUSED** | Rival facilities carry no blueprintId, LotCell, placement row, construction project or StudioSet. hollywoodTick.ts:166 calls addManagedProductionWorkflow with requiresSetBinding defaulted false, so no rival picture ever binds a set. P13 must not infer rival room/lot geometry from capacity. |
| Existing owners reserve resources and advance actual projects through to settled releases | `src/core/hollywoodTick.ts`<br>`advanceHollywoodWeek / decide / operateStage`<br>69-82, 139-210, 213-290 | **REUSED** | The full causal chain already exists and is owner-scoped. A P13 rival consequence must be injected as an input to a step in this chain, never as a parallel rival lifecycle. Note the rival never calls arriveDueScenery; it clears scenery-load-in directly (hollywoodTick.ts:76-79). |
| Typed opening capital with a declared basis | `src/core/hollywoodTypes.ts`<br>`RivalAccount.openingBalance / openingBasis`<br>56-61 | **REUSED** | Opening capital is authored per studio in HOLLYWOOD_STARTING_MANIFEST (20M-38M, hollywoodStartingData.ts:11-36) and is pinned to entry. P13 must not re-endow a rival or reinterpret the basis literal; it is a validated string. |
| Finite Cash and reconciled category periods | `src/core/hollywood.ts`<br>`moveRivalMoney / newFinancePeriod`<br>25-42 | **REUSED** | Periods are 52-week campaign-year blocks, per-kind, and the save re-derives cash from them (hollywoodValidation.ts:221-224). Negative cash is lawful: the only cash<0 throw is at entry (hollywood.ts:188) and validation applies no minimum. P13 must not read negative Cash as distress, closure or bankruptcy. |
| A per-studio research / adoption / technology expenditure line | `src/core/hollywood.ts`<br>`RIVAL_MONEY_KINDS`<br>15-16 | **NEW P13** | No accepted code spends rival money on capability. The 'development' kind is declared, persisted and reconciled against a sum that is always 0 (validation:242), it is an empty slot, not a working line. P13 must not add a parallel rival ledger; any new spend must move through moveRivalMoney and reconcile in its period. |
| scriptProjectId is scoped by studio owner, not globally unique | `src/core/scriptDevelopment.ts`<br>`canonicalScriptProjectId`<br>64-69 | **REUSED** | LiveIndustryFilm.scriptProjectId (hollywoodTypes.ts:39) is resolvable only with its studioId. Contrast productionId/conceptId/contractId, which ARE globally unique (`${b.studioId}:film:N`, hollywoodTick.ts:159). Any P13 key over screenplays must be (studioId, scriptProjectId). |
| Scoping mutable state, receipts and caches by campaign/session (Save As copies share entity IDs) | `src/core/hollywood.ts`<br>`hollywoodWorldKey / HollywoodState.worldId / nextReceipt`<br>44-49, 107-109, 127 | **NEW P13** | Two Save As copies mint identical worldIds and identical receipt eventIds. The one existing cache is safe only by accident of shape: `employmentByPerson` is a WeakMap keyed by the employment ARRAY object (hollywood.ts:44), so copies never collide. P13 must key caches by state-root object identity, never by worldId or a studio id string. |
| A lawful rival consequence that passes through the existing owner | `src/core/hollywoodPolicy.ts`<br>`chooseIndustryPackage / perceivedPlanningInputs`<br>21-66 | SHARED GEN. | The lawful injection point is the shared reception/forecast input (era, concept, shapeEffects) or RivalBusiness.policy, both already flow through the owner. Widening owner is whoever changes EraConfig, because that one product has 7 call sites (candidates.ts:286, filmPackage.ts:97/755, forecast.ts:145, hollywoodPolicy.ts:45, reception.ts:264, studioRunRecap.ts:495). Do NOT add a rival-side lever. |
| Era state that actually advances over the campaign | `src/core/types.ts`<br>`EraConfig`<br>284-289 | **NEW P13** | No code path anywhere in the accepted runtime mutates state.era, grep found only reads and pass-throughs (actions.ts:493, tick.ts:548, save.ts:5035/5049/5274/5313). The FIELD exists; era advancement does not. P13 must not duplicate EraConfig; it must supply the missing driver and must not add a second calendar to schedule it. |
| A rival technology tree (research, adoption, standardization per studio) | `src/core/hollywoodTypes.ts`<br>`RivalBusiness`<br>79-95 | **NEW P13** | There is no attachment point on the rival side at all, rival facilities carry no blueprintId and never enter placement/construction. P13 must not create per-rival technology state; the handoff's own line is that a rival technology tree was not implemented and must not be inferred. |
| Whether rivals already run a second player-finance formula | `src/core/economy.ts`<br>`openTheatricalRun`<br>53-70 | **REUSED** | No second finance formula exists today. The only divergence is the facilityOpex SOURCE (rivalCapacityOpex off an abstract list vs weeklyPlacementOperatingCost off state.placement) and the separate RivalAccount ledger. P13 must keep rival revenue on the shared economy builders. |

**Hazards.**

- Changing any rival facility id, name, capability or capacity breaks the exact-match assertion at hollywoodValidation.ts:267-269 against rivalStartingFacilities(b.studioId), 'rival facilities differ from their costed configuration'. Every existing save then refuses to load. Note assertStudioOperationsInvariants runs for rivals under facilityPolicy 'configured' (operations.ts:745-763), which asserts NOTHING about the facility set, so the Hollywood validator is the sole authority here.
- movements.facilityOpex is reconciled in closed form as `-elapsed*rivalCapacityOpex(b)` (hollywoodValidation.ts:241), where elapsed = market.tick - enteredWeek and rivalCapacityOpex reads the CURRENT facility list. Any mid-campaign capacity change retroactively invalidates every prior week's opex, not just future weeks.
- movements.capacity must equal exactly -(BASELINE_DEVELOPMENT_CASTING_CAPEX + STAGE_STANDARD_CAPEX + SCENERY_SHOP_CAPEX + POST_BUILDING_CAPEX), 'capacity acquisition not paid exactly once' (hollywoodValidation.ts:228-229). A rival purchasing capacity or technology through the 'capacity' kind is unlawful under the current save.
- RIVAL_MONEY_KINDS is a closed union checked structurally by `exact(p.movements,RIVAL_MONEY_KINDS)` (hollywoodValidation.ts:219) with a per-kind sign rule (studioRevenue >= 0, all others <= 0, line 220). Adding a kind changes every persisted RivalFinancePeriod and needs a save migration.
- Every rival type is key-exact at the save boundary: RivalBusiness 14 keys (validation:204), RivalAccount 4 (212), RivalFinancePeriod 5 (217), RivalProjectCosts 8 (282), HollywoodState (59). Adding one field to any of them refuses every existing save.
- Period chronology requires p.fromWeek >= enteredWeek and throughWeek <= market.tick (hollywoodValidation.ts:215-218). No invented retrospective rival research, expenditure or technology history can be persisted in the account, the validator refuses it structurally.
- Rival throughput is bounded and validated: one production at a time (`if(b.productions.length!==0)break`, hollywoodTick.ts:146), at most 2 active screenplays (hollywoodTick.ts:176, validated at hollywoodValidation.ts:259 'bounded unique active screenplay index'), soundstage capacity 1. A P13 consequence that raises rival output breaks the validated bound.
- Reserved rival IDs (manifest rows 5-9, arrival weeks 520/988/1560/1872/2548 in calendar.ts:3) have enteredWeek null and NO RivalBusiness. Validation refuses a film (141), contract (160) or business (205) owned by an un-entered identity. P13 must not give a reserved ID technology state, capacity, an account, or treat it as an employer.
- There is no dormant/closed/bankrupt rival arm: grep for bankrupt\|dormant\|closure\|closed\|exit\|shutdown\|insolven across src/core/hollywood*.ts returns nothing. Negative Cash has no consequence anywhere. P13 must not infer rival exit behavior.
- Rival entry is driven by the state's own market.tick (tick.ts:1038-1042, enterRival(...,'scheduled')) and enterRival throws 'Rival is not due for entry' if eligibleWeek > market.tick (hollywood.ts:143). Anything P13 adds that advances rivals off a clock other than this campaign's own tick would break the shared-timeline-within-a-campaign invariant.
- b.projects is index-aligned with b.development.projects and the validator reads `b.development.projects[ordinal]` for `b.projects[ordinal]` (hollywoodValidation.ts:283-284). Any P13 insertion, reordering or deletion in either array silently desynchronizes the cost-to-screenplay join.

**Open in this area.**

- Whether any player-build or source-manifest Unity code (deca39521da1baeca61898d156a43f4ae6a7e035 / 2bc8d304b79a72bf20fda1d462ec3d96df253992) consumes RivalAccount, RivalBusiness, RivalFinancePeriod or EraConfig. I inspected only the accepted runtime TypeScript and did not query the Unity repo for this area.
- Whether save.ts carries a version-bump/migration mechanism that could lawfully widen RIVAL_MONEY_KINDS or the RivalBusiness key set without refusing existing saves. I read hollywoodValidation.ts (the refusal authority) but did not trace the V18/V19 migration chain in save.ts.
- Whether the P13 catalogue or owner-direction docs in this repo name a specific owner for era advancement. I deliberately did not treat any doc as evidence of an interface; the code facts above stand on their own.
- Whether TUNING.HOLLYWOOD_NEGATIVE_CHOICES, HOLLYWOOD_PROMISE_HALF_WIDTH or HOLLYWOOD_POLICY_PREFERENCE_COST would need era-dependence for a P13 consequence to register in chooseIndustryPackage's score. I read the chooser but did not sweep the tuning values.
- Whether the four EraConfig fields are surfaced in any UI read model (ui/src/**). I searched src/core only.

#### 1.4 People, employment, contracts, assignments and career facts

**Finding.** No research-staff role, laboratory seat assignment, or research payroll charge exists anywhere at 592e926, "Scientist" and "Laboratory" appear only in planning markdown, never in a source file, so every named Scientist is NEW P13 WORK against closed unions (CreativeRole, Discipline, FacilityCapability, LedgerKind, and a six-key Contract), while P12's employer indexes, contract-interval algebra and one-employer exclusivity are reusable as-is.

*NEW P13 WORK 5 · REUSED 6 · SHARED GENERALIZATION 1*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| Exact contract identity and contract-interval algebra (who is under contract, from when to when) | `src/core/employment.ts (+ src/core/industryEmployment.ts, src/core/hollywood.ts)`<br>`activeContract / isContracted; contractId minting`<br>employment.ts:90-102; industryEmployment.ts:30; hollywood.ts:180; hollywoodValidation.ts:163 | **REUSED** | Half-open [startWeek, endWeekExclusive) is the ONLY interval rule; validation line 163 pins the id string exactly. P13 must call activeContract, not re-derive the comparison, and must not mint a third contract-id grammar. |
| Current-employer index and active-employment index (which studio holds a person this week) | `src/core/hollywood.ts`<br>`rivalEmployment; studioEmployerId; HollywoodState.activeEmploymentOrdinals`<br>hollywood.ts:44-65; hollywoodTypes.ts:115-116; hollywoodValidation.ts:180 | SHARED GEN. | rivalEmployment is already consumed by employment.ts (4 call sites), reuse as-is. studioEmployerId is the single 'who employs this person' authority but has zero external callers; P13 becomes its first caller. New caller only, NO signature widening. Owner: P13. |
| Lawful busy / availability check for any new P13 assignment | `src/core/employment.ts`<br>`busyTalentIds (+ activeProductionCompanyTalentIds, activeWritingAssignmentIds, industryBusyTalentIds)`<br>employment.ts:124-164; hollywood.ts:67-79; productionPeople.ts:4-11 | **REUSED** | If P13 ever makes a person unavailable, it must add that source INSIDE busyTalentIds, not build a parallel availability set. employmentStatus (employment.ts:372-383) is first-match priority over it and must stay the only status derivation. |
| One-employer exclusivity binding on any P13 hire | `src/core/hollywoodValidation.ts (+ src/core/hollywoodTick.ts, src/core/actions.ts)`<br>`validateHollywood interval checks; rival hiring `unavailable` set; applySignContract`<br>hollywoodValidation.ts:169-176, 401; hollywoodTick.ts:108-110; actions.ts:2587-2589, 2623-2627 | **REUSED** | Exclusivity is enforced three ways: save validation (no overlapping intervals per person, no double employer at the tick), the rival hiring loop's `unavailable` set (busy ∪ player contracts ∪ founding applicants ∪ every active industry employment), and applySignContract's hiringMarketIds gate. P13 must not add a fourth enforcement point. |
| Where P10 ownership ends and P12 transition recording begins | `src/core/industryEmployment.ts (+ src/core/actions.ts)`<br>`recordPlayerEmployment; sameContractTerms`<br>industryEmployment.ts:4-35; actions.ts:3019; hollywood.ts:135 | **REUSED** | state.contracts (P10) is the mutation authority; hollywood.employment + receipts (P12) is a derived mirror reconciled after each action, and validation:401 asserts every active player contract has its observation row. P13 must write P10 contracts and let the mirror follow, never append an employment row directly. |
| "A credit alone does not prove employment", the code meaning | `src/core/employment.ts (+ bridge/industry.ts, src/core/hollywoodValidation.ts)`<br>`creditedWriterIds vs busyTalentIds; industry employer map; employer-authority requireFacts`<br>employment.ts:104-116, 144-164; bridge/industry.ts:34-36, 65; hollywoodValidation.ts:254, 318 | **REUSED** | Three separable facts in code: (a) creditedWriterIds is a distinct set never unioned into busyTalentIds; (b) bridge/industry.ts builds its employer map ONLY from activeEmploymentOrdinals + current state.contracts, never from f.credits; (c) the implication runs one way only, validation demands a credit be BACKED by an employment interval ('rival production lacks its historical employer authority'), it never infers one. A P13 research credit must follow the same three rules. |
| A "named Scientist" as a person in the world | `src/core/types.ts (+ src/core/tuning.ts, src/core/worldgen.ts)`<br>`CreativeRole; Discipline; ROLE_TO_DISCIPLINE; generateIndustryTalent`<br>types.ts:18, 45, 107-140; tuning.ts:1811-1816; worldgen.ts:545-549 | **NEW P13** | Both unions are closed at four. A Scientist is therefore either a fifth CreativeRole (cascading into Talent.skills/ceilings/devRate/genreExperience/workHistory, all Record<Discipline,...>, plus ROLE_BLOCKS, salaryCurve, FOUNDING_MINIMUMS, rosterCoverage and the save leaf validators) or a non-Talent entity. P13 must NOT duplicate Talent, salaryCurve, or the offerForTalent pricing curve, those are reusable whichever shape is chosen. |
| A research-staff role and a payroll charge for one | `src/core/employment.ts (+ src/core/types.ts, src/core/save.ts)`<br>`weeklyPayroll / annualPayroll; LedgerKind; v8Contract`<br>employment.ts:183-198; types.ts:352-395; save.ts:1832-1859 | **NEW P13** | Payroll is exactly Σ over state.contracts and nothing else; v8ExactKeys pins Contract to exactly six keys with no optionals, so a role/lab/research field on a contract is a save-schema version bump (currently protocol 4 / projection 29 / inner Save V19). P13 must route any research wage through a real Contract + a ledger kind of its own, never a side account, and never an untyped negative. |
| A laboratory seat assignment | `src/core/types.ts (+ src/core/blueprintRequirements.ts, src/core/presence.ts, src/core/hollywoodValidation.ts)`<br>`FacilityCapability; NOT_YET_ATTAINABLE_KINDS.research; PresenceEngagement / ROSTER_HOME_FACILITY; claimAssignment`<br>types.ts:548-552; blueprintRequirements.ts:62-68; presence.ts:64-108; hollywoodValidation.ts:187-202 | **NEW P13** | There is no lab capability, no lab facility, no research engagement kind (PresenceEngagement is 'production'\|'script'\|'casting'\|'roster') and no home site for a research role (ROSTER_HOME_FACILITY is Record<CreativeRole,...>). P13 must NOT duplicate occupancy.ts's occupiedResourceSlots union producer or presence.ts's beat projection, a lab seat must register as a root inside them. |
| Seating a Scientist at a rival studio | `src/core/hollywoodStartingData.ts (+ src/core/hollywoodValidation.ts, src/core/hollywoodTick.ts)`<br>`RIVAL_TEAM_ROLES / RIVAL_CREDIT_ROLES; six-credit rule; payroll & overhead reconciliation`<br>hollywoodStartingData.ts:46-47; hollywoodValidation.ts:150, 236-240; hollywoodTick.ts:114-135 | **NEW P13** | A rival holds exactly six seats, refilled slot-by-slot each cadence. Adding a seventh would break three literal reconciliations ('signing bonuses do not reconcile', 'payroll does not reconcile with employment intervals', 'overhead does not reconcile', which multiplies OVERHEAD_PER_EMPLOYEE by person-weeks) and the authored six-credit rule. P13 must not widen this to give rivals research staff, the handoff already states P12 implemented no rival technology tree. |
| Era progression touching labour cost, salaries or payroll | `src/core/worldgen.ts (+ src/core/types.ts, src/core/tick.ts)`<br>`EraConfig; salaryCurve`<br>types.ts:284-289; worldgen.ts:161-167, 658-663; tick.ts:548 | **NEW P13** | state.era exists but NEVER advances and is not in HollywoodState; its single consumer is costScale in the negative-cost formula (candidates.ts:286, filmPackage.ts:97). It does not price salaries, payroll, overhead or termination. A P13 era timeline must key off market.tick (no second calendar), must not retro-fit an era history, and must not silently start scaling salaryCurve. |
| Surfacing a Scientist contract in the shipped player build | `Assets/Studio/Runtime/Infrastructure/StudioPersonContractContracts.cs (+ StudioRosterContracts.cs)`<br>`StudioPersonContractContracts; StudioRosterContracts.Filter.Profession`<br>StudioPersonContractContracts.cs:7-14, 51-59; StudioRosterContracts.cs:29-37, 54-56 | **REUSED** | The P10 contract sheet is a pure phraser over engine quotes, and the roster filter is role-agnostic (OrdinalIgnoreCase string compare, not an enum), so the Unity client is not the blocker for a new profession, the blocker is engine-side. No 'scientist', 'laboratory' or 'research' path or token exists in the player-build tree at deca3952. P13 must supply new wire snapshots, never new client math. |

**Hazards.**

- Design docs name the feature, code does not: PROJECT-STUDIO-SUCCESS-BLUEPRINT.md:871 says "a Laboratory with hired Scientists drove four research tracks" and THE-MOVIES-PARITY-MASTER-PLAN.md:336 names "Laboratory/research as the unlock engine". Those are the ONLY 'Scientist'/'Laboratory' hits in the repo, all in markdown. Zero source files at 592e926 contain either token. Treating those docs as an existing interface would be the single biggest launch error in this area.
- Adding a fifth CreativeRole or Discipline breaks exhaustiveness in every Record<Discipline,...> on Talent (skills, ceilings, devRate, genreExperience, workHistory), plus ROLE_TO_DISCIPLINE (tuning.ts:1811-1816, `satisfies Record<'writer'\|'director'\|'actor'\|'craft', Discipline>`), ROLE_BLOCKS in generateIndustryTalent (worldgen.ts:546 uses a non-null `!` that would throw on an unknown role), FOUNDING_MINIMUMS, rosterCoverage, foundingGaps and the save leaf validators.
- Any research wage that is NOT a row in state.contracts silently escapes weeklyPayroll, annualPayroll, weeklyOverhead (which multiplies OVERHEAD_PER_EMPLOYEE by state.contracts.length, economyView.ts:47/81), the canAfford solvency gate, weeklyBurn/runway, and the V19 ledger reconciliation, a second player-finance path by omission.
- v8Contract (save.ts:1838-1850) calls v8ExactKeys with six required keys and an EMPTY optional list. A research role, lab id, or track id on a Contract is a hard save-schema break at protocol 4 / projection 29 / inner Save V19, not an additive field.
- hollywoodValidation.ts:160 requires `studio?.enteredWeek !== null` for any contract owner. Assigning a research seat to one of the nine reserved rival IDs before its fixed arrival week throws 'contract owner inactive/unknown', reserved IDs are not employers.
- claimAssignment (hollywoodValidation.ts:187-190) throws 'person <id> has simultaneous active assignments' on a second claim. A laboratory seat layered onto someone already in a production company or holding a writing assignment aborts save validation on load.
- FOUNDING-PHASE HOLE: applySignContract (actions.ts:2600-2611) checks only founding.applicantIds and the recruitment budget, it does NOT call rivalEmployment or hiringMarketIds. Exclusivity there is closed only from the rival side (enterRival reserves applicants at hollywood.ts:163, and validation:174 asserts 'rival stole founding applicant'). A P13 pre-founding or founding-window research hire on a different code path would reopen it.
- hollywood.ts:44 `employmentByPerson` is a module-level WeakMap keyed by the employment ARRAY OBJECT, which is why it survives Save As copies that share talentIds. A P13 index keyed by talentId, contractId, or any campaign-invariant entity ID would leak state across Save As copies and across inactive campaigns.
- Contract (types.ts:336-343) carries no studioId, the player employer is implicit in state.contracts. Deriving employer from anything other than state.contracts ∪ hollywood.employment creates a second employer authority, which the handoff forbids.
- state.era never advances anywhere in tick.ts (tick.ts:548 only copies it forward). A P13 era timeline must be driven by market.tick within each campaign; a per-era clock would be the second calendar the invariants prohibit, and must not advance in inactive campaigns.

**Open in this area.**

- Whether any P13-specific design document (beyond the P12->P13 handoff and the P12 R05 Owner acceptance receipt, both of which I read) names a Scientist/Laboratory interface, and per the evidence rule a doc naming one would not change the NEW P13 WORK classification anyway.
- Where the Unity roster's Profession filter CHIP LIST comes from (engine-supplied vs client-enumerated). I read StudioRosterContracts.Filter/Matches at deca3952, which treats profession as a free string, but did not trace the view that populates the chip values.
- Whether the tools/source-manifest identity 2bc8d304b79a72bf20fda1d462ec3d96df253992 or the published technical-evidence identity d4e1915ba075b4e4c1c9a6c880c8b0d4257659c0 contain research or scientist vocabulary. I inspected only the runtime TS sha 592e926 and the actual player-build source deca3952.
- The full contents of hollywoodValidation.ts (447 lines), I read the employment, assignment, credit-authority, reconciliation and receipt sections by targeted grep plus lines 25-60 and 120-215, not the whole file. Checks outside those ranges are unexamined.
- Whether ui/src (the web UI) exposes any employment surface that a research role would have to appear in. My token sweep for scientist/research/laboratory covered the whole tree and found nothing in ui/src, but I did not inspect ui/src employment components structurally.

#### 1.5 P09 facilities, reservations and physical production

**Finding.** Placement Core V12 in src/core/placement.ts already owns the whole build lifecycle P13 needs, catalog, legality, capex, weekly progress, capability grant, opex and demolition, but it contains no Laboratory blueprint, no research/laboratory capability, and no conversion or retrofit job type of any kind, so P13's Laboratory and physical conversion are new work that must be built as additional P09 catalog rows and a new P09 job arm rather than as a second construction authority.

*NEW P13 WORK 3 · REUSED 5 · SHARED GENERALIZATION 4*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| The buildable blueprint set with capex / buildWeeks / weeklyOperatingCost that P13's catalogue must price against | `src/core/tuning.ts`<br>`FACILITY_BLUEPRINTS`<br>1194-1204 (entries authored at 898-1184; TUNING numbers at 584-596, 684-732) | **REUSED** | P13 must read prices from this list, never restate them. Blueprints are authored constants and never persisted ('a placed facility stores only its blueprintId'), so a P13 catalogue row is an addition here, not a parallel table. Paper rates in the P13 catalogue ($3,000/wk laboratory, $1,200/wk converted stage) match no accepted row. |
| A Laboratory blueprint for P13 research | `src/core/tuning.ts`<br>`FACILITY_BLUEPRINTS (absence) / FacilityCapability (src/core/types.ts:548-552)`<br>tuning.ts:1194-1204; types.ts:548-552 | **NEW P13** | P13 must author a Laboratory blueprint and a new capability arm. It must NOT duplicate FacilityBlueprint, the legality query, the capex charge, the completion pass or the opex charge, all nine required blueprint fields (incl. effectSummary ending in a period) are enforced by assertBlueprintCatalogInvariants, placement.ts:1145-1295. |
| How a construction job is represented and who owns its progress | `src/core/placement.ts`<br>`completeDuePlacements / PlacedFacility`<br>placement.ts:762-815; types.ts:1040-1058; sole caller tick.ts:427-431 | **REUSED** | There is no percent-complete, no partial progress and no pause: a job is a committed completion week that flips once. P13 must not introduce a second progress clock or a second weekly completion pass; tick.ts step 1.6 is the one caller and it runs after script/casting/production allocation, before release. |
| How a facility capability is granted on completion | `src/core/placement.ts`<br>`placedStudioFacility + the capacity>0 registry gate inside completeDuePlacements`<br>313-331 and 799-802 | **REUSED** | A capability is granted only by appending a StudioFacility to operations.facilities at completion. A capacity-0 building becomes operational, pays opex and satisfies a `facility` requirement but never enters the allocator. P13 must decide whether a Laboratory provides shared slots (capacity>0, enters the registry) or is effect-only; it may not invent a third path. |
| A conversion / retrofit job type for P13 physical conversion | `src/core/placement.ts`<br>`PlacementStatus / LedgerKind (absence)`<br>types.ts:1040; types.ts:378-394; placement.ts whole file | **NEW P13** | P13's physical conversion is entirely new work. The only accepted mutations of a standing building are moveFacility (cost FACILITY_MOVE_COST = 0) and demolishFacility. A conversion must be a new P09-owned job arm on the existing PlacedFacility record and tick step 1.6, never a second construction authority, second ledger family or second completion pass. |
| Reservations and occupancy for P13 laboratory seats and named researchers | `src/core/occupancy.ts`<br>`ResourceOwnerKind / ResourceClaim / occupiedResourceSlots`<br>90-112, 119-186, 274-287, 301-495 | SHARED GEN. | A research/researcher hold needs a NEW arm in ResourceOwnerKind + ResourceClaim + resourceClaims(), plus a new OccupancySources root. Owner is occupancy.ts (shared P09/operations authority), not P13. P13 must not keep a private seat count: findDoubleBookedResourceSlot/assertNoDoubleBookedResourceSlots (occupancy.ts:609-643) are the only double-booking proof. |
| Adding a 'laboratory' (or research) facility capability | `src/core/types.ts`<br>`FacilityCapability + ACQUISITION_RANK_BY_CAPABILITY + OPERATIONS_CAPABILITIES`<br>types.ts:548-552; productionPhases.ts:129-140; save.ts:864-886, 2350-2357, 2523-2534 | SHARED GEN. | Three co-owned edits, none of them P13's alone: the union (types.ts), the exhaustive rank record (productionPhases.ts, charter §3.2 deadlock law), and the save vocabulary (save.ts). The presence law at save.ts:2523 is guarded by !placementAwarePolicy(policy), so a new capability must not be added to the V8-V11 presence list or every legacy history fails to load. |
| Gating a blueprint on an era / calendar date | `src/core/blueprintRequirements.ts`<br>`LIVE_REQUIREMENT_KINDS / blueprintRequirementMet case 'date'`<br>49-54, 141-144; reason copy 104-107; types.ts:903 | **REUSED** | The evaluator needs no change, P13 authors data only. But ZERO shipped blueprints carry a date requirement today (only development-office-3 authors any requirement at all, a `facility` one), so the path is live and unexercised by catalog content. The gate reads state.market.tick, the one campaign calendar; P13 must not add a second. |
| Gating a blueprint on completed P13 research | `src/core/blueprintRequirements.ts`<br>`NOT_YET_ATTAINABLE_KINDS.research / blueprintRequirementMet case 'research'`<br>62-68, 160-168; types.ts:921; catalog check placement.ts:1279-1281 | SHARED GEN. | The vocabulary, the player copy, the notYetAttainable flag and the catalog validation already exist. Activation is exactly two edits IN blueprintRequirements.ts: add a real `case 'research'` reading P13's state root, and move 'research' into LIVE_REQUIREMENT_KINDS. Owner is P09's evaluator; P13 owns only the state root it reads. No new requirement kind, no new rejection code. |
| Weekly operating cost for a Laboratory or a converted building | `src/core/placement.ts`<br>`weeklyPlacementOperatingCost`<br>375-386; charged at tick.ts:935-954; note tuning.ts:1207 | **REUSED** | A P13 building pays opex automatically the moment it is an operational placement with a catalog row, P13 must write no opex code and no second facility charge. A conversion that changes opex must change it by changing which blueprint the placement points at, or this Σ will not see it. |
| Cancelling / disposing of a committed construction or conversion (P13 'Option B') | `src/core/placement.ts`<br>`demolishFacility / facilityDemolitionRefund`<br>1026-1028, 1054-1096; fraction tuning.ts:1613; bound asserted placement.ts:1151-1156 | **NEW P13** | There is no abort verb: cancelling committed work today means demolishing the site for a rounded half of capex, writing off everything paid. P13's atomic disposition needs its own authored components and must not be computed from this credit. Any P13 refund path must keep the strictly-lossy invariant or assertBlueprintCatalogInvariants throws. |
| Era cost scaling applied to facility capital cost | `src/core/placement.ts`<br>`quoteForBlueprint (chargedCost)`<br>565 (within 523-658); EraConfig types.ts:284-289; worldgen.ts:658-663 | SHARED GEN. | era.costScale today multiplies only film negative cost (candidates.ts:286, filmPackage.ts:97, forecast.ts:145, reception.ts:264, studioRunRecap.ts:495). If P13 wants era-scaled building costs, that is one widening of chargedCost inside quoteForBlueprint, owned by placement.ts, and it must flow through the same query commitPlacement re-runs, a caller may never supply a cost. |

**Hazards.**

- Adding a capability arm to FacilityCapability without also extending ACQUISITION_RANK_BY_CAPABILITY (productionPhases.ts:129-135) is a compile break at best and a silent undefined rank at worst; the rank record is the stated anti-deadlock law (a workflow may only wait on a resource ranked strictly above everything it holds). A Laboratory that enters the production wait graph would change that law.
- Adding a new capability to save.ts OPERATIONS_CAPABILITIES (864-886) would extend the V8-V11 'four-capability presence law' at save.ts:2523-2534 and make every pre-V12 history fail to load ('managed operations have no facility for <capability>'). That law is deliberately skipped only under a placement-aware policy.
- A second construction authority breaks the runner invariant stated at placement.ts:29-33: commitPlacement calls queryPlacement itself and charges only the cost that query computed. Any P13 path that charges capex or conversion cost outside queryPlacement lets a caller supply a price.
- A conversion that mutates a PlacedFacility's blueprintId in place would silently rewrite that building's demolition refund and its opex, because both are derived from the CURRENT blueprint (facilityDemolitionRefund, weeklyPlacementOperatingCost) while the surviving constructionCapex row is correlated only by projectId and identified after demolition by ledgerNote (blueprintByLedgerNote, placement.ts:255-260). Two blueprints sharing a ledgerNote is refused by invariant.
- Any P13 hold on a facility MUST be added to facilityEngagements (placement.ts:829+) as well as to occupancy.ts. The module header (placement.ts:52-79) states the five persisted holders exhaustively and warns a dangling facilityId 'does not degrade, it THROWS, out of studioCalendar and scriptCapacityView'. A researcher holding a Laboratory that move/demolish cannot see is exactly that bug.
- Authoring a {kind:'foundingOffice'} requirement on a blueprint throws: assertBlueprintCatalogInvariants (placement.ts:1241-1293) has no case for it and falls to the default 'unknown requirement kind'. It is regime-derived only (blueprintRequirements.ts:275-283).
- Reserved ground is per-parcel and absolute: RESERVED_PARCEL_BLUEPRINTS (placement.ts:224-226) refuses any blueprint but the Annex on the legacy 'expansion' parcel, and refuses ANY move onto it. A P13 Laboratory placed or converted there is rejected as 'groundReserved', which ranks above 'occupied'.
- P13 must not treat a placed Laboratory as shareable across campaigns. All placement state lives in GameState (state.placement, state.operations, state.ledger) and I found no module-level mutable cache in placement.ts, construction.ts, blueprintRequirements.ts, facilityEffects.ts or occupancy.ts, every index (occupiedCellKeys, groundOccupiedCellKeys, occupiedResourceSlots) is rebuilt per read. Any P13 memoized research/lab cache would be the first violation of that law and would leak across Save As copies that share facility ids.

**Open in this area.**

- Whether the Unity/native player-build (deca39521da1baeca61898d156a43f4ae6a7e035) or the source manifest HEAD (2bc8d304b79a72bf20fda1d462ec3d96df253992) has any Laboratory prefab, conversion animation or build-mode affordance. I inspected only the runtime TypeScript at 592e926bfbf4574df94b38fc8dd594fc5df2ac8d and did not call gh api.
- Whether ui/ (the UI layer) holds any facility catalog copy that would also need a Laboratory row. I searched src/core only; PlacementCatalogView (placement.ts:2089-2140) is the engine-side surface a UI is supposed to render, but I did not confirm the UI consumes only that.
- The exact tick-order position a P13 research advance would take relative to tick.ts steps 1.5/1.6/1.7 (construction, placement, set completion). The comments assert those three are 'INSERTION, NOT A REORDERING' under D-12 §9, but I did not read docs/D-12-economy-contract.md to confirm a fourth insertion is permitted.
- Whether the P12->P13 producer handoff (13370d428f0693f3279732f6f4cc360a7fcaa4df:docs/engineering/P12-TO-P13-PRODUCER-HANDOFF.md) or the Owner acceptance receipt constrains facility work beyond the one financial-boundaries row I read, which mentions 'facility Opex' only in passing. I did not read either document in full.
- Whether tests/ pin the catalog to exactly nine blueprints (which would make adding a Laboratory a test-fixture change too). I did not read the test suite and ran nothing.

#### 1.6 P11 cost consequences and charge onset

**Finding.** Charge onset IS determinable in code, not just on paper: tick step 7.6 charges the pre-advance placement snapshot, so a facility whose completesWeek is W is first charged as a ledger row stamped week W during the advance W → W+1, and no during-work rate exists anywhere in the accepted runtime.

*NEW P13 WORK 1 · REUSED 7 · SHARED GENERALIZATION 4*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| One player wallet and one signed ledger for every P13 cost | `src/core/types.ts`<br>`Studio.cash / LedgerEntry / LedgerKind`<br>291-296, 352-394, 416-435 | **REUSED** | There is exactly one player wallet (state.studio.cash) and one append-only signed ledger. RivalAccount.cash (src/core/hollywoodTypes.ts:56-61) with RivalMoneyKind incl. 'facilityOpex' is a SEPARATE per-rival account. P13 must not create a second wallet and must not route a player adoption cost through moveRivalMoney. |
| When a weekly facility operating charge is actually applied | `src/core/tick.ts`<br>`tick step 7.6 PLACED-FACILITY OPERATING COST`<br>935-954 | **REUSED** | One aggregated row per week, charged against `state.placement` (the START-of-advance snapshot), deliberately NOT the post-completion set. The header states it: 'a site that becomes operational during this advance was still a construction site for the week being charged, so its first operating charge lands on the NEXT advance.' This is the code answer the catalogue reconciled on paper; do not re-derive it. |
| When a construction job flips to operational, relative to that charge | `src/core/placement.ts`<br>`completeDuePlacements`<br>762-773 (caller src/core/tick.ts:427-431) | **REUSED** | Exact onset: a facility with completesWeek W flips operational during the advance W-1 → W (arrivalWeek W), is NOT charged for week W-1 (step 7.6 read the pre-flip snapshot), and is first charged as a ledger row stamped `week: W` during the advance W → W+1. Charged weeks are therefore [W, ...] inclusive, which matches the catalogue's C2 convention ('a body operational in week w is charged 53 − w weeks'). |
| A during-work versus after operating rate | `src/core/placement.ts`<br>`weeklyPlacementOperatingCost (and FacilityBlueprint, src/core/types.ts:958-971)`<br>375-386 | **NEW P13** | ABSENCE IS THE FINDING. No during-work rate exists: `git grep -ni 'during work\|duringWork\|during construction'` over src/, bridge/ and ui/ at this sha returns ZERO hits, and the blueprint type has no second rate field. An underConstruction site pays nothing. The two-rate card ('$5,500/week during work, $7,000/week after') cited in the catalogue is a card shape, not accepted TypeScript. If P13 wants one it is new work on FacilityBlueprint + weeklyPlacementOperatingCost + expectedWeeklyOperatingCostAt together, and it must not be added to only one of them. |
| Onset is not merely conventional, it is enforced against every historical row | `src/core/placement.ts`<br>`expectedWeeklyOperatingCostAt + the opex reconciliation invariant`<br>1386-1417, 1757-1771 | **REUSED** | Every past facilityOpex row is re-provable from the durable placement record plus the capex/refund ledger pair. Any P13 multiplier or surcharge applied to facility opex must be applied identically in BOTH the charge site and this expected function, or every pre-P13 save fails validation on load. |
| The one weekly-burn formula a P13 recurring adoption cost would have to land in | `src/core/economyView.ts`<br>`weeklyBurn / weeklyFacilityOperatingCost / financeView`<br>45-72, 628-649 | SHARED GEN. | weeklyBurn is described in-file as 'the ONE authoritative burn every player-facing surface reads'. A recurring P13 adoption/upkeep cost becomes a fourth summand HERE, once, owned by the economy module, plus a matching fourth gated block in tick.ts beside 7/7.5/7.6. Do NOT add a parallel P13 burn formula; runway, netWeeklyCashflow and financialConsequence all derive from this single call. |
| Where a NEW ledger kind is forced to declare itself (compile-time gates) | `src/core/economyView.ts`<br>`KIND_FIELD + periodSummary exhaustiveness guard`<br>455-491, 594-599 | SHARED GEN. | A P13 adoption cost as a new LedgerKind breaks, at compile time, KIND_FIELD (economyView.ts:455-491), FINANCE_CATEGORIES (financeReport.ts:6-14, a total Record<LedgerKind,string>) and periodSummary's never-guard; and at runtime the versioned save kind lists (src/core/save.ts:965 V12_LEDGER_KINDS, :6461). That makes it a SAVE-FORMAT change, not a display change. Owner: whoever owns economyView + save migration, not P13 alone. |
| How payroll, project spending and capital are kept separate | `src/core/types.ts`<br>`LedgerEntry discriminated union`<br>400-440 (facilityOpex arm 427-435) | **REUSED** | Separation is by KIND plus a type-enforced correlation field: payroll/overhead/facilityOpex = studio-wide and uncorrelated; production/freelancerFee/publicity = per-film via productionId (bridge/finance.ts:41); constructionCapex/setCapex/refunds = per-project via constructionProjectId. A per-technology itemized adoption charge CANNOT reuse facilityOpex, that kind forbids every correlation field and the invariant forbids a second row in a week. |
| Retrospective per-film fixed-cost attribution and what it does NOT include | `src/core/fixedCostAllocation.ts`<br>`ledgerFixedCostByWeek / allocateFixedCosts`<br>131-143, 205-216 | **REUSED** | The R7 invariant is 'Σ per-film allocated + idle === Σ ledger payroll+overhead' EXACTLY, in whole dollars. Facility operating cost is already outside it while being inside weeklyBurn. P13 must document that asymmetry rather than 'fix' it: adding any kind to this filter redefines the stated total and invalidates the negative-control test. Also note the module requires integer dollars and throws on a non-integer row. |
| An era cost lever that already exists, and its exact reach | `src/core/types.ts`<br>`EraConfig.costScale (GameState.era)`<br>284-289, 479 (worldgen.ts:657-663; filmPackage.ts:97) | SHARED GEN. | state.era is a real persisted field and costScale already multiplies requiredNegative in six places (candidates.ts:286, filmPackage.ts:97, forecast.ts:145, reception.ts:264, studioRunRecap.ts:495, hollywoodPolicy.ts:45). It reaches FILM negative cost ONLY, not payroll, not overhead, not facility opex. And era is never mutated after worldgen: tick.ts:548 is a pass-through `era: state.era` and the finalize block writes no era. P13 must not invent a second era scalar; widening this one to vary over time is the owned change, and it is the economy owner's. |
| Player-facing onset copy already published by the bridge | `bridge/finance-upcoming.ts`<br>`financeUpcoming (facilityOpex event)`<br>19-31 (and bridge/finance.ts:33-36 onsetLine) | **REUSED** | Three independent surfaces already agree on onset (tick 7.6, expectedWeeklyOperatingCostAt, this bridge copy). P13 reuses this event shape and onsetLine verbatim; it must not author a fourth onset sentence. Note the deliberate two-date convention below in hazards. |
| The decision-time cost-consequence surface a P13 adoption decision would use | `bridge/finance-consequence.ts`<br>`financialConsequence`<br>6-23 | SHARED GEN. | The 'later' arm already models exactly a delayed-onset charge: the OWNING action builds and discards a future state, and finance only reads it. A P13 adoption decision reuses this rather than computing its own before/after. What must be widened: the hardcoded `exclusions` and `currentBasis` strings name only contracts, facilities and theatrical receipts, and financialConsequence has no adoption-cost field. bridge/finance-route.ts (6 lines, 7 kinds, 'never authorize or execute an action') has no route kind for a technology or adoption target, adding one is a one-line widening owned by the bridge. |

**Hazards.**

- TWO CORRECT DATES, ONE ADVANCE. The first facilityOpex ledger row is stamped `week: currentTick` = completesWeek (tick.ts:948), while bridge/finance-upcoming.ts:27 dates the 'first operating charge arrives' event at completesWeek + 1. Both describe the same single advance. P13 documentation that treats these as a contradiction, or as two charges, will be wrong.
- A P13 modifier applied to facility opex at the charge site only would make EVERY historical opex row fail `invariant(expected > 0 && entry.amount === -expected)` (placement.ts:1766-1770) on load. The charge site and expectedWeeklyOperatingCostAt must change identically, or pre-P13 saves break.
- facilityOpex is one aggregated row per week with `constructionProjectId?: never` and `productionId?: never`, and placement.ts:1763 forbids a second row in a week. Any per-technology or per-building itemized adoption charge cannot be expressed on this kind; it needs its own kind, which is a save-format change (save.ts:965, :6461).
- Adding a new LedgerKind is not additive: FINANCE_CATEGORIES is a total `Record<LedgerKind, string>` (financeReport.ts:6-14), KIND_FIELD is total (economyView.ts:455-491), and periodSummary throws on an unhandled kind via a `never` guard (economyView.ts:594-598). All three plus the versioned save kind lists must be updated in the same change.
- weeklyBurn includes facility opex but fixedCostAllocation's per-film attribution does not (it filters to payroll\|overhead only, fixedCostAllocation.ts:134). A P13 adoption cost added to burn is silently absent from per-film break-even; adding it to the allocator instead redefines the R7 reconciliation invariant and invalidates its negative-control test. Pick one and say so.
- era.costScale is a campaign-lifetime constant (1.0, set once in worldgen.ts:662, never mutated; tick.ts:548 is pass-through). Every consumer recomputes requiredNegative from the CURRENT state.era at call time, so making costScale time-varying retroactively changes the quoted cost of packages already in flight across candidates.ts, filmPackage.ts, forecast.ts, reception.ts, studioRunRecap.ts and hollywoodPolicy.ts simultaneously.
- Rivals carry their own RivalAccount.cash with a 'facilityOpex' RivalMoneyKind and their own reconciliation (hollywoodTick.ts:282 moveRivalMoney; hollywoodValidation.ts:241 asserts `elapsed * rivalCapacityOpex(b)`). Routing a player adoption cost through that path, or a rival technology cost through state.ledger, creates a second player-finance formula and blurs the two wallets.
- Everything here is gated on `engaged && state.founding === null` (tick.ts:927, :943; economyView.ts:46, :54, :70). During a founding draft the engine charges NOTHING and weeklyBurn returns 0. A P13 adoption cost that skips this gate would debit a studio that does not exist yet.
- All of the money state read here (studio.cash, ledger, placement, era) lives on GameState, and none of these modules holds module-level mutable state or a cache (verified: no top-level `let`/`var`/`new Map` in economyView.ts, financeReport.ts, fixedCostAllocation.ts, placement.ts, bridge/finance.ts, bridge/finance-upcoming.ts). A P13 adoption cache introduced at module scope would break the Save-As campaign-scoping invariant.

**Open in this area.**

- Whether the first opex charge is ALSO suppressed when a facility completes and is demolished in the same window, the demolition arm (placement.ts:1412-1416) charges `if (demolished.completesWeek > week) continue; if (week >= demolished.demolishedWeek) continue`, but I did not trace a same-week complete-and-demolish case end to end.
- Set maintenance: I confirmed only ONE setMaintenance charge site (src/core/sets.ts:567-575, TUNING.SET_REPAIR_COST at repair commission, labelled 'One-time Set repairs' in FINANCE_CATEGORIES) and that no tick step charges sets weekly (tick.ts ledger.push sites are exactly 631 boxOffice, 767 studioRevenue, 920 payroll, 931 overhead, 947 facilityOpex). I did not audit sets.ts in full for any other recurring path.
- The P12→P13 producer handoff (13370d4:docs/engineering/P12-TO-P13-PRODUCER-HANDOFF.md) and the Owner acceptance receipt: my grep for onset/operating-charge language in both returned nothing, and I did not read either document in full. The catalogue comparison above is against docs/design/STUDIO-UPGRADE-AND-RESEARCH-CATALOGUE-01.md at working-tree HEAD, not against the handoff.
- Unity / player-build side (2bc8d30 manifest HEAD, deca395 actual player-build source): not inspected at all in this pass. No claim is made about whether any C# surface renders a during-work rate or an onset line.
- Whether any P11 read model other than the ones inspected (e.g. studioRunRecap, economyView's affordability scopes) would need a new field for a recurring adoption cost, I traced weeklyBurn's consumers only as far as financeOverview and financialConsequence.

#### 1.7 Public disclosure, Industry/History and native navigation

**Finding.** Every lawful public fact P13 needs is already produced by one entry-gated, ID-joined, session- and revision-pinned projection (bridge/industry.ts industryPage at 592e926b) whose secrecy is enforced by omission plus a serialized-envelope leak test, while era and research exist only as declared-but-dead stubs: state.era is a write-once constant with three inert fields, and the blueprint 'research' requirement kind is authored on zero blueprints and hard-returns false.

*NEW P13 WORK 1 · REUSED 8 · SHARED GENERALIZATION 3*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| Publish only studios that have actually entered; reserved rival IDs must never surface as studios or employers | `bridge/industry.ts`<br>`industrySummary / indexFor → studios`<br>21-25, 73 (plus src/core/hollywoodValidation.ts:160) | **REUSED** | P13 reuses this exact entered-filter for any technology/adoption row. Do not build a second studio registry. HAZARD: industry.ts:33 `names=new Map(h.identities.map(s=>[s.studioId,s.name]))` includes reserved, not-yet-entered identities, a P13 row that resolves a name by studioId without the enteredWeek filter would publish a reserved rival's brand as an active adopter. |
| Hidden rival Cash, salaries, policy and forecasts stay hidden | `bridge/industry.ts`<br>`indexFor / industryPage (RivalBusiness.account, .policy, .nextDecisionWeek are never read)`<br>26-101, 108-189 (types at src/core/hollywoodTypes.ts:56-95, src/core/types.ts:336-343) | **REUSED** | Secrecy here is by omission, not by redaction, there is no filter function to call. P13 must extend the existing guard rather than invent one: tests/bridge-p12-industry.test.ts:42-43 stringifies the whole served page and asserts it contains none of ['cash','account','annualSalary','negativeScale','affinities','expectedTotal','forecast','directCommitment','studioRevenueReceived','skillsActual']. Any P13 research/expenditure field name must be added to that list. |
| Joins are by exact ID; title and display-name joins are forbidden | `src/core/worldgen.ts`<br>`ID SCHEME (module header law), consumed by industry.ts Index.filmById / studioById / people and IndustryQuery.targetId`<br>44-49 (industry.ts:56, 89, 133, 171, 178, 181; industry-schema.ts:16 `targetId:nullable(id())`) | **REUSED** | Titles are display-derived with an id fallback and carry no uniqueness invariant anywhere in the engine. Studio NAMES are unique only within one campaign's identity list (hollywoodValidation.ts:84) and Save As copies duplicate them across campaigns, so a name join is unsafe even for studios. Every P13 research/adoption row must key on studioId / filmId / talentId and let the projection resolve the label. |
| The public projection boundary: session- and revision-pinned, bounded pages, no aliasing of retained state | `bridge/session.ts`<br>`BridgeSession.industry`<br>1312-1317 (industry.ts:111 page(), :188 structuredClone; industry-schema.ts:16 pageSize max 50) | **REUSED** | A P13 research read must enter through this same gate (or an identical sibling), not by reading GameState directly. The paging contract is already enforced: pageSize 1..50 in schema, and industry.ts:111 throws 'That page is outside this snapshot.' beyond pageCount. tests/bridge-p12-industry.test.ts:29-30 proves the consumer copy is detached, mutating a returned studio name does not reappear in the next page. |
| Caches and receipts scoped per campaign/session so Save As copies sharing entity IDs cannot collide | `bridge/snapshot-build-context.ts`<br>`snapshotBuildContextFor / contexts WeakMap (mirrored by industry.ts:28 `const indexes=new WeakMap<GameState,Index>()`)`<br>23-39, 105-116 (industry.ts:28-30) | **REUSED** | This is the only mechanism satisfying the Save As invariant, and it works BECAUSE the key is object identity, not an id. HAZARD: a P13 cache keyed by worldId, campaign storage id, studioId or filmId would collide across Save As branches that legitimately share those ids. Key on GameState identity, or route through snapshotBuildContextFor. |
| Only actually announced rival work is public; unannounced development and schedules stay private | `bridge/industry.ts`<br>`industryPage views 'studio' and 'project'`<br>150-151, 159-166 | **REUSED** | This is the disclosure gate a P13 research/adoption item must pass through: an unannounced rival research commitment has no announcedWeek analogue in the accepted code and therefore has no lawful public row. P13 must mint its own announcement receipt (owned by hollywoodTick/hollywood.ts, not by the bridge) before anything about rival research can be published. |
| A bounded, non-predictive way to describe observed rival behaviour over a window | `bridge/schema/industry-schema.ts`<br>`StudioIndustryTendency`<br>13 (producer at bridge/industry.ts:155-157) | SHARED GEN. | The row type is already generic enough for an observed-adoption tendency, so NO wire-schema change is needed, only new rows from the industry.ts producer (its owner, not P13's). Widening required: nothing structural; widening forbidden: the `basis` sentence must keep disclaiming forecasting. HAZARD: a 'rival will adopt X by week N' row breaks the stated no-forecast and no-rival-technology-tree invariants. |
| An era model that advances with the shared campaign timeline | `src/core/worldgen.ts`<br>`generateWorld → `const era: EraConfig` (type at src/core/types.ts:284-289; validator v8Era at src/core/save.ts:1667-1683)`<br>657-663 | **NEW P13** | state.era is written once at world generation and never reassigned; tick.ts:548 and actions.ts:493 only READ it. soundRequired/televisionCompetition/censorship have no reader anywhere in src/core, dead fields. P13 must not relabel EraConfig as its era system: save.ts:1669 `v8ExactKeys(era,['soundRequired','televisionCompetition','censorship','costScale'],[])` makes any added field a governed Save V19 / projection-29 migration, and costScale is already load-bearing in reception/candidates/filmPackage/forecast/hollywoodPolicy. No second calendar: era progression must derive from market.tick via campaignDate(). |
| A lawful gate that makes a facility depend on completed research | `src/core/blueprintRequirements.ts`<br>`LIVE_REQUIREMENT_KINDS / NOT_YET_ATTAINABLE_KINDS.research / blueprintRequirementMet case 'research'`<br>49-54, 62-68, 119-120, 160-168 (type src/core/types.ts:921; wire bridge/schema/bridge-schema.ts:529-534; producer bridge/placement.ts:203-207) | SHARED GEN. | The kind is declared, evaluated, wire-carried (`kind: nonEmptyText()`, no schema widening needed) and rendered natively, but authored on zero blueprints, so it is completely unexercised. Activation, per the module's own comment, is moving 'research' out of NOT_YET_ATTAINABLE_KINDS into LIVE_REQUIREMENT_KINDS plus one real `case 'research'` reading the state root P13 adds. Owner is blueprintRequirements.ts (C1-M2), which declares itself the ONE reader of unlock data, P13 must not add a second unlock evaluator. The copy string is rendered verbatim in the native catalogue and must change the same day, or the build screen tells the player research does not exist while gating on it. |
| A native lot-anchored workspace route a P13 research slice could attach to | `Assets/Studio/Runtime/Presentation/UI/StudioWorkspaceHost.cs`<br>`StudioWorkspaceHost.OpenBuild(string parcelId, string blueprintId)`<br>1157-1196 (route booleans declared 53-107; entry control Assets/Studio/Runtime/Presentation/StudioBuildCommandHud.cs:19,64,115, `hud-open-build` chip plus key B) | SHARED GEN. | This is the ONLY workspace route that keeps the lot live and the camera unsuspended, the realistic host for an on-lot research interaction, reached today from an on-lot chip. Absence is a finding: no file, symbol or asset named research / technology / innovation exists anywhere in the tree at deca395 (searched all paths, not just .cs), so the screen itself is new work. Widening required: 7 route booleans are reset across 55 `RouteOpen = false` assignments; a P13 route added without being included in every existing Open* reset would leave two routes open at once. The catalogue already renders unmet-requirement copy at Assets/Studio/Runtime/Presentation/UI/StudioBuildWorkspace.cs:1056-1066 (`UnavailableReason(entry)`), so a research gate becomes visible there with no native change. Identity note: this file is byte-identical at source-manifest HEAD 2bc8d30, which is 4 commits ahead of the player-build source but changes only Tools/ (hid-guard*.mjs, ownerinput.swift, p12a-native-preview.mjs), zero Assets/ difference. |
| A placed lot body routing to a workspace keyed by its exact building id | `Assets/Studio/Runtime/Presentation/UI/StudioWorkspaceHost.cs`<br>`StudioWorkspaceHost.OpenFacilityHistory(string buildingId)`<br>1044-1062 (id minted at bridge/history.ts:363 `buildingId: current ? \`placed-${String(row.placementId)}\` : null`, 592e926b) | **REUSED** | This is the shipped precedent and the exact seam a P13 lot research entry point attaches to: a lot body carries SelectableEntity.StableId, the bridge mints `placed-N`, and the host matches with StringComparison.Ordinal and degrades to unselected rather than substituting a subject. P13 must reuse StudioLocateAction.Locate (the one sanctioned programmatic world route, buttons only) rather than adding a second selection path. NOT VERIFIED: which on-lot control invokes OpenFacilityHistory today. |
| Native enforcement that a public page belongs to the campaign and revision that asked for it | `Assets/Studio/Runtime/Infrastructure/StudioBridgeClient.Industry.cs`<br>`StudioBridgeClient.QueryIndustry`<br>147-171 (context reset at Assets/Studio/Runtime/Presentation/UI/StudioIndustryWorkspace.cs:84-89) | **REUSED** | A P13 native research query reuses this stamping and mismatch rejection rather than trusting the response. Pair it with StudioIndustryWorkspace.Observe:87 `if (session != null && session != snapshot.sessionId) { back.Clear(); context = new Context(); page = null; ... }`, retained browse state is discarded on campaign switch. A P13 surface that retains research browse context without that reset would show one campaign's state after a Save As / Load, which is the same class of bug the campaign-scoping invariant forbids. |

**Hazards.**

- industry.ts:33 builds `names` from ALL h.identities, including reserved rivals that have never entered. Nothing public resolves a reserved id through it today (films, businesses and employment are all validated to entered studios), but any P13 lookup of a studio name by id without re-applying `enteredWeek!==null` would publish a reserved rival's brand as an active studio or employer, directly breaking the reserved-IDs invariant.
- Keying a P13 research/adoption cache or receipt store by worldId, campaign storage id, studioId or filmId collides across Save As branches, which legitimately share entity IDs. The accepted pattern is WeakMap on GameState object identity (bridge/snapshot-build-context.ts:36-39, bridge/industry.ts:28) and it works only because the sim core replaces the state object on every accepted command.
- Extending src/core/types.ts EraConfig is not an additive read: src/core/save.ts:1669 validates it with v8ExactKeys over exactly ['soundRequired','televisionCompetition','censorship','costScale'], so any new field is a governed Save V19 / protocol 4 / projection 29 migration plus a DTO regeneration, not a relabel of historical headers.
- costScale on state.era is already consumed by reception.ts, candidates.ts, filmPackage.ts, forecast.ts and hollywoodPolicy.ts. A P13 era that starts advancing costScale would silently change accepted P07 reception and P11 finance outputs, that is a second player-finance formula by side effect.
- StudioIndustryTendency rows are explicitly framed as observation ('this is observation, not a strategy forecast'). Adding a rival research forecast or adoption prediction row breaks both the stated no-forecast disclosure law and the no-rival-technology-tree invariant.
- src/core/blueprintRequirements.ts:66 ships the player-facing copy 'Research is not part of the game yet.' and blueprintRequirementMet hard-returns false for kind 'research'. That copy is rendered verbatim in the native build catalogue (StudioBuildWorkspace.cs:1056-1066). Landing any research gate without changing both in the same slice makes the catalogue state a falsehood to the player.
- Adding a P13 route to StudioWorkspaceHost without adding it to every existing Open* reset list leaves two routes open simultaneously, there are 7 route booleans and 55 `RouteOpen = false` assignments in that one file, and each Open* method resets the others by hand.
- No rival research expenditure, adoption date or capability may be back-dated. The Industry recording boundary is explicit (industry.ts:87 recordingNotice, :173 'Employment recording begins ...', history.ts:454-457 notRecordedNotice); an invented retrospective research history would contradict copy the accepted build already shows.

**Open in this area.**

- Which on-lot control invokes StudioWorkspaceHost.OpenFacilityHistory(buildingId). The route, the `placed-N` id convention and the bridge producer are verified; the caller was not located in StudioHud.cs, StudioBuildCommandHud.cs, StudioSelectionManager.cs or StudioBridgePresentation.cs at deca395, and GitHub code search returned no results for this repo.
- Whether the four commits by which source-manifest HEAD 2bc8d30 leads the player-build source deca395 have any behavioural effect on the runtime. Verified: the compare reports 4 commits ahead / 5 files, all under Tools/ (hid-guard.mjs, hid-guard-tests.mjs, hid-witness-state.mjs, ownerinput/ownerinput.swift, p12a-native-preview.mjs), and every Unity file cited in this matrix is byte-identical between the two shas. The Tools changes themselves were not read.
- Whether any Unity scene, prefab or UXML/USS asset carries a latent research affordance. Searched every path in the deca395 tree for research/tech/era/innovat and found only Char_CameraDP_*, Prop_StudioCamera* and GoldenAgePosterAtlas.png, no match, but scene/prefab file CONTENTS were not decoded.
- Whether bridge/history.ts has any rival-facing surface at all. Verified that historyProjection(state) reads only state.studioHistory / state.studio.releasedFilms / state.placement / state.talent and is player-scoped, and that industry.ts:141 routes the player studio's history view through it while rivals fall back to index.activities. Not verified: whether any P13 research event would be admissible as a StudioHistoryEvent kind, the union is exhaustively switched at history.ts:277-386 with a `never` guard, so a new kind is a typed break, but the owning producer (src/core/studioHistory.ts) was not inspected.
- Whether the P12 Owner acceptance receipt imposes any disclosure constraint beyond the handoff. Scanned 13370d42:docs/campaigns/P12-R05-OWNER-ACCEPTANCE-RECEIPT.md for industry/public/hidden/native/research/era; the only Industry line is a playtest pass note ('Industry -> rival/film/person/employer -> Back works'). No disclosure rule was found there that is not already in the handoff.

#### 1.8 Named campaigns, Save As, stored RNG and governed migration

**Finding.** All five persistence numbers verify exactly as the handoff states (protocol 4 and projection 29 at bridge/schema/bridge-schema.ts:19,36; inner Save V19 at src/core/save.ts:364; outer checkpoint 1 at bridge/runtime-checkpoint.ts:26; separate lossless library storage format 2 at bridge/runtime/campaign-storage-codec.ts:27), and the accepted code already branches an entire world and its stored RNG under a new storage ID, so P13's whole persistence cost is minting SaveFileV20 by the existing peel-exactly-one-root ladder, retargeting the three places that hard-pin V19, and appending the current schema identity 8b2569b1… to SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS at its projection bump.

*NEW P13 WORK 2 · REUSED 9 · SHARED GENERALIZATION 2*

| Connection | Where it lives | Class | P13 obligation |
|---|---|---|---|
| Wire/contract version floor P13 must bump and migrate (protocol + projection) | `bridge/schema/bridge-schema.ts`<br>`PROTOCOL_VERSION, PROJECTION_VERSION`<br>19, 36 | **REUSED** | Verified 4 / 29 exactly as the handoff states. A P13 wire field bumps PROJECTION only; protocol stays 4. Generated Unity header at this sha confirms ProtocolVersion=4 / ProjectionVersion=29 / SchemaId sha256:8b2569b1f925bedf214ee556841fe28b61e544f1f13bb4741c84a0a318e81a85. Do not mint a second version constant. |
| Inner save envelope version the campaign library stores | `src/core/save.ts`<br>`SaveFileV19, makeSave, validateSaveV19, validateSave`<br>363-366, 5016-5018, 6048-6050, 7228-7248 | **REUSED** | Inner Save V19 confirmed numerically. V19 is the live boundary and the ONLY version makeSave emits. P13 state cannot widen V19 in place; see the SaveFileV20 row. |
| Outer runtime checkpoint envelope version wrapping each campaign record | `bridge/runtime-checkpoint.ts`<br>`BRIDGE_RUNTIME_CHECKPOINT_VERSION, BridgeRuntimeCheckpointV1`<br>26, 188-201, 875-876, 1293-1306 | **REUSED** | Outer checkpoint 1 confirmed. The envelope is deliberately generic, currentSaveJson / savedSaveJson are opaque strings plus byte digests, so P13 state rides INSIDE the inner save and needs no change here. Changing checkpointVersion would fail every accepted durable profile closed. |
| Separate lossless campaign-library storage format | `bridge/runtime/campaign-storage-codec.ts`<br>`encodeCampaignStorage, decodeCampaignStorage`<br>18-19, 27, 50-62 | **REUSED** | Storage format 2 confirmed and distinct from the other four numbers: 2 is the ON-DISK representation (gzip-base64/v1 per checkpoint, sha256+exact-length verified), 1 is the in-memory logical shape loadCampaignLibrary requires (campaign-library.ts:93). P13 must add no state to the codec; it is representation-only and must stay lossless. |
| New Game seeds an independent world (seed, RNG, world identity) | `bridge/session.ts`<br>`BridgeSession.createRuntime`<br>1276-1284 | **REUSED** | Independence is already end-to-end from one fresh UUID seed: worldgen.ts:679 `rngState: RngStream.fromSeed(seed).serialize()`, hollywood.ts:113/127 `worldId:`world-${key}`` with key=hollywoodWorldKey(state.seed), and hollywoodValidation.ts:70 rejects a world whose identity differs from its seed. P13 must not introduce a second world-identity source or a second seed. |
| Save As branches a complete world and RNG under a new storage ID while preserving the original | `bridge/runtime/campaign-library.ts`<br>`proposeCampaign (case 'saveAs')`<br>181, 229-235 | **REUSED** | The copy re-imports the exact current save bytes (so rngState, seed, worldId, playerStudioId are identical by design, campaign-origin.ts:4-6 asserts exactly that) under a fresh record UUID at revision 0 and a fresh sessionId; the original record object is left untouched (L233 `records.push(record)`), and overwriting the ACTIVE record is refused (L181). P13 mutable state, receipts and caches must therefore be scoped by session or record id, never by worldId/playerStudioId/personId. |
| Rename changes the catalogue label only, the anchor for 'no relabelling historical headers' | `bridge/runtime/campaign-library.ts`<br>`proposeCampaign (case 'rename'), normalizeCampaignLabel`<br>30-34, 167, 179-180, 241-243 | **REUSED** | Rename mutates label + revision and nothing else; the record's inner V19 save, its studioHistory and its hollywood receipts are not re-derived. P13 must not smuggle era/technology facts into `label` or `revision`, and must not rewrite a stored record's header to imply a history the record's own bytes do not contain. |
| Strict governed checkpoint/schema migration at startup (what a projection bump costs) | `bridge/runtime-checkpoint.ts`<br>`SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS, migratePriorProtocol4Checkpoint, importPriorSaveViaCanonicalChain, loadBridgeRuntimeCheckpoint`<br>57-155, 823-834, 854-1006, 1225-1267 | SHARED GEN. | P13 owns exactly one edit here at its projection bump: append sha256:8b2569b1f925bedf214ee556841fe28b61e544f1f13bb4741c84a0a318e81a85 with a 'projection-v29' label. What governed migration actually does: verifies the ORIGINAL bytes (exact keys, canonical-JSON+LF, journal and save byte digests), discards the journal as opaque history, re-imports both save slots through importSave→migrateToV19→exportSaveJson, then mints a NEW sessionId with stateRevision 0 (L991-1005). It never advances a week, each record migrates at its own week. Owner: whoever mints the new schema identity. |
| A governed save/schema/consumer migration for NEW P13 state | `src/core/save.ts`<br>`SaveFileV20 / validateSaveV20 / convertV19ToV20 / migrateToV20`<br>4936-4957, 7228-7256 (absence) | **NEW P13** | P13 must mint SaveFileV20 by the same peel-exactly-one-root pattern (one new GameState root, delegate the remainder to the frozen validateSaveV19), plus convertV19ToV20, migrateToV20, a `saveVersion === 20` arm in validateSave (L5016) and the updated 'versions 1 through N' message (L5018). Then retarget the three hard-pinned V19 consumers: runtime-checkpoint.ts:422 `type CurrentEnvelopeSave = SaveFileV19`, runtime-checkpoint.ts:437 `if (imported.saveVersion !== 19)`, and campaign-library.ts:48 and :127 `migrateToV19(importSave(...))`. It must NOT duplicate the migration chain, widen V19 in place, or add a root without a version bump. |
| How stored RNG is carried across save, Save As, migration and reload | `src/core/rng.ts`<br>`RngStream.serialize / RngStream.deserialize / GameState.rngState`<br>137-165, 204-206; types.ts:477; save.ts:2210-2215 | **REUSED** | Stored RNG is one string root (4 sfc32 words) inside the inner save. It survives Save As because Save As re-imports the exact currentSaveJson, and it survives migration because every converter copies it verbatim; runtime-checkpoint.ts:440 `if (exportSave(imported) !== saveJson)` enforces byte-exact round-trip at every checkpoint boundary. P13 must not add a second RNG root or reseed on branch. |
| Deterministic randomness for any new P13 mechanic without breaking replay | `src/core/rng.ts`<br>`RngPurpose, stream()`<br>34-70, 204-206 | SHARED GEN. | Widening is one versioned member appended to the RngPurpose union (the existing precedent: 'discovery-v1', 'casting-v1', 'presence-v1'), drawn via stream() and keyed on a mint ordinal or entity id, L59-60 states keying on a week or a year is what makes a generator era-UNclean. Drawing from state.rngState instead would shift every existing campaign's draw sequence and break M0A byte-identity. Owner: core (src/core/rng.ts). |
| How caches and receipts are scoped by campaign/session | `bridge/runtime/campaign-library.ts`<br>`summaries (WeakMap), CampaignReceipt, MAX_RECEIPTS`<br>17, 24, 44-53, 113-122, 158-159, 258 | **REUSED** | Cache scoping is already correct for Save As copies that share entity IDs: every cross-call cache is keyed on OBJECT IDENTITY, and snapshot-build-context.ts:36-39 states the invalidation law (the sim core is pure, so a new state object simply has no entry). Receipts are the weaker half: the CampaignReceipt ring is LIBRARY-wide, capped at 128 (L17, L258 `.slice(-MAX_RECEIPTS)`), keyed by commandId alone, and the replay lookup at L158-159 runs BEFORE the session/revision/catalogue guards. Per-campaign receipts are the ones inside the save (hollywood.receipts / nextReceipt, hollywood.ts:129). P13 must route campaign-affecting operations through the existing commandId ledger and must not build a second one. |
| Era state that P13's era/technology progression would extend | `src/core/worldgen.ts`<br>`generateWorld (era: EraConfig), types.ts EraConfig`<br>655-663; types.ts:284-289; save.ts:900, 1668-1682 | **NEW P13** | An `era` root already persists and is exactly-key validated (save.ts:900 in V8_STATE_KEYS; v8Era at 1668-1682), but it carries NO year, is constructed only at worldgen.ts:658 and mutated nowhere in src/ or ui/ (all other `era:` sites are reads passing state.era into input objects), is byte-identical in every campaign, and appears nowhere in bridge/schema/bridge-schema.ts or generated/unity/StudioBridgeDtos.Generated.cs. Era progression is genuinely new. It must NOT duplicate era.costScale, which is already the live multiplier in the one negative-cost formula at candidates.ts:286, filmPackage.ts:97 and :755, forecast.ts:145, reception.ts:264 and hollywoodPolicy.ts:45. |

**Hazards.**

- Adding a root to GameState without minting SaveFileV20 breaks EVERY save immediately: the V19→V18→V17→V16 peel ladder bottoms out at save.ts:2207 `v8ExactKeys(state, V8_STATE_KEYS, [], label)`, which rejects any key it does not name. There is no 'additive field' escape hatch in this validator.
- Bumping the projection to 30 without appending the outgoing identity sha256:8b2569b1f925bedf214ee556841fe28b61e544f1f13bb4741c84a0a318e81a85 to SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS fails every accepted P12 durable library closed: loadBridgeRuntimeCheckpoint (runtime-checkpoint.ts:1251-1261) only migrates enumerated prior ids, otherwise falls through to decodeBridgeRuntimeCheckpoint, which requires `record['schemaId'] !== SCHEMA_ID` to fail (L1036-1038). The comment at L102-104 names this the schema-bump law; no automated check enforces it.
- Writing a libraryVersion 3 on disk without teaching decodeCampaignStorage: L53 returns the value untouched for any version but 2, and campaign-library.ts:93 then throws 'Unsupported campaign library format/version'. All 32 records fail closed together, not one at a time.
- Any module-scope Map keyed by worldId, playerStudioId, seed or personId would alias Save As copies, which share all of those by design (campaign-origin.ts:4-6). The accepted runtime has zero such maps; every cache is a WeakMap on an object identity.
- Mutating state.era mid-campaign changes era.costScale, which is a live multiplier inside the single required-negative-cost expression replicated at five call sites. Unless P13 reuses that exact expression, it creates a second player-finance formula.
- A P13 campaign operation that bypasses the commandId receipt ledger double-applies on a durable retry: proposeCampaign checks the receipt (L158-159) BEFORE the sessionId, stateRevision and catalogueRevision guards, so an exact-byte replay returns the cached response regardless of what the library did since.
- The library receipt ring is 128 entries LIBRARY-wide, not per campaign (L17, L258). A chatty P13 operation stream would evict older campaign receipts and silently destroy exact-retry idempotence for operations still in flight.
- Rename is the only operation that touches a record header, and it touches only label and revision. Repurposing either to carry era or technology facts would relabel a historical header, precisely what the handoff forbids.
- Loading the library re-derives every record through hydrate() → loadBridgeRuntimeCheckpoint → recordFor (campaign-library.ts:103-111), so a governed migration rewrites ALL records at once and sets `changed`, triggering a full durable rewrite (runtime-coordinator.ts:306). Migration is not per-record-on-demand; P13 must size its migration cost against 32 records, not one.

**Open in this area.**

- Whether the Unity player build (deca39521da1baeca61898d156a43f4ae6a7e035) reads or writes any campaign-library or checkpoint field. I inspected only TypeScript at 592e926b plus the generated DTO header in that same TS tree; I fetched no Unity source.
- Whether any test, lint rule or CI gate actually ENFORCES the schema-bump law. The law exists only as a code comment at runtime-checkpoint.ts:102-104; I did not search the test suite for an assertion that the outgoing identity was appended, and I ran nothing.
- Whether 'inactive campaigns do not advance' holds across every tick and command path. I confirmed that proposeCampaign and withWorkingCampaignCheckpoint never re-derive a non-active record's game state, but I did not audit src/core/tick.ts or hollywoodTick.ts for any global iteration over stored records.
- Whether the 128-entry library receipt cap has ever been reached in practice, and what the observed behavior is when it evicts. Bound read from source; no run, no evidence file inspected.
- Whether P12 implemented any dormant or closed rival behavior. Outside my assigned files; I did not inspect the rival lifecycle beyond hollywood.ts initializeHollywood/enterRival, which show reserved identities carrying enteredWeek:null.
- The concrete size of a P13 SaveFileV20 migration for real Owner data. DEFAULT_BRIDGE_RUNTIME_CHECKPOINT_LIMITS comments cite a measured 49.6 MB Save receipt and 133.5 MB single-save outer1 at R05 week8791, and state the rebaseline 'does not satisfy the original save-growth target' (runtime-checkpoint.ts:157-164), I read that note but measured nothing.

#### 1.9 Matrix totals

| Classification | Count |
|---|---:|
| REUSED | 60 |
| SHARED GENERALIZATION | 19 |
| NEW P13 WORK | 18 |
| NOT VERIFIED | 0 |
| **Total** | **97** |

#### 1.10 What the refresh settled, and what it corrected

**The five POST-P12 verification items, closed.**

| Item (design §31, rulings §2.4.3) | Answer from accepted source |
|---|---|
| The production lifecycle's first-filming seam for the §12.9 lock | **Closed. Witness found.** `src/core/operations.ts:1233` appends `{ kind: 'phaseEntered', productionId, phase: targetPhase }` and the shooting task is minted immediately after at L1235-1240. The event is persisted, not ephemeral (`src/core/studioEvents.ts:99`, `src/core/types.ts:1472`, save vocabulary `src/core/save.ts:4175`, `:4545`). The seam is **REUSED**; the loadout it would lock is **NEW P13 WORK**, because `Production`, `ProductionWorkflow` and `ShootingTask` carry no method, format or technology field at `592e926`. Note `productionPhases.ts:36-40` maps remaining ticks 5 and 4 both to `shooting`, so "first authoritative take" is a week later and weaker than `phaseEntered`. |
| The authorized employment action for researchers | **Closed, and the answer is that none exists.** No research-staff role, laboratory seat assignment or research payroll charge exists anywhere at `592e926`. `FacilityCapability` is a four-arm closed union (`src/core/types.ts:548-552`), `LedgerKind` has fifteen members (`src/core/financeReport.ts:6-14`), and `Contract` is a six-key shape. Every named Scientist is **NEW P13 WORK** against closed unions. P12's employer indexes, contract-interval algebra and one-employer exclusivity are **REUSED** underneath it. |
| P12 conserved-resource seams funding rival research ceilings | **Closed, with a hazard.** A rival's capacity is frozen three ways at once: the facility list must byte-match `rivalStartingFacilities` (`src/core/hollywood.ts:98-105`), the capacity movement must equal the four-capex sum exactly, and `facilityOpex` reconciles as elapsed weeks times current capacity opex. **P13 cannot give a rival new or upgraded capacity without a save-schema change and a migration.** `rivalCapacityOpex` (`hollywood.ts:88-96`) is a hard-coded four-arm object lookup with no default, so a fifth capability makes it return `undefined` and the sum `NaN`. |
| P11 quote/commit symbols and refundable-commitment dispositions | **Closed.** `bridge/finance-consequence.ts:6-23` and `bridge/finance-route.ts` carry seven route kinds and no technology kind; `src/core/placement.ts:1026-1028` is the only refund path (`facilityDemolitionRefund`). A P13 adoption cost lands as a new `LedgerKind` member plus a route kind, both governed. |
| The migration starting version | **Closed, and it is not what the design assumed.** The accepted save head is **inner Save V19** (`src/core/save.ts:364`), not the V15 envelope design §18.1 and §31 historically pin. P13's migration starts at V19. |

The calendar item was already closed on 2026-09-11 and the refresh confirms it in code: `market.tick` is the only clock and `src/core/tick.ts` its only writer (`tick.ts:182`, `:999`), with `campaignDate` mapping week 0 to 1920 Week 1 under the `campaign-calendar-1920-52/v1` literal (`src/core/calendar.ts:2`, `:17-18`) and `RIVAL_ARRIVAL_WEEKS = [0,0,0,0,520,988,1560,1872,2548]` at `calendar.ts:3`.

**The delivered persistence contract, verified numerically rather than quoted.**

| Contract element | Stated | Verified at | Result |
|---|---|---|---|
| Protocol | 4 | `bridge/schema/bridge-schema.ts:19` `PROTOCOL_VERSION = 4 as const` | matches |
| Projection | 29 | `bridge/schema/bridge-schema.ts:36` `PROJECTION_VERSION = 29 as const` | matches |
| Inner Save | V19 | `src/core/save.ts:364` `saveVersion: 19` | matches |
| Outer checkpoint | 1 | `bridge/runtime-checkpoint.ts:26` `BRIDGE_RUNTIME_CHECKPOINT_VERSION = 1 as const` | matches |
| Campaign-library storage format, separate and lossless | 2 | `bridge/runtime/campaign-storage-codec.ts:27` `libraryVersion:2` | matches |

**Charge onset, which the catalogue could only settle on paper, is settled in code.** `bridge/finance-upcoming.ts:19-31` emits the completion event at `week: placed.completesWeek` with the detail "Already paid capital; no additional purchase charge on completion", and the first operating charge at `week: placed.completesWeek + 1` with the verbatim detail **"First operating charge is the advance Week X → X+1, after completion."** The ledger row itself is stamped off the pre-advance placement snapshot (`src/core/tick.ts:943-953`). So the accepted runtime charges a facility's operating cost from completion, never before it, which is the convention the catalogue reconciled to at `e48541b`. **No during-work rate exists anywhere in the accepted runtime.** The "$5,500/week during work" figure in the facility addendum is an illustrative card example, not delivered behavior, and the launch package treats it as such.

**Seven detail defects the adversarial pass found in the matrix above, corrected here.** The claims they support all survived; these are the supporting details.

| # | Where | Corrected fact, verified independently |
|---|---|---|
| F1 | §1.1, hazard on exhaustive guards | `studioHistory.ts:164`'s `never` guard is over `StudioHistoryEvent`, not over `SimStopReason`. Only `ui/src/lot/livingTurn.ts:134-156` guards stop reasons. |
| F2 | §1.1, history event kinds | **Ten**, not eight: `studioFounded, standingChanged, standingDriftFolded, filmReleased, theatricalRunCompleted, facilityCommitted, facilityCompleted, facilityDemolished, facilityMoved, careerMilestone` (`src/core/save.ts:4905-4915`). |
| F3 | §1.4, ledger size | **Fifteen** `LedgerKind` members, not twenty-two (`src/core/financeReport.ts:6-14`). |
| F4 | §1.4, hazard on token search | Wrong on both halves. `src/core/tuning.ts:707` carries a `laboratory` mention, so "zero source files" is false, and the area's own connection row said so. The substantive claim, that no research role or seat exists, stands. |
| F5 | §1.7, Unity route flags | **Eight** distinct route flags, not seven. The 55 `RouteOpen = false` assignments are exact. |
| F6 | §1.5, research-gate activation | **Three** edits in `blueprintRequirements.ts`, not two: remove `research` from `NOT_YET_ATTAINABLE_KINDS` (L66), add it to `LIVE_REQUIREMENT_KINDS` (L49-54), and split it out of the shared `return false` arm (L160-168). The removed copy string is player-facing and rendered in the Unity build. |
| F7 | §1.7, wire schema | "No wire-schema change needed" holds only for tendency rows on an existing view. A new view value changes `StudioIndustryRequest`/`Response` and therefore the protocol. |

**Three things no area found, which the adversarial pass did.**

- **The recommended hang point for a dated milestone is wrong.** `tick.ts:1038-1042`'s rival loop sits *after* `commitStudioEvents` (`tick.ts:1025`) and `commitStudioHistory` (`tick.ts:1036`) inside the same finalize literal, so a milestone hung there cannot record itself through either surface in the same advance. P13 must place a dated technology milestone before both commits, not beside the rival gate.
- **Every new 1920 campaign is seeded `soundRequired: true`** (`src/core/worldgen.ts:657-663`), under a comment calling the three non-`costScale` fields "inert data here". Inert today, because nothing reads them. The moment P13 makes era live, a 1920 campaign inherits a world where sound is already required, which is the opposite of the silent-era opening the design assumes. This is decision **D4** below.
- **The shipped web lot renders a hardcoded year and it is not the campaign's year.** `ui/src/lot/StudioLotScreen.tsx:301` `const LOT_ERA_KEY = '1948'`, commented "this is where it lives until the engine owns a calendar year", while `campaignDate(0)` is `1920 · Week 1`. This is decision **D5** below.

**What the refresh could not complete.** The source-manifest HEAD `2bc8d30` was not diffed against the player-build `deca3952` (the performance attestation binds `2bc8d30`, while the accepted candidate directory names `deca395`; both identities are carried separately here rather than merged). Three Unity contract files were not fetched. The V18 to V19 migration chain was not traced, so every "adding a field refuses existing saves" statement rests on the validators actually read (`v8ExactKeys`, `exact`) rather than on whether a migration could lawfully widen them. No test was read or run. These are listed so the execution order can close them rather than inherit them silently.

---

## 2. Recommended scope, and the conditions for activating it

**This section recommends. It grants no implementation authority.** Current Ops decides activation; P13 coding needs a separate execution order.

**The execution boundary this package proposes is P13A Core.** It is not authorization for the Ready tier. Core carries every capability that settled Owner direction already assigns to it, including retained verified research progress, which stays in Core even though producing a first successful sound film never requires a cancellation. Ready is preserved in §2.4 by name, owner, acceptance condition and next bounded placement, as the next bounded execution rather than deleted scope. The three product critiques therefore read: an early playable slice, a completed causal Core, then the integrated final P13A Core candidate.

### 2.1 Reuse the existing tiers rather than inventing new ones

Design §2A.2 already carries a Core / Ready / Later table and the recommended order after P13A. This package activates that table rather than competing with it. Owner rulings §2.1 already bound P13A to "one synchronized-sound transition, one Research Laboratory, one Scientist assignment, research versus wait, one rival consequence, and one Production/world consequence", which is the outcome-first slice this assignment describes. Nothing below widens that boundary.

### 2.2 Recommended resequencing: bring the native entry forward

Design §26 sequences P13A.1 headless core, P13A.2 upstream consequence integration, P13A.3 bounded read side and world route. **Recommendation: move a thin native lot/workspace entry out of A.3 and into the first playable gate.** The reason is the review model, not taste: the three product critiques inspect actual screens and tasks, so the first critique cannot be satisfied by a headless core, and a slice that reaches a reviewer only at A.3 defers all product feedback past two thirds of the work. The correctness content of A.1 does not move; only the point at which a person can reach it.

The refresh says this is cheap. `bridge/industry.ts`'s `industryPage` is one entry-gated, ID-joined, session- and revision-pinned projection that already produces every lawful public fact P13 needs, and the Unity client already rejects on eight-field mismatch with `sessionId` and `expectedStateRevision` pinned. A research view is a new view value on an existing, proven surface, which per F7 is a protocol change and not a new architecture.

### 2.3 Core, recommended for activation

| Capability | Activation condition |
|---|---|
| Early-research window with knowledge prerequisites (design §12.1a) | PREQ-1 to PREQ-8 built, plus PREQ-9's receipt. The `research` requirement kind already exists as a live-refusing stub, so the gate is a three-edit activation, not a new mechanism |
| One named researcher with continuing payroll | New role, seat and ledger member against four closed unions; governed Save V19 migration |
| Per-project R&D budget ceiling with disclosed marginal benefit and bottleneck (§12.6, §16a.1) | Fixture values only, all labelled hypothesis |
| Retained verified work across pause and cancel (§12.8) | PREQ-9's receipt shape defined; restart seeds exactly once. Owner direction 7A makes this unconditional, so it is Core even though a first sound film need never cancel anything |
| The technology lock at first actual filming (§12.9) | Seam is REUSED at `operations.ts:1233`; the locked loadout is new |
| Wait-for-commercial-release purchase route (§12.1a) | A new P09 catalogue row gated on a `{kind:'date'}` requirement. `capex > 0` and `buildWeeks >= 1` are hard catalogue invariants, so an instant zero-week grant is not expressible and must not be faked |
| One rival consequence, by the commercial route | The rival buys at the released milestone and installs on capacity it already owns. Must not add rival capacity, and carries no recurring rival operating change in P13A. See §4.1 |
| A thin native lot/workspace entry | New view value on `industryPage`; protocol bump |

### 2.4 Ready, recommended next, preserved by name

**Ready is the next bounded execution, not deleted scope.** Nothing below is dropped or folded into Core. Where Core independently requires an element for its own reason, Core's requirement governs and the Ready row narrows to what Core does not already need.

| Ready requirement | Owner | Accepted when | Next bounded placement |
|---|---|---|---|
| Staffing to full laboratory capacity | P13 with the staffing owner | Four seats are assignable, a fifth is refused with the seat count and a remedy, and payroll charges per seat whether or not a project uses the person | P13B, directly after P13A Core. Core seats one researcher; the cap itself is Ready |
| Multi-laboratory cooperation and splitting, with diminishing returns | P13 | Two laboratories on one brief produce the summed researcher output times a concentration factor of **0.75–0.875**, never the Owner's 1.5–1.75× total, and splitting two equal briefs is compared against sequencing on the same fixture | P13B, gated on a second authored brief with a real consumer (§4.3) |
| Persistent plan queues with dependencies and an admission policy | P09 scheduling | A queued plan survives save and load, admission refuses an unsatisfiable dependency, and nothing queued reserves a resource it has not started | P13B. PREQ-10; not needed for a first sound film |
| Broader gap-aware conversion quotes, and direct purchase without obsolete intermediates | P13 with P09 | A studio two generations behind is quoted the direct step and is billed for no obsolete intermediate | P13B |
| Forecast precision: windows becoming concrete public dates, and replacement disclosure beside purchases (design §16a.3) | P13 with the industry-disclosure owner | A window narrows to a date only from facts already public, `StudioIndustryTendency` keeps its no-forecast disclaimer, and a purchase quote discloses what the purchase replaces | P13B |
| Option-B installation cancellation | P09 physical with P11 | Cancellation pays completed work and necessary restoration and recovers genuinely refundable unused commitments, through the one existing refund path | P13B. Core carries retained *research* work (PREQ-9); installation cancellation is a different thing and stays Ready |
| Decomposed inventor pricing with prototype credit | P13 under design §16a.2 | A component quote shows access, equipment, site adaptation and installation charged once each, the prototype credited once, and no quote running negative | P13B. Core needs the four components only for the single first installation |
| Symmetric rival research, and rivals out-investing the player | P13 with the Hollywood owner | A rival funds research on rival laboratory capacity and can reach a technology before the player, under the same catalogue and release law, with recurring rival operating consequences reconciled honestly | P13B, priced with the rival capacity save-schema change and migration it requires (§4.1) |

**A parallel-research proof needs two genuinely useful briefs, not an empty second queue.** Cooperation and splitting cannot be demonstrated against one brief and a placeholder: the catalogue's §9.4 fixtures show that the answer turns on whether seats sit idle and on whether the first or the last completion date carries the value, and neither question exists without a real second consumer. Activating this tier therefore depends on authoring a second brief with a real consumer, which is the first item of the §4 sequence for OPEN-2.

### 2.5 Later, placement and terms pending

Supplier commercialization and its terms, transfer and co-development entitlement, exclusivity, early supplier launch. Equipment rental and a hire route for scarce capabilities stay unselected later scope (catalogue LATER-2 and LATER-5). None is a prerequisite of the first research experience.

### 2.6 Necessary sound work, and what is not a launch prerequisite

Required: acoustic treatment on one stage, the recording package, compatible capture, sound-capable Post. Not a launch prerequisite and not to become one: My Buildings, ordinary office renovation, broad upgrade families, and any 2→3-slot tuning. The catalogue's corrected accounting at `e48541b` is retained with its disclosed limitations, and its closed correction is not reopened.

---

## 3. Preserved requirements, mapped

Nothing below is dropped. Every row lands in Core, Ready, Later or a named prerequisite.

### 3.1 The fourteen Owner-selected directions (rulings §2.4.1)

| # | Direction | Lands in |
|---:|---|---|
| 1 | Early research, fixed commercial rollout | **Core** |
| 2 | Knowledge prerequisites; cash cannot bypass them | **Core** |
| 3 | Rivals can out-invest | **Core** for one commercial-route consequence; **Ready** for rival research and out-investment, which needs rival laboratory capacity, a save-schema change and a migration. See §4.1 |
| 4 | Named researchers to capacity | Core seats one; **Ready** for capacity |
| 5 | R&D budgets that materially accelerate | **Core** as a ceiling; tiers are hypothesis |
| 6 | Cooperation and diminishing returns | **Ready** |
| 7 | Retained progress (7A) | **Core** |
| 8 | Film technology lock at first filming | **Core**, seam REUSED |
| 9 | Persistent queues | **Ready** |
| 10 | Installation cancellation (10B) | **Ready** |
| 11 | Inventor pricing | **Ready** |
| 12 | Leapfrog purchase with gap-aware conversion | **Ready** |
| 13 | Forecast windows becoming concrete | **Ready** |
| 14 | Supplier commercialization desired | **Later** |

### 3.2 Obligations and facility requirements

`P13-OD-01` … `P13-OD-14` and `FUP-001` … `FUP-023` are preserved in full at `e48541b` and none is renumbered. Four carry direct refresh consequences: **P13-OD-03** (payroll continues whether or not a project uses the researcher) needs the new `LedgerKind` member; **P13-OD-14** and **FUP-013** (campaign-specific state, honest old-save behavior) start from V19 rather than V15; **FUP-010** (completion atomically changes capacity, effect and cost at a defined onset) is now backed by the verified onset in §1.10; **FUP-019** (P13 adoption and P09 physical conversion share one physical work and commit owner) is the ownership rule of §5.

The catalogue register at `e48541b` carries forward unchanged: `OWN-1..9` settled, `ENG-1..9` pending disposition, `PREQ-1..10` engineering prerequisites, `OPEN-2/4/5` genuinely unresolved, `LATER-1..5` unselected. `CAT-001`…`CAT-058` and the fifteen research briefs are untouched.

### 3.3 Prerequisites, with owners and acceptance

These are engineering prerequisites for Current Ops to sequence. **None is an Owner question.** The Owner settled that the department exists; what remains is to build it.

| | Prerequisite | Owner | Depends on | Accepted when |
|---|---|---|---|---|
| **PREQ-1** | Research Laboratory substrate | P09 owner, sequenced by Current Ops | none | A laboratory blueprint exists in the P09 catalogue and passes `assertBlueprintCatalogInvariants` |
| **PREQ-2** | Laboratory body carrying seats | P09 | PREQ-1 | `completeDuePlacements` grants the capability; a fifth `FacilityCapability` arm is added **and** `rivalCapacityOpex`'s no-default lookup is fixed in the same change |
| **PREQ-3** | Researcher employment record, wage, payroll charge | staffing owner with P10 | none | A new role widens `CreativeRole`/`Discipline`; a new `LedgerKind` member charges payroll whether or not a project uses the person |
| **PREQ-4** | Per-project seat assignment honoring caps | P13 | PREQ-2, PREQ-3 | A fifth assignment is refused with the seat count and a remedy |
| **PREQ-5** | Per-project budget with saturation ceiling | P13 under design §16a.1 | PREQ-3 | Spend above the usable amount is never charged; the bottleneck is named |
| **PREQ-6** | Acoustic instrument module as a purchasable body | P09 | PREQ-2 | A brief blocked for want of instruments proceeds after installation; another discipline stays blocked |
| **PREQ-7** | Invention provenance record | P13, design §16a.2 already defines it | none | A component quote shows prototype equipment billed once |
| **PREQ-8** | Installation jobs respecting the dependency chain | P09 physical, P11 onset | PREQ-1, PREQ-6 | Operating charge starts at completion per §1.10, never during work |
| **PREQ-9** | Retained work across cancel and restart | P13 | none for the receipt itself; PREQ-10 only for queue-driven cancellation | Not needed for a first sound film to be *legal*, but the capability sits in **Core** per §2.3, because Owner direction 7A makes retained work unconditional. A first sound film simply never exercises it |
| **PREQ-10** | Plan queue with admission policy | P09 scheduling | none | Not needed for a first sound film, and not in Core. **Ready** |

---

## 4. Genuine decisions

Five. Everything else is settled direction or engineering. No decision below asks whether laboratories, scientists, staffing or budgets should exist. D1 and D3 are answered in full in §4.1 and §4.2 rather than left as a choice for the implementation lead.

| | Decision | Recommendation |
|---|---|---|
| **D1** | **What "one rival consequence" is allowed to be.** A rival's capacity is frozen three ways and `rivalCapacityOpex` has no default arm, so the obvious consequence, a rival building a laboratory, costs a save-schema change and a migration inside P13A. | **The rival buys and installs; it does not research.** It takes the commercial route at the released milestone, on the soundstage capacity it already owns, under the same catalogue, release, compatibility, adoption and installation law as the player. Rival research, rival laboratories and out-investing the player are preserved as Ready work with their schema change priced there. Stated in full, with the one disclosed asymmetry, in **§4.1**. |
| **D2** | **OPEN-4, per-entry commercial-release weeks.** The calendar exists and does not deliver technology, so these are now a P13 recommendation rather than a wait. | Pin **one** fixture milestone for P13A and defer the full per-entry table to the Ready tier. The milestone gate belongs before `commitStudioEvents`, not beside the rival gate. |
| **D3** | **OPEN-5, the absolute money scale, and which weekly rate governs.** The catalogue used the §5.1 K2 floor of $1,200 while the cheapest accepted building charges $2,000 and H1 implies $5,000. The catalogue publishes all three and the sign of its result holds at every one. | **One named candidate scale, not a menu: S1-A, S1 capital with accepted-floor operating.** Every value P13A needs is set out in **§4.2**, with one meaningful alternative (S1-U) and the difference between them. These are candidate tuning, provisional and subject to implementation review and playtest. |
| **D4** | **The silent-era opening.** Every new 1920 campaign is seeded `soundRequired: true` while the field is inert. When P13 makes era live, does a 1920 campaign start sound-required, as the data says, or silent, as the design assumes? | **Silent.** Correct the seed as part of P13's era activation, with a governed migration, and add a proof that a 1920 campaign can produce a lawful silent film with no sound capability anywhere. |
| **D5** | **Which year the lot shows.** The shipped web lot renders a hardcoded `1948` while the campaign's own date is 1920 Week 1. | Take the year from `campaignDate` and retire `LOT_ERA_KEY`. Presentation only; no simulation law changes. |

### 4.1 D1 in full: how a rival lawfully obtains synchronized sound in P13A

**In P13A the rival buys. It does not research.** The one rival consequence is an adoption outcome reached by the commercial route:

- the rival uses the commercial wait and purchase route, at the same released milestone the player's wait route uses;
- it installs synchronized sound on the soundstage capacity it already owns, and gains no new or upgraded capacity;
- it obeys the same global catalogue entry, commercial-release week, compatibility requirement, adoption rule and installation law as the player;
- it does not research, because it has no research capacity, and P13A simulates no rival research.

**Why it cannot research inside P13A, in code, not in prose.** A rival's plant is frozen three ways at once: the facility list must byte-match `rivalStartingFacilities` (`hollywoodValidation.ts:267-269`, else every existing save refuses to load), `movements.capacity` must equal the four-capex sum exactly (`:228-229`), and `facilityOpex` reconciles in closed form as `-elapsed * rivalCapacityOpex(b)` (`:241`), where `rivalCapacityOpex` is a four-arm object lookup with no default (`hollywood.ts:88-96`). `RivalBusiness` is key-exact at 14 keys (`:204`) and carries no attachment point for per-studio technology state (`hollywoodTypes.ts:79-95`). Giving a rival a laboratory therefore costs a save-schema change and a migration, which is exactly the cost this package refuses to hide inside P13A.

**How the purchase is paid, and the one execution choice inside it.** The lawful movement is `moveRivalMoney` into the rival's own period, never `state.ledger` and never the `capacity` kind, which is reconciled as paid exactly once. `RIVAL_MONEY_KINDS` is a closed union checked structurally, so **adding a kind needs a save migration**; the already-declared `development` kind is persisted today against a reconciliation that currently evaluates to zero (§1.3, `hollywoodValidation.ts:242`). **Recommendation: route the P13A purchase through `development` and extend that reconciliation in the same change**, which leaves the persisted shape untouched so existing saves still load, and accept that the kind's name describes script development rather than a technology purchase. Rename or split the kind in Ready, alongside the reconciliation change that tier already needs. If the reconciliation cannot be extended without a persisted-shape change, escalate under §7 rather than adding a kind inside P13A.

**The one asymmetry, disclosed rather than papered over.** The player's converted stage takes a recurring weekly operating change from the week its installation completes. A rival cannot take a recurring change in P13A: `facilityOpex` is recomputed in closed form over every week since entry from the *current* rate, so a mid-campaign rate change retroactively invalidates every prior week rather than only future ones. **The rival's P13A adoption therefore carries a one-time cost and no recurring operating change**, and no report, acceptance task or release note may present the two routes as financially identical. Recurring rival operating consequences belong to Ready, with the reconciliation change and migration priced there.

**A second asymmetry, from the same place.** Rival facilities carry no `blueprintId`, `LotCell`, placement row, construction project or `StudioSet` (§1.3), so the rival's installation is an adoption recorded against abstract managed capacity, not the player's P09 placement job with its reservations and completion. The *law* both obey is the same; the *physical job* is the player's alone. A proof that compares the two must compare eligibility, release week, compatibility and outcome, never job progress.

**What the rival's route must still prove.** The adoption is public only through the existing disclosure gate: the entered-studio filter in `bridge/industry.ts` (a name resolved by `studioId` without that filter would publish a reserved rival as an adopter, §1.7), an announcement receipt minted by the Hollywood owner rather than by the bridge, and a `StudioIndustryTendency` row that keeps its no-forecast disclaimer. The rival's throughput bounds are validated and must not move: one production at a time, at most two active screenplays, soundstage capacity 1.

**What P13A may and may not claim.** It may claim that one catalogue entry, one release week and one installation law govern the player and a rival alike, and that a player who waits forfeits the eleven-week lead the research route produces. Both the waiting player and the rival are gated on the same release week, so **P13A must not claim that a waiting player falls behind the rival**: what it demonstrates is a forfeited lead, not a lost race. **It may not claim symmetric research, because the rival did not research.** Symmetric rival research and the ability for a rival to out-invest the player are preserved in §2.4 by name, owner, acceptance condition and next bounded placement, and are proven there, on rival laboratory capacity, with the schema change and migration paid honestly.

### 4.2 D3 in full: the recommended P13A candidate economic scale

**Read this first.** Every value below is **candidate tuning**. It reaches playable cash, duration and decisions, which is what makes it tuning rather than illustration, and it is provisional and subject to implementation review and playtest. None of it is approved tuning, and the Owner's 2026-09-11 record that the five-times scale remains a hypothesis stands. What this section removes is the menu: an implementation lead should build against one coherent scale, not choose between four rates. **It amends nothing at `e48541b`.** The catalogue published its headline figures at the $1,200 K2 floor and published the other rates beside them as a sensitivity, leaving the choice to ENG-1 and ENG-7. Choosing one here exercises that open decision; it does not reopen the closed catalogue correction, and the catalogue's own numbers stand as published.

#### A. Recommended: candidate scale **S1-A**, S1 capital with accepted-floor operating

One rule resolves every conflict: **where an accepted blueprint and the H1 fixture price the same object, the accepted blueprint governs; S1 (H1 multiplied by five) fills only the gaps where nothing accepted prices the object.** Applied consistently, that rule fixes the capital side at S1, fixes the one contested capital object (a new soundstage) at the accepted blueprint, and fixes the weekly operating change on every K2-class object at the accepted floor of $2,000 rather than at the band floor of $1,200. **The laboratory is the one exception, and it is an exception the rule predicts**: nothing accepted prices a laboratory and H1 prices no laboratory operating change, so no accepted value and no S1 value exists to govern, and the K3 band value stands.

| Element | Candidate value | Where it comes from |
|---|---|---|
| Research Laboratory capital | **$900,000 over 12 weeks** | S1; sits inside K3 ($700,000–$1,500,000, 10–16 weeks) |
| Research Laboratory operating change | **+$3,000 a week**, from the week it completes | K3 band ($2,500–$5,000); nothing accepted prices a laboratory |
| Scientist payroll | **$2,000 a week** per named researcher, charged whether or not a project uses them | S1; `P13-OD-03` |
| Laboratory seats | **4** | catalogue §5.2 |
| R&D usable spend ceiling (saturation) | **$40,000 a week** for a full four-seat laboratory; spend above the usable amount is never charged, and the bottleneck is named | S1; PREQ-5 |
| Research output | **1.0 unit per researcher-week**, times a **1.5** budget multiplier at saturation, so four funded seats produce **6.0 units a week** | catalogue §5.2 |
| The synchronized-sound brief | **W2, 64 units**: 11 funded weeks, **$440,000** of spend and **$88,000** of payroll | catalogue §5.2 and §9.7 |
| Acoustic instrument module | **$350,000 over 5 weeks**; **+$2,000 a week** | S1 capital; accepted floor operating |
| Stage site adaptation | **$450,000 over 9 weeks** for an older stage, **$50,000 over 1 week** for a nearly compatible one | S1 |
| Equipment installation | **$150,000 over 3 weeks** | S1 |
| Converted stage operating change | **+$2,000 a week**, from the week the installation completes, never during work | accepted floor; onset verified in §1.10 |
| Compatible capture package | **$75,000**, no operating change | S1 |
| Sound-capable Post fit-out | **$300,000 over 6 weeks**; **+$2,000 a week** | S1 capital; accepted floor operating |
| Commercial purchase, technology access | **$200,000**; **$0** for the inventor, which is an expense never incurred rather than a discount | catalogue §9.2 |
| Commercial purchase, production equipment per stage | **$300,000**; inventor's first installation **$0**, later installations **$225,000** | catalogue §9.2, design §16a.2 |
| New soundstage, if built instead of converted | **$2,400,000 over 16 weeks, +$9,000 a week** | **accepted `stage-standard` blueprint governs**, not S1's $2,000,000 over 20 weeks |
| Commercial-release milestone | purchasable at the **start of week 40** on the E1 fixture | fixture assumption, OPEN-4, D2 |

**What S1-A produces on envelope E1** (cash $3,000,000, 52-week horizon), recomputed from the catalogue's own two conventions:

| Result | S1-A |
|---|---:|
| Inventor startup, researchers kept from laboratory completion | **$3,235,000** |
| Inventor startup, researchers paid during research only | **$3,003,000** |
| Commercial buyer | **$1,491,000** |
| Startup premium | **117%** |
| Cash left from $3,000,000 | **−$235,000** / **−$3,000** / **$1,509,000** |
| Existing department against the buyer, over 52 weeks | research **$52,000 dearer** |
| Capability dates | inventor **week 41**, buyer **week 52**, an **eleven-week lead** |

#### B. Why this one

1. **It is the only candidate that satisfies ENG-1 on both readings.** Catalogue ENG-1 names exactly two doubly-priced objects and rules that "the accepted or accepted-anchored value governs in both". For the new soundstage that is unambiguous, and S1-A takes the accepted blueprint. For the weekly rate the sentence is ambiguous, because $1,200 is accepted-*anchored* (the K2 band floor) while $2,000 is the accepted value itself. **$2,000 is the only candidate that is both**: it sits inside the accepted-anchored K2 band and equals what the cheapest accepted building actually charges. $5,000 satisfies neither reading of ENG-1, which is the weakness of the alternative in C.
2. **No runtime object ends up cheaper to run than the cheapest accepted building.** The $1,200 the catalogue used is the K2 band *floor*, not a price anything accepted charges; the accepted blueprints charge $2,000 to $9,000 a week. Shipping $1,200 would make a converted soundstage cheaper to run than the $400,000 Craft Services Annex.
3. **One conflict rule, and the two places it bites are both disclosed.** The same sentence settles the new-soundstage price and the weekly rates. It is not a pure uniform multiplier, and §4.2's claim is not that nothing moves: substituting the accepted soundstage widens the convert-beats-build margin from $1,600,000 and eight weeks to **$2,000,000 and four weeks**, which catalogue §9.1 already publishes as the figure against the accepted blueprint. Conversion still wins on both cost and date, so no ordering moves; the margin does.
4. **The comparisons internal to H1 survive.** The research, payroll, spend, deployment and schedule inputs are all still a single uniform multiplier on H1, so the §9.2 orderings, the §9.4 allocation results and the break-evens hold. What the operating rate does move is the crossover, which D states exactly rather than asserting it is unchanged.

#### C. The meaningful alternative: candidate scale **S1-U**, uniform S1 including operating

Same capital, same durations, same payroll, same spend, same dates. The single change is that the multiplier that sets every capital value also sets the operating changes: H1's M4 `opexChange` of $1,000 a week becomes **$5,000 a week**, carried across the instrument module, the sound Post room and the converted stage. The laboratory stays at **$3,000**, because H1 prices no laboratory operating change for the multiplier to act on.

| Result | S1-U |
|---|---:|
| Inventor startup, researchers kept | **$3,430,000** |
| Inventor startup, researchers during research only | **$3,198,000** |
| Commercial buyer | **$1,515,000** |
| Startup premium | **126%** |
| Cash left from $3,000,000 | **−$430,000** / **−$198,000** / **$1,485,000** |
| Existing department against the buyer, over 52 weeks | research **$220,000 dearer** |

Its honest argument is consistency: one multiplier governs the whole fixture, and rejecting it means accepting that capital and operating are set by different rules. Against that, it is the one candidate ENG-1's "accepted or accepted-anchored value governs" sentence does not reach, since $5,000 is neither an accepted value nor a band anchored to one. Its cost is proportion. At $5,000 a week a $350,000 instrument module costs as much to run as the $1,150,000 Post Building, which inverts the capital-to-operating relation the §5.1 bands exist to hold.

#### D. The consequential difference between them

**Operating cost is the whole difference.** Capital, durations, payroll, spend, output and every date are identical. Both keep the decision's shape: waiting is cheaper in cash, research is earlier in weeks, and the eleven-week lead is real under either.

1. **The price of being early moves about fourfold.** For a studio that already has the department, whose sound is operational at week 24 against the buyer's week 52, the **twenty-eight-week** lead costs **$52,000** over the horizon under S1-A and **$220,000** under S1-U. (The *startup* comparison's eleven-week lead is priced by the premium row instead, 117% against 126%.) The mechanism is the same in both: $60,000 of project expenditure saved against 56 extra body-weeks of holding the capability (28 on the stage, 28 on the Post room). S1-A makes being early a modest, arguable premium. S1-U makes it a commitment a studio can visibly regret.
2. **It decides whether a founding studio can self-fund invention at all.** On E1 the inventor route ends at **−$235,000 or −$3,000** under S1-A and **−$430,000 or −$198,000** under S1-U, against **−$183,000 or +$49,000** at the $1,200 floor the catalogue used. The route fits inside $3,000,000 in exactly one of those six cases, the $1,200 floor on the project-only employment assumption, and the catalogue itself records that this assumption "assumes hiring and release are free and instant, which nothing in the accepted product supports" (§9.2). On the employment assumption the accepted product does support, no candidate rate lets the startup route fit. This is a product question about the intended difficulty of the opening, and the answer should be the envelope or the programme size, not a quietly cheaper weekly rate.
3. **It sets how loudly the operating charge reads.** Under S1-A the charge is legible but rarely decisive; under S1-U it is the dominant recurring line in the first year and will drive a player to time installation against release rather than build early.

4. **The 24-stage crossover holds under S1-A and moves under S1-U.** The catalogue's displayed expression carries the department's fixed cost, which includes the instrument module's standing charge at the K2 rate (§9.5: $1,732,000, of which $162,000 is standing charges). Re-solving `(1,905,000 + 35r) + 825,000n < 200,000 + 900,000n` gives `n > 23.29` at $1,200 (first integer **24**, as published), `n > 23.67` at S1-A's $2,000 (still **24**), and `n > 25.07` at S1-U's $5,000 (**26**). The bounded first-integer result the catalogue publishes therefore survives S1-A unchanged and must be restated under S1-U.

**What does not change under either.** The §9.2 orderings and break-evens; the §9.4 allocation results; converting an owned stage beating a new build on both cost and date; the eleven-week lead and the twenty-eight-week existing-department lead; the T2 nine-week acceleration; and the fact that the catalogue's published sign holds at every rate.

### 4.3 OPEN-2, the order in which additional catalogue consumers are authored

**OPEN-2, which additional catalogue consumers are authored first**, is answered with a sequence rather than returned as a list. Recommended order, cheapest real consumer first: Set size classes for CAT-011, then a setup workload for CAT-012 and CAT-025, then Set turnover for CAT-013, then a costume workload for CAT-018, then property identity for CAT-017b, then a deliverable format for CAT-039, then a planning-evidence owner for CAT-044. Each unblocks one named entry, and none is a first-sound dependency.

---

## 5. Ownership, and the acceptance tasks that prove it

**The split, restated so no one has to infer it.** P09 owns the physical job, its progress, its reservations and its completion. P13 owns eligibility, research, adoption and the capability consequence. P11 owns cash, ledgers and charge onset. P12 owns studio and rival identity; P13 references its IDs and opens no second registry. P10 owns people; P13 adds a role, not a person store. One physical work owner and one commit, per `FUP-019`: no duplicate progress, reservation, charge or capability grant.

**Acceptance tasks, mapped to the existing L0–L9 proof pyramid (design §28).** Each is a check, not a promise of a date.

| Layer | P13A acceptance task |
|---|---|
| L0 | The synchronized-sound catalogue entry lints: unique ID, acyclic reachable prerequisites, valid capability, stable ordering |
| L1 | Research, wait and purchase each reach a truthful outcome; an ineligible route refuses with the exact missing element |
| L2 | Player and rival resolve **adoption** through the same function; no hidden RNG; no duplicate capability grant. P13A's rival buys rather than researches, so this proves symmetric adoption law and **must not be reported as symmetric research** (§4.1) |
| L3 | Save V19 round trip mid-research; unknown-ID refusal; a pre-P13 save loads honestly with no invented research history |
| L4 | One rival consequence is deterministic and replayable, by the commercial route on capacity the rival already owns; sound gates one exact production decision; the operating charge appears in the advance after completion and not before |
| L5 | 6,240-week run with bounded storage growth and bounded migration duration |
| L6 | The new view value carries closed DTOs, pinned `sessionId` and `expectedStateRevision`, and refuses stale intent |
| L7 | Laboratory and soundstage states are legible on the lot at target viewports |
| L8 | Mouse, keyboard, controller, focus, Back and Locate on the research route |
| L9 | Owner performs the named routes and rules KEEP, REVISE or REJECT |

**A lawful silent film with no sound capability anywhere must pass at L1 and L4.** That is the preservation test for the silent route and it is cheap to state now and expensive to retrofit.

---

## 6. Migration and persistence proof plan

**Starting point: V19, not V15.** Design §18.1 and §31 pin a V15 envelope from a historical baseline. The accepted save head is inner Save V19, so every P13 migration statement starts there. Protocol 4, projection 29, outer checkpoint 1 and library storage format 2 are the other four pins, all verified in §1.10.

**What makes this expensive, stated up front.** `src/core/save.ts:1669-1671` validates `era` with `v8ExactKeys(era, [4 keys], [])`, so adding one field to `EraConfig` is a governed Save V19, protocol 4 and projection 29 migration, not a local edit. The same discipline applies to `StudioIdentity`, which the save validator rejects any extra key on, and to the `STUDIO_HISTORY_KINDS` table at `save.ts:4905-4915`, where omitting a new kind fails load rather than degrading.

**Proofs.**

1. A pre-P13 save at V19 loads with no P13 state and produces no invented research, expenditure or technology history. Week ordering in `studioHistory` already refuses retrospective events; the proof records that it does.
2. A mid-research save round-trips: accumulated work, budget ceiling, assigned person and facility IDs, provenance and RNG all restore without rerolling.
3. Save As branches a complete world and its stored RNG under a new storage ID, the original is preserved unchanged, and research state does not leak between the copies. Copies may legitimately share entity IDs, so the proof asserts that receipts and caches are scoped by campaign and session, not by entity ID.
4. An inactive campaign does not advance: its research makes no progress while another campaign ticks.
5. Rename changes the catalogue label only.
6. Migration duration is measured at the endurance horizon and reported against the storage budget, not asserted.

---

## 7. Execution ceiling and protected verification reserve

**Not copied from P12.** P12A ran a ceiling of 72 productive lead hours against 96 elapsed, with the final 24 lead hours protected for verification, corrections and delivery. P13A's shape differs in both directions: its feature surface is narrower, one technology and one laboratory, but it carries eighteen NEW P13 WORK connections, four closed unions to widen, a governed save migration from V19, and a native entry moved earlier. Reconnaissance is already done and does not need re-budgeting.

**The denominator, stated so nobody has to infer it. The 80 hours include the reserve.**

| Element | Hours | What it may be spent on |
|---|---:|---|
| Capability and implementation work | **52** | Building P13A Core, and nothing outside it |
| Protected verification, correction and delivery reserve | **28** | Correctness, migration and performance verification, corrections, delivery. **Never new capability, and never reallocated to finish a feature** |
| **Productive lead total** | **80** | The two rows above, summed. This is the productive ceiling, not 80 plus 28 |
| Elapsed ceiling | **108** | Wall clock. P12A sustained 96 elapsed against 72 productive, a 4:3 ratio. Applied to 80 productive hours that projects about 106.7 elapsed hours; the retained 108-hour ceiling adds a small conservative whole-hour buffer, and Current Ops' review directs that this ceiling stands |

P12A's 72 productive carried 24 protected, so 48 of capability. P13A proposes **52 of capability and 28 of reserve**: four more capability hours for the prerequisite stack and the governed V19 migration, four more reserve hours for the wider verification surface, and no re-budgeting of the reconnaissance this package already completed. The reserve is 35% of productive hours against P12A's 33% (24 of 72), because P13A carries a governed save migration P12A did not.

**Gate split, applied to the 52 non-reserve hours and not to the 80.** These are planning allocations for orientation, not contractual mini-budgets.

| Gate | Share of 52 | Hours | What its product critique inspects |
|---|---:|---:|---|
| Prerequisites and the early playable slice | 35% | ~18 | A laboratory on the lot, a named researcher in a seat, a funded project and an estimate that moves |
| Completed causal Core | 40% | ~21 | Research, wait and purchase reaching truthful outcomes; the filming lock; one rival consequence; the charge appearing after completion |
| Integrated / final P13A Core candidate | 25% | ~13 | The whole Core slice together on real screens, with migration and disclosure behaving |
| Protected verification, correction and delivery | outside the split | 28 | Ring-fenced; the split above does not reach it |

**Escalation criteria. Stop and return to Current Ops when any of these is true**, rather than spending the reserve:

- The governed V19 migration cannot preserve an existing save without loss.
- A rival consequence cannot be delivered without a rival capacity schema change, or without a persisted-shape change to `RivalBusiness`, `RivalAccount` or `RivalFinancePeriod`, contradicting §4.1.
- The `research` requirement activation turns out to need more than the three `blueprintRequirements.ts` edits plus its player-facing copy.
- Any P13 change would make `rivalCapacityOpex` return `undefined`, or would break an exhaustive `never` guard that the compiler does not catch.
- Measured incremental P13 save or load cost exceeds the retained P12 figures in §8 by a margin Current Ops sets at activation.
- The reserve is reached with any L0–L6 acceptance task unmet.

**No completion time is promised here**, and none should be inferred from these numbers.

---

## 8. Performance limits carried forward

**Retained in full, not summarized.** The handoff retains three figures; the attestation at `d4e1915:evidence/p12a/final-native-20260911/performance-attestation.md` retains roughly a dozen. A launch package built from the handoff alone would understate them, so the full set is carried here. These bind **TS `592e926` / Unity `2bc8d30`**, against the immutable 197-file package manifest `25facdbc105674ed86153568ac27b1e60558679431288ca7f9261b37a7c0ac64`.

| Phase | Median ms | p95 ms | Max ms |
|---|---:|---:|---:|
| Production durable write | 52.011 | 53.564 | 56.602 |
| Complete library load to ready | 13,322.655 | 13,414.888 | 13,423.440 |
| Complete campaign Save | 9,517.586 | 9,577.754 | 9,612.708 |

- Native Save response **10.542 s**; Save first observed re-enabled about **11.792 s** after submission; 41 sampled maps advanced 1,740 frames during the wait. That shows rendering progress and **does not measure input responsiveness**. **UI-unresponsiveness duration remains unmeasured.**
- Save p95 improved 47.51% and load-ready p95 27.48% against the unchanged sampler control. Improvement is not target attainment.
- Standalone high-water **RSS 4.821 GB**, a reference cost and not a minimum machine specification.
- The 30-second frame window has **zero Save overlap**; its 8.504 ms p95 and 85.573 ms max must never be presented as Save-time frame performance. Its 1,368 non-live frames stay disclosed despite continuity true and zero recorded outages.
- **Original targets still missed:** paired full serialize plus digest **199.524 ms against 100 ms**; Hollywood field delta **37,829,874 bytes against 8 MB**; durable film average **3,146.433 bytes against 1.5 KB**.
- **Unmeasured:** the 12-active / 48-project / 4,000-film / 50-archived-studio stress, and isolated write-only worker handoff, queue and durable-ack transport.
- Intermittent **Reconnecting** observations remain recorded despite no recorded transport outages. This one is retained by the handoff (`13370d4`) and by the evidence JSONs at `d4e1915`, not by the attestation Markdown; the attribution is split here rather than merged.
- Still binding regardless: permanent history, strict validation and references, exact receipts, campaign isolation, page bounds, and the **synchronous engine-main storage 50 ms prohibition**.
- No workload, profile, build or native input was rerun for the attestation, or for this package.

**Incremental P13 risk, assessed rather than assumed.** P13 adds state to a save whose complete-campaign Save p95 already sits at 9.58 s and whose Hollywood field delta already exceeds its 8 MB target by 4.7 times. Research projects, budgets, queues and provenance are all per-studio and per-campaign, so they scale with ten identities rather than with film count, which is the smaller of the two growth curves. The measurable risk is concentrated in three places.

**Proposed focused measurements, not an optimization programme.** Measure the P13 state delta in bytes per studio at the endurance horizon against the existing 8 MB Hollywood budget; measure migration duration for a V19 save carrying full P13 state; and measure complete Save p95 with P13 state present against the retained 9,577.754 ms baseline. Those three numbers decide whether any repair is genuinely necessary. No broader optimization work is proposed, and none of the open P12 items is reopened by this package.

**Howard's data.** His current campaigns and named copies are authoritative mutable user data. They were not opened, hashed, replaced or inspected in producing this package, and they must not become an engineering fixture during execution. Any authorized later test needs protected copies and an explicit ownership boundary.

---

## 9. Status

**Hook remains INACTIVE / EXPLICIT-CHECKER FALLBACK.** Explicit milestone and evidence checks were used throughout. No hook was installed, repaired or activated.

This package stops at launch-review readiness. P13 execution requires a separate Current Ops order.
