# Bounded specialist assignment

Task ID / phase / desired player or technical outcome:
Agent role and requested model (record actual model when observable):
Mode: READ_ONLY / DESIGN_PROTOTYPE / IMPLEMENT / VERIFY
Controlling Current Ops order and exact applicable Owner decisions:
Repositories / exact source commits / owned worktrees / working candidate identity:
Allowed write paths (empty for read-only) / explicitly excluded paths:
Inputs actually provided and relevant requirements:
Known-working behavior that must be preserved:
Acceptance checks / authorized commands / disposable fixtures:
Native input owner and slot (none unless assigned):
Evidence/output path and checkpoint expectation:
Allowance / escalation condition / stop rule:

Return:
- DONE / PARTIAL / BLOCKED and what that covers.
- Exact changed paths and candidate identity (or no changes).
- Requirement-to-result mapping and checks actually run.
- Evidence locations, failures, untested limits and important tradeoffs.
- Next concrete action and any ownership handback.

No hidden research restart, model escalation, delegation, state mutation, package advancement or
review-policy change follows from receiving a specialist task. Parent persists read-only returns.
