# Project: Studio — AI Music Foundry Marathon 01 Resume

The machine-readable recovery authority is:

`/Users/bruce/Project Studio Audio Foundry Marathon 01/00_state/MARATHON-STATE.json`

Current documentation worktree:

`/Users/bruce/The Movies - Audio Marathon 01`

Current branch:

`codex/era-aware-audio-marathon-01`

Recovery law:

1. Read the state JSON and confirm its time-window and `next_resumable_step`.
2. Verify Git local/remote SHA equality without switching another worktree.
3. Re-run the read-only process collision gate before any inference.
4. Verify retained marathon bytes remain below 80 GiB.
5. Never overwrite an existing raw file; generation scripts skip hash-verified completed rows and stop on any unknown pre-existing path.
6. Keep `HF_HUB_OFFLINE=1`; use only the recorded pinned Small or Medium route for any separately authorized rerun.
7. Continue independent CPU-light work if Unity, Project: Studio, batchmode, or a known P05 proof process is active.
8. Atomically update state after each completed phase with `tools/audio_foundry_marathon_01/state_update.py`.

All authorized lanes are complete. The library contains 144 canonical Small sources, 20 one-round rescue sources, nine one-shot Small refinements, and 18 Medium comparisons. Thirty-five families pass the machine gate. `NHY-04` is frozen as `FAMILY NEEDS OWNER / HUMAN AUDIO DIRECTION`; there is no R2 route. The provisional shortlist contains 27 primaries and 27 alternates. The 126-unit radio bank, 30 scratch delivery units with clean/period variants, three mixed radio demos, 12 unselected motif sketches, nine endurance simulations/demos, offline audition application, integration metadata, and Desktop return package are complete.

Normal next action is Owner review, not more autonomous generation:

1. Open `/Users/bruce/Desktop/Project-Studio-Audio-Return-Package-01/START-HERE.md`.
2. Double-click `AUDITION-APP/START-AUDITION.command`, then open `http://127.0.0.1:8765/`.
3. Rate the provisional music and radio concepts blind where useful, add notes, and export CSV and JSON feedback.
4. Decide which weak families/refinements/Medium comparisons merit another brief, and obtain the needed human music-history, creator, cultural, and rights review.
5. Authorize a separate Unity audition-import task only after selecting exact source hashes.

If a technical recovery is needed, use the external state and manifests. Never regenerate or overwrite an existing raw destination. A rerun should verify an existing artifact by hash or stop on mismatch.

The isolated worktree was recovered from pushed SHA `6d83a382a7da7ddac99dca0c574f978dbc084812` after a quarantined radio self-test cleanup defect emptied that directory. External audio/evidence was unaffected. Never reintroduce recursive staging cleanup; the corrected radio and endurance tools preserve failed staging and only publish via exact-path atomic rename.

The return packager was initially placed in the original checkout's ignored `tools/` path. It was moved byte-for-byte into this isolated marathon worktree immediately; the original path was verified absent and no tracked or production file changed.

No completion statement in this document constitutes human listening acceptance, a rights ruling, or production authority. All assets remain `PROTOTYPE_ONLY` or `PROTOTYPE_READY_FOR_OWNER_AUDITION`; Unity integration remains prepared but not executed.
