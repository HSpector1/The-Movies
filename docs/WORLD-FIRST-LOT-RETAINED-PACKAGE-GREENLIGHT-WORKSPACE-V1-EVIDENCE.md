# World-First Lot-Retained Package & Greenlight Workspace V1 Evidence

Status: **IMPLEMENTED, VALIDATED, AND RETAINED ON THE AUTONOMOUS MARATHON BRANCH**

Date: 2026-08-15

Branch: `operation-hollywood-autonomous-marathon`

Frozen contract authority: `cc2c4af067f681d1a26f10959eb1c0dbd7512d0d`

Accepted parent / Lot-Native Casting Review closure:
`5c6f7573c85498fde2ce17c39d7058e13e4fdd06`

Implementation authority: `729afb72d345e3430655d33f6b73ed8c7a33f1df`

Closure documentation authority: the documentation checkpoint containing this record; its commit
SHA is intentionally not guessed before that checkpoint exists.

## Keep ruling

World-First Lot-Retained Package & Greenlight Workspace V1 passes its bounded Keep gate.

The retained ordinary-player loop is:

```text
LIVE LOT → EXACT CASTING REVIEW → TAKE RESULTS TO PACKAGE
→ EXACT CASTING SUCCESSOR COMMITS + AUTOSAVES
→ CANONICAL PACKAGE WORKSPACE OPENS OVER THE SAME MOUNTED LOT
→ CHOOSE COMPANY / BUDGET / MARKETING WITH EXISTING PACKAGE LAW
→ CANCEL: SAME LOT / VIEW / CANVAS / CAMERA, BYTE-NEUTRAL
→ ACCEPT: EXACT GREENLIGHT SUCCESSOR COMMITS + AUTOSAVES
→ WORKSPACE CLOSES → SAME MOUNTED LOT PAINTS PICTURE FORMED
→ CONTINUE LIVE LOT PLAY
```

The Package remains a substantial deep management surface, but it now supports the world instead
of replacing it. The player carries exact Casting evidence into the canonical Assembly, can inspect
Talent or create Custom Talent, can cancel, or can greenlight and immediately see the exact formed
picture and company in the same living studio.

This is a bounded host-continuity and presentation repair. It adds no watched preparation,
rehearsal, shooting, Post, travel, occupancy, queue, workload, or new simulation law.

## Exact authority and same-mounted invariant

App owns one independent transient Package-presentation session with `editing` and `committed`
phases. It is not a new `Screen`, is not serialized, and does not create another App, Lot, or
renderer tree.

Across Package open, nested Profile, explicit cancel, Custom Talent, and accepted greenlight, the
ordinary Operation Hollywood path retains exactly:

- the same `kind: 'lot'` Screen object;
- the same opaque App-owned Lot presentation token;
- the same mounted `StudioLotScreen` component;
- the same `StudioLotView` / Phaser instance when the renderer is available;
- the same canvas DOM node;
- the same camera and Lot-local selection/presentation memory; and
- one authoritative App/GameState tree.

The Package layer suspends all semantic and renderer-owned world input while leaving the studio
visibly alive behind it. Its neutral scrim does not cancel. There is one bounded internal scroll
owner and no duplicate application shell, top bar, or page scroll owner.

Classic / Operation-Hollywood-off compatibility remains exact: the same accepted Casting handoff
uses the established standalone Assembly screen and prior remount behavior. No retained Hollywood
host claim leaks into Classic.

## Casting successor, autosave, and workspace-open order

The existing strict Lot-native Casting authority remains unchanged. After the one clear
acknowledgement:

1. App commits the exact `review → complete` Engine successor;
2. the established autosave effect persists that successor;
3. a later microtask independently re-proves exact state, screen, presentation token, project,
   accepted clear result, and absence of a conflicting Profile; and
4. only then does one canonical Package workspace open over the retained Lot.

Blocked Casting successors keep the accepted prior same-Lot behavior and open no workspace.
Presentation drift after a valid Engine successor cancels only the optional workspace; it never
rolls back or re-dispatches the accepted action.

The workspace begins on the canonical Cast & crew step for the exact Ready screenplay, carries the
existing six-row Casting evidence, and preselects no audition winner or Talent.

## Canonical Assembly, cancel, and rejection

The implementation reuses `Assembly`; it does not copy package logic. A bounded
`surface="lot-workspace"` presentation adapter removes standalone shell chrome and targets the
workspace scroll owner while preserving:

- the same candidate pools, filters, sorting, availability, and profiles;
- Production/Craft requirements and complete package legality;
- Production Budget, marketing menu, forecast, solvency, and blockers;
- Custom Talent;
- the canonical review step and sole **Greenlight this film** action; and
- existing Engine rejection, error, latch-release, and legal retry behavior.

Explicit cancel changes no GameState or save byte, closes only the exact current session, returns
to the same Lot/view/canvas/camera, and restores the exact connected Casting-success opener when it
still owns the presentation. It does not replay the consumed Casting receipt.

Canonical Assembly rejection coverage proves one Engine call, preserved mounted draft/error, and a
fresh legal retry after latch release. Because Assembly calls App only after Engine acceptance, a
rejected attempt leaves the exact retained workspace, App state, autosave bytes, and world behind
it unchanged.

## Custom Talent and nested Profile

The existing Talent Creator renders in place inside the canonical Assembly. An accepted creation
may update App state only while the exact workspace identity, Lot Screen, presentation token, and
rendered before-state remain current. The one successor autosaves while the same Lot, canvas,
workspace, Assembly component, package draft, and step remain mounted.

Focus moves to the first creator mode on entry and returns to the exact **Create Custom Talent**
control after Back or accepted creation. No document scroll is used.

The canonical Talent Profile opens above the Package. While it owns the top modal:

- both the Lot and Package layer are inert;
- the Profile is the sole focus owner;
- retained Lot/Profile callback tails cannot open or close another owner; and
- close removes inert state before restoring the exact connected **View Profile** opener.

The workspace focus trap never steals focus from that nested Profile. Accessible picker names and
`aria-current="step"` are shared maintenance annotations, not business behavior.

## Accepted greenlight and live formation

Assembly still calls the existing `greenlightScriptProject` and emits its strict nullable formation
receipt. App independently requires the exact current workspace, exact rendered before-state,
same Lot Screen/presentation token, no Profile conflict, and an independently recomputed
`acceptedGreenlightFormationReceipt`.

For an exact formation:

1. App synchronously accepts the one Engine successor;
2. a noninteractive committed workspace prevents an invalid in-production Assembly flash;
3. the established autosave effect persists the successor while the workspace still covers the
   same Lot;
4. a later revalidated microtask atomically closes the workspace and publishes one transient live
   receipt; and
5. the already-mounted `StudioLotScreen` consumes that receipt once.

The retained Lot then exposes exact **PICTURE FORMED**, title, project/production identity,
Development phase, reservation/status/countdown, selected Director, independently selectable Lead,
and complete six-person company. Multiple-picture and array-order proof selects the receipt-owned
picture rather than a first/last candidate. Stale or mismatched presentation receipts preserve a
valid Engine/autosave successor but expose neutral Lot truth instead of guessing.

Renderer delay, rejection, recreation, Profile suspension, and rerender cannot replay the witness
or substitute another production. The semantic formation remains complete when renderer creation
fails.

## Stale callback, replacement, teardown, and input containment

Screen drift, Lot presentation unmount, whole-studio replacement, accepted load/import, confirmed
New Studio, and App teardown synchronously clear Package handoff, workspace identity, committed
close, live receipt, and held activation memory. A retained callback from an old workspace cannot
cancel, mutate, focus, or greenlight a newer owner.

Pointer, mouse, touch, Enter, Space, repeated/cross-key tails, virtual-AT clicks, pointer cancel,
neighbor transitions, visibility change, nested modal ownership, and compatibility clicks are
exact-once or neutral. Background Lot controls, pan/zoom, semantic destinations, time actions, and
renderer pointer input remain inert for the complete workspace lifetime.

## Responsive, accessibility, and real-browser proof

The retained Package was played in ordinary Chromium at desktop scale, 960×540, effective 200%,
forced colors, reduced motion, and 480×270 CSS pixels at device scale factor 2.

At wide desktop the large right-anchored workspace leaves the studio visibly present. At compact
sizes it may cover most pixels but still keeps the same underlying Lot/canvas mounted. The header,
active step, explicit close, and primary action remain reachable through one internal scroll owner.

The effective-200% hard gate measured the close control at
`x=564.046875, y=45.5, 299.953125×88` inside a 960×540 visual viewport. It passed full containment,
44px minimum dimensions, a real pointer trial, focus-plus-Enter cancellation, one scroll owner, no
page overflow, and same-canvas identity.

Manual in-app browser play imported the real Casting-review save, selected a legal package, opened
and closed a nested Profile, cancelled once, then replayed and accepted greenlight. Lot/canvas
counts remained 1/1, browser errors/warnings/failed requests remained clean, and the exact formed
title/company appeared in the same canvas.

Retained screenshots:

- `out/world-first-lot-native-next-event-v1/02-casting-lot-native-package-handoff.png`;
- `out/world-first-lot-native-next-event-v1/02b-casting-profile-over-retained-package.png`;
- `out/world-first-lot-native-next-event-v1/02c-casting-package-960x540.png`;
- `out/world-first-lot-native-next-event-v1/02c-casting-package-cancel-same-lot.png`;
- `out/world-first-lot-native-next-event-v1/02d-casting-greenlight-same-canvas-formation.png`;
- `out/world-first-lot-native-next-event-v1/02d-casting-package-forced-colors-200.png`; and
- `out/world-first-lot-native-next-event-v1/02f-casting-package-480x270-dsf2.png`.

## Save, simulation, content, renderer, and economy boundary

Implementation `729afb72d345e3430655d33f6b73ed8c7a33f1df` changes thirteen UI and UI-test
files with 2,116 insertions and 81 deletions. Runtime change is bounded to App/Lot interaction,
modal hosting, presentation adapters, accessibility/focus maintenance, styling, and one transient
live-formation input.

No file under `src/core/` changed. No GameState, SaveFileV1–V11, schema, migration, import/export,
Casting, screenplay, package, greenlight, assignment, staffing, facility, capacity, production,
release, construction, publicity, employment, tick, RNG, money, ledger, cash, payroll, overhead,
awareness/reach, economy, or outcome behavior changed.

No authored/generated art, source/runtime manifest, exporter, atlas, texture, Place, building,
route, actor, animation, draw owner, renderer clock, or district geometry changed. The exporter was
not run. The governed one-production fixture remains exactly **42 objects / 19 actors / 11,096,896
decoded bytes / one draw**. Structural equality is not relabelled GPU/FPS certification.

V1 adds no physical Hollywood Casting or Writers building, worker, personal location, travel,
arrival, occupancy, queue, workload, task, stress, fatigue, relationship, autonomy, pathfinding,
character control, watched intermediate production, or second simulation.

## Final automated verification

| Gate | Result |
| --- | --- |
| Complete UI suite | **PASS — 108/108 files, 1,388/1,388 tests** |
| Complete repository suite | **PASS — 195/195 files, 2,618/2,618 tests** |
| Governed D-16/D-17 config | **PASS — 10/10 files, 176/176 tests** |
| Full next-event Chromium specification | **PASS — 19/19** |
| Focused retained-Package Chromium matrix | **PASS — 4/4** |
| Greenlight-formation Chromium specification | **PASS — 10 passed / 1 explicit GPU-only skip** |
| D-14 career Chromium regression | **PASS — 7/7** |
| Root and UI TypeScript projects | **PASS** |
| Production build | **PASS — 148 modules transformed; existing large-chunk warning retained** |
| Direct Engine action / SaveFileV11 parity | **PASS — exact bytes** |
| Desktop, 960×540, effective 200%, 480×270/DSF2, forced colors, reduced motion, renderer failure | **PASS** |
| `git diff --check`, protected paths/refs, structure, manual play, independent P0/P1 audit | **PASS** |

The explicit GPU wall-clock evidence case did not run and no GPU certification is claimed. No
threshold was relaxed.

Protected refs remain exact:

- `main`: `33eb33ae307904aa3f00db20bc695e40bf46d1e4`;
- accepted D-17B: `35d42687a410a621becf1df35c75986657f8c44e`; and
- Operation Hollywood bridge: `623b8b2a80e9c6b85304eaa2a338b6045e8f6b21`.

## Economic and publication boundary

The governing result remains exactly:

> **D-17B ACCEPTED — BOUNDED REPAIR, MACROECONOMY RESIDUALS REMAIN OPEN**

Cash runaway, top-studio economic immortality, the week-208 synchronized roster wall, P5
dominance, world-led variance, cheap-film purpose, premium-film purpose, remaining menu breadth,
and formal G12 timing remain open. This milestone certifies no complete macroeconomic balance and
authorizes no financing, loans, bailouts, restructuring, failure ladder, hard bankruptcy, or
arbitrary cash sink.

Nothing was merged to `main`, pushed, tagged, or published.
