# World-First Annex Construction Interaction V1 Evidence

Status: **FINAL AUTONOMOUS-MARATHON EVIDENCE**

Date: 2026-08-14

Branch: `operation-hollywood-autonomous-marathon`

Contract authority: `6cab9c90cb185faffad096083f5309796012d287`

Implementation authority: `7a370fda50bc6f9065918098de24455195414af8`

## Result

The retained implementation closes the next bounded break in continuous world play. The visible
Development & Casting Annex parcel and its lifecycle label now select one exact same-lot context.
That context reads every construction fact from the existing authoritative
`studioDevelopment(state)` projection and, while Vacant and legal, sends one parameter-free start
intent to the App-owned `startDevelopmentCastingAnnexAction` boundary.

An accepted start debits the existing $780,000 capex once, creates the existing 13-advance Building
project, and immediately repaints cash, parcel, progress, due week, focus, and announcement in the
same mounted Studio Lot. The player can then use the already-shipped live week action to watch
Building progress reach Operational without leaving the world.

```text
VISIBLE FIXED ANNEX PARCEL
→ PHYSICAL OR SEMANTIC SELECTION
→ EXACT SAME-LOT PRICE / CASH / CLOCK / CAPACITY TRUTH
→ ONE AUTHORITATIVE BUILD INTENT
→ SAME CAMERA AND LIVING PRODUCTION
→ VACANT → BUILDING → OPERATIONAL
```

This is a world-interaction repair around the accepted Annex. It is not a facility catalogue,
placement system, construction simulation, new cash sink, or second simulation authority.

## Exact implementation surface

Implementation commit `7a370fd` changes exactly 14 files:

- `ui/e2e/lot.spec.ts`;
- `ui/src/App.tsx`;
- `ui/src/lot/StudioLotScreen.import-race.test.tsx`;
- `ui/src/lot/StudioLotScreen.test.tsx`;
- `ui/src/lot/StudioLotScreen.tsx`;
- `ui/src/lot/StudioLotView.hollywood.test.ts`;
- `ui/src/lot/StudioLotView.ts`;
- `ui/src/lot/WorldFirstAnnexConstruction.test.tsx`;
- `ui/src/lot/hollywood/HollywoodScene.test.ts`;
- `ui/src/lot/hollywood/HollywoodScene.ts`;
- `ui/src/lot/lot.css`;
- `ui/src/lot/navigation.ts`;
- `ui/src/screens/StudioDevelopment.tsx`; and
- `ui/src/screens/studio-development-ui.test.tsx`.

The runtime change is bounded to App/React action ownership, same-lot context/focus/announcement
presentation, physical and semantic Annex selection, input containment, responsive styling, and
the historical deep-route ownership description. No core action, tick, GameState/SaveFileV1–V11
schema, migration, facility rule, construction rule, reservation, production, publicity,
awareness/reach, economy, ledger/accounting rule, random draw, district manifest, image asset,
texture allocation, actor, display object, or renderer draw-budget source changed.

## Authoritative interaction and lifecycle proof

| Frozen law | Retained proof |
| --- | --- |
| Exact physical identity | Polygon and visible lifecycle label share one selector for `annex-parcel` + `expansion`; the scene emits identity only. Wrong, stale, absent, mismatched, or duplicate visual projections fail closed. |
| Semantic parity | The native `expansion` companion enters the same context by pointer, `Enter`, and `Space`; a missing renderer or Hollywood manifest blocks only the physical outline, not authoritative DOM truth. |
| One read model | Vacant, Building, Operational, legacy, affordability, price, clock, cash, capacity, progress, and consequence are field-exact to `studioDevelopment(state)`. No presentation constant reconstructs construction law. |
| One action owner | App invokes the existing parameter-free adapter action against its latest state. Accepted output replaces App state once; rejection returns the exact core error without an alert or second owner. |
| Exact successor | One start produces one $780,000 debit, one correlated capex row, one parcel claim, and one Building project due 13 weekly advances later; it adds no facility, advances no week, and consumes no RNG. |
| Same living world | The same `StudioLotScreen` and renderer survive. Camera preset/destroy/recreate are not called; production, people, Stage 7 operation, reservations, task, publicity, and RNG remain unchanged while cash and Annex truth repaint. |
| Focus and feedback | Vacant actionable selection focuses Build. Acceptance focuses persistent Building status and announces the exact commitment once. Rejection preserves context and replaces even an identical prior live-region child so retry feedback is observable. |
| Duplicate/stale safety | A synchronous pending guard prevents a rapid double commitment. Building, Operational, legacy, malformed identity, and current-unaffordable states expose no legal start; a stale post-render rejection preserves all authoritative bytes. |
| Clock continuity | Selected Annex context survives ordinary lot-origin advances, repaints exact progress, and yields to the existing sole completion focus/announcement on the thirteenth advance. |
| Reload truth | Native SaveFileV11 reloads reconstruct Vacant, Building, and Operational facts without replaying start or completion ceremony. |
| Deep compatibility | Dashboard, Calendar, Finance, recap, facility/capacity projections, and the complete standalone Studio Development surface remain available. Lot and deep-surface starts produce byte-identical GameState, RNG, ledger, and SaveFileV11 output. |
| Non-renderer paths | Delayed import initializes from latest post-action truth. Renderer construction/import rejection, reduced motion, and procedural rollback retain the same semantic inspect/start path. |
| Input and accessibility | Changed over-canvas surfaces contain pointer, mouse, and touch down families. Native Build supports pointer, `Enter`, and `Space` exactly once; status, shape, text, progressbar, focus, and atomic live-region ownership do not depend on colour or canvas. |

Automated boundaries also prove exact $780,000 affordability, $779,999 rejection with the core
reason, the valid `affordability.ok === true && !canStart` generic explanation, selection neutrality,
host-outline selection without event recursion, person/production/place context replacement, and
the absence of command leakage after selection changes.

## Deterministic SaveFileV11 replay

The existing public-authority generator was replayed before live acceptance. It reported every
fixture and its manifest `unchanged`; no fixture was copied or hand-edited.

| Fixture | Bytes | SHA-256 | Annex use |
| --- | ---: | --- | --- |
| Week 30 — *Nights of Watchtower*, Stage 7 scheduled | 227,429 | `e922f9b7e957388bed7c7674be8c17596245823200e478371dc7ff970458f46b` | Vacant, affordable physical inspect/start while a real production remains active. |
| Week 11 — Annex Building at 11/13 | 220,812 | `ffc443f9fb3a75ec9f79be967600bb94933c9dacdd0b4f0bee878c37cdbd03ac` | Building reload, Week-12 progress, and Week-13 completion. |
| Week 12 — Annex completion next | 220,793 | `63669586ad8f0256ac165a5141b8dd770dadc68840c3e69a31f9abc8b72ab712` | Isolated one-advance completion authority. |
| Week 12 — *House of Cipher* release + Annex co-event | 224,835 | `c7f03299b75e9d1c0a99bb3a47abdccfa1d854f33b29f2c72902c74ed41d64f2` | Unchanged generator output retained from the live-week corpus. |

Generator:

```text
node_modules/.bin/vite-node scripts/gen-live-week-advance-fixtures.mts
```

Manifest SHA-256:
`3f06eb81957c5f49fa5be3b8b0d3239e9c3305426b266be19ac5bf224b24905e`

## Ordinary-player live acceptance

### Week-30 physical inspect and accepted start

The exact Week-30 fixture was imported through the visible save UI and opened on the Hollywood lot.
The physical Annex parcel exposed exact Vacant truth without navigation:

- Week 30;
- cash now **$11,160,898**;
- capex **$780,000**;
- cash after **$10,380,898**;
- construction clock **13 weekly advances**;
- current shared Development & Casting capacity **2 slots**; and
- the exact permanent **+1 shared slot** consequence.

Pointer activation accepted one start. The same lot and camera immediately showed cash
**$10,380,898**, Building **0 of 13**, and committed completion in **Week 43**, with the single exact
success announcement. *Nights of Watchtower*, Estelle Delgado, the scheduled Stage 7 take,
reservations, production card, people, and RNG truth remained unchanged. One live week advance then
repainted Week 31 and Building **1 of 13** while the Annex remained selected.

Selecting a named person exited Annex command context as required. Returning to the physical parcel
restored the exact context and selected outline rather than opening Studio Development.

### Keyboard acceptance exception

The in-app browser controller focused both native semantic controls but did not synthesize their
physical `Enter`/`Space` activation in the live page. Required live keyboard acceptance is therefore
recorded as a controller limitation, not claimed as a physical-browser pass and not replaced by a
relabelled click.

The distinct automated native-control proof passes for both `Enter` and `Space`: each activation
invokes the no-payload App owner exactly once and produces the same authoritative successor,
focus, announcement, same-mounted-view, RNG, ledger, and SaveFileV11 result as pointer activation.

### Week-11 through Week-13 lifecycle and reload

The exact Week-11 fixture reopened the lot at Building **11 of 13**. The first ordinary lot advance
reached Week 12 and repainted **12 of 13** in the same selected context. The second reached Week 13,
painted Operational, reported **3 shared slots**, and gave the existing Annex completion notice sole
focus without a duplicate ordinary-week or generic Operational announcement.

That exact two-advance Operational successor was exported once and immediately reimported:

- bytes: **221,237**;
- SHA-256: `4b883cef80f920eccbcfddc09f9e06290cc0c3c5d9344f6cda2c71b3bc04b6cf`;
- parsed week: **13**; and
- project status: **completed**.

After reimport, physical Annex selection reconstructed exact Operational Week-13 truth and three
slots without replaying the completion notice. The existing generic persisted-Operational
orientation announcement remained allowed by the frozen contract. This same-session successor was
not promoted into a reusable fixture.

### Renderer failure and reduced motion

An isolated proof build removed only the lazy `StudioLotView` chunk. The initial visible save flow
loaded the exact Week-30 fixture; the complete semantic fallback still exposed Vacant Annex truth.
Pointer Build accepted the same $780,000 debit, painted Building **0 of 13** due Week 43, retained
the Stage 7 production/person truth, and stayed on the fallback lot. The temporary fault server was
stopped and its repository chunk was restored byte-exactly.

An isolated reduced-motion session supplied `prefers-reduced-motion: reduce` before the product
bundle loaded. The exact Week-30 semantic path remained complete and an accepted pointer start
produced the same **$10,380,898**, Building **0 of 13**, and Week-43 result with production/person
truth unchanged. The automated reduced-motion gate independently proves byte-identical state, RNG,
focus priority, and availability.

Both adversarial starts prove that Phaser readiness and motion are presentation concerns, not
construction legality or authority.

### Visual defects found and repaired

Live play found that the visible lifecycle label could be occluded at its original depth **88**.
The retained implementation raises only that existing label to depth **169**, above moving actors
while leaving the selected-place outline and person-nameplate priorities intact. No object, draw,
texture, or asset was added.

The same pass exposed a hover transition that could clear the already-selected Annex outline when
the pointer left another place. The retained repair redraws the current selected place on pointer
out, restoring persistent selection feedback. Both defects are covered by scene tests and were
replayed in the live lot.

### Responsive, zoom, performance, and diagnostics

| View | Live result |
| --- | --- |
| 1280×720 | Exact Annex context, Build, both topbar actions, production, and people remained reachable with no overlap or clipping. |
| 1366×768 | Same governed pass. |
| 1440×900 | Same governed pass. |
| 1920×1080 | Same pass plus warm performance measurement. |
| 1536×864, 125%-equivalent | Same governed pass under compact layout. |
| 960×540 | Additional responsive stress pass; exact facts and actions remained reachable. |
| Actual maximum in-world zoom | Repeated real wheel input reached the camera maximum; the physical Annex label/parcel, exact context, Build, and both topbar actions remained readable and selectable at human-story scale. |

At 1920×1080 the warmed Week-30 Hollywood scene measured **180 FPS average**, **143 FPS
1%-low**, **7 ms p99/worst**, **0.01 ms update**, one renderer draw, **33 display objects**, and
**15 dynamic actors**. The UI reports **10.6 MB** decoded textures; the exact governed total remains
**11,096,896 bytes**, with the unchanged **281 KB** role atlas.

Clean ordinary, responsive, maximum-camera-zoom, and reduced-motion acceptance ended with no
product warnings, errors, or failed requests. The deliberate missing-chunk diagnostic in the
isolated renderer-fault session is the injected fault itself and is not mislabeled as a clean-session
diagnostic.

## Final verification

| Gate | Result |
| --- | --- |
| Focused Annex matrix | **PASS — 6/6 files, 114/114 tests** |
| Complete repository suite | **PASS — 160/160 files, 1,952/1,952 tests** |
| Governed D-16/D-17 harness | **PASS — 10/10 files, 176/176 tests** |
| Lot Playwright suite | **PASS — 14/14 tests** |
| Root and UI TypeScript | **PASS** |
| Production build | **PASS** |
| Deterministic fixture replay | **PASS — every output unchanged** |
| Physical-browser native `Enter` / `Space` replay | **CONTROLLER LIMITATION — focus observed; activation not synthesized; exact automated native parity PASS** |
| `git diff --check` | **PASS** |
| Protected-ref verification | **PASS** |
| Independent strict review | **No unresolved P1–P3 findings** |

The production build retains the pre-existing large-chunk advisory. The Studio Lot remains
lazy-loaded; that advisory is not an Annex interaction correctness failure.

## Authority neutrality and open boundaries

This slice intentionally changes how the player selects and starts the already-authorized Annex.
It changes no authoritative simulation result: the pre-existing core read model and parameter-free
action still own every fact and transition. Complete SaveFileV1–V11 schemas and migration remain
frozen. Camera, selection, outline, focus, announcement, renderer readiness, reduced motion, and
route presentation consume no RNG and serialize no session authority.

No additional parcel, facility, fourth slot, construction queue, worker simulation, maintenance,
opex, placement, demolition, financing, or arbitrary cash sink was introduced. The accepted D-17B
residuals remain open and this interaction supplies no broad economic certification.

The governing status remains exactly:

> **D-17B ACCEPTED — BOUNDED REPAIR, MACROECONOMY RESIDUALS REMAIN OPEN**

Cash runaway, top-studio economic immortality, the week-208 synchronized roster wall, P5
dominance, world-led variance, cheap-film purpose, premium-film purpose, remaining menu breadth,
and formal G12 timing remain open. No financing, loan, bailout, restructuring, failure ladder, or
arbitrary cash sink was introduced.
