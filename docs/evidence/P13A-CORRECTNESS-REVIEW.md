# P13A correctness review

Review date: 2026-09-11. Scope: the authorized P13A Core implementation under `OPS-P13A-SYNCHRONIZED-SOUND-20260911-01`, especially catalogue/adoption law, person and production preservation, campaign persistence, receipt chronology and cash reconciliation. This was a targeted implementation review using the accepted reconnaissance and generated fixtures. No user campaign was inspected or used as evidence.

The TypeScript implementation worktree is `/Users/bruce/The Movies - P13A Synchronized Sound TS`, branch `wip/p13a-synchronized-sound-01-ts`. The accepted product base is `592e926bfbf4574df94b38fc8dd594fc5df2ac8d`; the initial implementation checkpoint is `4dd667dcf433971feabae15ffaf2fde5b5d15bb1`. The initial runtime corrections were committed through `64b8a2beda3c42e13288c88b75577058d02170d4`; the subsequent bounded performance correction is `50460f3aa14f77cfe38be15ed9d991cf983cfa92`, reviewed below. Each test run retains its own source chronology. The performance escalation does not turn unperformed native checks into passes. [The execution log](../campaigns/P13A-EXECUTION-LOG.md) records the authoritative packet and the ordered Unity base comparison. Protected refs were not changed.

The catalogue has one entry, `synchronized-sound`, with research eligibility at week 260 and commercial purchase at week 416. Catalogue lint checks identity uniqueness, ordering, acyclic reachable prerequisites, valid capability and actual P09 physical blueprints. Player and rival commercial access both call `commercialAccessRefusal`; both adoption routes call `adoptionRefusal`. These functions check the same technology access and exact compatible stage/Post capabilities. The rival uses its existing abstract plant and the dedicated `technologyAdoption` money movement. No rival research staff or research process is introduced. Research and commercial purchase share adoption eligibility, while their financing and existing physical owners remain explicit.

The live-engine causal fixtures establish one Scientist, 43 funded weeks, 64 verified work units, $430,000 of R&D and $86,000 of Scientist payroll during that research period. Research begins at 260, completes at 303, and the real P09 stage/capture/Post chain becomes operational at 315. Waiting grants nothing; purchasing access at 416 and installing the chain gives operation at 428. The observed difference is 113 weeks of forfeited lead. The tests do not assert that a waiting player must fall behind the rival.

The following independent checks preserve accepted behavior and authority:

| Area | Evidence and finding |
| --- | --- |
| Existing creative people and RNG | `tests/p13a-scientist-foundation.test.ts` compares three exact hashes emitted from an isolated archive of accepted commit `592e926…`. It verifies all 60 original people, concepts, market, existing era economics and simulation RNG. Only the additive neutral research leaves and the separately authorized D4 `soundRequired: false` correction are excluded from that historical comparison; D4 is asserted separately. Scientist generation uses separate deterministic purpose streams. |
| Person/employment ownership | The Scientist is a real Talent with research skill, ceiling, development, genre and work-history leaves. The six-key P10 Contract is unchanged. Payroll is $2,000/week while employed, including unassigned/paused weeks. Release and contract expiry pause active research; rehire/resume retains the same person/project and verified work. |
| Silent film and production decisions | `tests/p13a-production-technology.test.ts` compares silent allocation, operations, sets, costs, ledger and RNG against the consumer-disabled control. Exact sound stage/Post selection is enforced; filming locks the choice before any take assignment. Already-filming historical productions remain silent without fabricated technology history. |
| Pre-P13 migration | `tests/fixtures/p13a/README.md` identifies the two canonical Save V19 fixtures emitted by the actual accepted engine. Migration preserves their original employment, balances, receipts, identities and RNG. V20 adds empty technology, neutral research person leaves and zero `technologyAdoption` to every existing rival period; no employment, invention, access or spend is reconstructed. |
| Frozen boundaries | V1–V19 exported validators remain frozen. V20 passes its original people, money, physical records and technology through the modern validation policy. The current writer validates before JSON detachment; unknown/non-JSON authority cannot be silently dropped. Older writers refuse to discard Scientist or nonempty technology authority. Historical harness comparisons strip only demonstrably empty technology and exactly neutral research leaves, and reject any real research/person authority. |
| Campaign isolation | `tests/bridge-p13-campaign-isolation.test.ts` exercises actual coordinator Save As, Save, Load, Rename, New Game and restart. One branch advances and another pauses through a real Laboratory intent. Inactive checkpoint bytes stay unchanged. Rename changes the record label only. A newly generated world has a different seed/world identity and no inherited Scientist or technology. |
| Native bridge boundary | Laboratory tests validate closed DTOs, exact building identities, session/revision refusal, stale sibling intents and idempotent named hire. Public Industry tests derive observed sound from the actual operational receipt and exclude private research/payroll/account fields. |

The review found and corrected concrete failures. Active research now pauses immediately when its Scientist is released. Paused/cancelled research retains its Laboratory identity without consuming active capacity, preventing demolition from orphaning history. Validation refuses impossible one-week inventions, duplicated researcher-week charges, unpaid acceleration, missing historical Scientist payroll, a purchaser claiming an inventor discount, adoption predating access, mismatched physical jobs, absent Post commitment and missing or false completion receipts. The nine adversarial tests in `tests/p13a-technology-validation.test.ts` exercise these refusals against otherwise lawfully generated campaigns; cash is adjusted in the financial forgeries so a mere balance mismatch cannot make the checks pass vacuously.

A final valid-input check found that $1 and $3 weekly budgets could accumulate floating residue and fail the paid-work save bound after 20 weeks. Work now accumulates integer units of 1/20,000; its persisted value must represent those units, and elapsed/paid bounds compare integer units. Regression cases cover $1, $3, $2,499, $5,600 and $9,999 budgets and continuous versus reloaded continuation. The native $0/$2,500/$10,000/$40,000 presets retain their existing results. This correction changes no cash debit, creative RNG draw, personnel count or production formula.

Completed verification before the final full run:

| Command/evidence group | Observed result |
| --- | --- |
| Current-version, historical boundary, roster continuation and attestation correction group (`/tmp/p13a-migration-current-group.txt`) | 28 files, 546 tests passed. |
| Migration, adversarial, property, Annex, V13 matrix and roster root correction group (`/tmp/p13a-correction-roots.txt`) | 8 files, 136 tests passed. |
| Legacy ground/body placement (`/tmp/p13a-legacy-ground-final.txt`) | 14 tests passed. Target installations are tested by their own P09 API rather than incorrectly treated as new building footprints. |
| Catalogue, causal route, exact lot projection and campaign isolation (`/tmp/p13a-integrated-focused.log`) | 4 files, 8 tests passed before the five additional budget precision cases. |
| Budget precision and adversarial finance (`/tmp/p13a-precision-closure.txt`) | 2 files, 17 tests passed. |
| Core, UI and bridge TypeScript (`/tmp/p13a-types.log`, subsequent lane checks) | All passed before the final full regression. |
| P09 derived demolition-history correction | Physical lane reported 93/93 tests passed, including lifecycle, exact-charge forgery and unchanged operating-cost results. Full core rerun includes that correction. |

Earlier full-core regression: `npm run test:core` passed **209 files / 2,565 tests**, exit 0, in 137.66 seconds. It began at 19:04:08 in the runner's clock; log `/tmp/p13a-final-full-core.log`. This run includes the final integer-work validation, catalogue, P09 derived demolition-history correction, and all current/frozen migration expectation corrections. The earlier full run had 101 failures across 46 files; those failures were resolved and rerun, not waived. Test counts above overlap and are not added together. Core, UI and bridge TypeScript also passed after the precision correction (`/tmp/p13a-postprecision-types.log`, `/tmp/p13a-postprecision-bridge-types.log`). `git diff --check` passed.

The subsequent 6,240-week complete-Save setup exposed an additional valid-input defect before any timed sample: the strict shared Gate Hiring adapter omitted the Scientist role. An expired Scientist returned to the hiring market and caused the entire lot snapshot to refuse. The accepted role set now includes Scientist without weakening identity, duplicate, offer or closed-shape checks. The Laboratory also offers the existing rehire action when a retained project's Scientist contract has expired, quoting the same person; no replacement identity, assignment or progress is invented. Its seat count now describes retained assignments, and completed research names the exact operational stage/Post chain or the actual remaining installation gate. `tests/bridge-p13-scientist-expiry.test.ts`, existing Laboratory bridge tests and strict Gate selector tests passed **3 files / 21 tests** (`/tmp/p13a-expired-scientist-gate-final.txt`). Core/UI and bridge TypeScript passed after those fixes (`/tmp/p13a-expiry-types.log`, `/tmp/p13a-expiry-bridge-types.log`). These are subsequent focused checks, not part of the earlier 2,565-test source freeze.

Actual native input then exposed a profile-label inconsistency after hire, assignment, funding and an advance to 261: the current research task was correct, but profile employment said Available. The projection now computes the shared core busy set once and uses its availability result for both profile and roster, preserving Unknown for ambiguous work. A historical writer credit alone stays Available; active directors, cast and craft company remain Working. The existing presence allowlist also now retains the already-authorized research engagement instead of converting it to null. `tests/bridge-p13-profile-availability.test.ts` verifies the real funded-tick/pause/resume sequence, unchanged employment terms, exact research presence and closed DTO fields, plus the writer-credit negative control. Together with existing P10 profile/contract and Scientist-expiry tests, **4 files / 22 tests** passed (`/tmp/p13a-profile-availability-closure.txt`); core/UI and bridge TypeScript passed. These projection changes followed the confirmed clean native Quit and do not alter campaign state. Genuine native person-body placement and Locate remain the native lane's responsibility.

The UI lane separately reported seven assigned files / 72 tests passed after accepting only the exact signed-zero equality lost during lawful JSON detachment; NaN and decorated input refusals remain. The parent then reported the full UI suite at 2,684 passed with five prior skips. These are lane reports, not additional independent runs by this reviewer.

The failed sampler produced zero Save measurements; its generated campaign and failure report remain evidence. The repaired one-shot 6,240-week library load/snapshot/durable Save preflight subsequently passed with the same digest and a clean lock (`artifacts/p13a/performance/p13-gate-preflight/preflight-report.json`); this is a correctness preflight, not a timing qualification. Two additional tests in `tests/p13a-rival-adoption.test.ts` passed, making the dedicated $1,475,000 rival adoption debit, unchanged other money kinds/plant, duplicate refusal and actual tick/reload receipt chronology explicit (`/tmp/p13a-rival-adoption-acceptance.txt`). Final source freeze, separate 6,240-week performance qualification and native viewport/input evidence still require their respective reports. Performance numbers must retain their measured source identities and accepted-P12 limitations; integer work accumulation leaves the $10,000 endurance scenario unchanged but still changes the source graph. This review records technical evidence only. Product critiques and Howard's Owner playtest remain separate decisions.

The subsequent sequential quiet comparison completed with P13 Save/load p95 increases of 14.057% / 17.759% and disjoint observed ranges. [Current Ops escalation](P13A-CURRENT-OPS-ESCALATION.md) records the stop. This does not invalidate the passing bounded state-preservation checks, and it does not satisfy the unperformed 32-record/native/final-review obligations. The native follow-up at `608f719` is explicitly uncompiled and untested. No final acceptance is claimed.

## Bounded performance correction — independent correctness review

[Current Ops order 02](../campaigns/OPS-P13A-PERFORMANCE-CORRECTION-20260911-02.md) subsequently authorized diagnosis, a P13-specific correction and a fresh matched benchmark, while freezing feature scope. The preserved recovery points are TS `1bba6645265ab59395c126b77afc95401cb7a29e` and Unity `608f719381938cc60126d31c7fc172c2b4d1dfb3`. The correction may use at most ten reserve hours and must leave at least eighteen protected hours. Both matched complete-Save and load-ready median and p95 regressions must be at most 5%; fresh matched ratios govern. This review does not grant a performance pass or resume capability work.

Profiling confirmed **6,435,042 historical-film predicate visits per `validateTechnology` call** on the generated 6,240-week state. The new P13 loadout loop searched the complete Hollywood film array for each historical rival production. Accepted P12 import/export, checkpoint and campaign reconstruction boundaries repeated that added work. Those existing strict validation boundaries were retained.

Commit `50460f3aa14f77cfe38be15ed9d991cf983cfa92` changes only the historical-membership mechanism in `src/core/technology.ts`, with the focused regression file. Each validation call builds a local `Map<studioId, Set<filmId>>` from the original Hollywood films. The rival historical fallback performs an exact pair lookup. No index survives the call, no entity-only or concatenated key is used, and no persisted state, history, receipt, array, gameplay calculation or schema changes.

Independent review confirmed that the active rival-production branch and player active/released/locked-ledger fallback are unchanged. Every original loadout still receives its exact-key, bounded-identity, ownership, duplicate, method, date and operational-adoption checks. First-filming and exact stage/Post binding assertions remain. All research, payroll, cash, physical-job and public-receipt reconciliation remains. The original Hollywood array still reaches its full validator, which rejects duplicates and malformed or unknown authority; the membership index does not sanitize it. A malformed row may refuse earlier while the index is built, but it cannot become accepted. For valid string identities, pair membership has the same boolean meaning as the former first-match search.

The nine tests in `tests/p13a-technology-membership.test.ts` use a lawfully generated week428 campaign. They reject absent films and films claimed by another entered studio with the existing unknown-loadout message; preserve blank-identity refusal; reject original duplicate, null and extra-field film rows; isolate independent round-tripped copies with identical world/entity IDs; and revalidate the same mutable object after removal and exact restoration of a referenced film. They also check exact canonical bytes after restoration and that the unaffected source/copy stays unchanged.

| Verification after the correction | Result |
| --- | --- |
| Membership, adversarial technology finance, exact production behavior, actual Save As/inactive/Rename/restart isolation and V19→V20 persistence (`/tmp/p13a-membership-correction-tests.txt`) | **5 files / 35 tests passed**, 19.19 seconds. |
| Core/UI and bridge TypeScript (`/tmp/p13a-correction-02-typecheck.log`, `/tmp/p13a-correction-02-bridge-typecheck.log`) | All passed. |
| Isolated full core (`/tmp/p13a-correction-02-full-core-isolated.log`) | **214 files / 2,582 tests passed**, 139.39 seconds. |
| Isolated full UI (`/tmp/p13a-correction-02-full-ui-isolated.log`) | **201 files / 2,684 tests passed, five existing skips**, 40.39 seconds. |

The initial concurrent full-suite attempts remain recorded in `/tmp/p13a-correction-02-full-core.log` and `/tmp/p13a-correction-02-full-ui.log`: five core timeouts, one UI timeout and seven subsequent DOM failures. The same tests passed when the full suites were run sequentially in isolation, without changing their assertions. The failed logs were retained, not replaced, and the passing results above are the isolated reruns. Focused and full-suite counts overlap.

Correctness review of this local-index correction is complete. Instrumented before/after component measurements and the fresh matched populations are still in progress. No performance-gate pass is claimed; the unfinished native compilation/input, 32-record recovery, critiques and delivery obligations remain separate and pending until the governing resume condition is met.
