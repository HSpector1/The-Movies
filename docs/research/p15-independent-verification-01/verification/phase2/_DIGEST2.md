
## verify:net-worth:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: Fabricated/incorrect citation: analysis attributes era.costScale's default-1.0 existence to 'calendar.ts:17 per finance-logic.md §5.2', but calendar.ts:17 is an unrelated line (year computation in campaignDate()) with no costScale reference anywhere in calendar.ts, and finance-logic.md's own citation set for this fact is types.ts:284-289 / hollywoodPolicy.ts:45 / reception.ts:264 / worldgen.ts:657-663 — never calendar.ts. Fact is true; citation is invented. Corrected: cite types.ts:284-289 (declaration) and worldgen.ts:657-663 (default), matching finance-logic.md's own sourcing.
- PROBLEM: Repeats a comparator claim the binding phase1-verify/_DIGEST.md explicitly REFUTED: net-worth.md §1 cites comp-ranking.md:144 verbatim ('a discounted price, reflecting its distressed financial condition', OFFICIAL/HIGH) for Capitalism Lab bankrupt-company acquisition, but _DIGEST.md's verify:comp-ranking:source-fidelity pass found that exact phrase absent from the cited page and the behavior gated behind an opt-in 'Acquire Companies Facing Bankruptcy' Banking & Finance DLC setting (tier: OFFICIAL-but-DLC-optional, MEDIUM, not OFFICIAL/HIGH). The analyst PREAMBLE requires respecting digest corrections; this one was not applied even though the analysis correctly applied a different digest correction (GearCity 'Networth' report) two sentences later. Corrected: describe it as a DLC-optional new-game setting at reduced (MEDIUM) confidence, not base-game OFFICIAL/HIGH behavior.
- PROBLEM: Loose citation: 'Power Ranking excludes cash; P15-PACKAGE.md:803' — line 803 says only 'avoids Standing/valuation duplication', not 'excludes cash' verbatim; the literal 'never reads Standing, cash/valuation...' text is at P15-PACKAGE.md §12.2 and P13-P15-LONG-RANGE-ROADMAP.md:682. Substance holds, citation pointer is imprecise. Corrected: cite §12.2 and/or ROADMAP:682 instead of line 803 for this specific claim.
- MISSING: Owner-facing flag (not a hard violation, since the analysis already hedges it): §4.3's proposal to let Standing feed a brand-adjustment band on Estimated Studio Value sits close to the same 'Standing is not rank' law (P15-PACKAGE.md §11 law 3; P15A.2's 'never reads Standing' clause) that prior-claims.md item B2 already flagged as a collision risk for Direction B's prestige lane. The analysis correctly labels this illustrative/unresolved (§4.3, §11 uncertainty 2), but the Owner should be told explicitly that a future Standing-into-valuation feed would need the same scrutiny as the Power Ranking prestige-lane question, since it reads the same persistent-reputation channel.

## verify:ranking-vs-value:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: Model B narrative arithmetic error (§1.3): the memo's own central worked example claims the Power-Ranking gap between Rose Lantern and Silver Current is '5 points down from 5, i.e. unchanged' after adding a financial lane. Recomputed: gap goes from 24-19=5 to 26-25=1, an 80% narrowing, not unchanged. Fix: state the gap narrowed from 5 to 1 — this strengthens rather than weakens the case against Model B.
- PROBLEM: Binding digest-correction violation: the preamble states 'Prima weights are on pp.46/47/51' as a binding correction, but §2.2 of the memo cites 'Prima pp. 45–46, 51' (reintroducing the corrected footer-misread and omitting p.47 for Lot Prestige). Fix: cite pp.46 (Capital/Movies/Stars), 47 (Lot Prestige), 51 (Awards).
- PROBLEM: The Financial Standing band definition relies on an undefined 'strong reserve' term: in the memo's own worked table, Your Studio and Bellwether are both at 0% debt/assets (below the stated 20% Thriving threshold) yet are banded Stable and Thriving respectively, with no reserve figure or formula given anywhere to explain the split — making the memo's own example unverifiable and failing the 'can a player explain it in one sentence' legibility test. Fix: add a numeric reserve column/threshold (e.g., weeks of runway) to the band table.
- PROBLEM: Unaddressed exploit: both Power Ranking and the recommended Financial Standing band are point-in-time snapshots taken every 13 weeks with no trailing average, so a studio (especially a deterministic rival with full self-account access, per the memo's own §7.2) can pay down debt right before the snapshot and re-borrow after — quarter-end balance-sheet window dressing. Not raised anywhere in the memo. Fix: smooth the band over a trailing window, or explicitly document accepting this risk.
- PROBLEM: Minor code-citation off-by-one: the memo cites hollywoodTick.ts:306 for the chart-row filter its §8 fix targets; the actual filter line (verified against accepted-592e926) is 307 (306 is the `if(week%13===0||h.chart===null)` line).
- PROBLEM: Several task-brief edge cases are unaddressed in the memo: (a) the n=1 sole-surviving-studio cohort case (only 2-3 studios is covered); (b) which stage of direction D's multi-stage failure ladder should trigger chart exclusion — the proposed closedWeek fix only covers final closure, not earlier stages like insolvency/settlement; (c) migration default for the new debt field on pre-P15 saves; (d) post-2040 endless-sandbox continuation of Power Ranking/Financial Standing without disturbing the frozen Legacy dossier (direction K). Each needs only a sentence to close.
- PROBLEM: Authority-consistency wrinkle: INT-009 bundles ownership of the 'Power Ranking' name with a versioned formula, cohort/window/ties/reasons, and annual summaries. The memo's §9 proposes relabeling the existing unversioned, two-snapshot-only per-lane view as 'Power Ranking' while separately noting annual summaries aren't built, but never states whether the rename can ship ahead of that missing infrastructure or must wait for it.
- MISSING: Definition/formula for the 'strong reserve' component of the Financial Standing bands (referenced but never quantified)
- MISSING: Explicit statement of whether debt/net-worth fields default to 0 on migration from pre-P15 saves
- MISSING: Explicit rule for which failure-ladder stage (declared distress vs. insolvency vs. auction-complete) should exclude a studio from the live Power Ranking cohort
- MISSING: Explicit statement on Power Ranking/Financial Standing behavior in the n=1 (sole surviving studio) end state
- MISSING: Explicit statement on Power Ranking/Financial Standing continuation post-2040 in the endless sandbox relative to the frozen Legacy dossier (direction K)
- MISSING: A stated decision on whether quarter-end window-dressing of the Financial Standing band is an accepted risk or needs a trailing-average mitigation

## verify:rival-failure:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: MAJOR/CONFIRMED — §0.2 attributes the quote "NOT net worth alone unless analysis strongly supports it" to "Direction D's own text," claiming Direction D "puts the question to whichever analyst owns the trigger hierarchy." I read the complete Owner Direction A-K verbatim (_phase2-script.js lines 14-27): Direction D contains no mention of net worth and delegates no interpretive authority to any analyst. The quoted phrase is real but comes from this analyst's own TASK PROMPT (_phase2-script.js line 75, the orchestrator's research instructions), confirmed absent from the Direction block and absent everywhere else in the phase-2 corpus by grep. This is a misattribution of a non-binding task hint as settled Owner authority, used to adjudicate a live disagreement between two sibling reports (player-failure.md vs loans.md). Corrected statement: the phrase is from this analyst's own task prompt, not Owner Direction; the §0.2 net-worth-disjunct removal is this report's own analyst-level reconciliation (already, and correctly, listed as Remaining Owner Decision #1 in §9) -- only the false 'Direction D's own text' citation needs to be struck or rewritten, not the underlying (independently sound) economic reasoning in Scenarios 3-4.
- PROBLEM: MINOR/CONFIRMED — the Sources list and §2 table quote hollywoodValidation.ts:72 as "exactly 10 identities." The actual string literal at that line is 'exactly player plus nine reserved studios' (verified by direct read of accepted-592e926/src/core/hollywoodValidation.ts:72). The fact (10 total identities) is correct but the quotation marks present a paraphrase as verbatim code text.
- PROBLEM: MINOR/CONFIRMED — §3 cites bridge/industry.ts:1 for the quote "future receipts, costs and studio revenue remain private." Line 1 is only the file-header comment; the quoted sentence is verbatim but lives at bridge/industry.ts:50 (confirmed by grep and direct read). Substance stands, line citation is wrong.
- MISSING: No treatment anywhere of migration-origin rivals (origin==='migration', founding:null, no authored films, hollywoodValidation.ts:99's no-fabricated-past rule) against Binding Law 7 ('no retroactive fiction in migrated saves'). The trigger hierarchy (§2) and all eight paper scenarios (§4) implicitly assume origin:'fresh' rivals only; a migration-origin rival has no pre-originWeek financial history to compute a 26-week or 52-week trailing window against. Not a violation (nothing written contradicts Law 7) but an uncovered package-law constraint worth one added line in Uncertainties/Owner Decisions.
- MISSING: The report never cites Blockbuster Inc. (2024), which the shared PREAMBLE explicitly lists among established digest facts ('Blockbuster Inc. (2024) ships rival bankruptcy with a one-year pause then return') and which the digest separately flags as the closest shipped precedent for exactly this report's rival dormancy-with-return shape. This is a completeness gap more than an authority-consistency one, noted for the completeness-critic phase rather than pressed here.

## verify:market-window:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: Claim: '“concept genre is a weighted random draw” and “per-genre weight = 5 if anchor else 1” is at src/core/hollywoodTick.ts:160-161'. Why wrong: at accepted 592e926, hollywoodTick.ts:160-161 is inside computeForecast/forecastHistoryForOwner, unrelated to genre weighting; the weight assignment is actually at src/core/hollywood.ts:156 (policy:{affinities:Object.fromEntries(GENRE_ORDER.map(g=>[g,template.anchors.includes(g)?5:1]))}) and the weighted draw is at src/core/hollywoodTick.ts:181-182 (roll=chooser.next()*sum(affinities); genre=find where roll crosses 0). Substance and the density arithmetic that depends on it are correct; only the citation is wrong. Corrected statement: cite hollywood.ts:156 for the weight assignment and hollywoodTick.ts:181-182 for the weighted genre draw.
- PROBLEM: Claim (§12 disposition table): row grouping 'Prior-claims register A7/A9/A13' gives Prior label 'CONFIRMED' and disposition 'CONFIRMED, unchanged by this section' for all three. Why wrong: per out/phase1/prior-claims.md:72, A9 (PKG §24 OQ1) is recorded with disposition SUPERSEDED BY OWNER DIRECTION, not CONFIRMED — and this is the identical citation (§24 OQ1) that market-window.md's own §1 and the first row of its own §12 table correctly label SUPERSEDED BY OWNER DIRECTION. The document thus gives two contradictory dispositions to the same source claim within one memo. Corrected statement: split the row — keep A7/A13 as CONFIRMED→CONFIRMED, and list A9 separately as OPEN QUESTION→SUPERSEDED BY OWNER DIRECTION, matching both the register and the document's own row 1.
- MISSING: A citation to Builder Annex D.2's `sourceReachBandOrValue: public authoritative input only` field, which would directly corroborate the §6/§7.1 recommendation to resolve Open Question 2 as reach-scaled contribution rather than flat-per-film (not an error, just an available supporting citation left uncited).

## verify:net-worth:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: MAJOR/exploit: Estimated Studio Value has no floor at Book Net Worth. The analysis's own Worked Example (iii) computes Book Net Worth=$42,000,000 but Estimated Studio Value=$36,400,000-$37,800,000 (recomputed and confirmed) - a 'worth more dead than alive' arbitrage signature. Once Direction I's eventual acquisition system consumes this number, a player/rival could systematically buy any rich-but-declining studio below its liquidation value and immediately strip its facilities/sets for a guaranteed profit, a repeatable snowball. Fix: clamp the low end of the Estimated (Equity) Value Range at Book Net Worth; if the computed low end is lower, replace it and label the range 'liquidation-bound.'
- PROBLEM: MAJOR/structural gap: The proposed Book Net Worth formula (Sigma facility ledger capex x 0.5 + Sigma set ledger capex x 0.35) is defined entirely over the player's PlacedFacility[]/StudioSet[] records. Verified against hollywoodTypes.ts:47-90 and hollywood.ts:140-165 that RivalBusiness has no such itemized ledger - rivals carry one lump 'capacity' RivalMoneyKind movement charged once at entry, never broken out by blueprint or given an individual refund fraction. Section 7/8's claim that the rival-side computation is a mechanical parallel glosses over this; a genuinely different, coarser proxy formula is needed and the document doesn't say so. Fix: one sentence naming the RivalAccount lump-movement basis as the rival-side proxy source instead of implying formula reuse.
- PROBLEM: MODERATE/legibility+disclosure leak: Section 6.2's cross-era ratio rule ('state the figure as a ratio of the era's own trailing baseline, e.g. 6x the era's median studio surplus') breaks down at the 2-3 studio consolidation end-state that Direction G/H explicitly treat as a normal, expected outcome. With exactly 2 studios, a 'median' is the mean of 2, so the ratio mathematically discloses the sign/magnitude of the gap to the one remaining rival - quietly defeating Section 7's own 'rivals' dollar figures stay hidden' rule stated two sections earlier. Sibling document ranking-vs-value.md already names this identical fragility for its own leaderboard models but net-worth.md's ratio mechanic has the same defect uncross-referenced. Fix: gate cross-era/cohort ratio display behind a minimum comparable-cohort size (e.g. >=4 non-player studios), below which show 'insufficient comparable population' instead of a ratio.
- PROBLEM: MODERATE/legibility: Because Book Net Worth reads ledgerCapex x refundFraction, every dollar of capex spending converts to only 50 cents (facility) or 35 cents (set) of book value the instant it is spent, so a player's headline 'net worth' visibly drops the moment they build anything - a deliberate but unexplained-at-the-moment-it-happens design choice that risks reading as a bug given Direction C's 'player must understand the number' requirement. Fix: add a purchase-time inline note explaining the book-value markdown, alongside the existing static tooltip.
- PROBLEM: MINOR/citation error (inherited from finance-logic.md, not corrected by _DIGEST.md): Section 6 cites era.costScale's default as 'calendar.ts:17'. Verified against accepted-592e926: costScale is declared at types.ts:288 and defaulted to 1.0 at worldgen.ts:662; calendar.ts:17 is inside the CampaignDate type block and contains no costScale reference. Correct citation: types.ts:288 (declaration), worldgen.ts:662 (default).
- PROBLEM: MINOR/UI completeness: Section 3.5 introduces Wind-Down Position specifically to prevent a player from reading a healthy Book Net Worth while ignoring large Guaranteed Obligations, but Section 8's 'where it lives' recommendation never states the two figures must render adjacently by default; a UI burying Wind-Down Position behind a tab would silently reopen the exact misreading Section 3.5 exists to prevent. Fix: one sentence requiring the two figures to render in the same panel by default.
- MISSING: A rival-side Book Net Worth computation method grounded in RivalAccount's actual data shape (see Finding 2) - currently assumed rather than specified.
- MISSING: A floor/guardrail preventing Estimated Studio Value from displaying below Book Net Worth (see Finding 1).
- MISSING: A minimum-cohort-size gate on the cross-era/cross-population ratio framing in Section 6.2 (see Finding 3), mirroring the degrade-gracefully pattern ranking-vs-value.md already uses for the same underlying problem.
- MISSING: An explicit note that under-construction placements are included in the Book Net Worth sum on the same legal basis as operational ones (currently true by implication from existing demolition-refund law, but not stated).

## verify:loans:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: HIGH: loans.md §3.3 and §7 both quote P15-PACKAGE.md §16 ('Distress must have at least two legitimate recovery routes... typed remedy capability family...for player and rival studios') as approving authority for the Rescue loan, and §7 states the design 'reus[es] package boundaries the Owner has already approved (P15-PACKAGE.md §3.2, §16).' Both quotations stop immediately before the same sentence's final clause: 'Loans, bailouts, investors, forced sales, or acquisition are not implied.' loans.md's own primary source, code-finance.md §7, already logged this exact sentence as CONFIRMED still-live prior-P15 law as of accepted 592e926 ('CONFIRMED — "Loans, bailouts, investors, forced sales, or acquisition are not implied" (P15-PACKAGE.md:619); none exists and all are Owner-blocked'). Direction F supersedes the 'loans' clause of that sentence specifically, but loans.md never applies the required CONFIRMED/QUALIFIED/CORRECTED/SUPERSEDED-BY-OWNER-DIRECTION labeling to it anywhere in the document, despite citing the surrounding text twice as approving authority. Corrected statement: label the sentence's 'loans' clause SUPERSEDED BY OWNER DIRECTION F, while noting its other four items (bailouts, investors, forced sales, acquisition) remain CONFIRMED excluded from P15 exactly as written, and explicitly note why the 'Rescue' loan (priced, interest-bearing, repayable) does not fall under the surviving 'bailouts' exclusion.
- PROBLEM: MODERATE: loans.md §3.2 states the 8-week sustained-negative-cash distress-interest surcharge as a flat 'Rule:' with no hedge, then only in §8 item 4 (eight sections later) admits it 'needs its own explicit Owner sign-off as a genuinely new mechanic.' This is technically disclosed (not a silent conversion of an open choice into law, per P13-P15-OWNER-RULINGS §6), but the placement invites a builder who reads only the actionable design sections (§3.2-§3.7) to treat a not-yet-approved, behavior-changing rule (it charges cost on an existing free/unlimited overdraft, touching Law 5's 'Negative cash is not bankruptcy... only through approved staged law') as settled. Corrected statement: state §3.2 as 'Recommended rule, not yet Owner-approved' at first mention rather than as a flat 'Rule:'.
- MISSING: No explicit line-item correction anywhere in loans.md naming P15-PACKAGE.md §16's 'not implied' clause and applying SUPERSEDED/CONFIRMED labeling to its five bundled items individually (loans vs. bailouts/investors/forced-sales/acquisition).
- MISSING: No explicit argument for why the 'Rescue' loan (Tier D) is a loan and not a 'bailout' under the four items of §16 that Direction F does NOT supersede.

## verify:market-window:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: 6-week run-overlap Poisson row has a real arithmetic error: table states busiest=37.4%/quietest=18.2%, recompute of P(≥1)=1−e^(−rate×6) from the section's own stated rates gives busiest≈37.6% and quietest≈17.7% (0.5pp off, the largest deviation). Smallest fix: correct that column; qualitative conclusion (4 weeks is the sensible middle) is unaffected since the error is in a rejected comparator column.
- PROBLEM: Minor rounding drift found on recompute: 2-week quietest (stated 6.4%, recomputes 6.3%) and 8-week quietest (stated 22.9%, recomputes 22.8%); release-density table truncates romance 4.0857→4.08 and drama/crime 1.6857→1.68 where standard rounding gives 4.09/1.69. Immaterial to conclusions but logged per the explicit recompute-and-report-any-error instruction.
- PROBLEM: §2's rival 'structural release ceiling' of ~11–14 weeks/film (3.7–4.7/yr) understates true throughput: code shows the ≤2-active-screenplay cap counts an 'inProduction' script (scriptDevelopment.ts status machine drafting→review→ready→inProduction→produced; hollywoodTick.ts filters only 'produced'), which is precisely why the cap is 2 not 1 — it lets screenplay #2 draft during production #1's 8-week shoot, so a ready script is already waiting when each production completes. True steady-state cadence is closer to PRODUCTION_TICKS+1≈9 weeks/film (~5.8/yr), not 11–14 weeks. Smallest fix: correct §2's estimate and strengthen (not weaken) the already-flagged upside-risk-vs-Prima's-2/yr note accordingly — does not affect §3's main arithmetic, which correctly uses Prima's rate as its stated assumption rather than this ceiling.
- PROBLEM: §6/§7 overclaim that the 3-stage taper (1.00/0.55/0.20) 'removes the discontinuity' of the boundary-dodge exploit: exposure is archived at exactly R+4 (weight 0.20→0), so a smaller but still-exact cliff remains — R+4 is itself a 'magic day', just a cheaper one than under the original hard-cliff design. Smallest fix: soften the claim to 'reduces the cliff from 1.00 to 0.20 rather than eliminating it', and optionally add a continuously-decaying tail past R+4 if fully cliff-free behavior is wanted (not required to ship).
- PROBLEM: The 'spamming cheap films' mitigation (§6) leans on the rival-only 1-production/≤2-screenplay cap plus reach-scaled contributionBounds, but nothing caps how many productions the PLAYER runs concurrently (bounded only by the player's own facility count/capital) — a well-capitalized player could release several well-marketed (so not stopped by reach-scaling) same-genre films in one window purely to spike every rival's saturation reading at once, an asymmetric degenerate strategy the BREAK-IT brief explicitly asks about ('player AND deterministic rivals') that the analysis doesn't name. Smallest fix: clamp contribution per (studioId, genre, window) before it enters the shared aggregate — a per-studio ceiling, law-8/9-compliant, no new mechanism.
- MISSING: Pre-P15A save migration: no statement of how known-preview/exposure state and the new saturation-stock accumulator are seeded/backfilled for a save with existing release history when P15A ships (binding law 7 'no retroactive fiction' implies cold-start-at-zero-with-a-recordedFromWeek-flag, but the section never says so).
- MISSING: Low-rival-count / consolidation-endgame worked example: §3's density arithmetic is computed only for the current 9-rival roster and handed to consolidation.md 'if it exists' for reuse, but no worked low-N (e.g., 2 rivals) check is shown to confirm the formula degrades sensibly.
- MISSING: Player/rival ID-swap symmetry, save/load mid-batch atomicity, and ID reuse are correctly left untouched (inherited from the frozen §12.1 law the task said not to redesign) — not a gap, noted for completeness of the edge-case sweep.

## verify:player-failure:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: Net-worth trigger contradiction: player-failure.md §2's Severe Distress row keeps 'OR book net worth negative for 26 consecutive weeks' as a trigger, but loans.md §3.7 (sibling report, same batch) explicitly states an uncured Event of Default is 'the only event that may hand the P15B condition machine a Warning trigger sourced from finance... Nothing else in finance may originate a Warning' — confirmed verbatim at loans.md:114. rival-failure.md §0.2 already adjudicated this exact collision against player-failure.md, with a concrete counter-scenario (a leveraged-but-current, profitable studio wrongly escalated by a conservative book-value haircut). Fix: delete the net-worth disjunct from §2 and §9; cite rival-failure.md §0.2.
- PROBLEM: Self-contradicting exploit: §2 explicitly rejects MGT2's 'resets on one good week' counter for the missed-instalment/EOD rule and hardens it into a rolling 13-week window, but the two stages that feed into it (Warning: RecoveryPosition=constrained for >=4 CONSECUTIVE weeks; Distress: severe sustained >=13 CONSECUTIVE weeks) use exactly the rejected shape — a single well-timed cash bump (opening-week revenue spike, facility-refund timing) resets classifyRecovery's standardOk boolean and the counter to zero, letting a persistently unhealthy studio that never strings together enough consecutive bad weeks avoid escalation indefinitely. Fix: change both to a rolling-window majority test (e.g. 'constrained in >=4 of the last 6 weeks'), matching the shape already used correctly for missed instalments.
- PROBLEM: Arithmetic/definition mismatch in the §5 worked example: the timeline labels '$75,000' as 'payroll/overhead-band' but by the report's own math (OVERHEAD_BASE 15,000 + 1,500x40) this is the overhead formula only (tick.ts:927-928) and contains no payroll term at all; facility Opex is also omitted despite the studio servicing a facility mortgage. This directly contradicts §2's own trigger definition two sections earlier, which correctly lists 'cash < 8 weeks of fixed costs (payroll+overhead+facility Opex)' — all three terms. For a 40-employee studio, payroll is likely the dominant cost, so the worked '$712,000' 8-week liquidity threshold and the whole cash-path timeline are understated/inconsistent with the rule the file says it is demonstrating, despite the closing note claiming 'arithmetic shown.' Recomputing the cash path independently (650,000 at week~230 to -180,000 at week~243, 13 weeks apart) also does not reconcile against the stated $88,976/week burn (implies -$506,688, a $326,688 gap) without an unstated revenue assumption. Fix: recompute with full payroll+overhead+opex+debt per §2's own formula, or explicitly relabel the figure as overhead-only and disclose the real threshold is higher.
- PROBLEM: Missing a comparator the task prompt explicitly required: 'Two Point Hospital -150k warn/-300k fail' is named in the assigned task text and present in the phase-1 evidence (comp-bankruptcy-loans.md:134-136,165,214) but never mentioned anywhere in player-failure.md, despite being directly on-topic for §3 (how much warning) as the simplest fixed-threshold alternative in the whole comparator set. Fix: one sentence noting it was considered and rejected because a bare dollar constant cannot survive era.costScale drift across 1920-2040.
- PROBLEM: No player-initiated ('voluntary declare') bankruptcy option is considered anywhere, despite the task's own comparator list naming 'Capitalism Lab Declare Bankruptcy choice' (confirmed in comp-bankruptcy-loans.md:96,208 as 'an explicit declare choice at the terminal edge (player agency)') and despite the file repeatedly invoking player agency as its central value when rejecting forced automatic rescue. Fix: add a voluntary-declaration option to the §4 remedy table and evaluate it.
- PROBLEM: The one-time, non-repeatable 13-week rescue window's scope across a potential 1920-2040+ campaign is never clarified (once per save forever, vs. once per default episode with a reset after sustained Healthy recovery), and this genuine open question is not listed among the file's own 8 remaining Owner decisions in §8 despite clearly qualifying. Fix: add it as decision #9 and state the Cities:Skylines precedent implies 'forever,' which needs explicit, highly visible player warning if adopted.
- PROBLEM: No consideration of save-scumming around the bounded rescue window: a manual save/load in a single-player sim lets a player reload before an unlucky release and retry the 13-week window repeatedly, undermining the 'meaningful, felt, bounded' framing the design depends on, and the file is otherwise attentive to exactly this class of agency/fairness problem (it rejects GDT's forced bailout on similar grounds). Fix: one sentence either accepting this as an ordinary out-of-scope single-player-sim caveat or proposing that rescue-window outcomes commit/seed at window-open.
- PROBLEM: No migration/grandfather rule for pre-P15 saves: a save already cash-negative or 'severe'-classified for years under the old no-bankruptcy law (the file's own §1 cites the D-16 lab's 99.69% weekly self-transition finding) could, on first load under the new law, instantly satisfy multiple stage thresholds simultaneously and skip the 'meaningful warning first' Direction E requires. Fix: one additive rule seeding all missed-obligation/consecutive-week counters at zero from the upgrade week forward, never evaluating pre-law history retroactively.
- PROBLEM: Remaining decision #7 ('what does continue mean after player bankruptcy... a fresh Founding Flip in the same world') is flagged as open but not connected to hollywoodValidation.ts:72's hard-coded exact-10-identity invariant (confirmed by direct inspection: 'exactly player plus nine reserved studios'), which any 'continue in the same world' answer would violate (needing an 11th identity) or force into tension with the file's own cited 'no identity death' law (reusing the closed StudioId). Fix: add this code constraint as a clause to decision #7 so it is understood as a feasibility constraint, not just a design preference.
- MISSING: Comparator: Two Point Hospital -150k/-300k fixed-tier warning (explicitly required by the task prompt)
- MISSING: Player-initiated/voluntary bankruptcy declaration as a remedy option (Capitalism Lab 'Declare Bankruptcy' comparator, also explicitly required)
- MISSING: Save/load mid-rescue-window exploit analysis (explicitly named in the verification brief)
- MISSING: Migration from pre-P15 saves / cold-start grandfather rule (explicitly named in the verification brief)
- MISSING: Explicit reconciliation with hollywoodValidation.ts's exact-10-identity invariant for the 'what does continue mean' open decision (ID reuse edge case named in the verification brief)
- MISSING: One-line cross-reference to whether the terminal ladder remains live post-2040 in Endless Sandbox (owned by finale-endless.md, but costless to flag here)

## verify:player-failure:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: Section 2's trigger table lets Severe Distress fire on 'book net worth negative for 26 consecutive weeks' alone. This violates the task's own imported rule ('NOT net worth alone unless analysis strongly supports it') — no support is given — and is now explicitly refuted by the coordinating sibling document phase2/rival-failure.md §0.2, which rules against it in favor of loans.md §3.7's stricter two-trigger law (missed instalment or covenant breach only). Corrected statement: drop the net-worth disjunct; Book Net Worth stays a disclosed figure, not a trigger input.
- PROBLEM: Section 2 frames the six-stage ladder (healthy/warning/distress/severe distress/insolvency/bankruptcy) as 'the smallest correction' derived from P15-PACKAGE §12.3's older five-state diagram plus comparator reasoning, captioned 'keeps the four inherited names, adds two.' In fact only warning/distress are literally kept (active->healthy is a rename, dormant is dropped, recovery is demoted to an arc, three not two new names are added), and more importantly the whole sequence is Owner Direction D's own verbatim wording ('healthy -> warning -> distress -> severe distress -> insolvency/bankruptcy -> settlement/auction/closure'), which the sibling rival-failure.md correctly attributes to Direction D but this report does not. Consequence: Remaining Owner Decision #2 asks the Owner to re-approve stage names the Owner already dictated, reopening a settled choice. Corrected statement: state plainly that the stage names are Direction D/E law, not an analyst recommendation; only the numeric thresholds and the insolvency/bankruptcy two-stage-vs-one-stage interpretation are genuinely open.
- PROBLEM: Section 9's corrections table labels finance-logic.md's caution about Book Net Worth needing its own P11 authorization as 'CORRECTED,' justified by circular reasoning ('this section's use of Book Net Worth as a trigger input... constitute[s] the authorization'). Direction C authorizes displaying net worth, not this specific mechanical use, and per the first finding that specific trigger use has since been rejected by the coordinating document, so the justification is now doubly unsupported. Corrected statement: relabel QUALIFIED, not CORRECTED — Direction C authorizes the read-model/display; the mechanical use remains for net-worth.md/Owner synthesis.
- PROBLEM: The report recommends new settlement receipts for player bankruptcy (contracts/talent to free agency, assets to settlement) without ever mentioning that IndustryReceipt is a closed five-kind union (hollywoodTypes.ts:96) requiring an additive extension, or naming the exact-key StudioIdentity constraint it must not violate -- both of which the sibling rival-failure.md handles explicitly for the symmetric rival case (new receipt kinds studioClosed/projectCancelled, a new additive studioOperatingState root, not a widened StudioIdentity). The report should state the equivalent mechanism for the player case rather than leaving it silent.
- PROBLEM: The report never mentions the already-shipped 'cashNegative' UI stop reason (ui/src/engine/adapter.ts, fires exactly when cash crosses from >=0 to <0), which the phase1-verify digest calls 'the natural P15B pre-terminal-warning seam.' Given the report's own preamble lists this as an established code fact, the Warning/Distress ladder design in Section 2-3 should state whether this existing governed stop is superseded, kept alongside the new ladder, or repurposed as a Warning-stage notice -- it currently leaves this open question unaddressed.
- MISSING: No discussion of whether the existing cashNegative UI stop-reason behavior is superseded, retained, or repurposed by the new Warning/Distress ladder
- MISSING: No naming of the specific additive IndustryReceipt kinds or an equivalent-to-studioOperatingState mechanism needed for the player's own bankruptcy settlement, despite proposing settlement receipts
- MISSING: No acknowledgment that the six-stage ladder's names are drawn verbatim from Owner Direction D rather than derived from P15-PACKAGE reasoning

## verify:talent-settlement-events:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: [MAJOR] Studio-auction row (§3.1: "an actual auction/buy-flow is P16+ scope by binding law 14"), the Package-ownership summary (§4: "P16+ — owns any live acquisition/auction mechanic"), and open-decision #4 (§6) all flatly reassert the pre-direction "acquisitions → P16+ only" ruling (P15-PACKAGE binding law 14/§23/§25; P13-P15-OWNER-RULINGS.md §4.2, an actual dated Owner ruling) without the required SUPERSEDED-BY-OWNER-DIRECTION label. Owner direction I explicitly states it "supersedes prior 'acquisitions unwanted/P16+ only' reading" and makes the exact owning package a genuinely open question delegated to the sibling ma-auction.md task ("P15B continuation / new P15D / P16 / other"). The document's own "Acquisition" row in the same table correctly hedges this as open ("package boundary is an open research question — nothing below is a scope authorization"), so the document is internally inconsistent. Corrected: label binding law 14/§23/§25 and P13-P15-OWNER-RULINGS §4.2 SUPERSEDED BY OWNER DIRECTION I regarding whether acquisition mechanics can ever be P15-owned; state that the owning package for the mechanic is undecided and reserved for ma-auction.md, not settled as P16+.
- PROBLEM: [MAJOR] §2.1 quotes GearCity's bankrupt-AI outcomes as "liquidated, refinanced under new ownership, or sold," reproducing a phrase phase1-verify/_DIGEST.md explicitly flags as a MISQUOTE/REFUTED: the source (Eric.B, ventdev pid 7643) actually says the refinanced path leaves "the company still ow[n]s the company," and the digest states "'under new ownership' does not appear and contradicts the source's own gloss." The task preamble makes digest corrections binding, and this analysis's own header claims to apply the digest's corrections (naming four others) yet missed this one, carrying the uncorrected phrase forward from phase1/comp-bankruptcy-loans.md §1.5. Corrected: GearCity's AI bankruptcy paths are liquidation, refinanced survival (a bank/investment firm finances it, but the company still owns itself — not new ownership), or sale.
- PROBLEM: [MODERATE] §2.7 attributes the 'genuinely fixed-size result' fixed-receipt allowance to "P15-BUILDER-ANNEX §12.3", but that exact clause is verbatim text from P15-PACKAGE.md §12.3 (verified by direct read); the Builder Annex has no numbered §12.3 at all (its sections are lettered A-S). This misattributes a Package-tier binding rule to the Annex. Corrected: cite P15-PACKAGE.md §12.3.
- PROBLEM: [MODERATE] §4's package-ownership summary names P12, P15B, P11, and P14 as the packages touched by the closure design but never mentions P10, even though P15-PACKAGE §12.3 (quoted earlier in the same document) explicitly states that for dormancy/closure "P10 settles contracts/person references" alongside P11 and P12, and binding law 16 requires P13/P10/P11/P12/P14 to each supply an idempotent settlement receipt in one atomic manifest before any closure can take effect. Since this task's whole Part 1 design concerns Contract/Talent-facing records, omitting P10 from the ownership map could lead a builder to skip its required settlement receipt. Corrected: add P10 as a required settlement co-signer per §12.3/binding law 16.
- MISSING: No note that P13-P15-OWNER-RULINGS.md §4.2 is the specific dated Owner ruling direction I's supersession clause refers to — naming it would have made the required SUPERSEDED-BY-OWNER-DIRECTION labeling unambiguous.
- MISSING: §12.3's atomic-manifest requirement also names P13 as a required closure-time signer once P13 exists; reasonable to omit since P13 is unimplemented, but the document flags this future-prerequisite caveat for P14 (§2.9/§4) and not for P13.
- MISSING: No cross-check against docs/OWNER-RULINGS-HOLLYWOOD-HORIZON.md §3 (a player no-bankruptcy/no-loans ruling other phase-1/phase-2 reports found) for the Part 2 "Studio bankruptcy (player)" row, which cites only P15-PACKAGE §23's asymmetry ruling.

## verify:rival-failure:break-it — REFUTED_IN_PART
- PROBLEM: Scenario 1 (§4.1) claims cash never crosses the Warning threshold, but by the report's own §2 trigger row ('cash < 8 weeks of fixed costs') and its own §4.8 practice (which fires Warning on this exact condition), Marigold's cash is below the $456,000 threshold continuously from week 403 through week 411 (9 consecutive weeks) — Warning should fire around week 406-407. Fix: correct the Scenario 1 table to show Warning entered ~wk406 and cured at wk412, or explicitly add a sustain-duration qualifier to the 8-week-fixed-costs clause in §2 and re-verify.
- PROBLEM: The report's header/§0.2 claims 'exactly one substantive disagreement' among the three sibling reports was found and reconciled. A second, unreconciled disagreement survives in the very line quoted: player-failure.md:106 counts 'payroll, overhead, or a loan instalment' as missable toward the 4-in-13 default rule, while loans.md §3.7 and this report's own §1/§4.5 count only loan instalments as ever 'missed.' Fix: add one line to §0.2 naming this second split and ruling it the same direction (loan-instalments-only), correcting player-failure.md:106 to drop the payroll/overhead clause.
- PROBLEM: Structural gap: every path from Distress to Severe Distress/Insolvency/Bankruptcy in §2's own trigger table requires a missed LOAN instalment (confirmed by §4.5's 'only the loan instalment is ever recorded as missed' and loans.md §3.2's 'distress-interest is charged unconditionally, never missed'). A studio that never borrows therefore has no stated mechanism to ever escalate past Distress, silently recreating the D-16-lab 'mechanically absorbing' trap Directions D/E exist to fix, and creating a discoverable player degenerate strategy (never take a loan = permanently bankruptcy-immune) and a rival dead-end (a rival that enters Distress without ever having drawn a loan can never resolve to Bankruptcy/settlement). Fix: add an explicit debt-free escalation path, e.g. 'Distress sustained for a longer window (PROVISIONAL 26-52 weeks) with no loan drawn and no improving trend escalates to Severe Distress on its own.'
- PROBLEM: Scenario 7 (§4.7) asserts credit grade degrades from Good->Watch->Impaired purely from refinance COUNT ('repeated rolls exhaust the Watch band'), but loans.md §3.5's actual grade formula is a function of missed-payment history, coverage/capacity, and collateral -- refinance count is not an input. The scenario also conflates loans.md's 'Stable' (corporate-ladder stage, gates refinancing per §3.8) with 'Good/Watch/Impaired' (credit grade, gates interest spread per §3.5) as if they were the same axis. This is an invented rule presented as already-settled reuse. Fix: either explicitly name it as a new rule distinct from §3.5's formula, or rewrite the scenario so the 3rd refusal follows from an independently eroded trailing surplus under the existing grade table.
- PROBLEM: The §4.6 bridging privilege ('at most once per rolling 13-week window per studio') is not well-defined for a rolling window -- unlike a fixed window, 'once per window' has no clear reset semantics, and a naive implementation could allow near-back-to-back reuse right after a nominal boundary, echoing the Mad-Games-Tycoon-2 reset exploit this same document elsewhere takes care to close (§3.7's 'catch up every currently-missed instalment' language). Fix: state it as a cooldown ('at least 13 weeks must elapse between uses by the same studio') instead of a per-window count.
- PROBLEM: The §6 studioClosed.triggerSummary enum ('missed-instalment' | 'covenant-breach' | 'uncured-distress') doesn't match the report's own state machine: per §4.3's Scenario 3, a covenant breach alone can never escalate past Warning (the missed-instalment counter never advances), so 'covenant-breach' is unreachable as a terminal cause; 'uncured-distress' is by definition true of every Bankruptcy (that is literally what the Bankruptcy trigger row says happens), so it is tautological and would appear on 100% of closures. The enum as proposed can't actually distinguish why a given studio failed. Fix: replace with values describing what originated the ladder entry (e.g. 'missed-loan-instalment', 'failed-rescue-film') rather than the mechanical shape of the terminal event.
- PROBLEM: The report's §5 proposes adding a new deterministic remedy-selection branch to rival policy, but never notes -- despite a direct code read confirming hollywoodValidation.ts:209 pins `b.policy.version === 1` and an exact key set -- that this is itself a versioned-schema change requiring the same discipline (bump policy.version, extend the exact-key validator, migration) the report correctly applies elsewhere to StudioIdentity. The phase1-verify digest flagged this exact gap for a sibling report; it was not carried into this one even though this report is the one proposing the change. Fix: one sentence in §5/§8 stating the policy.version bump and validator extension needed.
- MISSING: Pre-P15 save migration: no discussion of how the new rolling-13-week missed-instalment counter, the >=4/>=13-consecutive-week clocks, or the 8-week distress-interest sustain window are initialized/backfilled when an existing save with years of unrecorded prior history is loaded under the new law for the first time.
- MISSING: Post-2040 endless sandbox (Direction K): the §7 population-hazard math is explicitly scoped 'by 2040' only; the report never states whether the missed-instalment ladder continues to run on rivals in endless sandbox, nor what happens if the rival population reaches 0-1 post-2040 (a real possibility per §7's own table above ~1%/yr hazard).
- MISSING: Save/load mid-settlement atomicity: §6 states settlement 'commits...in the same week' but never explicitly rules out a multi-step, interruptible settlement process (i.e., never states the transition is computed as a single atomic tick-boundary event the way the rest of the weekly tick already is).
- MISSING: 2-studio / near-monopoly end state: §7 names population collapse as a risk to avoid via calibration but does not state whether anything about the failure ladder itself (as opposed to the shared market, out of scope) changes or needs special handling if the rival count nonetheless falls to 1-2 -- even one sentence disclaiming this as deliberately out of scope would close the gap.
- MISSING: The report's own §7 does not note that its two hazard-rate columns (120yr vs 71yr exposure) carry opposite direction biases (one pessimistic, one optimistic) -- it flags only one direction.
- MISSING: The report never states the practical bound on the §4.6 bridging lookahead (it reads as a full 13 weeks, far more generous than the 1-4 week comparators cited to justify it, though in practice bounded to <=6 weeks by the reused THEATRICAL_WEEKS:6 code fact) -- worth making explicit rather than leaving implicit.

## verify:loans:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: [MAJOR] §3.7 'missed instalment' rule never states what happens to the skipped money (capitalized onto balance? just gone?). As written, a rational studio (or symmetric P12 rival) can miss 3 of every 13 weekly instalments forever, never reaching the 4-in-13 Event-of-Default threshold, with zero recorded cost, extracting an indefinite ~23% discount on debt service. This defeats Direction F/D's premise that failure comes from sustained inability to meet real obligations. Fix: one sentence — a missed instalment's principal+interest capitalizes onto the balance and the loan re-amortizes over its remaining term.
- PROBLEM: [MAJOR] §3.8's escalating refinance fee (the document's own stated fix for the Scenario-C bullet-rollover Ponzi) is trivially bypassed: §3.1 allows penalty-free early repayment and nothing stops a studio from originating a brand-new Studio Loan at the current (Good-grade) rate to pay off an existing loan in full instead of formally 'refinancing' it — never triggering the escalating fee/rate brake. Fix: define 'refinance' functionally (any new loan whose proceeds retire ≥50% of an existing balance within ~4 weeks counts, regardless of transaction label), attaching the escalation counter to the original obligation's correlation id rather than to a nominal 'refinance' action.
- PROBLEM: [MODERATE] Scenario B (§5.2) compares Rescue against 'a Studio Loan (8%), same size/term' at a 3-year term, but §3.1 restricts Product 1 to 5- or 10-year terms only — the comparator loan does not exist under the design's own rules (arithmetic itself is exact, $150,633 gap confirmed). Fix: extend Product 1's term menu to include short terms, or relabel the comparator as illustrative-only/not an actual menu option.
- PROBLEM: [MINOR] Scenario C's prose says the refinance fee rises '1.0% → 1.5% → 2.0%' (three values, implying three refinance events) and that the bullet is 'rolled three times (6 years total)', but the table only ever applies two fees (1.5%, 2.0%; total $70,000 = $30k+$40k, the 1.0%/$20k value unused) across three 2-year cycles (only two roll/refinance events). Numbers are internally consistent with each other; the prose is not. Fix: drop the unused '1.0%' and say 'issued once, rolled twice', or add a fourth cycle to literally match 'rolled three times'.
- PROBLEM: [MODERATE] `finance-logic.md` §4.3's own recommended anti-exploit rule set has four legs, including 'minimum cash ≥ 8 weeks of fixed costs, tested every week.' `loans.md` §3.4 adopts only the leverage cap, coverage floor, and collateral rule — the minimum-liquidity covenant is dropped without comment, leaving no covenant against a studio borrowing to the leverage/coverage ceiling and then spending down to $0 cash. Fix: adopt the minimum-cash floor too, or state explicitly why it's redundant.
- PROBLEM: [MODERATE] §7's ownership table gives P12 rivals a concrete deterministic rule for drawing a Studio Loan preventively, but no rule at all for whether/when a rival accepts an offered Rescue loan — leaving Route 2 of the P15B ladder's required 'two legitimate recovery routes' unusable by deterministic rivals as specified, despite §17's symmetry requirement. Fix: state a simple deterministic acceptance rule (e.g., accept whenever eligible and post-draw coverage improves).
- PROBLEM: [MINOR] P15-PACKAGE §18.2 explicitly forbids inventing pre-migration distress on old saves, but `loans.md` never states how its three new time-window counters (13-week missed-instalment window, 8-week covenant cure, 8-week sustained-negative-cash trigger) initialize for saves that predate the loan system. Fix: state that every counter starts at zero from the studio's migration boundary week, never backfilled from pre-existing ledger history.
- PROBLEM: [MINOR] The terminal fate of a closed/settled studio's own outstanding Loan balance is never stated, despite §13.2's ID-permanence law and Direction D's 'history preserved forever' requirement implying it should move to some terminal status rather than vanish. Fix: one sentence — balance marked `writtenOff` (never deleted) as part of the studio's closure/settlement event.
- PROBLEM: [MINOR] Direction A's shared market pressure plus Direction D's 'same economic law' for failure creates a real risk of correlated covenant breaches across several genre-concentrated rivals in the same quarter (a multi-studio failure wave), which is exactly the ecosystem-thinning scenario Direction G asks to be reported rather than silently floored — `loans.md` never runs or mentions this multi-studio case (every scenario in §5 is single-studio). Fix: flag it as a tuning risk to pressure-test in §8's open decisions.
- PROBLEM: [MINOR] §7's package-ownership table never mentions P15C (Legacy/finale) despite Direction J requiring the 2040 dossier to acknowledge 'financial highs and crises' and 'bankruptcies & recoveries' — exactly what this loan/default system generates — breaking the document's own method rule to state which package owns each piece. Fix: one line noting the new ledger kinds/default facts are eligible legacyFinaleSnapshot evidence.
- PROBLEM: [TRIVIAL] §9 Sources cites `bridge/industry.ts:1` for the quote 'future receipts, costs and studio revenue remain private'; the quote is actually at line 50 (line 1 is an unrelated file-header comment). Verified by direct read. Fix: correct the line number.
- MISSING: Explicit statement of what happens to a missed loan payment's principal/interest (capitalize vs. forgive vs. extend) — the single most consequential gap, since the entire default ladder's teeth depend on it
- MISSING: A functional (not nominal) definition of 'refinance' that closes the new-loan-to-payoff-old-loan fee-dodge
- MISSING: A stated minimum-cash-on-hand covenant, or an explicit justification for omitting the one finance-logic.md itself recommends
- MISSING: A deterministic P12 rival rule for accepting/using the Rescue tier (only the preventive Studio-Loan-draw rule is specified)
- MISSING: A migration policy for seeding the new distress/default time-window counters on pre-existing saves
- MISSING: A stated terminal status for a closed studio's own outstanding loan balance
- MISSING: Any analysis of correlated/simultaneous rival covenant breaches from shared market pressure (Direction A) and its interaction with Direction G's no-artificial-floor preference
- MISSING: A line connecting this system's ledger/default facts to P15C's 2040 Legacy dossier (Direction J requires 'financial highs and crises' and 'bankruptcies & recoveries' content)
- MISSING: An explicit statement that the new 13-week/8-week counters are pure selectors over persisted ledger facts (not separately mutable state), to satisfy P15-PACKAGE §18.4's save/load replay-determinism requirement

## verify:ranking-vs-value:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: MODERATE: The analysis never applies the PREAMBLE-mandated CONFIRMED/QUALIFIED/CORRECTED/SUPERSEDED-BY-OWNER-DIRECTION label to P15-PACKAGE §12.2/§23 (verified at lines 439-458 and 803) — the exact prior-P15 claim ("never reads Standing, cash/valuation..."; "unweighted 0-30 sum, dense ties") that Owner Direction B's 'fourth lane vs. separate ranking' research question reopens. Two sibling phase-2 analyses (net-worth.md:409-412 and boundaries-corrections.md:87) did apply this exact labeling to this exact clause, showing it was both expected and answerable. The analysis's substance is compatible with the correct answer, so this is a completeness/process gap rather than a wrong conclusion. Corrected statement: label the clause 'SUPERSEDED BY OWNER DIRECTION B, partially' — the categorical financial exclusion is reopened as a live research question (which §4-§5 answer), while the clause's engineering discipline (never read live/raw state, only versioned/disclosed facts) survives; separately mark the 'unweighted sum, dense ties' candidate QUALIFIED (never Owner-ruled; this analysis argues against the dense-tie half only).
- PROBLEM: MODERATE: §6 and §10 item 3 cite '(P15-PACKAGE §12.2/B7: ...)' and 'closing the open B7 item' as though 'B7' is a label used inside P15-PACKAGE.md itself. Verified by grep across P15-PACKAGE.md, P15-BUILDER-ANNEX.md, P13-P15-OWNER-RULINGS.md, P13-P15-LONG-RANGE-ROADMAP.md, and the P12A register: zero matches for 'B7'. The only match anywhere in the reviewed corpus is phase1/prior-claims.md:93, a phase-1 analyst's own internal claim-tracking row ID ('B7 | B | PKG §12.2 (L456-458); §26 P15A.2 | Owner must approve/revise/reject...'). The underlying fact and the P15-PACKAGE quote are both accurate, but attributing 'B7' to P15-PACKAGE misattributes the source. Corrected statement: cite it as 'phase1/prior-claims.md's own tracking item B7, itself referencing P15-PACKAGE §12.2 (lines 456-458)' — never as a P15-PACKAGE label.
- PROBLEM: MINOR (digest-binding): §2.2 cites the retail Studio Rating weights as '(Prima pp. 45-46, 51)'. The phase1-verify _DIGEST.md preamble states as a binding, must-respect correction: 'Prima weights are on pp.46/47/51.' The analysis's '45-46' range reintroduces the page-45 slip the digest specifically corrected, and omits p.47 (Lot Prestige) entirely even though Lot Prestige is one of the five weights the same sentence lists. Corrected citation: 'Prima pp. 46 (Capital/Movies/Stars), 47 (Lot Prestige), 51 (Awards).' (Independently also caught by the sibling ranking-vs-value.break-it.md §3; listed here because checking digest-correction compliance is explicitly part of this lens too.)
- MISSING: A stated CONFIRMED/QUALIFIED/CORRECTED/SUPERSEDED disposition, applied directly to the P15-PACKAGE §12.2/§23 'never reads Standing, cash/valuation' exclusion clause and its dense-tie candidate, matching the labeling discipline two sibling analyses already applied to the same clause.
- MISSING: Correct sourcing of the 'B7' reference to phase1/prior-claims.md rather than to P15-PACKAGE.md.

## verify:talent-settlement-events:break-it — REFUTED_IN_PART
- PROBLEM: CRITICAL: §2.6/§2.9 claim closure ends all obligations and enumerate every needed code change, but nothing gates hollywoodTick.ts's advanceHollywoodWeek per-business loop — staff()/decide() would keep hiring a fresh 6-person roster and greenlighting new films for a 'closed' rival forever, since h.businesses must retain the record (hollywoodValidation.ts:331) and nothing in the proposal skips it. Fix: gate staff()/decide() calls on studioOperatingState[b.studioId]?.status!=='closed', leave operateStage/advanceManagedProductions/run-payout unconditional, and zero overhead/facilityOpex once no production/run remains.
- PROBLEM: HIGH: §2.9's V20 migration ('every identity active') contradicts its own cited precedent hollywood.ts:111-136, which leaves not-yet-entered rivals (eligibleWeek>tick) with enteredWeek:null and no business object — most migrated saves have such rivals. Fix: key studioOperatingState only by identities with enteredWeek!==null; populate lazily in enterRival going forward.
- PROBLEM: MEDIUM: §2.3 cancels in-development screenplays at closure without reconciling actions.ts:2694-2707's existing D-11.9 rule that a writer mid-task cannot be released until the screenplay finishes — a real, uncited tension between 'protect in-progress creative work' and 'cancel uncommitted work for free.' Fix: one sentence stating the guard is a solvent-employer courtesy that does not bind involuntary closure.
- PROBLEM: MEDIUM: P15-PACKAGE §12.3 (cited as authority) also requires 'duplicate requests return the existing manifest/receipt' and 'one failed participant leaves every root unchanged'; §2.9 never addresses idempotency/atomicity for the closure transition, leaving the save/load-mid-settlement edge case unresolved. Fix: state the transition is one synchronous action plus a no-op-on-already-closed re-entrancy rule.
- PROBLEM: MEDIUM: extending zero-severance/write-down to a future player closure (Part 2 table) creates a possible rational incentive to let a studio fail rather than pay releaseTalent's 50% buyout (tuning.ts HIRING_TERMINATION_FRACTION 0.5) on unwanted long guarantees; not acknowledged or cross-referenced to player-failure.md. Fix: note the terminal state must be unambiguously worse than any buyout savings.
- PROBLEM: MINOR: ma-auction.md's 'large failed rival' scenario (20 employees, 3 active projects) contradicts this document's own correctly-sourced rival capacity ceiling (one Production Stage, single-digit headcount, hollywood.ts:98-105) — a cross-document inconsistency the completeness critic should reconcile.
- PROBLEM: MINOR: the proposed employerClosed validator disjunct has no ledger cross-check (unlike termination's mandatory ledger match at hollywoodValidation.ts:394-395); correct for rivals (no ledger) but under-specified if the reason is ever fired for a player-role closure, which Part 2 anticipates.
- PROBLEM: MINOR: §2.5 punts free-agent access to P14/M&A without naming that a single settlement week dumps an entire roster simultaneously, favoring whichever studio has the most idle cash — a legible rich-get-richer snowball risk worth naming explicitly.
- MISSING: A tick/simulation-layer change entry in §2.9 (the advanceHollywoodWeek business-loop gate) — the single most load-bearing implementation seam for the whole recommendation, absent from a table that claims to be exhaustive.
- MISSING: Any statement of atomicity/idempotency scope for the closure transition, required by the very P15-PACKAGE §12.3 passage the document cites as authority.
- MISSING: A cross-reference to player-failure.md flagging the zero-severance/50%-buyout incentive interaction once player closure is authorized.
- MISSING: An explicit note that a closed studio remains in every future Power-Ranking/Studio-Charts cohort forever with a standing frozen at closure (enteredWeek never un-sets) — relevant to P15A.2/consolidation.md and not stated anywhere in this document.

## verify:ma-auction:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: Arithmetic error: Scenario B's set-refund row states $2,150,000 for illustrative capex ≈$6,100,000 at the shipped 35% fraction; correct product is $2,135,000 (6,100,000×0.35). The subsequent 'cash/receivables' row is back-solved to hit the task's given $22,000,000 assets figure using the wrong number, so the BV identity only closes with the erroneous figure. Fix: restate set refund as $2,135,000 and cash/receivables as $15,015,000.
- PROBLEM: The subsidiary-ownership model claims to be purely additive ('no existing root is touched') but StudioIdentity already carries a fixed role:'player'|'rival' literal (hollywoodTypes.ts:6-17) that the report never examines for the reverse acquisition direction (rival acquires a failed player studio). Fix: state explicitly whether role transitions on ownership change, and if not, what a rival-controlled ex-player account becomes.
- PROBLEM: Section 8's facility-transfer rule is explicitly asymmetric (abstract capacity for rival buyers vs. physical PlacedFacility for the player) but neither the reverse direction (rival acquires the player) nor the infeasible-placement case (player's lot too small for acquired capacity) is worked, despite 'player/rival symmetry swap' being a named lens edge case. Fix: state both conversion rules (collapse PlacedFacility to a capability tally when a rival becomes operator; cap the transfer at buildable footprint and auto-refund excess at the existing 50% rate when the player is buyer).
- PROBLEM: 'Cash-only, no borrowed money above LTV' (the primary anti-borrow-to-buy guardrail) is unenforceable as specified because cash is fungible -- there is no way to trace which dollars in a buyer's balance were borrowed. Fix: replace with a balance-sheet snapshot cap (buyer's total outstanding loan principal, post-acquisition, may not exceed a fixed fraction of its own FRP-equivalent net worth), checkable from state the report already keeps.
- PROBLEM: The recommended per-buyer acquisition cooldown can be defeated by routing successive acquisitions through freshly-bought subsidiary labels (each a separate RivalBusiness-shaped account per the report's own §9 design), each starting with its own unused cooldown clock -- a reset-counter exploit. Fix: scope the cooldown to the ultimate controlling StudioId (follow ownedBy pointers to the root) and share one clock across it and every label it controls.
- PROBLEM: No deterministic tie-break is specified for the recommended 'sealed simultaneous offers' default, yet rival AI is deterministic and rule-driven, making identical bids (e.g. every eligible rival bidding exactly FRP) a likely outcome, not an edge case. Fix: add an explicit tie-break, e.g. lowest/earliest StudioId (founding-order seniority).
- PROBLEM: No bidding-window length is stated for the auction; if resolution is same-tick, deterministic AI will always out-bid a slower human player, undercutting direction I's intent that the player gain this ability. Fix: state that the settlement/auction stage (already a named stage in direction D's ladder) is itself a minimum-length bidding window, resolving only at window close, never same-tick.
- PROBLEM: Section 11 names 'a buyer rolling up the whole ecosystem' as the severe failure direction G warns against, but never states the terminal rule for the last-remaining-rival case ('2-studio end state' is a named lens edge case) nor cross-references consolidation.md, which owns that question. Fix: add one sentence stating the terminal rule (recommended: block a bid that would leave zero independent active studios, surfacing G's severe-failure notice) or explicitly defer to consolidation.md.
- PROBLEM: No persisted save state is defined for an in-flight sealed-bid auction (bids collected, not yet resolved) even though the recommended sealed-simultaneous mechanism inherently requires a collection step that can span a save/load boundary; 'save/load mid-settlement' is a named lens edge case not worked through. Fix: either resolve auctions synchronously within one tick (no persisted pending state needed) or add a fourth short-lived additive save root for open auctions.
- PROBLEM: Internal inconsistency: Section 3's shapes table states Shape 1 contracts are 'terminated at 50% cost (no assumption possible)', but Scenario A's Shape 1 note reads '...terminated rather than expired/assumed', implying assumption is a live alternative under Shape 1, which contradicts Section 3. Fix: drop '/assumed' from the Scenario A note.
- MISSING: No statement of whether P15D's own guardrails (cooldown, monopoly ceiling, eligibility filter) continue unchanged post-2040 in Endless Sandbox, or how acquisition events there feed the separate dated post-2040 record set finale-endless.md's task requires.
- MISSING: No serviceability check for Shape 3 (assumption of debt) on whether the assumed debt could itself push a solvent buyer onto the failure ladder immediately post-acquisition (low priority given the report already downgrades Shape 3 wherever it would matter).
- MISSING: No explicit statement of what a pre-P15D save's rivals default to for the new ownership field on first load (almost certainly 'ownedBy: null', but never stated, unlike the report's otherwise explicit migration discipline elsewhere).

## verify:boundaries-corrections:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: Recommended player-terminal Option B (permanent, never-archived, always-re-enterable Administration) satisfies Direction E's negative constraint ('not silent game-over') but the report never checks it against E's explicit positive requirement ('PLAYER STUDIO CAN ULTIMATELY FAIL AND GO BANKRUPT... revises the earlier protected-continuity ruling') -- a permanently-recoverable floor is arguably protected continuity restated. Corrected statement: flag this as an explicit interpretive choice needing Owner sign-off, and state whether outstanding loan principal survives Dormancy unchanged (otherwise it's a repeatable debt-discharge exploit loop: borrow recklessly, ride into Dormancy, shed the consequence, recover).
- PROBLEM: The recommended player terminal law (permanent floor, studio 'never actually stops existing', Sec 3.4) and the recommended rival terminal law (bounded dormancy with 'automatic return-or-close resolution', Sec 3.9) are categorically asymmetric, but the report never names this against its own cited remedy-family symmetry law (Sec 3.6, P15-PACKAGE.md Sec17). Corrected statement: add one sentence stating this is an intentional, Direction-driven (E vs D) asymmetry, not an oversight.
- PROBLEM: Sec1 row 11 / Sec3.7's Stage-1 bankruptcy auction 'short window' has no stated determinism/save-scum-resistance requirement, leaving an unaddressed save-reload exploit that would defeat the report's own 'player-first refusal, then merit order' fairness design. Corrected statement: require single-tick, campaign-seed-deterministic resolution (matching the existing stream(state.seed,...) pattern already used in hollywoodTick.ts).
- PROBLEM: Sec1 row 1 (market pressure, owned by P15A) and Sec3.1 do not require the genre+window competition formula to be well-defined at zero or one active competitors, even though Direction G's no-floor rule makes a 2-studio or 1-studio (player-only) end state reachable, not hypothetical. Corrected statement: add an explicit well-definedness requirement (no competitors => zero competitive pressure) to row 1/Sec3.1.
- PROBLEM: Sec3.12 dismisses any monopoly consequence citing only a 'boring dominance' UX complaint, without considering that Sec1 row 1's own market-pressure mechanic (which consumes a rival's publicly disclosed release schedule) is itself a plausible tool for deliberately timing releases to hasten a struggling rival's failure -- a snowball path distinct from organic consolidation. Corrected statement: flag this targeting-strategy risk for playtesting before accepting 'no consequence' as final.
- PROBLEM: Minor: the Sec3.5 sentence 'costs $13,976/week -- nearly doubling the $15,000 OVERHEAD_BASE' is arithmetically misleading as literally worded ($13,976 is 93% of $15,000, not double it); the 'nearly doubling' claim is only true of the combined sum ($28,976 vs $15,000, 1.93x). Corrected statement: reword to attribute 'nearly doubling' to the stacked total, not the loan payment alone.
- MISSING: No statement on whether entering the player terminal floor discharges/pauses/leaves-unchanged outstanding loan obligations (debt-loop exploit surface)
- MISSING: No determinism/save-scum-resistance requirement for the Stage-1 auction's bidding window
- MISSING: No requirement that the P15A market-pressure formula be well-defined at 0/1 active competitors despite Direction G making that state reachable
- MISSING: No reconciliation of the player-permanent-floor vs rival-bounded-closure asymmetry against the project's own cited remedy-family symmetry law
- MISSING: No statement on whether Dormant/Insolvent/Closed studios are excluded from the live-ranked Power Ranking and Studio Charts lanes while dormant, versus continuing to occupy a ranked row

## verify:ma-auction:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: §2's FRP facility-refund claim ('the code's own rule ... off the ledger-recorded capex row ... not a re-derived today's catalog price, per the P11 handoff law') is wrong on the code fact and overstates a digest-refuted citation: the live shipped mechanism (demolishFacility/facilityDemolitionRefund, placement.ts:1026,1066) actually looks up blueprintById(placed.blueprintId) and prices off TODAY'S catalog blueprint.capex, not a ledger-recorded historical row (placement.ts:1719-1745 is only an invariant checker, not the pricing path) — sibling analysis boundaries-corrections.md already flags this exact divergence ('the live demolition refund already violates this narrowly, placement.ts:1026'). Separately, _DIGEST.md (verify:code-finance:source-fidelity, line 22) explicitly downgrades treating P11-TO-P12-PRODUCER-HANDOFF.md:37 as a general 'P11 handoff LAW' binding future book-value readers to 'a MEDIUM design inference, not a cited law' — a correction the analyst was bound to respect but did not apply. CORRECTED: state that using the ledger-recorded original capex (rather than today's catalog price) for FRP is a NEW design choice for this proposal — sound, and arguably better than the live code's own behavior — but not a restatement of an existing P11 law, since the digest already refuted that reading.
- PROBLEM: §13's package-boundary recommendation adopts the identifier 'P15D — Bankruptcy Settlement & Studio Disposal,' but five other phase-2 sibling analyses (boundaries-corrections.md row 4, net-worth.md:397/442, ranking-vs-value.md:25-26/249-251, loans.md:213) already converge on 'P15D' naming the direction-C VALUATION/NET-WORTH package ('Studio Value & Net Worth' / 'Studio Value & Legacy Economics'), while two others that touch the M&A question directly (rival-failure.md:406, player-failure.md:182,306) deliberately left the M&A slice's letter as 'undetermined — P15D or P16+' rather than claiming it. Ma-auction.md's choice therefore creates two incompatible definitions of 'P15D' across the corpus that the deliverable's package-boundary map (§13) will inherit unless reconciled. CORRECTED: rename the bankruptcy/M&A package (e.g., P15E) to avoid colliding with the already-converged P15D valuation package, or explicitly frame it as a labeled sub-slice of that P15D (as boundaries-corrections.md row 12 itself hedges: 'P16+ (or a scoped P15D extension...)').
- PROBLEM: §13's 'Why not fold this into P15B directly' argument attributes the verbatim quote "very large / high risk ... must not be bundled with acquisition transactions" to 'the prior-wave risk table (P15-BUILDER-ANNEX.md risk register)', but that sentence does not appear in P15-BUILDER-ANNEX.md (confirmed by grep across the whole file, which has no risk table) — it is the P15B row of the risk table in P13-P15-LONG-RANGE-ROADMAP.md:757. The report's own §15 Sources line does cite the Roadmap correctly, so only the in-body attribution sentence is wrong. CORRECTED: attribute the quote to P13-P15-LONG-RANGE-ROADMAP.md:757, not P15-BUILDER-ANNEX.md.
- MISSING: No mention that P12 is the accepted-doc-established sole registry/identity authority ('P12 must supply real studio identities'; 'P12 remains the sole active/dormant/closed registry and entrant-identity/business-state owner') and should therefore be named as a required co-authority/coordinator for the new StudioIdentity-adjacent ownership root in §13's dependency list, even though the additive-root technique chosen (a separate versioned root rather than a StudioIdentity field) is itself sound.
- MISSING: No flag that the 'P15D' identifier it proposes collides with prior use elsewhere in the phase-2 batch (see problems list) — the analysis shows no awareness of the naming conflict for the completeness critic to catch.
- MISSING: No treatment of migration/old-save behavior for a studio that becomes a label mid-campaign (e.g., a save created before the ownership root existed) — not disqualifying for this lens but a gap the completeness critic should note given the package's own additive-root/migration discipline elsewhere.
- MISSING: Minor: the Offworld Trading Company row in §2's comparator table ('no sale price at all; the loser's income flows to shareholders') could be misread as denying that Offworld's majority-buyout has any pricing mechanic at all, when per _DIGEST.md's correction it is in fact a priced, escalating per-share purchase — the report is right to reject that pricing model and adopt only the post-acquisition operating pattern, but the sentence doesn't make that distinction explicit.

## verify:finale-endless:break-it — VERIFIED_WITH_CAVEATS
- PROBLEM: MAJOR: §4.9 recommends the unenforced 'UI-convention-only' path as the default mechanism for freezing the 2040 record ('never re-opening that record for ticking' as a product policy), even though direction K's core invariant is that Endless play 'must not rewrite the frozen Legacy interpretation.' Nothing at the engine level stops a future bug/regression from re-opening that record and ticking it forward, silently invalidating every archetype/dossier fact ever shown. Smallest fix: embed a frozen marker inside the existing opaque checkpointJson blob (no schema/library-version bump needed) and have the load/tick path refuse advance-simulation commands when present, turning 'the UI never offers the button' into an actual engine-level refusal.
- PROBLEM: MAJOR: §4.4's claim that 'P08 awards ceremonies... may still accrue after the frozen trigger (Civ VII 1.2.0 precedent: non-legacy achievements may still accrue)' rests on Civ VII 1.2.0's 'grab Legacy Path achievements' detail (comp-finale.md:53), which the task's own prompt and the digest explicitly flag as STALE: 'Civ VII 1.4.0 replaced Legacy Paths with Triumphs but kept One More Turn' (_DIGEST.md:298). The words 'Triumphs' or '1.4.0' never appear anywhere in the analysis — the exact caveat the analyst was handed was dropped. Smallest fix: drop the Legacy-Path-achievements framing and either re-source the lesson to 1.4.0 Triumphs (confirmed current) or drop the comparator citation entirely.
- PROBLEM: MODERATE-MAJOR (legibility): §4.2 proposes two distinct player-facing choices/backend states ('End the campaign here'→ended vs 'Browse the archive'→archive-browsable) but explicitly says the second is 'same as above' — the document itself cannot state a functional difference between the two, failing the task's own 'can a player explain the stage in one sentence' test and leaving two of Annex C.5's three terminal branches undifferentiated. Smallest fix: name a real distinction (e.g. 'ended' permanently forecloses ever offering Endless later) or collapse the two into one state/button.
- PROBLEM: MODERATE: migration edge case is only half-covered — domain-history honesty (recordedFromWeek) is addressed, but the mechanical case of a save whose CURRENT week already exceeds W_2040 the first time it loads under a finale-bearing build is never addressed (does eligibility fire immediately/retroactively? is the frozen span backdated to a 2040 the save has already passed?). Smallest fix: state eligibility evaluates on the ordinary tick cycle regardless of how far past W_2040 a migrated save already is, with the frozen span ending at the week eligibility actually evaluates true, never backdated.
- PROBLEM: MODERATE: no atomicity guarantee is stated for the 'eligible → source-frozen' transition across a save/load boundary (the task's named 'save/load mid-settlement' edge case) — only the later interpretation step is stated to be pure/idempotent. A save captured strictly between eligibility becoming true and the manifest sealing could reload into a manifest that silently differs from what an uninterrupted playthrough would have sealed. Smallest fix: state the eligible→source-frozen transition commits atomically within the same tick/save write.
- PROBLEM: MODERATE: duopoly/'zombie rival' degenerate market case unaddressed — the accepted-code fact that rivals below operatingReserve 'can run negative forever' without a formal closure event means a near-monopoly 2040 state has no honest degrade for 'too few comparable rivals to characterize a dominance period,' distinct from the existing 'insufficient snapshot history' (data-availability) degrade; left implicit, decades of 90%+ share snapshots can visually read as an implicit score despite the anti-score rule. Smallest fix: add this as its own named honesty-degrade case.
- PROBLEM: MINOR-MODERATE: §4.5's 'second LegacyFinaleSnapshot-shaped record' for Endless never states whether post-2040 evidence IDs (FilmId/PersonId/etc.) continue the same P07/P12/P08 identifier sequences or start a fresh namespace, creating a collision risk if any Endless-era view ever cross-references pre-2040 evidence. Smallest fix: one sentence stating IDs are continued draws from existing sequences — only the presentation record is second, not the identifier space.
- PROBLEM: MINOR: 'End Endless Play' (§4.3) reversibility is undefined — ambiguous between a resumable pause (consistent with the Civ VII 'No More Turns' precedent it cites, which doesn't actually stop free-form play) and a second hard terminal freeze. Smallest fix: state explicitly which it is.
- PROBLEM: MINOR: no tie-break rule stated for a same-tick race between the calendar-2040 trigger and a player's own terminal-bankruptcy trigger (§2.4 treats them as temporally separable but doesn't resolve simultaneity). Smallest fix: define an explicit evaluation order within a tick.
- PROBLEM: MINOR (evidence fidelity): three code citations are imprecise when checked against the accepted snapshot — CampaignRecord shape cited at campaign-library.ts:17 (actual line 18); 'Current campaign copied; the copy is now active' cited at campaign-library.ts:229 (actual line 255; line 229 is unrelated 'case saveAs:' text and doesn't contain the quote); dateLabel projection cited at campaign-library.ts:44-48 (the field is actually set at line 51, outside the cited range). Underlying facts are all real; only the line pointers are off. Smallest fix: correct to lines 18, 255 (or 229-255 for the whole case block), and 44-51.
- MISSING: No handling for a migrated save whose current week already exceeds W_2040 on first load under the finale-bearing build.
- MISSING: No atomicity statement for the eligible→source-frozen transition across a save boundary.
- MISSING: No honesty degrade for 'too few comparable active rivals' distinct from 'history not recorded before week N.'
- MISSING: No statement that Endless-era evidence IDs share the pre-2040 identifier sequences rather than a new namespace.
- MISSING: No reversibility statement for the 'End Endless Play' exit.
- MISSING: No explicit tie-break rule for a same-tick calendar-2040-vs-player-bankruptcy race.
- MISSING: No explicit domain-vs-sub-view accounting against the P15-PACKAGE §22 16-domain cap (the lens table pairs several lenses with named sub-views without stating whether each sub-view consumes one of the 16 slots).
- MISSING: The document never revisits the one stale Civ VII 1.2.0 comparator detail despite the task prompt naming that exact correction as a required input.

## verify:boundaries-corrections:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: §3.4 recommends Option B ('no hard game-over ever,' studio 'never actually stops existing') while claiming it satisfies settled Direction E ('PLAYER STUDIO CAN ULTIMATELY FAIL AND GO BANKRUPT... revises the earlier protected-continuity ruling'). Verified against the direction text: B is functionally the pre-E protected-continuity ruling relabeled as a ladder stage (the report's own text calls sibling Option A 'closest to the pre-direction-E baseline'). Only C and D actually reach a terminal state. Corrected: relabel B/A as non-compliant with settled Direction E; recommend C or D as the direction-compliant floor, or explicitly escalate the B-vs-E conflict to the Owner instead of asserting compliance.
- PROBLEM: This creates an unflagged internal inconsistency: §3.9 reinforces B as 'the player's permanent floor' while §3.10 recommends the player can fail 'yes, identically' pre/post-2040 — moot if B (no true failure ever) is adopted. Corrected: reconcile §3.9/§3.10 with whichever terminal structure is actually recommended.
- PROBLEM: Row 5's Doc. prerequisite cites 'D-17B §8 (docs/D-17B-OWNER-AUTHORIZATION.md:37-38)' for the loan prohibition. Verified directly: lines 33-40 are §4 ('Authorized surface'), and the 'NOT AUTHORIZED: [loans...]' text is at lines 37-39 inside §4, not §8 (§8, lines 58-61, is the unrelated publicity-mechanic clause). Corrected: cite D-17B §4, not §8.
- PROBLEM: Row 3 presents the P11-TO-P12-PRODUCER-HANDOFF.md:37 sentence ('never reconstruct a purchase from today's blueprint price') as 'the P11 handoff rule' binding a not-yet-built net-worth reader. Per phase1-verify digest (verify:code-finance:source-fidelity), that sentence in context governs capital-contributor History rows specifically; extending it to a future valuation reader is 'a MEDIUM design inference, not a cited law.' Corrected: label this an inference by analogy, not an already-binding P11 rule.
- MISSING: Rows 15-16 (2040 finale, Endless Sandbox) never connect Direction J ('every factual claim traces to recorded evidence') / K ('must not rewrite the frozen Legacy') to the existing migration-origin guardrail in code (origin==='migration' rivals have null founding/no authored films, and the validator already forbids a fabricated migration past, per phase1-verify digest item 44 of code-hollywood:completeness-overclaim) — a package-law item ('no retroactive fiction in migrated saves') the task brief explicitly names.

## verify:consolidation:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: MAJOR: consolidation.md's 'Corrections to prior P15 text' section is missing P15-PACKAGE.md's own §23 Owner-decisions table (the flagship package doc, cited by consolidation.md only once at an unrelated line 158), which at line 800 recommends 'deterministic bounded eligibility with P12's minimum three active AI rivals' and at line 798 conditions the future rival-closure implementation slice on an 'entrant floor'/'minimum-rival law.' Corrected statement: both rows must be recorded SUPERSEDED BY OWNER DIRECTION G, and the rival-closure slice must not be gated on proving a floor.
- PROBLEM: MAJOR: consolidation.md never cites accepted-592e926/docs/design/CODEX-RIVAL-STUDIOS-HOLLYWOOD-ECOSYSTEM-PACKAGE-12-BUILDER-ANNEX.md:626, an ACCEPTED 'implementation-ready' design doc whose State/Edge Case Matrix contains an 'active-count floor' row prescribing 'deterministic entrant scheduling... Hollywood remains populated' -- a literal synthetic-floor mechanism the new Direction G forbids. This is exactly the kind of on-topic prior commitment (independently flagged as 'on-topic' and previously skipped, per _DIGEST.md:39) this section needed to find and supersede. Corrected statement: label this row SUPERSEDED BY OWNER DIRECTION G; no builder should implement scheduled entrants beyond the nine authored arrivals to keep the count up.
- PROBLEM: MODERATE: the same P13-P15-LONG-RANGE-ROADMAP.md §19.3 table consolidation.md already cites (and correctly supersedes for the 'Later entrants / active-rival floor' row) also has a 'Rival closure' row requiring 'active-rival-floor proof' as a precondition for the P15B terminal slice; consolidation.md left this coupled clause uncorrected even though it read the adjacent row in the same table.
- PROBLEM: MINOR (mischaracterization): section 5 treats P15-BUILDER-ANNEX.md §C.2's archived/excluded rank-state table as an already-existing 'state machine' that 'does not need to be invented, only specified,' when that annex is explicitly headed 'Mode: DOCUMENTATION ONLY,' 'NO PRODUCTION AUTHORIZATION,' 'Build nothing from this annex now,' pinned to an older base (7811377) than 592e926, and no such rank state exists in accepted code (only an unrelated per-lane Standing-derived chart, itself in tension with 'Standing is not rank' per the digest). Should be labeled QUALIFIED design proposal, not treated as existing machinery.
- PROBLEM: MINOR (citation precision): hollywoodTick.ts:174 is cited in section 9 for the reserve-gate check, but the actual line (used correctly elsewhere in the same report) is 176; _DIGEST.md 'line 31/58' is cited for the Transport Fever 2 re-tier correction, but the actual lines are 242 and 252. Substance in both cases is accurate; only the pointers are wrong.
- MISSING: A corrected 'Corrections to prior P15 text' entry covering P15-PACKAGE.md:798 and :800 (§23 Owner-decisions table)
- MISSING: A corrected entry covering docs/design/CODEX-RIVAL-STUDIOS-HOLLYWOOD-ECOSYSTEM-PACKAGE-12-BUILDER-ANNEX.md:626 ('active-count floor' edge case)
- MISSING: A corrected entry covering the P13-P15-LONG-RANGE-ROADMAP.md §19.3 'Rival closure' row's 'active-rival-floor proof' clause
- MISSING: A CONFIRMED/QUALIFIED/CORRECTED/SUPERSEDED label on the P15-BUILDER-ANNEX.md §C.2 rank-state table rather than treating it as already-built

## verify:finale-endless:authority-consistency — VERIFIED_WITH_CAVEATS
- PROBLEM: §4.8: The analysis declares P15-PACKAGE §16's 'PRELIMINARY RECOMMENDATION: apply the same...predicates...to player and rivals...neither gets a secret pre-terminal rescue' clause 'settled symmetry-of-guards law, not an open recommendation,' claiming Owner Direction E supersedes its 'preliminary' framing entirely. Verified against §12.3 (the source section), the package text contains two distinct claims: a PROJECT AUTHORITY VERIFIED fact (player has no mandatory hard-bankruptcy game-over) that Direction E does revise, and a separately-hedged, un-tagged 'recommendation' (predicate/remedy symmetry, no secret rescue) still gated by 'Only an Owner-approved policy may activate these states' -- which none of Directions A-K touches. Treating the second as now-settled law is exactly the pattern P13-P15-OWNER-RULINGS.md §6 forbids ('may not silently convert an open choice into package law'). Corrected: label the §16 row QUALIFIED (Direction E settles only the terminal-eligibility fact); the predicate/remedy-symmetry design remains an unratified recommendation and should be restored to §6's remaining-Owner-decisions list.
- PROBLEM: §4.9 cites bridge/runtime/campaign-library.ts:17 for the CampaignRecord shape; the type alias is actually on line 18 (line 17 is CAMPAIGN_LIBRARY_MAX_RECORDS=32). Substance unaffected.
- PROBLEM: §4.3 cites campaign-library.ts:44-48 for the dateLabel/campaignDate projection; the dateLabel field itself is written at line 51, not within 44-48 (that range is the function preamble). Substance unaffected.
- PROBLEM: §4.4 table cites '(per §4.6)' for post-2040 rival failure/consolidation continuation; that topic is actually argued in §4.8, not §4.6. Cosmetic internal cross-reference slip, not an authority-doc problem.
- MISSING: §6 (Remaining Owner decisions) should list 'whether the player/rival remedy-predicate structure must be fully symmetric beyond presentation/selection mechanism, or may differ further' as a genuine open decision -- it is silently treated as closed instead.
