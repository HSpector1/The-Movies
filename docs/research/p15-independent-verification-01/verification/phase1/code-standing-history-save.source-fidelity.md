# Source-Fidelity Verification — `phase1/code-standing-history-save.md`

**Verifier:** adversarial source-fidelity pass (independent of the author)
**Report under test:** `scratchpad/out/phase1/code-standing-history-save.md`
**Method:** every checked citation was re-opened directly (`sed -n` on the accepted snapshot at `scratchpad/accepted-592e926/`, `grep` across `src/core`, `bridge`, `ui/src/engine`, the three registers, the authority docs, the plain-text original-game extractions, and one Prima PDF page via Read). No git, build, test, or player data touched. Nothing written outside `scratchpad/out/`.
**Default rule applied:** a claim is "weak" if I could not reproduce the citation at the stated location.

**Verdict: VERIFIED WITH CAVEATS.** Every consequential claim reproduces from the cited source. Two citations carry wrong line numbers (substance intact), and two relevant items are missing from the report's register / original-game sweeps. No claim was refuted; no confidence tier was mis-assigned.

---

## 1. Checks performed (13 claims, ranked by consequence for P15)

| # | Claim (from author summary) | Source I opened | What the source actually says | Citation exact? | Tier correct? | Result |
|---|---|---|---|---|---|---|
| 1 | Three Standing channels, clamped 0..100, `INITIAL_STANDING` 40/40/50; no composite; bridge says "there is no combined Power score" | `types.ts:267-271`; `tuning.ts:1698-1702`; `standing.ts:202-206`; `bridge/industry.ts` | `Standing = {audienceAwareness /*0..100*/, industryPrestige, commercialConfidence}` (types 267-271). `INITIAL_STANDING = {40, 40, 50}` (tuning 1698-1702). Return block clamps each channel `clamp(..., 0, 100)` (standing 203-205). The notice `'Public facts only. Standing channels and film measures have separate meanings; there is no combined Power score.'` exists verbatim but on **`bridge/industry.ts:110`**, not `:117` (line 117 is the recent-output `count` helper). Rank helper at `:68-72` (`1 + rows.filter(r => value(r) > value(own)).length`) and per-lane mapping at `:77-86` confirmed; `INDUSTRY_LANES` = 4 separate lanes (`industry-schema.ts:3`). | Line slip: `:117` → `:110` | HIGH (code) — correct | **CONFIRMED, minor line error** |
| 2 | D-6 update formulas + pivot 0.45/0.58 + weekly drift 0.04·max(0, aw−35) | `standing.ts:143-207`; `tuning.ts:89-121, 165-183`; `tick.ts:825-856` | `updateStanding` body: awareness `clamp(REACH_WEIGHT·(reach−reachNeutral) + STAR_WEIGHT·starAttention, ±DELTA_CAP)` with `reachNeutral = ctx.engaged ? 0.45 : 0.58`; prestige `clamp((criticScore−45)/1.2, ±10)`; confidence `clamp(4·clamp(roi/5,±1) − 4·clamp(overrun,0,1), ±5)` with cost floor 500 000. Constants exactly as listed (`tuning.ts:89-110, 120-121, 167-169, 178-182`). Drift at `tick.ts:848-856`: `excess = max(0, aw − 35)`, `aw − 0.04·excess`, inside `if (engaged)`. | Yes | HIGH — correct | **CONFIRMED** |
| 3 | Rival Standing per-studio & symmetric; `engaged:true` hard-coded; `filmReleased` receipt with before/after; rivals get same drift | `hollywoodTypes.ts:79-86`; `hollywoodTick.ts:249-253, 277-278` | `RivalBusiness.standing: Standing` at `:83`. `:249 const before=b.standing; :250-252 b.standing=updateStanding(before, filmResult, p.forecastSnapshot, {... engaged:true}); :253 appendReceipt(h,{... kind:'filmReleased', ..., before, after:b.standing})`. Drift at `:277-278` uses the same `AWARENESS_DRIFT_ANCHOR`/`RATE`. Payroll/overhead/opex `moveRivalMoney` at `:280-282`. | Yes | HIGH — correct | **CONFIRMED** |
| 4 | Chart snapshot every 13 weeks, retained as `chart`/`previousChart` only | `hollywoodTick.ts:306-313`; `hollywoodTypes.ts:104, 122-123` | `if(week%13===0||h.chart===null){ ... h={...h, previousChart:h.chart, chart:{week,rows}} }` at `:306-313`. `HollywoodChartSnapshot = {week; rows:{studioId; standing; output}[]}` at `:104`; state holds `chart` and `previousChart` only (`:122-123`). | Yes | HIGH — correct | **CONFIRMED** |
| 5 | `careerMilestone` typed + rendered but has no producer; actual producers listed | grep of `kind: '…'` drafts across `tick.ts`, `actions.ts`, `studioHistory.ts`, `releaseCareers.ts`, `industryCareer.ts`, `hollywoodTick.ts`; `types.ts:1646-1652`; `bridge/history.ts:367` | Producers found: `filmReleased` tick 613, `theatricalRunCompleted` tick 760, `standingChanged` tick 793/864 + actions 2833, `facilityCompleted` tick 1057, `studioFounded` actions 1308, `facilityCommitted/Demolished/Moved` via `withFacilityHistoryRow` actions 1444-1446, `standingDriftFolded` studioHistory 293. Every `careerMilestone` hit is a type, validator (`hollywoodValidation.ts:442`, `save.ts:4915/4929`), identity collector (`productionIdentity.ts:68`), classifier (`studioHistory.ts:161`), or renderer (`bridge/history.ts:367,395`). Type comment at `types.ts:1646`: "No row exists until P10 emits it." | Yes | MEDIUM for absence — appropriately hedged | **CONFIRMED** |
| 6 | No Awards simulation: only `{kind:'award'}` requirement that always evaluates UNMET with "Awards are not part of the game yet."; handoff sentence exact | `types.ts:918-919`; `blueprintRequirements.ts:62-66, 117-118, 160-165`; full grep; `authority/P12-TO-P13-PRODUCER-HANDOFF.md:29` | Type at 918-919 with comment "Awards land in C3". `NOT_YET_ATTAINABLE_KINDS.award = 'Awards are not part of the game yet.'` (65); sentence builder (117-118); `satisfied` switch returns `false` for `'award'` (160-165, "UNMET on purpose"). Whole-tree grep for award/ceremon/nominat/trophy/oscar/academy yields only these, `placement.ts:1276-1277` (validates `awardId` non-empty), `tuning.ts:1056` comment, `bridge/people.ts:269` copy. Handoff :29 contains exactly "acceptance does not deliver unimplemented Awards simulation". | Yes | HIGH — correct | **CONFIRMED** |
| 7 | `classifyRecovery` predicates and the "No recovery mechanic (loans/financing)" sentence | `studioRunRecap.ts:79-84, 962-1007` | Type union at 79-84. Function 962-1007: `incomplete` iff `!inp.films.length || inp.cheapest == null` (974-978); `noNormalProduction` iff `!cheapestOk` (985-989); `healthy` iff `standardOk && typicalOk && waitingHelps` where `waitingHelps = netWeeklyCash >= 0` (990-993); `severe = !standardOk && !hasActiveRevenue && !waitingHelps && runwayWeeks != null`, else `constrained` (1005-1006). Reason string at 1003 ends "No recovery mechanic (loans/financing) exists in the current rules." | Yes | HIGH — correct | **CONFIRMED** |
| 8 | Recap re-exported only to browser UI; bridge has no recap consumer; `runwayState 'inRed'` iff `cash <= 0`; "Cash is in the red…" attention line | `ui/src/engine/adapter.ts:7941-7962`; grep `bridge/`; `financeReport.ts:274-281`; `bridge/finance.ts:83` | Re-export block "D-15 Studio Run Recap" at 7941-7962 incl. `RecoveryPosition`. `grep -rn "studioRunRecap\|classifyRecovery\|RecoveryPosition" bridge/` → no hits. `runwayState` ternary at 274-276: `founding!==null ? 'unavailable' : view.cash <= 0 ? 'inRed' : …`. `bridge/finance.ts:83` attention array begins with the exact "Cash is in the red. Current receipts and existing obligations remain visible; voluntary decisions follow their own affordability rules." | Yes | HIGH — correct | **CONFIRMED** |
| 9 | No game over / bankruptcy: tick has no cash predicate, throws only at 143/475/506; debits unconditional; calendar has no end; `canAfford` gates listed; `releaseTalent` ungated | `tick.ts` grep `throw`; `tick.ts:182-216, 913-953`; `calendar.ts:13-21`; `employment.ts:70-86`; `actions.ts` grep `canAfford`; `actions.ts:2694-2725`; whole-tree grep | `throw` in tick.ts only at 143, 475, 506 (all id/reference guards). Lines 182-216 contain no `cash` token. `cash -= payroll/overhead/facilityOpex` at 919/930/946 with no floor. `campaignDate` rejects only non-safe/negative weeks; year = 1920 + ⌊w/52⌋ unbounded. `canAfford`: `after = cash − amount; ok iff after >= 0` (78-80). `canAfford` call sites: 572, 1421, 1607, 1625, 2629, 2674, 2790 — exactly the seven listed. `applyReleaseTalent` (2694-2725) has no `canAfford`; debits `cash − cost` at 2720. Grep for game over/bankrupt/insolv/liquidat/foreclos/terminal state → no hits in src/core, bridge, ui/src/engine. | Yes | HIGH — correct | **CONFIRMED** |
| 10 | Rivals can run negative indefinitely: `moveRivalMoney` no floor; reserve gates at 98/126/176; still pay at 280-282; no closure transition | `hollywood.ts:31-41`; `hollywoodTick.ts:98, 126, 176, 280-282`; `hollywoodTypes.ts:6-17` | `moveRivalMoney` only throws on non-finite; `account.cash += amount` unconditionally (31-41). Lines 98/126: `if(b.account.cash − signingBonus < reserveAfterOffer(...)) continue`; 176: `if(... || b.account.cash < operatingReserve(b,h,week)) return`. 280-282: payroll/overhead/facilityOpex debits every week. `StudioIdentity` fields (6-17): no status field. | Yes | HIGH — correct | **CONFIRMED** |
| 11 | Newspaper = single-film clipping for the player's own release; DISCLOSURE constant at 401-402; built only via adapter 5965-5990; no bridge exposure; reads nothing from `hollywood.*` | `newspaper.ts:1-10, 30-31, 401-402`; grep; `adapter.ts:5934-5992` | Header 1-10 matches. `DISCLOSURE` constant at 401-402 verbatim. Masthead `NEWSPAPER_MASTHEAD = 'The Silver Screen Gazette'` is at **`:31`** (`:30` is the "Original fictional…" comment). `grep hollywood|rival|studioEntered newspaper.ts` → none. `grep -rl newspaper|Gazette|clipping|buildNewspaper bridge/` → none. `releaseNewspaper` at adapter 5965-5990 is the only builder call; `filmRecordView` (5934-5936) calls `releaseNewspaper`, so still the same path. | Line slip: `:30` → `:31` (trivial) | HIGH — correct | **CONFIRMED, trivial line error** |
| 12 | Industry Pulse seam: view `'pulse'`, 13-week receipt feed, activity kinds, disclosure gated on `announcedWeek!==null` | `industry-schema.ts:16`; `bridge/industry.ts:90-98, 150, 162, 183-186` | `view: enumeration([...,'pulse',...])` at schema :16. Activities `flatMap` over `h.receipts` at 90-98 with `studioEntered → "joins Hollywood"`, `employment` (skips renewal/entry/existing-player-contract), `filmReleased` with three labelled before→after standings, `filmSettled`, `filmAnnounced → "announces a film … No release date is promised."` `:150 if(cost?.announcedWeek===null||!cost)continue`; `:162 if(!business||!cost||cost.announcedWeek===null) throw`. `:184-186` title `'Industry Pulse'`, notice text verbatim, filter `week >= tick − 12`, order releases/people/studios/announcements. | Yes | HIGH — correct | **CONFIRMED** |
| 13 | Save V19 pattern: `GameStateV19 = GameStateV18 & {hollywood}`; `validateSaveV19` peels & delegates; `convertV18ToV19 = makeSave(initializeHollywood(state,'migration'))`; downgrade refusals; `assertFrozenBuilderRetainsHollywood`; Save As semantics | `types.ts:1689-1690`; `save.ts:363-368, 5675-5679, 7228-7257`; grep `saveVersion === 19`; `hollywood.ts:111-136`; `campaign-library.ts:14-18, 181, 229-235`; codec 6-15 | All exact: type at 1689; envelope 363-368; validator 7228-7249 (`Hollywood root missing` guard, `{hollywood,...legacy}` peel, `validateSaveV18` delegate, `validateHollywood`); `convertV18ToV19` 7250-7253; `migrateToV19` 7254-7257; refusals at 7072/7139/7147/7160/7176/7202 with the exact message; guard 5675-5679. `initializeHollywood` sets `origin`, `originWeek: tick`, player `recordedFromWeek = studioHistory.recordingStartedWeek`, rival `founding: null` under migration, `enterRival` only when `eligibleWeek <= tick`. Library: max 32 records / 256 MiB (15-16); `CampaignRecord={id,label,revision,checkpointJson}` (18, no frozen flag; grep readOnly/frozen/locked/finale → none); `:181` "Save As must preserve the original record."; `:229-235` `fromSaveJson(session.exportRuntimeCheckpoint().currentSaveJson…)` + `randomUUID()`; codec gzip level 1. | Yes | HIGH — correct | **CONFIRMED** |

### Register and original-game citations (also re-opened)

| Cited row / source | Re-read at | Result |
|---|---|---|
| P08-P10 register rows P08-REQ-003 (:12), -022 (:13), -023 (:14), -024 (:15), -027 (:18), -028 (:19), -029 (:20), P09-REQ-035 (:26), P10-REQ-039 (:40) | `docs/operations/P08-P10-DEFERRED-NOT-DROPPED-REGISTER.md` | All IDs, dispositions ("REJECTED BY OWNER", "OWNER-BLOCKED", "DEPENDENCY-BLOCKED", "DEFERRED TO NAMED PACKAGE", "P09-LATER-LAND", "P10-LATER-RANK", "Owner later") reproduce verbatim at those lines. Grep for loan/bankrupt/distress/net worth/valuation/finale/endless/minimum rival/floor → no hits. **CONFIRMED** |
| P12A register GOV-005 (:74), GOV-006 (:75), ID-011 (:93), SIM-006 (:105), HIS-013 (:166), INT-009 (:196), INT-010 (:197), SAF-004 (:228), SAF-008 (:232), SAF-009 (:233), SAF-016 (:240) | `docs/engineering/P12A-DECISION-AND-REQUIREMENT-REGISTER.md` | All reproduce at the stated lines, including "No campaign ending.", "Owner-blocked future mode, irrelevant to P12A acceptance", "At least two comparable snapshots; permutation ties; reasons trace; no view-time recompute", "Bankruptcy, recovery, closure/replacement/churn remain P15B." **CONFIRMED** — but see Missing item M1 (SIM-015). |
| P11A register P11-REQ-023 (:122), -041 (:140), -042 (:141), §4.E (:187-194) | `docs/engineering/P11A-DECISION-AND-REQUIREMENT-REGISTER.md` | Exact: "Preserve recoverable negative Cash; do not invent bankruptcy" / PROVEN; "No fake disabled tabs" / OWNER-BLOCKED / DEFERRED; "No implicit terminal state" / OWNER-BLOCKED / DEFERRED; §E items 2-4 as quoted. Grep for net worth/valuation/acquisit/power rank/finale/endless → none. **CONFIRMED** |
| Manual "balance can go into the red … won't be able to build new sets or add certain facilities" — `manual.txt:129-130`, page header "Page 6" at `:105` | `scratchpad/original-text/manual.txt:105-130` | qxp header `Page 6` at :105; the Note sentence is at :129-130 exactly. RETAIL SHIPPED MECHANIC tier correct. **CONFIRMED** |
| Prima "Building in Debt" printed p.14 / PDF p.15 — `prima.txt:769-775` | `prima.txt:768-777` and Prima PDF page 15 (Read) | Text at :768-775 lists Casting Office, Crew Facility, Production Office, Basic Script Office, Stage School, Stage Set, Star & Script Selling Facility. PDF page 15 shows printed folio "14" with the "Building in Debt" heading. Tier PRIMA correct. **CONFIRMED** |
| GameFAQs (Maxx) repeats the debt rule — `gamefaqs-maxx.txt:659` | `:659-660` | "Most sets and buildings cannot be built once you are in debt, but there are a few important facilities that can…" Labelled COMMUNITY. **CONFIRMED** |
| §7 corrections: P15-PACKAGE `:352,:355,:356,:358,:359,:362` and `src/core/ledger.ts`/`theatrical.ts`/`events.ts` absent; `worldgen.ts:645 competingSlate: []`; `reception.ts:679 competitionFactor = 1.0`; handoff `:27` no dormant/closed status field | authority docs + `ls` + sed | All reproduce. The three seam files do not exist in the snapshot. **CONFIRMED** |

---

## 2. Refuted or weak claims

None refuted. Two citation-precision defects, both non-substantive:

1. **`bridge/industry.ts:117` for "there is no combined Power score"** — the notice is on line **110** (the `IndustryPage` result literal). Line 117 is the `count` helper in the recent-output branch. Corrected citation: `bridge/industry.ts:110`. Substance and HIGH tier stand.
2. **`newspaper.ts:30` for the masthead** — `NEWSPAPER_MASTHEAD = 'The Silver Screen Gazette'` is on line **31**; line 30 is the preceding comment. Corrected citation: `newspaper.ts:31`. Trivial.

Everything else I opened matched at the exact lines given, including all seven `canAfford` sites, all six downgrade-refusal lines, the classifier line ranges, and all register line numbers.

---

## 3. Missing items (relevant to the topic, not in the report)

- **M1 — P12A register SIM-015 (`:114`) belongs in the "minimum rivals" sweep.** It reads: "Use three rivals for bounded P12A proof; treat roughly 6–10 active AI rivals as later operating target and 12 as stress, not Owner law. Entrants/active-floor/churn occur later…" with disposition "Implemented four fresh incumbents and fixed 520/988/1560/1872/2548 arrivals, nine plus player… Twelve-rival stress remains distinct/unmeasured." The report states "the only related phrase is INT-010's 'active-floor/cap'"; SIM-015 is a second, more specific row (6–10 target / 12 stress, explicitly "not Owner law") that a P15B active-floor design should cite. PERF-001 (`:173`) repeats the 6–10 / 12 figures as a performance envelope.
- **M2 — The original game's RETAIL composite "Studio Ranking" is on the same manual page the report cites and was not noted.** `manual.txt:111-125` (manual p.6): the Studio Ranking icon's rank "is determined by a number of factors, such as your cash balance, the quality of movies…, the quality of Stars…, how well connected and laid out your studio is and even how clean and tidy you keep the place"; `:126-128` adds that Cash Balance "contributes toward your ranking in the Charts". This is a RETAIL SHIPPED composite ranking — precisely the thing P08-REQ-003 rejects and INT-009/SAF-004 re-home to P15A.2. The report's original-game cross-check covered only negative cash; the composite-ranking contrast is the more consequential parity fact for P15A.2 and should be recorded (with the Owner's explicit rejection alongside it, so it is not read as a parity target).
- **M3 — Original-game Awards are on the cited Prima page and were not cross-checked.** Prima printed p.14 (`prima.txt:764-780`) describes nine Achievement Awards and three Lifetime Achievement Awards (awards granted "for getting a studio to 2005…"), pointing to the chapter "Awards, Achievements, and Lifetime Honors". The report's §2.3 correctly proves no award simulation exists in code, but the topic asked what a 2040 Legacy could cite; noting that the retail game had a 2005 lifetime-honor structure (and that the accepted code has none, P08-REQ-022..029 blocked) would complete the picture. Tier: PRIMA / RETAIL SHIPPED.
- **M4 (minor) — `broadcast.ts`/`BroadcastItem` bridge exposure claim not re-verified here** (author says "no bridge exposure of `broadcastItems`"); low consequence for P15 and not among the twelve checks, so it stands as the author's MEDIUM/HIGH claim, unverified by me.

---

## 4. Tier-assignment audit

- Code claims are labelled HIGH only where the line was read; absence claims (`careerMilestone` producer, composite score, "records") are labelled MEDIUM — appropriate.
- Original-game claims are correctly split RETAIL SHIPPED (manual) / PRIMA / COMMUNITY (GameFAQs), with the explicit "No inspected retail source establishes a bankruptcy game-over" formulation rather than a proof of absence — correct discipline.
- No pre-release material is cited, so no promotion risk.
- The author's design suggestions (frozen-finale record attribute; P15 root naming) are marked LOW / "reader's projection" — correctly separated from evidence.

---

## 5. Bottom line

The report is a faithful read of `592e926`. Of the thirteen consequential claims re-opened, thirteen reproduce; two carry off-by-a-few-lines citations (`industry.ts:110` not `:117`; `newspaper.ts:31` not `:30`). The register sweep missed SIM-015 (rival cohort target), and the original-game cross-check omitted the retail composite Studio Ranking and the retail Awards structure that sit on the very pages it cites. None of these change any conclusion the report draws; they should be folded in before the report is used as P15A.2/P15B authority.
