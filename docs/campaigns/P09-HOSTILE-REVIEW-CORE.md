# P09 HOSTILE REVIEW — core checkpoint (verbatim reviewer report)

Independent, read-only reviewer (fresh context; no authorship of any P09 change) on TS `fee206fa` × Unity `a8f4c1aa`, 2026-09-05 12:37–12:53Z. Recorded verbatim; the author's disposition of every item is in `P09-TECHNICAL-CHECKPOINT.md` §9.

---

**P09 HOSTILE REVIEW — core checkpoint (independent read-only reviewer, 2026-09-05 12:37–12:53Z)**

Reviewed pair: TS `fee206fa039fa96e29c98108c206f56df1b1bb4c` (product) on `wip/p08-p10-autonomous-stack-01-ts`, local HEAD `74b1acdc` == `hspector-github` (docs commit carrying the checkpoint); Unity `a8f4c1aaa35da483a1fd8463718a3c33fa0b4535` (product) on `wip/p08-p10-autonomous-stack-01-client`, local HEAD `79c5fd66` == `origin` (one later commit, `Tools/p09-run-hid-build.sh` +4/−2 only, no `Assets/` change). Both working trees clean. Candidate `~/Desktop/P09-Core-Technical-Candidate-fee206f-a8f4c1a/`. Approximately 90 tool uses; no file inside either worktree, the candidate, or the Owner baseline was created, modified or moved; scratch under `…/scratchpad/hostile/` only. Unity EditMode was NOT re-run (see N9).

**VERDICT: KEEP WITH CORRECTIONS.** The code passes every law tested (no hidden cash, no free facility, no special hire, no skipped cost, no future-result knowledge, no Owner-profile mutation, no decorative worker called a Builder, no deferred system built, no merge/rebase/force-push, campaign branches and `main` unchanged). The real-HID Build flow is PENDING and is reported honestly as pending (§5 row is the literal placeholder `⟨HID_RESULT⟩`; handoff C5 "OPEN"; `candidate/evidence/hid/` empty; no `Evidence/P09-HID*` directory exists; the launcher is owner-idle gated at `Tools/p09-run-hid-build.sh:34-45`). The checkpoint must not be presented as closed until that result lands. Required text corrections before the seal stands: (1) disclose the phantom authored Scenery Shop on the bare lot (B1) in §7 and qualify REQ-004 / the "bare-lot art law" claims; (2) replace the wrong generated-contract identity in §1 (B2); (3) N1, N2, N3, N4, N5, N6, N7, N8 wording/accounting corrections below.

**BLOCKING**

B1. The bare lot is not bare: the authored operational Scenery Shop stands on it. Claim: §2 W3a / §4 "bare-lot art law"; §3 REQ-004 done ("only Gate, Administration/Staff Office, authored access, owned parcels, and non-mechanical utility dressing"). Ran/read: opened every run-7 bare-lot PNG (`Evidence/P09-Oracle/p09-sparse-start-20260905T122755Z/p09-sparse-start-sparse-lot.png` and 15 others), cropped/enlarged the north-west quadrant; audited `Assets/Studio/Scenes/StudioLot.unity` (the lot authored by `Assets/Studio/Editor/Authoring/StudioLotArchitectureAuthoring.cs`) for every `StudioLocationBinding` (guid `f05399f3…`) and every unbound child of the ARCHITECTURE/WORLD groups, mapped to the gate-anchored grid (origin (−91.75, 58.62), cell 4.1667, from the sparse-start sidecar). Found: `09_Operational_Scenery_Support` (green-marquee "SCENERY SHOP" hangar; world (−29.5, 0, 47.0), walls x −36.7..−22.3, z 41.2..52.4 → grid cols ~13–17, rows ~1–4) has NO binding and is active; it sits inside the owned, buildable North Yard (`BARE_LOT_PARCELS[0]` = x 0..38, y 0..10, `src/core/lot.ts:318-324`), which the chooser reports as "0 of 429 cells occupied". It is visible at week 0 in the sparse-start capture and in every later bare-lot capture, so from week ~25 the lot shows two scenery shops (the phantom hangar and the generic body the player paid $850,000 for). Cause: `StudioLotGrowthPresentation.ApplyAuthoredRoots` (`…/Presentation/StudioLotGrowthPresentation.cs:165-200`) hides only `StudioLocationBinding` roots; nothing in `Assets/Studio/Runtime` references `09_Operational_Scenery_Support`. The oracle's "only Gate and Administration stand" (`StudioPostReleaseOracleRunner.cs:421`, helper 1963-1969) checks bound roots only; the visual question "no phantom studio?" is `visualReviewStatus: "pending"` in every sidecar — nobody opened the image (order §10: "Capture actual images and open them"). Lesser, same root: the authored concrete aprons of the hidden endowed buildings (Post at col 19/row 6, Stage A col 33/row 6, Casting col 19/row 18, Theater col 29/row 20) remain as the pale rectangles the captures show; the engine parcel quads (green tint α 0.26, `:441`) are not distinguishable from grass, so "engine ground map drawn" is a boolean, not what the player sees. The water tower (`10_Studio_Water_Tower`, col 25/row 2) is legitimately "utility dressing". Why it blocks "as claimed": a Scenery Shop is a facility the player must build, not dressing; REQ-004's rationale is "the sparse start is legible and truthful", and §7 ("honest, not deferred scope in disguise") omits it. Correction: disclose in §7 as a known P09 defect with the object name and cause, mark 004 "done with disclosed visual defect", and either hide unbound facility art under the bare-lot regime with a machine assertion (W6) or record it as the first item for the next wave. No economics are affected (the wire carries no scenery capacity; the first-film journey still paid for its shop).

B2. §1 "Generated contract … output identity `ae177bb5f6c62dc8430007dee13a74a4b954f35327c2c77b7305956f08232ca2`" is not the generated contract's identity. That hash is the generator-test fixture identity pinned for `F10_CURRENT_QUOTE_UNIONS` and `F11_CURRENT_COMMAND_UNION` at `tests/bridge-contract-generator.test.ts:620-621`; it appears nowhere in the manifest, the CF-09 reports, or Unity. The generated file's sha256 is `ba9e5874b8335077a16d779f2b6704642a9c75e532b506863af0311a06bc5b9f` (`generated/unity/project-studio-bridge.contract-manifest.json` `typescriptGeneratedContractSha256` == `unityGeneratedContractSha256`; `cf09-p09-w5b.json` `generatedContractSha256`; git blob `f1c5dcd855cd…` in both repos). The byte-identity claim itself is TRUE (`cmp` identical; `shasum` equal). Blocks because the seal's identity table must carry the exact generated-consumer identity (order §10); replace the number.

**NON-BLOCKING** (N1–N8 are required text corrections; N9–N11 are notes)

N1. "every step a wire command" / "driven through the bridge alone" (§5, §2 W5) is overstated. `tests/bridge-p09a-w5-bare-lot-first-film.test.ts:24-43` founds the studio with core `applyActions` (6× `signContract`, `foundStudio`, three `activate*`) before the `BridgeSession` exists, although `signContract`/`foundStudio` are wire intents (`bridge/session.ts:523,578`); and the loop's decisions read core state (`session.gameState` → `foundingPhaseOf`, `state.operations.facilities`, lines 71-83) rather than the snapshot. Everything that mutates after founding IS `session.quote`/`session.command` (46 commands), the next intent each week comes from `selectJourneyIntent` (`bridge/session.ts:204-247`, a pure map of `journey.next.kind` to an available intent — no outcome peeking), cash reconciles to `TUNING.INITIAL_CASH` (line 95), `minCash > 0`, five ledger kinds present, no `fixture|adjust` notes. Say "every command after founding is a wire command; founding uses the same core actions as the W0 gate; decisions read core state".

N2. §23 required-fixture accounting is missing; two rows need explicit disposition. Fixture 9 "P08 history exactness" is covered only by s10's three assertions (`StudioPostReleaseOracleRunner.cs:632-634`: `films.Length==1 && historyRecorded`, `recordingStartedWeek==0`, a `history-row-prod-*` element) plus the TS `studioFounded`/`filmReleased` rows — no construction milestone rows exist because REQ-040 is not built; state that. Fixture 4 "each material invalid placement family": the oracle shows two families (`notOwned` ×2, `offLot`; 14 assertions); `tests/bridge-p09a-w1-placement-quote.test.ts` rides 9 of 12 on the wire (offLot, groundReserved, noRoadAccess, terrainUnbuildable, occupied, clearanceRing, requirementsUnmet, instanceLimit; unknownBlueprint as a draft refusal) with `expectDirectRead` proving verbatim engine read; `notOwned`, `seversLot`, `insufficientFunds` have core tests only (`tests/property-state-v13.test.ts`, `tests/blueprint-requirements.test.ts`, `tests/facility-move-demolish.test.ts`). REQ-016 "one fixture per blocker" holds in core, not on the wire for those three; say which.

N3. REQ-031 (Gate/Administration cannot be demolished): enforcement is structural only — `demolishFacility` (`src/core/placement.ts:1054`) addresses `placement.facilities`; Gate/Admin are `property.structures` (`lot.ts:378-395`); `verb:'demolish'` is refused by the validator (W1 R7). No test attempts to demolish `gate`/`admin`; the matrix's proof column says "Protected fixture". §3 already says "structurally"; add "no explicit protected fixture".

N4. REQ-022 (completion does not move the camera / auto-open): no runtime observation of a completion transition exists — all 12 scenarios are static checkpoints with `mutationsSubmitted 0`; `p09-same-week-completion` asserts only bodies operational + "BUILD chip quiet" (`:604-606`). The claim rests on code (growth presenter makes no camera call; the notice is an IMGUI box that withdraws after `CompletionNoticeSeconds`, `StudioLotGrowthPresentation.cs:513-531`) and the EditMode transition test (`StudioP09AGrowthPresentationTests.cs:211-246`: same GameObject/SelectableEntity survives, notice text). The `Time.realtimeSinceStartup` timer at `:280/:515` only fades that notice; progress is wire-only (REQ-018 holds; `Update()` at `:491` animates crew markers from `Time.unscaledTime`). Say "proven by code + EditMode, not by a live oracle transition".

N5. Stale fixture pair committed: `ui/e2e/p09-visual-oracle-v1/s11-p09-endowed-build.{checkpoint,save}.json` (W4 era, schema id `eb95add0…`) remains beside `s12-p09-endowed-build.*` (`18de162d…`); `git ls-files` = 27 files = manifest + 13 pairs, while §4 says "12 fixtures s1–s12". Unused (`manifest.json` lists 12; `Tools/p09-run-visual-oracle.sh:48` binds `s$ORD-$SCENARIO`; run-7 endowed-build bound s12) but it is the order-§10 "twelve versus thirteen" ambiguity. Remove or document.

N6. Candidate docs are stale vs the committed docs: `candidate/docs/P09-TECHNICAL-CHECKPOINT.md:22` still reads `⟨CANDIDATE_PATH⟩`; `candidate/docs/P08-P10-AUTONOMOUS-STACK-HANDOFF.md` CURRENT STATE is the C3 (P08) table. Evidence, player, engine, fixtures in the candidate are correct (exe `3a40670f…`, `engine.mjs` `5185e3a2…`).

N7. §7 "a new site can sit visually close to authored bodies" (endowed lot) understates the capture: `p09-endowed-build-endowed-with-site.png` shows the Craft Services Annex site straddling the authored road south of the Post building. Say "can overlap authored roads/aprons".

N8. §7 discloses mirrored nameplates; the world ghost's reason label is mirrored/upside-down too from the management camera (`invalid-preview-not-owned.png`: "THIS GROUND IS NOT OWNED"; `valid-preview.png`: "VALID SITE"). REQ-015 "reason text" is readable from the dock, not the ghost, at that camera. Add to §7.

N9. Unity EditMode 847/847 was verified from the candidate XML only (`editmode-a8f4c1aa.xml`: `total="847" passed="847" failed="0" skipped="0"`, start 12:27:27Z, i.e. before the 12:27:46Z build; the seven named P09 classes carry 11/8/8/9/4/2/4 = 46 test-cases). I did not re-run batchmode, to avoid CPU contention with the queued owner-idle HID gate. NOT INDEPENDENTLY RE-RUN HERE.

N10. `scripts/p09-solvency-preflight.mts` models "all four committed at founding while cash allows", which the engine's founding-phase law forbids (allowance 1 while the office rises); §3.1.5 already explains the week-25 vs week-37 gap. Fine as a floor; the preflight's own caveat line should say its sequencing is not the engine's.

N11. The builder's EditorBuildSettings lists only `Assets/Scenes/SampleScene.unity` (6 objects, no prefab instance) while the captures show the `StudioLot.unity` hierarchy; how the lot reaches the player is not visible from the repo alone. NOT VERIFIABLE HERE without opening the editor; note it in the build record.

**VERIFIED TRUE (measured)**
- Ladder: TS 15 commits `2753e18b..74b1acdc`, Unity 15 commits `c4c65db4..79c5fd66`; `git log --merges` empty in both; local == remote for both WIP branches; `campaign/living-lot-ts` = `2753e18ba8fb5f65b936c22cde9531646fecc6cd`, TS `main` = `c902a704eb948cc576083d0973c8c23e59937dc1` (ancestor of the base), `campaign/living-lot-client` = `c4c65db464ef9abcf3bdcc088f5c8a47cc9081b6`; Unity `origin` has no `main`; `FINAL_DOCS_SHA` `72ca8e79` present.
- Full TS floor at HEAD (clean tree): **370 files / 4979 passed / 5 skipped (4984)**, 125.7 s, exit 0 — identical to the claim and to `candidate/evidence/ts-floor/floor-fee206fa.log`; the 5 skips are pre-existing `ui/src/test/contracts/audio-service.contract.test.ts`; all five P09 test files executed inside the floor (vitest `include: tests/**/*.test.ts`; `tsconfig.bridge.json` includes `tests/bridge*.test.ts`). `typecheck`, `typecheck:bridge`, `check:bridge-contract`, `check:bridge-contract:fixtures` all clean.
- Focused P09 tests: 5 files / 31 tests passed; printed lines `[p09 gate] release week 37; cash floor 7888869; final cash 9778380.03; weeks 42` and `[p09 bridge journey] released week 37; commands 46; cash floor 8864638; final cash 9374658`.
- Solvency preflight re-run: VERDICT SOLVENT, floor $6,713,185 (week 23), payroll $24,440/wk, overhead $24,000/wk, release week 25 — matches §5; the envelope is stated as "prototype evidence only / PROVISIONAL WIP tuning".
- Schema id `sha256:18de162d…` in `StudioBridgeDtos.Generated.cs` (both repos, byte-identical), manifest, sidecars; prior ids `85a6d125…`, `c9dad9f3…`, `eb95add0…` in `SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS`. CF-09 `cf09-p09-w5b.json`: PASS at TS `fee206fa` × Unity `a8f4c1aa`.
- Oracle run 7 (`Evidence/P09-Oracle/*-20260905T1227..1229Z`): 12/12 `status:complete`, **137 assertions, 0 failed, 0 mutations, 26 PNG captures**, every `build-manifest.json` exe `3a40670f…`, Assembly-CSharp `693f85f9…`, Unity `a8f4c1aa` `dirty:false`, TS `fee206fa` `dirty:false`, engine `5185e3a2…`; `p09-office-rising-…122816Z` `playerExit:255` with a complete 14/14 sidecar; re-run `…123314Z` `playerExit:0`, 14/14, 0 mutations. Viewports (`…1229..1233Z`): 12/12, **222 assertions, 0 failed, 36 captures**, PNGs exactly 1280×800 / 1720×1046 / 1440×900 with `-screen-fullscreen 1` on the 1440×900 runs. `app.log`/`engine.log` of all 13 run-7 directories: 0 hits for Exception/NullReference/error.
- Captures agree with the claims they are cited for: `sets-house-set-preview.png` shows "House Set · THE SHOP SAYS Ready to commission · Soundstage 3 · $150,000 · Week 33 (3 weeks) · Quality 45 · Cash after $12,958,270 · COMMISSION HOUSE SET — $150,000"; the chooser lists four yards each with a "BUILD HERE" button (`StudioBuildWorkspace.cs:886`); invalid previews show "NOT A VALID SITE" disabled with the §26.1 headlines; the multi-site office body opens Development ("Capacity 0/2 · Commission a screenplay"); first-film capture shows office/scenery/stage/post bodies, Gate and Administration in place, Films tab "Ashes of Impresario · Released Week 36"; no Pause overlay beyond the normal paused clock band.
- Laws: no `Math.random`/`Date.now`/`.skip`/`it.todo`/`.only`/TODO added in P09 TS source; no `UnityEngine.Random`/coroutine-driven progress in Unity P09 source; no roads editor, land acquisition, utilities, landscaping, decay/maintenance or builder assignment in either diff; no Unity route for repair/strike/move/demolish exists (grep empty), `CommitSetCommissionQuote` exists (`StudioWorkspaceHost.cs:705`) with `CommitSetEnabled` pinned in `StudioP09AW5SetsRouteTests.cs:95-98`; the catalogue is `FACILITY_BLUEPRINTS.map(...)` (`placement.ts:2221`) and Unity `Ordered()` never hides a row (`StudioBuildWorkspaceContracts.cs:125-138`); unknown requirement kinds fail closed (`blueprintRequirements.ts:123-127,171-173`); stale-revision refusal, quote retirement on any accepted command, replay refusal and commit revalidation proven (`W1 R4/R5`, `session.ts:1394,1432-1447`); `FindObjectsByType` runs per snapshot (`ApplyProjection`) and per Locate, not per frame; no "Builder" wording anywhere in Unity P09 runtime (only the pre-existing `presentation-builder-*` stableIds in the accepted scene).
- Owner profile `bridge-runtime-v1.json` sha256 `d949003e1874406170bfd3e7c8f4c6dc2dc92d24bb125376c435cdf21eec8b4b`, mode `r--------`, mtime Sep 1 21:46:56 — unchanged; no P09 script/test/harness names the baseline or `~/Library/Application Support` for writing (the P06/P07/P08 journey scripts read it and assert its hash; the HID launcher binds a private `RUNTIME` dir and the exe by sha, `p09-run-hid-build.sh:24-26,60-66,109-110`).
