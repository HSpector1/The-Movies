# P10 HOSTILE REVIEW — cross-stack person route (verbatim confirmed + refuted)

**Adversarial workflow** (7 attack dimensions → per-finding adversarial verify). 26 raw findings → **15 confirmed / 11 refuted**. Reviewed pair: Unity `6494cef` × TS `396d08c` × engine `3b9e3432` (the pre-remedy state). Remedies landed at Unity `68d20c1` × TS `af8c19c`.

## Confirmed findings (severity, dimension, claim, disposition)

### C1 · MAJOR · hidden-info
**Attention-tier casing mismatch: wire sends lowercase, four C# sites compare UPPERCASE — 'Attention first' sort and all attention colour cues are dead in production, and tests mask it**

- Claim: The bridge delivers person attention tiers as lowercase strings ('blocking' / 'decision' / 'attention' / 'info'), but four Unity runtime sites compare them with StringComparison.Ordinal against UPPERCASE literals, so every real row falls through: the Roster 'Attention first' sort collapses to plain OVR-descending (all rows get AttentionRank 0) and the attention colour/badge classes ('blocked' / 'attention') are never applied on any roster row, inspector card, or profile status.
- Evidence: Wire is lowercase: bridge/schema/bridge-schema.ts StudioRosterRowSnapshot.attentionTier enum = ['info','attention','decision','blocking'] and attentionTierEnum(); generated constants confirm it — Assets/Studio/Runtime/Data/Generated/StudioBridgeDtos.Generated.cs:723-725 and :1172-1174 ('attention'/'decision'/'blocking'). Uppercase comparisons: Assets/Studio/Runtime/Infrastructure/StudioRosterContracts.cs:112-115 (AttentionRank switch on "BLOCKING"/"DECISION"/"ATTENTION"/"INFO" -> _ => 0); Assets/Studio/Runtime/Presentation/UI/StudioRosterWorkspace.cs:243-244; Assets/Studio/Runtime/Presentation/UI/StudioPersonInspectorCard.cs:141-142; Assets/Studio/Runtime/Presentation/UI/StudioProfileWorkspace.cs:183-184 — all string.Equals(..., "BLOCKING"/"DECISION"/"ATTENTION", StringComparison.Ordinal). The test hides the bug by seeding uppercase fixtures that never occur on the wire: Assets/Studio/Tests/EditMode/StudioP10AW3RosterContractsTests.cs:31 and :34 build rows with "DECISION" and "ATTENTION", so AttentionRank matches and the assertion at :76-77 passes.
- Verify: real=True adj-sev=MAJOR — Reproduces exactly in the production wire→C# path. Wire emits lowercase tiers (bridge/people.ts decideAttention returns 'blocking'/'attention'/'decision'/'info'; people.ts:705 attentionTier=p.attention.tier; schema enum lowercase at bridge-schema.ts:1614; generated C# constants + canonical schema JSON all lowercase). Four C# sites compare with StringComparison.Ordinal against UPPERCASE literals: S

### C2 · MAJOR · requirement-fulfilment
**Roster→Profile route (OPEN PROFILE footer) is not on-screen in the oracle's own 1440×900 capture — passes assertions, fails the journey**

- Claim: The Roster's selection footer — the ONLY affordance that carries OPEN PROFILE from the Roster to a Profile — is not visible in the packaged-player oracle frame, so the depicted Roster→Profile journey is proven only at the element level, not as something a player can actually see or click.
- Evidence: Roster list ScrollView is built with classes ps-production-list + roster-list (StudioRosterWorkspace.cs:108-110); .roster-list (StudioPeopleWorkspace.uss:133-135) sets only flex-grow:1 and, unlike its working scrolling sibling .ps-production-detail which sets BOTH flex-grow:1 AND min-height:0 (StudioProductionWorkspace.uss:103-107), omits min-height:0, while inheriting .ps-production-list{flex-shrink:0;width:30%} (StudioProductionWorkspace.uss:47-51) meant for the fixed side-column of the row-layout panels; the panel is overflow:hidden (StudioProductionWorkspace.uss:7-19) docked top=90/bottom=170 (StudioWorkspaceHost.cs:1594-1600), so the un-capped list overflows and the trailing flex-shrink:0 footer is pushed past the clip. The oracle PNG Evidence/P10-Oracle-Conv/p10-person-inspector-*/p10-person-inspector-roster-selected-person.png shows the list running to the panel bottom with NO footer and no visible selection (the selected person Miriam Grimaldi is OVR 62, below the fold). The passing assertion checks only enabledSelf: WorkspaceEnabled = enabledSelf (StudioPostReleaseOracleRunner.cs:2245), asserted at :451; and the runner never clicks a row — it injects host.RosterContext.SelectedTalentId directly then rebinds (CaptureRosterWorkspace, StudioPostReleaseOracleRunner.cs ~2093-2098).
- Verify: real=True adj-sev=MAJOR — CONFIRMED against source and the oracle's own PNG. The roster ScrollView (StudioRosterWorkspace.cs:108-110) carries ps-production-list (flex-shrink:0; width:30%; StudioProductionWorkspace.uss:47-51) + roster-list (flex-grow:1 only; StudioPeopleWorkspace.uss:133-135), omitting the min-height:0 that its working sibling .ps-production-detail sets (StudioProductionWorkspace.uss:103-107). In the column

### C3 · MAJOR · roster-scale-state
**attentionTier case mismatch: wire is lowercase, C# tests UPPERCASE — "Attention first" sort is inert and tier colour never renders (roster + card + profile)**

- Claim: The TS bridge emits attentionTier as lowercase ('blocking'|'decision'|'attention'|'info'), but every C# consumer compares it against UPPERCASE string literals, so the "Attention first" sort ranks every row 0 (collapsing silently to Overall-highest order) and the blocking/attention colour class never attaches on the roster row, the world inspector card, or the Profile.
- Evidence: Wire lowercase: /Users/bruce/The Movies - P08-P10 Stack TS/bridge/people.ts:128 (type = 'info'|'attention'|'decision'|'blocking'), :638/:641/:647/:654/:661 (literal returns), :705 (attentionTier: p.attention.tier). C# uppercase compares: StudioRosterContracts.cs:110-117 AttentionRank switch on "BLOCKING"/"DECISION"/"ATTENTION"/"INFO" (used by the AttentionFirst sort at :99) — every lowercase tier hits the `_ => 0` default, so OrderByDescending(rank) is a no-op and the sort degrades to .ThenByDescending(ovr). Roster row colour: StudioRosterWorkspace.cs:243-244 string.Equals(row.attentionTier,"BLOCKING"/"DECISION"/"ATTENTION",Ordinal) never matches. Same defect in StudioPersonInspectorCard.cs:141-142 and StudioProfileWorkspace.cs:183-184, both fed by StudioPersonInspectorContracts.cs:83 AttentionTier(p)=>p?.attention?.tier (raw lowercase wire, not normalized). The unit test masks it: StudioP10AW3RosterContractsTests.cs:29 & :32 build fixtures with UPPERCASE "DECISION"/"ATTENTION", so the AttentionFirst assertion at :76 passes against casing the real wire never sends.
- Verify: real=True adj-sev=MAJOR — CONFIRMED. The wire emits attentionTier in lowercase and every C# consumer compares it case-sensitively against UPPERCASE literals, so the mismatch is real end-to-end. Wire side (lowercase) is quadruple-confirmed: people.ts:128 type union 'info'|'attention'|'decision'|'blocking', the decideAttention literal returns (~:638-661), :705 attentionTier: p.attention.tier, PLUS the generated canonical sch

### C4 · MAJOR · presence-seating
**Roster footer LOCATE is still a dead control for on-lot-but-unseated people (residual F1 gap, unfixed on this surface)**

- Claim: The Roster footer enables LOCATE purely from the wire's canLocate with no world-body reconciliation, so for a person who is on-lot per the authority but whom the scene cannot seat, the button is enabled yet clicking it does nothing — the exact dead Locate the Profile, History, and inspector card no longer offer.
- Evidence: StudioRosterWorkspace.cs:313-315 gates the footer LOCATE on `anyRow.canLocate` alone (`var canLocate = anyRow.canLocate; footerLocate.SetEnabled(canLocate);`), and :329 fires it on the same flag (`if (row != null && row.canLocate) locateFor(id);`). The Roster's Configure signature (StudioRosterWorkspace.cs:141 `Configure(Action requestClose, Action<string> openProfile, Action<string> locate)`) has NO body-resolver parameter — unlike the fixed Profile, whose Configure takes `Func<string,string> resolveBody` (StudioProfileWorkspace.cs:105) and ANDs it in (StudioProfileWorkspace.cs:147-149 `bodyOnView = wireOnLot && !string.IsNullOrEmpty(resolveBodyFor(profile.talentId))`). The host wires the roster's locate to LocatePersonFromProfile with no resolver (StudioWorkspaceHost.cs:1593); LocatePersonFromProfile (StudioWorkspaceHost.cs:727-732) calls ResolveLocateStableId (StudioWorkspaceHost.cs:1741-1745), which returns null when TryGetAuthoritativePersonStableId finds no seated body, so the handler early-returns — a silent no-op. That the scene cannot seat these people is a hard fact, not capacity churn: StudioLot.unity authors 10 authoritative person bodies, all in zones stage-a (4), stage-b (4) and scenery (2) — ZERO in the casting or writers zones — while ResolveZoneId (StudioBridgePresentation.cs:1112-1119) maps facility-development-casting to the 'casting' zone, so TakePreferredAuthoritativeSlot always returns null for them. Running the sealed engine (bundle 3b9e3432) against the exact oracle fixture ui/e2e/p10-visual-oracle-v1/s1-p10-person-inspector.checkpoint.json yields 6 on-lot people all with canLocate=true, of which 5 sit at facility-development-casting and only t-cra-04 sits at facility-scenery-shop (the one that seats).
- Verify: real=True adj-sev=MAJOR — Confirmed on every load-bearing claim. The Roster footer LOCATE is enabled purely from the wire's canLocate with no world-body reconciliation (StudioRosterWorkspace.cs:313,329; Configure at :141 has no resolveBody param), while the fixed Profile ANDs in a body resolver (StudioProfileWorkspace.cs:105,147-149) wired to ResolveLocateStableId (host :1581); the Roster's locate is wired to LocatePersonF

### C5 · MINOR · deep-links
**Roster footer LOCATE is dishonest — F1 fix never reached it (enabled control, silent no-op)**

- Claim: The Roster selection-footer LOCATE ON LOT button is enabled purely from the wire's canLocate flag and never gated on a resolvable world body, so for an on-lot-but-unseated person it shows enabled and clicking it does nothing — the exact F1 dishonesty the Profile fix closed, left uncorrected on the roster footer the review asked to verify.
- Evidence: StudioRosterWorkspace.cs:313-315 sets footerLocate.SetEnabled(anyRow.canLocate) with EnableInClassList on the raw wire flag; :323-330 LocateSelected() fires locateFor(id) whenever row.canLocate, no body check; :141 Configure(Action,Action<string>,Action<string>) takes NO resolveBody seam and there is no locate-reason label on the footer (footerNotice only covers off-books/hidden-by-filter). Host wires it at StudioWorkspaceHost.cs:1593 rosterWorkspace.Configure(RequestCloseWorkspace, OpenProfileFromRoster, LocatePersonFromProfile); the click lands in StudioWorkspaceHost.cs:727-732 LocatePersonFromProfile → ResolveLocateStableId → (StudioBridgePresentation.cs:64-71 TryGetAuthoritativePersonStableId reads personSlots) returns null for an unseated person → early return, SuspendForLocate never called → silent no-op with no reason shown. Contrast the honest gold standard the Profile now mirrors: StudioProfileWorkspace.cs:147-149 (bodyOnView = wireOnLot && resolveBodyFor(talentId) non-empty) and StudioHistoryWorkspaceContracts.cs:373-376 DecideLocate (requires currentLocation AND non-empty resolvedStableId, else disabled with NoBodyRightNowReason). Reachability confirmed: people.ts:583 sets canLocate:true for any authority-present person (presence!=null), independent of scene seating; scratchpad p10/FINDINGS.md F1 records that casting/writers-zone roster people report canLocate=true at week 2 while the scene seats no selectable body for them.
- Verify: real=True adj-sev=MINOR — Every code citation verified exact. StudioRosterWorkspace.cs:313-314 enables footerLocate purely from anyRow.canLocate (raw wire flag) with no body resolution; :323-329 LocateSelected() calls locateFor(id) whenever row.canLocate; :141 Configure(Action,Action<string>,Action<string>) has no resolveBody seam and the footer has no locate-reason label (footerNotice only covers off-books/hidden-by-filte

### C6 · MINOR · requirement-fulfilment
**Residual F1: Roster footer LOCATE gates on the wire canLocate alone and silently no-ops for an on-lot-but-unseated person**

- Claim: The Profile and inspector card were fixed to withhold Locate when no world body resolves, but the Roster footer still enables LOCATE ON LOT purely on the wire's canLocate flag, with no body-resolution gate and no 'no body to locate right now' reason — so for an on-lot-but-unseated person the button is offered and clicking it does nothing, violating the same 'a visible control must never sit inert' law the card/Profile honor.
- Evidence: StudioRosterWorkspace.cs:313-316 sets footerLocate.SetEnabled(anyRow.canLocate) with only a 'dropped'/'hidden-by-filter' notice path (no locate reason); LocateSelected fires locateFor(id) whenever canLocate (StudioRosterWorkspace.cs:323-330); the host wires that seam to LocatePersonFromProfile (StudioWorkspaceHost.cs:1593), which resolves via ResolveLocateStableId and early-returns on null (StudioWorkspaceHost.cs:727-732 → 1741-1746, fail-closed TryGetAuthoritativePersonStableId). The TS wire sets canLocate:true for ANY on-lot person regardless of Unity seatability (bridge/people.ts:583). Contrast the correct Profile gate: StudioProfileWorkspace.cs:147-164 layers bodyOnView = wireOnLot && resolveBodyFor(id)!=null and shows 'On the lot this week; no body to locate right now.' — proven by StudioP10AW1bPersonRouteTests.cs:154-169.
- Verify: real=True adj-sev=MINOR — CONFIRMED. Every citation reproduces exactly. The Roster footer enables LOCATE ON LOT on the wire flag alone (StudioRosterWorkspace.cs:313-315: `var canLocate = anyRow.canLocate; footerLocate.SetEnabled(canLocate)`) with only "dropped"/"hidden-by-filter" notice branches and no "no body" reason path, and LocateSelected fires `locateFor(id)` whenever `row.canLocate` (line 329). The roster's `Configu

### C7 · MINOR · requirement-fulfilment
**Documented scroll-state preservation across Profile↔Back is declared but never implemented (Roster and Profile contexts)**

- Claim: Both retained contexts declare and document a scroll-offset field as preserved across Profile↔Back / snapshot rebind, but neither field is ever read or written (only reset to zero), so scroll position is not actually preserved — unlike the History workspace which does it correctly.
- Evidence: StudioRosterWorkspaceContext.ScrollOffset (StudioRosterWorkspaceContext.cs:18, doc claims '...the scroll offset, so that Profile → Back ... return the reader to exactly the list state') and StudioProfileWorkspaceContext.DetailScrollOffset (StudioProfileWorkspaceContext.cs:28) are only ever reset (StudioRosterWorkspace.cs:215 resets listView.scrollOffset to 0; Open() sets DetailScrollOffset=0 at StudioProfileWorkspaceContext.cs:35); a repo-wide grep finds no read/write of either. History does it properly: StudioHistoryWorkspace.cs:236-237 capture and :881/:886 restore. OpenRoster's own doc claims 'Retains filter/sort/search/selection/scroll' (StudioWorkspaceHost.cs:632). Load-bearing state (filter/sort/search/selection/origin) IS preserved via the context; scroll is the documented-but-missing piece.
- Verify: real=True adj-sev=MINOR — Confirmed by direct code reads. StudioRosterWorkspaceContext.ScrollOffset (cs:18) is declared and its doc (lines 8-9) explicitly promises Profile→Back/snapshot-refresh returns the reader to the exact list state; a repo-wide grep shows the field is never read or written (dead). StudioProfileWorkspaceContext.DetailScrollOffset (cs:28, doc line 27) is only ever set to 0f in Open() (cs:35) — never cap

### C8 · MINOR · roster-scale-state
**Retained scroll is never implemented: ScrollOffset context field is dead, and every rebind does listView.Clear()+rebuild — scroll jumps to top on each snapshot and on Profile→Back**

- Claim: StudioRosterWorkspaceContext.ScrollOffset is documented as retained list state but is never read or written anywhere; combined with the unconditional listView.Clear() on every Rerender, the reader's scroll position is lost on every snapshot rebind while the Roster is open and on every Profile round-trip peel-back, contradicting the design-§6 promise to "return the reader to exactly the list state they left."
- Evidence: StudioRosterWorkspaceContext.cs:18 declares `public float ScrollOffset;` with a doc-comment (lines 8-13) promising scroll retention; a repo-wide grep shows it is referenced nowhere else (only the Casting/Profile contexts wire their own offsets). The workspace touches listView.scrollOffset only to ZERO it: StudioRosterWorkspace.cs:215 `if (resetScroll) listView.scrollOffset = (0,0)`, and never captures/restores context.ScrollOffset. RenderList() unconditionally tears the list down: StudioRosterWorkspace.cs:220 `listView.Clear();` then re-adds every row (:222-223), called from Rerender (:213) on every Bind (:157) and on every OnSnapshotApplied while rosterRouteOpen (StudioWorkspaceHost.cs:2147-2150). Peel-back path StudioWorkspaceHost.cs:1025 OpenRoster→BindRosterWorkspace→Rerender→Clear() rebuilds the list from empty, dropping any prior offset.
- Verify: real=True adj-sev=MINOR — CONFIRMED against source. (1) StudioRosterWorkspaceContext.cs:18 `public float ScrollOffset;` sits under a doc-comment (lines 5-12) promising Profile→Back and snapshot refreshes "return the reader to exactly the list state they left (design §6)", yet a repo-wide grep shows the Roster's ScrollOffset is referenced nowhere else — never read, never written (dead field; other hits are Casting's separat

### C9 · MINOR · roster-scale-state
**Whole 60+ row list torn down and rebuilt on every snapshot with no reconciliation (churn at scale)**

- Claim: Every applied snapshot delta (and every keystroke, chip toggle, sort cycle, and row selection) rebuilds the entire roster list — listView.Clear() followed by constructing a fresh multi-element Button per row — with no diffing or element reuse, so at 60+ people the list churns continuously during a time-advance while the Roster is open.
- Evidence: StudioRosterWorkspace.cs:218-224 RenderList clears and rebuilds all rows; RosterRow (:226-265) builds a Button + primary VisualElement + 2-3 Cell labels + a meta Cell per person (~5-6 elements each). Rerender (:192-216) is the single render path, invoked from Bind (:157), SelectRow (:270), the search callback (:82), every filter chip (:166/:174/:180/:188), ToggleAttention (:334), CycleHorizon (:344), CycleSort (:356). Snapshot cadence: StudioBridgeClient.cs:1477 raises SnapshotApplied on each applied delta → StudioWorkspaceHost.cs:2147-2150 calls BindRosterWorkspace() (full rebuild) whenever rosterRouteOpen. No ListView virtualization or key-based reconciliation is used (a plain ScrollView, StudioRosterWorkspace.cs:108).
- Verify: real=True adj-sev=MINOR — All cited lines verify against source. RenderList (StudioRosterWorkspace.cs:218-224) does listView.Clear() then unconditionally rebuilds every visible RosterRow (226-265: Button + primary VE + name/ovr/optional-attn/meta Cells, ~5-6 elements each) with no diffing or dirty-check. Rerender (192-216) is the single render path, invoked from Bind(157), SelectRow(270), search callback(82), all four filt

### C10 · MINOR · presence-seating
**Oracle roster capture never exercises the footer LOCATE for an unseated person, so the gap ships uncaught**

- Claim: The p10-person-inspector oracle's Roster step deliberately selects t-cra-04, the one on-lot person the scene can seat, and asserts nothing about the footer LOCATE state, so the dead-Locate gap on the Roster footer is invisible to the packaged-player oracle that otherwise anchors P10 honesty.
- Evidence: StudioPostReleaseOracleRunner.cs:447-451 — RosterWorkspace("CAM-MGMT-POST-V1", "t-cra-04", "roster-selected-person", ...) checks only WorkspaceCount("roster-row-")>5, roster-footer-name starts with 'Miriam Grimaldi', and WorkspaceEnabled("roster-footer-profile"). No check of roster-footer-locate enabled/disabled state, and the selected id is the single seated person. The inspector sidecar (Evidence/P10-Oracle-Conv/p10-person-inspector-*/p10-person-inspector-sidecar.json) confirms the roster assertion set stops at 'OPEN PROFILE is offered for the roster selection'. The Profile off-lot case IS covered (t-wri-00), and History reconciles, but the on-lot-unseated Roster case is a coverage hole.
- Verify: real=True adj-sev=MINOR — CONFIRMED. Both halves of the finding check out against the actual code, evidence, and the team's own prior investigation.

1) Oracle coverage gap is real. StudioPostReleaseOracleRunner.cs:447-451 — the Roster step is RosterWorkspace("CAM-MGMT-POST-V1", "t-cra-04", "roster-selected-person", ...) and asserts only: WorkspaceCount("roster-row-")>5, roster-footer-name starts with "Miriam Grimaldi", an

### C11 · NOTE · identity
**Profile->History link for a credit-less person lands on the generic empty tab, not that person's own 'no records' history**

- Claim: OpenHistoryFromProfile promises (comment) that 'a person with no captured records still opens their history honestly (History says there are none) — the link is never silently dropped', but history.people only contains persons who hold at least one captured film credit, so a credit-less subject id is cleared to null on Bind and the panel shows the generic 'Select a person to read their credits.' rather than the named subject's empty history.
- Evidence: Claim comment: /Users/bruce/The Movies - P08-P10 Stack Unity/Assets/Studio/Runtime/Presentation/UI/StudioWorkspaceHost.cs:678. Membership: /Users/bruce/The Movies - P08-P10 Stack TS/bridge/history.ts:416-438 (people map built only from released-film participants). Clear-on-missing: StudioHistoryWorkspace.cs:187-188 (`if (selected != null && !RowExists(tab, selected)) context.Select(tab, null);`). Generic empty copy: StudioHistoryWorkspace.cs:628-630.
- Verify: real=True adj-sev=NOTE — Reproduces as described. Verified in code: the Profile's STUDIO HISTORY button is always shown for any bound profile (StudioProfileWorkspace.cs:150, ungated on credits); it calls OpenHistoryFromProfile -> OpenStudioHistory("people", talentId) (StudioWorkspaceHost.cs:680, comment 673-677 promising "History says there are none — the link is never silently dropped"). A genuine set difference exists: 

### C12 · NOTE · deep-links
**Deep Profile↔History / Roster→Profile→History chains collapse to the world on Back (breadcrumb lost, not trapped)**

- Claim: The host keeps only single-level return linkage, so ping-ponging Profile→History→Profile discards the earlier origin: Back returns to the immediate origin correctly and the player is never trapped, but a multi-hop chain unwinds to the world instead of back through the original Roster/Profile it came from.
- Evidence: StudioWorkspaceHost.cs:990-1050 CloseWorkspace peels exactly one relationship (historyReturnsToProfile / releaseResultReturnsTo* / profile origin) then falls through to a full close to the world — it never no-ops, so no strand. But OpenProfile resets historyReturnsToProfile=false (StudioWorkspaceHost.cs:615) and OpenStudioHistory resets it too (:749), so a second hop overwrites the first. Trace: Roster→OpenProfile(X,Roster)→OpenHistoryFromProfile(X) (:680-687 sets historyReturnsToProfile=true, profileReturnOrigin=Roster)→ from History OpenProfileFromHistory(Y) → OpenProfile(Y,History) clears the History→Profile linkage → Back gives Profile(Y)→History→world, dropping Profile(X) and the Roster origin.
- Verify: real=True adj-sev=NOTE — Reproduces exactly as claimed against the actual code. The host keeps only single-level return state: one historyReturnsToProfile flag plus one profileReturnTalentId/profileReturnOrigin pair, with no stack. OpenProfile (StudioWorkspaceHost.cs:615) and OpenStudioHistory (:749) both hard-reset historyReturnsToProfile=false, so a second hop overwrites the first. Traced journey (all hops wired and rea

### C13 · NOTE · migration
**Stale 'current running schema' comment names a now-accepted prior id (01f15efc) as SCHEMA_ID**

- Claim: The SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS exclusion comment says the current running schema is 'SCHEMA_ID, sha256:01f15efc...', but the current schema is now ea5d645f and 01f15efc is itself an accepted prior (projection-v11) inside the very map the comment describes, creating a maintenance hazard.
- Evidence: bridge/runtime-checkpoint.ts:44-45 comment: 'excluding the current running schema (SCHEMA_ID, sha256:01f15efc...)'; but the map lists ['sha256:01f15efc8fc33fd810b051242857385ca23b5e1c775b357db1bfe5a70e907e1e','projection-v11'] as an accepted prior, and the real current SCHEMA_ID is sha256:ea5d645f (generated/unity/StudioBridgeDtos.Generated.cs:20). SCHEMA_ID is imported from bridge/protocol.ts:33 (schemaIdentity(BRIDGE_SCHEMA)), so it tracks ea5d645f, not the frozen string in the comment.
- Verify: real=True adj-sev=NOTE — Facts verified against the cited files. runtime-checkpoint.ts:44-45 states the current running schema is "(SCHEMA_ID, sha256:01f15efc...)", but the actual current SchemaId is sha256:ea5d645f (generated DTO line 20, ProjectionVersion 18), and 01f15efc is itself a listed accepted prior in the same map (line 90, projection-v11). SCHEMA_ID is a live computed constant (protocol.ts:33, schemaIdentity(BR

### C14 · NOTE · roster-scale-state
**Filtered header trailer reports whole-roster employed/attention totals, not the visible set**

- Claim: When a filter is active the counts label reads "{visible} of {total} shown · {employed} employed · {withAttention} need attention", but the employed and need-attention figures are the unfiltered whole-roster totals, so the trailing stats no longer describe the rows actually shown.
- Evidence: StudioRosterWorkspace.cs:205-211: `employed`/`attention` are built from roster.counts (the wire's global counts, people.ts:717) and appended unchanged to both the all-shown and the "X of Y shown" branches. The visible/total split is computed from visibleRows vs roster.rows.Length, but the counts trailer is never recomputed over visibleRows.
- Verify: real=True adj-sev=NOTE — Confirmed against exact cited code. StudioRosterWorkspace.cs:205-211 builds `employed`/`attention` from `roster.counts` and appends them verbatim to both the all-shown and the "X of Y shown" branches; only the visibleRows/total split reflects the filter. people.ts:711-719 confirms counts.employed/withAttention are computed with rows.filter() over the FULL projected roster at projection time, so th

### C15 · NOTE · presence-seating
**Wire canLocate over-promises: true for every on-lot person regardless of facility or seatability**

- Claim: canLocate on the bridge is a restatement of onLot, not a promise of a locatable body: buildPresence sets canLocate=true (and locateReason=null) for every person with a presence row, unconditionally — even when facilityId (p.site) is null — so a consumer that treats canLocate as a Locate affordance is trusting a fact the bridge cannot back (the bridge has no knowledge of Unity's authored-body capacity per zone). This is the root that makes the Roster's dead Locate possible; the honest surfaces defend against it by ANDing canLocate with the actually-resolved world body.
- Evidence: bridge/people.ts:577-584 — the presence-present branch returns `onLot: true, facilityId: p.site` (p.site may be null) with `canLocate: true, locateReason: null` unconditionally; the roster row copies it verbatim at people.ts:707 (`canLocate: p.presence.canLocate`). The correct reconciliation the Roster is missing is present in StudioProfileWorkspace.cs:147-163 (disables and says 'On the lot this week; no body to locate right now.') and in StudioHistoryWorkspaceContracts.cs:373 `DecideLocate(currentLocation, resolvedStableId)` used at StudioHistoryWorkspace.cs:803. A more honest wire name would be onLotThisWeek, with locatability decided Unity-side; alternatively canLocate should be gated on a resolved facility/seat. The inspector card also trusts wire canLocate (StudioPersonInspectorCard.cs:145 via StudioPersonInspectorContracts.cs:87-88) but is honest by construction — it can only attach to an already-seated selected body, so an unseated person can never surface it.
- Verify: real=True adj-sev=NOTE — Every factual claim verified true against both stacks. bridge/people.ts:577-584 sets canLocate=true/locateReason=null unconditionally for any person with a non-null presence row, independent of p.site; buildRoster copies canLocate verbatim at people.ts:707. The site==null premise is real and reachable, not hypothetical: src/core/presence.ts emits site:null for present people in two designed paths 

## Refuted findings (attacked, verified fail-safe / already-correct)

- **P10 information-visibility law holds: no hidden datum crosses the bridge or appears in a panel; projection is a fresh clone; F1 Profile-Locate fix confirmed** (hidden-info) — Verified every cited target against the exact code in both worktrees and the generated Unity DTOs; the finding is an accurate clean-bill NOTE with no actionable defect (real=false). (1) Fresh clone holds: bridge/people.ts buildProfile/buildCareer/buildRoster construct all wire objects as new literal
- **Release-result deep-link destination silently substitutes the first film instead of explaining absence** (identity) — The cited code is quoted accurately: StudioReleaseResultWorkspace.Bind (lines 119-123) does fall back to activeRows[0]/historyRows[0] when RowById(preferredResultId) is null, and the deep-link caller chain is exactly as described (StudioProfileWorkspace CareerRow -> openResultFor(id) -> OpenReleaseR
- **Named same-name/same-title attack surfaces verified fail-safe (roster sort, reverse talentId lookup, all person/film joins)** (identity) — Verified every cited join against the actual files in both worktrees; the finding is a self-declared fail-safe confirmation and describes NO defect. Roster sort terminates in .ThenBy(talentId, Ordinal) on all four branches (StudioRosterContracts.cs:90,97,102,106). The reverse lookup is a genuine 1:1
- **Film Result workspace silently falls back to the first result for an unknown deep-link id** (deep-links) — Code reading is accurate: StudioReleaseResultWorkspace.cs:119-123 does silently fall back to activeRows[0]/historyRows[0] for an unknown non-empty preferredResultId, contradicting both the visibility law and the Bind docstring's honest-empty claim (the honest else-branch exists at RenderDetail:189-1
- **Profile (and Roster) LOCATE gate is not re-decided per frame like History/Production** (deep-links) — Structural claims are accurate (StudioProfileWorkspace.cs:147-149 computes locateEnabled only in Bind; no MaintainLocateGates on Profile or Roster; host 199-214 re-gates only Production/History), but the CLAIMED defect cannot occur in the implemented journey. The failing journey requires a body to l
- **OpenReleaseResult does not reset releaseResultReturnsToProfile (flag-hygiene asymmetry)** (deep-links) — Code claims all verified accurate: OpenReleaseResult (StudioWorkspaceHost.cs:567-580) clears releaseResultReturnsToHistory (575) but not releaseResultReturnsToProfile, while OpenProfile (612-613), OpenRoster (640-641), and OpenStudioHistory (744-745) clear both; releaseResultReturnsToProfile is set 
- **CONFIRMED: projection-18 pair, prior-17 support, engine-sha binding, clean-build, and both migration/pairing attack vectors fail closed** (migration) — All six sub-claims reproduce exactly against source and both packaged-oracle evidence runs, so this is an accurate no-defect confirmation, not a genuine defect (real=false). Verified: (1) projection-18/ea5d645f pair — bridge-schema.ts PROJECTION_VERSION=18, both generated Unity DTOs ProjectionVersio
- **Build-pair integrity relies on manifest honesty plus the runtime backstop, not an independent harness cross-check of engine-schema vs player-schema** (migration) — All cited code and evidence check out exactly as described, but the finding is self-refuting as a defect and reproduces no failing player journey. The harness (Tools/p10-run-visual-oracle.sh:44-52) does only SHA-vs-manifest checks with no independent engine-schema/player-SchemaId cross-check — accur
- **Stale-build guard covers only Assets/**/*.cs and does not re-derive the manifest dirty flag** (migration) — The two code observations are literally accurate: Tools/p10-run-visual-oracle.sh:53 uses `find "$CLIENT_REPO/Assets" -name '*.cs' -newer "$EXE"` (matches only C# sources), and line 108 copies build-manifest.json into evidence with no assertion on unity.dirty/typescript.dirty; the exe sha256 is pinne
- **Roster view interactivity and Profile↔Back state retention are IMPLEMENTED-BUT-UNPROVEN end-to-end** (requirement-fulfilment) — All factual claims verified TRUE against the files: the W3 test covers only pure StudioRosterContracts; grep for StudioRosterWorkspace across Assets/Studio/Tests returns nothing (no view instantiation, chip/search/sort toggle, or row click); the oracle CaptureRosterWorkspace opens the roster at defa
- **Proven baseline (for the ledger): world reachability, exact-person open, F1-Profile fix, F6, and the TS visibility law all verified** (requirement-fulfilment) — This finding explicitly self-identifies as "code-only: no defect" — a positive ledger entry documenting the proven parts of the person route, not a defect claim. There is no failing player journey to reproduce, and the finding does not assert one. I verified every cited fact against the actual file 
## Disposition (remedies at Unity `68d20c1` × TS `af8c19c`)

**No BLOCKERs. 4 MAJOR + the actionable MINOR/NOTE findings fixed; the design-acceptable NOTEs documented; the 11 refuted findings confirm the law holds.**

- **MAJOR (attention-tier casing, C1/C3):** the wire's lowercase tiers are now matched case-insensitively at all four sites (`AttentionRank` upper-normalises; a shared `AttentionClass` replaces the inline uppercase checks in the roster row, inspector card, and profile status). The W3 test that masked it now uses lowercase fixtures + a dedicated case-insensitivity test. "Attention first" sort and the attention colour cues are live again. EditMode 873/873.
- **MAJOR (roster footer off-screen, C2):** `.roster-list` now caps (`min-height:0`, `width:100%`) so the selection footer with OPEN PROFILE stays on screen — confirmed in `Evidence/P10-Remedy/.../roster-selected-person.png` (footer visible with OPEN PROFILE + LOCATE).
- **MAJOR (roster footer LOCATE dead, C4):** the footer LOCATE now gates on the resolvable body (host passes `ResolveLocateStableId`), matching the Profile — no more dead control.
- **MINOR (retained scroll C7/C8; churn C9):** scroll is now captured into the retained context and restored after layout; the list rebuilds only when its rendered signature or the selection changes.
- **NOTE (stale schema comment C13; filtered header totals C14):** the checkpoint comment no longer hardcodes a now-prior schema hash; the filtered header no longer shows whole-roster employed/attention totals.

**Documented as acceptable (design, not defects):**
- **C11** — a Profile→History link for a person with no captured records opens the generic empty People tab; the person genuinely has no history record to show. Honest, not a fallback to the wrong subject.
- **C12** — deep Profile↔History / Roster→Profile→History chains peel ONE layer per Back/Esc (the sealed Esc grammar) and a very deep chain eventually returns to the world rather than replaying every hop; the player is never trapped.
- **C15** — the wire's `canLocate` is "claimed on the lot", not "has a seated body"; the TS engine has no knowledge of scene seating capacity, so this is correct wire semantics, and every P10 surface now gates its Locate on the actual resolvable body (F1 + C4). A future ruling could tighten `canLocate` itself.

**Refuted (11):** the information-visibility law holds (no hidden datum crosses or renders); same-name/same-title joins are fail-safe (exact id everywhere); the Film Result deep link does NOT silently substitute the first film; build-pair integrity (projection-18 pair, prior-17 support, engine-sha binding, clean-build dirty=false) is confirmed.

## Remedy verification (returned to the same reviewer)
A focused remedy-verification pass (5 agents over the fixed code at `68d20c1`/`af8c19c`) returned
**5/5 RESOLVED** — MAJOR-1 (casing), MAJOR-2 (footer), MAJOR-4 (footer Locate), MINOR (scroll/churn),
and the NOTEs — with 0 PARTIAL and 0 UNRESOLVED. No verdict-shopping: the same adversarial reviewer
that surfaced the findings confirmed the fixes.
