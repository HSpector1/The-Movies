# P08–P10 Autonomous Stack — Execution Handoff (live, append-only)

**Authorization:** `OPS-P08P10-20260905-01` · **Campaign policy:** `CAMPAIGNS_FROZEN_UNTIL_P08_P10_OWNER_ACCEPTANCE`
**Delta:** `docs/operations/OPS-P08P10-20260905-01-CURRENT-OPS-DELTA.md` · **Preflight:** `docs/engineering/CODEX-P08-P10-AUTONOMOUS-STACK-CURRENT-REFRESH.md`

## CURRENT STATE (rewritten at every checkpoint; history below is append-only)

| Item | Value |
|---|---|
| Package / wave | **P10A person route (W1b inspector + W2 Profile + W3 Roster + cross-stack person↔history) — TECHNICAL KEEP FOR AUTHORIZED READY SCOPE, hostile-reviewed, combined candidate READY**; projection 18; P09 core 9/9 retained + re-verified on the P10 player; P08 core KEEP retained. OWNER ACCEPTANCE PENDING. Next = owner playtest + acceptance decision; material contract actions remain dependency-blocked (no producer) |
| TS WIP `wip/p08-p10-autonomous-stack-01-ts` | worktree `/Users/bruce/The Movies - P08-P10 Stack TS`; remote `hspector-github`; product tip `af8c19c`; docs/fixtures tip `05139c1` |
| Unity WIP `wip/p08-p10-autonomous-stack-01-client` | worktree `/Users/bruce/The Movies - P08-P10 Stack Unity`; remote `origin`; tip `68d20c1` (hostile-review remedies). Final candidate player `1212fe63` (dirty=false). P09 sealed player `d41c0d4b` preserved at `~/Desktop/P09-Core-Technical-Candidate-fee206f-8f30d0e/` (untouched) |
| Save / protocol / projection | **V18** / 4 / **18** (schema id `sha256:ea5d645f…`; CF-09 PASS TS `9bc01ea` × Unity `26a543a1`, contract `6c26f13e`; P09 control remains projection 17 `18de162d`) |
| Accepted base (frozen) | TS `2753e18ba8fb5f65b936c22cde9531646fecc6cd` · Unity `c4c65db464ef9abcf3bdcc088f5c8a47cc9081b6` — both campaign branches and TS `main` (`c902a704…`) re-read unchanged on the remotes at C5; Unity `origin` has no `main` |
| `FINAL_DOCS_SHA` | `72ca8e797e5185a5dec13ac4c4311e391b8e96e3` |
| Owner durable profile | `d949003e…` unchanged (hash-checked at C5; P09 automation ran only on fixture checkpoints) |
| P08 seal record | `docs/campaigns/P08-TECHNICAL-CHECKPOINT.md`; hostile review verbatim `docs/campaigns/P08-HOSTILE-REVIEW-CORE.md`; candidate `~/Desktop/P08-Core-Technical-Candidate-8a23cb3-64dab80/` |
| P09 seal record | `docs/campaigns/P09-TECHNICAL-CHECKPOINT.md` (9/9); hostile review verbatim `docs/campaigns/P09-HOSTILE-REVIEW-CORE.md`; candidate `~/Desktop/P09-Core-Technical-Candidate-fee206f-8f30d0e/` (player `d41c0d4b`, sealed engine `5185e3a2`, 12 fixtures, docs, W7 oracle + viewports, EditMode `8f30d0ef`, the passing official HID `hid-20260905T150855Z` + the four prior failed HID runs, proof scripts) |
| Next concrete command | OWNER: play `~/Desktop/P10-Person-Route-Candidate-af8c19c-68d20c1/playtest.sh endowed` (and `firstfilm`, `barelot`). No further build step pending. If acceptance is granted, the promote-to-main discussion is SEPARATE and not part of this overnight order. If material contract actions (renew/release) are wanted, they need a new bridge command producer first |

## CHECKPOINT HISTORY (append-only)

### C0 — 2026-09-05 — publication + preflight
- Inputs verified: ZIP `887f4a57…`, execution pack `189075a0…`, manifest `cfa8eed4…`, patch `929c0187…`, 35 docs byte-identical.
- Published `docs/p08-p10-autonomous-stack-launch-01` @ `72ca8e797e5185a5dec13ac4c4311e391b8e96e3` (parent = accepted base); local == upstream == advertised.
- WIP pair created/pushed empty at the accepted pair and verified; TS WIP fast-forwarded to the docs commit; order + delta committed `5e01714866134552eb28ad6fb7753b598f5df123`; preflight refresh committed `bf85336c987b7bae8606099ca5e1e8d7661073bd`.
- Baseline floors at the WIP tip: TS 363 files / 4926 passed / 5 skipped; core+ui+bridge tsc clean; Unity EditMode 784/784 (fresh worktree import).
- No process, worktree, or profile belonging to another session was touched; the Owner's durable profile (`d949003e…`) is read-only to this program.

### C1 — 2026-09-05 — P08A Wave 0/1 (TypeScript) — COMPLETE at `15113a026213fdcb575d5905fa6a8d312d37fe74`
- NEW `src/core/studioHistory.ts` (additive `studioHistory` root, sink, significance classifier, exact-once append, deterministic routine fold, invariants, selectors).
- Receipts at all three Standing mutation sites (`tick.ts` release + drift; `actions.ts` publicity); founding landmark; `filmReleased` / `theatricalRunCompleted` rows; `standing.ts` gains `STANDING_FORMULA_VERSION` + `releaseStandingDrivers` (no formula change).
- Save V17: `validateSaveV17` (strip-and-delegate to V16 + strict root validation), `convertV16ToV17` (recording begins at the migration week; nothing reconstructed), `migrateToV17`, downgrade guards, `makeSave → V17`; adapter/bridge chains; `CURRENT_ACCEPTED_SAVE_VERSION = 17`.
- Test cutover of live-version pins (the same mechanical cutover every prior bump made); reload-continuity twins now compare the V16 projection + the forward history boundary (`tests/_p08HistoryTwins.ts`) because a migrated world's history lawfully differs from a native one.
- Focused proof `tests/p08a-w0-studio-history.test.ts`: 13/13.
- Full floor at `15113a02`: 364 files / 4939 passed / 5 skipped; core+ui+bridge tsc clean. Pushed; local == upstream.
- 6,240-week growth measurement (`scripts/measure-p08-history-growth.mts`, disclosed $50B solvency-floor fixture, NOT a product claim): save 1,928,459 B, history 573,279 B, 952 rows (routine capped at 52 unfolded + 119 folded summaries), timeline 781, avg tick 0.296 ms — bounded and explained by the 52-week fold.

### C2 — 2026-09-05 — P08A Wave 2 (projection 16 on the wire) — COMPLETE at TS `90a3635e175013d2b3833c389bdebe985e1babf2` × Unity `685f113e480ee18ea242ad8a341e7710523f840f`
- NEW `bridge/history.ts` `historyProjection(state)`: Standing channels + receipts with fact-derived reason lines (releaseResult / publicity / awarenessDrift / settled fold), non-routine timeline, every durable P07 film record (`historyRecorded`, `resultAvailable`), captured people only, `notRecordedNotice` for saves whose recording began after founding.
- `PROJECTION_VERSION 15 → 16`, canonical `$id …projection-16`, schema id `sha256:85a6d125960dce49b4775f842d7b56d7360c81cef3638cd819057c79c99f0236`; projection-15 id appended to `SUPPORTED_PRIOR_PROTOCOL_4_SCHEMA_IDS` (schema-bump law); lazy `history()` fact in the snapshot build context.
- Generated contract regenerated (`generated/unity/StudioBridgeDtos.Generated.cs` sha256 `8aad2079…`, git blob `223564d1…`) and landed byte-identical in the Unity WIP with normalize partials + `StudioHistoryTestFixtures.EmptyHistoryProjection()` wired into the three existing fixture builders.
- Floors: TS 365 files / 4944 passed / 5 skipped, tsc + bridge tsc clean; Unity EditMode 784/784. CF-09 `verify:bridge-contract-consumer --verify-only` PASS (report `cf09-w2-report.json`, both blobs `223564d1…`).
- Campaign branches and `main` unchanged; Owner profile untouched; no acceptance claimed.

### C3 — 2026-09-05 — P08 CORE TECHNICAL CHECKPOINT (sealed) — TS `8a23cb3b` (product) × Unity `64dab80e`
- W3 (Unity `04267dd8`): `StudioHistoryWorkspace` + contracts + retained context + USS; host route `OpenStudioHistory`, Film Result detour peels back to History, one-layer Esc; Administration card = the world-first route ([OPEN STUDIO HISTORY] + a three-channel Standing row; no engine intent; pinned test updated to say exactly that); 22 EditMode tests → 806/806.
- W4 (TS `8a23cb3b`): 8 deterministic oracle fixtures (`ui/e2e/p08-visual-oracle-v1`, generator `scripts/gen-p08-visual-oracle-fixtures.mts`, three refusals recorded in its comments) + `scripts/p08-real-profile-journey.mts` (35/35, baseline read-only, sha unchanged). Final TS floor at the product tip: 365 files / 4944 passed / 5 skipped; typecheck, typecheck:bridge, contract checks clean.
- W4 (Unity `9f64006` → `fded233` → `64dab80`): oracle runner P08 scenarios + history/selection steps; `Tools/p08-*` oracle + HID harnesses; oracle run 1 caught two visual defects (IMGUI card over the workspace; long lines clipping — root cause the shared `VisualElement { flex-shrink: 0 }` guard pinning the detail pane wide), both fixed and pinned; HID run 2 caught the People-pane presence/body contradiction, fixed in `64dab80` (wording + reason, pinned). EditMode 808/808 at `64dab80`.
- Visual Oracle canonical run 5 on exe `043ae03e…` (Unity `64dab80` clean): 8/8, 120 machine assertions, 0 failed, 0 mutations, 19 captures; run 4 identical on the prior sealed exe; runs 1–3 preserved and indexed in the checkpoint.
- Growth re-measured from the product tip (the C1 figures above came from an uncommitted W1 state — kept as written there, corrected in the checkpoint §5): 6,240 weeks → save 2,039,441 B; history 624,990 B; 999 rows; 52 unfolded routine at every checkpoint; 118 folded; timeline 829; 0.281 ms/tick.
- Hostile review (fresh reviewer, read-only): first verdict REJECT of the checkpoint AS CLAIMED — code passed every law tested; blocking items were the HID gate standing as a failed run, the wrong oracle run cited, and non-reproducing growth figures — all three resolved in the checkpoint (§5/§9); 12 non-blocking findings dispositioned (§9), none changed under the seal.
- Real HID: run 1 FAILED (session at the login window at launch; Save/Load half passed once unlocked; preserved); run 2 on exe `32262dbf…` 43/44 (one harness false negative) and the presence/body catch; run 3 on the sealed exe `043ae03e…`: pending the next ≥10-minute idle window (the machine is in active human use; the harness seizes the pointer) — result appended as a C3 addendum when it lands.
- Campaign branches, TS `main`, and the Owner profile verified unchanged. No P08 Owner acceptance is claimed. Non-blocking backlog for the next change windows is enumerated in the checkpoint §9.

#### C3 addendum — 2026-09-05 09:28Z — HID run 3 on the sealed exe
- Real HID run 3 (exe `043ae03e…`, Unity `64dab80e`, driver from `64dab80e`): 43/44 steps green — the whole P08 route with real input (Administration click → card → OPEN STUDIO HISTORY → STANDING drivers → TIMELINE filter/milestone → FILMS exact row → OPEN FILM RESULT detour → Esc back to the same film → PEOPLE exact person → Locate disabled with the §6.6 reason → BACK → Save → Load → arrow-key pan → route reopens with the same film row). The single failed step is the driver's framing self-report (harness ordering; product steps all green) — same as run 2. Evidence copied into the candidate `proof/hid/run3-43of44-sealed-exe/`.


### C4 — 2026-09-05 — P09A Wave 0/1 (TypeScript) — COMPLETE at `71a879be7359b6d446b5c68406144f22f6a9ad8f`
- P09 §15A solvency preflight (`scripts/p09-solvency-preflight.mts`, engine arithmetic, cheapest legal minimum roster, no subsidy): VERDICT SOLVENT (modelled floor $6,713,185; payroll $24,440/wk; overhead $24,000/wk). The prototype envelope ($20M start; office $1.5M / 14 w / $5.5K / +2) is therefore in force as PROVISIONAL WIP tuning (docket/correction 5.x), not permanent tuning authority.
- W0: `foundingRegime` root on GameStateV18/SaveFileV18 (written once, never inferred; V17 downgrade of a bare-lot save refused); `BARE_LOT_PROPERTY` (28×26, Gate+Administration landmarks only, own parcel ids, no `expansion`); property-driven founding-facility law; bare-lot activation mints no facility/set/capacity; `generateWorld/newGame(seed,{regime})`; `PROJECT_STUDIO_NEW_GAME_REGIME` (fresh runtime only); test pins cut over to 18; `tests/p09a-w0-founding-regime.test.ts` R1–R9 incl. the HARD-STOP first-film gate.
- W1: `quotePlacement` → `StudioPlacementQuoteSnapshot` on projection 17 (schema `sha256:c9dad9f3d8bb94445db1a5425d90db3f9894da9354f47a07992ff96261cfc399`; prior projection-16 id `85a6d125…` appended to the runtime-checkpoint acceptance list); `bridge/placement.ts` (draft→engine, quote snapshot, §26.1 headlines); illegal preview = accepted `ok:false` with ordered rejections + primary + per-cell verdicts, id never registered; legal quote = the one digest-bound `placeFacility` intent, committed once, retired by any accepted command; `tests/bridge-p09a-w1-placement-quote.test.ts` R1–R7 (full-grid sweep proves the wire is a verbatim read of `queryPlacement`).
- Design §10.3 founding-phase catalogue law implemented in TypeScript (`foundingPhaseOf`, regime-derived `foundingOffice` requirement, allowance 1 for the office while it rises, `neededNow`); catalogue rows publish `unmet` / `atInstanceLimit` / `neededNow`; `property.regime` on the wire.
- CURRENT OPS CORRECTIONS RECORDED (not silent): (a) design §29.3 lists `construction.parcels: []` for a bare lot; the frozen V12 construction invariants require exactly one registry row `{ id:'expansion', projectId:null }`, so the bare lot keeps that frozen bookkeeping row while its PROPERTY carries no `expansion` parcel and no Annex ground — the legacy Annex shortcut is not offered there (`legacyAnnexOffered`, read model never throws). (b) The placed-facility reservation-evidence invariant compared a stage's completion week to the GREENLIGHT week; a picture greenlit while its stage rose is legitimate and now holds from `bindings.heldSinceWeek`; the post capability persists no acquisition week and is NOT judged — deferred-not-dropped item P09-DEF-01 (persist `acquiredWeek` on FacilityReservation in a future save version). (c) The engine's own HARD-STOP gate on this seed: release week 37, cash floor $7,888,869, final cash $9,778,380 at week 42 (later than the §15A model's week 25 because the plant is legal only after the office is operational and the real pipeline is longer than the model) — still solvent, still on the studio's own money.
- Floor at `71a879be`: 367 files / 4966 passed / 5 skipped; core+ui+bridge tsc clean; contract + fixtures verified. Pushed; local == `hspector-github`. Campaign branches, `main`, and the Owner profile untouched; no acceptance claimed.

### C5 — 2026-09-05 — P09 CORE TECHNICAL CHECKPOINT (sealed pending two gates) — TS `fee206fa` (product) × Unity `a8f4c1aa`
- W1b/W1c (TS `33a362d`, `364079f`, `81ee766`): roads and the founding regime on the wire, property-driven lot bodies, the bare-lot `no-capacity` first-film beat (next kind `build`); the bare lot re-authored to the accepted world's 42×27 grid measured from the Unity scene (Gate (21,26) 3×1, Administration (8,18) 3×3, three road rectangles, four owned parcels) — the 28×26 figure in C4 is superseded.
- W2/W2b (Unity `2e7cea6`, `4afd6e8`): projection 17 contract synced byte-identical; placement quote family, property regime, catalogue unmet facts.
- W3a/W3b/W3c (Unity `5357301`, `e5da469`, `d936830`): gate-anchored `StudioLotGridMap`; N generic sites/bodies on exact footprints with wire-owned status; Build mode (parcel chooser → catalogue → world ghost preview with arrow nudges → BUILD, one-layer Esc ladder, BUILD chip + `B`, Administration `OPEN BUILD` + capacity blocker); the completed placed office opens the real Development & Casting workflow (alias resolution at `StudioLocateAction.Locate`); authored Annex art reserved for the Annex.
- W4 (TS `bfe79dc`/`71ef492`; Unity `a909d97`/`437210d`/`ac60ca0`): 11 → 12 oracle fixtures and scenarios (`p09-*`), viewport-parameterised harness `Tools/p09-run-visual-oracle.sh`, real-HID harness `Tools/p09-run-hid-build.sh` + `Tools/p09-proof-build.mjs`; oracle-driven corrections kept in the checkpoint §6 (shader fallback in the packaged player, wire-owned status text, BUILDING outranks the allowance lock, Annex-art gating).
- W5 (TS `fee206fa`; Unity `d3a22868`): `quoteSetCommission` → `StudioSetCommissionQuoteSnapshot` (`bridge/setCommission.ts`; refusal = accepted `ok:false`, legal = the one digest-bound `commissionSet` intent), `lot.setCatalog` on the wire, the Sets route on the Build dock (stage picker → Set catalogue → live quote strip → COMMISSION), fixtures s11 (stage ready, no Set) and s12 (endowed build). The whole bare-lot first film is now driven through the bridge alone (`tests/bridge-p09a-w5-bare-lot-first-film.test.ts`: released week 37, 46 commands, cash floor $8,864,638, final $9,374,658, ledger reconciles, no fixture adjustments). CURRENT OPS CORRECTION recorded (checkpoint §3.1.3): Set commission pulled forward from P09-R3 into core because REQ-025/041 cannot be met on a bare lot without it; repair/strike remain R3, not built.
- W5b (Unity `a8f4c1aa`): the Sets oracle waits for the authority's answer and asserts the legal House Set quote (run 6 had captured "Asking the studio…" before the answer — checkpoint §6.13).
- Gates at the pair: TS 370 files / 4979 passed / 5 skipped, tsc + bridge tsc + contract checks clean; EditMode 847/847; packaged build clean (exe `3a40670f…`); Visual Oracle run 7 on the sealed exe 12/12, 137 assertions, 0 failed, 0 mutations, 26 captures (`p09-office-rising` player exit 255 after a complete 14/14 sidecar; re-run exit 0 14/14, both kept); viewports 1280×800 / 1720×1046 / 1440×900 native fullscreen × 4 scenarios 12/12, 222 assertions, 0 failed; CF-09 PASS. Core HARD-STOP gate (week 37, floor $7,888,869) and the bridge-only journey both release a film on the studio's own money.
- **C5 UPDATE — real-HID Build flow = RECORDED FAILURE (not passed); P10A W0 landed.** Four real-input runs on the sealed player (checkpoint §5 Real-HID row): runs 1–2 failed because a background-launched player never took focus (fixed: launcher/driver use System Events, tools `95a3eaf`); run 3 hit "Studio data is incompatible" because the working-tree engine bundle had moved to projection 18 for P10 (fixed: the launcher pins the engine bundle to the manifest's `engineBundleSha256` and takes `P09_ENGINE_BUNDLE`, tools `6e57eb3`); run 4 on the sealed player × sealed bundle passed 24 steps (connects, Administration framed, durable checkpoint read, final Save lands a **V18 bare-lot** save) but failed the Build flow on **P09-DEF-02** (§6.17): the accepted scene's `StudioSnapshotProvider` loads an endowed fixture at boot and the W6 bare-lot art law does not stand it down after the live bare-lot snapshot in the real-input path — the Oracle does not exhibit it on the same exe (run 8, clean bare-lot capture). It does not reach the wire, the save, or the automated bridge-only first-film journey (the order's HARD STOP, which passes). Deferred to a Unity rebuild wave (W7); recorded, not claimed. The P09 candidate is rebuilt at `~/Desktop/P09-Core-Technical-Candidate-fee206f-f7cf20a/` with all four HID runs' evidence under `evidence/hid/`.
- **P10A W0 (TypeScript) COMPLETE at `e8d178d`** on `wip/p08-p10-autonomous-stack-01-ts`: the player-safe PEOPLE projection (projection 18). NEW `bridge/people.ts` (`peopleProjection`) owns profiles / roster / grouped attention per the §26A visibility table (`docs/engineering/P10-INFORMATION-VISIBILITY-TABLE.md`, committed before the contract change); one profile per stable Talent record, joined by talentId, estimates carry their wording and OVR its discipline, career rows are frozen `TalentCareerEvent` facts with honest legacy provenance, nothing hidden crosses (schema-negative tests). The Talent section rides its OWN additive bundle section so the lot-snapshot partition invariant holds; projection 17→18, prior id `18de162d` appended to the acceptance list, `generated/unity` regenerated (schema `ea5d645f`). `tests/bridge-p10a-w0-people-projection.test.ts` R1–R10; full TS floor green (371 files / 4990 passed / 5 skipped). Not yet on the Unity side (W1+ = inspector/Profile/Roster).

### C6 — 2026-09-05 — P09-DEF-02 FIXED → P09 9/9 TECHNICAL KEEP; P10A W0 intact
- Current Ops ruling: continue the campaign, fix P09-DEF-02 (the sole red gate: real-HID Build flow) in the ORDINARY product boot path before further P10 Unity work; keep P10A W0; bind compatible build pairs explicitly.
- DEF-02 was three faults behind one symptom (checkpoint §5.3 attempt ladder): (1) the background-launched player never took focus (System Events frontmost, tools `95a3eaf`); (2) the working-tree engine bundle had drifted to projection 18 for P10A W0, which the projection-17 player rejects as "Studio data is incompatible" — the launcher, manifest generator and oracle now bind an explicit manifest-verified `P09_ENGINE_BUNDLE` (`6e57eb3`/W7); (3) the ordinary boot rendered the endowed studio on a bare-lot save — a STALE `f0b32599` build; the clean W6 rebuild renders the correct bare lot (`SnapshotsApplied=1, bare=1, phantom=0`), now guarded by a COMPLETE facility-art health signal + EditMode regression guards A–F (W7 `8f30d0ef`); (4) the Administration card was fixed at 490px and overflowed [OPEN BUILD] off-window (card now sizes for the construction content; the driver also uses the on-screen HUD BUILD chip); (5) the framing metric judged a comfort margin a large edge-projected building can never reach (now "the click lands inside the visible content", matching `clickNamed`).
- The reconciliation distinguishes gameplay facilities (stood down when the property does not carry them) from permanent landmarks (Gate, Administration) and harmless decoration (roads, water tower); it never indiscriminately hides the scene. Regression guards A–F prove endowed→bare hides, endowed keeps, late/null-frame no-op then binds, reconnect cannot resurrect, save/load stable, and F (not hardcoded) catches a stale bound building, a stale unbound hangar, or a missing reconciler group by name.
- **Real-HID Build flow: PASS 30/30, 0 failures, on the EXACT committed candidate** (exe `d41c0d4b…`, Unity `8f30d0ef` `dirty:false`, sealed engine `5185e3a2…`, TS `fee206fa` product) on a genuine 604s owner-idle window (`Evidence/P09-Journey/hid-20260905T150855Z`): ordinary launch → correct bare lot → Administration framed and clicked → OPEN BUILD → BUILD HERE Gate Court West → office $1,500,000 / Week 14 → BUILD commits (placed-1 stands, cash $20,000,000 → $18,500,000) → Esc ladder → select "Development & Casting Office" → Save V18 bare-lot, `placements=1`. The four prior failed runs are preserved.
- Clean committed W7 re-verification: EditMode 849/849; Oracle 12/12 (140 assertions, 0 failed, 26 captures); viewport matrix 1280×800 / 1720×1046 / 1440×900-fullscreen × 4 = 12/12 (0 failed); generated contract byte-identical to the P09-sealed contract (`ba9e5874…`) so CF-09 remains PASS by exact identity. Candidate rebuilt at `~/Desktop/P09-Core-Technical-Candidate-fee206f-8f30d0e/`.
- P09 promoted **8/9 → 9/9 TECHNICAL KEEP**. Campaign branches, `main`, and the Owner profile verified unchanged; no P09 Owner acceptance claimed. Next: P10 Wave 1; then re-verify P09 live-boot/Build on the final projection-18 combined candidate (addendum item 4).

### C7 — 2026-09-05 — P10A W1b + W2 + W3 (person route) — KEEP, proven in the packaged player
- **Projection-18 build-pair sealed:** engine bundle `3b9e343209706589…` (built from committed TS, projection 18, schema `ea5d645f…`) at `$SCRATCH/p10-sealed/engine.mjs`; P10 player built from committed Unity clean source (`dirty:false`) with the manifest binding that exact engine sha (build-pair integrity, order §10). Harness `Tools/p10-run-visual-oracle.sh` (port 43384) requires `P10_ENGINE_BUNDLE` and verifies it against the manifest before any run; the 80H-worktree defaults in `p04a1-build-manifest.sh` are overridden explicitly every build.
- **W1b (Unity `a78fa40`→`ce739e2`):** `StudioBridgePresentation.TryGetPersonTalentId` (reverse of the Locate join; reads `personSlots` only, so decorative/ambient crew never get a profile). `StudioPersonInspectorCard` (compact world-entry card over a selected authoritative person, phrased by the W1a pure contracts; OPEN PROFILE + canLocate-gated Locate; selection is inert). `StudioProfileWorkspace` + context (read-only retained Profile; all-active rebind; a dropped person empties honestly; Back restores origin). Host: the person route wired end to end. EditMode +6.
- **W2 (Unity `b87b1fa`):** `StudioPersonProfileContracts` + the full Profile — CRAFT (perceived discipline OVR, proven/capable-but-unproven, potential BAND always marked a scouted estimate, credited counts, the OVR definition), SPECIALTIES, COMMERCIAL (Star Power distinct + work ethic + its one effect), CONTRACT (terms/guarantee/termination/renewal or labelled market rate), CAREER (filmography; each available result deep-links via `OpenReleaseResultFromProfile`, whose Back peels back to the exact Profile; honest empty/partial lines). No hidden truth. EditMode +6.
- **W3 (Unity `043870e`→`9a1c23e`):** `StudioRosterContracts` (pure filter/search/sort; one search box over name+specialty; deterministic sorts breaking ties by name then exact id) + `StudioRosterWorkspace` + context (two-line readable rows, filter chips, sort/horizon/attention cycles, single-scroll, compact card mode, a selection footer with OPEN PROFILE + Locate, retained filter/sort/search/selection; a filtered-out or dropped selection reported truthfully, never silently swapped). Entry: the People strip's COMPANY header ("ROSTER ▸", rect exposed for HID). OPEN PROFILE from a row opens the exact person with origin Roster so Back peels back to the list. EditMode +5.
- **Proof:** EditMode **871/871**. Oracle `p10-person-inspector` **22/22** on the fixture `s1-p10-person-inspector` (projection-18 re-envelope of the accepted endowed state; the sealed engine restores it verbatim, session preserved) at 1440x900, 1920x1080, and 1200x700 — the inspector card, the full Profile (on-lot with Locate; off-lot with Locate disabled + reason), and the Roster (60 people, selection footer) all captured and visually inspected. Evidence `Evidence/P10-Oracle*/p10-person-inspector-*`.
- **Findings (for the §8 UX pass / hostile review):** F1 presence seating vs canLocate (only the scenery-zone craft person seats a body on the endowed lot at week 2; casting/writers-zone roster people report canLocate but seat no body — pre-existing presence-seating behaviour, not P10's); F2 Profile header/LOCATE underlap the top time bar at short viewport heights. See `$SCRATCH/p10/FINDINGS.md`.
- Campaign branches (`campaign/living-lot-ts` `2753e18b`, `campaign/living-lot-client` `c4c65db4`) and the Owner profile verified unchanged; no P10 Owner acceptance claimed.


### C8 — 2026-09-05 — P10A person route COMPLETE, hostile-reviewed, combined candidate READY
- **W1b/W2/W3 + cross-stack person↔history**: all built, EditMode **873/873**, CF-09 PASS, Visual Oracle `p10-person-inspector` 22/22 (three viewports) + `p10-person-history` 9/9, captured and visually inspected in the packaged player.
- **P09 re-verify (addendum item 4)**: 6/6 P09 scenarios / 69 assertions on the projection-18 P10 player — boot/Build/construction/migration/reconnect/Save-Load.
- **Fresh independent cross-stack hostile review**: 26 raw → 15 confirmed / 11 refuted, no BLOCKERs. 4 MAJOR (attention-tier casing killing "Attention first" sort + all attention colours; the Roster OPEN PROFILE footer pushed off-screen; the Roster footer Locate a dead control; retained scroll unimplemented) + the actionable MINOR/NOTE all remedied at Unity `68d20c1` × TS `af8c19c`, and **returned to the same reviewer → 5/5 RESOLVED**. Verbatim + disposition in `P10-HOSTILE-REVIEW-CORE.md`; checkpoint in `P10-TECHNICAL-CHECKPOINT.md`; morning report `P10-MORNING-DELIVERABLE.md`.
- **Combined candidate**: `~/Desktop/P10-Person-Route-Candidate-af8c19c-68d20c1/` (player `1212fe63`, engine `3b9e3432` == the final committed TS engine byte-verified, Unity `68d20c1` dirty=false, generated contract `6c26f13e`), with saves, docs, evidence, proof, and a `playtest.sh` launcher. Save V18 / protocol 4 / projection 18.
- **Extension dispositions**: R4 (career/history) implemented; R1 grouped attention via the Roster; R1 contract actions DEPENDENCY-BLOCKED (no `renewContract`/`releaseContract`/`terminateContract` producer); R2/R3 reuse the Casting authority. Star badge Owner-blocked.
- Campaign branches (`2753e18b`, `c4c65db4`) and `main` re-verified unchanged; the P09 CORE candidate untouched; no P10 Owner acceptance claimed. No force push / rebase / Golden tag / campaign promotion.
