# World-First Publicity Campaign V1 Evidence

Status: **IMPLEMENTED, VALIDATED, AND RETAINED ON THE AUTONOMOUS MARATHON BRANCH**

Date: 2026-08-14

Branch: `operation-hollywood-autonomous-marathon`

Contract authority: `f83f27f0d42b16ea177b35f1a39e23236faf6831`

Implementation authority: `f2f2e22fe292772ef8aba4f97f42ea38855cbf96`

## Keep ruling

World-First Publicity Campaign V1 passes its bounded Keep gate.

The accepted D-17B publicity decision is no longer a globally exposed Lot shortcut or a
Dashboard-only purchase. The ordinary player can select the physical Administration & Publicity
building, inspect all three exact current Engine offers, purchase one once, see the fresh cash,
Audience Awareness, ledger, and cooldown result, receive a bounded photocall acknowledgement, open
Dashboard details when needed, and return to the same Administration context on the Studio Lot.

The retained loop is:

```text
PHYSICAL ADMINISTRATION & PUBLICITY
→ INSPECT WHISPER / PUSH / BLITZ
→ BUY ONE THROUGH THE EXISTING APP / ENGINE OWNER
→ REPAINT EXACT SUCCESSOR TRUTH IN THE SAME MOUNTED LOT
→ ACKNOWLEDGE AT THE PHYSICAL PHOTOCALL
→ OPEN DASHBOARD DETAILS IF NEEDED
→ RETURN TO FRESH ADMINISTRATION CONTEXT
```

The Studio Lot remains the primary game surface. Dashboard remains the complete supporting
management surface and exact parity owner; it was neither deleted nor weakened.

## Authority and exact accounting

The authority chain is singular:

```text
publicityDecision(GameState)
→ field-exact StudioLotSnapshot offer projection
→ pure publicityCampaignContext validation
→ selected physical/semantic Administration context
→ App-owned runPublicity(tier)
→ exact accepted clocks receipt
→ fresh GameState and StudioLotSnapshot
```

React selects, validates, dispatches, focuses, and presents. Phaser supplies canonical place
identity and a presentation-only flash. Neither layer computes price, lift, affordability,
cooldown, accounting, action legality, or a business recommendation.

The governed Week-30 native SaveFileV11 prestate has `$11,160,898.29` cash,
`36.46928821615352` Audience Awareness, null publicity clocks, and 62 ledger rows. Independent
identical prestates produce these exact Engine successors:

| Tier | Cost | Exact lift | Successor cash | Successor Awareness | Purchased clock |
| --- | ---: | ---: | ---: | ---: | --- |
| Whisper | `$1,200,000` | `1.18351790258165` | `9,960,898.29` | `37.65280611873517` | `whisper = 30` |
| Push | `$3,600,000` | `1.9725298376360831` | `7,560,898.289999999` | `38.4418180537896` | `push = 30` |
| Blitz | `$8,000,000` | `2.7615417726905167` | `3,160,898.289999999` | `39.230829988844036` | `blitz = 30` |

Each result sets `publicity.lastUsedWeek = 30`, changes only the selected tier clock, and appends
exactly one Week-30 ledger row:

```text
kind   == publicity
amount == -exact tier cost
note   == publicity: <tier>
productionId is absent
```

After purchase the shared six-week cooldown repaints all three offers immediately. The purchased
tier retains its longer tier cooldown where applicable. Week, RNG, productions, people, contracts,
tasks, reservations, facilities, construction, theatrical runs, career state, and every unrelated
standing channel remain byte-identical.

## Physical identity and world continuity

The accepted runtime district uniquely owns the `publicity` / `admin` place, its exact selection
polygon, `entry`, `photocall`, and `queue` anchors, and the matching `publicity` activity. Exact
validation rejects absent, malformed, duplicate, contradictory, non-finite, or wrong-identity
records. A rejected physical identity creates no hotspot, outline, or flash; the native semantic
Administration control retains the complete action path and states honestly that the physical
office is unavailable.

Physical polygon activation and the semantic Administration companion enter one context. They
clear incompatible person, production, scenery, Annex, and generic-place contexts. The selected
building, office framing, exact offers, and action buttons remain in the living Lot. Selecting any
other world context closes the campaign context rather than leaving an invisible action armed.

The explicit **Open Dashboard details** handoff carries one narrow, consume-on-return
`publicity-campaign` intent for the same loaded studio. Return rebuilds current offers, restores the
Administration context, refocuses its stable heading, and never replays the prior flash. Ordinary
Dashboard entry, unrelated navigation, unmount, or studio replacement clears that intent.

## Exact-once, stale truth, and failure behavior

Immediately before dispatch the Lot revalidates the latest snapshot, exact selected context, all
own fields of the selected offer, availability, and the synchronous pending latch. Double-click,
rapid cross-tier activation, repeated Enter/Space, held key, stale affordability, stale lift,
changed tier identity, lost context, and slow parent replacement cannot create a second owner call.

An accepted callback is trusted only when its tier and accepted week match and the fresh successor
replaces the previously available offer with authoritative cooldown truth. A mismatched receipt
fails closed. Rejection preserves context, changes no authoritative byte, announces one named
error, and restores focus inside the campaign region.

Renderer import rejection, manifest failure, delayed import, reduced motion, hidden-tab resume,
WebGL context loss, scene recreation, and the person-profile modal preserve the semantic action
surface and suspend the world correctly. Final lifecycle hardening explicitly closes failures that
arrive while Phaser is still loading or creating: the registered generation is hidden,
input-disabled, selection/flash-cleared, and paused again at the create boundary so it cannot
survive as a running orphan. A validated later generation clears the host failure state; stale
ready/failure signals cannot revive an older generation.

## Visual and accessibility result

The canonical photocall anchor reuses the existing local flash rectangle. Stage 7 shooting paint
has separate graphics ownership, so a campaign cannot erase, borrow, or inherit production truth.
The cue is bounded, suppressed under reduced motion, cleared on recreation/shutdown, and never
plays for rejection, stale truth, cooldown load, or Dashboard purchase.

All three offers are native buttons with exact cost, immediate lift, price per point, tier/global
cooldown, available week, and named reason. The panel makes no `best`, `safe`, `profitable`, or
rescue claim. Pointer, Enter, and Space are exact-once; focus, status, reasons, 44×44 targets,
forced colors, grayscale, 200% page zoom, and semantic renderer-failure parity are covered.

At the governed wide layout the campaign panel occupies the right management lane without covering
the selected office, Stage 7 production truth, or named-people inspector. Compact layouts retain a
scrollable reachable panel rather than replacing the Lot.

## Deterministic fixture and non-change proof

Evidence reuses the accepted native fixture without regenerating it:

```text
ui/e2e/world-first-scenery-load-in-v1/
  week-30-nights-of-watchtower-stage-7-blocked.save.json
SHA-256 7534518e4db3970bb4ca988b0b0fa78975f5053ee67fd42377f69b80ebe711dc
```

The implementation changed no `src/core`, GameState, SaveFileV11 schema/migration, tuning,
district manifest, exporter, or authored/generated art. Frozen source/runtime/art identities remain:

| Artifact | SHA-256 |
| --- | --- |
| authored source manifest | `5af27d7a97739724990ec08ef1fe5888eeb069bccc8e81b351271c2268914889` |
| accepted runtime manifest | `23bf9451b3a62099ed724b0f3a4082839b8246862ac5e61f3b72233dc5430d92` |
| concept plate | `a6279762ab7db8b5a16ea71627e63ae918b74c2db8e0874731c34c09947e7c34` |
| exporter | `405cb831d7d0cf4daaefe2259b0b27160157cbd65cb86c056814059c37b488fe` |
| district base | `a920e651d9b48b81dbcd6b2923f3c558326692705ea7b6a8fcb854055d009978` |
| truck occluder | `c559cce2a06bb35da5aeda6fd237ed2a2abfdcc1f85954b898fe84cd6da6c4a1` |
| camera dolly | `c190166b8e8b7efa5c4c37e30f59b0c6684aff15deaabb774b9e55e3f22c2dc5` |
| gate foreground | `c91b9b831efd9a58ad6047013f300228663dc5ddd410d94188436327c054179a` |

## Performance evidence

The retained scene stays at exactly 34 display objects, 15 dynamic actors, 11,096,896 decoded
texture bytes, and one renderer draw after an exact fresh 240-frame post-panel window. No image,
atlas frame, actor, route, simulation timer, or second draw loop was added.

An accelerated in-app Chromium run at 1920×1080 measured the exact fresh window at **179 average
FPS, 145 FPS 1%-low, 6.9 ms p99, 11.3 ms worst, and 0.01 ms average update**, clearing every frozen
Hollywood wall-clock gate. The telemetry seam proves the campaign panel first commits a new window
with fewer than 240 samples and only then accepts exactly 240, preventing a pre-panel warm window
from being relabelled as campaign evidence.

The default headless Playwright host was compositor-contended and did not clear the absolute
wall-clock gate; the committed baseline missed it under the same contention. This is retained as
host evidence, not hidden or reclassified as a product pass. The absolute GPU assertion remains
unchanged and explicitly opt-in with `PROJECT_STUDIO_PERFORMANCE_EVIDENCE=1`; default Playwright
reports it **skipped**. The always-on structural 240-frame test still proves exact object, actor,
texture, draw, and fresh-window budgets.

## Final verification

- root and UI TypeScript: **passed**;
- complete repository suite: **168/168 files, 2,171/2,171 tests passed**;
- exact affected Scene/View/Screen lifecycle set: **3/3 files, 131/131 tests passed**;
- governed D-16/D-17 harness: **10/10 files, 176/176 tests passed**;
- focused publicity Chromium: **5 passed, 1 explicit GPU-evidence skip**;
- complete Lot Chromium: **20/20 tests passed**;
- production build: **passed, 136 modules transformed**;
- `git diff --check`: **passed**;
- exact fixture, manifest, exporter, and art hashes: **passed**;
- protected branch refs: **unchanged**; and
- independent strict authority and visual/harness review: **no unresolved P1–P3 findings**.

The repository's existing large-chunk advisory remains visible and is not reclassified as a
publicity defect. A new full economy corpus is not proportional to this UI/world-boundary slice.

## Governing boundary

This milestone makes an accepted action spatial, continuous, and understandable. It does not
create a Publicity Office facility, a queue, named publicist assignment, person travel, campaign
duration, film-specific publicity, workload, stress, relationship, pathfinding, or new economy law.

The governing status remains exactly:

> **D-17B ACCEPTED — BOUNDED REPAIR, MACROECONOMY RESIDUALS REMAIN OPEN**

Cash runaway, top-studio economic immortality, the week-208 synchronized roster wall, P5
dominance, world-led variance, cheap-film purpose, premium-film purpose, remaining menu breadth,
and formal G12 timing remain open. No financing, loan, bailout, restructuring, failure ladder,
arbitrary cash sink, or macroeconomic certification follows.
