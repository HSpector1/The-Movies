# World-First Scenery Load-In V1 Evidence

Status: **IMPLEMENTED, VALIDATED, AND RETAINED ON THE AUTONOMOUS MARATHON BRANCH**

Date: 2026-08-14

Branch: `operation-hollywood-autonomous-marathon`

Contract authority: `b03bb101a6880552c299f94240d65c04ace98682`

Implementation authority: `3a667e05986579d6474878f238d1c6dbc4a7e362`

## Keep ruling

World-First Scenery Load-In V1 passes its bounded Keep gate.

The Stage 7 scenery blockage is no longer legible only as inspector text. The ordinary player can
click the physical Scenery & Service yard/marker, inspect the exact blocked film and destination,
invoke the existing Engine-owned clear command once, receive the fresh shooting decision while a
bounded world acknowledgement begins, then either schedule while it is moving—cancelling that
motion—or let the acknowledgement reach Soundstage 7 before continuing on the same mounted lot.

The retained authority chain is singular:

```text
EXACT ENGINE PRODUCTION OPERATION
→ PURE STUDIO-LOT SELECTOR
→ PHYSICAL SERVICE-YARD IDENTITY OR NATIVE SEMANTIC CONTROL
→ LATEST REACT HOST REVALIDATION
→ EXISTING APP / ADAPTER COMMAND OWNER
→ FRESH GAMESTATE + SNAPSHOT
→ SAME MOUNTED WORLD ACKNOWLEDGEMENT
```

The renderer never clears a blocker, schedules a take, advances time, changes a reservation,
spends money, writes a save, or consumes RNG. Its 1,200 ms normal-motion scenery sweep is
presentation-only evidence of an already accepted `blocked → ready` transition.

## Exact bounded behavior

| Boundary | Retained result |
| --- | --- |
| Shared selector | One pure selector accepts only managed mode, Engine stage authority, exactly one Stage 7 Shooting operation, exact `scenery-load-in` blocked/ready task shape, and a field-consistent projected command. Every other state returns `null`. |
| Physical identity | Only the frozen canonical `service-yard` / `post` runtime place and canonical `stage-7` / `stage-a` destination can embody the interaction. Stage 12 cannot borrow it. |
| Player context | Physical marker/polygon, production blocker, and native **Scenery & Service** control enter the same exact film → service source → Soundstage 7 destination context without navigation. |
| Existing command owner | Clear and Schedule reuse the current `onProductionCommand` / `runProductionCommand` path. The lot passes the field-exact projected command and adds no new action. |
| Same-lot continuity | Accepted Clear retains context, paints exact ready truth, focuses Schedule immediately, and does not wait for motion. Accepted Schedule exits service context, selects Stage 7, and focuses existing scheduled truth. |
| Input safety | Pointer, `Enter`, and `Space` entry/activation are exact-once. Synchronous pending, double-click-detail, held-key-generation, and latest-command guards prevent Clear from becoming a second Schedule consent. |
| Rejection truth | Stale Clear and Schedule each preserve blocked/ready context, exact error, focus, and all authoritative bytes. Repeated identical errors replace the live-region child so both attempts remain observable. |
| Failure truth | Renderer construction rejection, delayed import, absent canonical service identity, and malformed/duplicate canonical records retain the complete semantic path without inventing an outline. |
| Direct paint | Blocked and ready SaveFileV11 inputs paint their exact static truth. Direct ready reload does not replay delivery ceremony. |
| Presentation | One draw-only Graphics object depicts a flat/crate marker and a bounded source-to-destination acknowledgement. It adds no texture, actor, route record, second draw, or manifest change. |
| Compatibility | Generic Scenery & Service place inspection remains intact when no exact production selector exists. Existing Stage 7 and Production Board commands remain byte-identical owners. |

## Frozen source and runtime provenance

The implementation treats the accepted runtime district as consumption-only. It does not edit or
regenerate the authored source, runtime manifest, district plate, exporter, or PNG layers.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| authored source manifest | 4,052 | `5af27d7a97739724990ec08ef1fe5888eeb069bccc8e81b351271c2268914889` |
| accepted runtime manifest | 6,761 | `23bf9451b3a62099ed724b0f3a4082839b8246862ac5e61f3b72233dc5430d92` |
| clean temporary exporter manifest | 5,787 | `56057b6bdfd7d2e7f31b6ae839121d3996f83fc75e1eb2e5955939d862846ab7` |
| source plate | 2,804,229 | `a6279762ab7db8b5a16ea71627e63ae918b74c2db8e0874731c34c09947e7c34` |
| exporter | 3,218 | `405cb831d7d0cf4daaefe2259b0b27160157cbd65cb86c056814059c37b488fe` |
| district base | 2,750,802 | `a920e651d9b48b81dbcd6b2923f3c558326692705ea7b6a8fcb854055d009978` |
| truck occluder | 245,021 | `c559cce2a06bb35da5aeda6fd237ed2a2abfdcc1f85954b898fe84cd6da6c4a1` |
| camera-dolly occluder | 106,780 | `c190166b8e8b7efa5c4c37e30f59b0c6684aff15deaabb774b9e55e3f22c2dc5` |
| gate foreground | 852,735 | `c91b9b831efd9a58ad6047013f300228663dc5ddd410d94188436327c054179a` |

Automated provenance replay runs the clean exporter in a temporary directory and proves its
deliberately divergent 5,787-byte manifest. Separate assertions freeze exact unique runtime
identity, anchors, affordances, and polygons for Scenery & Service, Stage 7, and the accepted Annex.
This closes the source/runtime hazard without laundering it: a blind exporter run would still erase
or move accepted runtime authority and remains forbidden.

## Deterministic SaveFileV11 authority

The committed generator constructs one real Week-30 *Nights of Watchtower* production through
public Engine actions. It writes only blocked, ready, and its manifest; unassigned remains an
in-memory proof and scheduled must match the already frozen Live Week Advance fixture.

| Anchor | Bytes | SHA-256 | Repository posture |
| --- | ---: | --- | --- |
| unassigned | 227,430 | `2b352e3ef1be5ab9d5e0ba0abfbeb6c0a717f5334afe7d6a60ff5a81cef584ca` | proved in memory only |
| blocked | 227,479 | `7534518e4db3970bb4ca988b0b0fa78975f5053ee67fd42377f69b80ebe711dc` | committed native SaveFileV11 fixture |
| ready | 227,425 | `6760b72739608e930da84726067685c515d87817cb3793f9d9d37fa9f2063f92` | committed native SaveFileV11 fixture |
| scheduled | 227,429 | `e922f9b7e957388bed7c7674be8c17596245823200e478371dc7ff970458f46b` | byte-identical to frozen Live Week Advance fixture |
| fixture manifest | 16,618 | `eee8dd476f3117dccc3c48985797e4a248827fb19b5a15d5c8a125b6c04780e4` | committed |

Replay reported every committed output `unchanged`, native V11 import with `converted === false`,
and byte-identical export after import. Selection consumes no action. Clear and Schedule preserve
week, cash, RNG, ledger, countdown, people, contracts, construction, publicity, releases, and
reservations except for the already-authorized task transition itself.

One frozen blocked prestate produces the same ready successor through all four supported command
surfaces:

1. Scenery & Service same-lot context;
2. generic Stage 7 inspector;
3. Production Board / application handler; and
4. direct adapter call.

All four results have byte-identical SaveFileV11, RNG, and ledger output.

## Automated authority proof

Combined final proof closes all 32 frozen contract families. The focused authority matrix passes
**7/7 files and 143/143 tests**, including:

- exact selector acceptance, hostile states, Stage 12 isolation, reversed operation order, and
  duplicate Stage 7;
- physical, visible-marker, host, semantic, generic-place, and `StudioLotView` event parity;
- exact context fields and person/place/Annex replacement;
- pointer/Enter/Space entry and command parity, rapid duplicate, real double-click, and held Enter;
- accepted/rejected Clear and Schedule with byte, RNG, ledger, focus, announcement, and selection
  proof;
- blocked/ready direct paint, normal sweep, reduced motion, cancellation, and absent/mismatched
  successor behavior;
- delayed renderer import after accepted Clear;
- renderer construction rejection and absent/malformed canonical manifest continuation;
- source/runtime/exporter/art provenance and deterministic fixture replay; and
- one draw-only scenery object with no texture request.

The complete repository passes **163/163 test files and 2,028/2,028 tests**. The governed D-16/D-17
harness passes **10/10 files and 176/176 tests**.

## Ordinary-player live acceptance

### Physical blocked → ready → scheduled loop

The exact blocked Week-30 fixture was restored and the Hollywood lot opened with:

- Week **30**;
- cash **$11,160,898**;
- *Nights of Watchtower* at Soundstage 7 + Scenery Shop;
- Estelle Delgado as Director;
- **5 weeks left**;
- exact `blocked` task and **Production hold** status; and
- a visible Scenery & Service world marker inside the authored service-yard polygon.

Clicking the marker at the physical yard opened the exact dedicated context and focused
`Clear scenery load-in` without navigation. One activation produced immediate Engine-ready truth,
retained the same canvas, camera, lot, week, cash, film, people, countdown, reservations, RNG, and
ledger, and focused the fresh Schedule command. The normal 1,200 ms flat/crate sweep began only
after that accepted snapshot.

Schedule was activated before the sweep elapsed. It remained legal, selected existing Stage 7
scheduled truth, cancelled the dedicated service projection and stale arrival feedback, retained
the original canvas, and stayed at the same URL. Waiting beyond 1,200 ms produced no contradictory
“ready to schedule” activity.

The in-app browser controller focused the native semantic control but did not synthesize its
physical Enter activation; that interaction is not relabelled as a live keyboard pass. The real
Chromium Playwright journey separately uses native Enter for semantic entry and native Space for
Clear in the renderer-failure path, while component-level native pointer/Enter/Space parity proves
exact one-dispatch semantics.

### Reload, reduced motion, and failure

- Direct blocked and ready fixture imports paint exact truth without transition replay.
- Ready page reload retains Delivered + Schedule and emits no false success ceremony.
- Reduced motion resolves the accepted delivery acknowledgement immediately, leaves Schedule
  available, and completes the scheduled transition with identical authority.
- An isolated renderer-module rejection exposes the complete visible semantic companion. Native
  Enter enters the exact context, native Space clears it, pointer Schedule completes it, and no
  canvas is required.
- With canonical manifest selection absent or malformed, the scene refuses an outline while the
  host retains the complete semantic Engine path.
- With no exact scenery authority, the physical service-yard polygon continues to select the
  ordinary **Scenery & Service** place rather than disappearing or impersonating Post Production.

### Live defects found and repaired

Audit/play found and closed these player-visible or authority-sensitive defects before retention:

1. the new draw marker could win Phaser top-only input over a named person; it is now draw-only and
   the existing depth-1 canonical polygon owns place input;
2. the initial marker sat outside the strongest authored service polygon; blocked and ready forms
   now sit inside it while the exact `[390,584] → [500,500]` source/destination line remains frozen;
3. generic service selection lost its outline when React cleared dedicated context; generic and
   dedicated cleanup ownership are now distinct;
4. a real double-click or held Enter could repaint Clear → Schedule inside one gesture; native
   click detail, pending, held-key, latest-selector, and field-exact command guards now prevent it;
5. repeated identical rejections could be silent to assistive technology; one stable live-region
   owner now replaces a keyed child per event;
6. scheduled/relocated/absent replacement could briefly retain the prior production or indefinitely
   retain “ready to schedule” feedback; external invalidation now fails empty and fresh scheduled
   truth clears matching activity on both dedicated and generic Stage 7 paths;
7. duplicate/malformed runtime places could throw or accept a canonical neighbour; validation is
   total, nonthrowing, and requires exact unique service and destination records;
8. recovery plus the 640 px authored stage pushed activity below short viewports; feedback is now
   viewport-visible and the 960 px layout keeps production, context, command, and toast disjoint;
9. renderer failure placed its explanation under the scenery context; it now occupies the open
   left lane with zero overlap; and
10. semantic place zones now expose the expected pointer cursor while routed named people retain
    input priority.

## Responsive, zoom, performance, and diagnostics

| View | Result |
| --- | --- |
| 1280×720 | Exact physical/semantic context and command reachable; no overlap, clipping, or horizontal page overflow. |
| 1366×768 | Same governed pass. |
| 1440×900 | Same governed pass. |
| 1920×1080 | Same pass plus 240-frame post-warm-up measurement. |
| 1536×864, 125%-equivalent | Same governed pass. |
| 1024×768 | Additional compact-management pass. |
| 960×540 | Additional stress pass; production, context, command, toast, and renderer-fallback explanation remain visible and pairwise disjoint. |
| Actual maximum world zoom | Twelve real wheel-up inputs reached the camera clamp; the physical marker/polygon, exact context, command, pan, and living world remain usable. |

The final 1920×1080 240-frame window measured:

| Metric | Result | Gate |
| --- | ---: | ---: |
| average FPS | **180** | ≥50 |
| 1%-low FPS | **143** | ≥30 |
| p99 / worst frame | **7 ms / 7 ms** | ≤33.4 ms |
| average update | **0.02 ms** | evidence |
| display objects | **34** | ≤34 |
| dynamic actors | **15** | 15 |
| renderer draws | **1** | 1 |
| decoded texture bytes | **11,096,896** | exact |

The player-facing debug readout rounds decoded texture cost to 10.6 MB and the encoded role atlas
to 281 KB. The exact byte total remains frozen by artifact/atlas authority. The scenery slice adds
one Graphics object and zero texture bytes, actors, routes, or draws.

The full governed browser matrix recorded zero product errors, product warnings, failed requests,
unreachable controls, overlap, clipping, or horizontal page overflow. Chromium's known headless GL
`ReadPixels` driver warning is classified as test-infrastructure output rather than concealed or
reported as a product warning. Evidence screenshots remain under ignored
`out/gate-d1-evidence/final/scenery-load-in-*.png`.

## Final verification

| Gate | Result |
| --- | --- |
| Focused scenery authority matrix | **PASS — 7/7 files, 143/143 tests** |
| Complete repository suite | **PASS — 163/163 files, 2,028/2,028 tests** |
| Governed D-16/D-17 harness | **PASS — 10/10 files, 176/176 tests** |
| Full Lot Playwright suite | **PASS — 20/20 tests** |
| Root and UI TypeScript | **PASS** |
| Production build | **PASS — 133 modules transformed** |
| Deterministic fixture replay | **PASS — every output unchanged** |
| Clean temporary exporter replay | **PASS — 5,787 bytes, frozen SHA-256** |
| `git diff --check` | **PASS** |
| Protected refs | **PASS — main and accepted D-17B unchanged** |
| Independent visual review | **PASS — no remaining P1–P3** |
| Independent strict review | **PASS — no remaining P1–P3** |

The production build retains the existing large-chunk advisory. The Studio Lot remains lazily
loaded; the advisory is not a scenery interaction correctness failure.

## Authority neutrality and open boundaries

This is an intended world interaction and presentation change. It adds no core behavior,
GameState/SaveFile field, migration, adapter command, App route, production clock, facility,
reservation, accounting, economy rule, random draw, manifest record, authored image, worker,
inventory, queue, pathfinding result, or authoritative truck.

The static truck occluder remains static authored composition. The existing director route remains
director travel. The flat/crate sweep is diagrammatic acknowledgement, not a claim that an
authoritative truck, scenery crew, inventory stack, loading bay, ETA, or travel duration exists.

The governing status remains exactly:

> **D-17B ACCEPTED — BOUNDED REPAIR, MACROECONOMY RESIDUALS REMAIN OPEN**

Cash runaway, top-studio economic immortality, the week-208 synchronized roster wall, P5
dominance, world-led variance, cheap-film purpose, premium-film purpose, remaining menu breadth,
and formal G12 timing remain open. No financing, loan, bailout, restructuring, failure ladder,
arbitrary cash sink, or broad economic certification was introduced.
