# Asset Inventory (contract §3)

Generated by `tools/inventory.mjs` (deterministic) → `manifests/source-assets.json`, with
runtime detail in `manifests/runtime-assets.json`. This document summarises; the JSON is the
machine-readable source of truth.

## Archives (SHA-256 preserved, contract §2)

Originals copied (never moved) into `sources/original-archives/`; the Downloads originals
are unmodified and verified byte-identical (`tools/hash-archives.mjs`).

| Archive | Bytes | SHA-256 (head) |
|---|---:|---|
| `Downtown City MegaKit[Standard].zip` | 234,753,092 | `5b1a945576d54cdb…` |
| `Universal Animation Library[Standard].zip` | 15,904,933 | `cc73fc4e495b8295…` |
| `FBX-20260727T232629Z-1-001.zip` | 1,467,539 | `410695b84e3cc87b…` |
| `wintersets.zip` | 17,487,044 | `167ecc34f454d2f4…` |

Full hashes: `manifests/source-archives.json`.

## Pack totals

| Pack | Files | Breakdown | Models | Provenance |
|---|---:|---|---:|---|
| **downtown-city-megakit** | 691 | 306 fbx · 153 gltf + 153 bin · 71 png · 3 hdr · 4 jpg · 1 txt | 459 (306 FBX + 153 glTF) | CC0 |
| **fbx-props** | 123 | 123 fbx | 123 | LICENSE-UNCLEAR |
| **universal-animation-library** | 9 | 2 glb · 2 fbx · 3 png · 2 txt | 4 | CC0 |
| **wintersets** | 209 | 180 dds · 5 msh · 5 cam · 5 ini · 5 inf · 5 jpg · 3 txt · 1 db | 5 (.msh, unreadable) | DO-NOT-USE |

## Downtown City MegaKit (CC0, Quaternius — free subset)

- **Formats:** ships the same models three ways — `Exports/FBX (Unity)`, `Exports/FBX
  (Unreal Engine)`, and `Exports/glTF (Godot)` (`.gltf` + `.bin`), plus a shared `Textures/`
  set (PBR: BaseColor / Normal / ORM packed) and 3 interior HDRs.
- **Scale (validated):** 1 unit = 1 metre. Measured local AABBs: `Door_1` = 1.0 × 2.2 × 0.26 m,
  `Sidewalk_Straight_3m` = 3.0 × 0.15 × 3.0 m, `Building_Small_1` = 12.5 × 17.0 × 14.5 m,
  `Building_Large_2` = 20.6 × 28.0 × 16.6 m. Matches the frozen 3D spike's 1.8 m convention
  with no rescale.
- **Content classes (153 glTF):** 3 assembled buildings, 38 brick modules, 15 cornices,
  14 street segments, 13 roof pieces, 11 trims, 8 sidewalks, 6 doors + frames, metal-facade
  modules, 5 street props (`Prop_ACUnit/Bollard/Drain/ManholeCover/Planter`), 17 road decals.
- **Curated runtime subset (31, §5):** 1 hero + 2 more assembled buildings, 12 modular parts,
  4 roads, 3 sidewalks, 5 street props, 2 extra door/window variants → optimized to
  self-contained GLB.

## Universal Animation Library (CC0, Quaternius)

- **Two libraries, identical clip set:** `UAL1_Standard.glb` (root motion **off**, in-place)
  and `UAL1_Standard_RM.glb` (root motion **baked**). Each: 1 skinned mesh, 1 skin,
  2 materials, ~13,744 triangles, **43 animation clips**, character height **1.829 m**
  (≈ the 1.8 m reference — no rescale).
- **Required roles (§5) → mapped clips:** idle→`Idle_Loop` (2.5s), walk→`Walk_Loop` (1.33s),
  talking idle→`Idle_Talking_Loop` (2.93s), seated→`Sitting_Idle_Loop` (1.67s),
  interaction/pickup→`PickUp_Table` (0.83s) (+`Interact` 2.0s), repair/maintenance→
  `Fixing_Kneeling` (5.2s). All six exist in the CC0 set; no substitution needed.
- **Other clips of interest:** Jog/Sprint/Crouch/Swim locomotion, Sitting_Enter/Exit,
  Driving_Loop, Dance_Loop, Push_Loop, plus combat/spell clips (not relevant to Studio).

## FBX interior props (LICENSE-UNCLEAR)

- **Format:** binary FBX v7400 (FBX 2014/2015). Small (15–35 KB each), untextured
  (0 textures in the archive), single-mesh.
- **Content (123):** Light×20, Bathroom×11, Kitchen×10, Door×10, Window×8, Houseplant×8,
  Couch×8, Shelf×6, Trashcan×5, Drawer×5, Column×5, Chair×4, Table×3, NightStand×3, Carpet×3,
  Bed×3, plus Bookshelf, Fireplace, Stool, Plate×3, and cutlery.
- **Curated runtime subset (12, §5):** Couch_Medium1, Chair_1, Table_RoundSmall, Bookshelf,
  Shelf_1, Houseplant_3, Light_Floor1, Light_Desk, Carpet_1, NightStand_1, Fireplace,
  Bathroom_Mirror1. Loaded directly via FBXLoader; auto-normalized (cm→m if needed, grounded,
  centred). **No license file present — treat as prototype-only.**

## wintersets (DO-NOT-USE — legacy Lionhead *The Movies* mod)

- **5 sets:** winterfield, winterforest, wintershack, winterstreet, wintersuburb. Each has a
  `.msh` mesh, a `.cam` camera path, a human-readable `.ini` set-definition, and a binary
  `.inf`. 180 `.dds` textures (DXT1 / DXT3, 16×256 up to 1024×1024). Packaged with `zapinfo/`
  (mod-installer metadata) and a German readme.
- **Meshes are not loadable** (proprietary Lionhead `.msh`); textures and configs ARE
  legible. Full analysis: `docs/WINTERSETS-ARCHAEOLOGY.md`.

## Runtime catalog (contract §9, `manifests/runtime-assets.json`)

- **45 runtime assets**, all present, **45/45 validate** (`manifests/validation.json`).
- **33 reusable (CC0)** · **12 prototype-only (LICENSE-UNCLEAR)** · wintersets excluded.
- **43 animation clips** exposed; 6 required roles mapped.
