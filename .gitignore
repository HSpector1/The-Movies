node_modules/
dist/
coverage/
out/
tools/
test-results/
playwright-report/
ui/e2e/fixtures/
.last-run.json
*.log
.tmp/3d-asset-audit.json
.DS_Store

# Design-archive material awaiting relocation by the owner — NOT FOR BUILD,
# must never enter git history. Remove these lines once the files leave the repo.
design-spec.md
README.md
1-career-talent-market.md
2-historical-talent.md
3-acquisition.md
4-filmmaker-pitches.md

# never track node_modules (build artifact / worktree symlink)
node_modules
__pycache__/

# M5 Owner playtest saves — MINTED, never committed. `npx vite-node
# tests/_m5PlaytestSave.ts` rebuilds them byte-identically from the seed, so the
# script is the artifact worth keeping and a 238KB blob is not.
m5-playtest-pressure.json
m5-playtest-wrap.json
