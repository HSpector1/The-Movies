# Independent R05 stress-fixture disposition

**Recommend closing the current R05 numeric performance disposition with a measured reference-cost rebaseline after the fixed real-store sampler completes. Keep the 12-rival/48-project/4,000-film/50-archive family explicitly UNMEASURED / FUTURE STRESS.** This is not an all-original-budgets PASS and does not claim that nine rivals prove twelve.

The retained register PERF-001 describes the horizon as binding and fixture bounds as provisional, with the churn fixture assigned to P15B. SIM-015 calls twelve a stress case rather than Owner law. PERF-009 permits a deliberate rebaseline with evidence. R05 §1 transfers only the initial nine-rival rollout; §6 excludes corporate closure/churn; §7 preserves substantive gates and provisional targets. These clauses support a qualified current R05 disposition using its legal production domain, with the original numbers and unsupported future domain kept visible.

At TS39c6401, the production validator requires exactly player plus nine reserved identities (`hollywoodValidation.ts:72`), fixed manifest/name/arrival facts, and at most two active screenplay records per rival (`:259`). The real policy admits one production per business at a time (`hollywoodTick.ts:145`). A screenplay and its `inProduction` stage are the same project, not two projects. Week8788 contains nine businesses,17 active screenplay records (15 ready and2 in production),2,900 films and17,352 career events. The sampler's raw `activeProjects:2` field actually counts productions and is qualified in the independent review.

The smallest honest future synthetic adapter would live entirely in a benchmark-only envelope with an explicit schema distinct from native v19. It would namespace complete, internally linked owner/person/contract/project/film/receipt graphs; declare twelve active owners, four unique projects each, at least4,000 durable films and50 archived identities; validate references and resources with a benchmark validator; then invoke unchanged shared production/operations and indexing primitives. Native import must continue rejecting that envelope. Fixture setup, injected work and refill/reset policy must be outside measured spans and explicitly reported. Repeating a fixed workload6,240 times would measure6,240 operations, not6,240 consecutive authoritative campaign weeks. Three independent shards cannot prove same-world cohort or shared-resource stress. Four project stages cannot be counted as four projects. No such adapter or stress run was built here.

A production-like twelve-owner authoritative endurance/import claim would require an approved expanded registry/active-cap/archive contract and matching strict validator, which this R05 does not authorize. Adding rivals, relaxing native validation, fabricating closures, topping up businesses or deleting history solely to obtain benchmark counts would be an invalid substitute.

The rebaseline does not waive the hard50ms synchronous engine-main storage blocker, permanent-history retention, strict reference/migration correctness, bounded pages, at-most-once per-revision cache obligations, or algorithm laws. Existing current-source nine-rival6,240-week replay, worker placement, native evidence and independent correctness reviews remain distinct required evidence. Owner play enjoyment remains a separate decision.
