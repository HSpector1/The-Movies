# Bounded prepared checkpoint correction

Private patch `prepared-checkpoint-final.patch` (SHA256 `8687d29d6188ebbc6633cfc2a8f5d8785036e457e9ea92f294545bc5358e1404`) applies cleanly to the runtime-equivalent TS `be09bcc5a767b45efb519250049c63b99928232c`. Exact original runtime base is `39c6401738f0f29700632acc4c03aeb6e3b5bc93`. `patch-manifest-final.json` records every original and final file hash. No shared source, engine, native-input or private save was changed by this task.

The correction reuses canonical bytes already built for strict checkpoint capacity validation. A stack-local dispatch handoff passes that successful preparation through the coordinator/campaign transaction. It never retains a preparation between calls. The final state reference, session/revision, saved bytes and every private journal field must match before a handoff returns; the committed journal receives a copy. Public checkpoint/journal encoders and reimports remain strict, mutable returned objects never become trusted inputs, caller and session limits both apply, and failed commits/rollover retain the existing authority semantics. The private raw four-field journal candidate is fully validated by the complete checkpoint boundary before commit.

Final verification: **130/130 tests across seven focused files**, including **21 new adversarial cases** authored independently by `/root/recovered_storage_review/package_runner_review`; TypeScript checking passes. Test relocation into `tests/bridge-runtime-checkpoint-prepared-reuse.test.ts` changed only imports and makes the cases part of default discovery. `focused-final-04.log` and `typecheck-final-05.log` retain exact output. The independent reviewer also reviews the frozen implementation; parent root separately reviewed it.

| Complete small-fixture operation | Original strict checkpoint passes | Final strict passes | Original exact checkpoint serializations | Final exact serializations |
| --- | ---: | ---: | ---: | ---: |
| Campaign Save | 9 | 3 | 13 | 3 |
| Ordinary Save | 3 | 1 | 4 | 1 |

`counter-baseline-report-02.json` and `counter-patched-report-04.json` are positive instrumented evidence. Each binds 106 imports; final imported source hashes were checked unchanged after the run. Complete canonical checkpoint bytes, including LF, were hashed for grouping: final campaign multiplicities are `[1,2]`, with one final accepted checkpoint and two strict export/import passes for the detached pre-Save prospective fork. Ordinary Save's final checkpoint is materialized once. Core imports decrease 16→8 / 8→3 and exports 13→5 / 7→2 for campaign/ordinary Save respectively. All retry and world-digest checks pass. These counters are diagnostic operation counts on a small deterministic fixture, **not latency budgets, RSS measurements, native acceptance or full-world performance approval**. Root's current-source emitted/real-store probes remain necessary for those conclusions.

Two early harness attempts are explicitly disqualified: `prepared-tests.log` is an interrupted mistakenly auto-discovered broad workspace run with missing unrelated private-copy UI fixtures; its owned processes were stopped. `counter-{baseline,patched}-report.json` are uninstrumented harness non-evidence because `/tmp` versus `/private/tmp` caused empty counters and source manifests, regardless of their original `completed:true` fields. Corrected positive intermediate counter02/03 evidence remains preserved and is not presented as final source verification. No performance or permanent-history waiver is claimed.

Final independent source attestation: `independent-final-review.json`, SHA256 `920365549c6a6476b5a2bbe12a9bbfed6d049a68a4dfcbc6e327828989f69c49`. The frozen patch is ready for root integration and source-bound performance retest.
