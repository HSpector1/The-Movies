# Integrated runtime-worker correctness review

Disposition: **No new correctness blocker found in this bounded read-only review.** This supports the worker placement remedy; it does not certify the 50 ms target, original latency budgets, late-world memory, Unity/native acceptance, or hook activation.

Reviewed the integrated TS worktree at `/Users/bruce/The Movies - P12A Living Hollywood TS`, HEAD `ff020706382cf9c6cf82c8709c5a377d1179a1cb`, plus the 11-file worker patch SHA-256 `1ea4c80ea71be9c486392bbf7397ddfa839e5ef9fc24c24a75443a0f4c477023`. The reviewer authored the eight worker tests and their fixture, but did not implement the runtime/build code. Implementation assessment is independent of that code's author; test authorship is not represented as independent test design. No source edits, tests, builds, engines, or native input were performed for this review.

`applicability.json` records every reviewed source/artifact hash. All 11 patch files match the patch manifest. All five projection-29 overlay files match. All **104 compiled worker sources** match current files. The older private authority manifest differs for `bridge/people.ts`, `bridge/schema/bridge-schema.ts`, and `bridge/runtime-checkpoint.ts`; these are the recorded projection-29 overlay, not unexplained changes. Session, coordinator, campaign library, codec, and production store remain unchanged by the worker patch.

| Boundary | Assessment |
| --- | --- |
| Authority and ordering | `worker-entry.ts:31` opens the sole store and creates one coordinator. Main has no session/state replica. Existing serialized coordinator reads, mutations, publication-after-commit, poison behavior, and immutable receipt journal remain authoritative. Handlers can await independently; all state access goes through the same coordinator. |
| Exact body and retry | `server.ts:121` collects bounded original bytes; SHA-256 accompanies a copied transferable request. `worker-adapter.ts:16` rechecks that digest before parsing. Command/Save/load use the coordinator's exact `responseJson`; worker transfers UTF-8 storage and main wraps that buffer without parsing or re-canonicalizing the Save. Timeout rejects the caller with unresolved outcome and causes no automatic retry. |
| HTTP security | Capability/Host/Origin/content-type/Expect checks precede body ingestion and worker access. Loopback binding and original header/body limits remain. Capability is removed from the main environment before startup; the worker receives an explicit restricted environment, with test mode preserved for the existing guarded test gate. |
| Failure and shutdown | Worker failure poisons pending/later calls once and asks the engine process to stop; no replacement worker is created. Startup cleanup precedes fatal publication. Close stops admission, drains accepted handlers, then closes coordinator/store before acknowledging exit. Store ownership remains bound to the engine PID/incarnation; process-level restart preserves existing stale-lock rules. |
| Bounded work | 2 MB request, 192 MiB transferred-response, 128 unfinished requests, 60 s waiter/startup/worker-close bounds remain explicit. Timed-out work still occupies capacity until actual response/exit. The supervisor's existing 5 s termination grace can supersede the worker's longer drain deadline; interruption is unresolved, not a receipt. |
| Main-thread placement | The emitted engine graph has 10 transport/schema/bootstrap inputs and excludes session, coordinator, store, and simulation authority. Snapshot construction, serialization, strict checkpoint handling, compression, and synchronous production-store work execute in the worker. Main still performs transport, small request hashing/copying, contract encoding, and response-buffer handoff; measured responsiveness and native parsing remain separate. |
| Build/source binding | Packaged engine embeds the exact worker hash and checks it against manifest and actual bytes before startup. Worker readiness asserts its own protocol/schema/projection constants. Dev bootstrap builds a fresh private worker, hashes compiler-consumed sources, and checks them again before launch. Package audit includes the worker graph and rejects non-builtin runtime dependencies. |

Existing integrated evidence was read, not rerun: `/tmp/p12a-worker-integrated-functional-01.log` records **5 files / 44 PASS**, covering worker, coordinator, campaign, HTTP security, and process restart; typecheck/build/audit logs also complete successfully. Earlier private 20-pass evidence is not used to certify the later projection-29 bytes.

The reviewed emitted engine hash is `ccd58600f6c43edc5abf814fdddf63d673fd69a98277ad61c80ea4e72e58d104`; worker hash is `b93597b747fbbf678b3024966e8c0559bf50c97534edc3dbb5aea75f755f0aa9`. Applicability requires these source/build bytes or a fresh review of subsequent changes. Worker placement alone does not shorten the unchanged durable transaction or eliminate the retained response's memory cost.
